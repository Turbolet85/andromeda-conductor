<!-- Cross-version residuals. One line per entry, master-route grammar minus the link tail:
  {origin-marker} · open · {text} (target: {version-hint|next})
Two writers total: /andromeda-wrap-session route-resolve APPENDS `open` entries; /andromeda-route
Phase A decides dispositions and Phase 6 flips `open → absorbed:{cap-id} | re-carried:{version} |
dropped ({why})`. Nothing else writes here. -->

2026-08-09-interpretation-correctness-posture · open · Interpretation-correctness real-model leg — under deterministic L4 (`ANDROMEDA_PULSE_L4_DETERMINISTIC=true`) a canned `L4Output` replaces the inference, so every live leg exercises the plumbing and never the interpretation: "Conductor green" does not mean Pulse's interpretation is trustworthy. Prove the real thing — deterministic mode OFF, a known root cause injected, the top hypothesis asserted to identify it (the P-033 path Pulse's own memo calls the single most important test in all of Pulse). Needs real per-check read-back extraction (v2-09) AND a non-deterministic live leg that can never be a CI gate, which is why it is not an 0.2.0 entry. Covers the diagnostic-quality cluster P-031/P-033/P-034/P-044, pinned meanwhile in `conductor_core::UNBACKED_AUTO` and asserted by `check_scenario_backing`. (target: 0.3.0)

2026-08-31-p-075-assert-round · open · `incident_events`-through-MCP — Pulse's corpus persists incident lifecycle events (created/resolved) in `incident_events`, the ONE corpus table whose content is not L4-authored and therefore the only remaining candidate for payload-level read-back fidelity under deterministic L4. Measured 2026-09-01 at Pulse HEAD `83d4060`: ZERO references in `crates/mcp-server` — written by `crates/triage/src/incident/persistence.rs::save_incident_event`, read only corpus-side (`crates/corpus/src/disposition.rs` + a `contract.rs` test helper) — so no MCP tool surfaces it at any width and Conductor cannot reach it. This is a PULSE-side capability gap, not Conductor work: surfacing it needs a new or extended MCP tool on the SUT, which the operator routed to Pulse's 0.4.0 incubator where it pairs with the big-brother MCP direction. Until then payload fidelity stays unattainable and Conductor asserts runtime-STATE fidelity instead (`mark_incident_resolved` + active-set membership, live-proven that chunk). (target: pulse-0.4.0 — an EXTERNAL repo's entry, recorded here so Conductor's next route intake can re-check it rather than re-derive it)

2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate · open · `v2-24` routine a11y specs — deferred in 0.2.0 because the runtime-major hypothesis was FALSIFIED, not because the work was descoped: CI run 34280136892 installed Evergreen 152.0.4191.66 and `DevToolsActivePort` still never appeared within 90s with the app alive and three webview children resident, so the hosted `windows-2025` image opens no remote-debugging endpoint under EITHER runtime major. Re-open on three unmeasured candidates, cheapest first: a hosted-image policy on remote debugging (an Edge/WebView2 policy registry read is the one-line probe) · a session or service-account property of the runner · msedgedriver 151 against a 152 runtime as a cause of the DRIVER failing to start, an axis never tested (skew was retired only as a cause of the ENDPOINT failing to open). Also carry the upstream finding: the routine arm's own red has moved to tauri-driver never listening on `:4444` (four refused POSTs inside ~1.5 s against a 6.9 s driver bind — a startup race, and present in runs 34162118841 / 34251573399 / 34256490781 at runtime 151, so it predates both the upgrade and any skew). Self-hosted runners on a public repo are not an option. (target: next)

2026-09-10-live-pulse-in-lane-scenario-round · open · `constellation-severity-live-wiring` grades on TWO structurally-dead assertions — one owned item, both measured live this chunk, neither blocking the release. (1) Its `[[expected]]` `CountAtLeast >= 1` reads `evidence_count`, summed from `retrieve_telemetry_slice.span_refs`, which Pulse writes EMPTY at every production incident (`pulse-app/src/inference_runtime.rs:871` sets `EvidenceRefs.span_ids: Vec::new()`; whole-repo enumeration finds no other non-test writer; `crates/mcp-server/src/tools.rs:430-439` maps `span_refs` 1:1 from it) — so `0 >= 1` is false in every world and the floor routes to `CalibrationRegion` regardless of the declared `Hard` (`conductor-verify/src/slo.rs:97-100`). The mechanism was already recorded at `architecture-amendments.md:403` (2026-08-21) as the ground on which FIVE severity-lifecycle scenarios retired their checks to declare-only; this scenario is an unswept member of that ruled class, and two siblings remain — `findings-counter-refresh.toml:49` (floor 3, its unmet-floor outcome already recorded accepted at `architecture.md:69`) and `pulse-run-contract.toml:58` (floor 1). Derivation, so the next reader does not land on seven: `grep -n 'kind = "CountAtLeast"' scenarios/*.toml` → exactly 3 live keys; a bare token grep hits 7 files because 4 mention it only in retirement comments. (2) Its declared `slo_tier = "<20s"` is UNATTAINABLE by construction — `latency_ms` is `read_back_observed_at − journal_emitted_at`, which spans the whole emission window, and this scenario's own phases sum to 30 s (`gap_ms` 3000+15000+12000 at `:29`/`:36`/`:44`); measured `latency_ms 30212` against `deadline_ms 20000`. Recorded, not established: defect (2) plausibly ORIGINATES in the obs-plan gloss this same wrap retired — a tier chosen believing the delta ≈ an MCP round-trip would be far too tight for a 30 s window. Disposition for the next version's route Phase A: take the three-scenario `CountAtLeast` class together rather than retiring this one alone, which would contradict an accepted sibling. Evidence: `conductor-0.2.0/chunks/2026-09-10-live-pulse-in-lane-scenario-round/evidence/live-round-verdict.md`. **Extended at `2026-09-10-release-build-and-bundle`** (operator direction at that wrap: extend this item to the full class, take the class together, no fix in this chunk). (3) A THIRD dead assertion of the same class and a different KIND: `cross-incident-recurrence` (P-036) declares `[[expected]]` `kind = "Contains"` / `class = "Hard"` / `expected = "Previously seen"` (`scenarios/cross-incident-recurrence.toml:42-45`), and it measured DEAD on its first-ever live drive as leg 3 of this chunk's SLO pass — the read-back succeeded and the run reached a verdict, but no producer anywhere emits that token; the scenario's own header records it as INFERRED (`:15`, "the spec uses prose"), an Epoch-8/10 calibration point that was never closed. The leg was SWAPPED to `investigate-actions-functional` on operator direction and the FAIL retained as committed evidence, so the release stands on three clean legs and this stays a routed finding, not a fix this chunk owes. Evidence: `conductor-0.2.0/chunks/2026-09-10-release-build-and-bundle/evidence/slo-verification-pass.md` + `evidence/leg3-2026-09-10T19-40-47-134.jsonl`. (4) Retiring that declaration is NOT a TOML-only edit: `crates/conductor-core/src/scenario.rs:1188` holds `cross_incident_recurrence_asserts_previously_seen_via_hard_contains`, a `#[test]` that reads the committed TOML and asserts exactly the `Contains` / `Hard` / `"Previously seen"` triple — it goes red the moment the declaration is retired, so the fix is a PAIRED edit and the scenario file alone is a false modify-set. (5) That test is invisible to the sweep the test rules prescribe for exactly this purpose, because it lives in an INLINE `#[cfg(test)]` module under `src/` while `.claude/rules/testing.md:54` prescribes `grep -rln "<scenario-name>" crates/**/tests`. Measured at this wrap: the prescribed form exits 1 with no output, while `grep -rln "cross-incident-recurrence" crates/` returns the file. hypothesis: that blind spot is also why this declaration was left standing when the surrounding family retired its checks — plausible, not measured. The rule entry was corrected in place at this wrap, so a later reader gets the wider sweep. (6) Corpus context, re-derived at this wrap and independently by the operator (36 scenarios parsed, summed `gap_ms` per scenario against the tier thresholds 5000 / 20000 / 90000 ms): **17 of 36 declare an `slo_tier` smaller than their own summed phase duration**, and they are THREE different situations that must not be collapsed into one defect — **9** declare the ceiling `<90s` and exceed it (the ratified posture per `matrix#v2-12`, not a defect); **2** are beyond EVERY tier while declaring a smaller one (`ack-cooldown` 370 000 ms at `<20s`, `severity-tier-autonomous` 120 000 ms at `<5s`); and **6** declare a tier that a larger EXISTING tier would hold — `constellation-severity-live-wiring` (30 000 ms at `<20s`) among them, which IS defect (2) of this item. So the tier half of this residual is one member of a 6-scenario class, and retiring it alone would leave five siblings inconsistent, exactly as retiring the `CountAtLeast` alone would. (target: next)
