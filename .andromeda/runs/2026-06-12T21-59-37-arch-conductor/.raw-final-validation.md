## Issues Found

- **[CONTRADICTION]** The two inserted timestamp-related passages assert mutually exclusive encodings for the same two named fields, `journal_emitted_at` and `read_back_observed_at`. The **Data model conventions** paragraph says they are "stored as the same integer-millisecond journal offsets the SLO math consumes, **not ISO strings**," while the **Timestamp formats** paragraph lists them among "in-payload instant fields [that] use colon-delimited RFC-3339 (`2026-06-12T21:59:37Z`)." A downstream skill reading the schema cannot tell whether these fields are integer-ms or RFC-3339 strings.
  Section: Conventions → "Data model conventions" ↔ Standard Contracts → "Timestamp formats"
  Severity: high

No other issues. Verified clean: no `report.md` stale artifact survives in the body; no specialist-content leakage; blocked-row NULL sentence, ManualCheck/KnownResidual triggers, Trust boundary bullet, and runs.db column types each internally consistent; Inherited Defaults is an accurate TL;DR.

## Patches

### Patch 1: Scope the Timestamp-formats RFC-3339 claim to the serialized representation so it stops contradicting the integer-ms stored-column declaration
**Reason:** [CONTRADICTION] — `journal_emitted_at`/`read_back_observed_at` are declared integer-millisecond stored columns ("not ISO strings") in the Data model conventions paragraph but RFC-3339 strings in the Timestamp formats paragraph.
**Old:**
```
In-payload instant fields (`checked_at`, `journal_emitted_at`, `read_back_observed_at`) use colon-delimited RFC-3339 (`2026-06-12T21:59:37Z`). The two encodings denote the same instant; only `run_id` is constrained to the hyphen form.
```
**New:**
```
In-payload instant fields use colon-delimited RFC-3339 (`2026-06-12T21:59:37Z`): `checked_at` in the readiness result, and the human-facing run-report serialization of `journal_emitted_at`/`read_back_observed_at` (whose `runs.db` columns remain the integer-millisecond journal offsets declared in the Data model conventions, not ISO strings). The encodings denote the same instant; only `run_id` is constrained to the hyphen form.
```

[APPLIED by orchestrator to architecture-draft.md]
