I have both files. Now I'll select the dimensions and generate the meta-prompt.

Selected dimensions:
- Mandatory: 1 (Internal Consistency), 2 (Decision Completeness), 3 (Conventions Specificity), 4 (Downstream Readiness), 5 (Complexity Calibration)
- Conditional: 8 (Cross-reference Integrity — high cross-ref surface), 9 (Specialist Content Boundary — Tauri UI surface), and a reframed 7 (Contract Completeness — framed around MCP-preflight and run-report contracts, not HTTP)
- Excluded: 6 (Infrastructure Realism — no Docker/K8s/multi-service)

Here is the meta-prompt.

---

# Conductor Architecture Iteration Meta-Prompt

You are an iteration agent improving the Conductor architecture document at `.andromeda/runs/2026-06-12T21-59-37-arch-conductor/architecture-draft.md`. Conductor is a headless-drivable Rust fault-injection/verification harness (a crate-per-seam Cargo workspace) with a thin Tauri 2 control panel, driving a live Pulse instance through 60 capabilities (P-001..P-060) on a deterministic seeded timeline.

## Output Protocol

Rules you MUST follow:

1. **Output = patches + changelog only.** Emit targeted patches (old text → new text) plus a changelog. Do NOT reproduce the full document.
2. **Patch format.** Each patch is:
   - `### Patch N: <short description>`
   - `**Old:**` followed by the *exact* current text from the document (verbatim, enough surrounding text to be unambiguous and uniquely locatable).
   - `**New:**` followed by the replacement text.
3. **Changelog.** After the patches, one line per change: `[Iteration N] [substantive|cosmetic] <description>`. `substantive` = changes a fact, resolves an ambiguity, adds/removes a constraint, fixes a real inconsistency. `cosmetic` = wording/formatting only.
4. **PROHIBITED:**
   - Full-document reproduction.
   - Restructuring sections without a stated reason.
   - Cosmetic changes labeled `substantive` (or vice versa).
   - Inventing technologies, versions, P-IDs, ports, env vars, MCP tool names, or crate names not grounded in the document.
   - Adding HTTP/REST/GraphQL surfaces, Docker/K8s, an ORM, a message broker, or native OS toasts — every one of these is an explicit non-goal or "N/A" in this document; "completing" them is a regression.
5. **If no issues found in a dimension:** write "No patches" for it. If the whole document needs nothing, output "No patches" and a single `[Iteration N] [cosmetic] reviewed, no changes` changelog line.

## Analysis Dimensions

### 1. Internal Consistency

- Do all 11 sections cite the same versions everywhere? Cross-check that `tokio 1.48.x current_thread`, `opentelemetry-proto 0.32.0`, `tonic 0.14.6` (+ `tonic-prost 0.14.6` / `prost 0.14`), `rmcp 1.7.0`, `rusqlite 0.38.0` (+ `libsqlite3-sys 0.38.0`, bundled SQLite 3.51.1), `garde 0.23.0`, `thiserror 2.0.18`, `anyhow 1.0.102`, and `Tauri 2 (bundler v2.10.x / 2.10.1)` are identical in Stack and Technologies, Established Decisions, Conventions, Inherited Defaults, and Infrastructure Patterns — flag any drift (e.g. a bare "tonic 0.14" vs "0.14.6", or the ratified `rusqlite 0.38.0` vs the brief's stale `0.31`/`SQLite ≥3.38` appearing un-superseded anywhere).
- Does the Verdict/ReportState wall read identically in every place it appears? Confirm `enum Verdict { Pass, Fail, CalibrationRegion }` and `enum ReportState { Pass, Fail, ManualCheck, KnownResidual, Blocked }` in the Design Philosophy bullet, Established Decisions error-handling entry, Conventions error-handling paragraph, and the Standard Contracts run-report envelope all list the exact same variants — flag if any list adds/drops a state (e.g. `KnownResidual` present in one place but missing in another) or if a state→trigger mapping ("MCP tool absent → Blocked", "transport refused → harness error", "wrong incident → Fail") conflicts between sections.
- Does Inherited Defaults faithfully summarize the body? Check each Inherited Defaults bullet (language/runtime, backend framework=None, database, interface style, validation, error handling, module boundaries, deployment, CI/CD, dev style) against its source section — flag any default that overstates or contradicts the detailed decision (e.g. interface style omitting the `blocked`-on-mismatch posture, or CI/CD not flagging that dynamic proof is a local operator gate).

### 2. Decision Completeness

- Does every `[Bracketed] Established Decision` carry a *rationale*, not just a pick? Specifically verify the load-bearing forks justify themselves: the `opentelemetry-proto` raw-types-over-`opentelemetry-otlp`-exporter decision ("single most consequential fork"), the `current_thread`-core-owned-under-Tauri choice, the `rmcp 1.7.0` vs hand-rolled-JSON-RPC/rust-mcp-sdk rejection, and the rusqlite-synchronous-off-the-async-runtime call — flag any that states a choice without the *why* or without naming the rejected alternative.
- Are there implicit decisions that should be promoted to explicit `[Bracketed]` entries? Probe: the hardware-profile tier thresholds (`<5s` / `<20s` / `<90s`) and the sample-count floors (50-sample latency / 10-span error-rate) appear in the Timing-Tolerance decision and the run-report `slo_tier` field — is the *binding of a scenario to a tier* a decided rule or left undefined? Is the `CONDUCTOR_SEED`-vs-CLI-flag precedence a stated decision? Flag implicit-but-load-bearing choices.
- Are there resolvable TBDs? The doc mentions "budget a small adjustment when mining Pulse's prior-art injectors that predate the [tonic-prost-build] split" — is anything left genuinely open (a version, a tool name, a contract field) that could be pinned now rather than deferred? Flag unstated-but-decidable gaps; do NOT invent values to fill them.

### 3. Conventions Specificity

- Are the data-model conventions for `runs.db` concrete enough to implement without a follow-up question? The doc says "primary index keyed by `run_id` (with `seed`/`scenario`)", "no synthetic UUID/serial PK", "timestamps stored as journal-relative values", "wall-clock stamps from `std::time::SystemTime`/`Instant` (never tokio's virtual clock)", "fingerprint arrays indexed via SQLite JSON1" — flag any column whose type/nullability/format is left implicit (e.g. is `latency_ms` an integer ms? is `slo_tier` a closed enum string `<5s`/`<20s`/`<90s`? are `p_ids`/`fingerprints` stored as JSON1 arrays of TEXT?).
- Are the naming laws unambiguous? Check that the P-ID scope law ("no scenario without a P-ID", `P-001..P-060`), the crate-name pattern (`conductor-<seam>`, kebab-case crate / snake_case module), and the on-disk artifact naming (kebab-case Markdown like `coverage-matrix.md`, per-run `<run_id>.jsonl` journals) are each stated as enforceable rules, not vibes — flag any naming surface (e.g. the `run_id` timestamp format `2026-06-12T21-59-37-...`) that is shown by example but never specified as a format.
- Is the config-validation contract complete? garde `range` rules (error fraction ∈ [0,1], non-negative durations, sane ramp factors) and `#[garde(custom)]` cross-field invariants (`p50≤p95≤p99` ordering, severity-mix sums) are listed — flag if any validated invariant is named without its bound, or if the serde→garde→`ConfigError` (`#[from]`) failure path is under-specified.

### 4. Downstream Readiness (bidirectional)

**Defect side (ambiguity / missing facts):**
- Can `/andromeda-scope-arch` Phase 0 fill its 11-section template entirely from this document for a new scope, with no questions? Flag any section a downstream scope would have to guess at.
- Can a route/sequence be derived without asking — e.g. is the crate build order (`conductor-core` → seams → `conductor-cli`/`conductor-tauri`) and the headless-is-the-release-gate ordering inferable from the directory tree and Design Philosophy?

**Completeness side (facts present but not elevated to a specialist-facing surface):** for each specialist below, locate the fact in the document; if absent OR present only implicitly (scattered, no aggregation), emit a substantive patch elevating it.
- **tests specialist:** Is the harness contract first-class? The headless `scripts/agent-run.sh` source-of-truth path, per-seam `cargo build -p`/`test -p` discoverability, the synthetic-injection/test-mode gate (`mcp-server` feature + `ANDROMEDA_PULSE_MCP_ENABLED`), golden tests, and the MCP-preflight readiness shape are the test substrate — flag if these are only implied rather than stated as a testability contract.
- **obs specialist:** The JSONL emission journal (the agent-parseable ground truth), the journal-relative SLO math, the Tauri 2 `Channel` live-counter stream, and the run-report envelope are the observability substrate — flag if the structured-output/journal format that harness polling depends on is under-elevated. (Do NOT name a logging library — see Dimension 9.)
- **security specialist:** The capture/trust boundary here is the loopback posture (`127.0.0.1:4317` only, `:4318` unused, no inbound listener of Conductor's own), the `TokioChildProcess` stdio trust model for the launched Pulse MCP server, the local-only/no-secrets/no-`DATABASE_URL` stance, and the port-occupier fault scenario that *deliberately* binds `:4317` — flag if these trust-boundary facts aren't aggregated where a security scope would read them.
- **design specialist:** The Tauri control panel is "convenience only / thin shell" and "must NOT emit native OS toasts" (those are Pulse behavior observed via the operator checklist) — flag if this brand/scope ceiling on the GUI is implicit rather than a stated design constraint. (Do NOT pick colors/fonts/components — that's the design specialist's territory.)
- **a11y specialist:** Input modalities are CLI/headless (source of truth) plus the Tauri webview start/stop/picker/operator-pause surfaces — flag if the operator-pause-prompt and picker focus surfaces are mentioned only in passing rather than identified as UI surfaces a downstream a11y pass would need.

### 5. Complexity Calibration

- Is the stack proportional to "Personal — solo developer, local dev host, no cloud, no multi-tenancy"? The doc already rejects ORM/migrations, message broker, web framework, containers, and a scenario DSL as over-scope — flag any *remaining* enterprise pattern that slipped in, OR conversely any robustness genuinely missing for a deterministic verification harness (e.g. is the determinism discipline — seeded RNG + `current_thread` + `SystemTime`/`Instant` for journal stamps — sufficient, or is a reproducibility gap under-addressed?).
- Does the seam count match the "modular monolith, one deployable" growth model? Seven workspace members (`conductor-core`, `-timeline`, `-emit`, `-faults`, `-verify`, `-report`, `-cli`, + the Tauri bin) for six named seams — flag if any crate is weight without a payoff, or if a stated seam (timeline · emission · faults · verify · report · control panel) lacks a home crate.

### 7. Contract Completeness (MCP-preflight & run-report envelopes — NOT HTTP)

This document defines no HTTP API; the binding contracts are the **MCP `initialize` preflight readiness gate**, the **OTLP egress liveness equivalent**, and the **run-report artifact envelope**. Judge *those* for completeness; do NOT request `/health`, `/ready`, pagination, or REST list/single/error shapes.
- Is the readiness-gate shape fully specified? The JSON shows `ready`, `negotiated_protocol_version`, `expected_protocol_version`, a `required_tools` map over the four tools (`query_incident_list`, `retrieve_report`, `retrieve_telemetry_slice`, `mark_incident_resolved`), `blocked_precondition`, and `checked_at` — flag if any field's value domain is loose (e.g. is each tool value a closed `present`/`absent` enum? is `negotiated_protocol_version` the dated `2025-11-25` form from `rmcp` `peer_info()`?), or if the version-mismatch path (vs the tool-absent path) isn't distinguished as a blocked precondition.
- Is the `blocked` state contract airtight end-to-end? The doc asserts that on `ready: false` every dependent auto scenario is emitted with `state: "blocked"` and the named precondition, "never silently downgraded to pass/fail/manual-check" — flag if the run-report envelope (`run_id`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `journal_emitted_at`, `read_back_observed_at`, `latency_ms`, `slo_tier`, `fingerprints`) doesn't show how a blocked scenario populates (or nulls) the latency/verdict fields it can't measure.
- Is the OTLP liveness/egress contract complete? "Before emission, confirm the gRPC channel to `127.0.0.1:4317` is connectable; a refused transport surfaces as a harness error (`Result::Err`), not a verdict" — flag if the boundary between a connectable-check failure (harness error) and a `tonic::Status` code received mid-run (first-class verification input) is under-specified.

### 8. Cross-reference Integrity

- Does every technology in Stack and Technologies reappear in Conventions, Established Decisions, or Cross-cutting Patterns (not orphaned in the table)? Walk each row — e.g. `prost 0.14`, `libsqlite3-sys 0.38.0`, `cargo-modules`/`cargo-rail`, `tonic-prost-build` — and flag any that appears only once with no operating context.
- Does every crate named in Occupied Resources and the Established Decisions module-boundary entry (`conductor-timeline`, `-emit`, `-faults`, `-verify`, `-report`, `-cli`, the Tauri bin, the runtime-agnostic core) appear in the Infrastructure Patterns directory tree, and vice versa? Flag any crate in prose-but-not-tree or tree-but-not-prose (e.g. `conductor-core` in the tree vs the seam list).
- Does every `CONDUCTOR_*` env var (`CONDUCTOR_RUNS_DIR`, `CONDUCTOR_SCENARIOS_DIR`, `CONDUCTOR_CONTRACT_MANIFEST`, `CONDUCTOR_SEED`) map to a concrete artifact/directory the document defines (`runs/`+`runs.db`, `scenarios/`, `contracts/` manifest, the seed), and does `ANDROMEDA_PULSE_MCP_ENABLED` consistently appear as Pulse-side / asserted-not-set-by-Conductor everywhere it's mentioned? Flag any env var with no consuming surface, or any resource (port, file, MCP tool, Tauri command) referenced in one section but absent from Occupied Resources.

### 9. Specialist Content Boundary

There IS a UI surface (Tauri 2 control panel), so this dimension applies — but Tauri 2 itself is the desktop *shell binding the primary language*, parallel to the backend, and is in-scope (like a mobile framework would be). The boundary violations to hunt are the layers *inside* that shell:
- Does any section name a **frontend framework** (React / Vue / Svelte / SolidJS / Angular), a **CSS tool** (Tailwind / vanilla-extract / CSS Modules / styled-components), or a **component library** (shadcn / Radix / Material UI / Headless UI)? If yes → flag for removal; these are design-specialist territory. (The doc currently describes the webview as "bundled" with no framework named — confirm it stays that way and that no patch introduces one.)
- Does any section name a **logger** (Pino / Winston / tracing-subscriber as a *chosen* logging stack), a **test framework beyond the Cargo-native** (the doc names `cargo test` / `cargo-nextest` / clippy / golden tests, which are the build-gate and stay; flag only if a non-Cargo test lib like an assertion/property framework is *picked*), or an **auth library**? If yes → flag for removal into obs/tests/security territory.
- Confirm the in-scope items are NOT mis-flagged: Tauri 2, the `Channel` IPC primitive, `#[tauri::command]`, the Tauri 2 bundler, serde, garde, thiserror, anyhow, rmcp, tonic, rusqlite, and opentelemetry-proto are architectural picks that belong here — do not strip them.

---

Read the document, analyze along all dimensions, output patches and changelog.
