I have both files. Now I'll construct the meta-prompt with the 5 mandatory dimensions and exactly 3 conditional dimensions (6, 7→relabeled from dim 9, 8→relabeled from dim 11), filling all placeholders with actual values from the plan draft.

---

# Meta-Prompt: Design System Plan Iteration — Conductor

You are an iteration agent improving the Conductor design system plan at `D:/dev/projects/conductor/.andromeda/runs/2026-06-14T12-50-18-design/design-system-plan-draft.md`. Your job is to find real weaknesses — brand drift, generic defaults, motion-expression mismatch, missing domain anchors, downstream handoff gaps — and patch them surgically. Stay strictly inside the design-tokens / patterns / anti-patterns lane: do NOT propose backend, database, frontend-framework, CSS-tooling, component-library, or mobile-framework changes (Phase 1 + architecture have settled tooling). Do NOT generate content owned by other specialists — no test cases, no observability span/metric/trace schemas, no accessibility attribute names or contrast-ratio numbers, no threat/tier/compliance content.

## Output Protocol

Rules for this iteration:

1. **Output patches, not the document.** Emit a list of patches (old → new) plus a changelog. NEVER reproduce the full document or whole sections.
2. **Patch format.** Each patch is:
   - `### Patch N: <short description>`
   - `**Old:**` followed by the exact verbatim text currently in the draft (enough to locate it unambiguously, no more).
   - `**New:**` followed by the replacement text.
3. **Changelog.** After the patches, list one line per patch: `[Iteration N] [substantive|cosmetic] <description>`. Tag a change `substantive` only if it alters meaning, anchoring, tokens, or downstream contract; tag wording/formatting-only fixes `cosmetic`.
4. **PROHIBITED:** full-document reproduction; restructuring sections without a stated reason; relabeling cosmetic edits as substantive; introducing changes outside the design-tokens / patterns / anti-patterns lane; writing content owned by tests / observability / accessibility / security specialists.
5. **If no issues found:** output the literal line `No patches` and a single cosmetic changelog entry stating the plan passed this dimension review.

## Analysis Dimensions

### 1. Brand Identity Coherence

- The signature element is the **Paused-count hold-point — the frozen heartbeat at the operator-pause**. Does it appear as concrete tokens/behavior in at least 3 distinct sections — beyond Brand Identity, verify it is realized in Motion (the green `#7EE787` → amber `#E3B341` freeze, "the absence of motion is the signature"), in the desktop-webview Component Patterns (Frameless titlebar count freeze), AND in the cli Component Patterns (`indicatif` "STOPS in place (not hides)")? If any of these three only *mentions* it abstractly rather than specifying the frozen-value behavior, flag it.
- Expression level is `0.3`. Does the Motion section's actual content match? Confirm the duration scale lists only `150ms ease-out` / `200ms fade` with `none` for entrances and scroll effects, AND that the Hard limits block explicitly bans `framer-motion`, spring physics, parallax, scroll-driven, staggered entrances, and pulse/blink/glow. If any surface or pattern smuggles in motion above `0.3` (e.g. a stagger, a spring, an animated progress bar), patch it down.
- Does the personality statement "Mission-control patience — armed, nominal, go/no-go … calm and resolute under load … no alarm" echo through the Color Palette rationale, Typography rationale, Motion rationale, AND Iconography rationale — or does any rationale read as if written in isolation (e.g. an Iconography or Spacing section with no tie back to "control surface, not a dashboard" / "calm under load")?
- Are the Domain anchors (Mission-control console · Count/heartbeat · Operator-pause go/no-go hold · Verdict/ReportState `Pass`/`Fail`/`CalibrationRegion`/`Blocked` · Coverage matrix) named concretely throughout — never softened into generic labels like "modern", "clean", "professional", or "user-friendly"?

### 2. Domain Anchoring (Exploration → Plan)

- Does every Core Color carry a named domain anchor traceable to the "k9s cluster watch on a Warp terminal" Color World? Verify Primary `#7DCFFF` (mono ID cyan → Coverage matrix / Fingerprint identity), Secondary `#A9B1D6` (journal line-text → Emission journal / Run report), and Accent `#7EE787` (k9s "healthy" → Count alive + `Pass`) each name a specific concept, not "feels right for the domain."
- Are the Surface Scale, Text Hierarchy, and Semantic Colors derived from the same Tokyo-Night slate world — or has any tier drifted into generic greys/blues/reds detached from the `#1A1B26` ground rationale? Confirm the status triad still maps 1:1 to typed outcomes (green=`Pass`, amber=`CalibrationRegion`/HOLD, red=`Fail`, slate-violet `#565F89`=`Blocked` "never measured").
- Does the Typography rationale name the domain metaphor — JetBrains Mono as the reserved **status tier** (count/heartbeat, P-001..P-060, run_id, SLO timings, fingerprints) and IBM Plex Sans as the humanist-grotesque Inter replacement for phase line + prose — rather than implying "mono = techy"? Confirm "mono everywhere" is explicitly held out as a Rejected Default.
- Do the Border Radius personality (sharp-leaning 4–8px instrument-panel precision, with the `radius-full` verdict-lamp dot as deliberate contrast), Spacing density (4px base, developer-tool tightness), and Iconography style (outlined + custom filled status-lamp glyphs) each stay tied to the mission-control / instrument-panel metaphor — or has any reverted to a generic consumer scale?

### 3. Library-Shortlist Faithfulness

- Does the Color Palette stay faithful to the exploration's Color World, with each hex traceable to a shortlist entry — and is the Q3 override (cool blue-cast dev-tool world chosen over the retro Apollo firing-room palette) recorded in the Decisions Log rather than silently applied?
- Does the Typography pairing match the shortlist pairing #1 (`typography-stack-install` spec): JetBrains Mono + IBM Plex Sans, self-hosted WOFF2 via Fontsource? Confirm no banned primary face (Inter / Roboto / Arial / Helvetica / system-ui) has crept back in as anything other than a fallback-stack tail.
- Does the Design Direction still reference its closest shortlist match — the "Precision & Density" / instrument-panel register (Linear's "instrument-panel density" over a Grafana tile wall) — and carry that register's density/flat-elevation effects into the plan?
- Are the developer-tool industry rules applied (data-density priority, reserved monospace status tier, keyboard-first run control mirroring k9s/lazygit) AND reflected as concrete entries in the Anti-Patterns section — not stated once and dropped?

### 4. Anti-Pattern Relevance

- Does the Anti-Patterns section retain all 3 subsections (Universal Bans / Rejected Defaults / Per-Surface Bans) with at least 5 bans total?
- Does Universal Bans still include all 6 template items (banned generic fonts / no purple-gradient-on-white / no cookie-cutter card grids / no same-layout-for-different-info / no decoration-only color / no safe-default convergence)? Flag any silently dropped.
- Are the Rejected Defaults from the exploration present as concrete `— rejected because:` entries — specifically the Grafana/Datadog KPI-card-grid dashboard, the flashing/pulsing red Fail banner, the progress bar that hides/animates-to-100% during the operator-pause, the indigo/violet "AI gradient" + glassmorphism, the Blocked-collapsed-into-red conflation, and mono-everywhere? Verify none has degraded into a generic warning.
- Are the Per-Surface Bans genuinely specific to **desktop-webview** (no visible Chromium artifacts / no unstyled web scrollbars / no browser nav / no `alert()`/`confirm()`/`prompt()` / borders-only no `backdrop-filter`) and **cli** (stdout-vs-stderr discipline / no emoji in piped output / dynamic terminal-width detection / `isatty` gating / never block the headless path) — not generic placeholders that would apply to any project?
- Do the per-section rationales (Color / Typography / Motion / Depth / Iconography) cross-reference the Anti-Patterns section rather than re-stating bans inline, so a future change to a ban does not drift across sections?

### 5. Downstream Readiness

- Can `tests` derive UI test SCOPE from the plan alone — visual-regression targets per Surface section, motion-budget compliance at expression `0.3`, a banned-font lint target from Universal Bans — without the plan itself enumerating test cases (that is tests' domain)?
- Can `obs` read the visual error/loading/empty states from each Surface's States notes (e.g. the operator-pause dialog Loading-Buttons in-flight state, the Coverage matrix "No scenarios loaded" empty state, the `Blocked` non-error rendering) without inventing any new design tokens — and does the plan avoid prescribing observability span/metric/trace schemas?
- Can `a11y` derive what it needs from the plan's *visual/motion* context only: the Text Hierarchy hex values (`#C8D0F0`, `#A9B1D6`, `#717AA0`, `#565F89`) as contrast inputs, the Focus row color `#7DCFFF`, the motion budget for `prefers-reduced-motion` at expression `0.3`, and the icon-label policy from Iconography? The plan should provide contrast values, focus-ring color, and motion budget; a11y derives the ARIA attributes itself. Flag any place the plan tries to specify accessibility attribute names or fixed contrast-ratio numbers — those are out of lane.
- Can `route` derive UI bootstrap items — the self-hosted Fontsource WOFF2 packages to vendor (`@fontsource/jetbrains-mono`, `@fontsource/ibm-plex-sans`), the Tailwind v4.1 `@theme` token system to scaffold, and the already-settled component library (shadcn/ui) — directly from the plan without ambiguity?
- Can `setup-project` materialize design rules (design-tokens, no-banned-fonts, expression-budget) from the Anti-Patterns + Self-Validation Protocol + Decisions Log as written? Flag any gap that would force a downstream skill to guess.

### 6. Multi-Surface Consistency [trigger: detected surfaces > 1]

- Do both the desktop-webview and cli Surface sections derive from the *same* Color Palette / Typography status-tier / Motion tokens — or has either drifted into a mini-design-system with divergent personality? Specifically verify the status triad maps consistently: dark hex (`#7EE787`/`#E3B341`/`#F85149`/`#565F89`) ↔ ANSI (114/179/203/60), and mono ID cyan `#7DCFFF` ↔ ANSI 117.
- Is the signature Paused-count hold-point manifested on each surface through a surface-appropriate channel — desktop-webview via the in-place CSS color freeze (`--count-nominal` → `--count-hold`), cli via the `indicatif` spinner that STOPS-not-hides with a colored `owo-colors` HOLD line? Flag any surface where the freeze semantics weaken (e.g. a hide, a blank, or an animate-to-100%).
- Are the platform-specific quirks in each "Platform-Specific Notes" used as *adaptation* (terminal owns the font and width on cli; Tauri frameless chrome owns drag/min/close on desktop-webview) rather than as an escape hatch that breaks brand cohesion?
- Squint/Sameness check: would an operator viewing both surfaces side-by-side read "this is one Conductor" — same calm, same status semantics, same reserved mono tier — rather than "two separate tools"?

### 7. CLI / TUI Surface Completeness [trigger: detected surfaces include cli OR tui]

- Are the Color World hexes mapped to ANSI 256 codes (not just hex) in the cli Tokens block — green `#7EE787`→114, amber `#E3B341`→179, red `#F85149`→203, blocked `#565F89`→60, ID-cyan `#7DCFFF`→117, journal `#A9B1D6`→146 — and is degradation noted (honoring `NO_COLOR` / `TERM=dumb` / pipe-stripping via `anstream`)?
- Is the cli output structure fully documented — the ASCII status prefixes (`[PASS]`/`[HOLD]`/`[FAIL]`/`[BLOCKED]`), the glyph prefixes (`✓`/`✗`/`→`/`•`, TTY-only), header formatting (bold + ANSI 117 via `owo-colors`), per-line information density, and stdout-data-vs-stderr-message separation?
- Is the signature expressed through a CLI-appropriate channel (the `indicatif` spinner STOPS in place at the hold value with a colored HOLD phase line) rather than a skeuomorphic attempt at shadows or rounded corners that don't exist in a terminal?
- Does the Typography section note the cli uses terminal monospace (the user's configured terminal font, not a webview face) and does the Spacing/Border-Radius guidance defer cli layout to terminal-controlled fixed-width (`comfy-table` grid + `indicatif`, width detected dynamically, never hardcoded)?

### 8. Expression-Anti-Pattern Alignment [trigger: expression level <= 0.3 OR >= 0.7]

- At expression `0.3` (low end), are the Motion bans aggressive and explicit — no `framer-motion`/animation libraries, no spring physics, no parallax, no scroll-driven, no staggered entrances, no 3D, no canvas/WebGL, no pulse/blink/glow on status, no flashing red on `Fail`, no animate-to-100% progress bar? Confirm the Hard-limits sub-block is *longer* than the active-animations list (a healthy signal at this expression).
- Does the rest of the plan respect the `0.3` boundary with no contradictions — verify no Surface Component Pattern, no Iconography behavior, and no Decisions-Log entry sneaks in a stagger, spring, parallax, or pulsing/flashing status that would violate the stated ceiling.
- Is the `prefers-reduced-motion: reduce` handling present and binding — confirm all transitions (including the count-tint transition on a held count) are dropped under reduced-motion, consistent with the `0.3` "functional motion only" stance.

---

Read the document, analyze along all dimensions, output patches and changelog.
