I now have all the context I need. Let me analyze the document to select the right dimensions.

**Document profile:**
- **Tier:** Minimal (0), upper-boundary, augmented with Section 5 coverage triggers
- **Security tier:** Minimal (0) — NOT Hardened, no compliance triggers → Dimension 7 (Compliance) does NOT trigger
- **Performance budgets:** explicitly N/A, no perf-budget trigger, NOT Comprehensive → Dimension 6 (Performance Budget) does NOT trigger
- **Multiple surfaces:** cli + ipc-internal (MCP) + ipc-internal (Tauri) + desktop-webview + persistent-storage; cross-surface-coordination trigger present → Dimension 8 (Cross-Surface Coordination) TRIGGERS
- **UI surface:** desktop-webview (Tauri 2 bundled webview) present, E2E Section 6 present → Dimension 9 (E2E Selector Strategy Robustness) TRIGGERS

So mandatory 1-5 plus conditionals 8 and 9 (the only two that trigger). That's 7 dimensions, meeting the "minimum 2 conditional" requirement.

**Stack/tools to anchor questions:**
- Rust 2024, tokio 1.48.x current_thread, opentelemetry-proto 0.32.0, tonic 0.14.6/prost 0.14, rmcp 1.7.0, rusqlite 0.38.0 (bundled SQLite 3.51.1), Tauri 2 (≥2.10.3), serde+garde 0.23.0, thiserror 2.0.18/anyhow 1.0.102
- Test tools: cargo-nextest 0.9.137, cargo test --doc, cargo-llvm-cov 0.8.7, rstest 0.26.1, assert_cmd 2.1.2/predicates 3/assert_fs 1, rmcp stub, tauri::test (tauri 2.10.3), @crabnebula/tauri-driver 2.0.9, proptest 1.9.0, insta 1.46.1, tokio test-util, turmoil 0.7.2, wiremock-rs 0.6.5, cargo-audit 0.22.2, cargo-deny 0.19.6, tracing-subscriber, serde_json+jq

Now I'll generate the meta-prompt.

---

# Meta-Prompt: Test Plan Iteration — Conductor

You are an iteration agent improving the Conductor test plan at `D:/dev/projects/conductor/.andromeda/runs/2026-06-14T16-35-16-tests/test-plan-draft.md`. Conductor is a scenario-driven OTLP fault-injection + verification harness (Rust 2024 workspace, 8 `conductor-<seam>` crates) that drives a live Pulse instance through 60 capabilities (P-001..P-060) and verifies each reaction via MCP read-back. Test tier is **Minimal (0)** at the upper boundary, augmented with Section 5 coverage triggers. The headless CLI (`agent-run`) is the release gate; the Tauri 2 GUI is "convenience only."

## Output Protocol

Rules you MUST follow:

1. **Output format:** patches (old → new) plus a changelog. Do NOT reproduce the full document.
2. **Patch format:** for each change emit `### Patch N: <short description>`, then `**Old:**` followed by the exact verbatim text being replaced, then `**New:**` followed by the replacement text. The `Old` text must match the document byte-for-byte so the patch is unambiguously locatable.
3. **Changelog:** one line per change in the form `[Iteration N] [substantive|cosmetic] description`. `substantive` = changes coverage, a tool/version, an assertion mechanism, an exit-code/state mapping, a trigger mapping, or a downstream contract. `cosmetic` = wording/formatting only.
4. **PROHIBITED:** full-document reproduction; restructuring sections without a stated defect; relabeling cosmetic edits as substantive; introducing test code blocks longer than 5 lines (concrete test *bodies* are tests-pass / phase work, not the plan); adding content owned by other specialists (product-side OTel span/metric schemas → obs; ARIA conformance rules → a11y; threat models / auth / encryption → security; design tokens / component patterns → design); recommending human-in-loop tools (Percy / Chromatic / Applitools / `cargo insta review` interactive UI / WebdriverIO inspector-recorder) anywhere except where Section 11 *rejects* them.
5. **If no issues found in a dimension:** state "No patches" and record a single cosmetic changelog line noting the dimensions reviewed.

## Analysis Dimensions

### 1. Test Scope Faithfulness

- Does every testable entity from test-scope Section 1 receive coverage in the plan's Section 4 (Unit) / Section 5 (Integration) / Section 6 (E2E)? Cross-check the 13 entities — `conductor-core`, `conductor-timeline`, `conductor-emit`, `conductor-faults`, `conductor-verify`, `conductor-report`, `conductor-cli`, `conductor-tauri`, the scenario-config validation surface, the error-handling surface, the scenario catalog + `coverage-matrix.md`, and the `runs.db` + JSONL store — against Section 4's "What unit tests cover" list. Is any entity silently dropped or, conversely, is any *partially-testable* entity (e.g., `conductor-emit` gRPC egress, the live-Pulse leg of `conductor-verify`) promoted to full CI coverage instead of being held to its unit/local-gate split?
- Are the four untestable zones from test-scope Section 1 (live Pulse + encrypted `corpus.db` / P-049, `OsKeychainBackend("com.andromeda.pulse")`, the `drive+observe` human visual judgment, `static-only` Pulse capabilities) excluded from Section 6 with no invented driver — and does the Section 6 driver table's skip note still enumerate all four?
- Is each of the 7 critical paths from test-scope Section 4 rendered as a `#### Scenario:` heading in Section 6 with Steps + Verification signal + Cleanup populated — specifically the `error-baseline-spike` MCP read-back path, fingerprint-storm, restart-suppression (incl. the bypass case), severity-lifecycle (auto-resolve + resolution summary), the P-032 known-residual path inside a real git workspace, the coverage-matrix completeness gate, and both-surface parity? Are `pii-scrub` and `connection-lifecycle` still accounted for as additional auto-scenario instances of Path 1's pattern (not dropped, not over-counted past 7)?
- Is each of the 11 coverage triggers from test-scope Section 5 addressed somewhere — e.g., the Vector-1 `CONDUCTOR_*` path-traversal negative test, the Vector-2 garde property test, the Vector-4 rmcp STDIO injection / empty-canary-`blocked` / bounded-prost negatives, the Vector-3 Tauri capabilities deny-by-default assertion, the `runs.db` bound-parameter (no `format!` SQL) assertion, the contract test negotiating down to `2024-11-05`, the determinism-replay property test, the cross-surface-coordination parity test, the bounded `conductor-faults` chaos test, and the `cargo-audit`/`cargo-deny` supply-chain gate? Flag any trigger with no home in Sections 2/5/8/9/10/11.
- Does the Test Scope Summary (Section 1) copy the Phase 1 tier label, justification, surfaces, harness requirements, paths, and triggers verbatim — or has any field been paraphrased or truncated?

### 2. Tier Calibration

- The plan declares **Minimal (0)** but explicitly augments with Section 5 triggers, so Integration (Sec 5), E2E (Sec 6), and Mocking (Sec 8) are present *by trigger*. Does each of those sections justify its inclusion against a specific trigger (integration boundaries: MCP read-back / `runs.db` / Tauri IPC; E2E: the 7 critical paths; mocking: the security-vector negatives + MCP stub) — or do they read like an un-triggered Comprehensive escalation that Minimal does not warrant?
- Do the Section 10 coverage thresholds match the Minimal-tier defaults of line ≥ 60% / branch ≥ 50% / function ≥ 70%? Verify the `cargo llvm-cov --fail-under-lines 60` gate and the "branch coverage informational on stable, line gate binding" note are internally consistent and have not been silently raised to Standard (75/70/85) or lowered below 60.
- Section 10 marks Performance Budgets as N/A (no perf-budget trigger; "NOT a load-tester," 50k+ spans/sec is Pulse's `perf_load_profiles.rs`) and omits the Compliance subsection (security tier Minimal, not Hardened). Are both omissions justified in-line and consistent with test-scope Section 5 — or does any section sneak in a perf-budget table or compliance row that the tier does not support?
- Does the Section 2 test pyramid table keep Performance/Load **Excluded** while keeping Property-based and Chaos/Fault **Included via trigger** (not via blanket Comprehensive escalation)? Confirm the "Included via trigger" labels point at real test-scope Section 5 triggers and the bounded P-060 chaos is not conflated with load.

### 3. Tool Anchoring (Catalog ↔ Plan)

- Does every named tool in the plan appear in `test-research.md` with the **matching version**? Spot-check: cargo-nextest 0.9.137, cargo-llvm-cov 0.8.7, rstest 0.26.1, assert_cmd 2.1.2 + predicates 3 + assert_fs 1, rmcp 1.7.0, tauri 2.10.3 (`test` feature), @crabnebula/tauri-driver 2.0.9, proptest 1.9.0, insta 1.46.1, turmoil 0.7.2, wiremock-rs 0.6.5, cargo-audit 0.22.2, cargo-deny 0.19.6, tracing-subscriber 0.3.x. Flag any version drift or any tool named in the plan but absent from the catalog (a fabricated/un-researched pick).
- For categories where the catalog presents alternatives, does the plan pick one and state the trade-off? Check three: (a) cargo-nextest vs. plain `cargo test` — is the "`cargo test --doc` fallback because nextest does not run doctests" rationale present? (b) wiremock-rs vs. an in-process tonic `TraceServiceServer` for the OTLP gRPC egress — does the plan justify NOT force-fitting wiremock onto gRPC? (c) turmoil vs. the in-crate `conductor-faults` generators + loopback port probe — is turmoil correctly framed as *optional / not load-bearing*?
- Is each tool's catalog `**Agent-runnable:**` mechanism preserved as a concrete machine signal in the plan — `NextestExitCode` (100/101/4) + JUnit XML; `cargo llvm-cov --fail-under-lines` exit code; `jq -e` exit (1 on false/null, 4 on parse error); `serde_json::from_str::<RunReportEnvelope>`; `assert_cmd` `.code()`/`.success()`; insta CI fail-don't-write? Flag any tool reduced to a name with no agent-readable signal.
- The Decisions Log flags "no JS/TS unit-test runner researched for the React 19 SPA frontend logic" as an open question. Is that gap honestly carried (webview asserted only at the E2E/parity layer; no vitest-class tool invented), rather than a frontend unit runner being slipped in that the catalog does not contain?

### 4. Anti-Pattern Relevance

- Are the Section 11 stack-specific bans actually grounded in *this* Rust/Tauri/MCP stack rather than generic? Verify the load-bearing ones map to real pitfalls: never `format!`-build `runs.db` SQL (rusqlite bound params); never stamp the journal from tokio's virtual clock (use `std::time::SystemTime`); never force-fit wiremock-rs onto the gRPC OTLP egress; never interpolate `ANDROMEDA_PULSE_DATA_DIR` into the sidecar argv (rmcp STDIO injection, CVE-2026-30623); never treat `blocked`/`ManualCheck`/`KnownResidual`/`CalibrationRegion` as a non-zero exit; never add a scenario without a P-ID; never promote the `:4317` port-occupier into a general listener.
- Does each domain subsection (Test Strategy / Unit / Integration / E2E / Test Data / Mocking / CI / Quality / Universal) carry at least one ban specific to this stack — not a placeholder? Flag any domain whose bans are purely generic.
- Do the Universal bans stay genuinely cross-stack and agent-driven (zero-flakiness / no nextest `retries` / no Percy-Chromatic-Applitools / no manual smoke / no Selenium-IDE or WebdriverIO recorder / no real network beyond loopback / no real time without tokio injection) — and is the `cargo insta review` interactive UI correctly banned *as a test author* while insta CI/assert mode remains the approved gate?
- Are bans centralized in Section 11 and merely cross-referenced from Sections 4-10 ("See Section 11 § ..."), rather than duplicated inline in the section bodies? Flag any ban re-stated in full inside a section body instead of cross-referenced.

### 5. Downstream Readiness

Per the D26 read chain, this plan is consumed by obs / a11y / route / setup-project. Each must derive its plan from this plan + arch.md + prior specialist plans alone.

- **setup-project (most weighted):** Can `scripts/agent-run.{sh,ps1}` be materialized from Section 3 alone? Are all five command bodies copy-paste concrete — `boot` = `cargo run -p conductor-cli ... preflight --json` (the no-daemon preflight gate, stub leg in CI vs. live-Pulse leg gated on `ANDROMEDA_PULSE_MCP_ENABLED`), `run` = `cargo nextest run --workspace --profile ci` + `cargo test --workspace --doc` + clippy, `status` = `jq -e` / rusqlite bound-param `SELECT`, `cleanup` = `rm -f runs/<run_id>.{jsonl,md}` + bound-param `DELETE` + `:4317` release, `logs` = read `runs/<run_id>.jsonl`? Is the binding contract that both shell variants expose identical semantics (Windows + Linux; webview E2E Linux-`xvfb`-only) stated?
- **obs:** Can obs derive the product-side log format from Section 3's Log format subsection — structured JSONL, one object per line via `tracing-subscriber` `format::Json`, required envelope fields (`run_id`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `latency_ms`, `slo_tier`, `journal_emitted_at` from `std::time::SystemTime`, `fingerprints`), no absolute host paths / internal struct names? Does the plan correctly mark the field-allowlist/redaction layer as *owned by obs* while this plan only *asserts* it via a negative test (i.e., it does not pick an observability platform or define product instrumentation)?
- **a11y:** Can a11y tell which Section 6 selectors are role/text/`aria-live`-based vs. CSS, so it can derive ARIA patterns without conflict? Confirm the Selector strategy declares text-paired status labels (`[PASS]`/`[FAIL]`/`[HOLD]`/`[MANUAL]`/`[RESIDUAL]`/`[BLOCKED]`), `role="alertdialog"` on operator-pause, `aria-live` HOLD/verdict, and the "HOLD — operator pause" heading text — and stays a *selector* contract, not an a11y conformance ruleset.
- **route:** Can `route` order the bootstrap phases from the Section 3 "Bootstrap phases" list + Sections 4/9/10 — test-runner-install → 5-command-discipline-wire → status-endpoint-implement → log-format-bind-with-obs → pid-file-commitment-wire → test-data-bootstrap-wire → coverage-tooling-install → quality-gate-config-emit? Are the N/A phases (status endpoint, PID file) explained as N/A-with-substitute (Run-report envelope serializer; `cleanup` removes artifacts) rather than left dangling?

### 8. Cross-Surface Coordination Coverage

- The plan spans cli + ipc-internal (MCP read-back) + ipc-internal (Tauri commands/Channel) + desktop-webview + persistent-storage. Is the cross-surface-coordination trigger from test-scope Section 5 covered in Section 5 Integration — specifically the parity test where the `tauri::test` mock-runtime run and the `assert_cmd` CLI subprocess run, both pointed at the *same* `assert_fs::TempDir` file `runs.db` via `CONDUCTOR_RUNS_DIR`, assert an identical envelope (`verdict`/`state`/`seed`) for the same scenario+seed?
- Is the Section 6 "Both-surface parity" scenario's `Surfaces involved` enumerated (cli + desktop-webview + ipc-internal Tauri commands/Channel), and does its verification signal specify *how* the agent confirms state transitioned across surfaces — i.e., the Tauri-launched run's `runs.db` envelope equals the headless `conductor run` envelope, with the emission journal written per run from both paths?
- Does the plan correctly acknowledge the documented coordination *gap* — that the `tauri::test` mock runtime does NOT bridge `Channel` frames, so the live-counter message-sequence assertion falls to either a direct unit test of the channel-producing fn or the tauri-driver E2E leg — rather than over-promising full Channel-frame coverage from the mock runtime? Is the CLI-as-release-gate / GUI-as-"convenience only" hierarchy preserved so parity is not mis-weighted as a primary gate?
- Is there a cross-surface anti-pattern in Section 11 guarding against testing a surface in isolation when a critical path (parity) spans both — and is it consistent with the "no scenario without a P-ID" / single-`:4317`-bind scope law?

### 9. E2E Selector Strategy Robustness

- The desktop-webview surface (Tauri 2 bundled webview, React 19 + Vite + Tailwind v4.1 + shadcn/ui) is driven by @crabnebula/tauri-driver 2.0.9 headless via `wdio run`. Does the Section 6 Selector strategy use role / text / data-testid / brand anchors and explicitly ban xpath and hashed Tailwind/CSS class selectors? Confirm it leans on the Color-Only a11y rule (every lamp text-paired) so text/role selectors are reliable.
- Are the selectors anchored in the upstream layout-templates Signature Placements — the Heading-role "HOLD — operator pause" phase line, the `role="alertdialog"` shadcn operator-pause dialog, `aria-live` HOLD/verdict announcements, and the mono P-ID tokens (`P-001`..`P-060`) for coverage-matrix/report rows? Flag any E2E scenario whose verification signal relies on a non-signature, drift-prone selector.
- Is the Section 6 selector strategy consistent with the Section 11 E2E bans (no xpath, no `sleep(N)` for synchronization — wait on the envelope `state` field / JSONL journal line / element role / `aria-live` / exit code, no hashed-class selectors)? Flag any Section 6 step that waits on a timer instead of an explicit signal.
- Is the macOS constraint honored — no WKWebView WebDriver, so the webview E2E runs on Linux CI under `xvfb` only — and does the plan keep visual-pixel/Percy-style assertions out of this surface, asserting role/text/`aria-live` only, consistent with the "NO UI automation of Pulse" creator anti-pattern?

Read the document, analyze along all dimensions, output patches and changelog.
