I have both files. Now I'll fill the template, tracing every finding to specific sections in the upstream context. Let me analyze the surface categories carefully: there are two real surfaces (desktop-webview/Tauri 2, and cli) plus an internal IPC contract (Tauri commands + Channel), and the MCP read-back is an IPC-internal-style surface driven by an MCP test client.

## 1. Coverage Scope

What entities, modules, and capabilities exist in this project that need testing:

- **Entity:** `conductor-core`
- **Source:** arch excerpt's Workspace/Modules ("runtime-agnostic engine library that every other crate depends on")
- **Testability:** testable
- **Reason if not fully testable:** N/A — pure Rust library, `cargo test -p conductor-core` per the per-seam test-isolation convention.

- **Entity:** `conductor-timeline` (deterministic seeded phase scheduler on `tokio::time`)
- **Source:** arch excerpt's Workspace/Modules + Stack ("tokio 1.48.x `current_thread` flavor — deterministic single-threaded scheduler for the timeline engine")
- **Testability:** testable
- **Reason if not fully testable:** N/A — determinism is unit-assertable (same seed ⇒ same stream shape per Creator Brief's "Determinism as a hard quality bar"); `current_thread` flavor makes virtual-clock scheduling deterministic in-test.

- **Entity:** `conductor-emit` (OTLP raw-type emission primitives — opentelemetry-proto + tonic)
- **Source:** arch excerpt's Workspace/Modules + Stack ("opentelemetry-proto 0.32.0 ... raw hand-built OTLP message structs for byte-level fault control")
- **Testability:** partially-testable
- **Reason if not fully testable:** OTLP struct construction and byte-level encoding are unit-testable in isolation; actual gRPC egress to `127.0.0.1:4317` requires a live Pulse ingest listener (arch CI/CD note: "dynamic end-to-end scenario proof requires a live Pulse"). The egress liveness/transport leg is local-gate-only, not CI.

- **Entity:** `conductor-faults` (ramps, silence, port-occupier, fingerprints)
- **Source:** arch excerpt's Workspace/Modules
- **Testability:** partially-testable
- **Reason if not fully testable:** Ramp/silence/fingerprint generators are unit-testable as pure functions; the `:4317` port-occupier fault (security excerpt's Vector 6) binds a loopback port — agent-runnable locally via a port-reachability probe but contends with a live Pulse on the same port, so it lives on the local/operator gate.

- **Entity:** `conductor-verify` (MCP read-back client via rmcp, preflight gate, verdict logic)
- **Source:** arch excerpt's Workspace/Modules + Stack ("rmcp 1.7.0 ... `serve_client()` over `TokioChildProcess` stdio") + Standard Contracts (preflight readiness gate)
- **Testability:** partially-testable
- **Reason if not fully testable:** Verdict logic and preflight state mapping (`ready:false ⇒ blocked`, `degraded_mode ⇒ KnownResidual`) are unit-testable against a stub MCP server; the real read-back path needs the spawned `andromeda-pulse-mcp` sidecar with `mcp-server` feature + `ANDROMEDA_PULSE_MCP_ENABLED` (Creator Brief's MCP read-back prerequisite) — that integration leg is local-gate-only.

- **Entity:** `conductor-report` (JSONL journal + Markdown report + `runs.db` rusqlite storage)
- **Source:** arch excerpt's Workspace/Modules + Stack ("rusqlite 0.38.0 ... embedded run-metadata index `runs.db`") + Standard Contracts (Run report envelope)
- **Testability:** testable
- **Reason if not fully testable:** N/A — Run-report envelope schema, JSONL journal shape, and `runs.db` rows are golden-testable via cargo-nextest (security excerpt's data-classification testability hint: "testable ... golden tests via cargo-nextest"); embedded SQLite (`bundled`) needs no external service; DB-row assertions run in-process.

- **Entity:** `conductor-cli` (the `agent-run` binary — headless source of truth)
- **Source:** arch excerpt's Workspace/Modules + Test Harness Commitment ("the headless `scripts/agent-run.sh` path is the source of truth and the release gate")
- **Testability:** testable
- **Reason if not fully testable:** N/A — binary edge testable via `Command::cargo_bin` subprocess + exit-code/stdout assertions (the agent-driven release gate).

- **Entity:** `conductor-tauri` (Tauri 2 GUI binary — commands + Channel; owns its own runtime)
- **Source:** arch excerpt's Workspace/Modules + Stack ("Tauri 2 ... + Tauri 2 IPC `Channel`") + Surfaces (Deferred: "the GUI shell itself is convenience only, the CLI/headless path being the release gate")
- **Testability:** partially-testable
- **Reason if not fully testable:** The Tauri command/Channel IPC layer is agent-drivable headless via tauri-driver; arch explicitly designates the GUI as "convenience only" and the CLI as the gate, so the webview leg is a secondary surface, not the release gate.

- **Entity:** Scenario-config validation surface (serde + garde — `#[derive(Validate)]` range rules + cross-field invariants)
- **Source:** arch excerpt's Stack ("serde 1.0.x + garde 0.23.0") + Standard Contracts + security excerpt's Vector 2 + data-classification "config (low)"
- **Testability:** testable
- **Reason if not fully testable:** N/A — load-time garde validation (error fraction ∈ [0,1], non-negative durations, p50≤p95≤p99, severity-mix sums) is unit-testable with valid/invalid config fixtures; "transient at load" per the data-classification hint.

- **Entity:** Error-handling surface (thiserror typed per-seam enums + anyhow at edges)
- **Source:** arch excerpt's Stack ("thiserror 2.0.18 + anyhow 1.0.102 ... serves the error-handling surface across all seams")
- **Testability:** testable
- **Reason if not fully testable:** N/A — typed error enums assert per-seam failure mapping; the sanitization invariant (security excerpt: "Exposing internal error details ... rejected") is assertable on the `anyhow` edge and report artifacts.

- **Entity:** Scenario catalog (P-001..P-060, one-per-P-ID) + `coverage-matrix.md` (all 60 P-IDs classified, zero gaps)
- **Source:** arch excerpt's Project Intent ("60 claimed capabilities P-001..P-060") + Test-Relevant Conventions ("no scenario without a P-ID") + Creator Brief's Must-Work ("`coverage-matrix.md` complete: all 60 P-IDs ... zero gaps")
- **Testability:** partially-testable
- **Reason if not fully testable:** Matrix completeness/zero-gaps and per-scenario P-ID keying are statically assertable (a missing P-XXX is a defect — Creator Brief). The `auto`-classified scenarios assert via MCP read-back (live Pulse, local gate); `drive+observe` scenarios induce state but the visual confirmation is an operator-checklist item — see untestable zones below.

- **Entity:** `runs.db` SQLite store + per-run JSONL journals + Markdown reports (persistent data store)
- **Source:** security excerpt's data-classification "test-telemetry / run-metadata (synthetic, not user data) (low) — stored in `runs/` ... and embedded SQLite `runs.db`"
- **Testability:** testable
- **Reason if not fully testable:** N/A — the one persistent store; self-generated synthetic data, append-mostly; fixture strategy is seeded synthetic OTLP via the timeline/emit seams (no developer-seeded DB needed). Golden tests cover artifact shape.

**Untestable zones (flagged here, not given invented drivers):**

- **Untestable:** Live Pulse instance + its encrypted `corpus.db` (P-049). Source: security excerpt ("Reading, copying, or decrypting Pulse's encrypted `corpus.db` ... owned and encrypted by Pulse") + Creator Brief ("NO Pulse process management — Conductor never starts/stops/restarts Pulse"). Reason: third-party process Conductor cannot start, edit, or reach internally; it is exercised only via MCP canary round-trip. Pulse-side state is untestable from Conductor.
- **Untestable:** `OsKeychainBackend("com.andromeda.pulse")` credential. Source: security excerpt's data-classification "credential — none owned by Conductor ... testability hint: untestable in current harness (out of scope)." Reason: Conductor never stores/generates/manages any secret; the keychain is Pulse-side.
- **Untestable (by agent):** `drive+observe` visual/UX confirmation step. Source: Creator Brief's coverage classification ("the operator confirms the visual/UX claim via the generated checklist") + Creator Brief anti-pattern ("NO UI automation of Pulse — visual claims are operator checklist items"). Reason: the *checklist rendering* and induced-state context are agent-assertable; the human visual go/no-go judgment is not agent-drivable and is explicitly an operator step — flagged untestable rather than given a manual-review workaround.
- **Out of scope (no driver):** `static-only` Pulse capabilities. Source: Creator Brief ("static-only ... stays with Pulse's own test matrix — Conductor explicitly does NOT duplicate them"). Reason: no dynamic telemetry dimension; owned by Pulse's suite.

## 2. Surfaces Under Test

- **Surface:** cli (`conductor-cli` / `agent-run` binary)
- **Driver:** subprocess + `Command::cargo_bin` (Rust language-native test client / assert_cmd-style), driving `conductor run <scenario>` / `conductor suite` / `conductor report <run_id>` / `scripts/agent-run.sh`
- **Signal:** exit code + structured stdout (status text labels `[PASS]`/`[FAIL]`/`[HOLD]`/`[BLOCKED]`/`[MANUAL]`/`[RESIDUAL]` per Design System brand anchors, survive `NO_COLOR`/piping) + `runs.db` row check via embedded SQL + per-run `<run_id>.jsonl` journal file existence/content
- **Boundary:** cross-surface (drives core → timeline → emit → verify → report; the release-gate path)
- **Notes:** Source-of-truth and release gate (arch Test Harness Commitment); headless path "never blocked on an interactive prompt" (Design System cli surface) — TTY-gated interactive `inquire` prompts (operator-pause) must be agent-bypassable on the headless path. Full dynamic end-to-end (auto scenarios) requires a live Pulse + `mcp-server` feature → local/operator gate, not CI (arch CI/CD note).

- **Surface:** ipc-internal (MCP read-back client — `conductor-verify` via rmcp over `TokioChildProcess` stdio)
- **Driver:** language-native IPC test client — a stub/mock MCP server (rmcp) over stdio + JSON schema assertion on the typed tool responses (`query_incident_list`, `retrieve_report`, `retrieve_telemetry_slice`, `mark_incident_resolved`); against the real `andromeda-pulse-mcp` sidecar on the local gate
- **Signal:** IPC response payload check (negotiated protocol version == `2024-11-05`, required-tool presence vs. pinned manifest, canary round-trip non-empty) → maps to verdict/state; `ready:false` ⇒ `blocked`, `degraded_mode` ⇒ `KnownResidual` (arch Standard Contracts)
- **Boundary:** cross-surface (preflight gate gates all dependent scenarios)
- **Notes:** Real read-back leg requires Creator Brief's documented prerequisite (Pulse `mcp-server` feature + `ANDROMEDA_PULSE_MCP_ENABLED` + matching `ANDROMEDA_PULSE_DATA_DIR`); mismatch ⇒ silently-empty read-back, which preflight must catch as `blocked` (security excerpt's Vector 4). Stub-server leg is fully CI-runnable.

- **Surface:** ipc-internal (Tauri 2 `#[tauri::command]` request/response + one `Channel` — `conductor-tauri`)
- **Driver:** language-native IPC test client + JSON schema assertion on command request/response (start/stop/picker/run-report/operator-pause) and Channel live-counter stream payloads; full webview leg via tauri-driver (desktop-webview)
- **Signal:** IPC response payload check (command return shape) + Channel message-sequence assertion (live counter / target-status frames) + DB row check (`runs.db`) after a start→stop command cycle
- **Boundary:** cross-surface (core ↔ bundled webview)
- **Notes:** Internal in-process boundary only — "no REST/GraphQL/tRPC" (arch Standard Contracts), "not network-exposed" (security excerpt's Vector 3). GUI is "convenience only," CLI is the gate (arch Surfaces) — this surface is secondary to the cli release gate.

- **Surface:** desktop-webview (Tauri 2 bundled webview — React 19 + Vite SPA + Tailwind v4.1 + shadcn/ui)
- **Driver:** tauri-driver (headless), exercising Run console idle/live/HOLD + Run report layouts (Layout Templates excerpt)
- **Signal:** role/text selectors on text-paired status labels (Design System: every lamp "mandatorily paired with a text label," color never sole signal) + `aria-live` HOLD/verdict announcements + `role="alertdialog"` on operator-pause dialog + signature heading text "HOLD — operator pause" (Layout Templates signature placements)
- **Boundary:** cross-surface (webview ↔ IPC ↔ core)
- **Notes:** Frameless, desktop-bound, "no responsive breakpoints" (Design System) — no viewport/mobile matrix needed. Mobile surface is N/A (arch Surfaces: "mobile framework is N/A — desktop-only, host-bound"). Reliable selectors guaranteed by the Color-Only a11y rule (text/role pairing). Secondary to the CLI release gate.

(No web-spa/web-ssr, no api-service with inbound listener, no tui, no mobile, no desktop-native surfaces — security excerpt confirms "no web framework, no HTTP surface"; arch confirms desktop-only + Tauri webview, not native widgets.)

## 3. Test Harness Specification

The central architectural decision tests owns — how the agent drives the product end-to-end and observes state. Arch commits to the pattern (Test Harness Commitment: agent-driven, `scripts/agent-run.sh` is source of truth + release gate); this section captures the per-command requirements and signal-shape contract.

- **5-command requirements:**
  - `boot` — Conductor itself has **no daemon / no inbound listener** (security excerpt: "zero network exposure ... no inbound listener of its own"); `boot` reduces to a **preflight readiness gate** (arch Standard Contracts) that asserts the MCP `initialize` handshake (protocol `2024-11-05`), required-tool presence vs. pinned manifest, and a data-dir canary round-trip. Readiness signal: preflight returns `ready:true`; `ready:false` ⇒ all dependent scenarios report `blocked` (never silently downgraded — security excerpt). Precondition: live Pulse + `mcp-server` feature reachable (Creator Brief prerequisite).
  - `run` — invokes the scenario catalog: per-P-ID scenarios (P-001..P-060) and/or the three named dynamic scenarios (`error-baseline-spike`, `fingerprint-storm`, `restart-suppression`) plus `pii-scrub`, `connection-lifecycle`, `severity-lifecycle` (Creator Brief Must-Work). Exit-code semantics: 0 = all checks Pass; non-zero = at least one hard `Fail`; `blocked`/`ManualCheck`/`KnownResidual`/`CalibrationRegion` are reported states, NOT process-failure exits (assertion-policy split: model-interpretive claims are "calibration-region checks + report-for-human, never hard-failed" — Creator Brief Risk Tolerance). Runs `cargo build` + `cargo nextest`/`cargo test` (incl. golden tests) + `cargo clippy` in CI (arch CI/CD).
  - `status` — structured-state query via the Run report envelope (arch Standard Contracts), polled per-check. Fields the agent polls: `run_id`, `seed`, `scenario`, `p_ids`, `verdict ∈ {Pass, Fail, CalibrationRegion}`, `state ∈ {Pass, Fail, ManualCheck, KnownResidual, Blocked}`, `latency_ms`, `slo_tier`, `journal_emitted_at`, `read_back_observed_at`, `fingerprints`. Mechanism: read the `runs.db` row (embedded SQL) and/or the per-run JSONL journal — no HTTP endpoint exists (no listener).
  - `cleanup` — teardown of per-run on-disk artifacts (`runs/<run_id>.jsonl`, `runs/<run_id>.md`) and `runs.db` rows; must be idempotent (re-running a clean state is a no-op). Conductor must NOT touch Pulse (Creator Brief: "NO Pulse process management"). The `:4317` port-occupier fault (security Vector 6) must release its bind on cleanup. Agent verifies cleanup via file-absence check + `runs.db` row-count query.
  - `logs` — the **emission journal** is ground truth: per-run `<run_id>.jsonl` under `runs/`, wall-clock-stamped via `std::time::SystemTime`/`Instant` (security excerpt: "Writing journal/report wall-clock stamps from tokio's virtual clock — rejected") — this is "the left side of every SLO check" (Creator Brief). Plus sanitized stderr (Design System cli "Error output"). Retention: per-`run_id`, append-mostly on disk.
- **Status endpoint shape:** No HTTP/IPC status *endpoint* (no listener). The agent-parseable equivalent is the Run report envelope JSON / `runs.db` row: `{ run_id: string, seed: number, scenario: string, p_ids: string[], verdict: "Pass"|"Fail"|"CalibrationRegion", state: "Pass"|"Fail"|"ManualCheck"|"KnownResidual"|"Blocked", latency_ms: number, slo_tier: string, journal_emitted_at: timestamp, read_back_observed_at: timestamp, fingerprints: ... }` (arch Standard Contracts, Run report envelope). Artifacts must NOT leak absolute host paths or internal struct names (security excerpt).
- **Log format:** Structured JSONL — per-run journal is one JSON object per line (arch Test-Relevant Conventions: "per-run JSONL journals stemmed by `run_id`"); agent-parseable with jq / serde. Markdown report (`<run_id>.md`) is the human/agent prose mirror sharing the same envelope fields.
- **PID file location:** N/A — no daemon (security excerpt: "no inbound listener of its own"; arch: `conductor-cli` is a one-shot `#[tokio::main(flavor="current_thread")]` binary, not a long-running service).
- **Test data strategy:** Self-bootstrapping via **deterministic seeded synthetic generation** — `conductor-timeline` produces the same stream shape for the same scenario+seed (Creator Brief: "Deterministic under a seed"); no developer-seeded DB. Config fixtures are declarative scenario files (serde + garde) one-per-P-ID under `scenarios/` (arch Test-Relevant Conventions). Golden tests assert envelope/journal shape via cargo-nextest (security data-classification hint). Two scenario families (P-032/P-036) require a real git workspace with known recent commits (Creator Brief environment requirement).

## 4. Critical Paths

- **Path:** Headless deterministic scenario run with MCP read-back verification (e.g., `error-baseline-spike`)
- **Surfaces involved:** cli + ipc-internal (MCP read-back) + persistent-storage (`runs.db` / JSONL)
- **Verification signal:** after `conductor run error-baseline-spike --seed <s>`, exit code 0 AND `runs.db` row has `verdict=Pass` + `state=Pass` with `latency_ms` within `slo_tier` AND the per-run JSONL journal is written AND MCP read-back (`query_incident_list`/`retrieve_report`) confirms Pulse's reaction; same seed ⇒ same stream shape on re-run.
- **Source:** Creator Brief Must-Work ("End-to-end proof against a real Pulse: at minimum `error-baseline-spike` ... produce verified expected outcomes (MCP read-back where applicable)") + arch Critical paths hint.

- **Path:** Fingerprint-storm scenario (fault-injection burst + read-back)
- **Surfaces involved:** cli + ipc-internal (MCP read-back) + persistent-storage; exercises `conductor-faults` fingerprints
- **Verification signal:** `conductor run fingerprint-storm` exits 0 with per-P-ID `verdict`/`state` rows recorded; fingerprints field populated in the envelope; read-back confirms Pulse fingerprint reaction within SLO.
- **Source:** arch Critical paths hint (named verbatim) + Creator Brief Must-Work.

- **Path:** Restart-suppression incl. one bypass case (suppression/bypass logic)
- **Surfaces involved:** cli + ipc-internal + persistent-storage
- **Verification signal:** `conductor run restart-suppression` produces a hard pass/fail on suppression logic AND the bypass case reports its expected distinct outcome (Creator Brief Risk Tolerance: "suppression/bypass logic = hard pass/fail"); journal + `runs.db` row assert the deterministic stream.
- **Source:** arch Critical paths hint (named verbatim) + Creator Brief Must-Work ("`restart-suppression` (incl. one bypass case)").

- **Path:** Severity-lifecycle full pass observing auto-resolve + resolution summary
- **Surfaces involved:** cli + ipc-internal (`mark_incident_resolved` lifecycle verification) + persistent-storage
- **Verification signal:** one full `severity-lifecycle` pass observes Pulse auto-resolve and a resolution summary via MCP read-back; lifecycle timing asserted as hard pass/fail, severity *choice* asserted as CalibrationRegion (report-for-human, not hard-failed) — Creator Brief assertion-policy split.
- **Source:** Creator Brief Must-Work ("one full `severity-lifecycle` pass observes auto-resolve + resolution summary").

- **Path:** Known-residual classification path (P-032 reports as KnownResidual, not a surprise failure)
- **Surfaces involved:** cli + ipc-internal (`retrieve_report` with `degraded_mode`) + persistent-storage; runs inside a real git workspace (P-032 environment requirement)
- **Verification signal:** `conductor run` for P-032 produces `state=KnownResidual` (NOT `Fail`); a `degraded_mode` read-back result maps to `KnownResidual` (arch Standard Contracts); run report distinguishes it from pass/fail/manual-check.
- **Source:** Creator Brief Must-Work ("P-032 reports as known-residual (not as a surprise failure)") + Creator Brief environment requirement (P-032 git workspace).

- **Path:** Coverage-matrix completeness gate (all 60 P-IDs classified auto / drive+observe / static-only, zero gaps)
- **Surfaces involved:** cli + persistent-storage (`coverage-matrix.md` artifact); static assertion
- **Verification signal:** the generated `coverage-matrix.md` enumerates all 60 P-IDs with zero unclassified entries; a missing P-XXX fails the gate ("A P-XXX missing from the matrix is a defect" — Creator Brief); this is the "0.1.0 done" definition (arch Critical paths hint).
- **Source:** Creator Brief Must-Work ("`coverage-matrix.md` complete: all 60 P-IDs ... zero gaps") + arch ("complete `coverage-matrix.md` ... is the definition of done").

- **Path:** Both-surface parity — every catalog scenario runs from control panel AND headless, deterministic under fixed seed
- **Surfaces involved:** cli + desktop-webview + ipc-internal (Tauri commands/Channel)
- **Verification signal:** a scenario launched via the Tauri start command yields the same `runs.db` envelope (verdict/state/seed) as the headless `conductor run` for the same scenario+seed; emission journal written per run from both paths.
- **Source:** Creator Brief Must-Work ("Every catalog scenario runs from the control panel AND headless; deterministic under a fixed seed; emission journal written per run").

(7 paths — the maximum. `pii-scrub` and `connection-lifecycle` Must-Work scenarios are covered by E2E suite expansion in Phase 3 as additional auto-scenario instances of Path 1's pattern.)

## 5. Coverage Triggers

- **Trigger type:** security-vector-coverage (negative-test)
- **Source:** security plan Vector 1 (CLI input — seed/scenario flags + `CONDUCTOR_*` env path handles)
- **Required test type:** negative-test asserting `std::fs::canonicalize` + type/existence check rejects path-traversal `CONDUCTOR_*` handles BEFORE any `runs.db`/journal write or manifest read (path handles sit outside garde).

- **Trigger type:** security-vector-coverage / property-test
- **Source:** security plan Vector 2 (config-file parsing) + anti-pattern "Deserializing scenario config without garde validation at load"
- **Required test type:** negative-tests asserting garde rejects out-of-range config (error fraction outside [0,1], negative durations, p50>p95>p99 ordering violations, severity-mix sum violations) at load; property test over the `#[garde(custom)]` cross-field invariants.

- **Trigger type:** security-vector-coverage (negative-test)
- **Source:** security plan Vector 4 + anti-patterns (rmcp STDIO command/argument-injection CVE-2026-30623; protobuf-decode DoS RUSTSEC-2020-0002/2024-0437; silent preflight downgrade)
- **Required test type:** negative-tests asserting (a) `ANDROMEDA_PULSE_DATA_DIR` with injection metacharacters is rejected and passed only via `.env(...)` — never into argv; (b) the sidecar program path is fixed/hard-coded, not operator-chosen; (c) an empty/malformed canary round-trip becomes `blocked` (never false pass-as-empty); (d) bounded prost recursion does not panic on malformed child stdout.

- **Trigger type:** security-vector-coverage (negative-test)
- **Source:** security plan Vector 3 + anti-patterns (Tauri `shell-open` plugin RCE CVE-2025-31477; remote-origin iframe IPC bypass GHSA-57fm-592m-34r7; missing capabilities file; origin-confusion CVE-2026-42184)
- **Required test type:** negative-test/contract assertion that the Tauri capabilities file is deny-by-default exposing only the actual commands + one live-counter Channel; assertion that no `shell-open` plugin and no remote-origin iframe is present; dependency-pin assertion (`tauri` ≥ 2.10.3).

- **Trigger type:** security-vector-coverage (negative-test)
- **Source:** security plan anti-pattern "String concatenation / `format!` to build SQL for `runs.db`"
- **Required test type:** negative/static assertion that all `runs.db` access uses rusqlite bound parameters (no string-built SQL), even for synthetic data.

- **Trigger type:** contract-test (no live SaaS, no vendor sandbox)
- **Source:** arch Standard Contracts (Pinned MCP contract manifest — protocol `2024-11-05` + required-tool set) + anti-pattern "Pinning the rmcp client to a strict newer protocol default"
- **Required test type:** contract test asserting the preflight gate negotiates DOWN to `2024-11-05` (not strict-newer) and that the required-tool set matches the pinned `contracts/` manifest; mismatch ⇒ `blocked`.

- **Trigger type:** property-test (determinism discipline)
- **Source:** Creator Brief Risk Tolerance ("Determinism as a hard quality bar: same scenario + seed ⇒ same stream shape")
- **Required test type:** property/golden test asserting a fixed scenario+seed reproduces an identical emission-journal stream shape across runs (the deterministic-replay invariant on `conductor-timeline`).

- **Trigger type:** cross-surface-coordination
- **Source:** Creator Brief Must-Work ("Every catalog scenario runs from the control panel AND headless") + Design System (both surfaces share heartbeat/coverage-matrix/report/operator-pause patterns)
- **Required test type:** cross-surface coordination test asserting the Tauri-launched run and the headless `conductor run` produce identical envelope verdict/state for the same scenario+seed (parity of the two surfaces over the same core).

- **Trigger type:** chaos-test (fault-injection — but bounded, NOT load)
- **Source:** arch `conductor-faults` (ramps, silence, port-occupier) + security Vector 6 (`:4317` port-occupier) + Creator Brief anti-pattern ("NOT a load-tester ... 50k+ spans/sec saturation regimes are explicitly out")
- **Required test type:** fault-injection scenario tests asserting Pulse's reaction to silence/ramp/port-occupier faults within SLO — bounded "typical/high" profiles only (P-060); explicitly NOT a saturation/load test (that is Pulse's `perf_load_profiles.rs`).

- **Trigger type:** supply-chain audit (Minimal-tier residual-risk control)
- **Source:** security plan anti-patterns ("Running `cargo build --release` or merging without `cargo-audit` (and `cargo-deny`) green"; "Letting `Cargo.lock` drift"; toolchain ≥ 1.94.1 for tar-rs CVE-2026-33056)
- **Required test type:** dependency/supply-chain audit gate (`cargo-audit` + recommended `cargo-deny`) green over the `bundled`-SQLite-from-C + OTLP/gRPC/MCP tree; committed `Cargo.lock`; toolchain pinned ≥ 1.94.1.

(Note: artifact-sanitization discipline — security anti-patterns "Exposing internal error details" / "leak absolute host paths or internal seam-crate struct names" — folds into the Run-report envelope golden tests in section 3, asserting artifacts carry verdict/state/identity fields only.)

## 6. Test Tier

**Minimal (0)** — selected, with justification below distinguishing it from Standard.

**Justification:** The passed `security_tier` is **Minimal (0)** (security excerpt: single-developer, local-only, no-cloud, no-auth, zero network exposure, only self-generated synthetic data) — the strongest single tier signal. The product is effectively a **single primary surface** (the cli release gate); the desktop-webview and the two ipc-internal surfaces are explicitly secondary ("the GUI shell itself is convenience only, the CLI/headless path being the release gate" — arch Surfaces), and there is **no persistent USER data** — the lone store (`runs.db` + JSONL) holds only synthetic, self-generated telemetry classified `low`. These three facts (Minimal security tier + single release-gate surface + no user data) place it below Standard.

However, this is **Minimal at the upper boundary**, NOT a trivial CLI: the coverage scope is 8 workspace crates plus several capability surfaces (>5 entities), there are 7 critical paths, and the Creator Brief mandates "production-grade verification rigor" (Risk Tolerance) with a determinism hard-bar and an assertion-policy split. Accordingly, the Minimal baseline (unit tests + smoke E2E + dependency audit) must be **augmented** with the coverage triggers in section 5 — security-vector negative-tests (6 mapped vectors), the deterministic-replay property/golden tests, the MCP contract test against the pinned manifest, the cross-surface parity test, and bounded fault-injection (NOT load) — without escalating to Comprehensive, which the project explicitly disqualifies itself from (Creator Brief anti-patterns: "NOT a load-tester," "NO multi-target / distributed," local-only, no regulated/multi-tenant data, mobile N/A).
