I have all four files. Now I'll synthesize the plan. The tier is Minimal with a desktop-webview UI surface present, so per the output_format directive the active sections are 1, 2, 3, 4, 5, 6, 9, 10, 11, 12 (SKIP 7 and 8). Let me produce the filled template.

# A11y Plan — Conductor

_Generated by `/andromeda-a11y`. This file is the single source of
truth for all accessibility strategy + harness contracts + WCAG
conformance + SLO invariants. Read by downstream skills (route /
setup-project per D26 cross-cutting), a11y-pass per-phase, combine +
`/implement` (via phase loop)._

_Do not edit during phase loop runs (a11y-pass / combine / `/implement`).
Manual edits between runs are fine._

_**Agent-driven invariant:** every WCAG criterion described in this
plan MUST have at least one agent-runnable verification path
(axe-core JSON / pa11y JSON / Lighthouse a11y category JSON / WAVE
API JSON / structured violation JSON aligned to obs Section 6).
Manual screen reader / keyboard / contrast verification is
supplemental, never sole. ARIA additions require semantic HTML first
(canonical principle "semantic HTML first, ARIA second")._

---

## 1. A11y Scope Summary

_[ALL tiers — VERBATIM copy from a11y-scope.md]_

**A11y tier:** `Minimal (0)`
_Justification: One assertable UI surface (desktop-webview Tauri control panel; the cli surface is not-assertable and the project has no web/HTTP, mobile, or native-toolkit surface) with four must-be-accessible paths (scenario pick/start, operator-pause go/no-go, run-report view, operator-checklist) — at the lower band of the Minimal 2-5 path range. Security tier is Minimal (single-developer local-only no-cloud no-auth utility), and both tests tier (Minimal at the upper boundary) and obs tier (Minimal) are Minimal — a11y matches its upstream tiers with no ±1 divergence. There are zero WCAG compliance triggers and no creator-set WCAG mandate (target user is a single solo developer-operator with no a11y-priority population); the only triggers detected are token-driven (visual-discrimination from six state-color tokens, motion-sensitive from reduced-motion binding) which mandate specific assertions but do not escalate the tier._

**A11y scope (entities needing assertions):**

- **Entity:** Operator-pause go/no-go dialog (Proceed / Abort)
  - **Source:** design excerpt's User-Facing Error Surfaces + ARIA-Relevant Component Patterns ("Operator-pause go/no-go dialog"); layout excerpt's Focus Management Anchors (run-console-HOLD)
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Verdict / report-state lamp (six states: `Pass` / `CalibrationRegion` / `Fail` / `Blocked` / `Manual` / `Residual`)
  - **Source:** design excerpt's User-Facing Error Surfaces (`Fail` lamp) + ARIA-Relevant Component Patterns ("Verdict / report-state lamp")
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Coverage matrix (single-row-per-P-ID list)
  - **Source:** design excerpt's ARIA-Relevant Component Patterns ("Coverage matrix") + Loading/Error/Empty State Patterns (Coverage-matrix empty state)
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Operator-checklist (`ManualCheck` render — checkbox-per-item)
  - **Source:** design excerpt's User-Facing Error Surfaces ("Operator-checklist") + ARIA-Relevant Component Patterns ("Operator-checklist"); creator brief excerpt's Must-Work Scenarios (Run report surface)
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Scenario / suite picker + start/stop (combobox/listbox over shadcn `Command`/`Select`)
  - **Source:** design excerpt's ARIA-Relevant Component Patterns ("Scenario / suite picker + start/stop"); arch excerpt's Project Intent Summary critical paths hint (scenario/suite picker action, start/stop)
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Button (titlebar min/close, start/stop, Proceed/Abort — icon-only controls)
  - **Source:** design excerpt's ARIA-Relevant Component Patterns ("Button"); layout excerpt's Focus Management Anchors (desktop-webview window controls)
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Form input (scenario / seed fields — textbox)
  - **Source:** design excerpt's ARIA-Relevant Component Patterns ("Form input")
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** Run-report view (run-states: idle / live / HOLD / report-terminal; live emission counters + target status streaming display)
  - **Source:** layout excerpt's Layout Types per Surface (desktop-webview four run-states); arch excerpt's Project Intent Summary critical paths hint (run-report view, live emission counters via Tauri `Channel`); design excerpt's Loading/Error/Empty State Patterns (run-report empty / in-progress states, paused-count hold-point)
  - **Assertability:** assertable
  - **Reason if not fully assertable:** N/A

- **Entity:** conductor-cli (`agent-run` headless binary / `conductor-run`, `conductor-suite`, `conductor-report`)
  - **Source:** arch excerpt's Stack ("conductor-cli `agent-run` headless binary") + Surfaces (headless CLI/`agent-run`); design excerpt's Surfaces (cli); layout excerpt's Layout Types per Surface (cli)
  - **Assertability:** not-assertable
  - **Reason if not fully assertable:** CLI line-oriented terminal surface — no DOM / native widget tree; a11y tools (axe / Lighthouse / pa11y) have no applicability per arch excerpt's Stack. Manual keyboard/terminal discipline + ASCII status prefixes / NO_COLOR text-paired output is fallback only. Excluded from Sections 2 and 4.

Cross-check against security plan excerpt's A11y Compliance Triggers: "No a11y compliance triggers in security plan" — no regulated-surface → WCAG SC mandate to bind. No boundary-only zones: arch excerpt's Surfaces confirms "No web/HTTP surface — Conductor exposes no HTTP/network service of its own" and Tauri capabilities are deny-by-default with "no remote-origin iframe affecting a11y selectors" (tests excerpt's Coverage Triggers, Vector 3) — so no third-party iframe / CAPTCHA / vendor-widget boundary zone exists.

**A11y surfaces & assistive tech reach:**

- **Surface:** desktop-webview (Tauri 2 bundled webview / React 19 + Tailwind v4.1 + shadcn/ui over Radix Primitives; frameless window, no responsive breakpoints)
  - **Source:** arch excerpt's Stack (Tauri 2 desktop shell) + Surfaces (Tauri 2 desktop webview GUI); design excerpt's Surfaces (desktop-webview)
  - **Automated tool reach:** axe-core via DevTools/CDP protocol against the embedded webview (`@axe-core/puppeteer` / `@axe-core/playwright` against the webview DOM) + Lighthouse a11y category (Chrome DevTools / `lighthouse` CLI against the webview); per design excerpt's Surfaces "axe-core + Lighthouse + screen reader (NVDA / VoiceOver) against the webview DOM." Native menubar / system dialogs are out of scope by design (arch excerpt's Stack: "no native OS toasts by design, so no OS-level notification a11y surface"). pa11y (`pa11y` / `pa11y-ci`) is applicable in principle against the rendered DOM but requires CDP attach to the Tauri webview.
  - **Manual verification (supplemental):** NVDA (Windows) / VoiceOver (macOS) / Orca (Linux) — desktop-bound Windows/macOS/Linux target per design excerpt's Surfaces; supplemental to automated, never sole. No TalkBack/mobile SR (arch excerpt's Surfaces: "No mobile surface").
  - **ARIA roles inventory:** interactive component roles — `alertdialog` (operator-pause), `button` (titlebar / start-stop / Proceed-Abort), `combobox`/`listbox` (scenario/suite picker), `textbox` (scenario/seed form inputs), `checkbox` + group/region (operator-checklist); live region roles — `status` / `aria-live` (verdict/report-state lamp, live emission counters, HOLD phase-line `aria-live="assertive"`); list/listitem (or table semantics) for the coverage matrix; landmark roles NOT explicitly assigned in layouts — per layout excerpt's Heading Hierarchy Anchors, Heading role on phase line, Label role on footer status strip, frameless titlebar `data-tauri-drag-region`; main / banner / contentinfo equivalents derived in this plan (Section 4).
  - **Service identity tagging:** `service.name` = `"conductor-tauri"` (Tauri GUI) or `"conductor-ui"` (browser frontend), overrideable via `$CONDUCTOR_SERVICE_NAME`; `deployment.environment` = `CONDUCTOR_ENV` (fallback `"local"`) — per obs excerpt's Service Identity. A11y violation artifacts tag with these resource attributes for cross-correlation.
  - **Notes:** Tauri webview a11y tree is exposed via DevTools/CDP, not a standard browser endpoint — axe-core/Lighthouse must attach over CDP to the embedded webview rather than a localhost URL (arch excerpt's Stack). Single frameless window with four run-STATES (idle / live / HOLD / report-terminal), not four routes — no router, no breakpoints (layout excerpt's Layout Types) → no route-change focus surface, all state transitions are in-place. Virtual-scrolled coverage matrix: off-screen rows must remain reachable/announceable (design excerpt's ARIA-Relevant Component Patterns). VoiceOver macOS has no automated SR test — manual pass required.

(cli surface omitted here — flagged not-assertable in Section 1: no DOM / native widget tree, no automated a11y tool reach per arch excerpt's Stack and design excerpt's Surfaces.)

**A11y assertion harness specification:**

- **A11y testing tool pick:** Per-surface tool reach (Section 2): axe-core via DevTools/CDP (`@axe-core/puppeteer` / `@axe-core/playwright` against the Tauri webview) + Lighthouse a11y category as the candidate set for the single assertable surface (desktop-webview). Note (arch excerpt's Stack/Testing frameworks): the Rust stack is cargo-nextest / cargo test / golden tests with "no Playwright/Cypress/Selenium or equivalent browser E2E driver exists in the stack for a11y harness reuse" — so the tests' E2E-driver-reuse binding contract has no existing browser driver to piggyback on; this plan reconciles it (a11y aligns to upstream tests harness if/when one is added).
- **WCAG criteria mapping:** Minimal tier (see Section 6) → baseline only: **SC 2.1.1 Keyboard** + **SC 1.4.3 Contrast (Minimum)** + **SC 2.4.3 Focus Order**. Design-token-backed non-text minima also in baseline reach from design excerpt's A11y-Relevant Design Tokens: SC 1.4.11 Non-text Contrast 3:1 (`--count-nominal`, `--color-focus` tokens annotated to serve it) and SC 1.4.1 Use of Color / not-color-alone (every state token carries a text-label + glyph supplement). SC 2.3.3 Animation from Interactions is bound by design's global reduced-motion rule. No AA/AAA escalation triggers present (Section 5 = None) — tier label stays Minimal, no trigger-pulled AAA SCs added.
- **Structured violation JSON schema:** Binding contract — a11y violation events align to obs excerpt's Log Format JSON Schema (obs Section 6), reproduced verbatim:
  ```jsonl
  {
    "journal_emitted_at": "ISO-8601 from std::time::SystemTime",
    "run_id": "YYYY-MM-DDTHH-MM-SS-<suffix> (filesystem-safe hyphen-delimited)",
    "seed": "u64",
    "scenario": "string",
    "p_ids": ["P-001", "P-002", ...],
    "verdict": "Pass | Fail | CalibrationRegion",
    "state": "Pass | Fail | ManualCheck | KnownResidual | Blocked",
    "latency_ms": "integer or null (null for blocked rows)",
    "slo_tier": "<5s | <20s | <90s",
    "fingerprints": ["fingerprint1", "fingerprint2", ...] or empty array
  }
  ```
  A11y emissions align to this obs schema (a11y narrows; obs fixed the schema). Per tests excerpt's Status JSON shape constraint, artifacts MUST NOT leak absolute host paths or internal seam-crate struct names — a11y violation artifacts inherit that constraint. WCAG-violation field mapping onto this envelope is defined in Section 3 (Structured violation JSON schema) without diverging from the obs contract.
- **Focus management test harness:** No existing E2E driver to reuse (arch excerpt's Stack: no Playwright/Cypress/Selenium). Candidate reach is axe-core/CDP-driver Tab tracking against the Tauri webview; focus anchors to assert from layout excerpt's Focus Management Anchors: modal focus trap on run-console-HOLD (shadcn AlertDialog / Radix, dimmed/inert console, visible `--color-focus` ring on trapped region, Proceed/Abort in-trap targets), focus restoration on dialog close (Radix default, implied), keyboard-first global focus ring on every interactive control with `--border-emphasis` on active picker item so focus is never color-only. Obs has no focus spans yet (obs excerpt's Focus-Relevant Span Coverage: "No focus-relevant spans"); this plan recommends focus.shift / focus.trap.enter / focus.trap.exit / focus.restore for obs to add later.
- **Keyboard test harness:** Scripted key sequences against the desktop-webview only. From design excerpt's ARIA-Relevant Component Patterns keyboard contracts: dialog Tab/Shift+Tab cycle within trap + Enter/Space activate Proceed/Abort + Escape per convention; button Enter/Space; picker Arrow-navigate + Enter-select + Escape-close + type-ahead; checklist Space toggles a row; coverage-matrix keyboard-first row navigation; first-class start/stop/proceed/abort shortcuts (layout excerpt's Focus Management Anchors). CLI keyboard verification is manual only and out of harness scope (not-assertable surface).
- **Screen reader test pattern:** Manual, supplemental (never sole). NVDA (Windows) / VoiceOver (macOS) / Orca (Linux) pass against the webview DOM per design excerpt's Surfaces. Must-announce items from design excerpt: empty-state real prose ("No scenarios loaded", "No run yet"), in-progress "Run in progress" not announced as Fail, HOLD phase-line via `aria-live="assertive"`, frozen count value (does not blank/keep running), verdict lamp `aria-live` status announcement, operator-checklist unticked-count roll-up. No automated SR tool for the stack.
- **Contrast verification harness:** Programmatic contrast assertion reading design excerpt's A11y-Relevant Design Tokens (binding — this plan reproduces token names verbatim) — emits machine-verifiable ratio assertions per WCAG SC 1.4.3 (AA 4.5:1 normal / 3:1 large) and SC 1.4.11 (3:1 non-text). Token pairs to assert (foreground / background, verbatim from design excerpt): `--text-primary`/`--color-base` (SC 1.4.3 4.5:1 normal-text), `--text-secondary`/`--color-raised-1`, `--text-tertiary`/`--color-base`, `--text-muted`/`--color-inset`, `--color-id-cyan`/`--color-base`, `--count-nominal`/`--color-base` (SC 1.4.11 3:1 non-text status), `--count-hold`/`--color-base`, `--status-fail`/`--color-base`, `--color-focus`/`--color-base` (SC 1.4.11 3:1 non-text focus indicator). Focus-ring tokens `--color-focus` + `--border-emphasis` (selection vs focus distinction). Candidate: axe-core color-contrast rule against rendered DOM + custom assertion reading the named token pairs. Raw values stay in design-system.md (`[NO_RAW_DESIGN_VALUES]`); names are the cross-skill anchor.
- **CI integration:** GitHub Actions (arch excerpt's CI/CD Platform) currently runs `cargo build` + cargo-nextest/`cargo test` (incl. golden) + `cargo clippy` — "build + test gating only; dynamic end-to-end scenario proof requires a live Pulse and is an operator/local gate, not a CI gate." The desktop-webview a11y check therefore lands as an operator/local gate, not a CI gate (this plan keeps it operator/local-gated, scoping a headless webview-CDP step to the Windows runner — Section 9). Tests' 5-command harness contract names (boot / run / status / cleanup / logs — tests excerpt's Test Harness Contract) is the binding command surface a11y integration reuses; Section 9 specifies the `logs` command emits a11y results. No `npm test` lane exists in the Rust-first stack (arch excerpt's Stack).

**Critical paths (must-be-accessible):**

(cli / headless `conductor run` paths and tests' security-vector / build-gate coverage triggers are excluded — not-assertable, no rendered-UI surface. The only accessible surface is the Tauri 2 desktop-webview control panel, per creator brief excerpt's Must-Work Scenarios.)

- **Path:** Scenario/suite pick → start run (operator launches a scenario or suite from the control panel)
  - **Surfaces involved:** desktop-webview
  - **Required ARIA roles:** `combobox`/`listbox` (picker, `aria-expanded` / `aria-activedescendant`), `button` (start/stop, `aria-label` on icon-only), `textbox` (seed field, associated label)
  - **Required focus order:** focus reaches picker input (visible `--color-focus` ring) → Arrow-navigate options with type-ahead → Enter selects → focus to start control → activate; active picker item carries `--border-emphasis` so focus is never color-only
  - **Required WCAG SC coverage per tier (Minimal):** SC 2.1.1 Keyboard / SC 2.4.3 Focus Order / SC 1.4.3 Contrast (Minimum) (baseline); plus SC 4.1.2 Name/Role/Value (icon-only `aria-label`) and SC 1.4.1 Use of Color (disabled options not color-alone) inherent to the named patterns
  - **Source:** creator brief excerpt's Must-Work Scenarios ("Control panel (minimal UI) — scenario/suite picker, start/stop…"); arch excerpt's Project Intent critical paths hint; design excerpt's ARIA-Relevant Component Patterns (picker)

- **Path:** Operator-pause go/no-go (HOLD) → Proceed or Abort
  - **Surfaces involved:** desktop-webview
  - **Required ARIA roles:** `alertdialog` (`aria-modal`, focus trap), `button` (Proceed / Abort), live region `aria-live="assertive"` on the phase-line HOLD flip
  - **Required focus order:** focus moves into the dialog trap on open (over dimmed/inert console) → Tab/Shift+Tab cycle within trap across Proceed/Abort → Enter/Space activates → Escape per convention → focus restored on close (Radix default); Proceed disabled/in-flight during async committed step (announced via aria-disabled)
  - **Required WCAG SC coverage per tier (Minimal):** SC 2.1.1 Keyboard / SC 2.4.3 Focus Order / SC 1.4.3 Contrast (baseline); plus SC 2.1.2 No Keyboard Trap (trap must be escapable), SC 4.1.2 Name/Role/Value, and SC 2.3.3 Animation from Interactions (dialog fade + count tint dropped under reduced-motion) inherent to the named patterns
  - **Source:** creator brief excerpt's Must-Work Scenarios ("operator pauses … pause with an explicit instruction and resume on confirmation"); design excerpt's User-Facing Error Surfaces + ARIA-Relevant Component Patterns (operator-pause dialog); layout excerpt's Focus Management Anchors (run-console-HOLD)

- **Path:** View run report — verdict/state outcomes + emission journal cross-reference (idle → live → report-terminal states)
  - **Surfaces involved:** desktop-webview
  - **Required ARIA roles:** list/listitem (or table) for the P-ID coverage matrix (`aria-selected` / `aria-current` + `--border-emphasis` on selected row), `status` / `aria-live` for verdict lamp + live emission counters; real-prose empty/in-progress states
  - **Required focus order:** keyboard-first row navigation across the coverage matrix; off-screen virtual-scrolled rows remain reachable/announceable; live count updates announced via `aria-live` without stealing focus; empty/in-progress prose announced (not as a Fail)
  - **Required WCAG SC coverage per tier (Minimal):** SC 2.1.1 Keyboard / SC 2.4.3 Focus Order / SC 1.4.3 Contrast (baseline); plus SC 1.4.1 Use of Color (six verdict/state treatments each text-paired, not color-alone), SC 1.4.11 Non-text Contrast (status lamps 3:1), and SC 4.1.3 Status Messages (aria-live verdict/count) inherent to the named patterns
  - **Source:** creator brief excerpt's Must-Work Scenarios ("Run report surface" + live counters/target status); design excerpt's ARIA-Relevant Component Patterns (lamp, coverage matrix) + Loading/Error/Empty State Patterns; arch excerpt's Project Intent critical paths hint (run-report view, live emission counters via Tauri `Channel`)

- **Path:** Operator-checklist (`ManualCheck`) — tick yes/no items for claims with no programmatic read-back
  - **Surfaces involved:** desktop-webview
  - **Required ARIA roles:** `checkbox` per item within a group/region container (`aria-checked` per row), unticked-count roll-up announced; each item exposes induced-state + expected-observation text; `ManualCheck` lamp links to this checklist
  - **Required focus order:** keyboard-first; Space toggles a focused row; unticked-count footer roll-up reachable so an incomplete pass is not mistaken for a finished run
  - **Required WCAG SC coverage per tier (Minimal):** SC 2.1.1 Keyboard / SC 2.4.3 Focus Order / SC 1.4.3 Contrast (baseline); plus SC 1.4.1 Use of Color (neutral-lavender checkbox glyph paired with text), SC 4.1.2 Name/Role/Value, and SC 4.1.3 Status Messages (unticked-count roll-up) inherent to the named patterns
  - **Source:** creator brief excerpt's Must-Work Scenarios ("manual-check items render as an operator checklist … with induced state + expected observation"); design excerpt's User-Facing Error Surfaces + ARIA-Relevant Component Patterns (operator-checklist)

(Note: tests excerpt's Critical Paths are predominantly headless `conductor run` / coverage-matrix-gate / both-surface-parity flows with no rendered-UI surface — not-assertable, excluded. Both-surface-parity's a11y-relevant half (the GUI-launched run rendering the same accessible envelope) is covered within the three desktop-webview paths above; tests excerpt's Coverage Trigger "Cross-surface coordination" names axe-core/pa11y on the desktop-webview confirming this.)

**A11y triggers:**

- **Trigger type:** visual-discrimination
  - **Source:** design excerpt's A11y-Relevant Design Tokens — State color tokens (six color-coded states: `--count-nominal` success, `--count-hold` warning, `--status-fail` error, `--count-blocked` info/blocked, `--status-manual` info/manual, `--status-residual` info/residual) + Verdict/report-state lamp with "six distinct visual+text treatments" (design excerpt's ARIA-Relevant Component Patterns)
  - **Required assertion:** axe-core color-contrast rule across the named foreground/background token pairs in Section 3 + machine-verifiable not-color-alone assertion that every state carries its text label (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) + glyph supplement (SC 1.4.1 Use of Color, SC 1.4.3 Contrast, SC 1.4.11 Non-text Contrast). This is a design-token-driven assertion, not a creator/compliance directive, so it does NOT escalate tier above Minimal.

- **Trigger type:** motion-sensitive (token-driven only; no creator vestibular ask)
  - **Source:** design excerpt's A11y-Relevant Design Tokens — Motion/transition tokens (`--motion-micro`, `--ease-quiet`) + global reduced-motion-preference rule "drops all animations/transitions (binding with a11y SC 2.3.3, including the held-count tint transition)"; design's Paused-count hold-point ("all accompanying color transitions are dropped under reduced-motion preference")
  - **Required assertion:** verify that under `prefers-reduced-motion`, the count tint (green → amber → slate-violet), focus-ring fade-in, status-light color transition, and operator-pause dialog fade are all dropped (SC 2.3.3 Animation from Interactions). Creator brief names no motion-sickness/vestibular requirement (creator brief excerpt's A11y Anti-Patterns = N/A), so this is token-binding only and does not escalate tier.

Cross-input check against expected triggers:
- WCAG compliance regime → wcag-mapping: **none** — security plan excerpt's A11y Compliance Triggers = "No a11y compliance triggers."
- Hardened tier → regulated-compliance: **none** — security_tier = Minimal (passed variable + security plan excerpt's Security Tier).
- Vestibular ask → motion-sensitive: only the token-driven half present (no creator vestibular language); recorded above as token-binding.
- I18n / RTL → multi-language: **none** — no language/RTL tokens in design excerpt; arch excerpt's Project Intent target users = solo developer, no international population.
- Explicit a11y scenario → screen-reader-priority / keyboard-only / cognitive: **none explicit** — creator brief excerpt's Rigor Hints: "No explicit WCAG target named," no blind/low-vision/cognitive user population; arch excerpt's Project Intent: "No a11y-priority user signals present." (Keyboard-first design exists structurally but is not a creator priority ask.)
- Multi-platform touch → target-size: **none** — arch excerpt's Surfaces: "No mobile surface — Desktop-only, host-bound"; design excerpt's Target size tokens: no dedicated tap-target tokens (desktop pointer + keyboard, no touch surface).
- Many state colors → visual-discrimination: **yes** (recorded above).

This tier is the SINGLE SOURCE OF TRUTH for how much accessibility
rigor downstream skills (a11y-pass per phase + cross-cutting reads
from route / setup-project) should apply.

---

## 2. A11y Strategy

_[ALL tiers — POUR principles signal pyramid + agent-runnable invariants]_

**A11y signal pyramid (per tier — POUR principles):**

| Principle | Coverage focus | Tier-specific depth |
|-----------|---------------|---------------------|
| Perceivable | Color contrast (`--text-primary`/`--color-base` 4.5:1, `--count-nominal`/`--color-base` 3:1) / not-color-alone six-state lamps / real-prose empty states | Minimal: SC 1.4.3 + SC 1.4.11 + SC 1.4.1 via axe-core `color-contrast` + colorjs.io token-name assertion |
| Operable | Keyboard-first nav (picker Arrow/Enter, checklist Space, dialog Tab-trap+Escape) / focus trap on run-console-HOLD / focus restoration | Minimal: SC 2.1.1 + SC 2.4.3 + SC 2.1.2 via puppeteer `page.keyboard` over CDP |
| Understandable | Form labels (scenario/seed `<label>` + textbox) / aria-describedby hint/error association / in-progress prose never downgraded to Fail | Minimal: SC 4.1.2 + SC 3.3.2 within named patterns (no AA escalation) |
| Robust | shadcn/ui-over-Radix semantic primitives / `alertdialog` / `aria-live` status / list-listitem coverage matrix | Minimal: SC 4.1.2 + SC 4.1.3 + SC 1.3.1 via axe-core `button-name`/`aria-*`/`list` rules |
| Cognitive | (N/A — no cognitive trigger; tier ≠ Comprehensive; security_tier=Minimal) | Not present |

**Agent-runnable invariants (apply across all WCAG SCs):**
- Every WCAG SC has at least one machine-verifiable verification path — axe-core v4.12.0 `AxeResults.violations[]` JSON (`@axe-core/puppeteer` v4.11.3 over CDP) / Lighthouse v13.0.3 a11y-category JSON / pa11y v9.1.1 JSON, all aligned to obs Section 6 envelope
- No manual screen reader / keyboard / contrast as ONLY method — NVDA/VoiceOver/Orca are supplemental to the axe-core + colorjs.io + puppeteer-keyboard automated baseline
- No ARIA additions on top of non-semantic HTML — `<button>` for start/stop/Proceed/Abort, shadcn `Command`/`Select` (Radix) for picker, native checkbox for checklist
- No WCAG conformance claim without machine-verifiable evidence (axe-core / pa11y / Lighthouse / colorjs.io JSON)
- No vendor-locked a11y tool as ONLY method — axe DevTools browser extension (UI-only) explicitly NOT used; JSON-producing axe-core library is the engine

**Naming conventions** (cross-section anchors):
- **WCAG SC IDs:** primary anchor (`SC 1.4.3`, `SC 2.1.1`, `SC 2.4.3`, `SC 4.1.2`, `SC 1.4.11`, `SC 1.4.1`, `SC 4.1.3`, `SC 2.3.3`)
- **ARIA roles:** per WAI-ARIA Authoring Practices Guide (`https://www.w3.org/WAI/ARIA/apg/`)
- **Design tokens:** verbatim names per binding contract (`--text-primary` / `--color-base` / `--color-focus` / `--border-emphasis` / `--count-nominal` / `--motion-micro`; raw values stay in design-system.md per `[NO_RAW_DESIGN_VALUES]`)
- **Resource attributes:** `service.name` = `"conductor-tauri"` / `"conductor-ui"`, `deployment.environment` = `CONDUCTOR_ENV` (fallback `"local"`) per obs Section 3 Service Identity (binding)

---

## 3. A11y Assertion Harness Contract

_[ALL tiers — central agent-driven mechanism; Section consumed by
setup-project to materialize a11y CI gates + axe-core integration +
contrast verification harness + screen reader test specs]_

This section specifies the SPECIFIC contract for how a11y assertions
run from CI / dev / and emit machine-parseable violation JSON.

### A11y testing tool pick

- **Primary tool per surface:** desktop-webview → **axe-core v4.12.0** injected via **`@axe-core/puppeteer` v4.11.3** attaching to the Tauri **WebView2** CDP endpoint (`puppeteer.connect({ browserURL: 'http://localhost:9222' })`, then `new AxePuppeteer(page).analyze()` → `AxeResults` JSON). Secondary coarse gate: **Lighthouse v13.0.3** (`lighthouse --only-categories=accessibility --output=json --port=9222`) for the `categories.accessibility.score` rollup. Tertiary SC-tagged runner: **pa11y v9.1.1 / pa11y-ci v4.1.1** with `runners: ['axe']` against the same CDP target. The cli surface is not-assertable (no axe/Lighthouse/pa11y reach) — no tool pick.
- **Configuration:** axe-core tag scope for Minimal baseline — `withTags(['wcag2a','wcag2aa','wcag21aa'])`; this lights `color-contrast` (SC 1.4.3 / SC 1.4.11), `button-name`/`aria-*`/`label` (SC 4.1.2), `aria-live`/status (SC 4.1.3), `list`/`listitem`/`aria-required-children` (SC 1.3.1). WCAG 2.2 rules ship off-by-default and are NOT added (no AA/AAA escalation trigger — Section 5 = None, so `wcag22aa` stays unset). **Binding constraint:** Tauri CDP is Windows-only (WebView2 exposes CDP via `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222`; macOS WKWebView / Linux WebKitGTK expose no CDP) — the automated harness runs against the Windows WebView2 build only; macOS/Linux fall back to manual NVDA/VoiceOver/Orca passes.

### WCAG criteria mapping

- **Tier coverage:**
  - **Minimal (selected):** baseline **SC 2.1.1 Keyboard** + **SC 1.4.3 Contrast (Minimum)** + **SC 2.4.3 Focus Order**, augmented by the design-token-bound **SC 1.4.11 Non-text Contrast** (3:1 status lamps + focus ring), **SC 1.4.1 Use of Color** (six-state not-color-alone), **SC 2.3.3 Animation from Interactions** (reduced-motion drop), plus pattern-inherent **SC 4.1.2 Name/Role/Value**, **SC 4.1.3 Status Messages**, **SC 2.1.2 No Keyboard Trap**, **SC 1.3.1 Info and Relationships**.
  - Standard: WCAG 2.1 AA full (~50 SCs) — not selected.
  - Comprehensive: WCAG 2.2 AAA + cognitive — not selected.
- **Compliance trigger override:** none — security plan excerpt's A11y Compliance Triggers = "No a11y compliance triggers" (no Section 508 / EU Accessibility Act / ADA mandate); security_tier=Minimal. No WCAG level mandate to bind beyond the Minimal baseline + token-driven SCs above.

### Structured violation JSON schema

Binding contract — see upstream-context Section 6 Obs Plan Excerpt → Log Format JSON Schema. A11y emissions align to the obs schema (a11y narrows; obs fixed it), reproduced verbatim:

```jsonl
{
  "journal_emitted_at": "ISO-8601 from std::time::SystemTime",
  "run_id": "YYYY-MM-DDTHH-MM-SS-<suffix> (filesystem-safe hyphen-delimited)",
  "seed": "u64",
  "scenario": "string",
  "p_ids": ["P-001", "P-002", ...],
  "verdict": "Pass | Fail | CalibrationRegion",
  "state": "Pass | Fail | ManualCheck | KnownResidual | Blocked",
  "latency_ms": "integer or null (null for blocked rows)",
  "slo_tier": "<5s | <20s | <90s",
  "fingerprints": ["fingerprint1", "fingerprint2", ...] or empty array
}
```

WCAG-violation field mapping onto this envelope (without diverging from the obs contract): each axe-core `violations[]` entry folds into a per-run a11y artifact whose envelope `state` = `Fail` when `violations.length > 0` else `Pass`; the axe `id` (e.g., `color-contrast`, `button-name`), `impact`, WCAG `tags`, and node `target` selector ride inside the `fingerprints[]` array (axe rule-id + WCAG-SC tag + selector tuple) so no new top-level fields are introduced. The colorjs.io token-name assertion and puppeteer-keyboard PASS/FAIL emit the same way. Per tests' Status JSON constraint, artifacts MUST NOT leak absolute host paths or internal seam-crate struct names — node `target` selectors are stored as DOM-relative selectors / `data-testid`, never filesystem paths. Resource attributes `service.name` = `"conductor-tauri"` / `"conductor-ui"` and `deployment.environment` tag each artifact for obs cross-correlation.

### Focus management test harness

- **Driver:** No existing E2E driver in the Rust-first stack to reuse (arch excerpt: no Playwright/Cypress/Selenium) — focus assertions piggyback on the same **`@axe-core/puppeteer` v4.11.3** CDP session via **`puppeteer-core` v24.10.1** `page.keyboard` rather than a dedicated E2E framework (`cypress-real-events` / `cy.tab()` NOT applicable — no Cypress in stack). Windows-WebView2-only CDP constraint applies.
- **Pattern:** scripted Tab / Shift+Tab traversal reading `document.activeElement` (`page.evaluate(() => document.activeElement?.getAttribute('data-testid'))`) after each key, asserting: run-console-HOLD `alertdialog` trap entry on open (over dimmed/inert console), Tab/Shift+Tab cycle staying inside the trap across Proceed/Abort, Escape-escapable trap (SC 2.1.2), focus restoration to the triggering control on dialog close (Radix default), and visible `--color-focus` ring on the active element (SC 2.4.7, paired with the contrast harness for the 3:1 ring). Recommended obs focus spans for obs to add later: `focus.shift` / `focus.trap.enter` / `focus.trap.exit` / `focus.restore` (obs Section 4 currently has no focus-relevant spans).

### Keyboard test harness

- **Sequences per ARIA pattern** (from design excerpt's keyboard contracts):
  - Button (titlebar / start-stop / Proceed-Abort): Enter / Space invokes default action
  - Dialog (`alertdialog` operator-pause): Tab/Shift+Tab cycle within trap; Escape closes per convention; Enter/Space activates Proceed/Abort
  - Combobox (scenario/suite picker): Arrow Down/Up navigates options; Enter selects; Escape closes; type-ahead filtering
  - Checkbox (operator-checklist row): Space toggles a focused row
  - Coverage matrix (list/listitem): keyboard-first row navigation across rows incl. off-screen virtual-scrolled rows
  - First-class shortcuts: start / stop / proceed / abort (layout excerpt's Focus Management Anchors)
- **Tooling:** **`puppeteer-core` v24.10.1** `page.keyboard.press('Tab' | 'Shift+Tab' | 'ArrowDown' | 'Enter' | 'Space' | 'Escape')` over the WebView2 CDP session (`Input.dispatchKeyEvent`); results emit as structured PASS/FAIL JSON folded into the obs Section 6 envelope. CLI keyboard verification is manual-only and out of harness scope (not-assertable surface).

### Screen reader test pattern

- **Per-surface test spec:** desktop-webview → **NVDA (Windows)** / **VoiceOver (macOS)** / **Orca (Linux)** against the webview DOM (no TalkBack/mobile SR — no mobile surface). No automated SR tool exists for the stack.
- **Manual pass spec format:** structured per-state (idle / live / HOLD / report-terminal) text + expected SR output. Must-announce items: empty-state real prose ("No scenarios loaded", "No run yet — pick a scenario/suite to begin"); in-progress "Run in progress" announced as status NOT Fail; HOLD phase-line flip via `aria-live="assertive"`; frozen count value (does not blank or keep running); verdict lamp `aria-live` status announcement on state change; operator-checklist unticked-count roll-up.
- **Supplemental to automated:** SR pass spec NEVER sole verification — axe-core covers SC 4.1.2 Name/Role/Value (`button-name`/`label`/`aria-*` rules) and SC 4.1.3 Status Messages (`aria-live`/status) programmatically; the SR pass verifies runtime announcement quality only. VoiceOver macOS has no automated SR test — manual pass required.

### Contrast verification harness

- **Source-of-truth tokens:** design token names from upstream-context Section 3 A11y-Relevant Design Tokens, reproduced verbatim — `--text-primary` / `--text-secondary` / `--text-tertiary` / `--text-muted` / `--color-id-cyan` / `--count-nominal` / `--count-hold` / `--status-fail` / `--color-focus` (foregrounds) over `--color-base` / `--color-raised-1` / `--color-inset` (backgrounds), plus `--border-emphasis` for the selection-vs-focus distinction. Never invent tokens; raw values stay in design-system.md (`[NO_RAW_DESIGN_VALUES]`).
- **Verification tool:** axe-core v4.12.0 `color-contrast` rule (rendered-DOM ratios after Tailwind compiles tokens) + **`colorjs.io` v0.6.1** token-name-bound assertion (`new Color(fg).contrast(bg, "WCAG21")`) that resolves each named token pair from the compiled CSS custom properties and emits machine-readable `{ token_fg, token_bg, ratio, required, pass }` JSON folded into the obs Section 6 envelope. (`chroma-js` v3.2.0 is an accepted alternative — either one, not both; `wcag-contrast` rejected as stale, last publish 2019.) axe measures rendered color only; colorjs.io supplies the token-NAME-level assertion the binding contract requires.
- **WCAG SC mapping:** **SC 1.4.3 Contrast (Minimum)** (AA 4.5:1 normal / 3:1 large — `--text-primary`/`--color-base` etc.) + **SC 1.4.11 Non-text Contrast** (AA 3:1 — `--count-nominal`/`--color-base` status lamps + `--color-focus`/`--color-base` focus ring). SC 1.4.6 Enhanced (7:1) NOT in Minimal baseline.

### CI integration

- **Pipeline integration:** GitHub Actions (arch excerpt's CI/CD Platform) currently gates `cargo build` + cargo-nextest/`cargo test` (incl. golden) + `cargo clippy` only ("build + test gating only; dynamic end-to-end scenario proof requires a live Pulse"). The desktop-webview axe-core/CDP step lands as an **operator/local gate, not a CI gate**, because (a) dynamic scenario proof needs a live Pulse and (b) CDP attach is Windows-WebView2-only. It reuses tests' binding 5-command harness surface (boot / run / status / cleanup / logs).
- **Command:** no `npm test` lane exists in the Rust-first stack — the a11y JSON step is an added Node+puppeteer step gated to `windows-latest` (single-OS CI "on the dev OS target") that launches the Tauri app with `--remote-debugging-port=9222`, attaches, runs axe-core + colorjs.io + puppeteer-keyboard, and surfaces results through tests' **`logs`** command (the artifact-emitting command of the 5-command contract).
- **Artifact:** structured violation JSON (axe `violations[]` + colorjs.io ratio assertions + keyboard PASS/FAIL) uploaded via `actions/upload-artifact`; the step fails (non-zero exit) when `violations.length > 0`; per-PR diff against base for new WCAG AA violations. Artifacts tag `service.name` / `deployment.environment` per obs Section 3.

### Bootstrap phases (derive for route / setup-project)

_[ALL tiers — explicit derivation hint for downstream consumers
per D26 chain. route reads this to plan bootstrap phase ordering;
setup-project reads this to materialize phase scaffolding.]_

The downstream skills derive the following bootstrap phases from
the contract above. Listed for explicitness — route may reorder /
combine, setup-project may add stack-specific intermediate steps.

- **a11y-tooling-install:** `npm install --save-dev axe-core@4.12.0 @axe-core/puppeteer@4.11.3 puppeteer-core@24.10.1 lighthouse@13.0.3 pa11y@9.1.1` and configure axe `withTags(['wcag2a','wcag2aa','wcag21aa'])` per the WCAG criteria mapping (Minimal — no `wcag22aa`).
- **focus-management-library-install:** no separate focus library needed — focus trap + restoration are provided by **shadcn AlertDialog over Radix Primitives** (already in the stack per design excerpt); harness asserts the Radix-default trap/restore behavior via the puppeteer-CDP session (no `focus-trap-react` / `react-aria` install).
- **aria-component-library-install:** **shadcn/ui over Radix Primitives** is the ARIA component library (already in the stack) — picker = shadcn `Command`/`Select`, dialog = shadcn `AlertDialog`; no additional ARIA library install.
- **contrast-verification-harness-setup:** `npm install --save-dev colorjs.io@0.6.1` and wire the token-name checker that resolves the named pairs (`--text-primary`/`--color-base` … `--color-focus`/`--color-base`) from the compiled stylesheet and emits PASS/FAIL JSON per pair.
- **screen-reader-test-spec-setup:** scaffold per-state NVDA (Windows) / VoiceOver (macOS) / Orca (Linux) manual pass-spec files per the Screen reader test pattern (must-announce list above); supplemental, not CI-gated.
- **a11y-ci-gate-wire:** wire the Windows-runner Node+puppeteer a11y step into tests' 5-command discipline emitting through `logs`, operator/local-gated per CI integration above.
- **violation-json-emission-wire:** emit structured violation JSON aligned to obs Section 6 log format (binding), folding axe rule-id + WCAG-SC tag + selector into `fingerprints[]` per the Structured violation JSON schema above.

route uses this list to plan phase ordering (typically:
a11y-tooling-install → focus-management-library-install →
aria-component-library-install → contrast-verification-harness-setup
→ screen-reader-test-spec-setup → a11y-ci-gate-wire →
violation-json-emission-wire). setup-project uses this list to
materialize each phase's bootstrap script + dependency list +
verification command.

---

## 4. ARIA Patterns & Roles

_[ALL tiers]_

**Per-surface landmark roles inventory:**

| Surface | Required landmarks | Optional landmarks |
|---------|-------------------|-------------------|
| desktop-webview (Tauri frameless window) | `main` (run-console/report body), `contentinfo` (footer status strip — Label role) | `banner` (frameless titlebar region, `data-tauri-drag-region`) |
| cli | (not-assertable — non-DOM line-oriented stdout; no ARIA / landmarks) | n/a |

(Landmark roles are NOT explicitly assigned in layouts — layout excerpt's Heading Hierarchy Anchors. This plan derives WCAG SC 1.3.1 + SC 2.4.6 defaults: single `h1`-equivalent on the phase-line Heading role per state, `main` for the console/report body, `contentinfo` for the footer status strip; the frameless titlebar carries `banner`. No `navigation` landmark — single frameless window with four in-place run-STATES, not routes; no router, no nav region.)

**Per-component pattern catalog** (cross-reference upstream-context
Section 3 ARIA-Relevant Component Patterns):

| Pattern | ARIA role | Key states | Keyboard contract | WCAG SCs | Tool reach (desktop-webview) |
|---------|-----------|------------|-------------------|----------|------------------------------|
| Operator-pause dialog | `alertdialog` + `aria-modal="true"` (shadcn AlertDialog/Radix) | `aria-labelledby` (header: frozen-count + step index); Proceed `aria-disabled` in-flight | Tab/Shift+Tab cycle in trap; Enter/Space activate Proceed/Abort; Escape per convention | 4.1.2, 2.1.2, 2.4.3, 2.3.3 | axe-core `aria-*` + puppeteer-CDP Tab/Escape trap assertion |
| Button (titlebar / start-stop / Proceed-Abort) | `button` (semantic `<button>`) | `aria-label` on every icon-only control; `disabled`/`aria-disabled` in-flight/unavailable | Enter / Space activate | 4.1.2, 1.4.1 | axe-core `button-name` + puppeteer Enter/Space |
| Verdict / report-state lamp | `status` (live-region) or text with accessible name | paired text label always present (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`); `aria-live` on state change; six visual+text treatments; `Fail` held motionless | non-interactive (no key contract) except `ManualCheck` lamp links to checklist | 4.1.3, 1.4.1, 1.4.11 | axe-core `aria-live`/contrast + colorjs.io 3:1 lamp pairs |
| Coverage matrix (single-row-per-P-ID) | `list` / `listitem` (or table semantics) | selected row `aria-selected`/`aria-current` + `--border-emphasis`; empty state real prose; virtual-scrolled off-screen rows reachable/announceable | keyboard-first row navigation | 1.3.1, 2.1.1, 2.4.3 | axe-core `list`/`listitem`/`aria-required-children` + puppeteer row-nav |
| Operator-checklist (`ManualCheck`) | `checkbox` per item, group/region container | `aria-checked` per row; unticked-count roll-up announced; each item exposes induced-state + expected-observation text | Space toggles a focused row | 4.1.2, 4.1.3, 1.4.1 | axe-core `aria-*`/`label` + puppeteer Space-toggle |
| Scenario / suite picker | `combobox` / `listbox` (shadcn `Command`/`Select` over Radix) | `aria-expanded` / `aria-activedescendant`; disabled options not color-alone; `--color-focus` ring on input | Arrow navigate; Enter select; Escape close; type-ahead | 4.1.2, 2.1.1, 1.4.1 | axe-core `aria-*` + puppeteer Arrow/Enter/Escape |
| Form input (scenario / seed) | `textbox` (semantic `<input>`) | associated `<label>`; `--color-focus` ring on focus; error/hint via `aria-describedby` | standard text-entry | 3.3.2, 4.1.2 | axe-core `label`/`aria-describedby` |

**Naming convention:** ARIA role names per WAI-ARIA Authoring
Practices Guide. Semantic HTML first (`<button>` for start/stop/Proceed/Abort, `<input>` for scenario/seed fields, native checkbox for checklist rows, `<main>`/`<footer>`); ARIA second (only where HTML semantics insufficient — `alertdialog`/`aria-modal` via shadcn AlertDialog, `status`/`aria-live` on the lamp, `combobox` via shadcn `Command`/`Select`).

(cli surface skipped — not-assertable per Section 1; no DOM/ARIA reach.)

(See `## 11. A11y Anti-Patterns` § ARIA for ARIA-level bans.)

---

## 5. Keyboard Navigation

_[SKIP if `a11y_tier=Minimal` AND no UI surface — desktop-webview UI surface present, so INCLUDED]_

**Focus order per layout** (cross-reference upstream-context Section 4 Focus Management Anchors):
- **run-console-idle:** titlebar window controls (icon-only, derived accessible names) → scenario/suite picker input (`--color-focus` ring) → Arrow-navigate options + type-ahead → Enter selects → seed `textbox` → start control → activate. Active picker item carries `--border-emphasis` so focus is never color-only (SC 2.4.3).
- **run-console-live:** keyboard-first row navigation across the coverage matrix (incl. off-screen virtual-scrolled rows); live emission counter updates announced via `aria-live` without stealing focus (SC 4.1.3).
- **run-console-HOLD:** focus moves into the `alertdialog` trap on open over the dimmed/inert console → Tab/Shift+Tab cycle across Proceed/Abort → Enter/Space activates → focus restored to the triggering control on close (SC 2.4.3, SC 2.1.2).
- **run-report-terminal:** keyboard-first coverage-matrix row navigation; selected row marked `aria-selected`/`aria-current` + `--border-emphasis`; operator-checklist `checkbox` rows reachable; unticked-count footer roll-up reachable.

**Skip links:**
- Not applicable — single frameless window with four in-place run-STATES (no router, no routes, no repeated nav blocks to bypass per layout excerpt's Layout Types). SC 2.4.1 Bypass Blocks has no repeated-block target on this single-region console; `main` landmark provides the primary-content anchor instead.

**Focus trap (modals / overlays):**
- run-console-HOLD operator-pause `alertdialog` (shadcn AlertDialog / Radix): focus moves into the trap on open; Tab cycles within across Proceed/Abort; Shift+Tab cycles backward; Escape closes (trap MUST be escapable — SC 2.1.2 No Keyboard Trap); focus restores to the triggering control on close (Radix default).
- Per WCAG SC 2.1.2 No Keyboard Trap + SC 2.4.3 Focus Order.

**Focus restoration:**
- Operator-pause dialog close → focus to triggering control (Radix AlertDialog default, layout excerpt's Focus Management Anchors).
- No route-change focus surface — four run-STATES are in-place transitions, not navigations; the `aria-live` phase-line flip announces HOLD without a focus move that steals from the trap.

**Per-surface keyboard shortcuts:**
- First-class start / stop / proceed / abort shortcuts via Radix primitives (layout excerpt's Focus Management Anchors). Must not conflict with assistive-tech shortcuts. (Creator brief names no additional explicit shortcut asks beyond these first-class controls.)

**Test harness pattern:** scripted Tab / Shift+Tab / Arrow / Enter / Space / Escape sequences via `puppeteer-core` v24.10.1 `page.keyboard.press()` over the WebView2 CDP session per `## 3. A11y Assertion Harness Contract` § Keyboard test harness, reading `document.activeElement` after each key; asserts SC 2.1.1 / SC 2.4.3 / SC 2.1.2 / SC 2.4.7.

(cli keyboard verification is manual-only, out of harness scope — not-assertable surface, no focus-trap concept; `inquire` confirm is `isatty`-gated and headless skips the prompt.)

(See `## 11. A11y Anti-Patterns` § Keyboard for keyboard-level bans.)

---

## 6. Visual Design Verification

_[ALL tiers — BINDING to design tokens from upstream-context Section 3]_

**Source-of-truth:** design token names from upstream-context Section 3 A11y-Relevant Design Tokens. Reproduced verbatim; no invented tokens; no contradiction of design's token names. Raw values stay in design-system.md (`[NO_RAW_DESIGN_VALUES]`); names are the cross-skill anchor.

### Color contrast pairs

For each foreground/background token pair from upstream-context Section 3:

| Foreground token | Background token | Context | WCAG SC | Required ratio |
|------------------|------------------|---------|---------|----------------|
| `--text-primary` | `--color-base` | phase-line headline / primary labels (body/heading text) | SC 1.4.3 (AA) | 4.5:1 |
| `--text-secondary` | `--color-raised-1` | run-report body prose on raised card | SC 1.4.3 (AA) | 4.5:1 |
| `--text-tertiary` | `--color-base` | metadata / timestamps / captions / empty-state prose (small text) | SC 1.4.3 (AA) | 4.5:1 |
| `--text-muted` | `--color-inset` | disabled labels / input placeholders | SC 1.4.3 (AA) | 4.5:1 |
| `--color-id-cyan` | `--color-base` | monospace status-tier text (count / P-IDs / run_id / SLO timings / fingerprints) | SC 1.4.3 (AA) | 4.5:1 |
| `--count-nominal` | `--color-base` | nominal-green / `Pass` status (non-text status indicator) | SC 1.4.11 (AA) | 3:1 |
| `--count-hold` | `--color-base` | hold-amber `CalibrationRegion` / HOLD status | SC 1.4.11 (AA) | 3:1 |
| `--status-fail` | `--color-base` | `Fail` verdict text / lamp | SC 1.4.11 (AA) | 3:1 |
| `--color-focus` | `--color-base` | focus indicator ring (non-text focus indicator) | SC 1.4.11 (AA) | 3:1 |

**Verification:** axe-core v4.12.0 `color-contrast` rule (rendered-DOM ratios after Tailwind compiles tokens) + `colorjs.io` v0.6.1 token-name checker (`new Color(fg).contrast(bg, "WCAG21")`) resolving each named pair from the compiled CSS custom properties; emits machine-readable PASS/FAIL JSON per token pair with WCAG SC reference, folded into the obs Section 6 envelope.

### Focus ring tokens

- **Tokens:** `--color-focus` (single focus-ring color, rendered as an inset ring) + `--border-emphasis` (active/selected-item border-emphasis marking the focused/selected coverage-matrix row and active picker item — selection indicator distinct from the focus-ring color).
- **WCAG SC:** SC 2.4.7 Focus Visible (AA — focus indicator visible on every interactive control) + SC 1.4.11 Non-text Contrast (AA — `--color-focus` ring meets 3:1 against `--color-base`).
- **Verification:** `colorjs.io` token-name checker emits the `--color-focus`/`--color-base` 3:1 ring-contrast assertion + asserts the `--border-emphasis` selection-vs-focus distinction (focus never color-only); puppeteer-CDP asserts the ring is present on `document.activeElement`.

### Target size tokens

- **Tokens:** no dedicated minimum-target-size tokens in design-system — interactive sizing is expressed through the icon-size grid token + `--space-sm` container-padding token (bounding titlebar controls, status lamps, picker/dialog actions), not named tap-target tokens.
- **WCAG SC:** SC 2.5.8 Target Size (Minimum, AA 24×24 CSS px) — desktop pointer + keyboard surface only; no touch surface (arch excerpt: "No mobile surface"), so SC 2.5.5 (AAA 44×44) is not a tier requirement.
- **Verification:** axe-core v4.12.0 `target-size` rule against the rendered DOM (icon-size-grid + `--space-sm`-derived control bounds) as a supplemental check; not a Minimal-baseline gate (no target-size trigger — Section 5).

### Motion tokens

- **Tokens:** `--motion-micro` (micro-interaction / state-transition duration — control hover-background lift, focus-ring fade-in, in-place status-light color transition, count tint green → amber → slate-violet at the hold) + `--ease-quiet` (primary easing curve). No dedicated reduced-motion override token — a global reduced-motion-preference rule drops all animations/transitions.
- **WCAG SC:** SC 2.3.3 Animation from Interactions (motion disabled via `prefers-reduced-motion: reduce`) + SC 2.3.1 Three Flashes (the `Fail` lamp is held motionless — no flashing).
- **Verification:** CDP media emulation `Emulation.setEmulatedMedia({ features: [{ name: 'prefers-reduced-motion', value: 'reduce' }] })` over the WebView2 session, then `Runtime.evaluate` reading computed `getComputedStyle(el).transitionDuration` / `animationName` to assert the count tint, focus-ring fade-in, status-light transition, and operator-pause dialog fade all collapse to none/0s; emits `{ element, prop, value, reduced_motion_respected }` JSON. Enforcement is CSS-native via Tailwind v4.1 `motion-reduce:` variant + `@theme`-defined `--motion-micro`/`--ease-quiet` (no framer-motion in stack). Single emulation set persists across the four in-place run-STATES (no routes, so no per-navigation reset).

### State color tokens (not-color-alone)

- **Tokens:** `--count-nominal` (success / `Pass`) / `--count-hold` (warning / `CalibrationRegion` / HOLD) / `--status-fail` (error / `Fail`) / `--count-blocked` (info / `Blocked`) / `--status-manual` (info/manual / `Manual`) / `--status-residual` (info/residual / `Residual`).
- **WCAG SC:** SC 1.4.1 Use of Color (A — six states not conveyed by color alone; each carries a text label + glyph supplement: `Pass`+filled-dot / `HOLD`+amber-filled-lamp+phase-line-swap / `Fail`+filled-lamp-motionless / `Blocked`+hollow-ring / `Manual`+checkbox-glyph / `Residual`+dashed-ring).
- **Verification:** custom not-color-alone assertion that every state carries its text label (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) + glyph supplement, machine-verifiable via DOM text-content + axe-core; folded into the obs Section 6 envelope.

### Typography tokens (readability)

- **Tokens:** `--font-mono` (monospace status/code — count / P-IDs / run_id / SLO timings / fingerprints / JSONL journal) + `--font-sans` (body/UI prose + display-heading — phase line / labels / run-report prose). Typography roles/weights/sizes/line-heights/tracking are table rows in design-system, not named CSS custom properties; only the two font-family tokens are emitted as `@theme` tokens.
- **WCAG SC:** SC 1.4.12 Text Spacing (AA — text adjustable without loss of content) — supported by the design's line-height/letter-spacing/word-spacing minima held in design-system. SC 1.4.8 Visual Presentation (AAA) is not a Minimal-baseline requirement (no escalation trigger).
- **Verification:** supplemental — only the two font-family tokens are machine-assertable as `@theme` tokens; line-height/letter-spacing/word-spacing values stay in design-system (not a11y-owned). No axe-core typography gate in the Minimal baseline.

(See `## 11. A11y Anti-Patterns` § Visual for visual-level bans.)

---

## 9. CI Integration

_[ALL tiers]_

**Platform:** GitHub Actions (arch excerpt's CI/CD Platform) — runs on the dev OS target; the CDP a11y path is scoped to a single `windows-latest` runner (Tauri CDP is Windows-WebView2-only).

**Per-pipeline-stage a11y artifacts:**

| Stage | A11y tool | Artifact | Consumer |
|-------|-----------|----------|----------|
| Lint | (no `eslint-plugin-jsx-a11y` lane mandated — Rust-first stack, no `npm test` lane) | n/a | n/a |
| Unit | cargo-nextest / cargo test / golden (no UI a11y tests at unit layer) | n/a | n/a |
| E2E (operator/local-gated) | axe-core v4.12.0 via `@axe-core/puppeteer` v4.11.3 + Lighthouse v13.0.3 + colorjs.io v0.6.1 + puppeteer-keyboard | structured violation JSON (axe `violations[]` + ratio assertions + keyboard PASS/FAIL) | `actions/upload-artifact` + per-PR diff |
| Aggregation | custom aggregator folding into obs Section 6 envelope | combined WCAG SC coverage JSON (per surface + per SC) | uploaded artifact |

**Pipeline integration:**

- A11y runs reuse tests' binding 5-command harness discipline (boot / run / status / cleanup / logs — tests excerpt's Test Harness Contract Summary). No `npm test` lane exists in the Rust-first stack, so the a11y JSON step is an added Node+puppeteer step that launches the Tauri app with `--remote-debugging-port=9222`, attaches over CDP, runs axe-core + colorjs.io + puppeteer-keyboard, and emits results through the **`logs`** command of the 5-command contract.
- This lands as an **operator/local gate, not a CI gate** — dynamic scenario proof requires a live Pulse and CDP attach is Windows-WebView2-only ("build + test gating only; dynamic end-to-end scenario proof requires a live Pulse and is an operator/local gate, not a CI gate" per arch excerpt's Pipeline note).
- Per-PR diff (new violations vs base branch); fail the gate on new WCAG AA violations (`violations.length > 0` → non-zero exit).
- WCAG criterion coverage report (per surface + per SC pass/fail history) for the desktop-webview surface; cli surface excluded (not-assertable).

**CI-specific resource attributes** (set via CI env per obs Section 3 binding):
- `service.name` = `"conductor-tauri"` / `"conductor-ui"`, `deployment.environment` = `CONDUCTOR_ENV` (fallback `"local"`), plus `ci.run.id` / `git.commit.sha` — a11y artifacts tag with the same OpenTelemetry resource attributes as obs traces for cross-correlation. Artifacts MUST NOT leak absolute host paths or internal seam-crate struct names (inherited from tests' Status JSON constraint).

**Artifact retention:** GitHub Actions default (typically 14-90 days), aligned with tests' CI patterns.

(See `## 11. A11y Anti-Patterns` § CI for CI-level bans.)

---

## 10. SLO Invariants & A11y Budgets

_[ALL tiers]_

**Always-required SLO invariant (every tier):**
- **Zero WCAG AA violations** on the four must-be-accessible paths (Section 4 ARIA Patterns + Section 5 Keyboard Navigation): scenario pick/start, operator-pause go/no-go, run-report view, operator-checklist. Specifically zero violations of SC 2.1.1 / SC 1.4.3 / SC 2.4.3 baseline + SC 1.4.11 / SC 1.4.1 / SC 4.1.2 / SC 4.1.3 / SC 2.1.2 / SC 1.3.1. NO retry-once policies for a11y violations — masks real failures (parallel to tests' zero-flakiness: "Do NOT set cargo-nextest `retries` > 0").

**Standard+ invariants:** (N/A — tier = Minimal.)

**Comprehensive invariants:** (N/A — tier = Minimal; no WCAG 2.2 AAA / cognitive-accessibility requirements.)

**Performance budget per a11y CI run:**
- axe-core duration < 30s for the single desktop-webview surface (Minimal — one surface, no `wcag22aa` rules).
- Total a11y operator/local gate (axe-core + colorjs.io + puppeteer-keyboard + reduced-motion emulation) bounded so it does not dominate the live-Pulse operator session.

**Cross-input parallel:** tests' zero-flakiness budget (Quality Gates Summary — "flaky tests are NOT tolerated… quarantined immediately and fixed") ↔ a11y' zero-WCAG-AA-violations invariant. Both reflect agent-driven discipline that retry-once policies mask real signals. A11y artifacts share the obs Section 6 envelope + service identity for cross-correlation with the `runs.db` row / JSONL journal.

**Build / deploy failure conditions:**
- Any new WCAG AA violation detected on a must-be-accessible path (`violations.length > 0` → non-zero exit on the operator/local gate).
- not-color-alone assertion failure (any of the six state tokens missing its text label + glyph).
- Reduced-motion assertion failure (any of count tint / focus-ring fade-in / status-light transition / dialog fade not dropped under `prefers-reduced-motion: reduce`).

(See `## 11. A11y Anti-Patterns` § SLO for SLO-budget bans.)

---

## 11. A11y Anti-Patterns (NEVER do these)

_[ALL tiers — single source of truth for all a11y bans]_

### Strategy

- NEVER skip POUR principle coverage at the Minimal tier (e.g., Operable keyboard assertions without Perceivable contrast verification) — both axe-core `color-contrast` and puppeteer-keyboard must run on every must-be-accessible path.
- NEVER claim WCAG conformance without machine-verifiable evidence (axe-core `AxeResults` JSON / Lighthouse a11y JSON / pa11y JSON / colorjs.io ratio JSON).
- NEVER use partial axe-core configuration (`runOnly` excluding default rules like `color-contrast`) without an explicit Decisions Log entry.
- NEVER use the axe DevTools browser extension (UI-only) as the engine — the JSON-producing axe-core library over `@axe-core/puppeteer` CDP is the source of truth.

### ARIA

- NEVER use ARIA on non-semantic HTML (`role="button"` on `<div>` instead of `<button>`) — semantic HTML first; start/stop/Proceed/Abort are `<button>`, scenario/seed fields are `<input>`, checklist rows are native checkbox.
- NEVER overuse `aria-label` — scenario/seed fields use a visible `<label>`; `aria-label` is reserved for the icon-only titlebar/start-stop/Proceed-Abort controls where no visible label is feasible.
- NEVER set conflicting ARIA states (`aria-hidden="true"` on a focusable Proceed/Abort button; `role="presentation"` on the `main`/`contentinfo` landmark).
- NEVER hand-roll `alertdialog`/`combobox` semantics over raw `<div>`s — use the shadcn AlertDialog / `Command` / `Select` over Radix Primitives that ship the roles + focus behavior.

### Keyboard

- NEVER create keyboard traps — the run-console-HOLD `alertdialog` MUST be Escape-escapable (SC 2.1.2); Tab/Shift+Tab cycling without an exit is banned.
- NEVER omit focus styles — `:focus { outline: none }` without the `--color-focus` ring replacement is an SC 2.4.7 violation; every interactive control shows the visible ring.
- NEVER set `tabindex` > 0 (creates non-natural focus order) — the picker → seed → start order and coverage-matrix row order rely on natural DOM/Radix order.
- NEVER let live emission counter `aria-live` updates steal focus from the operator during a live run or from the HOLD dialog trap.

### Visual

- NEVER convey state by color alone — the six verdict/report-state lamps (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) MUST each carry their text label + glyph supplement (SC 1.4.1).
- NEVER ship foreground/background pairs below WCAG minimum — `--text-primary`/`--color-base` etc. below 4.5:1 (SC 1.4.3) or `--count-nominal`/`--color-base` / `--color-focus`/`--color-base` below 3:1 (SC 1.4.11) fail the colorjs.io gate.
- NEVER autoplay motion without `prefers-reduced-motion: reduce` respect — the count tint, focus-ring fade-in, status-light transition, and operator-pause dialog fade MUST drop under reduced-motion (SC 2.3.3).
- NEVER render the empty/in-progress run-report as a gray decorative skeleton — use real prose ("No scenarios loaded" / "No run yet" / "Run in progress") so screen readers announce it, and never downgrade in-progress to a Fail.

### Cognitive

(N/A — no cognitive trigger; tier ≠ Comprehensive)

### Motion

- NEVER autoplay motion without `prefers-reduced-motion: reduce` respect (SC 2.3.3 AAA) — the global reduced-motion rule MUST drop the `--motion-micro`/`--ease-quiet`-driven transitions.
- NEVER flash content > 3 times per second (SC 2.3.1 A) — the `Fail` lamp resolves in place and motionless (no flashing/pulsing), held muted per "no alarm".
- NEVER use motion as the ONLY signal for the hold-point — the paused-count freeze + phase-line text swap ("HOLD — operator pause") + `aria-live="assertive"` carry the state without relying on the count-tint transition.
- NEVER rely on framer-motion or a runtime JS motion library for the reduced-motion drop — enforcement is CSS-native via the Tailwind v4.1 `motion-reduce:` variant + `@theme` tokens (no framer-motion in the stack).

### CI

- NEVER ship lint-only a11y without runtime axe-core / pa11y / Lighthouse — the Rust-first stack has no `eslint-plugin-jsx-a11y` lane, so the runtime axe-core/CDP step is the only a11y catch and MUST run.
- NEVER skip a11y on CI/operator-gate failure — the a11y step is a FAIL-fast gate (`violations.length > 0` → non-zero exit), not a warning.
- NEVER use human-only NVDA/VoiceOver/Orca review without the machine axe-core/colorjs.io/puppeteer output — manual SR is supplemental only.
- NEVER run the CDP a11y step on a non-Windows runner expecting CDP attach — macOS WKWebView / Linux WebKitGTK expose no CDP; those targets fall back to manual passes, and the gate is scoped to `windows-latest`.

### SLO

- NEVER use a retry-once policy for a11y violations — masks real failures (parallel to tests' zero-flakiness "Do NOT set cargo-nextest `retries` > 0").
- NEVER define a soft a11y SLO — a new WCAG AA violation, a not-color-alone failure, or a reduced-motion-not-dropped failure MUST fail the operator/local gate.
- NEVER skip the per-PR diff (regression detection) for the desktop-webview surface — new violations vs base branch gate the PR.
- NEVER let a11y artifacts leak absolute host paths or internal seam-crate struct names into the obs Section 6 envelope (inherited from tests' Status JSON constraint).

### Universal (agent-driven specific)

- NEVER use manual screen reader / keyboard / contrast as the ONLY method — NVDA/VoiceOver/Orca + manual keyboard are supplemental to the axe-core + colorjs.io + puppeteer-keyboard automated baseline.
- NEVER use a vendor-locked a11y tool as the ONLY method (axe DevTools extension UI-only / Stark plugin without machine output / Tenable.io UI-only without API) — JSON-emitting tooling only.
- NEVER introduce an inaccessible captcha / paywall / modal that breaks the keyboard navigation chain — the operator-pause `alertdialog` is the only modal and it is Escape-escapable with focus restoration.
- NEVER claim WCAG conformance without machine-verifiable evidence (axe-core JSON / pa11y JSON / Lighthouse JSON / colorjs.io ratio JSON aligned to obs Section 6).
- NEVER treat the cli surface as accessibility-assertable — it is not-assertable (no DOM / widget tree); its ASCII status prefixes + NO_COLOR text-pairing are output-stream discipline, not an ARIA a11y verification path.

---

## 12. A11y Decisions Log

_Records key decisions during plan generation + manual additions
between phase loops._

**Initial entry:**

`2026-06-14` — Initial a11y plan generated by `/andromeda-a11y`
- **Tier:** Minimal (0) — justified by: one assertable UI surface (desktop-webview Tauri control panel; cli is not-assertable; no web/HTTP, mobile, or native-toolkit surface) with four must-be-accessible paths at the lower band of the Minimal 2-5 range; security tier Minimal, tests tier Minimal, obs tier Minimal (a11y matches upstream with no ±1 divergence); zero WCAG compliance triggers and no creator-set WCAG mandate (solo developer-operator, no a11y-priority population); only token-driven triggers detected (visual-discrimination from six state-color tokens, motion-sensitive from reduced-motion binding) which mandate assertions but do not escalate the tier.
- **Key decisions:**
  - **A11y testing tool:** axe-core v4.12.0 via `@axe-core/puppeteer` v4.11.3 over the Tauri WebView2 CDP endpoint — chosen because it is the sole JSON-producing automated engine for the single assertable surface (a11y-scope Section 2 names `@axe-core/puppeteer` as THE candidate driver; CDP attach over `connect({browserURL})` targets the Windows WebView2 Chromium protocol directly); Lighthouse v13.0.3 + pa11y v9.1.1 are secondary/tertiary gates; the axe DevTools UI-only extension is explicitly NOT used.
  - **Focus management library:** none installed — shadcn AlertDialog over Radix Primitives (already in the stack) provides the run-console-HOLD focus trap + Escape-escape + focus restoration; the harness asserts Radix-default behavior via the puppeteer-CDP session rather than adding `focus-trap-react` / `react-aria`.
  - **ARIA component library:** shadcn/ui over Radix Primitives (already in the stack) — picker = shadcn `Command`/`Select`, dialog = shadcn `AlertDialog`, supplying `combobox`/`listbox`/`alertdialog` roles + keyboard contracts without hand-rolled ARIA; honors "semantic HTML first, ARIA second."
- **Open questions:** No existing browser E2E driver in the Rust-first stack (arch excerpt: no Playwright/Cypress/Selenium) — the tests' E2E-driver-reuse binding contract has no driver to piggyback on, so the a11y harness piggybacks on its own added Node+puppeteer CDP step gated to `windows-latest` as an operator/local gate; a11y aligns to the upstream tests harness if/when a browser driver is added. macOS/Linux have no CDP a11y reach (WKWebView / WebKitGTK expose no CDP) — those targets are manual-pass-only. Phase 5 iteration loop will flag missing tokens if upstream design Section 3 token names change (current upstream Section 3 supplies all referenced token names — no N/A fallback needed).