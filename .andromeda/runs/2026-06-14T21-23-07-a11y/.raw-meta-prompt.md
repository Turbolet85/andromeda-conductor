I now have the complete a11y plan. I have all the information needed:

**Stack/technologies:** Tauri 2 desktop shell, React 19 + Tailwind v4.1 + shadcn/ui over Radix Primitives, WebView2 CDP, conductor-cli (Rust), GitHub Actions, cargo-nextest.

**A11y tools (with versions):** axe-core v4.12.0, @axe-core/puppeteer v4.11.3, puppeteer-core v24.10.1, Lighthouse v13.0.3, pa11y v9.1.1/pa11y-ci v4.1.1, colorjs.io v0.6.1 (chroma-js v3.2.0 alternative).

**Tier:** Minimal (0). Sections 5, 6 present; Section 7 (SR) is supplemental-only within Section 3; Section 8 (Cognitive) is N/A.

**Triggers present:** visual-discrimination (six state-color tokens), motion-sensitive (reduced-motion binding). Binding constraint: Windows-only WebView2 CDP. Obs Section 6 log-format envelope alignment.

**Dimension selection:**
- Mandatory: 1, 2, 3, 4, 5 (all required)
- Conditional dim 6 (Visual Design Verification to Design Tokens): triggers — Section 3 has explicit A11y-Relevant Design Tokens
- Conditional dim 7 (WCAG Mapping Discipline): does NOT trigger (Minimal, not Standard/Comprehensive)
- Conditional dim 8 (Cognitive): does NOT trigger
- Second project-specific conditional: I'll author one keyed to the real `motion-sensitive` + `visual-discrimination` triggers combined with the binding Windows-only WebView2 CDP harness-reachability constraint and the obs Section 6 envelope alignment.

Now I'll generate the meta-prompt.

---

## Output Protocol

Rules for iteration agents working on the Conductor a11y plan (`a11y-plan-draft.md`):

1. **Output patches, not the document.** Emit only the specific `Old → New` text changes plus a changelog. Do NOT reproduce the full a11y plan, and do NOT re-emit unchanged sections.
2. **Patch format** — for each change:
   - `### Patch N: <short description>`
   - `**Old:**` followed by the exact verbatim text currently in the document (enough surrounding text to be unambiguous, copied character-for-character).
   - `**New:**` followed by the replacement text.
3. **Changelog** — after the patches, one line per change:
   - `[Iteration N] [substantive|cosmetic] <description>`
   - `substantive` = changes meaning, coverage, a tool/version, a WCAG SC mapping, a token pair, a constraint, or a binding contract. `cosmetic` = wording/typo/formatting only.
4. **PROHIBITED:**
   - Full document reproduction or wholesale section re-emission.
   - Restructuring (reordering sections, renaming headings, re-tabulating) without a stated correctness reason.
   - Cosmetic edits labeled `substantive`, or substantive edits hidden as `cosmetic`.
   - Inventing tools, versions, design tokens, or WCAG SCs not already anchored in the plan / upstream.
5. **If no issues are found:** output exactly `No patches` followed by a single cosmetic changelog line (e.g. `[Iteration N] [cosmetic] reviewed; no changes`).

## Analysis Dimensions

### 1. A11y Scope Faithfulness

- Section 1 enumerates eight assertable entities (operator-pause go/no-go `alertdialog`, six-state verdict/report-state lamp, coverage matrix, operator-checklist `ManualCheck`, scenario/suite picker, icon-only buttons, scenario/seed form inputs, run-report view) plus the not-assertable `conductor-cli`. Does every assertable entity receive concrete coverage in Section 4 (ARIA Patterns & Roles per-component table), Section 5 (Keyboard Navigation), and Section 6 (Visual Design Verification) — and is `conductor-cli` excluded from Sections 2/4 exactly as its not-assertable flag states, with no axe-core/Lighthouse/pa11y reach claimed for it anywhere?
- The four must-be-accessible paths (scenario/suite pick → start run; operator-pause HOLD → Proceed/Abort; view run report idle→live→report-terminal; operator-checklist `ManualCheck`) each declare Required ARIA roles + Required focus order + Required WCAG SC coverage in Section 1. Are all four faithfully carried into Section 4 / Section 5 without a path silently losing its `aria-live="assertive"` HOLD phase-line, its virtual-scrolled off-screen-row reachability, or its unticked-count roll-up?
- Are the two A11y triggers from Section 1 (visual-discrimination from the six state-color tokens; motion-sensitive from the reduced-motion binding) each addressed by the mandated sections (visual-discrimination → Section 6 contrast pairs + state-color not-color-alone; motion-sensitive → Section 6 Motion tokens + Section 11 Motion bans), and does the plan hold the line that both are token-driven and do NOT escalate above Minimal?

### 2. Tier Calibration (WCAG Mapping)

- The plan declares `Minimal (0)`. Section 5 (Keyboard Navigation) is INCLUDED on the "desktop-webview UI surface present" exception, while Section 8 (Cognitive Accessibility) is marked N/A and Screen Reader support is supplemental-only inside Section 3. Does each inclusion/exclusion match a real trigger — i.e. is there genuinely no screen-reader-priority or cognitive trigger (Section 1 cross-input check says "none explicit"), and is Section 5 justified solely by the present UI surface rather than a keyboard-only trigger?
- Section 3 WCAG criteria mapping lists the Minimal baseline (SC 2.1.1 + SC 1.4.3 + SC 2.4.3) augmented by token-driven SC 1.4.11 / SC 1.4.1 / SC 2.3.3 and pattern-inherent SC 4.1.2 / SC 4.1.3 / SC 2.1.2 / SC 1.3.1, and explicitly leaves `wcag22aa` unset and Standard/Comprehensive "not selected." Does any section quietly pull in an AA/AAA-only SC (e.g. SC 1.4.6 Enhanced, SC 2.4.13 Focus Appearance, SC 2.5.5 44×44) as a gate rather than as an explicitly-labeled non-baseline/supplemental note?
- Section 10 states the Minimal SLO invariant as "zero WCAG AA violations" on the four paths with Standard+/Comprehensive invariants marked N/A. Does Section 10 stay at the Minimal default (no regression-budget or AAA-per-regulated-surface invariant smuggled in), consistent with the tier?

### 3. Tool Anchoring (Catalog ↔ Plan)

- Every tool named carries a pinned version: axe-core v4.12.0, `@axe-core/puppeteer` v4.11.3, `puppeteer-core` v24.10.1, Lighthouse v13.0.3, pa11y v9.1.1 / pa11y-ci v4.1.1, colorjs.io v0.6.1 (chroma-js v3.2.0 as the stated either/or alternative). Are these versions consistent everywhere they recur (Section 2 invariants, Section 3 tool pick, Section 6 verification, Section 9 CI table, the bootstrap `npm install` line in Section 3), and does the plan match what `a11y-research.md` catalogs — with no version drift between sections?
- Section 3 picks axe-core as primary, Lighthouse as secondary coarse gate, pa11y as tertiary SC-tagged runner, and colorjs.io for the token-NAME-level assertion that axe's rendered-DOM `color-contrast` rule cannot supply. Is each pick justified against its alternative (e.g. colorjs.io vs chroma-js "either one, not both"; `wcag-contrast` "rejected as stale, last publish 2019"; axe DevTools extension "explicitly NOT used"), and are the `**WCAG SCs covered:**` per tool honored (axe → SC 1.4.3/1.4.11/4.1.2/4.1.3/1.3.1; colorjs.io → SC 1.4.3/1.4.11; puppeteer-keyboard → SC 2.1.1/2.4.3/2.1.2/2.4.7)?
- The Decisions Log "Open questions" records that the Rust-first stack has no Playwright/Cypress/Selenium E2E driver to reuse, so the harness piggybacks on its own added Node+puppeteer CDP step. Is this "no agent-runnable maintained driver to reuse" gap stated explicitly (rather than a fabricated browser driver being slipped in), and is the reconciliation with tests' E2E-driver-reuse binding contract coherent across Section 3, Section 9, and the Decisions Log?

### 4. Anti-Pattern Relevance

- Section 11 has nine domains plus Universal. Are the stack-specific bans actually grounded in THIS stack — e.g. the Motion domain's "NEVER rely on framer-motion … enforcement is CSS-native via the Tailwind v4.1 `motion-reduce:` variant" (framer-motion confirmed not in stack), the CI domain's "no `eslint-plugin-jsx-a11y` lane … Rust-first stack," and the ARIA domain's "use the shadcn AlertDialog / `Command` / `Select` over Radix Primitives" — rather than generic placeholders for tools absent from the Tauri/React/Radix stack?
- Does each populated domain (Strategy / ARIA / Keyboard / Visual / Screen Reader / Motion / CI / SLO / Universal) carry at least three real bans, and is the Cognitive domain's `N/A — no cognitive trigger; tier ≠ Comprehensive` left correctly empty rather than padded with inapplicable bans?
- Does Section 11 Universal correctly REJECT (never recommend) the banned vendor-locked / manual-only patterns — axe DevTools extension UI-only, Stark-without-machine-output, Tenable.io UI-only, and "manual NVDA/VoiceOver/Orca review without machine output" as sole verification — and do the section bodies cross-reference Section 11 (via the `(See ## 11 …)` pointers) rather than duplicating the full ban lists inline?

### 5. Downstream Readiness

- Per the D26 chain, `route` and `setup-project` are the only consumers. Can `route` derive the bootstrap phase ordering directly from Section 3's "Bootstrap phases" list (a11y-tooling-install → focus-management-library-install → aria-component-library-install → contrast-verification-harness-setup → screen-reader-test-spec-setup → a11y-ci-gate-wire → violation-json-emission-wire) without inventing phases, and does each derived item name its concrete artifact (the `npm install` line, the shadcn/Radix "no separate library" decision, the colorjs.io token-pair checker, the Windows-runner CDP step)?
- Can `setup-project` materialize the gates copy-paste-ready from Section 3 + Section 9 — specifically: launching the Tauri app with `--remote-debugging-port=9222`, `puppeteer.connect({ browserURL: 'http://localhost:9222' })` → `new AxePuppeteer(page).analyze()`, axe `withTags(['wcag2a','wcag2aa','wcag21aa'])`, the colorjs.io named-token-pair resolution from compiled CSS custom properties, and surfacing results through tests' `logs` command of the 5-command (boot/run/status/cleanup/logs) contract — with the operator/local-gate-not-CI-gate decision and the Windows-only scoping both unambiguous?

### 6. Visual Design Verification to Design Tokens

- Section 6 reproduces design token NAMES verbatim from upstream-context Section 3 A11y-Relevant Design Tokens across its sub-blocks (Color contrast pairs, Focus ring tokens, Target size tokens, Motion tokens, State color tokens, Typography tokens). Does every token in Section 6 — `--text-primary`/`--text-secondary`/`--text-tertiary`/`--text-muted`/`--color-id-cyan`/`--count-nominal`/`--count-hold`/`--status-fail`/`--color-focus` over `--color-base`/`--color-raised-1`/`--color-inset`, plus `--border-emphasis`, `--motion-micro`/`--ease-quiet`, the six state tokens (`--count-nominal`/`--count-hold`/`--status-fail`/`--count-blocked`/`--status-manual`/`--status-residual`), and `--font-mono`/`--font-sans` — actually exist in upstream Section 3, with no invented or contradicting token introduced?
- Scan Section 6 (and any token reference in Sections 1–5) for RAW VALUE bleed-through that belongs to design-system, not a11y: hex literals (`#RRGGBB`/`#RGB`), pixel values (`Npx`), duration values (`Nms`/`Ns`), RGB/RGBA/HSL/HSLA literals, or cubic-bezier curves placed next to or in place of a token name. Any such literal violates the `[NO_RAW_DESIGN_VALUES]` binding contract and must be stripped to the bare token name. (Whitelist: the WCAG threshold ratios 4.5:1 / 3:1 / 7:1 and target sizes 24×24 / 44×44 are WCAG standards, not tokens, and may appear in context notes.)
- Does each Section 6 sub-block cite the correct WCAG SC and required ratio/size: each color-contrast pair tagged SC 1.4.3 (4.5:1) for text vs SC 1.4.11 (3:1) for the non-text status lamps + focus ring; the focus-ring block citing SC 2.4.7 Focus Visible + SC 1.4.11 (3:1 `--color-focus`/`--color-base`) and the `--border-emphasis` selection-vs-focus distinction; the target-size block citing SC 2.5.8 (AA 24×24) and correctly marking SC 2.5.5 (AAA 44×44) as a non-requirement given no touch surface; the Motion block citing SC 2.3.3 + SC 2.3.1; and is every Section 6 token mirrored by a corresponding entry in upstream Section 3 (no orphan token on either side)?

### 7. Token-Driven Trigger Verification under the Windows-WebView2-CDP Constraint and Obs Envelope `[trigger: motion-sensitive + visual-discrimination triggers in the Section 1 A11y Scope Summary, the binding Windows-only Tauri WebView2 CDP harness-reachability constraint in Section 3, and the obs Section 6 log-format envelope alignment]`

- **Motion-sensitive (reduced-motion):** The plan asserts reduced-motion via CDP `Emulation.setEmulatedMedia({ features: [{ name: 'prefers-reduced-motion', value: 'reduce' }] })` then reads `getComputedStyle().transitionDuration`/`animationName` to confirm the count tint (green → amber → slate-violet), focus-ring fade-in, status-light transition, and operator-pause dialog fade all collapse, with enforcement CSS-native via Tailwind v4.1 `motion-reduce:` + `@theme` tokens. Is this verification path internally consistent everywhere it appears (Section 1 trigger required-assertion, Section 6 Motion block, Section 10 build/deploy failure conditions, Section 11 Motion bans), and does it name the SAME four animated surfaces in each place — with SC 2.3.3 (and SC 2.3.1 "Fail lamp held motionless") cited consistently?
- **Visual-discrimination (not-color-alone):** The six verdict/report-state tokens must each pair a text label (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) + glyph supplement, machine-verified via DOM text-content + axe-core. Do Section 1, Section 4 (lamp row), Section 6 State-color block, Section 10 failure conditions, and Section 11 Visual/Screen Reader bans agree on the SAME six states and the SAME label+glyph pairing — with no state quietly conveyed by color alone and no AA/AAA escalation implied by the trigger?
- **Windows-only CDP reachability + obs envelope:** Section 3 binds the automated harness to Windows WebView2 only (CDP via `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222`; macOS WKWebView / Linux WebKitGTK expose no CDP → manual NVDA/VoiceOver/Orca fallback), and binds all violation emissions to the obs Section 6 JSONL envelope (axe rule-id + WCAG-SC tag + node `target` selector folded into `fingerprints[]`; `state` = `Fail` when `violations.length > 0`; no new top-level fields; no absolute host paths or seam-crate struct names leaked). Is the Windows-only scoping stated consistently across Section 1, Section 3, Section 9 (the `windows-latest` runner), and Section 11 CI bans — and does every emission path (axe `violations[]`, colorjs.io ratio assertions, puppeteer-keyboard PASS/FAIL, reduced-motion JSON) fold into that single obs envelope without introducing a divergent ad-hoc schema?

Read the document, analyze along all dimensions, output patches and changelog.
