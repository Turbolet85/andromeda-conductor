[preamble omitted: amendments file does not exist — normal on a fresh project]

# obs extract

## Relevance
Relevant — cargo workspace scaffold establishes infrastructure surface (8 crates, dependency seams) that must be observed per critical paths.

## Constraints
- No OTel SDK for self-observation; `tracing` 0.1.44 + `tracing-subscriber` 0.3.23 JSON formatter only (obs-plan §3 OTel SDK init).
- `[workspace.dependencies]` must PIN tracing, tracing-subscriber, opentelemetry-proto 0.32.0, tonic 0.14.6 (obs-plan §3 Logging stack + §1; no floating versions).
- Service identity via `env!("CARGO_PKG_NAME")` compile-time + `$CONDUCTOR_ENV` runtime (obs-plan §3 Service identity).
- Crate-per-seam dependency structure enforced — cross-seam edges must fail (obs-plan §1 Module Boundaries + §4).
- Tauri desktop-webview requires `#[tracing::instrument]` on command handlers later (obs-plan §1 desktop-webview + §4).
- Cargo.lock committed (obs-plan §3 Bootstrap logger-stack-install + §9 CI artifact handling).

## Patterns to follow
- Root [workspace.package] + [workspace.dependencies] for pinned versions (obs-plan §3 + §1).
- rust-toolchain.toml ≥1.94.1 (security-plan; scope acceptance hints).
- tracing/tracing-subscriber wired in conductor-cli + conductor-tauri only (not all libs); core/timeline/emit/verify/report instrumentation-ready but don't import the logger at scaffold time (deferral).
- Binary crates declare tracing/tracing-subscriber/anyhow for CLI bootstrap (§3 init + §10 error bridging).

## Anti-patterns to avoid
- NEVER hardcode service identity — use env!("CARGO_PKG_NAME") (obs-plan §11 Universal).
- NEVER add OTel SDK (opentelemetry_sdk/_api) — breaks current_thread determinism (obs-plan §11 + §1 creator-explicit anti-pattern).
- NEVER floating versions for tracing/opentelemetry-proto/tonic in [workspace.dependencies] (obs-plan §1).
- NEVER cross seams in the crate graph (obs-plan §4).

## Contract bindings
Tests harness contract (obs ↔ tests): the JSONL schema (journal_emitted_at, run_id, seed, scenario, p_ids, verdict, state, latency_ms, slo_tier, fingerprints) binds to tests' harness §5/§3 (obs-plan §3 Log format — "Binding contract from upstream Section 5"). Binding materializes in downstream feature chunks; scaffold just provides the crates.

## Acceptance criteria contributions
- (obs) cargo build --workspace succeeds — no lint warnings from the logger stack (tracing/tracing-subscriber resolve via workspace.dependencies).
- (obs) service.name compile-time injectable via env!("CARGO_PKG_NAME") on each binary crate.
- (obs) Cargo.lock committed (deterministic obs artifact snapshots in CI per §9).
- (obs) No OTel SDK in the dependency tree — `cargo tree | grep -i opentelemetry_sdk` returns zero (obs-plan §11).

## Relevant amendment history
(none) — initial cargo scaffold chunk; no prior obs-plan amendments.