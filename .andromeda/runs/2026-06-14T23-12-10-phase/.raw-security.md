[preamble omitted: "The amendments file doesn't exist yet (which is expected for a fresh project)..."]

# security extract

## Relevance — partial
This chunk establishes the Cargo workspace structure and toolchain foundation; it touches the security-critical dependency audit and tool-chain hardening requirements, but does not implement input validation, error handling, or the other runtime seam boundaries.

## Constraints
1. `rust-toolchain.toml` MUST pin ≥ 1.94.1 (security plan § Dependency Security, Bootstrap phases — clears tar-rs symlink-chmod CVE-2026-33056 / RUSTSEC-2026-0033).
2. `Cargo.lock` MUST be committed (security plan § Dependency Security — makes cargo-audit/cargo-deny deterministic; prevents bundled SQLite C version floating past advisory tracking).
3. `Tauri` MUST pin ≥ 2.10.3 in `[workspace.dependencies]` (security plan § Dependency Security § Update policy — clears origin-confusion CVE-2026-42184; arch 2.10.1 is exposed).
4. All 8 member crates declare correctly (6 lib, 2 bin) with `conductor-*` names (security plan § Input Validation § Boundary — crate-per-seam enforcement).
5. Seam library → `conductor-core` dependency edges present (architecture §Module Boundaries — compiler-enforced seam isolation).
6. Root workspace manifest declares `[workspace.package]` with centralized version + `edition = "2024"` (architecture §Infrastructure Patterns + §Occupied Resources).

## Patterns to follow
1. `[workspace.dependencies]` single source of truth for pinned versions; no floating semver (Dependency Security).
2. `conductor-core` minimal compiling library this chunk; shared-type bodies deferred to next chunk.
3. Workspace positioned for cargo-audit 0.22.2 + optional cargo-deny 0.19.8 CI gates later (this chunk pinning is the prerequisite).

## Anti-patterns to avoid
1. NEVER tauri < 2.10.3 or toolchain < 1.94.1 — required bumps (§ Dependency Security § Update policy; § Anti-Patterns § Universal).
2. NEVER floating semver in workspace deps — pin so cargo-audit is deterministic.
3. NEVER omit Cargo.lock from the commit (§ Anti-Patterns § Universal).

## Contract bindings
Design ↔ workspace structure: arch Directory structure + Occupied Resources materialized here; downstream design chunks reference this topology.
Tests ↔ CI gate: cargo-audit/cargo-deny CI integration wired later; workspace structure + Cargo.lock are prerequisites.

## Acceptance criteria contributions
- (security) rust-toolchain.toml pins ≥1.94.1 and workspace is edition-2024.
- (security) tauri pinned ≥2.10.3 in [workspace.dependencies].
- (security) Cargo.lock committed; no .gitignore entry suppresses it.
- (security) cargo build --workspace succeeds (green skeleton; no audit run yet — that gate is the CI chunk).

## Relevant amendment history
(none)