[preamble omitted: agent reasoning — chunk is STRUCTURAL ONLY, no testable behavior]

# tests extract

## Relevance
partial — establishes the workspace structure that downstream test chunks will reference; no testable behavior in this chunk itself.

## Constraints
- Per test-plan §1 (Test Scope Summary): the 8 crate-per-seam members (6 libs + 2 bins) are the boundary for per-seam `cargo nextest run -p conductor-<seam>` unit isolation.
- Per test-plan §3 (Harness Contract bootstrap): `[workspace.dependencies]` + `[workspace.package]` centralize pinned versions so later chunks reference `{dep}.workspace = true`; enables the 5-command discipline + CI stage flags.
- Per test-plan §9 (CI Integration): workspace structure + Cargo.lock committed are prerequisite for deterministic Cargo.lock-keyed cache + reproducible cargo-audit gates.
- Per test-plan §10 (Quality Gates): `cargo build --workspace` is a smoke gate; coverage tooling keyed on resolved Cargo.lock.
- Per security plan §Dependency Security (in test-plan §10): rust-toolchain ≥1.94.1 (tar-rs CVE); Cargo.lock committed+undrifted is a build-failure condition.

## Patterns to follow
- Per test-plan §3 (Test Data Bootstrap): the 8 crates host per-seam `#[cfg(test)]` modules + `tests/` integration; scaffold establishes the `crates/conductor-*` layout these inhabit.
- Per test-plan §2 (naming): snake_case source + test fns; golden snapshots under `<crate>/tests/snapshots/` (insta) later.

## Anti-patterns to avoid
- Do NOT introduce cross-seam deps beyond seam→core; the Cargo.toml structure is compiler-enforced and blocks later violations.
- Per test-plan §3 (PID file): no daemon / no inbound listener; never add a listener outside the :4317 port-occupier fault scope.

## Contract bindings
obs — the log-format binding §3 defines per-run JSONL at runs/<run_id>.jsonl; this scaffold establishes the crates that will implement journal emission.

## Acceptance criteria contributions
- (tests) cargo build --workspace succeeds (green skeleton, prerequisite for all later test layers).
- (tests) All 8 crates exist with correct names + crate-types; seam→core edges compile-enforced (forbidden cross-seam edge fails the build).
- (tests) rust-toolchain.toml pins ≥1.94.1; root manifest edition = 2024 workspace-wide.
- (tests) Cargo.lock committed (deterministic cargo-audit + reproducible builds prerequisite for CI gates §10).

## Relevant amendment history
(none)