# layouts extract

## No domain coverage
Cargo workspace scaffold is a Rust build-system / crate-organization task with no layout, wireframe, component-placement, or focus-order content — all downstream of this foundational compilation skeleton.