[RAW sub-agent output — a11y validator]

Now I have all the materials. Let me analyze the route draft against the a11y plan and focus guide.
[analysis preamble omitted in this transcription; full reasoning was emitted, noting shadcn/Radix are already in-stack so focus/aria "installs" are reuse acknowledgments, and the single "Desktop a11y assertion harness" chunk conflates the 7 bootstrap items]

# A11y validation — route draft

## Insert

- Between `Operator-pause go/no-go dialog` and `Desktop a11y assertion harness`: **"Focus-management + ARIA library assertion — shadcn/Radix AlertDialog trap/restore + pattern semantics validation"** (epoch: `9`)
  Reason: Per a11y-plan § Bootstrap phases, focus-management-library and aria-component-library installs must be named as distinct bootstrap items even though they reuse the stack's existing shadcn/Radix; the single aggregated "Desktop a11y assertion harness" chunk does not cite the library binding contract from the plan.

- Between `Operator-pause go/no-go dialog` and `Focus-management + ARIA library assertion`: **"A11y tooling install — axe-core v4.12.0 + @axe-core/webdriverio + Lighthouse v13.0.3 + colorjs.io v0.6.1 over tests' tauri-driver"** (epoch: `9`)
  Reason: Per a11y-plan § Bootstrap phases + § Sequencing, a11y-tooling-install is a primary bootstrap item that must execute before any verification chunk; it is currently embedded in the aggregated "Desktop a11y assertion harness" without being called out as a separate phase.

- Between `Focus-management + ARIA library assertion` and `Desktop a11y assertion harness`: **"Contrast-verification harness setup — colorjs.io token-pair assertion for SC 1.4.3 + SC 1.4.11 pairs from design excerpt"** (epoch: `9`)
  Reason: Per a11y-plan § Bootstrap phases, contrast-verification-harness-setup is a dedicated bootstrap phase that must run before the ci-gate-wire phase; currently not surfaced as a separate chunk.

- Between `Contrast-verification harness setup` and `Desktop a11y assertion harness`: **"Screen-reader test-spec setup — NVDA/VoiceOver/Orca per-state manual pass specs per the must-announce items (empty-state prose, in-progress, HOLD, verdict, checklist roll-up)"** (epoch: `9`)
  Reason: Per a11y-plan § Bootstrap phases, screen-reader-test-spec-setup is an explicit bootstrap item (though supplemental to automated) and must be named; currently omitted from the route.

- Between `Screen-reader test-spec setup` and `Desktop a11y assertion harness`: **"A11y CI gate + violation JSON wiring — axe/colorjs.io/keyboard PASS/FAIL folded into obs Section 6 envelope with service.name/environment tags"** (epoch: `9`)
  Reason: Per a11y-plan § Bootstrap phases, a11y-ci-gate-wire + violation-json-emission-wire are the final two bootstrap items (both Polish/CI-gated per plan), not subsumed in a single "assertion harness" chunk.

## Rewrite

- `Desktop a11y assertion harness — axe via tauri-driver + colorjs.io contrast + ARIA patterns`: remove this chunk entirely from Epoch 9 and replace after the four inserts above with **"Desktop a11y verification — axe violations + contrast ratios + keyboard trap/focus-order assertions on the four must-be-accessible paths (scenario/suite pick, operator-pause dialog, coverage-matrix, operator-checklist)"** (epoch: `9`)
  Reason: The original conflates multiple bootstrap phases into one; the rewrite names the end-state verification surface after all bootstrap chunks are sequenced, keeping focus on what is verified (the four paths) rather than conflating the tool picks.
