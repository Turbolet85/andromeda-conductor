[RAW sub-agent output — design validator]

Now I'll analyze the draft route against the design specialist focus guide and plan.
[analysis preamble omitted in this transcription; full reasoning was emitted, including reading both design-system.md and layout-templates.md]

# Design validation — route draft

## Insert

- Between "Scenario/suite picker + start/stop" (line 115, Epoch 9) and "Live-counter Channel stream" (line 117): **"Component primitives library — status-lamp variants (Pass/CalibrationRegion/Fail/Blocked/ManualCheck/KnownResidual), dialog scaffolds, operator-checklist structure"** (epoch: `Epoch 9`)
  Reason: Design tokens → component foundations → composite surfaces is the required sequencing (per design-system plan §Iconography + §Component Patterns); lamps and dialogs are foundations for the coverage-matrix and run-report views that follow (per layout-templates §Component sections).

## Reorder

- Move "Paused-count hold-point signature — frozen heartbeat freeze/tint/resume in titlebar" (line 113, Epoch 9) to immediately after "Frameless window shell" (line 111) and before "Scenario/suite picker + start/stop" (line 115).
  Reason: The signature is the design-system's primary anchor (design-system §Brand Identity, signature element); it must precede all desktop UI chunks so subsequent components inherit the design personality, not the reverse.

## No other sections apply.
