[RAW sub-agent output — obs validator]

Now I'll analyze the draft route against the obs plan and focus guide.
[analysis preamble omitted in this transcription; full reasoning was emitted, including the note that the focus guide's "W3C traceparent" bootstrap item does NOT apply to Conductor — obs-plan bans W3C context, correlation is run_id + runs.db envelope]

# Obs validation — route draft

## Insert

- CI artifact gate chunk (after Epoch 9, before Epoch 10) — obs-plan §9 "CI Integration" requires log upload + conformance validation + zero-unlogged-panics gate; route has "Base CI + agent-run harness skeleton" (Epoch 1) but no obs-specific CI stage binding logs/agent-latest.jsonl upload + schema conformance check + panic-hook verification.

- Panic-capture instrumentation span (within Epoch 3 or as own chunk before Epoch 8) — obs-plan §10 SLO Invariants "Zero unlogged panics" requires std::panic::set_hook() + structured error logging; route has error spans (Epoch 3) and sanitized stderr (Epoch 8) but no explicit chunk for panic hook wiring + tracing::error!(panic=…) JSON formatting.

## Reorder

- Artifact redaction layer → earlier phase — obs-plan §3 Observability Harness Contract states "Redaction must wire at processor stage (tracing-subscriber layer) so failures upstream don't leak via stderr" and §6 Log Coverage binds redaction to every JSON line; route places "Artifact redaction layer" in Epoch 6 (post-emission, post-report), but it should sit in Epoch 1 alongside structured logging stack to gate all telemetry at the source, before any production-code chunk emits.

## No suggestions
(none beyond Insert/Reorder above)

## Rewrite

- "Structured logging stack" chunk: Clarify that service identity (service.name/version/deployment.environment) is emitted as fields on every JSONL line (not as separate resource attributes in an OTel span), per obs-plan §3 Service Identity and §6 Log Coverage binding contract.
