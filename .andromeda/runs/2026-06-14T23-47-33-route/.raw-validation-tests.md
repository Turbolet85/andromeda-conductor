[RAW sub-agent output — tests validator]

Now I'll analyze the draft route against the test specialist plan to identify gaps, mis-sequencings, and mis-rewrites.
[analysis preamble omitted in this transcription; full reasoning was emitted, mapping test-plan §3 bootstrap phases]

# Tests validation — route draft

## Insert

- Between `Base CI + agent-run harness skeleton` and `Seeded phase scheduler`: **"Test framework + coverage-tooling install — cargo-nextest 0.9.137, proptest 1.9.0, insta 1.46.1, assert_cmd 2, assert_fs 1, cargo-llvm-cov 0.8.7"** (epoch: `Foundation`)
  Reason: Test framework installation must precede all feature development per test-plan §3 bootstrap phases (test-runner-install → 5-command-discipline-wire sequence); currently no explicit cargo-nextest / coverage-tooling bootstrap.

- Between `Base CI + agent-run harness skeleton` and `Seeded phase scheduler`: **"Test data bootstrap — rstest 0.26.1 fixtures, scenario config one-per-P-ID, in-memory/TempDir runs.db isolation, deterministic seeding"** (epoch: `Foundation`)
  Reason: Test-data bootstrap (rstest fixtures, scenario config files, per-test DB isolation) must be in place before any timeline/emission chunks are tested per test-plan §3 test-data-bootstrap-wire.

- Between `Coverage-matrix generator` and `Artifact redaction layer`: **"CI test-job config — cargo-nextest JUnit XML, cargo-llvm-cov LCOV/Cobertura, dorny/test-reporter integration, parallel matrix per OS"** (epoch: `Epoch 6`)
  Reason: CI integration (test-job staging, artifact upload, parallelization) is part of test-plan §3 5-command-discipline-wire and must be explicitly wired alongside the status/logs endpoints per §9 CI Integration.

- Between `Coverage-matrix generator` and `Artifact redaction layer`: **"Quality gate config — coverage threshold ≥60% lines/50% branch/70% function, zero-flakiness budget, cargo-audit/deny in CI, Tauri ≥2.10.3, toolchain ≥1.94.1"** (epoch: `Epoch 6`)
  Reason: Quality gates (coverage thresholds, supply-chain audit, dependency pins) must be configured alongside coverage tooling per test-plan §3 quality-gate-config-emit and §10 Quality Gates.

## Reorder

- Move `Coverage-matrix completeness gate` from Epoch 10 to Epoch 6 (before `Artifact redaction layer`)
  Reason: Coverage-matrix artifact is a test-data contract and must be positioned after the data-persistence layer (runs.db + JSONL) per test-plan §3 status-endpoint-implement, not deferred to Polish.

## Rewrite

- `Determinism-replay harness`: "same-seed identical stream-shape property/golden assertion" → "determinism-replay harness — same scenario+seed ⇒ same stream shape via insta golden tests + proptest regressions; tokio start_paused + time::advance"
  Reason: Current wording omits the machinery (insta/proptest/tokio) and tooling dependencies required per test-plan §7 Fixture pattern and §2 Agent-runnable invariants.

- `5-command agent-run harness`: "boot/run/status/cleanup/logs across .sh and .ps1" → "5-command agent-run harness — boot (preflight gate via rmcp negotiate 2024-11-05), run (nextest + scenarios), status (runs.db/JSONL read), cleanup (idempotent artifact+DB removal), logs (JSONL emission journal wall-clock stamps)"
  Reason: Current wording is abstract; test-plan §3 specifies concrete command semantics (boot=preflight, status=disk-read, no daemon PID file) per Test Harness Contract.
