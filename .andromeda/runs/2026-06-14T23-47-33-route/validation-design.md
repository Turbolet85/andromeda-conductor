# Design validation — route draft

## Insert
- Between `Scenario/suite picker + start/stop` and `Live-counter Channel stream`: **"Component primitives library — six status-lamp variants, dialog scaffolds, operator-checklist structure"** (epoch: `Desktop control panel`)
  Reason: Per design-system §Iconography + §Component Patterns, tokens → component foundations → composite surfaces is required; lamps/dialogs underlie the coverage-matrix and run-report views.

## Reorder
- Move `Paused-count hold-point signature` before `Scenario/suite picker + start/stop` (immediately after `Frameless window shell`)
  Reason: Per design-system §Brand Identity, the signature is the primary anchor and must precede all desktop UI chunks so subsequent components inherit the personality.
