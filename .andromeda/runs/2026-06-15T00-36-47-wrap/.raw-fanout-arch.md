proposals:
  - detector: D-arch-resources
    severity: warning
    section: Stack and Technologies
    change: "| Serialization | serde 1.0.x + serde_json 1.0 | Data serialization for config, type-erased edges, and JSONL journal/report envelope format |"
    sidecar: "serde_json 1.0 added to [workspace.dependencies] for canonical-format round-trip tests and JSONL journal/report seam (Epoch 6); registered in Stack."
    rationale: "Report §Dependencies explicitly notes 'New workspace pin: serde_json = 1.0' added to root [workspace.dependencies] as a serde companion for canonical-name tests and notes 'the JSONL journal + run-report seam consume it at runtime in Epoch 6.' Currently absent from §Stack table and §Inherited Defaults. serde_json belongs there alongside serde."

# D-arch-decisions → no drift (serde/thiserror/serde_json + #[non_exhaustive] error-wall all consistent with §Stack + §Established Decisions).
