proposals:
  - detector: D-design-tokens
    severity: warning
    section: Design Decisions Log
    change: "2026-06-15: conductor-core chunk implements Verdict/ReportState types with ASCII status_prefix() accessor; cli/desktop-webview rendering phases bind colors/glyphs per design-system (no hardcoded values in core, deferred binding confirmed)."
    sidecar: "conductor-core exports status_prefix() ASCII text ([PASS]/[FAIL]/[HOLD]/[MANUAL]/[RESIDUAL]/[BLOCKED]); rendering surfaces consume these + map to design-system tokens per phase."
    rationale: "Report §Coverage confirms tokens n/a for core (pure data), state→color/glyph mapping deferred to cli/GUI. Chunk confirms the architecture — no color/glyph hardcoding in core, only ASCII accessors. Update log to reflect implementation alignment."

# Orchestrator note: no invariant violation (no color-alone, no hardcoded values). The proposal records an implementation-status note, not a spec-truth change.
