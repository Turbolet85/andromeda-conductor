Chunk adds no new user-facing surfaces/regions — only data types the cli + desktop-webview surfaces will later render. The wireframes already document all verdict/report-state renderings (verdict lamp, coverage matrix, run-report lines). No drift.

proposals: []
