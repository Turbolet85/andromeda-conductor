All three detectors pass.
- D-tests-coverage: 10 unit tests at the required tier (serde round-trip + canonical names + wire forms + accessors + Scenario round-trip + CoreError Display/wall); seeded/deterministic, no real network/time. No drift.
- D-tests-framework: `cargo test -p conductor-core` is the fallback runner cargo-nextest wraps; clippy invocation matches §plan. No off-spec runner. No drift.
- D-tests-obs-harness: chunk adds the exact enum variants §3 Status-endpoint shape specifies (verdict/state/slo_tier/p_ids); does not change the 5-command harness or JSONL format. §3 ↔ obs-plan §3 still agree. No drift.

proposals: []
