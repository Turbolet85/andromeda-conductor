The amendment file doesn't exist yet, which is normal for a fresh project. Now let me extract the relevant obs content for the config-validation-surface chunk.

# obs extract

## Relevance
partial — obs covers config-validation's logged errors + potential panic capture, but the chunk is primarily a validation surface (not a telemetry producer).

## Constraints
- per §1 Obs Scope: Conductor is Minimal tier (0) with no OTel SDK; all self-observation is structured tracing logs via `tracing-subscriber` JSON (§3 Logging stack)
- per §11 Anti-Patterns: "NEVER skip garde validation on scenario config deserialization — `range` + cross-field rules enforce error fraction ∈ [0,1], p50≤p95≤p99, severity-mix sum constraints"; validation failures are harness faults, never verdicts
- per §10 SLO Invariants: "Zero unlogged panics" — any panic during config validation MUST be captured by `std::panic::set_hook()` + `tracing::error!(panic_msg, backtrace)`
- per §11 Anti-Patterns: "NEVER leak absolute host paths or internal struct names in logs" — path-canonicalize guard must scrub any exception traces before display
- per §6 Log Coverage: all boundary errors (including config validation failures) are logged at `error` or `warn` level with structured fields; `ConfigError` harness-fault class logs to JSONL with `run_id` context (once available)
- per §11 Anti-Patterns: "NEVER pass `ANDROMEDA_PULSE_DATA_DIR` via argv to sidecar spawn — must use `.env(...)` builder"; applies to any `CONDUCTOR_*` path-handle resolution wiring

## Patterns to follow
- per §4 Span / Trace Coverage: garde validation failures are boundary-call errors (MCP-class: external contract violation); if a span wraps the config load, use `error` level + structured error context (validation report fields)
- per §6 Log Coverage: boundary-call wrappers on config deserialization / garde validation / path canonicalization (error if any field is malformed); required fields in error log: error type, field name, constraint violated, suggestion (per garde `Report`)
- per §2 Telemetry Strategy: validation failures are "boundary calls" in the Minimal-tier pyramid; they are agent-parseable structured errors, NOT dashboards-only

## Anti-patterns to avoid
- do NOT introduce an OTel SDK or metrics instrument for config validation (violates §11 creator-explicit ban)
- do NOT allow malformed config to proceed; a validation failure MUST be `Result::Err`, never a `Verdict::Fail` or `ReportState` (§11: verdict/error wall)
- do NOT add retry-once or silent degradation on config validation failure (§10: zero-flakiness parallel)

## Contract bindings
- **config-validation ↔ tests harness (§3):** tests' binding contract (§6 Log Coverage schema) includes `run_id`; config validation at CLI edge must not emit runs until config is validated; agent reads validation errors from stderr/logs for CI gate (obs CI gate, §9)
- **config-validation ↔ security plan (Input Validation):** path-canonicalize guard is the edge control; garde validates identity fields; both report separately (path guard errors are I/O errors, config errors are validation reports)

## Acceptance criteria contributions
- "(obs) Config validation errors logged as structured `error` or `warn` with fields: validation_error_type (string), field_name (string), constraint (string), suggestion (string); no absolute paths or module names in error messages."
- "(obs) Path-canonicalize errors logged separately (I/O boundary, not validation); redact absolute paths from exception traces before display."
- "(obs) Any panic during config/path validation captured by `std::panic::set_hook()` and emitted as `tracing::error!(panic_msg, backtrace_sanitized)` to JSONL; no unstructured panic to stderr."
- "(obs) Validation failures = `Result::Err(ConfigError)` harness fault, never a verdict or report state; CI log-conformance gate (§9) reads stderr/logs and fails build if unlogged panics or validation errors lack required fields."

## Relevant amendment history
(none)