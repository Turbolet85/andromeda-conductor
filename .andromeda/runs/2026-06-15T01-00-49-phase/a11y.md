# a11y extract

## No domain coverage
Config-validation surface deals exclusively with load-time validation of scenario config structs (garde rules on `Scenario`/`PId`, path-handle canonicalization) and error classification (`CoreError` variants). The chunk specifies "NO scenario-file loading/deserialization pipeline" and "NO new seam crates and no behavior in timeline/emit/faults/verify/report" — it is entirely internal config-validation logic. Per a11y-plan §1, "cli surface is not-assertable (no DOM / native widget tree)" and "only the desktop-webview is assertable"; a11y domain applies ONLY to the Tauri GUI surfaces (operator-pause dialog, verdict lamp, coverage matrix, etc.). Validation failures → `CoreError` → harness faults are error-handling contracts, not UI-surface accessibility concerns.