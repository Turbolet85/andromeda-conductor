proposals: []

(D-obs-instrumentation: no drift — config validation is load-time, not one of the 7 must-trace ops; instrumentation n/a. D-obs-stack: no drift — only dep change is garde (validation lib), no OTel SDK/OTLP exporter. D-obs-redaction: no drift — resolve_under error messages carry no absolute paths / struct names; no logging/artifact sink added.)