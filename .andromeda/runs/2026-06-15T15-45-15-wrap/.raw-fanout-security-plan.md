proposals:
  - detector: D-security-input
    severity: warning
    section: "## Input Validation (scenario-config row)"
    change: "garde 0.22.1 #[derive(Validate)] range + #[garde(custom)] cross-field, co-located; failed Report → ConfigError via #[from]"
    sidecar: "garde downgraded 0.23.0 → 0.22.1 (user-authorized — garde_derive 0.23.0 absent from registry; 0.22.1 + derive available, functionally equivalent for Scenario/PId validators)"
    rationale: "Report: downgrade user-authorized this session (garde_derive 0.23.0 unavailable). Security-plan §Input Validation specifies 0.23.0; update to 0.22.1."
  - detector: D-security-input
    severity: warning
    section: "## Bootstrap phases — input-validation-library-install"
    change: "confirm garde 0.22.1 + serde 1.0.x wired with #[derive(Validate)] + #[garde(custom)]; CONDUCTOR_* path canonicalize at the cli edge outside garde"
    sidecar: "input-validation-library-install garde pin 0.23.0 → 0.22.1 per chunk 2026-06-15-config-validation-surface"
    rationale: "Report designates spec impact; bootstrap phase specifies 0.23.0; chunk used 0.22.1 with functionally identical validators."

(D-security-input on the validation SURFACES themselves: PASS — Scenario/PId garde-validated, resolve_under canonicalize+reject present per §Input Validation. D-security-subprocess: PASS — report does not touch sidecar/preflight/data-dir. D-security-deps: §Dependency Security does NOT pin a garde version, so no drift there; the only garde mentions are §Input Validation + §Bootstrap phases above.)