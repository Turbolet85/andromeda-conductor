proposals:
  - detector: D-tests-framework
    severity: warning
    section: "2. Test Strategy"
    change: "Framework: cargo-nextest 0.9.137 (target framework; Foundation chunks use interim cargo test + #[test] until the Test-Framework chunk lands per the recurring pattern)."
    sidecar: "Clarified test-framework interim state for Foundation chunks (plain cargo test acceptable before nextest/rstest installation chunk)."
    rationale: "Report: plain cargo test + #[test] as interim pattern; test-plan §2 names nextest 0.9.137 without caveat that early chunks defer to a later framework-installation chunk."

(D-tests-coverage: PASS — new paths resolve_under + Scenario/PId validation carry unit tests at the mandated tier; deterministic. D-tests-obs-harness: PASS — report changes no harness/status-read/log-format; test-plan §3 ↔ obs-plan §3 still agree.)