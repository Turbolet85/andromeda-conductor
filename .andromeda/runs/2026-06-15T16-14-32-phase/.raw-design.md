The amendments file doesn't exist (normal for a fresh project). Now let me structure the extract based on the focus guide, the design system plan, and the chunk scope.

# design extract

## Relevance
out-of-scope — dependency-audit gate is a supply-chain tooling chunk with no surface/UI rendering.

## No domain coverage
Dependency audit is a build-gate configuration task (cargo-audit + cargo-deny + Cargo.lock verification) with no rendered UI, typography, color, motion, spacing, depth, or icon requirements; the design system governs surfaces (`desktop-webview` + `cli`), and this chunk adds neither.