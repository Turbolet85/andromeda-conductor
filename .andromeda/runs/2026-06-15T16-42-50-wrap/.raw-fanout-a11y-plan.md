VERDICT: no drift on both. D-a11y-surface — no new interactive UI element. D-a11y-obs-schema — no change to
the a11y violation JSON schema or the obs log schema (only deny.toml config added).

proposals: []
