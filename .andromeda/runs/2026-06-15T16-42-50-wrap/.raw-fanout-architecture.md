VERDICT: no drift. deny.toml is build-config (not an Occupied Resource — no port/socket/env/crate);
cargo-audit/cargo-deny are external CLI dev tooling (not Cargo.lock deps), consistent with §Infrastructure
Patterns precedent (clippy/nextest, optional cargo-modules/cargo-rail). Stack unchanged. publish=false is
consistent with the local-only/no-cloud posture.

proposals: []
