VERDICT: no drift on all 3. D-obs-instrumentation — no new operations (no code). D-obs-stack — no telemetry
deps added; cargo-audit/cargo-deny are CLI gates, not the logging library; no OTel SDK. D-obs-redaction — no
logging/artifact writes added; deny.toml is a static policy file.

proposals: []
