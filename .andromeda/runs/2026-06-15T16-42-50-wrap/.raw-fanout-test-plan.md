VERDICT: no drift on all 3. D-tests-coverage — no new code paths (no source). D-tests-framework — runner is
cargo-nextest (on-spec §2/§4); the default-vs-ci profile difference is acknowledged temporary sequencing
(ci profile built by a later chunk), not framework drift. D-tests-obs-harness — 5-command harness/envelope
untouched.

proposals: []
