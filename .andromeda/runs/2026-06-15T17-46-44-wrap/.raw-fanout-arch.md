proposals:
  - detector: D-arch-resources
    severity: warning
    section: "§Occupied Resources / Environment variables"
    change: "Add `CONDUCTOR_SERVICE_NAME` (obs-plan §3; overrides default service name for observability) and `CONDUCTOR_ENV` (obs-plan §3; deployment environment tag, default `local`) to the list of reserved `CONDUCTOR_*` env-var handles."
    sidecar: "Register CONDUCTOR_SERVICE_NAME and CONDUCTOR_ENV in arch env-var inventory (chunk 2026-06-15-structured-logging-stack)"
    rationale: "init_observability reads CONDUCTOR_SERVICE_NAME and CONDUCTOR_ENV to populate ServiceIdentity (report Changes/env vars), but these are not enumerated in arch §Occupied Resources env-var section (lists only RUNS_DIR/SCENARIOS_DIR/CONTRACT_MANIFEST/SEED). Report flagged this for D-arch-resources."

# D-arch-decisions: NO DRIFT — tracing 0.1.44 + tracing-subscriber 0.3.23 are named in arch §Stack ("Structured JSON self-observation logs; NOT an OTel SDK"); chunk uses them without an OTel SDK, consistent with §Established Decisions [OTLP Emission Strategy].