proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "3. Observability Harness Contract / Log format JSON schema"
    change: "Self-obs JSON line schema is {timestamp_ms (epoch millis), level, target, service.name, service.version, deployment.environment, run_id, + event fields}. §3/§6 'Log format JSON schema' currently lists journal_emitted_at (ISO-8601) + the full envelope on every line; reconcile to a two-schema model: (a) per-line self-obs base fields incl timestamp_ms + service.*; (b) the scenario-result envelope (journal_emitted_at, seed, scenario, p_ids, verdict, state, latency_ms, slo_tier, fingerprints) emitted on the scenario.run terminal event by later chunks."
    sidecar: "Self-obs line timestamp is epoch-millis timestamp_ms (std::time); envelope journal_emitted_at + result fields populated by later timeline/verify/report chunks."
    rationale: "Report line 17 + Deviation 3: self-obs base line carries timestamp_ms; obs §3/§6 schema block currently conflates the per-line base with the scenario-result envelope. Justified divergence (foundational lines can't carry verdict/latency yet) → clarify the two-schema model."

  - detector: D-obs-redaction
    severity: warning   # (detector drift-base severity is ESCALATE)
    section: "6. Log Coverage / §11 redaction"
    change: "Note the field-allowlist redaction wrapper is a deferred seam (built, not wired) — wired by the next chunk 'Log + error-boundary redaction'; current synthetic-only self-obs logs leak no paths/struct names."
    sidecar: "Redaction layer deferred to the next chunk; seam built, not wired; no current leak."
    rationale: "Report 'PII n/a … redaction is the next chunk — seam built, not wired'. obs §6/§11 mandate redaction on every log write; obs §3 already lists pii-scrubbing-wire as a SEPARATE downstream bootstrap phase, so this is correct sequencing, not a leak."

# D-obs-stack: NO DRIFT — tracing JSON only, NO OTel SDK (report confirms no opentelemetry-*); matches obs §3 exactly.