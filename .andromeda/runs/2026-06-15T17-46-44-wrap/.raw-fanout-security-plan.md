proposals:
  - detector: D-security-input
    severity: warning   # (detector drift-base severity is ESCALATE)
    section: "## Input Validation"
    change: "Add rows for CONDUCTOR_SERVICE_NAME (string, default 'conductor'; service.name JSON field) and CONDUCTOR_ENV (string, default 'local'; deployment.environment field) — non-secret CONDUCTOR_* handles; validation implicit (safe defaults, no user input required if unset)."
    sidecar: "Input Validation — added CONDUCTOR_SERVICE_NAME / CONDUCTOR_ENV env-var boundaries (safe defaults)"
    rationale: "Report Changes/env vars document two new env-var reads with safe defaults; the §Input Validation table is the canonical boundary registry; agent proposes enumerating them even though validation is implicit (not garde/canonicalize)."

# D-security-subprocess: NO DRIFT — chunk does not touch MCP sidecar spawn / preflight / data-dir.
# D-security-deps: NO DRIFT — tracing/tracing-subscriber audit-green (cargo audit exit 0 / cargo deny ok per report Outcome); toolchain ≥1.94.1; no tauri/sqlite change.