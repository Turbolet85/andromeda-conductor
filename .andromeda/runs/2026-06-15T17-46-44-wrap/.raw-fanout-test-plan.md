proposals:
  - detector: D-tests-obs-harness
    severity: warning
    section: "3. Test Harness Contract / Log format"
    change: "Add service.name, service.version, deployment.environment as required flat-level fields on every JSONL line."
    sidecar: "Log format binding contract updated to include service identity fields (obs-plan §3 alignment)."
    rationale: "Report Deviation 1 + Outcome confirm JsonObsLayer emits flat service.* on every line per obs-plan §3 'Service identity'. Test-plan §3 log-format schema currently lists only envelope identity fields, omitting service.* — binding-contract gap (harness writes them, tests grep them, but test-plan does not document the contract)."

# D-tests-coverage: NO DRIFT — 5 new unit tests for conductor-core::obs; nextest 25/25; deterministic (std::time, no real network).
# D-tests-framework: NO DRIFT — runner is cargo-nextest per §4 (default profile interim; --profile ci pending the Test-framework chunk's .config/nextest.toml — playbook rule).