# design extract

## Relevance
Out-of-scope for this chunk.

## No domain coverage
This chunk is test infrastructure (test framework, fixtures, coverage tooling, exemplar tests); design system applies to user-facing UI surfaces (desktop-webview, cli) and content rendering. The scope explicitly excludes production code and content — it wires dev-dependencies and proves tool functionality with minimal exemplar tests only, touching no surfaces, typography, colors, spacing, motion, or iconography.