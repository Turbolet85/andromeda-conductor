# a11y extract

## Relevance
out-of-scope for this chunk — test framework / fixtures / coverage tooling is headless infrastructure without rendered UI surface.

## No domain coverage
The chunk covers only Rust test harness infrastructure (nextest, cargo-llvm-cov, rstest, proptest, insta, assert_cmd/fs) with no desktop-webview or interactive UI surface; per a11y-plan §1 accessibility assertion specification, "The cli surface is not-assertable (no axe/Lighthouse reach)" and this chunk spans only CLI-adjacent test tooling with zero UI surface. No WCAG criteria, ARIA roles, focus management, keyboard navigation, or color-contrast tokens are triggered. A11y verification harness integration (per a11y-plan §3) reuses tests §6's `@crabnebula/tauri-driver` + WebdriverIO session for webview-E2E a11y gates — this chunk provisions the foundation CI runner and test-profile config, not the a11y-specific injection points, which land in a later chunk's UI test layer.