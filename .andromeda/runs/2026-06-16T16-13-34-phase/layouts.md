# layouts extract

## Relevance
Out-of-scope for this chunk

## No domain coverage
This chunk (test framework + fixtures + coverage tooling) is a backend/CLI tooling task with zero UI surface modifications. Layout-templates covers desktop-webview + cli OUTPUT surfaces; this chunk only stands up dev-dependencies, nextest config, and coverage tools — no layout structure, wireframes, component placement, focus order, or responsive behavior are introduced or modified. No new surfaces are rendered; no existing surfaces are altered.