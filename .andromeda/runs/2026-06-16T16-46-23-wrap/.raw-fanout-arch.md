Verdict: 1 proposal (warning). cargo-nextest is in arch but marked optional; the chunk makes it the pinned
runner with `.config/nextest.toml` retries=0; llvm-tools-preview + dev-test crate pins are unregistered.

proposals:
  - detector: D-arch-decisions
    severity: warning
    section: "Infrastructure Patterns — Build system"
    change: "test with `cargo nextest` (required, 0-retry CI profile in `.config/nextest.toml`) + `cargo test --doc`; toolchain component `llvm-tools-preview` required for cargo-llvm-cov; dev-test crate pins rstest/proptest/insta/assert_cmd/assert_fs/predicates."
    sidecar: "Promote cargo-nextest optional→required; document `.config/nextest.toml` zero-retry; register llvm-tools-preview + dev-test crate pins."
    rationale: "Report: chunk adds `.config/nextest.toml` [profile.ci] retries=0 as the pinned determinism gate; nextest now enforces the CI test path; llvm-tools-preview + dev-deps are infrastructure facts arch should register."
