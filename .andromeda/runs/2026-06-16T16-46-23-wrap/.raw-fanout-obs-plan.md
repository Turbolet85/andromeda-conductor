Verdict: no drift. D-obs-instrumentation (no new must-trace operation — test code only). D-obs-stack (no
telemetry dep added; tracing stack untouched; no OTel SDK/exporter). D-obs-redaction (no logging/artifact
write added; cli_smoke uses a synthetic temp path; redaction layer is exercised + proven by the proptest
idempotence exemplar).

proposals: []
