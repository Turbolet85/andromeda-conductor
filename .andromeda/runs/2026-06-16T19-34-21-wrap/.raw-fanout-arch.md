# arch drift-detector return

Evaluated D-arch-resources + D-arch-decisions against the report.
- D-arch-resources: conductor-timeline already registered (§Occupied Resources crate names); no new port/env-var/IPC/crate. BUT rand_chacha 0.9 / rand_core 0.9 added to [workspace.dependencies] are absent from §Stack.
- D-arch-decisions: tokio current_thread / tracing / thiserror all allowed; RNG selection (ChaCha8Rng + seed_from_u64) is a new runtime decision not in §Established Decisions.

```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: Stack and Technologies
    change: "Add a Determinism RNG row: rand_chacha 0.9 (ChaCha8Rng) + rand_core 0.9 (SeedableRng) — seeded per-gap jitter, seed_from_u64 reproducibility."
    sidecar: "rand_chacha + rand_core added to workspace deps for deterministic RNG; registered in Stack table."
    rationale: "Report §Dependencies lists rand_chacha 0.9 + rand_core 0.9 as new workspace deps; absent from §Stack."
  - detector: D-arch-decisions
    severity: warning
    section: Established Decisions
    change: "Add [RNG / Deterministic Seeding] rand_chacha::ChaCha8Rng via seed_from_u64 — algorithm-stable across platforms/versions; same seed ⇒ same shape; elapsed_ms is a cumulative sum, not a clock read."
    sidecar: "RNG selection + seeding strategy locked (ChaCha8 + seed_from_u64) for deterministic jitter."
    rationale: "Report §Decisions & corrections documents the RNG/seeding commitments future seams depend on; not yet in §Established Decisions."
```
