# design-system drift-detector return

Evaluated D-design-tokens. The chunk is headless backend (conductor-timeline); the report marks tokens **n/a (no UI)**. No UI rendered → invariant inapplicable.

```yaml
proposals: []
```
