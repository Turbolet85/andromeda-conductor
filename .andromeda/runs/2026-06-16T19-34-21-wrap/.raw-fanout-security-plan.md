# security-plan drift-detector return

Evaluated D-security-input + D-security-subprocess + D-security-deps.
- D-security-input: no new external-input surface (typed PhaseTimeline + u64; no scenario-config/CONDUCTOR_*/MCP stdout). No drift.
- D-security-subprocess: no sidecar/preflight/data-dir touched. No drift.
- D-security-deps: new deps rand_chacha/rand_core present (transitively via proptest; audit-green). Agent ALSO claimed tauri "remains 2.10.1, below the ≥2.10.3 floor" and proposed an escalate.

```yaml
proposals:
  - detector: D-security-deps
    severity: escalate
    section: "Dependency Security § Update policy"
    change: "the chunk adds rand_chacha 0.9 + rand_core 0.9 (audit-green); claims tauri must bump 2.10.1→≥2.10.3 (CVE-2026-42184)."
    sidecar: "D-security-deps: tauri bump required per §Dependency Security."
    rationale: "agent read §Dependency Security's tauri ≥2.10.3 mandate."
```

**ORCHESTRATOR VALIDATION (main):** FALSE POSITIVE — REJECTED. Verified against ground truth: `Cargo.toml:60` already pins `tauri = "2.10.3"` (at floor); `git diff HEAD -- Cargo.toml` shows this chunk added ONLY rand_chacha/rand_core (tauri untouched). The agent misread the security-plan's historical rationale ("arch originally pinned 2.10.1 → bump to ≥2.10.3") as a current gap. The chunk's real new deps are audit/deny-green → allowed by §Dependency Security. Dismissed WITH the user (2026-06-16); a playbook rule was added for the misfire class. No security-plan amendment.
