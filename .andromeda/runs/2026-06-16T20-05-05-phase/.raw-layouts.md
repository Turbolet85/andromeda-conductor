The amendments file does not exist yet (expected on a fresh project). Let me now extract the relevant domain guidance based on the focus guide, plan, and chunk scope.

# layouts extract

## Relevance
partial — the chunk is config-model and validation; layouts does not govern data schema but touches interface boundaries and multi-surface coordination

## Constraints
- Per layout-templates §Surface: cli + desktop-webview, both single-surface (no router/breakpoints); the scenario config's phase sequence maps deterministically to timeline phases, which feed into both surfaces' display logic — must maintain cross-surface token-mapping consistency (per §Multi-surface coordination)
- Per layout-templates §Signature placement, the operator-pause go/no-go dialog (both surfaces) depends on a frozen phase step-index + count visible at the decision moment; the scenario config's per-phase identity/kind must be legible as phase-line text (Heading role, `text-primary`) on both desktop and cli
- Per layout-templates §Component placement, the coverage matrix (desktop) and comfy-table (cli) surface per-P-ID verdict lamps; the scenario fixture must exercise at least one representative P-ID to ensure the phase-timeline conversion feeds verdict-lamp display without loss
- Per layout-templates §Motion, verdict lamps resolve motionless via `motion-micro` color transition; the scenario-config model is data only, but the phase sequence's deterministic order ensures lamps resolve in the correct sequence (no rework on Epoch-3 OTLP emission)
- Per layout-templates §IA notes, both surfaces surface "Conductor never emits a native OS toast"; the scenario config's emission spec must be forward-compatible with Epoch-3 emit (no rework, no skipped phases) — the declarative spec shape must not force re-deserialization

## Patterns to follow
- Phase identity (phase-line text on both surfaces): short, operator-meaningful names (`error-baseline-spike`, `restart-suppression`, `fingerprint-storm`) — legible as Heading role, fit in titlebar width (desktop) and pre-prompt line (cli)
- Deterministic phase ordering: scenario config's phase sequence is order-preserving into `PhaseTimeline`; the coverage matrix and SLO table both rely on step-index (`step 14/27`) — phase list length must be stable (no dynamic count, no conditionals)
- Per-phase emission spec: declarative serde struct carrying phase identity, timing contribution (duration/gap), and a description of what that phase emits; must not embed computation or wire-building (deferred to Epoch 3)

## Anti-patterns to avoid
- Do NOT use phase identity text longer than ~20 chars (titlebar width constraint on desktop §Component — Header; cli phase line width constraint §Component — Primary navigation)
- Do NOT make the per-phase emission spec conditional or non-deterministic (phase ordering must be stable; the signature placement depends on a frozen step-index that never changes post-deserialize)
- Do NOT embed OTLP emission logic or wire-building in the config model (emission spec is declarative data only; Epoch-3 emit owns the actual span/metric/log emission)

## Contract bindings
- **desktop-webview titlebar §Component — Header:** phase-line text derives from `Scenario.phases[N].identity` (Heading role, `text-primary`); on operator-pause, the frozen step-index is legible in the titlebar and dialog header (signature placements #1/#2)
- **cli phase line §Component — Primary content block 1:** phase-line text is the same `identity` field, printed with colored prefix (ANSI 114 / 179 / 203 / 60 per verdict state), above the `inquire` hold prompt (signature placement #2)
- **coverage matrix + SLO table step counts:** both surfaces display `step N/total` derived from `Scenario.phases.len()` (deterministic, stable post-deserialize); the total never changes mid-run

## Acceptance criteria contributions
- "(layouts) Phase identity (Scenario phase field) renders as Heading text in desktop titlebar + cli phase line without truncation at 20-char boundary (layout-templates §Component — Header + §Component — Primary navigation)."
- "(layouts) Scenario phase sequence deterministically maps to `PhaseTimeline` with order preserved and no insertion/deletion; step-index in both surfaces matches `Scenario.phases` indexing (layout-templates §Signature placement + §IA notes Determinism invariant)."
- "(layouts) Scenario fixture with ≥1 representative phase exercises phase-timeline conversion end-to-end; coverage matrix (desktop) and comfy-table (cli) both display the phase identity without truncation (layout-templates §Component — Primary content block 1 + §Component — Primary content block 2)."

## Relevant amendment history
(none)
