# a11y extract

## No domain coverage

This chunk is a backend config-schema/validation layer with no rendered UI surface; a11y domain covers only the desktop-webview Tauri GUI (per a11y-plan §1 Scope Summary, "assertable surface") and explicitly excludes the cli/headless surfaces (not-assertable, no DOM/widget tree).
