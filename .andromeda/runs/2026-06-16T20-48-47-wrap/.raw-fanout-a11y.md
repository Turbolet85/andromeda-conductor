proposals: []

# D-a11y-surface: no drift — new items are internal modules/types (phase_spec, convert, EmissionSpec, Signal), not interactive UI; the four must-be-accessible paths unchanged.
# D-a11y-obs-schema: no drift — the new TOML scenario-config schema is a different artifact from the a11y violation schema; a11y violation schema → obs §6 envelope binding unchanged (chunk emits no telemetry).