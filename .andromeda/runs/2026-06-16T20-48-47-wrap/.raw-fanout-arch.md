proposals:
  - detector: D-arch-decisions
    severity: warning
    section: "Stack and Technologies"
    change: "Add row: | Scenario config serialization | toml 0.9 | On-disk TOML config for scenario loading (`from_toml_str`); TOML chosen by P4 user decision over JSON for hand-author ergonomics and inline comments. |"
    sidecar: "toml 0.9 added to stack per P4 user decision (scenario-config-model chunk, 2026-06-16). Transitive: toml_parser 1.1.2, toml_datetime, winnow 0.7.15."
    rationale: "Report documents P4 user decision to use TOML (not JSON) for scenario config; toml 0.9 integrated into conductor-core, validated with garde, tested. §Stack omits TOML; register alongside serde_json to reflect actual production deps."

# D-arch-resources: no drift — new items are internal Rust modules/types (phase_spec, convert), not new crates/ports/env vars/IPC; scenarios/ TOML format already covered by §Conventions.