proposals: []

# D-design-tokens: no drift — chunk is pure backend (Rust types, TOML parse/validate, type conversion); renders no UI, emits no tokens. Report Coverage shows tokens n/a for all three new surfaces. design-system.md unviolated.