proposals: []

# D-layout-surface: no drift — chunk adds backend config infrastructure (TOML parse, validation, types) but no new user-facing surface/region; parse boundary + conversion explicitly not external surfaces. Scenario selection uses the pre-existing picker UI already documented in layout-templates.