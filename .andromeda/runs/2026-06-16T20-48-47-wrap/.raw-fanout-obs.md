proposals: []

# D-obs-instrumentation: no drift — chunk adds no new must-trace operations; from_toml_str is a parse boundary, From<&Scenario> is an internal conversion feeding the already-instrumented run_timeline; config layer emits no telemetry per obs-plan §1.
# D-obs-stack: no drift — only toml added (data-plane); no OTel SDK / tracing-subscriber / exporter introduced.
# D-obs-redaction: no drift — parse errors go through conductor_core::sanitize_error (redaction✓); no new JSONL/runs.db/report writes; PII n/a (synthetic fixture). Redaction obligation properly scoped to the Epoch-6 report seam.