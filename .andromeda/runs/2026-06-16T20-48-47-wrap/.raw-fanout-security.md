proposals: []

# D-security-input: no drift — scenarios/*.toml external input validated garde✓ (from_toml_str validates after deserialize; bounds documented).
# D-security-subprocess: no drift — chunk makes no MCP sidecar/preflight/data-dir change.
# D-security-deps: no drift — new dep toml 0.9 is audit-green + cargo deny ok + Cargo.lock updated; toolchain ≥1.94.1 and tauri pins intact (untouched). §Dependency Security invariant holds.