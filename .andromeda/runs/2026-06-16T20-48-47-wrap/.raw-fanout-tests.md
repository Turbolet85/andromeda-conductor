proposals: []

# D-tests-coverage: no drift — new APIs carry unit tests; llvm-cov 89.52% (phase_spec.rs & convert.rs 100%), well above the §10 Minimal 60% threshold; determinism seeded (start_paused, no real network/time).
# D-tests-framework: no drift — runner is cargo-nextest; plain #[test] + helper fns is allowed by §4 (rstest listed alongside #[test]/#[tokio::test], not mandatory) and matches the existing conductor-core convention per the report's deviation note.
# D-tests-obs-harness: no drift — chunk does not modify the 5-command harness, status-read, or JSONL log format; test-plan §3 ↔ obs-plan §3 remain in sync.