# a11y extract

## Relevance
Partial — chunk introduces a data-persistence/schema surface (emission journal JSONL) with no UI rendering; a11y applies only to the schema contract binding with obs, not to the journal-writer implementation itself.

## Constraints
1. Per a11y-plan §3 Structured violation JSON schema (binding to obs §6 Log Format JSON Schema): a11y violation events must align to obs envelope format, folding WCAG rule-id + severity + selector into `fingerprints[]` without introducing new top-level fields or leaking host paths / internal struct names.
2. Per a11y-plan §2 Agent-runnable invariants: every WCAG SC verification must emit machine-verifiable JSON (axe-core / colorjs.io / WebdriverIO structured output), never sole manual verification.
3. Per a11y-plan §3 A11y assertion harness specification: artifacts MUST NOT leak absolute host paths or internal seam-crate struct names (consistent with tests' Status JSON constraint).
4. Per a11y-plan §3 Structured violation JSON schema: journal envelope fields stay fixed to obs-defined shape; a11y narrows within that contract without diverging.

## Patterns to follow
1. Reuse obs-owned log schema envelope (journal_emitted_at, run_id, seed, scenario, p_ids, verdict, state, latency_ms, slo_tier, fingerprints) per a11y-plan §3 Structured violation JSON schema; emit a11y violations as fingerprint tuples (axe rule-id + WCAG SC tag + DOM selector) folded into the existing schema, not new top-level fields.
2. Emit machine-verifiable violation JSON from axe-core (via @axe-core/webdriverio against tauri-driver/WebdriverIO session) + colorjs.io token-name assertions + WebdriverIO keyboard PASS/FAIL structured output; all three fold into obs §6 envelope per a11y-plan §3.

## Anti-patterns to avoid
1. Do NOT invent new top-level fields in the journal envelope; a11y violations ride inside fingerprints[] only — a11y-plan §3 Structured violation JSON schema.
2. Do NOT leak absolute host paths, internal struct/field names, or crate-scoped types into journal lines (artifact hygiene constraint) — a11y-plan §3 Artifact hygiene.

## Contract bindings
**obs ↔ a11y:** Journal envelope fields (journal_emitted_at, run_id, seed, scenario, p_ids, verdict, state, latency_ms, slo_tier, fingerprints) are obs-owned; a11y violations fold into fingerprints[] as structured tuples per a11y-plan §3. No new envelope fields added by a11y domain.

## Acceptance criteria contributions
1. (a11y) Journal schema respects obs §6 envelope contract — no new top-level fields introduced; WCAG violations nest inside `fingerprints[]` as structured tuples (axe rule-id + WCAG SC tag + selector) — per a11y-plan §3 Structured violation JSON schema.
2. (a11y) Artifact hygiene: journal lines contain no absolute host paths or internal struct/field names (redaction allowlist consistency) — per a11y-plan §3 Structured violation JSON schema.

## Relevant amendment history
(none) — a11y-plan-amendments.md does not exist; sidecar created lazily on first amendment.