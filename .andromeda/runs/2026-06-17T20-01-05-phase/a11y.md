# a11y extract

## No domain coverage
Out-of-scope — the determinism-replay harness is backend test infrastructure within `conductor-timeline` producing only `Vec<PhaseTransition>` assertions under `start_paused`, with zero UI surface, DOM, ARIA roles, keyboard contracts, focus traps, contrast tokens, or screen-reader announcements. Per a11y-plan §1 (assertable-vs-not), only the desktop-webview surface is assertable; this chunk has no renderable surface, so no WCAG criteria apply.
