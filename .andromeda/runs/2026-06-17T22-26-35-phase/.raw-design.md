Now I have all the context I need. Let me analyze the focus guide, the design system plan, the chunk scope, and the amendment history to extract the relevant design guidance for this chunk.

**Analysis:**

1. **Chunk scope**: The chunk is "Raw OTLP message scaffold" — building `conductor-emit` crate with raw protobuf structs, tonic/gRPC client, and minimal end-to-end emit path. It explicitly does NOT render UI.

2. **Focus guide scope**: The focus guide instructs extraction of tokens/typography/motion/iconography/component-patterns that applies when "the chunk involves... renders text / involves layout / creates rectangular containers / animates / transitions / uses iconography / extends or composes them."

3. **Chunk coverage**: This chunk is foundational backend infrastructure — raw protobuf message construction + gRPC egress layer. It has:
   - No UI rendering (no colors, typography, spacing, depth, border radius, motion needed)
   - No component patterns to reuse
   - No iconography
   - No animations or transitions
   - No layout or placement decisions

4. **Amendment history**: The single amendment (2026-06-15) affects token *declaration syntax* (`:root` vs `@theme`), not design guidance. It's relevant only when implementing the `tokens.css` — which this backend chunk does not do.

This chunk is explicitly out of scope for the design domain. It's a backend emission primitive with no surface rendering.

# design extract

## No domain coverage

This chunk builds backend emission infrastructure (`conductor-emit` crate — raw OTLP protobuf structs + tonic gRPC client) with no UI surface, rendering, or component composition; design tokens, typography, motion, and component patterns do not apply.