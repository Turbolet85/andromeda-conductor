# a11y extract

## Relevance
out-of-scope for this chunk

## No domain coverage
This chunk (Raw OTLP message scaffold) is a backend/gRPC emission primitive with no rendered UI surface, no DOM, no interactive controls, and no visual rendering — therefore no WCAG criteria, ARIA roles, focus management, or contrast verification apply per a11y-plan §1 assertability classification ("not-assertable — CLI line-oriented terminal surface, no DOM / native widget tree; a11y tools have no applicability"). The chunk's boundaries explicitly exclude all UI-facing work (emotion surface lives in Epoch 4+; only backend message construction and gRPC egress in scope here).