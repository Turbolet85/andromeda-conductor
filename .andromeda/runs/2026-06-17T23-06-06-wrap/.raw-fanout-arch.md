proposals:
  - detector: D-arch-decisions
    severity: warning
    section: "§Stack and Technologies / OTLP emission"
    change: "opentelemetry-proto 0.32.0 (`gen-tonic` + `trace`, `default-features = false` to exclude unused SDK); tonic 0.14.6; tonic-prost 0.14.6; prost 0.14 (via transitive only)"
    sidecar: "arch: document opentelemetry-proto feature gate to exclude SDK crates (emit chunk follows OTLP raw-type strategy, no SDK initialization); deferred in 2026-06-17 chunk as 'possible follow-up'"
    rationale: "Report §Deviations item 2: opentelemetry-proto's default feature pulls opentelemetry 0.32.0 + opentelemetry_sdk 0.32.1 into the tree, unused and contrary to §Established Decisions / OTLP Emission Strategy (raw types to avoid the SDK). Mitigation documented in report but not applied."
  - detector: D-arch-decisions
    severity: warning
    section: "§Stack and Technologies / Async runtime"
    change: "tokio 1.48.x (`current_thread`); tokio-stream 0.1 (dev/test fixtures only)"
    sidecar: "arch: explicitly list tokio-stream as a dev-only dependency for test-harness loopback gRPC stubs (emit chunk test infrastructure)"
    rationale: "Report §Changes: conductor-emit dev-deps now include tokio-stream 0.1 (NEW to workspace). Not listed in the declared stack table; arch §Stack is the single source of pinned deps."
# D-arch-resources: no drift (port :4317, conductor-emit crate, CONDUCTOR_SERVICE_NAME all already registered in §Occupied Resources).
