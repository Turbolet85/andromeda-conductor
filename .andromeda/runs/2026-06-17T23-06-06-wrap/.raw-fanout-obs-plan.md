proposals:
  - detector: D-obs-stack
    severity: warning
    section: "3. Observability Harness Contract / OTel SDK init"
    change: "SDK packages: NONE — No OTel SDK for Conductor self-observation; note that opentelemetry-proto transitive pull of opentelemetry 0.32.0 + opentelemetry_sdk 0.32.1 must be trimmed via default-features = false to keep the tree clean (current: not initialized but present; follow-up required)."
    sidecar: "2026-06-17 obs-pass: D-obs-stack warning — opentelemetry SDK crates transitively pulled by opentelemetry-proto default-features; no initialization/usage; follow-up: conductor-emit Cargo.toml set opentelemetry-proto default-features = false."
    rationale: "Report Deviation #2: opentelemetry-proto transitively pulls opentelemetry + opentelemetry_sdk. §3 OTel SDK init prescribes NONE; §11 anti-patterns ban SDK inclusion. While not initialized, the crates' presence diverges from the no-SDK invariant; report documents default-features=false as the follow-up."
# D-obs-instrumentation: no drift — emit.batch span present; emission_count-only (p_id_count) deviation documented+justified (scenario-level context unavailable at emit boundary); run_id via subscriber; std::time nanos.
# D-obs-redaction: no drift — EmitError not logged this chunk; no host-path/struct-name leak; field-allowlist ready.
