proposals:
  - detector: D-tests-obs-harness
    severity: warning
    section: "3. Test Harness Contract / 5-command implementation / `run` command body"
    change: "Dependency note: the opentelemetry-proto 0.32.0 transitive pull of opentelemetry + opentelemetry_sdk (through default features) is not used by Conductor and must be trimmed by setting default-features = false to preserve obs-plan §3's 'No OTel SDK' invariant (currently complied with in-code but not in dep-tree)."
    sidecar: "Observed transitive SDK crate inclusion; recommend future dep-tree cleanup to align opentelemetry-proto with obs-plan no-SDK baseline."
    rationale: "Report §Deviations(2); obs-plan §3 'No OTel SDK for self-observation' — the two now diverge on intent (SDK unused but present in tree). Report suggests default-features = false as follow-up."
# D-tests-coverage: PASS — trace_request unit-tested, egress/transport integ-tested, at mandated tiers.
# D-tests-framework: PASS — cargo-nextest as mandated.
