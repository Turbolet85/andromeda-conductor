# a11y extract

## Relevance
out-of-scope for this chunk

## No domain coverage

This chunk (error-spans emission primitive) is backend/IPC only: constructs OTLP span protobuf messages with error status and parent-child topology, ships them to `127.0.0.1:4317` via tonic gRPC. No rendered UI surface, no DOM, no desktop-webview. Per a11y-plan §1 A11y Scope Summary, conductor-cli ("agent-run" headless binary) is marked not-assertable — "CLI line-oriented terminal surface — no DOM / native widget tree; a11y tools (axe / Lighthouse / pa11y) have no applicability." This chunk is emission-side span construction only (no fault injection, no read-back verdict logic, no MCP/UI surface per scope.md boundaries). A11y assertions apply only to the desktop-webview control panel surface, which this chunk does not touch.
