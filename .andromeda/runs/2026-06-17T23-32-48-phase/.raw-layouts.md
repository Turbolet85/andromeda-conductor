The amendment history file does not exist, so I'll treat it as empty. Now let me analyze the coverage:

The chunk scope is **"Error spans — Status.Code=ERROR with root-vs-child placement"** from the `conductor-emit` crate (Epoch 3, emission primitives). It builds the first fault-meaningful span content by extending raw OTLP trace scaffolding with ERROR status construction and intra-trace span tree placement at root or deep-child positions.

The focus guide is clear: extract **layout structure, wireframe, component placement, focus order, responsive behavior, modal patterns, and navigation** that applies to this chunk's scope.

The chunk scope is **entirely a backend emission-construction primitive** in `conductor-emit` (Rust crate). It has:
- No UI surface (no desktop-webview, no CLI output beyond existing error/transport paths)
- No new component placement
- No wireframe modifications
- No focus order additions
- No responsive layout concerns
- No modal patterns introduced
- No navigation surface affected

The layout-templates.md plan describes surfaces (desktop-webview, cli) and their structures. This chunk operates at the OTLP protobuf emission layer — it produces `Status.Code=ERROR` signals and trace-tree topology for consumption by *later* visualization chunks (P-005 verdict logic in Epoch 5/7, P-008 calibration-region weighting). The layout templates document what *shows* these errors on the surfaces (verdict lamps, HOLD dialog, coverage matrix); this chunk is purely the data-construction that feeds them.

# layouts extract

## No domain coverage

Error spans chunk is a backend emission-construction primitive (OTLP Status/Span tree builder) with no UI surface, component placement, wireframe, or navigation footprint — layout templates apply to visualization consumption (later chunks), not emission construction.
