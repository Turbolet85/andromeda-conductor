# fan-out: obs-plan (obs-plan.md)
- D-obs-instrumentation → NO DRIFT: new ops are PRODUCT OTLP builders, not self-obs spans; they ride the existing `emit.batch` span via `TraceEmitter::export` (unchanged). No new must-trace critical path.
- D-obs-stack → NO DRIFT: no OTel SDK / OTLP exporter for self-obs; `rand_chacha`/`rand_core` are determinism deps, not telemetry. Dormant transitive `opentelemetry_sdk` is playbook-ratified routine (raw-otlp-message-scaffold wrap; obs-plan §3 already states the invariant is behavioral).
- D-obs-redaction → NO DRIFT: no new logging / artifact write; `message` flows into the PRODUCT `Status.message`, not self-obs logs; tests use synthetic payloads. No host-path/struct-name leak.

proposals: []
