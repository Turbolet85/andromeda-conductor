# fan-out: security-plan (security-plan.md)
- D-security-input → NO DRIFT: `error_trace_request(service_name, seed, placement, message)` takes typed args — no deserialization / CONDUCTOR_* path / MCP stdout / config surface. Report: validation n/a (no external input).
- D-security-subprocess → NO DRIFT: chunk is conductor-emit only; no sidecar spawn / preflight / data-dir touch.
- D-security-deps → NO DRIFT: `rand_chacha`+`rand_core` are workspace deps already in the lock (via conductor-timeline); no new external crate, no version bump; cargo-audit + cargo-deny green; Cargo.lock committed. (Playbook-ratified routine: seeded-phase-scheduler wrap.)

proposals: []
