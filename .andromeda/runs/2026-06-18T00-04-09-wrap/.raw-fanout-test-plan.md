# fan-out: test-plan (test-plan.md)
- D-tests-coverage → NO DRIFT: new paths carry tests at the mandated tier (4 unit + 2 loopback integration); determinism seeded; loopback stub never binds :4317 (ephemeral 127.0.0.1:0).
- D-tests-framework → NO DRIFT: cargo-nextest runner; 10/10 seam + 91/91 workspace; matches §2/§4.
- D-tests-obs-harness → NO DRIFT: chunk does not change the 5-command harness / envelope shape / JSONL log format (owned by test-plan §3 ↔ obs-plan §3, conductor-report). Agent drafted then RETRACTED a proposal on realizing the harness is untouched and 91/91 (incl. conductor-report envelope goldens) passed.

proposals: []
