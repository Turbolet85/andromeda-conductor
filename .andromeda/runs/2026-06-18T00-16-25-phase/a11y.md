# a11y extract

## No domain coverage

This chunk's scope (exception-event builder API + fingerprint-compute function in `conductor-emit`) has no rendered UI surface or interactive controls — a11y asserts only the desktop-webview Tauri GUI (per a11y-plan §1 Scope Summary), which this backend Epoch 3 emission-primitives chunk does not touch.
