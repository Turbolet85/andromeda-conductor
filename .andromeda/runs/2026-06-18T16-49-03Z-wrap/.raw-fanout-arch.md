proposals: []
# No drift: new symbols (Severity/severity_logs_request/LogsEmitter) live in the already-registered
# conductor-emit crate; no new IPC/endpoint/port/socket/env-var; opentelemetry-proto "logs" is a feature
# of an already-listed dep (no new library); no contradicted §Established Decision. emit.logs_batch span
# belongs to obs-plan, not arch §Occupied Resources (which enumerates ports/routes/crates/env vars).
