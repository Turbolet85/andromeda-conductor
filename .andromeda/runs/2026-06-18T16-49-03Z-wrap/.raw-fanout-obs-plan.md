proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "4. Span / Trace Coverage"
    change: "Add `emit.logs_batch` (OTLP logs egress self-observation span, record_count attribute) to the bounded span-name set alongside `emit.batch`."
    sidecar: "2026-06-18: Added emit.logs_batch span name to bounded set (logs egress distinguishable from trace egress in self-obs); acknowledged in severity-logs chunk report."
    rationale: "severity-logs (2026-06-18) introduces a new self-observation span `emit.logs_batch` on LogsEmitter::export to distinguish logs egress from trace egress. Low-cardinality, follows the {module}.{operation} convention, observation-only — must be added to §4 to preserve the bounded-set invariant."
# D-obs-stack: pass (NONE deps added; opentelemetry_sdk dormant/uninitialized; self-obs stays tracing-JSON; obs-plan §3 already states the invariant is SDK init/use not tree presence — playbook 2026-06-17).
# D-obs-redaction: pass (body is a fixed benign const, no host paths/struct names; no journal/runs.db writes).
