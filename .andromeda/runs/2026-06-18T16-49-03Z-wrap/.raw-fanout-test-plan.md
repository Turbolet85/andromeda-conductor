proposals:
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Observability Harness Contract / Critical paths (must-trace spans)"
    change: "Add `emit.logs_batch` to the enumerated must-trace span set for emission boundaries; distinguish from `emit.batch` (trace egress) per OTLP type (logs vs. traces)"
    sidecar: "Docs: obs-plan §3 critical-paths table extended to include logs-emission span `emit.logs_batch` (record_count field) per 2026-06-18-severity-logs chunk"
    rationale: "Report: new instrumentation span `emit.logs_batch` on LogsEmitter::export; report explicitly flags 'NEW span name not yet in obs-plan §4 bounded set'. One-sided drift — the span set must list it."
# D-tests-coverage: pass (5 unit + 2 integration tests; nextest 25/25 + 106/106; deterministic, loopback stub no real net).
# D-tests-framework: pass (cargo-nextest runner + plain #[test]/#[tokio::test], mirrors egress.rs; no rstest needed — not parametrized).
# NOTE (orchestrator): this is the test↔obs-bind VIEW of the same amendment obs-plan proposes; the edit lands in obs-plan (span set), not test-plan body.
