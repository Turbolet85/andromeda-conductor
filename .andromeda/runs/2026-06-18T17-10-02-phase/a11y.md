# a11y extract

## No domain coverage
Latency shaping is a backend emission primitive (conductor-emit) with no UI surface — CLI/headless code has no DOM, ARIA, focus, contrast, or keyboard reach per a11y-plan §1 (not-assertable surface excluded from Sections 2–4).