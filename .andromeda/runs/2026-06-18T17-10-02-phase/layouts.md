# layouts extract

## No domain coverage
Latency shaping (P-011, P-012) is a backend emission primitive that produces span duration values — zero surface layout, wireframe, component placement, focus order, or responsive behavior. Layout authority applies only to surfaces with user-facing interaction or rendering; this chunk is `conductor-emit` module scope with no desktop-webview / cli / UI element touched.