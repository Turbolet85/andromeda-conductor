# a11y extract

## No domain coverage

Multi-service topology is a backend emission primitive with no user-facing UI surface or rendered DOM — outside a11y assertion reach per a11y-plan §1 (assertability scope: desktop-webview only; conductor-emit/CLI marked not-assertable).
