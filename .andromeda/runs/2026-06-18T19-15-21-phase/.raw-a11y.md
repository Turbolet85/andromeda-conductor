# a11y extract

## Relevance
Out-of-scope for this chunk.

## No domain coverage
Chunk focuses on PII payload corpus emission (OTLP signal assembly, seeded corpus generation, deterministic embedding across spans/logs/exceptions) — no UI surface, no interactive controls, no WCAG criteria, no focus/keyboard/contrast/screen-reader contracts. A11y plan's entire scope is desktop-webview (Tauri GUI) + CLI (not-assertable); this chunk is headless emit-side infrastructure with no rendered surface per a11y-plan §1 "not-assertable CLI/headless surfaces — no DOM/ARIA reach." No ARIA patterns, no keyboard sequences, no contrast bindings, no focus traps. The upstream amendment (emission-journal-writer) tracks obs schema alignment only; no a11y behavior change.