## D-a11y-surface — PASS (no drift)
a11y-plan §1 lists 9 UI entities. This chunk adds NO interactive UI element — only programmatic OTLP builders emitting at Pulse :4317 (not to the Tauri webview). No WCAG/focus/keyboard surface to cover. No drift.

## D-a11y-obs-schema — PASS (no drift)
Report: "no runs.db/violation-schema touch." The a11y violation JSON schema (§3, reproduces obs §6) is unchanged; obs log schema unchanged. No divergence. No drift.

```yaml
proposals: []
```