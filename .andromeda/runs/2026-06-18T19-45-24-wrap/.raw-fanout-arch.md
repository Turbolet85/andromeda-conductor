## Detector: D-arch-resources — PASS (no drift)
New public APIs (PiiCategory, PiiCorpus, pii_trace_request, pii_logs_request) + new module conductor-emit::pii are additions to the already-registered conductor-emit crate. Report: "no new crate; one new module conductor-emit::pii (internal, re-exported)." No new IPC/endpoint/event/socket/port/env-var/crate to register.

## Detector: D-arch-decisions — PASS (no drift)
Dependencies: none added/none bumped (rand_chacha/rand_core/tokio/tokio-stream already deps). Uses pinned ChaCha8Rng (rand_chacha 0.9 / rand_core 0.9) per §Established Decisions [Determinism RNG]. OTLP raw-type builders feeding Pulse's P-035 scrubber — aligned with the emission-primitive seam. No new library/runtime, no contradicted locked decision.

```yaml
proposals: []
```