## D-design-tokens — PASS (no drift)
Detector scopes to "new UI the chunk renders." This chunk produces NO UI — backend/library Rust (PiiCategory/PiiCorpus + emission builders). Report Coverage marks all new surfaces "tokens n/a · a11y n/a". Implementation-only session. The invariant cannot be violated with no UI to evaluate.

```yaml
proposals: []
```