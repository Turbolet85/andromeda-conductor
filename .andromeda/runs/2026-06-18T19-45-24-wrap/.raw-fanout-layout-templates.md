## D-layout-surface — PASS (no drift)
layout-templates covers two user-facing surfaces (desktop-webview, cli). This chunk adds ZERO user-facing surfaces/regions: library-internal conductor-emit primitives (no binary/entry-point, no boot-path change, no new CLI verb/UI). Internal library APIs are not surfaces. No §Wireframe entry to reconcile. No drift.

```yaml
proposals: []
```