## D-obs-instrumentation — PASS (no drift)
Report: "instrumentation: existing egress spans emit.batch / emit.logs_batch cover the export (no new must-trace op)." The pure request-builders add no new traced operation. No uninstrumented critical path, no missing run_id, no virtual-clock journal stamp. No drift.

## D-obs-stack — PASS (no drift)
No OTel SDK / OTLP exporter for self-observation added. opentelemetry-proto (raw proto) + tonic are the PRODUCT emission lib (per obs-plan §3: opentelemetry-proto is the PRODUCT, not self-instrumentation). Self-obs stays tracing-JSON. No drift.

## D-obs-redaction — PASS (no drift)
The emitted PII is the deliberate synthetic PRODUCT fault payload (emitted AT Pulse :4317, feeds the P-035 scrubber) — NOT self-observation. Self-obs logs no payload values (egress spans skip_all, count-only). No host path / internal struct name on the wire or in logs/runs.db/report. The §6/§11 redaction invariant is not engaged (no self-obs logging/artifact write added). No drift.

```yaml
proposals: []
```