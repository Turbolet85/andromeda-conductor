## D-tests-coverage — PASS (no drift)
New symbols (PiiCategory/PiiCorpus/pii_trace_request/pii_logs_request + pub(crate) span_with_attributes) all carry tests: 7 unit + 3 integration; coverage pii.rs 99.43% / message.rs 100% / total 97.46% (≫ 60% Minimal floor); cargo nextest -p conductor-emit 57/57. Determinism seeded (same/≠ seed), loopback stubs (no real network/time). New paths tested at the mandated tier.

## D-tests-framework — PASS (no drift)
Mandated RUNNER is cargo-nextest (§2/§4) — matched. rstest (§4) is an optional fixture layer, NOT a per-test mandate. conductor-emit has no rstest dev-dep and all six existing emit test modules use plain #[test]/#[tokio::test] + variant loops; the chunk matches that established convention. Runner matches spec → no off-spec framework. No drift.

## D-tests-obs-harness — PASS (no drift)
No change to the 5-command harness, status-read, envelope, or JSONL log format. New code is product-stream builders covered by existing emit.batch/emit.logs_batch egress spans (no new must-trace op). §3 ↔ obs-plan §3 untouched. No drift.

```yaml
proposals: []
```