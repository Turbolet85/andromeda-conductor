# arch extract

## Relevance
Partial — traffic-rate ramps are an Epoch-3 emission primitive under conductor-emit, touching determinism contract + OTLP builders, but **no MCP verification** (P-026 is visual/operator-checklist only, routes to ManualCheck).

## Constraints
1. **Crate placement:** Code lives in `conductor-emit` per §Infrastructure Patterns (directory-tree crate-per-seam) and §Occupied Resources (crate names LOCKED).
2. **Determinism discipline:** Must use `ChaCha8Rng` + `seed_from_u64` per §Established Decisions [Determinism RNG] — same scenario+seed ⇒ identical rate-curve shape, platform/version-stable.
3. **No scenario-config in this chunk:** This is a standalone seeded primitive (§Conventions: "no scenario without a P-ID" applies at scenario level, not Epoch-3 primitives); scenario wiring is deferred to Epoch 5.
4. **No new cross-seam dependencies:** Reuses existing `rand_chacha` + OTLP message builders per §Established Decisions [Backend Framework] (no new async runtime); must not take a dependency on `conductor-timeline` (§Cross-cutting: crate-per-seam law, emit ↔ timeline is a later emit-driver's concern).
5. **No new external surface:** Emission journal line schema is a **research question, not a commit**, per scope.md; existing trace/span message builders are reused (rate produces *counts*, not a new wire shape), per §Occupied Resources (routes/interfaces LOCKED).
6. **OTLP wire format:** Spans emitted via existing opentelemetry-proto raw-type builders, aligned to OpenTelemetry Semantic Conventions (§Conventions: "outbound emission").

## Patterns to follow
1. **Seeded primitive module precedent:** Match `pii.rs` (PII latency-shaping) — a standalone `rate.rs` module re-exported from `lib.rs` with a module-doc explaining the determinism invariant and the rate-curve→counts transformation.
2. **Loopback-capture test style:** Per §Infrastructure Patterns (test stack: rstest/proptest/insta), unit tests verify reproducibility (identical seed ⇒ identical counts) and curve-realization (ramp segments move monotonically, breathing oscillates within tolerance) by capturing OTLP spans to a loopback-testonly journal.
3. **ChaCha8Rng seeding pattern:** Per amendment 2026-06-16-seeded-phase-scheduler — initialize via `seed_from_u64(scenario.seed)` for cross-platform stability (referenced in scope.md line 20 but not yet landed in scenario-config, so the test hardcodes a seed).
4. **Reuse existing builders:** Span and trace-message factories (opentelemetry-proto struct construction per §Stack and Technologies and §Conventions: outbound emission) already exist; the new code composes them, does not invent wire shapes.

## Anti-patterns to avoid
1. **Do NOT depend on `conductor-timeline`:** The phase scheduler owns wall-clock driving; emit produces the rate-curve math + counts, timeline consumes it. Cross-crate dependency is forbidden by §Inherited Defaults (module-boundary rules, crate-per-seam law).
2. **Do NOT add a HTTP/REST endpoint or IPC method:** No new external surface per §Occupied Resources (routes/interfaces LOCKED); this is a library primitive callable only by its owning crate and later drivers.
3. **Do NOT journal per-window rates inline:** The journaling schema is deferred per scope.md; the test observes OTLP stream throughput, not a separate rate-record artifact.

## Contract bindings
emit ↔ timeline (later driver): emit produces rate-curve→counts; timeline will consume via a TBD interface (plan-time resolution per scope.md line 35, "emit/timeline division of labour" — a P3/P4 question). No other cross-seam contracts for this chunk.

## Acceptance criteria contributions
1. (arch) Code lives in `conductor-emit` per workspace boundary rules (arch §Inherited Defaults, §Infrastructure Patterns directory-tree).
2. (arch) Seeded rate-profile type uses `ChaCha8Rng` + `seed_from_u64` — identical scenario+seed reproduces identical count sequence across platforms (arch §Established Decisions [Determinism RNG]).
3. (arch) Reuses existing opentelemetry-proto span/trace builders — no new wire format introduced (arch §Occupied Resources routes/interfaces).
4. (arch) No new cross-seam dependency on `conductor-timeline` (arch §Inherited Defaults module-boundary rules, crate-per-seam law).

## Relevant amendment history
- **2026-06-18-exception-events-fingerprint-control** (§Infrastructure Patterns directory-tree + CLAUDE.md §Modules): exception-event fingerprint primitive placed in `conductor-emit`; `conductor-faults` fingerprint-STORM is a later Epoch-7 compose, not Epoch-3. Relevant to this chunk only if rate-ramps also touch fingerprinting (scope.md does NOT mention it — rate shapes *throughput*, not error fingerprints); no blocking precedent, but note the emit-seam's expanding role as Epoch 3 closes.
