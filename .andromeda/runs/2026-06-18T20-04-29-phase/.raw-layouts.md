The amendment history file does not exist (expected for a fresh project). Now let me synthesize the extract based on the focus guide, specialist plan, and chunk scope.

# layouts extract

## Relevance
out-of-scope — chunk 2026-06-18-traffic-rate-ramps is a backend emission primitive (seeded rate-profile builder), not a surface implementation.

## No domain coverage
Traffic-rate ramps (P-026) is a `conductor-emit` backend primitive (deterministic span-count sequencing) with no UI surface, layout structure, or component placement. Layout templates apply only to rendered surfaces (desktop-webview, cli) — this chunk produces the data shape that *downstream* surfaces will consume (e.g., halo breathing in Pulse's constellation view), but owns no layout of its own.
