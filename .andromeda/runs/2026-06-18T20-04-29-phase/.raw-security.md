Now I have all the context. Let me analyze what applies to the traffic-rate ramps chunk.

**Chunk analysis:**
- **Scope:** Traffic-rate ramps (P-026) — a deterministic, seeded rate-profile primitive in `conductor-emit` that generates per-window span counts to realize a target traffic-rate curve
- **Type:** Core emission primitive (Epoch 3)
- **Surfaces touched:** new `conductor-emit` module, determinism contract, reuses existing OTLP builders, possible journal-schema touch
- **No new dependencies expected**

**Security domain coverage check:**

From the focus guide, I need to map the chunk to security-plan sections:
1. **Threat Model Summary** — loopback-client OTLP egress (in-process boundary, seeded data) ✓ relevant
2. **Input Validation** — no external input; rate-curve params are test-internal determinism, not config boundary ✗ out-of-scope
3. **Data Protection** — only emits synthetic telemetry ✓ minimal relevance
4. **Dependency Security** — "no new dependency expected" ✓ minimal check
5. **Error Handling** — seeded primitive, no new error surfaces introduced ✗ out-of-scope
6. **Logging & Monitoring** — journal schema touch is marked "research question (obs-owned schema)" ✓ touches but deferred
7. **Security Anti-Patterns** — subprocess/spawn bans N/A; secret/auth bans N/A; the "clock source" logging ban (Logging § no virtual clock) applies if the primitive emits wall-clock stamps ✓ relevant

**Amendment history check:**
- Amendment 1 (config-validation): garde version — N/A (no config in this chunk)
- Amendment 2 (audit-gate): audit tool floors + toolchain done — relevant as a standing constraint
- Amendment 3 (structured-logging): obs identity env-handles — N/A (no logging here)
- Amendment 4 (npm/frontend): npm audit gate — N/A (no frontend in `conductor-emit`)

**Coverage level:** partial — the chunk is a standalone seeded primitive with minimal security surface (no config, no new deps, no secrets, loopback-only telemetry), but it touches the **determinism discipline** and **logging clock source** anti-patterns if it ever emits wall-clock stamps.

---

# security extract

## Relevance
partial — core determinism contract + loopback-only telemetry emission; minimal config/auth/dependency surface

## Constraints
1. Seeded determinism via `ChaCha8Rng::seed_from_u64` — identical seed ⇒ identical rate-curve output, cross-platform stable (security-plan §Threat Model Summary; loopback-client trust boundary; Epoch-3 determinism discipline).
2. Synthetic data only — the rate-curve primitive emits synthetic OTLP span counts; no real user data, PII, or external input (security-plan §Threat Model Summary § Data classification: test-telemetry).
3. No new dependencies without audit — if any dependency is added counter to the scope's "no new dependency expected," it MUST clear `cargo-audit` ≥0.22 (installed 0.22.1) + optional `cargo-deny` ≥0.19 (installed 0.19.4) before merge (security-plan §Dependency Security; §Bootstrap phases § dep-audit-tooling-install; standing gate per amendment 2026-06-15-dependency-audit-gate).
4. Loopback-only OTLP egress — reuses existing trace/span message builders for `127.0.0.1:4317`; no new network surface introduced (security-plan §Threat Model Summary § Attack surface — OTLP/gRPC egress).
5. No wall-clock timestamp pollution — if the primitive emits journal lines or metadata with timestamps, use `std::time::SystemTime`/`Instant`, never tokio virtual clock (security-plan §Security Anti-Patterns § Logging; Conventions: Determinism discipline).

## Patterns to follow
1. **Seeded per-window determinism** — match the latency-shaping precedent in the same crate: `ChaCha8Rng` instantiation via `seed_from_u64`, rate-curve iterations keyed by scenario seed + window index.
2. **Loopback-capture unit testing** — reuse existing OTLP builders; emit shaped span stream to loopback and assert curve properties (ramp monotonicity, breathing oscillation) via loopback fixture (matches `pii.rs`/latency-shaping test style).
3. **Module-level documentation** — re-export from `lib.rs` with module-doc describing the determinism contract and rate-curve semantics (matches `pii.rs` precedent in Epoch-3 emission primitives).

## Anti-patterns to avoid
1. NEVER use tokio virtual clock for timestamps — if wall-clock emission is journaled, use `std::time::SystemTime`/`Instant` only (security-plan §Anti-Patterns § Logging; clock-source corruption breaks ground-truth SLO math).
2. NEVER skip `cargo-audit` / `cargo-deny` if any dependency is added — the audit gate is the Minimal-tier residual-risk control for the OTLP/gRPC tree (security-plan §Anti-Patterns § Universal).
3. NEVER introduce config/scenario wiring or guard-band validation — this is a standalone seeded primitive; scenario-config integration deferred to Epoch 4 (scope: "No scenario-config / garde wiring").

## Contract bindings
obs ↔ journal schema (if per-window rate/count is journaled, schema ownership lives in obs-plan § PII Scrubbing; clock source matters for SLO math ground truth)

## Acceptance criteria contributions
- (security) No new dependencies, OR if any dependency added: `cargo audit` + `cargo-deny check advisories` pass (Minimal-tier residual-risk gate per §Dependency Security).
- (security) Seeded determinism verified: identical `CONDUCTOR_SEED` + scenario ⇒ identical rate-curve span counts (unit test assertion).
- (security) Loopback-capture test confirms shaped stream conforms to rate-curve spec (ramp monotonicity within tolerance; breathing oscillation verified).
- (security) No wall-clock timestamps in emitted journal — if timestamps appear, use `std::time::SystemTime` only (ground-truth artifact integrity per §Anti-Patterns § Logging).

## Relevant amendment history
- **2026-06-15-dependency-audit-gate:** cargo-audit ≥0.22.1 (installed) + cargo-deny ≥0.19.4 (installed) are minimum floors; toolchain ≥1.94.1 confirmed done (1.95.0). Applies as a standing CI/pre-merge gate for any new dependencies this chunk introduces (currently expected: none). (security-plan §Dependency Security § Audit tool + Pinning; standing gate, not chunk-specific.)
