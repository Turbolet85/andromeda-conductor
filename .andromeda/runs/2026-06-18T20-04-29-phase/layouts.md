# layouts extract

## No domain coverage
out-of-scope — Traffic-rate ramps (P-026) is a `conductor-emit` backend primitive (deterministic span-count sequencing) with no UI surface, layout structure, or component placement. Layout templates apply only to rendered surfaces (desktop-webview, cli); this chunk produces the data shape that *downstream* surfaces will consume (halo breathing in Pulse's constellation view), but owns no layout of its own.
