D-arch-resources: new module `rate` + public symbols (RateCurve, RateCurve::ramp/breathing/windows/window_counts, rate_trace_request) inside the already-registered conductor-emit crate. No new IPC/endpoint/event/socket/port/env var/workspace crate → library-internal APIs, not §Occupied Resources entries. No drift.
D-arch-decisions: no deps added/bumped; Cargo.toml unchanged. rand_chacha 0.9 ChaCha8Rng + seed_from_u64 aligns exactly with §Stack + §Established Decisions [Determinism RNG]. Two-RNG seeded determinism + bounded jitter conform to the seed-materially-shapes-output decision. No drift.

proposals: []
