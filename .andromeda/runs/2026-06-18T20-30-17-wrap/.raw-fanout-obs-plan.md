D-obs-instrumentation: new symbols are pure functions; rate_trace_request is a PRODUCT-stream trace builder (synthetic fault telemetry for Pulse), NOT a self-observing operation — no new must-trace critical path, so no new span/log required. No new run_id-bearing log line; no virtual-clock journal stamp (no journaling). No drift.
D-obs-stack: deps unchanged; uses only opentelemetry-proto PRODUCT types (obs-plan §3 already documents the dormant transitive opentelemetry_sdk as non-violating — never initialized). No OTel SDK/exporter for self-obs added. No drift.
D-obs-redaction: no logging, no runs.db, no artifact writes; PRODUCT spans use synthetic names + random ids (no user-input/host-path/struct-name leak). No drift.

proposals: []
