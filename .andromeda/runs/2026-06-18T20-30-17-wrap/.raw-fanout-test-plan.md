D-tests-coverage: new paths (RateCurve, rate_trace_request) carry 7 unit + 1 integration test at the Minimal tier; rate.rs 99.34% lines; determinism seeded (same-seed/diff-seed), loopback stub (no real network/time). No drift.
D-tests-framework: runner = cargo-nextest (+ clippy + doctest + llvm-cov) per §2/§4. "plain #[test]" is standard Rust syntax compatible with nextest (rstest is an opt-in fixture lib, not the mandated runner); matches latency/pii/topology precedent. No off-spec framework. No drift.
D-tests-obs-harness: report states self-observation unchanged — no harness/status-read/log-format change. §3 ↔ obs-plan §3 still agree. No drift.

proposals: []
