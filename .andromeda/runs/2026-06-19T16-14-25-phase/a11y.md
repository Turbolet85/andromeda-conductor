# a11y extract

## Relevance
out-of-scope — chunk is headless infrastructure (sacrificial port occupier, no UI surface)

## No domain coverage
Chunk builds a headless `conductor-faults` socket occupier with no rendered DOM, no interactive controls, no keyboard/screen-reader/color-contrast surface. A11y plan §1 explicitly excludes cli/headless surfaces ("not-assertable — no DOM/native widget tree; a11y tools have no applicability"). This chunk touches no UI.
