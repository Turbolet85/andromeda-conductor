## arch drift-detector — raw return

Verdict: D-arch-decisions → no drift (thiserror in §Stack; std::net allowed; no new lib/runtime).
D-arch-resources → PROPOSAL (warning).

proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources — Crate names (workspace members)"
    change: "`conductor-faults` (fault helpers — `PortOccupier` + `FaultError` for loopback `:4317` bind lifecycle; also exports `OTLP_INGEST_PORT` const)"
    sidecar: "Added public API surface for conductor-faults: PortOccupier struct (occupy/occupy_default/local_addr/release/Drop), FaultError enum (Bind variant), OTLP_INGEST_PORT constant."
    rationale: "Report documents new public symbols (PortOccupier, FaultError, OTLP_INGEST_PORT). The arch crate-names list names conductor-faults but does not document its public API surface. Report confirms the port + bind mode are already arch-documented, but the struct/method APIs are unregistered per the detector invariant."

Orchestrator note: the agent itself observed "arch lists crate names and ports/env vars but does not enumerate public struct/method APIs per-crate — it only names the crates." The chunk's actual RESOURCES (the :4317 socket + the conductor-faults crate) are BOTH already registered.
