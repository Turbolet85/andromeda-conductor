# a11y extract

## No domain coverage

This chunk builds a backend fault-injection helper (`conductor-faults` crate) with no rendered UI surface, no DOM, and no interactive controls — a11y has no applicability per a11y-plan §1 (only assertable surface is desktop-webview; cli/backend surfaces are not-assertable and excluded from Sections 2+).
