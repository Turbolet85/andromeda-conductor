# a11y extract

## No domain coverage
This chunk builds a Rust fault-injection helper (`conductor-faults`) with no UI surface, DOM, or rendered components — a11y guidance applies only to webview/desktop-GUI surfaces, which this scope explicitly excludes.