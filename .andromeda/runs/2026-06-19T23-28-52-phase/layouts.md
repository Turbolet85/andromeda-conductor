# layouts extract

## No domain coverage
P-014 abrupt-silence fault is a fault-helper primitive (cargo crate `conductor-faults`); the chunk ships a Rust type with determinism + error-wall contracts, no UI, no surface rendering, no component placement, no wireframe elements. Layout templates define per-surface wireframes for desktop-webview + cli render models; fault helpers are backend domain (not a surface). No layout constraint applies. (Distiller also emitted a one-line `## Relevance: out-of-scope` — normalized here to the canonical `No domain coverage` form.)