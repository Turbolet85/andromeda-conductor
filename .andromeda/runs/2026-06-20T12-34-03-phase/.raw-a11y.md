# a11y extract

## Relevance
out-of-scope for this chunk

## No domain coverage
The bursty-train pattern (P-013) is a headless Rust fault-helper primitive in `conductor-faults` with no rendered UI surface, keyboard interface, focus management, ARIA roles, or visual elements. Per a11y-plan §1, a11y assertability requires a DOM/native widget tree (desktop-webview Tauri surface only); the headless `conductor-faults` crate produces no accessible surface.