Based on the files read, I can now determine the design domain coverage for this chunk:

# design extract

## Relevance
Out-of-scope for this chunk — P-013 is a backend fault-helper primitive in `conductor-faults` (Rust, no UI surfaces).

## No domain coverage
This chunk is a purely backend descriptor primitive (BurstyTrain type in `conductor-faults`, Epoch 4, Fault helpers). Design tokens, typography, motion, color, iconography, spacing, and component patterns all apply exclusively to UI surfaces (desktop-webview: React/Tailwind, and cli: ANSI terminal). The bursty-train duty-cycle helper owns no user-facing rendering, interaction model, or visual state — it is consumed later in Epoch 7 (timeline wiring / scenario integration / OTLP emission) where UI and obs surfaces will inherit design constraints. The chunk's boundaries explicitly exclude surface wiring ("Timeline/scheduler wiring — driving the duty cycle as phases under `conductor-timeline`"; "Actual OTLP emission during active windows") and any fault-observation span (obs spans routed to Epoch 7/8). Design domain applicability is deferred to the surface chunks that *render* activity-floor verdicts (run-report coverage matrix, operator-pause, heartbeat state) and emit observability artifacts.