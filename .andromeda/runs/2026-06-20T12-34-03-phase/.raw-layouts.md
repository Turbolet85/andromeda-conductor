The amendment history file does not exist, which is normal. Now let me analyze the relevance of this chunk to the layouts domain.

This chunk (2026-06-20-bursty-train-pattern) is for **P-013 (activity-floor)**, a fault-helper primitive in `conductor-faults`. It's purely a **Rust library descriptor** with:
- New module `crates/conductor-faults/src/train.rs`
- Type `BurstyTrain` + constants
- `error.rs` / `FaultError` extension if needed
- No UI/layout surface rendering

The scope explicitly defers:
- Timeline/scheduler wiring (Epoch 7)
- Scenario declarations (Epoch 7)
- Obs spans (Epoch 7/8)
- Actual OTLP emission

This is a **backend/engine** chunk with **zero layout surface impact**.

# layouts extract

## No domain coverage
This chunk (bursty-train-pattern) is a backend fault-helper primitive in conductor-faults with no UI/layout surface rendering — layouts domain does not apply.