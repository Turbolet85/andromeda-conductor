# a11y extract

## No domain coverage
The bursty-train pattern (P-013) is a headless Rust fault-helper primitive in `conductor-faults` with no rendered UI surface, keyboard interface, focus management, ARIA roles, or visual elements (per a11y-plan §1, a11y assertability requires a DOM/native widget tree — desktop-webview Tauri surface only).