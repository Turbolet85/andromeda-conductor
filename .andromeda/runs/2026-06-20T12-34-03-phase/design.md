# design extract

## No domain coverage
P-013 bursty-train is a backend descriptor primitive (`BurstyTrain` in `conductor-faults`, Epoch 4) with no user-facing rendering, interaction model, or visual state — design tokens / typography / motion / color / components apply only to UI surfaces (Tauri webview + cli), which are deferred to later epochs.