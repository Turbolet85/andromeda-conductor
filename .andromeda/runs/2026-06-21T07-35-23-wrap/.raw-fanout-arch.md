D-arch-resources: BurstyTrain + consts + FaultError variants are library API symbols within the
already-registered conductor-faults crate; no new port/socket/env var/IPC/crate. arch §Occupied Resources
tracks occupied resources, not per-crate public API. No drift.
D-arch-decisions: uses only std::time::Duration + existing thiserror (no new dep/runtime); follows the
established Result/FaultError fault-helper validated-constructor pattern (EmissionGap/PortOccupier); garde
deferred to Epoch 7 per established posture. No drift.

proposals: []