D-obs-instrumentation: BurstyTrain is a helper primitive, not a must-trace operation; the fault.* span is
route-sequenced to Epoch 7/8 timeline wiring (report Coverage: instrumentation n/a — deferred). Per the
2026-06-20 playbook rule (fault-helper primitive deferring its fault.* span ⇒ routine non-drift). No drift.
D-obs-stack: only std::time + thiserror; no OTel SDK / OTLP exporter / tracing-opentelemetry for self-obs. No drift.
D-obs-redaction: no logging / artifact write / runs.db write added; no host paths or struct names emitted
(PII n/a). No drift.

proposals: []