D-tests-coverage: new train.rs paths carry unit tests (8 train + 3 error Display) + doctest; 98.22% lines;
determinism asserted both-directions (no real net/time). No drift.
D-tests-framework: cargo-nextest + cargo test --doc + clippy — matches test-plan §2/§4. No drift.
D-tests-obs-harness: no harness / status-read / JSONL envelope change (library-only). §3 ↔ obs §3 untouched. No drift.

proposals: []