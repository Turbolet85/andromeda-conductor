# a11y extract

## No domain coverage
out-of-scope — This chunk (MCP read-back client) is a headless subprocess-transport layer with no rendered UI surface, DOM, or interactive controls, so it has no WCAG applicability per a11y-plan §1 (not-assertable surface — cli/headless pattern). Accessibility rules apply only to the desktop-webview control panel. The client's typed error wall (`VerifyError`) and protocol binding do not trigger WCAG assertions.
