# layouts extract

## No domain coverage
out-of-scope — MCP read-back client is an infrastructure/transport layer with no UI rendering, layout, or component-placement concerns; layout templates cover desktop-webview, cli, and their cross-surface patterns. This chunk is pure backend (rmcp client spawn, version negotiation, typed tool-call surface, error mapping) with no surface contact until later verification chunks wire it into the verdict flow.
