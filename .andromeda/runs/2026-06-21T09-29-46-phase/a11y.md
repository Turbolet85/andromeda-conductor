# a11y extract

## No domain coverage

This chunk (preflight readiness gate — Epoch 5.2, MCP contract manifest + `ReadyState` value type + test stub) has no a11y-applicable surface. Per a11y-plan §1, the preflight gate is backend/IPC infrastructure (transport / contract manifest / test harness); it has no DOM, native widget tree, or rendered user interface. The four must-be-accessible paths (scenario pick/start, operator-pause, run-report view, operator-checklist) are webview-only. Preflight assertions emit structured JSON (`ReadyState`) that becomes part of the obs envelope (per the binding D-a11y-obs-schema in amendments), but the validation logic itself is not user-facing. CLI headless output is explicitly out-of-scope per a11y-plan §1 ("not-assertable surface, no DOM / native widget tree").
