# layouts extract

## No domain coverage

This chunk implements pure backend conductor-verify readiness gate logic (MCP initialize assertions, contract manifest loading, `ReadyState` value type, canary round-trip) with no rendered UI surface; layout templates scope ends at rendered output (desktop-webview + cli output structure), not backend verification code.
