# a11y extract

## No domain coverage

OTLP egress liveness check is a backend connectivity primitive in conductor-emit with no rendered UI surface or user-interactive controls — it falls entirely outside the a11y domain scope (desktop-webview Tauri control panel + four must-be-accessible paths). A11y focuses only on assertable UI surfaces per §1; backend transport primitives are infrastructure-only.
