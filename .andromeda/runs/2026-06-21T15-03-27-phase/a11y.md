# a11y extract

## Relevance
Partial — chunk is verdict-classification logic (non-UI domain), not rendering; a11y applies narrowly to verdict-JSON schema alignment and the harness contract, not the classification algorithm itself.

## Constraints
- a11y-plan.md §3 Structured violation JSON schema: verdicts fold into obs Section 6 envelope via `fingerprints[]` (rule-id + WCAG-SC tag + selector), never leak host paths or internal struct names
- a11y-plan.md §3 CI integration: violation JSON artifacts tag `service.name` / `deployment.environment` per obs Section 3 Service Identity, enabling cross-correlation with observability logs
- a11y-plan.md §3 Assertion Harness Contract: every WCAG criterion MUST have at least one agent-runnable machine-verifiable path (axe-core JSON / Lighthouse / colorjs.io / WebdriverIO JSON)
- a11y-plan.md §1 A11y scope: `Verdict` type is already defined in `conductor-core` (Pass / Fail / CalibrationRegion); this chunk consumes it, no new type invention
- a11y-plan.md §2 Strategy: no manual screen-reader / keyboard / contrast as ONLY method — machine-verifiable JSON is the baseline, SR is supplemental
- a11y-plan.md §3 WCAG criteria mapping: Minimal tier baseline SCs (SC 2.1.1, SC 1.4.3, SC 2.4.3) + design-token-bound SCs (SC 1.4.11, SC 1.4.1, SC 2.3.3) must all have assertion paths; calibration-region verdicts never block hard-fail on model-interpretive claims

## Patterns to follow
- a11y-plan.md §3 Structured violation JSON schema: violations map to obs envelope with `state` field (Pass | Fail | ManualCheck | KnownResidual | Blocked); axe/colorjs.io/WebdriverIO PASS/FAIL fold into this same envelope per fingerprints pattern
- a11y-plan.md §1 Critical Paths: verdict-classification must preserve the observable signal for all four critical paths (scenario/start, operator-pause, run-report, operator-checklist) without swallowing a11y-relevant state transitions
- a11y-plan.md §3 Bootstrap: violation-json-emission-wire phase requires classification to route model-interpretive assertions to `CalibrationRegion` (report-for-human), never hard-fail on exact-value mismatch

## Anti-patterns to avoid
- a11y-plan.md §11 (Anti-patterns — not fully visible in excerpt, but referenced in §4): do NOT invent new ARIA roles or verdict types; reuse existing shadcn/Radix patterns + `conductor-core::Verdict` enum
- Do NOT leak absolute host paths or seam-crate struct names into violation artifacts; selectors must be DOM-relative or `data-testid` only
- Do NOT emit verdict JSON without resource attributes (`service.name` / `deployment.environment`), breaking obs cross-correlation

## Contract bindings
- **obs ↔ a11y schema bind (D-a11y-obs-schema):** verdict-classification's Result::Ok(Verdict) output must align to obs Section 6 Run-report envelope structure; violations tag `read_back_observed_at` (added 2026-06-16, per a11y-plan-amendments.md)
- **tests harness ↔ a11y assertion:** verdict classification feeds the obs journal that tests' 5-command harness (boot / run / status / cleanup / logs) will read back; `logs` command surfaces the a11y JSON

## Acceptance criteria contributions
- (a11y) Verdict classification for hard-path assertions (Pass/Fail) aligns to obs Section 6 envelope `state` field and does not corrupt selector targets.
- (a11y) Calibration-region verdicts never emit hard-fail on model-interpretive claim mismatches; they route to `CalibrationRegion` with delta-signal in fingerprints for human review.
- (a11y) Violation artifacts include `service.name` / `deployment.environment` tagging for observability cross-correlation (per a11y-plan.md §3 CI integration).

## Relevant amendment history
- **2026-06-16-emission-journal-writer:** `read_back_observed_at` field added to obs Run-report envelope; a11y-plan §3 Structured violation JSON schema reproduction updated to match (11 fields total). This chunk's verdict JSON output must include this field in the reproduced envelope shape so a11y violations align with the obs contract.
