proposals: []

# D-a11y-surface: no interactive UI element added (no UI).
# D-a11y-obs-schema: obs §6 envelope (RunRecord) untouched; Assessment is an internal intermediate form, not the violation/obs schema.
