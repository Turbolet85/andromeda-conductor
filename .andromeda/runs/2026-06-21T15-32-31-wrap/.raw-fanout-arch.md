proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Standard Contracts"
    change: "Add a Classification types subsection registering ClaimClass {Hard, CalibrationRegion} + Assessment {verdict, observed, expected, delta} + classify(class, matched, observed, expected) -> Assessment as conductor-verify's verdict-policy contract."
    sidecar: "Registered conductor-verify's ClaimClass enum, Assessment struct, and classify() function as Standard Contract types for the verdict policy split."
    rationale: "The chunk introduces three new public APIs from conductor-verify implementing the [Probabilistic-Assertion Policy] decision; the concrete type shapes are not registered in Standard Contracts / Occupied Resources."

# D-arch-decisions: no drift (deps are pre-existing conductor-core + serde; classify infallible aligns with outcomes-are-values; tracing un-instrumentation per plan).
