proposals: []

# D-obs-instrumentation: classify is pure logic, not itself a must-trace op; the must-trace op is the read-back verification flow (verify.readback* spans), per obs §4. Un-instrumented per plan + egress-probe precedent.
# D-obs-stack: no telemetry dep; tracing-only; no OTel SDK.
# D-obs-redaction: observed/expected pass through conductor_core::redact_value at capture — actively compliant with §6/§11.
