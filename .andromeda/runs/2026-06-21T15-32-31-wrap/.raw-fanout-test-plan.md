proposals: []

# D-tests-coverage: 11 new tests (unit+integ) at the mandated tier; deterministic (no real network/time).
# D-tests-framework: cargo-nextest + rstest — on-spec (§2/§4).
# D-tests-obs-harness: RunRecord envelope untouched; Assessment is an internal type, not the envelope — §3 ↔ obs §3 unchanged.
