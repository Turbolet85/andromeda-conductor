# a11y extract

## Relevance
Partial — chunk touches run-report envelope schema + a11y violation JSON binding, but does NOT add UI surface or interactive controls requiring WCAG assertions.

## Constraints
- Per a11y-plan §3 Structured violation JSON schema: a11y violation artifacts fold axe rule-id + WCAG-SC tag + selector into `fingerprints[]`, inheriting the obs-contracted envelope shape (a11y-plan §3 Structured violation JSON schema).
- Per a11y-plan §3 CI integration: structured violation JSON emitted through tests' `logs` command aligns to obs Section 6 envelope schema (no absolute host paths or internal struct names in node targets); a11y-plan §1 Service Identity specifies `service.name` + `deployment.environment` resource attributes tagging each artifact (a11y-plan §3 Structured violation JSON schema).
- Per a11y-plan §3 Bootstrap phases: contrast-verification-harness + violation-json-emission-wire (colorjs.io token-name assertions + axe rule artifacts) already planned for Foundation-epoch scaffolding; this chunk inherits them as preconditions (a11y-plan §3.5 Bootstrap phases).
- Per a11y-plan §1 A11y Scope Summary: expected-outcome model is NOT UI surface, so no ARIA roles / focus order / keyboard nav / screen-reader contracts apply here. The scope's four must-be-accessible paths (scenario-pick, operator-pause, run-report, operator-checklist) are already assertable; this chunk evaluates them post-read-back, not renders them.
- Per a11y-plan §3 Structured violation JSON schema: `read_back_observed_at` field (added June 2026 amendment per a11y-plan-amendments.md) must remain in all violation artifact reproductions of the obs envelope (a11y-plan-amendments.md 2026-06-16-emission-journal-writer).

## Patterns to follow
- Store violation artifacts using the same obs §6 envelope shape (per a11y-plan §3 Structured violation JSON schema reproduced verbatim); no a11y-specific envelope innovation.
- Redact node targets as DOM-relative selectors / `data-testid`, never filesystem paths (per a11y-plan §3 Structured violation JSON schema "MUST NOT leak absolute host paths").
- Tag artifacts with `service.name` + `deployment.environment` resource attributes per obs Section 3 Service Identity binding (a11y-plan §1 A11y Scope Summary).

## Anti-patterns to avoid
- Do NOT emit a11y violation artifacts with new top-level fields outside the obs §6 envelope; fold all a11y metadata into `fingerprints[]` (per a11y-plan §3 Structured violation JSON schema "no new top-level fields").
- Do NOT invent new WCAG SCs beyond Minimal tier coverage (SC 2.1.1, SC 1.4.3, SC 2.4.3 baseline + token-driven SC 1.4.11, SC 1.4.1, SC 2.3.3, plus pattern-inherent SCs) for evaluation logic; this chunk does not add UI (a11y-plan §2 Agent-runnable invariants).

## Contract bindings
- **a11y violation JSON ↔ obs §6 envelope:** structured violation JSON from axe-core + colorjs.io (Foundation-epoch tools, a11y-plan §3.5) must inherit the obs run-report envelope shape including `read_back_observed_at` field (amended per a11y-plan-amendments.md 2026-06-16).
- **Contrast assertion → design-token redaction:** colorjs.io token-name pairs resolved from compiled CSS custom properties; violation artifacts reference token NAMES only, no raw RGB/hex values (per a11y-plan §3 Contrast verification harness "raw values stay in design-system.md").

## Acceptance criteria contributions
- (a11y) Structured violation JSON emitted from a11y evaluation inherits the obs §6 envelope schema (including `read_back_observed_at` field per amendment 2026-06-16).
- (a11y) Node targets in violation artifacts stored as DOM-relative selectors / `data-testid`, never absolute host paths (per a11y-plan §3 Structured violation JSON schema redaction edge).
- (a11y) Artifacts tagged with `service.name` + `deployment.environment` resource attributes (obs Section 3 Service Identity binding).

## Relevant amendment history
- **2026-06-16-emission-journal-writer** — a11y-plan §3 Structured violation JSON schema: added `read_back_observed_at` field to both reproduced obs Run-report envelopes (violates fold into `fingerprints[]` inherit envelope shape). This chunk's expected-outcome + SLO timing model computes `read_back_observed_at` (Journal-relative latency, per chunk scope), so violation artifacts emitted here MUST include that field in the reproduction. (First amendment to a11y-plan.)

## Phase note (orchestrator)
a11y coverage here is FORWARD-LOOKING and out-of-implementation for THIS chunk — it governs Epoch 9/10 a11y-violation-JSON artifacts, which reuse the obs run-report envelope. No UI/WCAG work in this chunk. Only binding bearing here: keep the envelope shape (incl. `read_back_observed_at`, computed by this chunk's SLO math) stable.
