Now I have all the information needed. Let me create the a11y extract for this chunk.

# a11y extract

## Relevance
partial — the chunk realizes the run-report envelope shape (schema + serialization) that feeds downstream a11y violation emission via obs Section 6 envelope binding; does not itself implement accessibility assertions or interact with UI surfaces.

## Constraints
1. **Violation JSON envelope alignment** — per a11y-plan §3 Structured violation JSON schema + §11 Universal anti-pattern; the envelope MUST reproduce the obs Section 6 Log Format schema (11 fields: `journal_emitted_at`, `read_back_observed_at`, `run_id`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `latency_ms`, `slo_tier`, `fingerprints`), and a11y violations fold into `fingerprints[]` (axe rule-id + WCAG-SC tag + selector tuple) without introducing new top-level fields — per D-a11y-obs-schema binding (a11y-plan-amendments §2026-06-16 entry).
2. **Blocked-row null rule** — per a11y-plan §1 Scope; a `Blocked` envelope populates only identity fields (`run_id`, `seed`, `scenario`, `p_ids`, `slo_tier`); `verdict`, `journal_emitted_at`, `read_back_observed_at`, `latency_ms`, `fingerprints` serialize as JSON `null` — never phantom/default values.
3. **Timestamp encoding consistency** — per a11y-plan §1; in-payload instants use RFC-3339 colon-delimited format for human/JSON serialization; the `run_id` is constrained to hyphen form (runs.db PK + artifact stem); downstream runs.db columns map to integer-millisecond journal offsets (ch2 binding).
4. **Artifact hygiene (no path leak)** — per a11y-plan §11 SLO anti-pattern; no absolute host paths or internal conductor struct/field names leak into the serialized form (canonical-name serde required per tests' Status JSON constraint).
5. **Verdict/error wall (infallible outcome serialization)** — per a11y-plan §1 Verdict/error wall; the envelope holds outcome VALUES; producing/serializing an envelope is infallible w.r.t. SUT outcomes — `Result::Err` stays reserved for harness faults only.
6. **State-naming crosswalk** — per a11y-plan §6 State color tokens; the six lamps map display labels (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) to obs enum values (`Pass`, `CalibrationRegion`, `Fail`, `Blocked`, `ManualCheck`, `KnownResidual`) — this mapping MUST be preserved in the envelope for downstream obs join (a11y assertion records must join back to their journal rows).

## Patterns to follow
1. **Reuse conductor-core verdict/state enums** — per scope §Open questions; `Verdict` / `ReportState` already exist upstream (conductor-core / conductor-verify); this chunk only consumes & serializes outcomes, never redefines them.
2. **JSONL alignment** — envelope shape matches the per-run emission journal line structure (obs Section 6); serde serialization must respect the 11-field contract for both JSON human-readability and downstream JSONL streaming.
3. **Resource attribute tagging** — per a11y-plan §3 CI integration; artifacts tag `service.name` = `"conductor-tauri"` / `"conductor-ui"`, `deployment.environment` = `CONDUCTOR_ENV` (fallback `"local"`) for obs cross-correlation; this metadata rides alongside the envelope in the emission stream.

## Anti-patterns to avoid
1. NEVER invent fields or deviate from the 11-field obs contract (a11y-plan §11 Universal) — every additional a11y metadata (axe rule-id, WCAG-SC tag, selector) folds into `fingerprints[]`, not a new top-level field.
2. NEVER use default/phantom values for `Blocked` rows — all omitted fields (verdict, journal_emitted_at, read_back_observed_at, latency_ms, fingerprints) MUST be explicit `null` in JSON.
3. NEVER leak filesystem paths or conductor internal struct names into the serialized envelope — serde canonical naming required per tests' Status JSON constraint (a11y-plan §11 SLO).

## Contract bindings
- **obs Section 6 Log Format envelope** ↔ a11y violation JSON schema (binding D-a11y-obs-schema; a11y-plan §3 + amendments); envelope schema must be 11-field obs contract, not a11y-specific extension.
- **conductor-core verdict/state enums** ↔ obs enum types; the state-naming crosswalk (a11y-plan §6 table) MUST be preserved for downstream join logic so violation records link back to their journal rows.
- **runs.db downstream schema** (ch2) ↔ this envelope's timestamp representation; RFC-3339 in JSON, integer-millisecond in runs.db mapping (ch2 consumes this envelope).

## Acceptance criteria contributions
1. (a11y) Envelope reproduces obs Section 6 11-field schema without additions; serde serialization round-trips the contract.
2. (a11y) Blocked-row envelope: identity fields present (`run_id`, `seed`, `scenario`, `p_ids`, `slo_tier`); verdict, journal_emitted_at, read_back_observed_at, latency_ms, fingerprints are explicit `null` in JSON (never default values).
3. (a11y) No filesystem paths or conductor internal struct names in serialized form; serde canonical naming applied.
4. (a11y) State-naming crosswalk: each of the six display labels (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) maps to the correct obs enum (`Pass`, `CalibrationRegion`, `Fail`, `Blocked`, `ManualCheck`, `KnownResidual`) in the verdict/state fields.

## Relevant amendment history
**2026-06-16 — violation-schema reproduction gains `read_back_observed_at` (obs bind)** — Section §3 Structured violation JSON schema; added `read_back_observed_at` field after `journal_emitted_at` in the obs envelope reproduction to track the now-11-field contract (obs gained this field same day; a11y reproduces that envelope verbatim so violations fold into `fingerprints[]` under the updated obs contract). No a11y behavior change; a11y-plan §3 envelope reproduction now includes all 11 fields: `journal_emitted_at`, `read_back_observed_at`, `run_id`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `latency_ms`, `slo_tier`, `fingerprints`.
