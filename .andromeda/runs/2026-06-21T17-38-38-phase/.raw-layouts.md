The amendment history file does not exist (which is expected on a fresh project). Now I'll synthesize the extract based on the focus guide, the layout-templates plan, and the chunk scope.

# layouts extract

## Relevance
partial — the chunk builds a typed serialization contract; layout coverage only where run-report data flows to the UI surfaces.

## Constraints
1. **Envelope serialization is surface-agnostic** (per layout-templates §Primary Surfaces): the run-report envelope carries the canonical shape shared by Markdown, runs.db, and JSONL; the desktop-webview and cli surfaces independently consume and render this contract. The envelope must not prescribe surface representation — colors, glyphs, or roles are assigned at render time, not in the struct.
2. **Verdict/state type mapping is fixed** (per layout-templates §Primary content block 2): the enum values `{Pass, Fail, CalibrationRegion}` for verdict and `{Pass, Fail, ManualCheck, KnownResidual, Blocked}` for state must remain stable, because both surfaces depend on these exact names for their status-color mappings (desktop CSS tokens + cli ANSI codes, per layout-templates IA notes).
3. **Blocked-row null rule applies at serialization** (per layout-templates §Primary content block 2 Blocked rows): a `Blocked` envelope must serialize verdict, latency_ms, journal/read-back instants, and fingerprints as JSON `null`, never phantom or zero values — this is the "never a red error" layout invariant made concrete.
4. **P-ID array matches the surface domain** (per layout-templates §Primary content block 1): the `p_ids` field in the envelope must align with the 60-row coverage matrix (P-001..P-060) that both surfaces render; serialization must not assume a flattened single P-ID per envelope (the envelope is per-scenario-check, which may span multiple P-IDs in drive-observe coverage).
5. **Timestamp RFC-3339 colon format is canonical for human/JSON** (per layout-templates Decisions Log): the envelope's in-payload instants use colon-delimited RFC-3339 (e.g., `2026-06-14T13:02:30.123Z`), which is the human-readable form the Markdown report and cli output both consume; the runs.db integer-millisecond mapping is a downstream storage concern.

## Patterns to follow
1. **Null-safe verdict/state fields**: follow the layout-templates §Primary content block 2 convention — every verdict and state enum value is represented as a distinct serializable variant, never collapsed or downgraded (e.g., `CalibrationRegion` stays distinct from `Fail`, `KnownResidual` is never silently `Pass`). Serialize missing outcomes as JSON `null` only when the Blocked-row rule explicitly permits it.
2. **No artifact path leakage**: the envelope's `run_id` field carries only the canonical hyphen-delimited timestamp form (the artifact stem), never absolute host paths or internal struct names in the serialized JSON or Markdown output (per layout-templates Artifact hygiene).
3. **Array-safe P-ID field**: the `p_ids` array must accommodate multiple P-IDs (coverage matrix logic may associate one check result with multiple P-ID rows in drive-observe scenarios); serialization must not flatten or assume cardinality = 1.

## Anti-patterns to avoid
1. **No implicit color/glyph mapping in the struct**: the envelope holds verdict/state type VALUES only; never embed color names, ANSI codes, glyphs (✓/✗/⊙/~), or role names (Label/Data/Body) in the serialized envelope — these are surface-specific render rules applied by the desktop-webview and cli renderers at display time.
2. **Never downgrade verdict/state to Fail on Blocked or CalibrationRegion**: the envelope serialization must preserve the distinction between the machine-verdict triad (`Pass`/`Fail`/`CalibrationRegion`) and the three non-verdict ReportStates (`ManualCheck`/`KnownResidual`/`Blocked`); downstream surfaces depend on this discretion to implement the layout-templates rule "never silently a Fail."
3. **No phantom values in Blocked rows**: when the Blocked-row null rule applies, serialize as JSON `null` (or runs.db NULL), never as zero, empty string, or a default value — the absence of a measurement is a layout-meaningful signal.

## Contract bindings
**run-report envelope ↔ desktop-webview & cli surfaces**: the envelope's verdict/state enum values map 1:1 to the status colors and glyphs (layout-templates §Primary content block 2 desktop / §Primary content block 2 cli); the P-ID array and run_id enable both surfaces to index into the 60-row coverage matrix and locate the run report artifact. The envelope is the single source of truth that both renderers consume — verdict changes are announced (a11y derives live-region semantics per layout-templates a11y notes), and the Blocked-row null convention ensures Blocked rows render with `—` / null in measurement columns (never red) as specified in the layout wireframes.

## Acceptance criteria contributions
1. "(layouts) Run-report envelope verdict/state enums map to desktop-webview status colors and cli ANSI codes per layout-templates IA mapping (count-nominal/count-hold/status-fail/count-blocked/status-residual/status-manual)."
2. "(layouts) Blocked-row envelope serializes verdict/latency_ms/timestamps/fingerprints as JSON `null`; desktop-webview and cli both render these fields as `—` / empty in measurement columns (never red) per layout-templates wireframes."
3. "(layouts) P-ID array in envelope accommodates multiple P-IDs per check (drive-observe coverage matrix); both surfaces consume and render the array without flattening or assuming cardinality."

## Relevant amendment history
(none)
