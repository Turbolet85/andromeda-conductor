Now I'll extract the security domain information relevant to this chunk, applying the focus guide.

# security extract

## Relevance
partial — this chunk is the serialization contract shape for run-report data (non-path artifact fields only); it does not handle input validation, dependency security, or secret material directly.

## Constraints
1. All serialized envelope fields MUST NOT leak absolute host paths or internal struct/field names — canonical serde naming required per security-plan §Error Handling and §Data Protection (artifact hygiene).
2. Blocked-state envelopes MUST serialize `verdict`, `journal_emitted_at`, `read_back_observed_at`, `latency_ms`, and `fingerprints` as JSON `null` (never phantom values), per scope's Blocked-row null rule and security-plan §Error Handling (sanitized externals).
3. Timestamp encodings MUST use colon-delimited RFC-3339 for human/JSON payload serialization per security-plan §Data Protection (no sensitive-data storage applies; this is structural hygiene).
4. All envelope struct fields carrying outcome classification MUST reuse the already-validated `Verdict` and `ReportState` types from conductor-core — no new verdict logic introduced per scope boundaries (security-plan §Error Handling: outcomes are typed VALUES, not panics).

## Patterns to follow
1. Module-internal `thiserror` typed enum for serialization errors (if any arise) that collapse to `anyhow` only at seam boundaries — mirrors conductor-verify pattern per security-plan §Error Handling.
2. Serde derive with explicit `#[serde(rename)]` / canonical names to prevent struct-field leakage — security-plan §Error Handling (artifact hygiene).
3. Treat envelope production as infallible with respect to SUT outcomes per scope ("producing/serializing an envelope is infallible"); `Result::Err` reserved for harness faults only per security-plan §Error Handling (verdict/error wall).

## Anti-patterns to avoid
1. NEVER serialize absolute host paths (canonicalized `CONDUCTOR_*` directories, `ANDROMEDA_PULSE_DATA_DIR`) into the envelope payload — security-plan §Anti-Patterns § Logging, §Error Handling.
2. NEVER populate `Blocked` envelopes with phantom values in nullable fields — security-plan §Anti-Patterns § Logging and scope's Blocked-row null rule must be enforced in serde.

## Contract bindings
- obs §PII Scrubbing (none expected in self-generated synthetic telemetry, but payload must not leak internal paths that obs redaction gates would catch retroactively)
- conductor-core (reuses `Verdict` / `ReportState` types; envelope is the type-level binding between verdict/error wall and downstream storage/rendering seams)
- Epoch 6 chunk 2 (runs.db schema maps envelope fields to columns; must preserve field semantics for NULL/type mapping)

## Acceptance criteria contributions
1. (security) Grep verify: no serialized envelope field names match internal struct/crate names (only canonical serde renames appear).
2. (security) Serialization round-trip: `Blocked` state envelopes serialize nullable outcome fields as JSON `null`, never phantom values; deserialization also enforces this invariant.
3. (security) Path hygiene: audit that no `canonicalize`d path, `CONDUCTOR_*` value, or `ANDROMEDA_PULSE_DATA_DIR` can leak into a serialized envelope (code review + grep for path-bearing field names).

## Relevant amendment history
(none) — no prior amendments touch run-report envelope serialization or artifact hygiene in secondary scope.
