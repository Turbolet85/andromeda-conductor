a11y detector returned: 1 proposal (D-a11y-obs-schema, warning).

proposals:
  - detector: D-a11y-obs-schema
    severity: warning
    section: §6 State color tokens (not-color-alone) → State-naming crosswalk table (a11y-plan lines 422–435)
    change: CalibrationRegion verdict now maps to ManualCheck state by default (Verdict::default_report_state);
      ManualCheck state now serves a dual role (operator-checklist AND auto-measured calibration verdicts).
      Clarify the HOLD row (was "verdict-only; do not join HOLD on state") and the Manual row (was
      "n/a — no verdict") to reflect the co-occurrence.
    sidecar: 2026-06-21 — CalibrationRegion→ManualCheck default mapping documented; ManualCheck semantics
      widened to include auto-measured calibration-region checks; crosswalk clarified.
    rationale: report §Decisions P4 mapping widens ManualCheck beyond operator-checklist to auto-measured
      calibration verdicts via Verdict::default_report_state(); the §6 crosswalk currently marks CalibrationRegion
      "verdict-only" without the default state mapping — divergence from obs §6 join semantics.

Orchestrator note: this exposes a six-lamp distinctness tension (verdict=CalibrationRegion + state=ManualCheck
now co-occur, which the a11y design treated as mutually exclusive) — ESCALATED to the user.
