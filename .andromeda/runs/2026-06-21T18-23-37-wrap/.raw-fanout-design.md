design detector returned: proposals: []

Gist: D-design-tokens has no scope — the chunk renders no new UI (all three new APIs are internal library
functions; report Coverage marks tokens n/a). The ManualCheck semantic widening changes meaning, not any
rendered token / label / glyph.
