obs detector returned: proposals: []

Gist: D-obs-instrumentation — the new APIs are pure constructors/assembly (no must-trace op; the live run is
Epoch 8) per the broadened deferred-span playbook rule; the 11-field line still carries run_id. D-obs-stack —
no OTel SDK / exporter; no telemetry deps. D-obs-redaction — exactly-11-keys golden proves no host-path /
struct-name / Assessment-field leak. obs envelope schema unchanged.
