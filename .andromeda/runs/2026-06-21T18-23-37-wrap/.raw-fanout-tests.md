tests detector returned: proposals: []

Gist: D-tests-coverage — all three new APIs carry unit tests (golden + inverse + 5 bridge tests), seeded/
deterministic. D-tests-framework — cargo-nextest, matches §2/§4. D-tests-obs-harness — envelope wire form
UNCHANGED (golden byte-identical), so test-plan §3 ↔ obs-plan §3 still agree.
