proposals: []
# D-a11y-surface: no new interactive UI element (static Markdown artifact); status-never-color-alone honored via ASCII prefix → no drift.
# D-a11y-obs-schema: no schema change (consumes existing RunRecord; no envelope/violation-schema edit) → no drift.
