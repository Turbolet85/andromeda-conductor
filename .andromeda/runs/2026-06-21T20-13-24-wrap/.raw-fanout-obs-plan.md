proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "4. Span / Trace Coverage"
    change: "Clarify that the report.generate span is emitted by the Epoch-8 cli/timeline caller (boundary wrapper around RunReport::render/write), not the library primitive."
    sidecar: "report.generate span instrumentation deferred to Epoch 8 (cli/timeline caller); RunReport::render/write are pure/boundary ops."
    rationale: "Report states 'no report.generate span yet — emitted by the Epoch-8 cli/timeline caller; the primitive has no driver'."
# D-obs-stack: serde_json/thiserror only; no OTel SDK/exporter → no drift.
# D-obs-redaction: hygiene test asserts no host-path/struct-name leak; blocked-row null rule renders em-dash not null → no drift.
