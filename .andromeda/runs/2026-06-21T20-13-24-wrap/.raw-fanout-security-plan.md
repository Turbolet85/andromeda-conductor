proposals: []
# D-security-input: no new external-input surface (renders already-validated envelope; runs_dir resolved at cli edge) → no drift.
# D-security-subprocess: chunk does not touch sidecar spawn/preflight/data-dir → no drift.
# D-security-deps: no new dependency (serde_json/thiserror already present; no Cargo.toml change) → no drift.
