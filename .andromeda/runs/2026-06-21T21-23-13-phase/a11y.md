# a11y extract

## Relevance
partial — this chunk classifies all 60 P-IDs into Conductor verification modes (auto/drive+observe/static-only) but does NOT add new UI surfaces; the classification render and artifact emit through Markdown/artifact paths, not DOM interactions.

## Constraints
Per a11y-plan §3 Structured violation JSON schema: artifact write is "loud-never-overwrite-safe" consistent with run-report seam (§3 uses the obs envelope contract). Per §3 Bootstrap phases, this chunk's artifact emission inherits the obs Section 6 envelope binding (service.name / deployment.environment tagging); violation artifacts MUST NOT leak absolute host paths or internal seam-crate struct names (inherited from tests' Status JSON constraint). Per §6 State color tokens, the display label (DOM text) for each P-ID's verdict/state MUST carry text label + glyph supplement (SC 1.4.1 not-color-alone) if rendered with a lamp state in any downstream view — this chunk's classification table only stores the mode and static-only intent; live lamp rendering (downstream Epochs 8/9) inherits this constraint.

## Patterns to follow
Per a11y-plan §3 Structured violation JSON schema and §10 SLO Invariants, if this chunk's artifact render surfaces any live-status column (resolved in planning), lamp precedence MUST reuse `Lamp::for_record` (not re-derive) — cited as a follow-up binding in scope.md. Per §6 State-naming crosswalk, if HOLD/Manual/Fail states appear, join on the verdict enum (verdict-first lamp precedence per amendment 2026-06-21-run-report-envelope-serializer) — `verdict == CalibrationRegion` ⇒ HOLD lamp regardless of state. Per §3 Artifact retention, if the coverage-matrix.md is uploaded as a GitHub Actions artifact, use default retention aligned with tests' CI patterns.

## Anti-patterns to avoid
NEVER render state colors by color alone (SC 1.4.1) — if the artifact includes lamp rendering, pair every state (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) with its text label + glyph supplement. NEVER skip the `Lamp::for_record` reuse binding — if a live-status seam is wired (open question 1), lamp rules MUST NOT be re-derived; this prevents the coverage-matrix view, CLI table, and desktop view from showing contradictory verdict presentations.

## Contract bindings
a11y ↔ obs: artifact envelope inherits obs Section 6 JSON contract (service.name / deployment.environment / structured violation fields); if live status is rendered, lamp semantics bind to obs verdict/state enums (verdict-first precedence per a11y-plan-amendments §2026-06-21-run-report-envelope-serializer).

## Acceptance criteria contributions
(a11y) If coverage-matrix.md renders a live-status column, `Lamp::for_record` precedence matches obs run-report lamp derivation (no contradictory state renderings across surfaces). (a11y) If artifact includes lamp rendering, every state (`Pass`/`HOLD`/`Fail`/`Blocked`/`Manual`/`Residual`) carries both text label and glyph supplement (SC 1.4.1 not-color-alone assertion — supplemental to axe-core, verified via DOM text-content inspection).

## Relevant amendment history
2026-06-21-run-report-envelope-serializer — CalibrationRegion→ManualCheck co-occurrence clarified; verdict-first lamp precedence rule added (verdict == CalibrationRegion ⇒ HOLD lamp regardless of state; Manual = ManualCheck state WITH NO verdict). This amendment affects any downstream lamp rendering of the classification's static/live status column — the coverage-matrix's state-label DOM text and obs envelope join MUST use verdict-first precedence.
