proposals: []
# D-security-input: clean (classification is code-native static const; CoverageMatrix::write takes a caller-resolved path; no new external-input boundary).
# D-security-subprocess: clean (no sidecar/preflight/data-dir touch).
# D-security-deps: clean (no deps added/bumped; floors met).
