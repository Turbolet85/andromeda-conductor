proposals: []
# D-tests-coverage: clean (11 unit tests at the mandated tier; deterministic, no network/time).
# D-tests-framework: clean (cargo-nextest + assert_fs + exact-string assert_eq, on-spec).
# D-tests-obs-harness: clean (coverage-matrix.md is a separate artifact; does NOT touch the 5-command harness / JSONL envelope / §3).
