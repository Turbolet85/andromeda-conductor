proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources / On-disk artifacts / database"
    change: "register declarative scenario config files under scenarios/ (one per P-ID, serde+garde validated at load) as an occupied artifact category"
    sidecar: "scenario config files registered as occupied artifact (connection-lifecycle chunk added 4 P-001..P-004 TOMLs)"
    rationale: "report lists 4 new scenarios/*.toml; arch §Occupied Resources §On-disk artifacts lists only run artifacts + coverage-matrix + manifest, not scenario config files."
