# a11y extract

## No domain coverage
Out-of-scope — this chunk defines scenario TOML configurations (error-baseline-spike, latency-regression) with no rendered UI surface or interactive DOM elements per a11y-plan §1 (cli surface not-assertable; desktop-webview UI rendering of results is a separate, later chunk).
