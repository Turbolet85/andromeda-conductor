# design extract

## No domain coverage
Out-of-scope — this chunk formalizes statistical-anomaly scenario TOMLs and their baseline-math assertions with zero UI/webview/CLI rendering surface (pure scenario catalog wiring over existing Epoch-3 emit primitives). The design system owns tokens / motion / typography / component patterns for surfaces; scenario TOML files are architecture/schema artifacts with no design expression layer to extract.
