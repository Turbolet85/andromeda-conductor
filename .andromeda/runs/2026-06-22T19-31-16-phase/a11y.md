# a11y extract

## Relevance
Partial — the chunk implements scenario-catalog infrastructure (declarative TOML + lifecycle timing) with no new rendered UI surface; a11y surfaces are read-only (existing verdict/state lamps, live counters, operator-pause flow) and forward-looking to Epoch 9.

## Constraints
- Per a11y-plan §1 A11y Scope Summary: the verdict/report-state lamp carries six state-color treatments, each text-paired + glyph-supplemented (SC 1.4.1 Use of Color).
- Per a11y-plan §6 Visual Design Verification: token pairs asserted at 3:1 (SC 1.4.11 Non-text Contrast); any new severity-tier / resolved-state color needs SC 1.4.3/1.4.11 assertion.
- Per a11y-plan §4 ARIA Patterns: the verdict lamp is `status` role with `aria-live` on state change (SC 4.1.3 Status Messages).
- Per a11y-plan §1 Critical Paths (View run report): live-counter updates announced via `aria-live` without stealing focus; in-progress prose announced as status, NOT Fail.
- Per a11y-plan §3 Bootstrap phases: no new UI surface ⇒ no contrast-harness or screen-reader-spec scope addition this chunk.

## Patterns to follow
- TOML scenario structure is read-only from the a11y perspective — no new form input, picker state, or interactive control.
- CalibrationRegion class binding (a11y-plan §4 + amendment 2026-06-21): `Verdict::CalibrationRegion` ⇒ HOLD lamp (verdict-first); `state=ManualCheck` with `verdict!=CalibrationRegion` ⇒ Manual lamp.
- Live run-report display (a11y-plan §1): existing `aria-live` coverage on the lamp + counters; lifecycle events (120 s cease, cool-down, reopen) are non-visual, no new status-message class needed.

## Anti-patterns to avoid
- Per a11y-plan §11 (ARIA): do NOT aria-hidden lifecycle timing state; the HOLD lamp transition is `aria-live`; manual cool-down steps are declare-only, not silent.
- Per a11y-plan §6: do NOT render state-color transitions without the text label (`Resolved`/`Active`/…) — every state not-color-alone (SC 1.4.1).

## Contract bindings
- **a11y ↔ obs (§6):** verdict/state lamps emit to structured `verdict`/`state` fields; no new emission shape.
- **a11y ↔ tests (§6):** reuse the `tauri-driver` + WebdriverIO harness; operator-pause dialog Tab-trap + Escape assertions already exist; no new driver.

## Acceptance criteria contributions
- (a11y) [Epoch-9 forward] Verdict/state lamp rendering: `Resolved`/`Active`/`Manual` text labels alongside color (SC 1.4.1); no new color token without SC 1.4.3/1.4.11 assertion (per a11y-plan §6).
- (a11y) [Epoch-9 forward] CalibrationRegion state mapping: HOLD lamp iff `verdict==CalibrationRegion` per the §6 crosswalk; axe-core `aria-live` passes on the lamp `status` role.
- (a11y) [Epoch-9 forward] In-progress prose persisted: lifecycle state changes do NOT downgrade in-progress prose to a Fail announcement (per a11y-plan §1 Critical Path).

## Relevant amendment history
- **2026-06-21-run-report-envelope-serializer:** CalibrationRegion→ManualCheck co-occurrence + verdict-first lamp precedence (a11y-plan §6 crosswalk). HOLD row = ManualCheck state + CalibrationRegion verdict; Manual row = ManualCheck state + non-CalibrationRegion verdict. Relevant: this chunk's CalibrationRegion legs route through `Verdict::default_report_state`, keeping the six lamps distinct.
