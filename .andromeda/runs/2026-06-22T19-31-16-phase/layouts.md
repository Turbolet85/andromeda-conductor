# layouts extract

## No domain coverage

This chunk is backend scenario-catalog TOML work (severity-lifecycle test fixtures); the layouts domain covers operator-facing surfaces (desktop-webview + CLI output structures), not test artifact structure.
