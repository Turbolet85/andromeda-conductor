# a11y extract

## Relevance
Partial — the chunk adds operator-checklist (ManualCheck) scenario entries and KnownResidual state exercise; no new UI surface is implemented, but the scenarios declare visual/manual verification contracts for existing ManualCheck + KnownResidual lamp states.

## Constraints
- Per a11y-plan §1 A11y Scope Summary: operator-checklist (`ManualCheck` render) is an assertable entity (checkbox-per-item with aria-checked + unticked-count roll-up)
- Per a11y-plan §4 ARIA Patterns & Roles: ManualCheck role → `checkbox` per item, group/region container, unticked-count roll-up announced (SC 4.1.3)
- Per a11y-plan §6 Visual Design Verification: `--status-manual` token pairs drive the lamp state (not-color-alone via text label `Manual` + glyph supplement)
- Per a11y-plan §2 A11y Strategy: every WCAG SC must have ≥1 machine-verifiable path (axe-core / WebdriverIO / colorjs.io); no manual-only verification
- Per a11y-plan §3.5 Bootstrap phases: scenario catalog exercises existing shadcn/Radix components; no new ARIA library install needed

## Patterns to follow
- TOML scenario declarations carry P-IDs and expected outcomes (ReportState `ManualCheck` / `KnownResidual`) — existing pattern from Epoch 7 scope
- Operator-checklist path (P-025/026/027) exercises the manual-verification contract (operator-checklist items with induced-state + expected-observation text per a11y-plan §1)
- KnownResidual verdict state (P-032) pre-accepts deviation without WCAG violation (aligned to a11y-plan §3 Structured violation JSON schema: state=`Fail` only when violations.length>0; pre-accepted deviations emit state=`KnownResidual` cleanly)

## Anti-patterns to avoid
- Do not introduce new ARIA patterns or component roles not named in a11y-plan §4 (existing checklist/button/status roles are sufficient)
- Do not add motion/animation to the scenario narratives without reduced-motion parity (per a11y-plan §9 Motion & Animation: SC 2.3.3 drop all animations under prefers-reduced-motion)
- Do not declare scenarios requiring untested UI paths (a11y focus trap / keyboard sequence assertions in §3 Keyboard test harness must cover scenario code paths)

## Contract bindings
- Operator-checklist test path ↔ tests harness (per a11y-plan §3 Keyboard test harness: Space-toggle harness via WebdriverIO `browser.keys()` must exercise P-025/026/027 checklist flows; binding to tests §6)
- ManualCheck/KnownResidual state expression ↔ obs harness (per a11y-plan §3 Structured violation JSON schema: state=ManualCheck/KnownResidual fold into `fingerprints[]` with WCAG SC reference aligned to obs Section 6 envelope; binding to obs §6)
- Verdict-lamp state (`status-manual` + `status-residual` tokens) ↔ design token harness (per a11y-plan §6 Visual Design Verification: contrast pairs for both states must meet SC 1.4.3 4.5:1 and SC 1.4.11 3:1 via colorjs.io; binding to design §Color Palette)

## Acceptance criteria contributions
- (a11y) Scenario TOML entries for P-025/026/027 declare ManualCheck state + operator-checklist items render with checkbox role + aria-checked + unticked-count announcement (per a11y-plan §4 Operator-checklist pattern).
- (a11y) WebdriverIO keyboard harness covers Space-toggle on checklist rows across new scenarios (per a11y-plan §3 Keyboard test harness § Sequences per ARIA pattern: checkbox Space-toggle).
- (a11y) P-032 KnownResidual verdict emits structured violation JSON with state=`KnownResidual` (no WCAG violations; pre-accepted deviation per a11y-plan §3 Structured violation JSON schema).
- (a11y) `--status-manual` + `--status-residual` token pairs in rendered lamps meet SC 1.4.3 4.5:1 normal-text + SC 1.4.11 3:1 non-text via colorjs.io harness (per a11y-plan §6 Color contrast pairs).

## Relevant amendment history
- **2026-06-21 run-report-envelope-serializer:** CalibrationRegion→ManualCheck co-occurrence rule (verdict-first lamp precedence) — P-032's `verdict==CalibrationRegion` + `state==ManualCheck` now co-occur, so HOLD lamp is identified by verdict, not state. Cascade confirms a11y-summary.md/rules carry no crosswalk detail; amendment resolves lamp-state naming in this chunk's KnownResidual exercise (a11y-plan §6 unmodified; amendment was observational).