# a11y extract

## Relevance
partial — chunk is CLI/headless harness (not-assertable per plan §1), but scope explicitly names the `logs` command emitting a11y results.

## Constraints
- Per plan §1, the cli surface is **not-assertable** (no DOM/widget tree; no axe/Lighthouse reach); it carries no ARIA roles, focus management, or keyboard-navigation assertions.
- Per plan §3 CI Integration, a11y artifacts (`violations[]` + contrast + keyboard PASS/FAIL) **ride the tests' `wdio run` webview-E2E job** and **emit through the `logs` command** of the 5-command contract — this harness scaffolds the shell discipline to surface those results.
- Per plan §6 State color tokens, the `logs` output must never render state by color alone; CLI path uses **`[PASS]`/`[FAIL]`/`[HOLD]`/`[BLOCKED]` ASCII prefixes** (binding to scope output-discipline § "status is never color-alone").
- Per plan §3 Structured violation JSON schema, a11y artifacts MUST NOT leak absolute host paths or internal seam-crate struct names (inherited from tests' Status JSON constraint).
- Per plan §3 Bootstrap phases, the `logs` command binds to the **violation-json-emission-wire** phase — structured JSON folded into obs Section 6 envelope with resource attributes (`service.name` / `deployment.environment`).

## Patterns to follow
- **Structured JSON emission (not just prose):** `logs` command surfaces the per-run JSONL emission journal + a11y artifact envelope (axe `violations[]` + colorjs.io ratio assertions + keyboard PASS/FAIL) folded into obs Section 6 format (`journal_emitted_at` / `read_back_observed_at` / `run_id` / `seed` / `scenario` / `p_ids` / `verdict` / `state` / `latency_ms` / `slo_tier` / `fingerprints`).
- **No color-alone status in CLI:** CLI surface status lines prefix with `[PASS]` / `[FAIL]` / `[HOLD]` / `[BLOCKED]` ASCII tokens, never color-only (scope violation) — plan §11 Anti-Patterns § CI ("never treat the cli surface as accessibility-assertable") + scope "status is never color-alone".
- **No human-only verification fallback:** if a11y results appear in `logs`, they MUST originate from the automated axe-core/colorjs.io/WebdriverIO stack (plan §3 § CI integration "rides tests' `wdio run` webview-E2E job"), never from manual NVDA/VoiceOver-only review.

## Anti-patterns to avoid
- NEVER skip the `logs` command a11y artifact emission — violations MUST surface deterministically for agent / operator parsing (plan §11 CI "a11y step is FAIL-fast gate" + scope "logs command surfaces per-run JSONL + a11y results").
- NEVER use color-only status indicators in the CLI output (plan §11 Visual "state by color alone" + scope "status is never color-alone").
- NEVER hand-emit CLI a11y data without the obs Section 6 schema binding — axe / colorjs.io / WebdriverIO outputs must fold into the named envelope structure, not arbitrary prose (plan §3 Structured violation JSON + obs binding).

## Contract bindings
- **Obs §6 Log Format envelope:** a11y violation artifacts (axe `violations[]` + ratio assertions + keyboard PASS/FAIL) fold into `fingerprints[]` per plan §3 Structured violation JSON schema (binding D-a11y-obs-schema); `logs` command emits this envelope.
- **Tests §6 5-command harness:** a11y step reuses tests' `@crabnebula/tauri-driver` + WebdriverIO session; results emit through the `logs` command of the boot/run/status/cleanup/logs discipline (plan §3 CI Integration — "rides tests' `wdio run` webview-E2E job").
- **Output discipline (scope § Surfaces/contracts):** CLI `[PASS]`/`[FAIL]`/`[HOLD]`/`[BLOCKED]` ASCII prefixes on status lines (not color-alone per scope); resource attributes `service.name` / `deployment.environment` per plan §3 CI-specific resource attributes.

## Acceptance criteria contributions
- (a11y) `logs` command emits structured a11y JSON (per obs Section 6 envelope + plan §3 schema) for the latest (or named) run without requiring a live Pulse (plan §3 CI Integration "no mutation, no live Pulse required").
- (a11y) CLI output status lines use `[PASS]`/`[FAIL]`/`[HOLD]`/`[BLOCKED]` ASCII prefixes, never color-only state indication (plan §6 State color tokens + scope output-discipline).
- (a11y) a11y artifact envelope MUST NOT leak absolute host paths or internal seam-crate struct names; node selectors are DOM-relative (plan §3 Structured violation JSON schema + tests' Status JSON constraint).

## Relevant amendment history
- **2026-06-16-emission-journal-writer** (plan § amendments): Structured violation JSON schema reproductions now include `read_back_observed_at` (obs envelope binding D-a11y-obs-schema). The `logs` command must emit this 11-field envelope, not a 10-field variant.
- **2026-06-21-run-report-envelope-serializer** (plan § amendments): State-naming crosswalk clarified — `verdict == CalibrationRegion` ⇒ HOLD lamp (verdict-first precedence); `ManualCheck` state without `CalibrationRegion` verdict ⇒ Manual lamp. The `logs` output must use this crosswalk when rendering a11y artifact state (not color-alone, per plan §11 Visual).
