# layouts extract

## Relevance
out-of-scope for this chunk

## No domain coverage
Agent-run harness (ch2/5) is shell orchestration + artifact-read; it touches neither surface layout (desktop-webview nor cli) nor component placement. The harness invokes pre-built `conductor-cli` verbs (ch1) and reads `runs.db` artifacts without modifying UI structure, wireframe, focus order, or responsive behavior. No layout work exists here.
