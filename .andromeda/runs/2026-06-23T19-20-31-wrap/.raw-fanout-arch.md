proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources › Environment variables"
    change: "Add `CONDUCTOR_PREFLIGHT_TIMEOUT` — timeout (seconds) for the preflight readiness gate; overridable for test hermicity."
    sidecar: "Register CONDUCTOR_PREFLIGHT_TIMEOUT in env-var table; consumed by conductor-cli::commands::preflight for MCP initialize timeout."
    rationale: "Report line 20 documents that the new preflight verb consumes `CONDUCTOR_PREFLIGHT_TIMEOUT` alongside existing `ANDROMEDA_PULSE_DATA_DIR` / `CONDUCTOR_CONTRACT_MANIFEST`, but this variable is not listed in arch §Occupied Resources §Environment variables."

# D-arch-decisions: clean (no new stack/dep; clap 4 verb is an allowed-pattern addition).
# NOTE (orchestrator): the env var is read by agent-run.{sh,ps1} (PREFLIGHT_TIMEOUT_SEC) since the
# Epoch-1 skeleton, not newly-landed this chunk — but it is a real CONDUCTOR_* handle genuinely absent
# from the arch registry, and this chunk makes the preflight path it gates load-bearing. Apply.
