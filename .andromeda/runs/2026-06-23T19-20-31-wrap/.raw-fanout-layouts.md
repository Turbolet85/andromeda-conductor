proposals:
  - detector: D-layout-surface
    severity: warning
    section: "Primary screens (commands)"
    change: "Add `conductor preflight [--json]` (readiness gate: exits 0 iff ready:true, else non-zero; emits ReadyState JSON with precondition) + note the agent-run `run` stage flags (--unit|--integration|--e2e; default=full bundled gate)."
    sidecar: "chunk adds conductor preflight verb (4th CLI verb) + agent-run run-stage flags to the cli Primary screens."
    rationale: "Report lines 14, 17 — new `conductor preflight` verb (verb surface 3→4) + run stage flags, not in layout-templates §cli Primary screens (which lists run/suite/report + agent-run.sh)."

# 5 clean (security/obs/tests/design/a11y returned proposals: []).
