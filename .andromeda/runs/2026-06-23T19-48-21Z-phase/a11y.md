# a11y extract

## Relevance — partial

## Constraints — domain rules that apply

1. CLI surface is not-assertable; no axe-core / Lighthouse / pa11y / WebDriver reach (per a11y-plan §1, Scope Summary — not-assertable surface)
2. Never convey state by color alone; ASCII status prefix MUST pair with any color output (per a11y-plan §11 Anti-Patterns § Visual + § Universal, and §1 Scope Summary a11y contract)
3. NO_COLOR environment variable must suppress color; terminal auto-detection preserves line meaning (per a11y-plan §11 Anti-Patterns § Universal — "NEVER treat the cli surface as accessibility-assertable; ASCII status prefixes / NO_COLOR text-pairing are output-stream discipline")
4. Six state tokens (`--count-nominal` / `--count-hold` / `--status-fail` / `--count-blocked` / `--status-manual` / `--status-residual`) must map to stable ASCII prefixes per the verdict-first lamp precedence rule (per a11y-plan §6 State color tokens crosswalk + amendments §2026-06-21 verdict-first lamp precedence)
5. All accompanying color transitions must be dropped under reduced-motion preference in downstream consumers (per a11y-plan §6 Motion tokens — global rule applies to all rendering surfaces)

## Patterns to follow — existing patterns relevant to implementation

1. Status lines render with ASCII prefix ALWAYS present (`[PASS]` / `[FAIL]` / `[HOLD]` / `[BLOCKED]`); color is optional overlay, never sole encoder (per scope.md Acceptance intent + a11y-plan §1)
2. Coverage table and summary table mirror the Markdown renderer's state→lamp mapping; verdict-first precedence: `verdict == CalibrationRegion` ⇒ HOLD lamp regardless of `state` (per a11y-plan §6 State color tokens + amendments §2026-06-21)
3. owo-colors `if_supports_color` / tty auto-detection ensures piped / non-tty output contains no raw escape sequences (per scope.md Boundaries ch5 + Acceptance intent)

## Anti-patterns to avoid — domain bans that apply

1. NEVER use color as the sole indicator of state (CLI carries no WCAG applicability, but the output-discipline "never color-alone" binding is mandated per a11y-plan §11 § Universal anti-pattern § 5: "ASCII status prefixes / NO_COLOR text-pairing are output-stream discipline")
2. NEVER invent new state tokens or rename the six existing tokens; use the verbatim names from design-system.md binding (per a11y-plan §6 Motion tokens / State color tokens — "Never invent tokens; raw values stay in design-system")

## Contract bindings — where your domain ties into another

- **a11y ↔ design (token names):** The six state-color token names (`--count-nominal`, `--count-hold`, `--status-fail`, `--count-blocked`, `--status-manual`, `--status-residual`) are fixed per design-system.md Section 3 A11y-Relevant Design Tokens (binding read-only — a11y reproduces verbatim, not invents); CLI renderer reads those same token names for the terminal-color mapping per scope.md "design-system.md palette → terminal-color mapping"
- **a11y ↔ obs (state→verdict→lamp mapping):** The verdict-first lamp-precedence rule (`verdict == CalibrationRegion` ⇒ HOLD lamp, per a11y-plan §6 State color tokens crosswalk + amendments §2026-06-21) is the canonical state-naming join with obs; the CLI renderer mirrors the Markdown renderer's existing lamp precedence (already in `conductor-report` per scope.md "It MUST mirror the existing state→lamp mapping… already realized in `conductor-report`'s Markdown renderer")

## Acceptance criteria contributions — concrete pass/fail checks your domain adds

1. "(a11y—output-discipline) Status lines render with ASCII prefix ALWAYS present; with color stripped (non-tty / `NO_COLOR`) the line is still unambiguous — the six state outcomes are not conveyed by color alone."
2. "(a11y—output-discipline) Coverage table and summary table state→lamp rendering mirrors `conductor-report` Markdown renderer; verdict-first precedence applied consistently across both surfaces (CalibrationRegion→HOLD regardless of ManualCheck state)."
3. "(a11y—output-discipline) Piped / non-tty / `NO_COLOR` output contains no raw ANSI escape sequences; owo-colors tty auto-detection + `if_supports_color` gate suppress color transparently."

## Relevant amendment history

- **2026-06-21 run-report-envelope-serializer — CalibrationRegion→ManualCheck co-occurrence + verdict-first lamp precedence:** The HOLD row now maps `verdict == CalibrationRegion` ⇒ HOLD lamp regardless of `state`; the Manual row is `ManualCheck` state with NO verdict. **Why:** this chunk mirrors the existing Markdown renderer's verdict-first precedence, so the same rule applies to the CLI terminal output (both surfaces render the same canonical lamp mapping). This amendment ensures the CLI renderer uses the correct lamp assignment when reading `RunRecord` + `Verdict`/`ReportState` enums (per scope.md "It MUST mirror the existing state→lamp mapping… already realized in `conductor-report`").