# a11y extract

## Relevance
partial — CLI interactive surface added; not-assertable per §1 (no DOM/ARIA tools reach the terminal) but a11y constraints on text-based feedback apply.

## Constraints
1. Per §1 scope: CLI surface is not-assertable (no axe-core / Lighthouse / pa11y reach on line-oriented stdout), so no automated a11y test harness applies.
2. Per §1 anti-patterns "status is never color-alone": the hold phase-line MUST carry `[HOLD]` ASCII prefix always; color overlay is optional/tty-gated and never sole encoder (scope §a11y notes).
3. Per §2 strategy POUR-Perceivable (Minimal): six state tokens (including `--count-hold` for HOLD) require text pairing; a11y triggers §visual-discrimination names state colors must always carry labels.
4. Per §5 keyboard navigation: `inquire` confirm prompt MUST remain keyboard-driven; no keyboard-trap risk (single TTY prompt, not a dialog with focus trap), but keyboard-first accessibility default applies.
5. Per scope: prompt MUST NOT leak raw redacted data (scope §obs notes redaction edge); the rendered prompt is human-read only, not machine-emitted to logs.
6. Per §1 scope: headless (non-TTY) path is never-blocking default — no interactive wait on a pipe, so no a11y surface for the headless resolver itself.

## Patterns to follow
1. CLI status-line pattern from §3 render seam: reuse ch3's `Lamp`→line mapping (verdict-first lamp precedence per 2026-06-21 amendment); hold line shows `[HOLD]` prefix alongside label text per scope.
2. Keyboard-first prompt: `inquire`'s default confirm is keyboard-operable (Enter/N/y responses); maintain character-based interaction (no mouse-only UI).
3. Color-pairing precedent: ch3's `NO_COLOR` / `stdout_color()` tty-gate (already in §1 tools inventory per scope); reuse the same gate for the hold line's amber overlay.

## Anti-patterns to avoid
1. Per §1 ARIA: no fake ARIA roles, landmarks, or live regions on CLI stdout (terminal has no DOM tree); never emit ANSI escape codes for structure (color is allowed if tty-gated and text-paired).
2. Per §2 invariants: do NOT use color alone to signal hold state — MUST pair `[HOLD]` text / ASCII prefix with any color (scope explicitly tests this constraint).
3. Per §1 scope: do NOT entangle the interactive resolver with the Tauri AlertDialog go/no-go UI — they are separate surfaces sharing the core `PauseResolver` trait only; no model coupling.

## Contract bindings
Obs ↔ tests harness: scope notes hold-await emit a bounded `tracing` event for the paused/resumed transition (run_id-tagged); MUST NOT stamp from virtual clock and MUST NOT leak raw prompt (redaction edge per scope §Obs). Obs plan's structured logging is ch5, not this chunk.

## Acceptance criteria contributions
1. (a11y/CLI) `[HOLD]` ASCII prefix + label text present on hold line; color is optional tty-gated overlay, never sole encoder — test via `NO_COLOR=1` run showing unambiguous hold state.
2. (a11y/CLI) `inquire` confirm prompt keyboard-operable: Enter to proceed, N/n to decline (when `allow_no_go == true`); Y/y to confirm; no mouse-only interaction path.
3. (a11y/CLI) Headless (non-TTY) path never blocks: auto-resolves via `HeadlessResolver::proceed()` and records the `Decision`; no hang on piped/agent runs.

## Relevant amendment history
2026-06-21-run-report-envelope-serializer — `Verdict::default_report_state` maps `CalibrationRegion → ManualCheck`, so hold line's `[HOLD]` state-color logic now pairs with `ManualCheck` state (not "verdict-only"). Verdict-first lamp precedence keeps six lamps distinct (HOLD = CalibrationRegion verdict regardless of state). No a11y behavior change (CLI hold-line pattern already text-paired under this amendment).
