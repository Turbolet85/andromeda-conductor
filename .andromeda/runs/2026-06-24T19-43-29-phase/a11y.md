# a11y extract

## No domain coverage

CLI stderr formatting + agent-mode flag are not-assertable surfaces per a11y-plan §1 (no DOM/widget tree, no axe-core/Lighthouse reach); a11y domain covers only the desktop-webview Tauri control panel, which this chunk does not touch.
