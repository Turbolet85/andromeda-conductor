proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources / Crate names (workspace members)
    change: Add conductor-run to the workspace-member list (position 7, before conductor-cli) with note that it houses the extracted pipeline-to-library APIs (execute_scenario/persist/drive_run) consumed by both bins.
    sidecar: "Add conductor-run library (9th workspace member) to crate registry; houses execute_scenario/persist/drive_run extraction from cli/pipeline.rs, usable by both conductor-cli and conductor-tauri bins."
    rationale: "Report §Changes lists 'ADDED conductor-run (9th workspace member, sits above the seams below the bins)'; current §Occupied Resources lists only 8 crates, omitting conductor-run."

# D-arch-decisions: no drift (current_thread runtime via Builder::new_current_thread aligns with §Async Runtime Flavor; Channel pinned in §Real-time Strategy; no new external crate).