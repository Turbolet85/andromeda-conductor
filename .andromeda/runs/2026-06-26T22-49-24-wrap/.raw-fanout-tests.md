proposals:
  - detector: D-tests-coverage
    severity: warning
    section: "§5 Integration Test Strategy — Boundary types covered table"
    change: "Add note after Module ↔ IPC (Tauri) row: 'tauri::test mock-runtime integration tests for start_run/stop_run commands and cross-surface parity validation deferred to Epoch-9 GUI test-harness chunk (2026-06-26-live-counter-channel-stream); run logic determinism covered by conductor-run unit tests (drive_run stream+persist, abort paths).'"
    sidecar: "2026-06-26: Tauri integration tests deferred to Epoch 9 per live-counter-channel-stream; unit-level run logic coverage via conductor-run satisfies test-coverage intent pending integration-tier harness completion."
    rationale: "Report Deviation 1: thread-timing flakiness in async Channel streaming deferred to operator-gated GUI smoke. Report Outcome: 'except §7b's literal tauri::test (intent met via conductor-run).' Test-plan §5 and §6 (Both-surface parity critical path) require tauri::test integration tests, which are not present in the Outcome."

# D-tests-framework: no drift (cargo nextest 402, +4 unit tests, framework matches §2/§4).
# D-tests-obs-harness: no drift (RunRecord envelope UNCHANGED; RunEvent is transient IPC, not in JSONL/obs schema; 5-command harness unchanged).
# (5 other doc-agents returned proposals: [] — security/design/layouts/obs/a11y; all escalate-severity detectors clean.)