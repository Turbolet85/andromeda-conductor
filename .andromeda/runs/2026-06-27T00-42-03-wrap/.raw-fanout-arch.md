DETECTOR: arch (D-arch-resources, D-arch-decisions)

D-arch-resources: report §Changes introduces NEW Tauri command `coverage_matrix() -> Result<Vec<CapabilityRow>, String>` (nullary, registered in main.rs generate_handler!). arch §Occupied Resources "Interface routes / surfaces" lists Tauri commands as "start/stop, scenario/suite picker, run-report view, operator-pause prompt; one Channel" — coverage_matrix not listed → proposes registering it.
D-arch-decisions: Dependencies "none added/bumped"; all tech in §Stack/§Established Decisions → no violation.

proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources / Interface routes / surfaces"
    change: "Tauri commands (internal IPC): start/stop, scenario/suite picker, coverage-matrix view, run-report view, operator-pause prompt; one Channel for live counters."
    sidecar: "Added coverage_matrix read-only command to Tauri interface surfaces list."
    rationale: "Report §Changes introduces NEW read-only Tauri command coverage_matrix() (nullary, registered in main.rs generate_handler!)."
