DETECTOR: design-system (D-design-tokens)

D-design-tokens: report Coverage of new surfaces — CoverageMatrix UI view: tokens✓ (all var(--…); zero hardcoded hex/px) + a11y✓ (never-color-alone via reused StatusLamp glyph+label). Both invariant conditions satisfied (tokens, never-color-alone). StatusLamp reused (design-system §Iconography "every status lamp paired with text label").

proposals: []
