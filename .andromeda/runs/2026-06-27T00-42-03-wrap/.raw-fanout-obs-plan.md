DETECTOR: obs-plan (D-obs-instrumentation, D-obs-stack, D-obs-redaction)

D-obs-stack: no new deps, no OTel SDK/exporter; frontend console JSON only. Aligns §3. No drift.
D-obs-redaction: no runs.db schema change; command logs only count/latency_ms (safe); static P-ID data, no paths/struct names. Aligns §6. No drift.
D-obs-instrumentation: ⚠ proposes — coverage_matrix is a nullary read-only command with NO scenario context; report confirms span✓ + info!(count, latency_ms) BUT does not confirm run_id is present; obs-plan §6/§1 require run_id on every line; obs-plan §4 is silent on read-only query commands lacking scenario context (precedented by list_scenarios, also unaddressed).

proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Telemetry surfaces / §4 Auto-instrumentation per surface"
    change: "Read-only Tauri commands (list_scenarios, coverage_matrix) that query state without executing a scenario use a synthetic run_id (query-<op>-<ISO>) or inherit the active scenario's run_id, to satisfy §6 run_id-on-every-line."
    sidecar: "Specified run_id handling for non-scenario read-only Tauri commands."
    rationale: "Report adds nullary coverage_matrix with no scenario context; §6 mandates run_id on every log line, ambiguous for a no-run command."
