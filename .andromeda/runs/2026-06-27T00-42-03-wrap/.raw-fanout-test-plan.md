DETECTOR: test-plan (D-tests-coverage, D-tests-framework, D-tests-obs-harness)

D-tests-coverage: coverage_matrix tests "compile-gated (clippy/nextest) + GUI-integration tauri::test deferred to ch9 per route". test-plan §5 already defers tauri::test integration to the Epoch-9 GUI test-harness chunk — authorized, not new drift. No drift.
D-tests-framework: gates cargo nextest 168/168 + 402/402, tsc/vite build/npm audit 0 — all match §2/§4 (cargo-nextest, npm/tsc build-gating). No drift.
D-tests-obs-harness: no change to the 5-command harness, the run-report envelope, or the JSONL log format; coverage_matrix is a Tauri IPC command, not the agent-run harness. §3 ↔ obs-plan §3 unchanged. No drift.

proposals: []
