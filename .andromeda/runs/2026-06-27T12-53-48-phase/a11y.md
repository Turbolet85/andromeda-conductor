# a11y extract

## No domain coverage
A11y verification (axe-core / contrast / keyboard) is explicitly non-CI-gated in this chunk; it lands in a separate downstream display-gated a11y CI gate chunk requiring Linux+xvfb+webview infrastructure that this config-only chunk does not touch.
