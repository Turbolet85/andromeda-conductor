# layouts extract

## No domain coverage
This chunk is a CI-infrastructure-only gate (GitHub Actions workflows, nextest config, artifact patterns) with zero layout / surface / component changes and no new UI surfaces per chunk boundaries ("No engine/seam code change").
