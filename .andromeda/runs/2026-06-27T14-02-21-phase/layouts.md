## No domain coverage

Out-of-scope: this chunk adds CI observability automation (agent-mode runs, log-schema conformance gate, panic check via existing CLI harness) — no surface layout, wireframe, component-placement, or focus-order changes apply. The chunk uses but does not modify the existing cli surface (`agent-run` harness verbs).