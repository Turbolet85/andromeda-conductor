# Clean fan-out returns (6 of 7) — all `proposals: []`

- **arch** (D-arch-resources, D-arch-decisions): no new IPC/endpoint/port/env-var/crate; GitHub Actions + shell:bash + jq already in §Stack. CI-config only.
- **security-plan** (D-security-input, D-security-subprocess, D-security-deps): no new external-input surface (consumes registered ANDROMEDA_PULSE_DATA_DIR / CONDUCTOR_AGENT_MODE); no sidecar/data-dir change; no new deps (Cargo.lock untouched).
- **design-system** (D-design-tokens): no UI rendered; tokens n/a.
- **layout-templates** (D-layout-surface): no new user-facing surface (CI-config only).
- **test-plan** (D-tests-coverage, D-tests-framework, D-tests-obs-harness): no new code path; CI quality gate is not a product test framework; **test-plan §3 already correctly defines the self-obs base fields separately from the per-run envelope — the conformance gate follows §3**. No drift.
- **a11y-plan** (D-a11y-surface, D-a11y-obs-schema): no interactive UI element; no a11y/obs schema change.