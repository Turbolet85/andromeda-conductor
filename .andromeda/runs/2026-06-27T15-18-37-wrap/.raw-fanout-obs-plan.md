proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "Section 9: CI Integration / Log conformance check"
    change: "Update line 531 to reference Section 3 self-obs base schema (timestamp_ms, level, target, service.name, service.version, deployment.environment, run_id per line) instead of Section 6 envelope; clarify that envelope validation for runs/<run_id>.jsonl is deferred per obs-plan §3 line 229 dual-record-shape"
    sidecar: "2026-06-27 obs-ci-conformance-gate reconciles obs-plan §9: logs/agent-latest.jsonl validates §3 self-obs base (per-line telemetry), not §6 envelope (scenario result)"
    rationale: "Report Decisions & corrections identifies reconciliation: obs-plan §9 currently describes validating logs/agent-latest.jsonl against §6 envelope field list, but the artifact carries §3 self-obs base fields. Implementation correctly validates §3 per testing; obs-plan §3 line 229 clarifies the dual-record-shape tension (base per-line vs. envelope scenario-result). obs-plan §9 must align with current truth."

# NOTE (orchestrator): the detector attributed this to D-obs-instrumentation, but the substance is an
# intra-obs-plan §9↔§3 record-shape reconciliation surfaced by the report's Decisions section, not a
# missing-span/run_id instrumentation gap (run_id + std::time stamps both confirmed present).