# a11y extract

## Relevance
partial — chunk produces run-report artifacts (JSONL, runs.db, Markdown) subject to a11y↔obs schema binding, but does not involve a11y testing or assertions (CLI surface is not-assertable per a11y-plan §1).

## Constraints
1. Report artifacts must align to obs §6 Run-report JSON envelope structure per a11y-plan §3 (reproduced verbatim: `journal_emitted_at`, `read_back_observed_at`, `run_id`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `latency_ms`, `slo_tier`, `fingerprints`).
2. Artifacts MUST NOT leak absolute host paths or internal seam-crate struct names per a11y-plan §3 & tests' Status JSON constraint.
3. Resource attributes `service.name` (e.g., `"conductor-cli"`) and `deployment.environment` (from `CONDUCTOR_ENV`, fallback `"local"`) must tag each artifact per a11y-plan §3 Service Identity Tagging.
4. CLI surface is not-assertable (no a11y test harness applies) per a11y-plan §1 — manual NO_COLOR text-paired output is fallback only.

## Patterns to follow
1. obs §6 Run-report envelope field order and types per a11y-plan §3 (verbatim binding: Structured violation JSON schema reproduced in both plans).
2. Verdict-first lamp precedence: `verdict == CalibrationRegion` ⇒ HOLD state regardless of `state` field value per a11y-plan §6 / 2026-06-21 amendment.

## Anti-patterns to avoid
1. Do not leak filesystem paths, config paths, or Rust struct names in artifact outputs (a11y-plan §3 & tests' Status JSON constraint inherited).

## Contract bindings
a11y↔obs schema binding — run-report artifacts envelope structure (obs §6) is the target container for future a11y violation emissions. The obs envelope `read_back_observed_at` field supports journal-relative latency assertions (per a11y-plan §3, added 2026-06-16).

## Acceptance criteria contributions
(a11y) Artifact schema: report envelope fields (`journal_emitted_at`, `read_back_observed_at`, `verdict`, `state`, `slo_tier`) present and correctly typed per obs §6 structure per a11y-plan §3.

## Relevant amendment history
1. **2026-06-16-emission-journal-writer:** obs §6 envelope gained `read_back_observed_at` field; a11y-plan §3 Structured violation JSON schema reproduced the updated envelope verbatim to maintain binding parity (no a11y behavior change; lazy-created amendment sidecar).
2. **2026-06-21-run-report-envelope-serializer:** clarified verdict-first lamp precedence (`verdict == CalibrationRegion` ⇒ HOLD lamp) and `CalibrationRegion→ManualCheck` co-occurrence mapping per a11y-plan §6 state-naming crosswalk (affects manual-check row classification in report).
