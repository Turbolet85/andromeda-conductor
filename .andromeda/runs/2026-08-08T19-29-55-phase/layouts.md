# layouts extract

## No domain coverage

This chunk is a lock-only `Cargo.lock` dependency bump (quick-xml/plist, crossbeam-epoch, anyhow) re-proven through the existing quality gate; scope.md §Surfaces/contracts explicitly states "No engine, seam, scenario, report-envelope, or GUI model change" and zero Rust source change expected — no surface, wireframe region, component placement, focus order, modal, nav, or empty state from layout-templates.md (desktop-webview or cli) is created or modified, and no new cli verb is added to §cli Primary screens.
