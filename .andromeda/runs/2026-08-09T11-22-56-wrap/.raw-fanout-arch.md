proposals:
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies (Error handling row) + Established Decisions [Error Handling] + Inherited Defaults (Error handling)
    change: Replace the pinned `anyhow 1.0.102` with `anyhow 1.0.104` in all three places (arch:31 §Stack Error-handling row, arch:52 §Established Decisions [Error Handling], arch:234 §Inherited Defaults), leaving thiserror 2.0.18 and the verdict/error-wall prose untouched.
    sidecar: anyhow pin 1.0.102 → 1.0.104 (workspace manifest floor raised to clear the unsound RUSTSEC-2026-0190 that cargo-deny denies) — 2026-08-08-dependency-advisory-remediation.
    rationale: The report's Changes → Dependencies bullets list `anyhow` 1.0.102 → **1.0.104** as a lock bump AND a `[workspace.dependencies]` manifest-floor raise, and explicitly names it "the one arch-registered version string this chunk moves", locating the stale pins at architecture.md:31, :52 and :234; arch's §Stack/§Established Decisions/§Inherited Defaults still say 1.0.102, contradicting the version the chunk now builds against (the clippy stage compiled anyhow 1.0.104 clean).

Notes on what I deliberately did **not** propose:

- **D-arch-resources — no drift.** The report's Changes state zero Rust source delta: no new fn / `#[tauri::command]` / endpoint / export / port / socket / env var, and "Crates / modules: none added, removed, or changed. Workspace membership unchanged (9 members)" — which matches the 9 crates already listed in arch §Occupied Resources → Crate names. Nothing new to register.
- **plist / quick-xml / crossbeam-epoch bumps** are transitive lock-only moves (Tauri tree and a dev-dep path); arch registers no version string for any of them, so no registry section goes stale.
- **`rmcp = "1.7.0"` manifest removal** is *convergence*, not drift: arch §Stack (MCP read-back client) and §Established Decisions [MCP Read-Back Client] already record "rmcp removed 2026-06-27" as the baseline; deleting the orphan declaration brings the manifest to the doc, not the reverse.
- **tauri 2.11.3 vs arch's "v2.10.x / latest 2.10.1", and tokio 1.52.3 vs arch's "tokio 1.48.x"** appear only in the report's **Unchanged (asserted)** list, not in the Bumped bullet — pre-existing gaps this chunk did not introduce, so per the changed-only-if-listed rule they belong to a dedicated reconcile pass, not this one.
