# a11y extract

## No domain coverage
Chunk is an offline Rust-side artifact-vs-artifact drift check in `conductor-core` (manifest ↔ coverage classification) whose only exposure surfaces are the cargo test lane and the possible `agent-run`/CLI path — both non-rendered, and the cli surface is declared not-assertable with zero automated a11y tool reach (a11y-plan.md §1 A11y scope, §11 Universal); it adds no desktop-webview UI, no ARIA component, no design token, and no motion/contrast/focus behavior.
