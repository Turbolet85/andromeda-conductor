# RAW — design distiller, ATTEMPT 1 (FAILED validation check 3 "Anchors")

Failure: every bullet under `## Acceptance criteria contributions` cited no `per design-system.md §…`
anchor (Constraints were correctly anchored). Retried once with the check named; the retry passed and
is the extract of record in `design.md`. Preserved verbatim below per `fan-out.md`.

Note: this attempt independently surfaced `layout-templates.md:177` and the layouts extract's `:222`
observation is its own; the retry dropped the extra-site note, which is preserved here.

---

# design extract

## Relevance
Partial — the chunk is core-Rust classification work, but it edits my plan's own text (`design-system.md:7`, `:257`) and widens a `CoverageMode` enum consumed by the webview coverage matrix + CLI coverage table.

## Constraints
- **Doc reconciliation is mine to specify.** `design-system.md:7` (§Brand Identity → Domain anchors, "wall of all 60 capabilities") and `:257` (§Surface: desktop-webview → Component Patterns §3, "the full 60-row wall") must become manifest-relative, exactly as the 2026-08-08 amendment already did for the P-ID-range examples in §Color Palette / §Typography / §cli ANSI map. No token, hex, or type-role changes — wording only.
- **The fourth `CoverageMode` is a classification axis, NOT a verdict/report-state.** Per §Color Palette (Verdict-3-vs-ReportState-5 note) and §Component Patterns §4, the six-lamp set (`Pass`/`CalibrationRegion`/`Fail`/`Blocked`/`Manual`/`Residual`) is a separate axis; not-Conductor's must not join the lamp set nor borrow `--count-hold` / `--status-fail`.
- **Mode strings stay in the reserved mono status tier.** §Typography (Data row) sanctions coverage-matrix columns as JetBrains Mono 500 / 13px / tabular-nums in `--color-id-cyan`; the existing `type-data` binding on the Mode column carries the new spelling unchanged.
- **No new color may be minted for the new mode.** §Surface: desktop-webview / Tokens is a closed 34-token `:root` contract and §cli / Tokens a closed ANSI map; anything the new variant renders binds an existing token by name (`var(--…)` / the mapped ANSI code) — never a raw hex, px, or ms.
- **Coverage matrix stays the dense single-row-per-P-ID list at 82 rows.** §Component Patterns §3 (instrument-panel density, `--border-subtle` dividers, no shadows, virtual-scroll) — row-count growth does not license grouping into tiles or sections.
- **CLI mode column: text first, color second, width dynamic.** §Surface: cli / Component Patterns §3 + Per-Surface Bans — the fourth mode needs an ASCII spelling that survives `NO_COLOR`/pipe stripping, and `comfy-table` width stays terminal-detected, never hardcoded.

## Patterns to follow
- **Single-source the static table through the read-only Tauri command** (`coverage_matrix()` returning the core type with serde wire spellings) — `CoverageMatrix.tsx`'s `CoverageMode` TS union is a mirror of `coverage.rs`, updated in lockstep, never re-authored as a TS table (`.claude/rules/frontend.md` Session Additions, 2026-06-27 coverage-matrix-view).
- **Single lamp truth** — `ui/src/lamp.ts` `LAMP_META` / `lampForRecord` mirrors `conductor-core/src/lamp.rs` arm-for-arm and `StatusLamp` drives color from `LAMP_META.token` inline; reuse it, do not re-spell or extend the lamp union with a coverage mode (design-system.md §Component Patterns §4; frontend rules 2026-06-26).
- **Prose for "no data yet", never a placeholder.** The 22 new rows arrive un-run; §Component Patterns §3 ("Empty") and §6 (empty / in-progress states) require real prose — the shipped "Not yet run" cell already satisfies this.
- **`type-data` / `type-label` class bindings + per-component `.css` binding tokens by name** is this repo's realization of §Tokens (frontend rules: tokens on `:root`, never `@theme`).

## Anti-patterns to avoid
- Never let a not-Conductor's row read as `Blocked` or `Fail` — §Anti-Patterns / Rejected Defaults explicitly bans conflating "not measured / out of remit" with failure (the same discipline that made `Blocked` slate-violet and `KnownResidual` dashed-not-red).
- Never add a palette row or ANSI entry for the new mode — the 2026-06-24 amendment set the correction precedent (reuse the shipped token; a "new color" for an edge case is drift, not design).
- Never restructure the widened wall into a KPI-card grid or grouped tile sections (§Anti-Patterns / Rejected Defaults, "a control surface, not a dashboard").

## Contract bindings
- **a11y §Use of Color (SC 1.4.1):** the Mode column is already textual; whatever visual treatment the *next* chunk adds must stay paired with that text — this chunk must not leave a color-only hook behind.
- **a11y §Contrast (SC 1.4.3 / 1.4.11):** any existing token re-pointed at the new mode must still clear 4.5:1 text / 3:1 non-text against `--color-base` in both dark and light blocks (colorjs.io token-pair assertions, `.claude/rules/a11y.md`).
- **tests/E2E harness:** CLI status anchors (`[PASS]`/`[FAIL]`/`[BLOCKED]`…) are keyed by `testing.md` selectors — a new CLI mode spelling is a shared contract string, not a free-text label.
- **arch / serde wire:** the wire spelling (`auto` / `drive+observe` / `static-only` + the new one) is what the mono Data column renders verbatim; picking the wire string is therefore a design-visible decision, not purely a serialization one.

## Acceptance criteria contributions
- (design) The fourth mode introduces zero new hex / ANSI / palette entries; every color it touches resolves to an existing `:root` token or mapped ANSI code by name (no hardcoded hex, px, or ms).
- (design) No capability row renders a `Blocked` or `Fail` lamp — or `--count-blocked` / `--status-fail` — by virtue of being not-Conductor's; the lamp union is unchanged by this chunk.
- (design) `design-system.md:7` and `:257` no longer hardcode "60"; both read manifest-relative, matching the 2026-08-08 de-hardcoding precedent (and `layout-templates.md:121`'s contradiction with `:37` is resolved the same way).
- (design) The webview matrix stays a single-row-per-P-ID list with the Mode column in the `type-data` mono tier, and its summary tally sums across all four modes to the total row count (no mode silently uncounted).

## Relevant amendment history
- **2026-08-08-sut-capability-manifest** — de-hardcoded the P-ID range in token-usage examples (§Color Palette Primary, §Typography Data, §cli ANSI map, §Brand Identity) when the manifest superseded the fixed 60. This is the same sweep; it updated *usage examples* but left the two prose counts at `:7` and `:257`, which is precisely what this chunk finishes. Precedent: wording-only, no token/hex/type-role change.
- **2026-06-24-sanitized-stderr-agent-mode-logging** — the detector proposed a NEW palette row for a new surface edge; CORRECTED at validation to reuse the existing shipped token, applied as a use-site note rather than a duplicate color. Directly governs the fourth mode: if it ever needs a visual, reuse before you add.
- **2026-06-15-design-token-typography-bundle** — established the "spec-illustration → sound-impl reconciliation" routine (and that tokens live on plain `:root`, not `@theme`); applies if any token file is touched in the doc-reconciliation pass.
