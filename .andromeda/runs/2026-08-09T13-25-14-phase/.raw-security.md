# RAW — security distiller, ATTEMPT 1 (FAILED validation check 3 "Anchors")

Failure: every bullet under `## Acceptance criteria contributions` cited no `per security-plan.md §…`
anchor (Constraints were correctly anchored). Retried once with the check named; the retry passed and
is the extract of record in `security.md`. Preserved verbatim below per `fan-out.md`.

---

# security extract

## Relevance
**Partial** — no new external-input surface is *required*, but the P4 representation decision can move classification into the already-registered `contracts/pulse-capabilities.toml` boundary, and the new rows/titles flow into agent-parseable artifacts governed by sanitization rules.

## Constraints
1. **If P4 picks "manifest data"** (extend the TOML with per-capability title/category/mode), those fields are external input at an already-registered boundary and MUST be bounds-checked inside `CapabilityManifest::validate()` — an unknown mode spelling or missing/blank title is a `CoreError::Config` harness fault, never a serde default that silently classifies (per security-plan.md §Input Validation, capability-manifest row; amendment 2026-08-08).
2. **Manifest read/parse hygiene is preserved verbatim**: read faults carry `e.kind()` only (never the path), parse faults go through `sanitize_error`, and the path stays fixed via `resolve_under` with no `CONDUCTOR_*` override (per security-plan.md §Input Validation env-path row + §Error Handling).
3. **Row text is artifact-visible**: titles/categories sourced from Pulse's repo must be literal capability prose — no absolute host paths (`D:\dev\projects\andromeda-pulse\…`), no Pulse-internal struct/field names, no file locations — because coverage rows render into `<run_id>.md`, the CLI table and the Tauri view, which are ground truth shared across hosts (per security-plan.md §Error Handling, run-report artifact sanitization).
4. **P3's Pulse-repo research is read-only spec sourcing**: never open, copy, or transcribe `corpus.db` content into Conductor code, tests, or artifacts — corpus access is MCP read-back only and the corpus is SUT-owned (per security-plan.md §Data Protection).
5. **`KNOWN_UNCLASSIFIED → []` must not weaken the gate**: a manifest id with no classification must stay a hard `Err`, never a default variant or an "unclassified" pass — the never-silently-downgrade-a-failed-precondition rule (per security-plan.md §Security Anti-Patterns → Universal; §Input Validation "never a silent widening").
6. **No dependency delta is expected**; `cargo audit` green + `cargo deny check` ok + `Cargo.lock` un-drifted remain preconditions of the release gate for this chunk regardless (per security-plan.md §Dependency Security; §Anti-Patterns → Universal).
7. **Tauri surface stays as-is**: the new `CoverageMode` variant crosses the IPC boundary as serialized *output* only — do not add a new `#[tauri::command]` or widen the capabilities file to render it (per security-plan.md §Threat Model, Tauri IPC vector; §Anti-Patterns → Code Patterns, minimal deny-by-default capabilities).

## Patterns to follow
- `CapabilityManifest::load` + its private `validate()` in `D:\dev\projects\conductor\crates\conductor-core\src\capability_manifest.rs` — non-empty checks, `P-NNN` shape, duplicate rejection, `e.kind()`-only read error. Extend this one function for any new field; do not add a second parse path.
- `conductor_core::resolve_under` (`D:\dev\projects\conductor\crates\conductor-core\src\config_path.rs`) — the shipped traversal guard already applied to the fixed manifest path at the binary edge.
- Verdict/error wall (per security-plan.md §Error Handling): config/shape faults are `Result::Err(CoreError::Config)`; classification results stay typed `Ok` values — the new not-Conductor's variant is a value, never an error channel.
- `conductor_core::sanitize_error` (Display-not-Debug + host-path scrub) for anything a coverage-render failure surfaces to CLI stderr or a Tauri return.
- Exact-set, both-direction ledger equality (the pattern the `sut-drift-check` chunk established) — when `KNOWN_UNCLASSIFIED` empties, keep the assertion bidirectional rather than collapsing it to a weaker one-way check.

## Anti-patterns to avoid
- **No `#[serde(other)]` / `#[serde(default)]` fallback on the new mode's wire spelling** if it becomes deserialized input — an unrecognized value must fault, not silently classify (per security-plan.md §Anti-Patterns → Input: never deserialize config without validation).
- **No absolute host paths or internal seam-crate/Pulse struct names** in coverage rows, the reconciled docs, or the report/CLI/Tauri renders (per security-plan.md §Anti-Patterns → Logging).
- **No downgrade of a classification/drift mismatch** to pass, warning, or a default mode — it stays a hard fault with a named precondition (per security-plan.md §Anti-Patterns → Universal).

## Contract bindings
- **security ↔ tests**: the manifest-boundary validation and the "zero unclassified ids" invariant are enforced through the tests-domain gate set (`nextest` workspace + doctest + clippy `-D warnings` + audit + deny inside `scripts/agent-run.sh run`); the reworked `matrix_has_exactly_sixty_capabilities` / contiguity / `committed_artifacts_match_the_known_gap` tests are the assertion sites for security's malformed-input and no-silent-widening constraints.
- **security ↔ obs**: coverage row text lands in report/journal artifacts subject to `conductor-core::redact` (host-path scrub + field allowlist); new titles/categories must contain no path-shaped tokens so they pass through unmodified rather than being scrubbed to `<redacted>`.
- **security ↔ frontend/design**: the `ui/src/lamp.ts` edit is a frontend change — if the npm tree moves at all, `npm audit --omit=dev` clean (0 production vulns) + committed `package-lock.json` applies (per security-plan.md §Dependency Security, Frontend (npm) supply chain).

## Acceptance criteria contributions
- (security) `cargo audit` exits 0 and `cargo deny check advisories bans sources licenses` is ok, with `Cargo.lock` un-drifted (`git diff --exit-code Cargo.lock` clean).
- (security) If the manifest schema gains per-capability fields: a fixture with an unknown `mode` spelling and one with a missing/blank title each produce `CoreError::Config` — asserted by test, with no default-classification fallback.
- (security) No absolute host path (`D:\`, `/home/`, `/Users/`, `%APPDATA%`) and no Pulse-internal struct name appears in `crates/conductor-core/src/coverage.rs`, the manifest, or the three reconciled docs (grep-verified).
- (security) With `KNOWN_UNCLASSIFIED = []`, an injected unclassified id still makes `check_sut_drift` return `Err` — assertion strength preserved, not loosened to reach green.

## Relevant amendment history
- **2026-08-08-sut-capability-manifest** (§Input Validation · §Data Classification) — registered `contracts/pulse-capabilities.toml` as an external-input boundary validated at load (non-empty version/date/set, `P-NNN` shape, no duplicates; read faults carry `e.kind()` only; fixed path via `resolve_under`, no `CONDUCTOR_*` override), and dropped the `P-001..P-060` naming from the scenario-config volume. **Why it matters here:** this is the exact file the P4 representation decision may extend — any new per-capability field inherits this boundary's validation obligation, and option B ("classification as manifest data") widens a boundary the plan already enumerates.
- **2026-06-27-live-pulse-e2e-proof** (§Threat Model · §Data Protection · §Anti-Patterns) — corrected `corpus.db` to plaintext SQLite (P-049 encryption assumption wrong) while *sharpening* the posture: Conductor's code never directly opens/copies/exfiltrates it, and never persists its content into Conductor artifacts. **Why it matters here:** P3 sources capability titles/categories from `D:\dev\projects\andromeda-pulse\` — spec/doc reads only; the corpus stays out of scope.
- **2026-06-27-desktop-a11y-harness-setup** (§Dependency Security, frontend) — npm gate is `npm audit --omit=dev` clean (0 production vulns) with committed `package-lock.json`. **Why it matters here:** `ui/src/lamp.ts` is in scope; the gate binds if the frontend dep tree moves (no delta expected).
- **2026-06-15-dependency-audit-gate** (§Dependency Security · §Bootstrap phases) — audit-tool versions are minimum **floors**, not lockable pins (installed cargo-audit 0.22.1 / cargo-deny 0.19.4); any tool ≥ floor running green satisfies the gate. **Why it matters here:** sets the bar the chunk's audit/deny acceptance criterion is measured against; a version below the researched numbers is not a finding.
