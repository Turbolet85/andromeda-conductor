# tests extract

## Relevance
Relevant — the chunk adds three committed scenario fixtures + shrinks a gate-asserted pin, both of which test-plan.md governs directly (§7 fixtures, §6 coverage-gate scenario-backing leg).

## Constraints
- Scenario configs are **committed declarative serde+garde fixtures, one-per-P-ID under `scenarios/`**, exercised through the validating load path with table-driven `#[rstest]`/`#[case]` rows — never hand-built structs (per test-plan.md §7 Test Data & Fixtures / "Fixture files (scenario config)"; §3 Test data bootstrap; §4 Fixture pattern at unit level).
- The **scenario-backing leg of the coverage gate** asserts `UNBACKED_AUTO` by exact-set equality **in both directions**, `Auto` only, unit-tier and CI-runnable: naming P-079 without shrinking the pin fails on pin-rot, shrinking without naming fails on unbacked-claim (per test-plan.md §6 Scenario: Coverage-matrix completeness gate → Scenario-backing leg).
- A `DriveObserve` scenario is asserted by the **absence** of `[[expected]]` routing to ManualCheck; ManualCheck/Blocked are reported envelope states, never non-zero exits (per test-plan.md §1 status fields + §11 Test Anti-Patterns → E2E, stack-specific).
- Project scope law is a **static assertion**: every catalog entry carries a P-ID, and each P-ID must be a member of the committed capability manifest's accepted set (per test-plan.md §4 "Scenario catalog + coverage-matrix" bullet; §11 Universal, project-specific).
- New tests land as crate-local `#[cfg(test)]` modules in the owning seam and must pass `cargo nextest run -p conductor-core` plus the workspace `--profile ci` leg; machine-parseable output, **zero retries** (per test-plan.md §2 conventions + §4 Framework + §10 Zero-flakiness budget).
- Binding CI gates unchanged by this chunk: line coverage ≥ 60% (`--fail-under-lines 60`), clippy `-D warnings`, and `cargo audit --deny warnings` + `cargo deny check` green (per test-plan.md §10 Build failure conditions; §11 CI stack-specific — matches the chunk's PREREQ bounded-wait posture).
- Determinism discipline applies to the fixtures' `seed`/`gap_ms`/`jitter_ms`: committed timing goldens are **seed-named**, so a fixture seed collision/edit can move a golden that a scenario-name grep misses (per test-plan.md §7 Golden artifacts + §11 Test Data).

## Patterns to follow
- **Family fixture-loader tests** live in `D:\dev\projects\conductor\crates\conductor-core\src\scenario.rs` `#[cfg(test)] mod tests` — `#[rstest]` `#[case]` over stems, path built as `format!("{}/../../scenarios/{stem}.toml", env!("CARGO_MANIFEST_DIR"))`, asserting p_ids/seed/slo_tier/phases; each family also carries a **suite-shape guard** (e.g. `constellation_context_grounding_suite_has_operator_checklist_members` at ~line 825, and `constellation_and_p032_are_operator_checklist_declare_only` at ~line 793 which asserts `expected.is_empty()`).
- **The shipped DriveObserve precedent** is `D:\dev\projects\conductor\scenarios\service-constellation-discovery.toml` — header comment naming the P-ID + mode + declare-only deferrals, phases with `gap_ms`, and a closing comment where the `[[expected]]` block would be; mirror it for P-067/P-072.
- **The live gate test** is `the_committed_catalog_matches_the_unbacked_ledger` in `D:\dev\projects\conductor\crates\conductor-core\src\drift.rs` (~line 303) — it loads the committed manifest + catalog and calls `check_scenario_backing(coverage_matrix(), &named, UNBACKED_AUTO)`; the three synthetic negative cases (~lines 318–345) cover unbacked / pin-rot / reclassified and need no change.
- **Roll-up assertions are already derived, not literal**: `crates\conductor-report\src\coverage.rs:192` and `crates\conductor-cli\src\render.rs:210` compute the qualifier from `UNBACKED_AUTO.len()`, and `crates\conductor-tauri\src\commands.rs` `unbacked_auto_command_returns_the_core_ledger` (~line 349) asserts IPC equality with `conductor_core::UNBACKED_AUTO` — the mock-runtime read-only-command tier per test-plan.md §5. Keep the single-source derivation; do not introduce a literal `10`. (Note `coverage.rs:140` is a synthetic 4-row fixture with its own numbers — unrelated to the real pin.)
- **Catalog visibility** is asserted in `D:\dev\projects\conductor\crates\conductor-core\src\scenario_catalog.rs` `mod tests` (`lists_the_committed_catalog_with_identity`) via manifest-validated `list_scenarios` with non-hardcoded counts — extend in that idiom if the chunk adds a presence check for the three new stems.

## Anti-patterns to avoid
- NEVER add UI automation or visual-pixel assertions for the P-067/P-072 DriveObserve claims — the visual go/no-go is a flagged-untestable operator step with no invented driver (per test-plan.md §1 Untestable zones + §11 Universal, "no Percy/Chromatic, no human screenshot review").
- NEVER assert Pulse's *reaction* as a CI verdict for the P-079 `[[expected]]` block, and never run a live-Pulse leg as a CI gate — this chunk authors the catalog; read-back proof is local/operator-gated (per test-plan.md §11 Test Strategy + CI, stack-specific).
- NEVER weaken the backing gate to absorb the change (range expressions, one-directional subset checks, or relaxing to a warning) — the pin shrinks under compulsion only (per test-plan.md §10 Quality, "never lower the threshold to pass"; §6 scenario-backing leg).

## Contract bindings
- **tests ↔ arch/core data**: test-plan.md §6 owns the scenario-backing gate whose *data* is `conductor_core::UNBACKED_AUTO` (`crates\conductor-core\src\drift.rs`) — the pin edit and the gate assertion must land together in one commit.
- **tests ↔ obs**: the `(N unbacked)` term is a **derived qualifier, not a fifth summand** (obs-plan §4); the roll-up tests test-plan.md §5/§6 own must keep asserting that the four per-mode counts still sum to the row total while the qualifier tracks `UNBACKED_AUTO.len()`.
- **tests ↔ security**: the three new TOMLs must be exercised through the garde-validating load path, not raw deserialization (test-plan.md §1 coverage trigger for security Vector 2 / "Deserializing scenario config without garde validation at load").
- **tests ↔ supply chain**: the chunk's PREREQ (`cargo audit` bounded wait, `cargo deny check` verified green) is the §10 build-failure condition / §11 CI ban — verify, do not amend CI or add ignores.

## Acceptance criteria contributions
- (tests) `cargo nextest run -p conductor-core` passes with new `#[rstest]` cases: all three stems load through the validating path, each names its P-ID, and each P-ID is accepted by `contracts/pulse-capabilities.toml` (per §7 / §4).
- (tests) Mode-shape assertions hold: the two `DriveObserve` stems assert `expected.is_empty()` (ManualCheck/operator-checklist path) and the `Auto` stem asserts a non-empty `[[expected]]` with its declared class; the family guard asserts the mixed `any(empty) && any(non-empty)` shape (per §6 / §11 E2E).
- (tests) `the_committed_catalog_matches_the_unbacked_ledger` is green with `UNBACKED_AUTO` at 10 entries — exact-set equality in both directions, with `P-073`/`P-074` and the four interpretation-cluster ids untouched (per §6 scenario-backing leg).
- (tests) Full gate green with no threshold change: `cargo nextest run --workspace --profile ci`, clippy `-D warnings`, `cargo llvm-cov nextest --fail-under-lines 60`, zero nextest retries, and the three roll-up surfaces' unbacked assertions still derived from `UNBACKED_AUTO.len()` (per §9 / §10 / §5).

## Relevant amendment history
- **2026-08-09-interpretation-correctness-posture** (§6 + §5) — added the scenario-backing leg to the coverage gate (exact-set equality both directions over `UNBACKED_AUTO`, `Auto` only, unit-tier CI-runnable) *and* recorded that the `tauri::test` mock-runtime command tier is live for read-only commands including `unbacked_auto`. This is the exact gate this chunk trips and the exact IPC surface whose count shifts; self-raised, not detector-driven.
- **2026-08-09-current-sut-coverage-classification** (§1 Critical paths / Path 6 + Scenario-catalog entity Source) — de-hardcoded the completeness gate to a manifest-relative set-equality assertion (no literal count, no P-ID range) and widened the mode list to four with `not-Conductor's`. Why it matters here: the chunk's new P-IDs are all above P-060, so any assertion added must be manifest-derived; the `coverage-matrix.md` artifact reference was deliberately left un-amended and routed as a CARRY to the Epoch-6 coverage-completeness-gate chunk.
- **2026-08-08-sut-capability-manifest** (§1 / §4 / §6 / §7) — catalog and coverage assertions re-sourced from the SUT capability manifest instead of a hard-coded 60, with membership-rejection coverage added. This is why P-067/P-072/P-079 validate cleanly as manifest members (`sut_version = "v0.3.0"`) rather than failing a stale range check.
