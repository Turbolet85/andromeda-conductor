RAW return — arch doc-agent (stripped to YAML for parsing; trailing note preserved here).

```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: Standard Contracts — "Readiness gate (MCP `initialize` preflight + data-dir canary)" paragraph
    change: State the readiness gate's named-precondition set as four members, not three — protocol version-mismatch · required-tool absence · empty canary corpus · **app/sidecar workspace-key agreement** — the fourth emitted as a distinct host-path-free `blocked_precondition` string (no absolute host path; `data_dir` redacted) that replaces the opaque corpus-empty string, inside an unchanged readiness shape returned as `Ok(Blocked)`.
    sidecar: arch §Standard Contracts — readiness-gate named-precondition set 3 → 4 (+ app/sidecar workspace-key agreement, host-path-free/redacted, replacing the opaque corpus string); per chunk 2026-08-10-workspace-key-divergence-probe.
    rationale: The report's Changes → "Counts / qualifiers moved" records the 3 → 4 move and "Spec-master edits" names the owned amendment; "Coverage of new surfaces" registers `blocked_precondition` as the operator-facing string, PII-redacted. arch §Standard Contracts still described only the three-check gate.
```

Trailing note (agent, not part of the payload): D-arch-decisions did NOT fire — the report's Dependencies
bullet is "none — zero delta"; no new crate/module/runtime; no §Stack or §Established Decisions entry
contradicted (`ReportState` still five; the `andromeda-pulse-mcp` sidecar build is SUT-side and already
registered in §Occupied Resources).
