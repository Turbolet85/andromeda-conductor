RAW return — security-plan doc-agent (stripped to YAML for parsing).

```yaml
proposals:
  - detector: D-security-subprocess
    severity: escalate
    section: "Security Anti-Patterns → Universal (the \"NEVER silently downgrade a failed preflight\" bullet)"
    change: "Restate the preflight cause list as four named preconditions — version mismatch / missing tool / canary-fingerprint absent / app-sidecar workspace-key agreement — each of which MUST surface as the distinct `blocked` state carrying its own named precondition string (the empty-corpus branch now names the workspace-key precondition, never the generic corpus string and never a host-path-bearing one), instead of the current three-plus-keychain enumeration."
    sidecar: "Security Anti-Patterns §Universal: preflight never-silently-downgrade ban updated 3 → 4 named preconditions (adds app/sidecar workspace-key agreement; empty-corpus branch no longer maps to the generic corpus string) per 2026-08-10-workspace-key-divergence-probe."
    rationale: "Report §Changes → Counts/qualifiers moved records the 3 → 4 move, implemented via NotFound{EmptyCorpus,FingerprintAbsent} + WORKSPACE_KEY_PRECONDITION/CANARY_FINGERPRINT_PRECONDITION, with §Outcome confirming the distinct named precondition returned as Ok(Blocked) and carrying no absolute host path. The doc's Universal ban still enumerated only three live causes plus the already-inapplicable keychain read-while-write mode. Spawn mechanics (fixed path, .env(...), no argv/shell interpolation, 2024-11-05 negotiate-down) untouched by this chunk and still accurate."
```

Orchestrator note: proposal severity `escalate` came from the DETECTOR's severity field; the playbook match
(rule 28 — spec wording → shipped impl, invariant preserved) resolved it **routine**, so no HALT was raised.
The agent's observation that the enumeration also carries a stale keychain cause was adopted as a cascade
step-2 fix (the bullet verbatim-cites the amended arch passage).
