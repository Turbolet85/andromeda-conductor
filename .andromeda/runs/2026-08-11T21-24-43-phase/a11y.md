# a11y extract

## No domain coverage

Chunk is entirely backend Rust (`conductor-run` dispatcher, `conductor-emit` primitives, `conductor-core::phase_spec`, `scenarios/*.toml`, obs `timeline.execute`/`emit.batch` spans) with no rendered surface — the only assertable a11y surface is the Tauri desktop-webview (cli/headless is not-assertable per a11y-plan §1), and the one cross-domain hook that would be mine (the violation-JSON reproduction of the obs §6 run-report envelope, per a11y-plan §3) is untouched here since the chunk changes span attributes, not that envelope's fields.
