# a11y extract

## No domain coverage
This chunk is Rust unit/golden test surface only (`crates/conductor-timeline/tests/replay.rs` + `pacing.rs` emission-stream goldens, `crates/conductor-core/src/load_envelope.rs` re-scope, `contracts/pulse-load-envelope.toml`, `cargo audit` re-check) with no rendered desktop-webview surface — a11y-plan §9 CI Integration explicitly maps the Unit stage (cargo-nextest / cargo test / golden) to "no UI a11y tests at unit layer, n/a", and §1 marks the cli/headless path not-assertable.
