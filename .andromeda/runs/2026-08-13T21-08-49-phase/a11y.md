# a11y extract

## No domain coverage

This chunk touches only the not-assertable `conductor-cli`/Rust backend surface (`preflight.rs`, `client.rs`, `spawn.rs`, `extract.rs`, run-contract TOML, evidence markdown, `verification-matrix.json`, `scripts/agent-run` `boot`) with zero desktop-webview DOM, ARIA, focus, keyboard, contrast, or motion work — the cli surface is explicitly excluded from a11y coverage per a11y-plan.md §1 (Assertability: not-assertable, "Excluded from Sections 2 and 4") and §11 Universal ("NEVER treat the cli surface as accessibility-assertable"), and the chunk's "Not a CI gate" boundary already matches a11y's operator/local-gated posture per §9, so no constraint, pattern, or acceptance criterion from this plan applies.
