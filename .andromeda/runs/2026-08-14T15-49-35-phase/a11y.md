# a11y extract

## Relevance
Partial (narrow) — this chunk adds no UI surface, component, keybinding, rendered state, motion, or form input; only the a11y↔obs envelope reproduction, artifact-hygiene, and not-assertable-CLI rules reach it.

## Constraints
- The capture path here is the CLI / headless `agent-run` + OTLP egress surface, which is flagged **not-assertable** (no DOM / widget tree, no axe / Lighthouse / pa11y reach) — no a11y assertion, ARIA claim, or WCAG coverage may be attached to it (per a11y-plan §1 A11y Scope Summary, `conductor-cli` entity).
- The capture artifact must not be presented as WCAG evidence: no conformance claim without machine-verifiable axe-core / Lighthouse / colorjs.io JSON (per a11y-plan §11 Strategy).
- If this chunk changes the shape or semantics of the obs §6 envelope's `fingerprints[]` array (its subject is exactly the fingerprint feed that populates it), a11y's verbatim envelope reproduction **and** the axe fold-in mapping (axe rule-id + WCAG-SC tag + node selector tuples ride inside `fingerprints[]`, no new top-level fields) must be amended in lockstep (per a11y-plan §3 Structured violation JSON schema).
- Any artifact riding the obs §6 envelope — including this chunk's capture — must not leak absolute host paths or internal seam-crate struct names (constraint inherited from tests' Status JSON shape rule) (per a11y-plan §11 SLO, reinforced in §9 CI-specific resource attributes).
- Resource-attribute identity on shared-envelope artifacts stays `service.name` = `"conductor-tauri"` / `"conductor-ui"` and `deployment.environment` = `CONDUCTOR_ENV` (fallback `"local"`); a11y cross-correlates on those names and they must not be renamed by capture work (per a11y-plan §2 Naming conventions, §9 CI-specific resource attributes).
- This chunk must not add an a11y step to the GitHub Actions CI gate — the webview a11y gate is deliberately operator/local-gated on the Linux+xvfb `wdio run` job, not a CI gate (per a11y-plan §9 CI Integration).

## Patterns to follow
- **Fold extra dimensions into `fingerprints[]`, never add top-level envelope fields** — the established precedent for encoding multi-part detail (rule-id + tag + selector tuple) inside the fixed obs envelope (per a11y-plan §3 Structured violation JSON schema).
- **Obs fixes the schema; a11y narrows and reproduces verbatim** — any envelope change originates in obs and is mirrored into a11y §3, not invented on the a11y side (per a11y-plan §3 Structured violation JSON schema; precedent in the 2026-06-16 amendment below).
- **Not-assertable-surface output discipline** — if the capture emits CLI output, ASCII status prefixes / `NO_COLOR` text-pairing are output-stream discipline only, explicitly *not* an a11y verification path (per a11y-plan §1 `conductor-cli` entity, §11 Universal).
- **Live-Pulse-dependent proof stays operator/local and emits through tests' `logs` command** — the same placement rule a11y's own gate follows for live-dependent verification (per a11y-plan §3 CI integration, §9 Pipeline integration).

## Anti-patterns to avoid
- NEVER treat the cli / headless surface as accessibility-assertable (per a11y-plan §11 Universal).
- NEVER let an artifact on the obs §6 envelope leak absolute host paths or internal seam-crate struct names (per a11y-plan §11 SLO).
- NEVER claim WCAG conformance without machine-verifiable JSON evidence — a diagnostic capture is not a11y evidence (per a11y-plan §11 Strategy).

## Contract bindings
- **a11y ↔ obs §6 Log Format** — `fingerprints[]` is shared surface: domain fingerprints (this chunk's subject) and a11y violation tuples occupy the same array in the same envelope; obs owns the schema, a11y reproduces it verbatim (a11y-plan §3 Structured violation JSON schema).
- **a11y ↔ obs §3 Service Identity** — `service.name` / `deployment.environment` resource attributes tag a11y artifacts for cross-correlation with journal rows (a11y-plan §2 Naming conventions, §9).
- **a11y ↔ tests harness** — artifact-hygiene (Status JSON constraint) inherited from tests; a11y results surface through tests' 5-command `logs` command on the shared `wdio run` webview job (a11y-plan §3 CI integration, §9).

## Acceptance criteria contributions
- (a11y) No WCAG/a11y assertion or conformance claim is attached to the CLI/headless capture path or its artifact (per a11y-plan §1 A11y Scope Summary — `conductor-cli` not-assertable / §11 Universal).
- (a11y) The capture artifact and any journal rows it produces contain no absolute host paths and no internal seam-crate struct names (per a11y-plan §11 SLO).
- (a11y) If the obs §6 `fingerprints[]` field's shape or semantics change, a11y-plan §3's verbatim envelope reproduction and axe fold-in mapping are amended in the same chunk (per a11y-plan §3 Structured violation JSON schema).
- (a11y) No a11y step is added to the GitHub Actions CI gate by this chunk; the webview a11y gate remains operator/local on the Linux+xvfb `wdio run` job (per a11y-plan §9 CI Integration).

## Relevant amendment history
- **2026-06-16-emission-journal-writer** — §3 Structured violation JSON schema: a11y's verbatim reproduction of the obs Run-report envelope gained `read_back_observed_at` when obs added it. *Why it matters here:* it is the working precedent for this chunk's one live binding — a11y's envelope reproduction tracks obs field-for-field under the a11y↔obs schema bind (D-a11y-obs-schema), so a fingerprint-feed change to that envelope pulls an a11y §3 amendment with it. (The 2026-06-21 lamp-precedence and 2026-08-09 out-of-scope-Mode-cell amendments are webview-lamp/visual-token scope and do not touch this chunk's area.)
