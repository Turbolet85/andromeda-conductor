Raw return — arch drift-detector (stripped of the enclosing ```yaml fence only).

proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources — contracts/pulse-run-contract.toml bullet (line 161, the warm-up / bootstrap paragraph)"
    change: "Scope the BootstrapState::Ready claim to the baseline-derived cue families only — crates/triage/src/cue/evaluate.rs:164 is the ONLY such gate in crates/triage/ and sits inside evaluate_service_went_silent, while the RetryStorm path the canary rides consults no baseline (crates/triage/src/pattern/storm.rs:245-285) — so 'without a baseline the L2 cue evaluator never considers it' holds for service-went-silent, and the warm-up's measured-false purpose is moot for the storm path rather than fatal to it."
    sidecar: "2026-08-15 — arch: the BootstrapState::Ready / no-baseline diagnosis rescoped to the baseline-derived cue families; the storm path the canary rides needs no baseline."
    rationale: "Report Changes §Spec claims disproved by measurement #1 (architecture.md:161): the storm path consults no baseline, Pulse measured 2 incidents with baseline_state at 0 rows, and this leg corroborates the scoping — triage.baseline.service_went_silent.evaluate fired 203 times while no storm cue fired at all."

  - detector: D-arch-resources
    severity: escalate
    section: "Occupied Resources — contracts/pulse-run-contract.toml bullet (line 161, the 'Reaching a live incident therefore requires a Pulse-side change' sentence)"
    change: "Retire 'Reaching a live incident therefore requires a Pulse-side change (a test-mode bootstrap override, or populating the already-present baseline_state table…), not a Conductor-side retune' and state the measured tier-band story instead: no such Pulse-side change exists, baseline_state still holds 0 rows, and Pulse forms incidents for a sustained storm anyway — what gates the canary is count >= autonomous_threshold (crates/triage/src/pattern/storm.rs:245) with DEFAULT_AUTONOMOUS_THRESHOLD = 10, Tier-1 Autonomous-only (crates/triage/src/cadence/coordinator.rs:390), so the retired 6-occurrence canary sat in 5 <= 6 < 10 — cue floor cleared, Autonomous band missed."
    sidecar: "2026-08-15 — arch: the Pulse-side-change-required blocker is measured false; incident formation relocated onto the tier band (5 <= 6 < DEFAULT_AUTONOMOUS_THRESHOLD = 10)."
    rationale: "Report Changes §Spec claims disproved by measurement #2 — 'measured false as stated'. Escalated over the detector's default warning because the retired sentence is a live operator directive: it sends the next operator to patch Pulse's baseline_state for a blocker that measurement shows is not the blocker."

  - detector: D-arch-resources
    severity: warning
    section: "Established Decisions — [Read-Back Dependency Posture] (line 60, the OTLP -> L1 -> L2 -> L3 -> L4 write-path sentence)"
    change: "The write-path's 'L2 RetryStorm cue (deterministic, >=5 same fingerprint / 30s)' must carry the tier band too — >=5 in 30s raises the cue and increments storms_detected_total on either branch, but only count >= DEFAULT_AUTONOMOUS_THRESHOLD = 10 reaches the Tier-1 Autonomous branch an incident-forming canary needs; severity_hint on triage.pattern.storm.detected is what names the tier."
    sidecar: "2026-08-15 — arch: [Read-Back Dependency Posture] write-path amended so the >=5 cue floor no longer reads as the whole storm-to-incident gate."
    rationale: "Second site restating the claim proposal #2 retires — that clearing the >=5 cue floor puts a storm on the incident path. The report names exactly this shape as the staleness mechanism it is fixing in code ('naming the cue floor and reasoning no further'); a §Occupied Resources-only apply leaves the same reasoning standing in §Established Decisions."
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources — contracts/pulse-run-contract.toml bullet (line 161, the 'SECOND, independent Pulse-side gap' passage)"
    change: "The second gap is NOT superseded — keep it and sharpen it from 'a region, not a named defect' to producer-dependent and localized between OTLP ingest receipt and the buffer's span-event enumeration: this leg's buffer.tick trio (span_events_seen 0, observer_invocations 0, fingerprints_computed 0, rows_ingested 1 against span_count 15, identical across 15 ticks) proves the fingerprint observer was never invoked rather than invoked-and-empty, and inject_demo's spans traverse the same path and fingerprint (c33df842) while Conductor's canary spans do not."
    sidecar: "2026-08-15 — arch: second Pulse-side gap un-retired and sharpened to producer-dependent, localized between OTLP ingest receipt and buffer span-event enumeration."
    rationale: "Report Changes §Spec claims disproved by measurement #3 plus §Outcome (the discriminating trio): the plan predicted this claim would be retired by the healthy-feed proof; the live leg is the evidence it LIVES and is the remaining preflight-green blocker."

  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources — contracts/pulse-run-contract.toml bullet (line 161, the 'Read the storm-detector telemetry accordingly' reading instruction)"
    change: "Restate the reading instruction at the shipped canary size — CANARY_STORM_COUNT is now 12, so a working identical-fingerprint storm reads tracked_fingerprints_count = 1, never 12 (a late sample still reads 0 on a healthy path) — and keep 'all nine spans / 1 -> 2 -> 3 -> 9' as the dated 2026-08-14 figure, adding this leg's span_count 15 (3 warm-up + 12 storm) as the current one."
    sidecar: "2026-08-15 — arch: storm-detector reading instruction refreshed from the retired six-occurrence canary to the shipped twelve; span_count 15 recorded beside the dated nine."
    rationale: "Report Changes §Symbols / APIs — conductor_run::CANARY_STORM_COUNT value 6 -> 12 (the chunk's only source edit) — and §Outcome's measured span_count 15. The present-tense 'a working six-occurrence identical-fingerprint storm reads 1, never 6' describes the canary at a size that no longer ships."

  - detector: D-arch-decisions
    severity: warning
    section: "Established Decisions (new scoped note beside [Accepted Capability Set]; cross-referencing the contracts/pulse-run-contract.toml bullet in §Occupied Resources)"
    change: "Record the P4 operator decision that SUT-derived canary SIZING stays a compile-time constant (conductor_run::CANARY_STORM_COUNT) whose doc comment cites the SUT symbols and states the relation, deliberately NOT a pulse-run-contract.toml term plus a RunContract field with bounds-check and tests — a stated, scoped exception to arch's SUT-facts-are-contract-data pattern, with its cost recorded (the next Pulse threshold move fails silently rather than loudly)."
    sidecar: "2026-08-15 — arch: SUT-facts-as-contract-data pattern gains a recorded exception — canary storm sizing stays a cited compile-time constant (P4 operator decision, scope-bounded)."
    rationale: "Report Changes §Schema / config — 'Notably no contract-manifest term was added — recording Pulse's threshold as contract data was considered and REJECTED at P4' — with the report itself conceding 'It is arch's own pattern for SUT facts'. The shipped code now holds a SUT-derived threshold-clearing value code-side while arch states the pattern unqualified; the deviation is operator-sanctioned, so the fix is recording it, not reverting it."
