# a11y extract

## No domain coverage

Chunk is confined to OTLP span construction / emission (`crates/conductor-emit`, `crates/conductor-run`) and Pulse's buffer enumeration — a headless wire path with no DOM or widget tree, and a11y-plan §1 flags every non-webview surface not-assertable while the only a11y↔obs binding (violation JSON folded into the §6 envelope's `fingerprints[]`, per a11y-plan §3 Structured violation JSON schema) is untouched here since this chunk's "fingerprints" are Pulse-side triage fingerprints, not envelope-schema changes.
