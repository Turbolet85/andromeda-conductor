# arch extract

## Relevance
Relevant — arch owns the crate seam the fix lands in, the OTLP wire vocabulary both leads turn on, the readiness-gate contract `v2-10` is claimed against, and the §Occupied Resources paragraph this chunk is expected to amend at wrap.

## Constraints
- **OTLP raw-type construction belongs to the `conductor-emit` seam.** arch §Infrastructure Patterns (directory tree) assigns `conductor-emit` "OTLP raw-type emission primitives + exception events / fingerprint primitive", and `conductor-run` is the composition root *above* the seams (preflight + execute_scenario + persist + drive_run). If research confirms scope LEAD A (the canary composing its own minimal spans in `conductor-run`), arch §Established Decisions [Module Boundaries] requires the correction land as, or behind, an emit-owned builder — not as inline span construction in the composition root. Whether the canary path already routes through emit's builders is research's question.
- **Raw `opentelemetry-proto` 0.32.0 structs only.** arch §Established Decisions [OTLP Emission Strategy] locks hand-built `ExportTraceServiceRequest`/`ResourceSpans`/`Span`/`Status` and explicitly rejects the `opentelemetry-otlp` SDK exporter (no fingerprint-identity or error-placement control, spawns its own batch tasks). A "just use the SDK to get well-formed spans" remedy is barred.
- **OTel Semantic Conventions + W3C Trace Context are the mandated wire vocabulary.** arch §Conventions (Interface surfaces — Outbound emission) requires `service.name`, span `Status.Code=ERROR`, exception span events carrying `exception.type`/`.message`/`.stacktrace`, and W3C Trace Context propagation across emulated service edges. W3C-conformant ids are 16-byte trace / 8-byte span, so non-empty ids are a *contract requirement* of this section, not merely what Pulse's guard happens to want — which is why LEAD A, if measured true, is a Conductor-side conformance defect. Whether the canary emit path already satisfies it is research's question.
- **Egress shape is pinned.** arch §Occupied Resources (Ports) + §Inherited Defaults (Interface style) fix emission at OTLP/gRPC to `127.0.0.1:4317` via tonic 0.14.6; `:4318` (OTLP/HTTP) is named as not used. Changing transport, batching endpoint, or protocol to work around the enumeration gap is outside the locked interface.
- **Verdict/error wall + `blocked` with a named precondition.** arch §Cross-cutting Patterns (Verdict/error wall) and §Standard Contracts (Readiness gate) require that a gate that cannot reach `ready:true` returns `ready:false` naming one of the FIVE preconditions, host-path-free (`data_dir` redacted), with dependent auto scenarios reported `blocked` — never `Result::Err`, never silently downgraded to pass/fail/manual-check. This is the arch-side shape of the scope's honest-exit rule (recorded measurement + un-claimed `v2-10`).
- **Non-determinism has one owner.** arch §Established Decisions [Determinism RNG] + §Cross-cutting Patterns (Determinism discipline) make the seeded `ChaCha8Rng` the timeline's sole non-determinism source, with journal stamps from `std::time`. If the correction introduces id generation on the canary path, it must not become a second unseeded influence on emission-stream shape.
- **Conductor drives the SUT; it does not manage it.** arch §Cross-cutting Patterns (Scope law) + §Project Intent name "no Pulse process management" among the non-goals, and §Cross-cutting (Trust boundary) casts Conductor as a gRPC/MCP *client* — the arch-side basis for the scope's Pulse-repo-read-only boundary.

## Patterns to follow
- **Builder-in-emit precedent.** The exception-event builder + `fingerprint()` primitive were placed in `conductor-emit` co-located with the content they derive from (arch §Infrastructure Patterns crate comment; user-ratified — see amendment 2026-06-18). The `topology.rs` / `span_tree.rs` id generators the scope names sit under the same seam rule.
- **Composition-root sharing.** arch §Occupied Resources (Crate names) records `conductor-run` as the run composition root shared by `conductor-cli` and `conductor-tauri` — a canary emit-path correction must be crate-level, not bin-local, or the GUI path diverges from the headless release gate (§Design Philosophy, headless-drivable core / thin shells).
- **Transcribed-SUT-record discipline.** arch §Occupied Resources (`contracts/pulse-run-contract.toml`) requires Pulse-side facts to enter Conductor as provenance-stamped records with a declared check kind — `shell-declaration` (blocking) / `asserted` / `declared-not-observable` — never as a Conductor measurement. The operator-verified Pulse coordinates in the scope carry this status; a term Conductor cannot observe must not become a blocking assertion.
- **Named-cause composition, not claimed measurement.** arch §Standard Contracts requires each precondition string to name its condition *and* its candidate causes where read-back cannot discriminate them (the zero-incident / workspace-key pair is the worked example). Any new gate wording from this chunk follows that form.

## Anti-patterns to avoid
- **Do not adopt an OTel SDK exporter** (or any OTel SDK) to normalize the canary's spans — arch §Established Decisions [OTLP Emission Strategy] and the §Stack self-observation row (tracing is *not* an OTel SDK; OTLP is the PRODUCT emission) both forbid it.
- **Do not inline OTLP raw-type construction into `conductor-run`** to route around the emit seam (arch §Established Decisions [Module Boundaries] — the seam is compiler-enforced and a bypass silently re-creates the shape divergence in a second place).
- **Do not introduce a new configuration handle** (env var, override path, canary-shape toggle) for this fix — arch §Occupied Resources reserves `CONDUCTOR_*` for concepts the document already defines, and the contracts manifests deliberately carry *no* `CONDUCTOR_*` override handle.

## Contract bindings
- **arch ↔ tests:** `crates/conductor-run/tests/canary_wire.rs` runs under the pinned cargo-nextest zero-retry `ci` profile (arch §Infrastructure Patterns — Build system; "no retries mask flakes"). If the wire assertion must widen to catch a shape divergence Conductor's own collector accepts (scope LEAD B), the assertion grain is tests-plan's call; arch fixes only the runner and the no-retry rule.
- **arch ↔ obs:** the live leg's capture surface is the self-obs `tracing` JSON stream registered as `logs/agent-latest.jsonl` (arch §Occupied Resources — On-disk artifacts; obs-plan §3 owns field/redaction detail). Pulse's `buffer.tick` / ingest counters are SUT telemetry read as ground truth, not a Conductor obs surface.
- **arch ↔ security:** the carried `cargo audit` re-check (20th pin) sits against arch §Infrastructure Patterns' deny.toml justification set, but the authority for the advisory/exception ledger is security-plan §Dependency Security — arch only requires audit+deny stay green.
- **arch ↔ wrap:** the scope names `.andromeda/architecture.md` §Occupied Resources (`contracts/pulse-run-contract.toml` bullet) as carrying the "second gap" paragraph, with an expected amendment once the cause is measured. That bullet's second-gap text is target-state doc content, not a claim about code.

## Acceptance criteria contributions
- (arch) The correction lands in `crates/conductor-emit` and/or `crates/conductor-run` with no file modified under the Pulse repo (per arch §Established Decisions [Module Boundaries] · §Cross-cutting Patterns Scope law).
- (arch) Any new or changed OTLP span/id/event construction is exposed by `conductor-emit`, not inlined in the `conductor-run` composition root (per arch §Infrastructure Patterns directory-tree crate comments).
- (arch) No new occupied resource: no env var beyond the registered `CONDUCTOR_*`/`ANDROMEDA_PULSE_*` set, no port beyond the `127.0.0.1:4317` OTLP/gRPC egress, no new workspace crate (per arch §Occupied Resources).
- (arch) A leg that does not reach `ready:true` returns `ready:false` with one of the FIVE named, host-path-free preconditions and `blocked` dependent rows — never a harness `Result::Err`, never a downgrade to pass/fail/manual-check (per arch §Standard Contracts Readiness gate · §Cross-cutting Patterns Verdict/error wall).

## Relevant amendment history
- **2026-08-15-canary-storm-autonomous-band** — un-retired and *sharpened* the second gap in §Occupied Resources (`pulse-run-contract.toml`) from "a region" to producer-dependent and localized between OTLP ingest receipt and buffer span-event enumeration, on the `buffer.tick` trio; corrected the `BootstrapState::Ready` misattribution (it scopes to baseline-derived cue families, not the RetryStorm path) and raised `CANARY_STORM_COUNT` to 12. This is the immediate predecessor state of the paragraph this chunk is expected to amend.
- **2026-08-14-canary-fingerprint-feed-capture** — first recorded the second Pulse-side gap beside the warm-up falsification, plus the telemetry-reading rules (`tracked_fingerprints_count` as a 60s-windowed DISTINCT gauge; the window-immune cumulative discriminators). Same section; the scope's standing telemetry rules descend from it.
- **2026-08-13-first-live-green-preflight** — recorded `[incident_formation]`'s warm-up purpose as measured FALSE, and why a term with `check = "asserted"` cannot catch its own falsity. Same bullet, and the cautionary precedent for this chunk's "never claim on argument" rule.
- **2026-06-18-exception-events-fingerprint-control** — placed the exception-event builder + `fingerprint()` primitive in `conductor-emit` (user-ratified crate seam; `conductor-faults` narrowed to the storm fault). This is the standing seam ruling for where a canary span/id/event builder correction belongs.
- **2026-06-27-live-pulse-e2e-proof** — reversed the canary premise to a unique fingerprint-storm with `retrieve_telemetry_slice.fingerprint_refs` as the fidelity carrier, and established the escalate-once-then-apply pattern for a live SUT contradicting a locked assumption. Defines what the canary is emitting *for*.
- **2026-08-10-workspace-key-divergence-probe / 2026-08-10-pulse-run-contract** — grew the readiness gate's named preconditions to four then five, fixed the ordering (contract-term arm before the canary arms, skipping rather than paying the poll), and raised `CONDUCTOR_PREFLIGHT_TIMEOUT`'s effective floor from `[incident_formation].min_canary_poll_seconds`. Governs how this chunk's re-run leg reports and how long it may poll.
