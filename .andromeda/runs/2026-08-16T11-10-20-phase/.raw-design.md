# design extract
[RAW — first return, FAILED validation check 2 (Sections): `## Relevance` emitted together with `## No domain coverage`, which must stand alone. Retried once; the retry passed and is saved as design.md.]

## Relevance
Out-of-scope — this chunk is headless Rust `tracing` self-observation with no rendered surface.

## No domain coverage

The chunk adds `fault.silence` / `fault.ramp` / `fault.port_occupier` spans to `logs/agent-latest.jsonl` via `JsonObsLayer` plus a `redact.rs` field allowlist — machine-parsed JSONL artifacts, not operator-facing output. The scope explicitly states "the a11y/GUI surfaces are untouched," and the CLI half of design-system.md governs `owo-colors`/`indicatif`/`comfy-table` human output on stdout/stderr, none of which this chunk touches (JSONL span records carry no ANSI tint, status prefix, lamp glyph, typography, spacing, radius, or motion decision). No design token, `Verdict`/`ReportState` lamp, or expression-level `0.3` motion rule applies.
