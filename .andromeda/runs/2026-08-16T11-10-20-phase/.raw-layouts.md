# layouts extract
[RAW — first return, FAILED validation check 2 (Sections): `## Relevance` emitted together with `## No domain coverage`, which must stand alone. Retried once; the retry passed and is saved as layouts.md. The cli-surface guard named below is preserved because it is a real constraint the retry dropped.]

## Relevance
Out-of-scope — this chunk is headless self-observation instrumentation (`tracing` spans in `logs/agent-latest.jsonl`), touching no desktop-webview or cli rendered surface.

## No domain coverage

The chunk's deliverable is three `tracing` spans (`fault.silence` / `fault.ramp` / `fault.port_occupier`) written to the agent-mode JSONL artifact plus a `redact.rs` field allowlist extension — layout-templates covers only the desktop-webview run console and the cli stdout surface, and the scope explicitly states "the a11y/GUI surfaces are untouched" with "no new fault behavior" and no change to any rendered output. The one cli-adjacent guard worth naming for the P3 research: layout-templates §Surface: cli — Component — Primary content block 2 closes the per-P-ID bracket-label set at six plus the single run-level `[ENVIRONMENT-SUSPECT]` qualifier, and §Component — Primary content block 1 fixes the results/SLO table at 6 columns and the coverage table at 4 — so if any part of this chunk were to surface a fault phase on cli stdout or in a `comfy-table` column, it would breach those closed sets; the scope as written does not, so no layout constraint binds.
