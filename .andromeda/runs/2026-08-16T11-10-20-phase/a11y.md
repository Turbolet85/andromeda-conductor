# a11y extract

## No domain coverage

Backend-only self-observation chunk (Rust `tracing` spans in the fault/dispatch path, the `redact.rs` field allowlist, and `logs/agent-latest.jsonl`) on the headless surface, which a11y-plan §1 flags **not-assertable** (no DOM/widget tree, no axe/Lighthouse reach) and explicitly excludes from §2 and §4 — no UI surface, WCAG criterion, ARIA role, focus/keyboard sequence, contrast pair, or a11y violation-JSON emission is touched; note only that the `focus.shift` / `focus.trap.enter` / `focus.trap.exit` / `focus.restore` spans a11y-plan §3 *recommends for obs to add later* are deliberately outside this chunk's three-name bounded set and must not be pulled in here.
