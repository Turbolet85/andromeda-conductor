RAW return — obs-plan drift-detector (the one doc whose return carried proposals).
Stripped of the ```yaml fence and the trailing D-obs-stack / D-obs-redaction clean notes before parsing;
those notes are preserved at the end of this file.

proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Fault-injection spans (closing paragraph, line 393)"
    change: "Replace 'Each fault span wraps the fault application phase in conductor-faults' with: the run-path fault spans (`fault.silence`, `fault.ramp`) open from a caller-supplied per-phase hook on the `conductor-run` seam (`run_timeline_observed`/`PhaseWindow`), because silence and ramp are declarative phase data with no fault-application call site in conductor-faults; only `fault.port_occupier` opens inside conductor-faults, at its RAII bind site; the run-path spans are created-not-entered children of `timeline.execute` and close at the phase boundary."
    sidecar: "§4 fault-span placement corrected: silence/ramp spans open from the conductor-run per-phase hook, not inside conductor-faults; only port_occupier is instrumented in-crate (2026-08-16-fault-application-spans)."
    rationale: "Report Changes → 'Spec claims disproved by measurement' #1: the code-graph returns 0 crate edges for `conductor-faults` and 0 external references to `PortOccupier`/`AbruptSilence`/`EmissionGap`/`BurstyTrain`, so the §4 placement claim is unreachable; new symbols `fault_span`/`classify_fault` are private to `conductor-run` and `PhaseWindow`/`run_timeline_observed` are the new timeline seam that carries them."

  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Obs Scope Summary → Instrumentation scope table, `conductor-faults` row (line 34)"
    change: "Change 'telemetry surfaces as spans around each fault application phase' to: telemetry surfaces as `fault.port_occupier` around the crate's own RAII hold; the declarative silence/ramp faults are instrumented from the `conductor-run` per-phase hook, not in this crate."
    sidecar: "§1 conductor-faults row realigned to the corrected fault-span placement (duplicate of the §4 claim)."
    rationale: "Same disproved claim as the §4 primary (report 'Spec claims disproved' #1) restated in the entity table; leaving it would let the retired 'spans around each fault application phase in conductor-faults' wording survive a single-site apply."
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Fault-injection spans → Fault.ramp span attributes (line 387)"
    change: "`ramp_factor` is a float in the range −1.0..1.0 — the normalized signed slope `(to_rate − from_rate)/max(from_rate, to_rate)` over `EmissionShape::Ramp`'s integer `from_rate`/`to_rate`/`windows`, so direction (rise vs fall) is carried in the value — not the previously specified 0.0-1.0."
    sidecar: "§4 `ramp_factor` range corrected from 0.0-1.0 to the signed normalized slope −1.0..1.0 (operator decision, phase P4)."
    rationale: "Report Changes → 'Spec claims disproved by measurement' #2 plus Decisions & corrections: the shipped `EmissionShape::Ramp` carries integers with no 0.0-1.0 value, and the operator ratified the normalized signed slope at phase P4; `ramp_factor` is one of the five newly allowlisted field names in the Symbols/APIs bullet."

  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Fault-injection spans → Fault.port_occupier span (lines 389-391) + closing paragraph (line 393)"
    change: "`fault.port_occupier` carries `fault_type` + `port` at open only (neither `fault_duration_ms` nor `fault_start_offset_ms` is knowable at bind time and there is no journal basis at that site); the realized hold is witnessed on the allowlisted `message` field at `debug` when the RAII guard releases, and its `timeline.execute` parentage is CONDITIONAL — no run path drives the occupier today, so it opens parentless outside a timeline run (matrix `v2-15` owns wiring the driver)."
    sidecar: "§4 port_occupier span: attribute set narrowed to `fault_type` + `port`, hold witnessed on `message` at release, `timeline.execute` parentage recorded as conditional."
    rationale: "Report Changes → 'Spec claims disproved by measurement' #3 and the phase-P4 operator decision to instrument at the RAII site now and record the parentage limit; corroborated by the Verification-matrix note that no port-occupier driver exists anywhere."

  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Obs Scope Summary → Telemetry triggers table, `chaos-instrumentation` row (line 145)"
    change: "Qualify the trigger's attribute list: `fault_type` on all three spans, `fault_duration_ms` + `fault_start_offset_ms` (journal offset) on the run-path `fault.silence`/`fault.ramp` only, plus `ramp_factor` on `fault.ramp` and `port` on `fault.port_occupier` (which carries no duration/offset at open)."
    sidecar: "§1 chaos-instrumentation trigger row realigned to the per-span attribute sets (duplicate of the §4 port_occupier claim)."
    rationale: "The trigger row restates the flat `fault_type`/`fault_duration_ms`/`fault_start_offset_ms` triple across all three fault spans — the exact claim report 'Spec claims disproved' #3 refutes for `fault.port_occupier`; it also omits the two newly allowlisted attributes (`ramp_factor`, `port`) the chunk shipped."
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§6 Log Coverage → Log levels mapping table, `debug` row (line 471)"
    change: "Scope the `debug` row to internal control flow such as timeline phase scheduling, and record that fault-span lifecycle lines (`fault.silence` / `fault.ramp` / `fault.port_occupier` open+close) emit at `info` like their sibling spans `scenario.run` / `timeline.execute` / `emit.batch` — one span per phase is not a hot path, so §11's `no-info-in-hot-path` ban is unaffected; the occupier's realized-hold witness stays at `debug`."
    sidecar: "§6 log-level table: fault-span lifecycle moved from `debug` to `info` (sibling-span parity; visible under the default INFO filter without a RUST_LOG opt-in)."
    rationale: "Report Changes → 'Spec claims disproved by measurement' #4 and Deviations #3: at `debug` the shipped spans are invisible under the subscriber's default INFO filter, so the chunk's own acceptance (spans appearing on emitted lines, asserted in `conductor-faults::port_occupier::the_hold_is_bracketed_by_a_fault_span_on_the_emitted_lines`) could not hold without a `RUST_LOG` opt-in."

--- trailing clean-detector notes (stripped from the parsed YAML) ---

- D-obs-stack — clean. The report's Dependencies bullet adds only `tracing` (workspace pin 0.1.44, normal
  dep) and `serde_json` (dev-dep) to `conductor-faults`; zero new `[[package]]` entries. The
  `tracing-subscriber` dev-dep was added and reverted (Reverted/negative API facts), with the occupier's span
  instead asserted through `conductor_core::init_observability(ObsSink::File(..))`. No OTel SDK, no OTLP
  exporter for self-obs — matches §3.
- D-obs-redaction — clean. The five new field names went through `conductor_core::redact::ALLOWLISTED_FIELDS`
  (31 → 36), the port-occupier hold is witnessed on the already-allowlisted `message` field, `port` is an
  integer, and no doc bakes the allowlist size (§4 names the constant, not its count). No path or struct-name
  leak in logs, `runs.db`, or artifacts.
