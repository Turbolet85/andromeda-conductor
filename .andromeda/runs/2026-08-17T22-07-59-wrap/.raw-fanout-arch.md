```yaml
proposals:
  - detector: D-arch-decisions
    severity: warning
    section: Established Decisions [Read-Back Dependency Posture]
    change: "Reduce the identity narrowings from TWO to ONE — only the first `NORMALIZED_FRAMES = 3` normalized lines contribute — and replace the leading-path-segment wording with the token-leading rule: `is_absolute_path_start` is guarded by `is_token_boundary`, so a relative path is preserved in full and is identity-significant at EVERY depth (`src/worker.rs` and `src/anything/else.rs` are now DISTINCT identities) while a token-leading absolute path is stripped to the empty form; line and hex-address stay the insensitive axes; re-pin from the retired `exception.rs::only_the_leading_path_segment_reaches_the_preimage` to `relative_paths_are_significant_at_every_depth` + `token_leading_absolute_paths_normalize_to_the_same_empty_form`, and record the byte-verification of the normalize half at SUT HEAD `efabe8e` (superseding `d090314` for that half)."
    sidecar: "2026-08-17 — P-017 identity narrowings 2 → 1: the leading-path-segment narrowing is RETIRED (measured FALSE at Pulse HEAD `efabe8e`; Pulse guards that arm with `is_token_boundary`), replaced by the token-leading rule; the first-`NORMALIZED_FRAMES` narrowing survives; pin moved to the two new tests."
    rationale: "Report §Spec claims disproved by measurement names this exact clause: arch states `is_absolute_path_start` fires on ANY `/` so `src/worker.rs` ≡ `src/anything/else.rs`, measured FALSE at Pulse HEAD `efabe8e` (evidence: Pulse's `compute_differs_for_relative_paths_differing_below_leading_segment` / `normalize_stacktrace_preserves_relative_paths_in_full` / `normalize_stacktrace_strips_only_absolute_paths`, reproduced by the new `relative_paths_are_significant_at_every_depth`). Changes confirm the code moved with it (ADDED `is_token_boundary`, REMOVED `leading_path_segment`, CHANGED `is_absolute_path_start`, `PathVariant` SAME-fp → DIFFERENT-fp), and the pinned test `only_the_leading_path_segment_reaches_the_preimage` was retired, so the locked decision as written contradicts the shipped derivation; report Expected amendment 1 requests exactly this. Per-occurrence sweep run over architecture.md for the retired wording (LEADING PATH SEGMENT · leading segment · is_absolute_path_start · skip_absolute_path · path-insensitivity · src/worker.rs · narrowing · P-017): the ONLY hit is this bullet — the flagged §Standard Contracts site points at [Read-Back Dependency Posture] rather than restating the claim, so no dependent-of proposals are due in this doc."
```

Notes for the orchestrator (outside the proposal set, no drift found):

- `D-arch-resources` — clean. Report states no public API, IPC method, endpoint, export, port, socket or env var touched; no crates added/removed; `is_token_boundary` is a private helper inside `conductor-emit::exception`, not a registry-level resource; `Cargo.toml`/`Cargo.lock` untouched (zero dependency delta), so §Stack, §Occupied Resources and §Inherited Defaults all still hold.
