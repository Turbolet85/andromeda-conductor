Raw return — arch doc-agent (verbatim):

proposals:
  - detector: D-arch-decisions
    severity: warning
    section: "Established Decisions → [Read-Back Dependency Posture] (the 'Freshness is the carrier' paragraph)"
    change: "Replace 'which the deterministic-L4 fixture pins to `[]` (`pulse-app/src/deterministic_inference.rs:35`)' with: the deterministic-L4 fixture now POPULATES `evidence_refs` with a constant `det-*` triple (`det-span-…` / `det-template-0007` / `det-fingerprint-…`, measured live 2026-08-18 at SUT HEAD `efabe8e`), so `fingerprint_refs` is non-empty but still payload-INVARIANT — freshness remains the carrier because the values are fixture constants that do not vary with what Conductor emitted, not because the field is empty."
    sidecar: "2026-08-18 — [Read-Back Dependency Posture] deterministic-L4 `evidence_refs` corrected from pinned `[]` to a constant `det-*` triple (SUT HEAD `efabe8e`); freshness-carrier conclusion unchanged, its supporting fact re-based from emptiness to payload-invariance. Source: chunk 2026-08-18-error-baseline-spike-live-proof."
    rationale: "Report §Changes 'Spec claims disproved by measurement' — leg A envelope carried the det-* triple; arch states the retired `[]` pin inside a locked decision, so a locked decision is contradicted by measurement."
  - detector: D-arch-decisions
    severity: warning
    section: "Standard Contracts → Readiness gate paragraph"
    change: "Change '(empty under deterministic L4)' to '(a constant `det-*` triple under deterministic L4, not empty — payload-invariant either way)'."
    sidecar: "2026-08-18 — §Standard Contracts readiness-gate restatement of 'empty under deterministic L4' retired alongside the [Read-Back Dependency Posture] primary."
    rationale: "Second verbatim occurrence in arch (grepped: lines 63 and 88 only)."
    dependent-of: D-arch-decisions
  - detector: D-arch-resources
    severity: warning
    section: "Standard Contracts → Run report envelope (per scenario check) JSON sample"
    change: "Sample row reads `\"slo_tier\": \"<90s\"` + a following note that the family ships declare-only (live rows `verdict: null` / `state: KnownResidual`)."
    sidecar: "2026-08-18 — §Standard Contracts envelope sample re-tiered + declare-only note."
    rationale: "Report Counts/qualifiers moved: `<5s` → `<90s`; disproved-claims: Pass/Pass unattainable. Sole occurrence (lines 60/82 carry the ENUM, which stays)."

Agent notes: h2 lock-only bump — no §Stack drift (arch's audit sentence still true). The witness line rides
the allowlisted message channel — no registry gap. SEED-forcing + percentile findings are not Changes-bullet
facts (Deviations/Decisions only) — correctly not proposed.
