Raw return — design-system doc-agent (verbatim):

proposals:
  - detector: D-design-derived-count
    severity: warning
    section: "Surface: cli → Component Patterns → 4. Verdict / report-state lines"
    change: De-literalize the sample verdict line so it stops pinning one scenario's tier — render it as `✓ P-009  Pass   1840ms <slo_tier>` (tier drawn from the scenario's declared `slo_tier`, the closed set `<5s`/`<20s`/`<90s`) instead of the baked `1840ms <5s`.
    sidecar: "cli Component Pattern 4: P-009 sample line's hardcoded `<5s` replaced with a set-naming tier placeholder (error-baseline-spike re-declared `<5s` → `<90s`, chunk 2026-08-18-error-baseline-spike-live-proof)."
    rationale: Report Counts/qualifiers moved names this exact site; placeholder-over-literal per the detector's "never substitute a fresh literal that re-stales".

Agent notes: D-design-tokens clean (no new UI element; the self-obs line grades tokens n/a). Grep confirmed
lines 86 + 319 name the SET (not hits, correct as written); line 321 was the sole literal; no other P-ID
sample carries an SLO-tier value. The other two counts-moved entries state no design-system value.
