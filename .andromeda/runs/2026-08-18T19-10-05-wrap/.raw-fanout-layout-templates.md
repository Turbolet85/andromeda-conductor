Raw return — layout-templates doc-agent (verbatim):

proposals (1 primary + 4 dependent-of: D-layout-derived-count, all P-009 error-baseline-spike sample
slo_tier `<5s` → `<90s`):
  1. §Wireframe Run console (HOLD) — matrix row `✓ P-009  error-baseline-spike   <5s   1840ms  Pass`
  2. §Wireframe Run report (terminal) — verdict line `✓ P-009 … 1840ms  <5s` [dependent]
  3. §cli Output structure `conductor run <scenario>` — sample line `… 1840ms <5s` [dependent]
  4. §cli Output structure `conductor suite` — comfy-table row `[PASS]     <5s    1840ms` [dependent]
  5. §cli Component Primary content block 2 — bullet sample `✓ P-009  Pass   1840ms <5s` [dependent]

Agent notes: D-layout-surface clean (no new surface/region). The `[[expected]]` 2→0 and `fingerprints`
[]→det-* entries have no layout-templates site (samples carry truncated hashes, never `[]`).
latency-regression `<20s` appears nowhere in the doc. Set-naming prose at lines 127 + 256 is correct as
written — not hits per the detector's carve-out, left alone.
