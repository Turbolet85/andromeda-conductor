Raw return — obs-plan doc-agent (verbatim):

proposals:
  - detector: D-obs-instrumentation (primary)
    section: §4 Fingerprint-storm Required log fields
    change: `fingerprints` is present and MAY BE POPULATED under deterministic L4 — the fixture now
      populates `evidence_refs` (leg A carried `det-span-…`, `det-template-0007`, `det-fingerprint-…`);
      retire "MAY BE EMPTY … pins `[]` … never by a populated envelope array"; keep
      `fingerprints_read_back_count` + the harvest surface as the storm-identity evidence.
  - detector: D-obs-instrumentation (dependent)
    section: §1 critical-paths table, Fingerprint-storm row — "(array, MAY BE EMPTY under deterministic L4)"
      → "(array, populated under deterministic L4 from `evidence_refs`-fed `fingerprint_refs` — see §4)".
  - detector: D-obs-instrumentation (dependent)
    section: §4 Required span attributes → `verify.readback_fingerprints` bullet — retire "pinned `[]` under
      deterministic L4" (a THIRD site the report's own doc list omitted; this bullet is the source the
      2026-08-16 sidecar cites for the pinning claim), leaving the surrounding claim (count-not-match;
      `span_events.fingerprint` zero reads) intact.

Agent notes: D-obs-stack clean (h2 is the PRODUCT egress path §3 sanctions; no self-obs stack change).
D-obs-redaction clean (the witness line rides the allowlisted `message` channel per the §6 key-set
precedent; run_id carried; 32-hex only). slo_tier moves + the verdict-null disposition deliberately not
proposed against obs-plan (enum + thresholds unchanged; the report scopes those to test-plan/design).
