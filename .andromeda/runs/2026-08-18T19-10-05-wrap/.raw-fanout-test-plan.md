Raw return — test-plan doc-agent (verbatim):

proposals:
  - detector: D-tests-derived-count (primary)
    section: §6 Scenario error-baseline-spike → Verification signal
    change: replace the exit-0/`[PASS]`/`verdict=Pass`/`state=Pass` signal with the declare-only outcome —
      `[RESIDUAL]` at exit 0, `verdict` null / `state=KnownResidual`, `latency_ms` against the declared
      `slo_tier` (name the field, never a literal tier), live claim graded at the harvest tier
      (`triage.cue.emit` via `crates/conductor-run/tests/baseline_harvest.rs`, the storm_harvest precedent).
  - detector: D-tests-derived-count (dependent-of primary 1)
    section: §1 Critical Path 1 (line 73) — the verbatim `verdict=Pass` + `state=Pass` restatement re-based
      the same way (pii-scrub / connection-lifecycle instances of the pattern unaffected).
  - detector: D-tests-derived-count (primary)
    section: §6 Scenario Fingerprint-storm → Verification signal (line 330)
    change: retire the `fingerprints` expected-`[]` pin + the superseded 2026-08-16 both-legs-empty
      parenthetical — the field is PRESENT and SUT-populated under deterministic L4 (leg A carried three
      `det-*` refs); the live proof stays the storm-log harvest, never an empty-literal value pin.
  - detector: D-tests-derived-count (dependent-of primary 3)
    section: §1 Critical Path 2 (line 74) — the verbatim "present but expected `[]`" restatement retired the
      same way.

Orchestrator apply note: the CP2 tail's "NOT via a populated `fingerprints` field" was refined to "NOT via
the `fingerprints` array (payload-invariant fixture constants)" in the same apply — the old negation's
premise ("populated" implying meaningful) inverted when the field became populated-but-constant.

Agent notes: §3 `"fingerprints": []` is a TYPE placeholder in the schema block — not a stated L4 value, not
proposed. No literal `<5s`/`<20s` exists in test-plan (field-naming form, correct). D-tests-framework /
-coverage / -obs-harness clean (runners match §2/§4; baseline_harvest + seeded snapshot match §2/§7; no
harness/envelope/log-format edit in Changes).
