```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: "§Occupied Resources — Service / process names"
    change: "Add an emitted-service-identity entry: Conductor's product OTLP emissions now carry TWO `service.name` values — `conductor` (`DEFAULT_SERVICE_NAME`, the scenario dispatcher's identity) and `conductor-canary` (`CANARY_SERVICE_NAME`, `conductor-run/src/lib.rs`) for the preflight canary's warm-up + storm — split so the preflight, which runs inside every scenario leg, no longer ages the dispatcher's per-service young-sample window or inflates its baseline in Pulse."
    sidecar: "2026-08-18-restart-suppression-live-proof — registered `CANARY_SERVICE_NAME` (`conductor-canary`) as the preflight canary's emitted OTLP service identity, distinct from the dispatcher's `conductor` (`DEFAULT_SERVICE_NAME`)."
    rationale: "Report Changes §Symbols/APIs lands NEW `pub const CANARY_SERVICE_NAME` with the canary's emissions re-attributed off `DEFAULT_SERVICE_NAME`, and Deviation 2 states the split's architectural consequence; arch §Occupied Resources 'Service / process names' lists only conductor-cli, the Tauri GUI bin and the spawned andromeda-pulse-mcp child — the new wire-visible service identity is unregistered; no other occurrence, no dependents."
```
