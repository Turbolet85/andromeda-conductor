proposals: [] — with reasoning: D-design-tokens (all three new surfaces tokens n/a; no UI rendered). D-design-derived-count (all three slo_tier sites — lines 86, 319, 321 — name the closed SET, the detector's explicit carve-out; the report localizes tier-literal staleness to layout-templates; restart-suppression appears nowhere in design-system; no other moved count has a design-system expression).
