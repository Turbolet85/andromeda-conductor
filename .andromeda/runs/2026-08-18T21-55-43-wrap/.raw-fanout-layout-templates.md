```yaml
proposals:
  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: desktop-webview › Wireframe — Run console (HOLD) — signature moment"
    change: In the `⊙ P-014  restart-suppression` sample row, drop the pinned `<20s` tier literal in favour of the set-named placeholder (`<slo_tier>`, one of `<5s` / `<20s` / `<90s`) and add a wireframe note that per-scenario tiers are declared in `scenarios/*.toml`, not fixed by this wireframe.
    sidecar: "layout-templates §Wireframe — Run console (HOLD): restart-suppression sample row no longer pins a `<20s` slo_tier literal; tier cell names the documented set and defers to the scenario TOML."
    rationale: "Report Counts/qualifiers moved: restart-suppression slo_tier <20s → <90s; the HOLD wireframe bakes the exact retired <20s value."
  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: desktop-webview › Wireframe — Run report (terminal)"
    change: In the `✗ P-014  restart-suppression  Fail  4120ms  <5s` verdict line, replace the `<5s` literal with the same set-named placeholder used in the HOLD wireframe.
    sidecar: "layout-templates §Wireframe — Run report (terminal): restart-suppression sample verdict line de-pinned from `<5s` to the set-named tier placeholder."
    rationale: "Same retired claim restated; one of the four <5s sites the report names."
    dependent-of: D-layout-derived-count
  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: cli › Output structure — `conductor run <scenario>`"
    change: In the `✗ P-014  restart-suppression   Fail   4120ms <5s` sample line, replace the `<5s` literal with the set-named tier placeholder, keeping the `[FAIL]`/ANSI 203 annotation unchanged.
    sidecar: "layout-templates §cli Output structure — `conductor run`: restart-suppression sample verdict line de-pinned from `<5s` to the set-named tier placeholder."
    rationale: "Third of the five tier-literal sites."
    dependent-of: D-layout-derived-count
  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: cli › Output structure — `conductor suite` (coverage table)"
    change: In the `P-014   restart-suppression    [FAIL]     <5s    4120ms` comfy-table sample row, replace the `<5s` slo column value with the set-named tier placeholder.
    sidecar: "layout-templates §cli Output structure — `conductor suite`: restart-suppression sample table row de-pinned from `<5s` to the set-named tier placeholder."
    rationale: "Fourth of the five sites."
    dependent-of: D-layout-derived-count
  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: cli › Component — Primary content block 2 (verdict / report-state lines)"
    change: In the `✗ P-014  Fail  4120ms <5s` bracket-label example, replace the `<5s` literal with the set-named tier placeholder so the `[FAIL]` label example illustrates the label, not a scenario's declared tier.
    sidecar: "layout-templates §cli Primary content block 2: P-014 `[FAIL]` example de-pinned from `<5s` to the set-named tier placeholder."
    rationale: "Fifth of the five sites the report names."
    dependent-of: D-layout-derived-count
```
Notes: D-layout-surface clean (no new user-facing surface; --seed change no new verb/flag/prompt). Other moved counts have no layout expression (grep cited). Set-naming sites (line 127 + cli Primary content block 1) correct, not flagged.
