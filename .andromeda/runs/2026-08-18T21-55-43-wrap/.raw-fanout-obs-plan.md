```yaml
proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage — Scenario: Restart-suppression scenario incl. bypass case"
    change: Replace the never-built family span chain (timeline.execute_restart_suppression → canonical/bypass emit.batch path_type split → verify.readback_suppression_bypass) and the bypass_triggered envelope extra with the shipped declare-only instrumentation — scenario.run → timeline.execute with two fault.silence children (offsets 7006ms/76436ms, info) and the standard eleven-field envelope, noting bypass/suppression evidence rides the check line + emit-absence (Pulse's triage.cue.tick counters read "<redacted>" live) and that suppression-eligibility is a service-cumulative sample window (10..29 samples), not a time property of the spike.
    sidecar: "2026-08-18 restart-suppression live proof — §4 restart-suppression row retargeted from the unbuilt bypass span family + bypass_triggered envelope extra to the shipped timeline.execute + fault.silence declare-only instrumentation."
    rationale: "Report disproof 1 names this site; Coverage bullet measures the actual instrumentation; Schema/config confirms no envelope change."
  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Obs Scope Summary — Critical paths (must-trace) table, Restart-suppression row"
    change: Restate the row's must-trace spans as scenario.run → timeline.execute (+ fault.silence per gap phase) and drop the bypass_triggered additional-field clause, leaving the standard eleven-field envelope.
    sidecar: "2026-08-18 restart-suppression live proof — §1 critical-paths row aligned to the declare-only landing."
    rationale: "Same disproof restated in the §1 table."
    dependent-of: D-obs-instrumentation
  - detector: D-obs-instrumentation
    severity: warning
    section: "§6 Log Coverage — Additional scenario-specific fields"
    change: Remove the bullet bypass_triggered (boolean, restart-suppression scenario) — never built; scenario landed declare-only.
    sidecar: "2026-08-18 restart-suppression live proof — §6 scenario-specific field list drops the unbuilt bypass_triggered extra."
    rationale: "Report disproof 1; no envelope change shipped."
    dependent-of: D-obs-instrumentation
  - detector: D-obs-instrumentation
    severity: warning
    section: "§3 Observability Harness Contract — Log format JSON schema (Additional fields per scenario)"
    change: Drop bypass_triggered from the e.g. list (bypass_triggered, lifecycle_phase, degraded_mode_response), leaving the extras that exist.
    sidecar: "2026-08-18 restart-suppression live proof — §3 additional-fields example no longer cites the unbuilt bypass_triggered extra."
    rationale: "Third restatement of the retired claim."
    dependent-of: D-obs-instrumentation
```
Notes: D-obs-stack clean (zero deps; tracing-JSON-only holds). D-obs-redaction clean (PII n/a grep-clean; the "<redacted>" reads are the SUT's own allowlist working, not a Conductor leak).
