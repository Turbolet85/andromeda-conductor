proposals: [] — with reasoning: D-security-input (TOML garde ✓ per the scenario-config row; CANARY_SERVICE_NAME is a hard-coded const, not an env/deserialized handle; restart_harvest.rs test-only; --seed change is precedence on a pre-existing validated handle). D-security-subprocess (spawn/path/.env/negotiate-down untouched; canary change is service.name attribution only; %TEMP%/pulse-legs is a curation candidate, not a landed change). D-security-deps (zero delta; the RUSTSEC-2026-0244 exit-1 + deny TRUE-exit-0 overlap is the prescribed bounded wait; the '22nd consecutive' in the doc is frozen history, not a counter this chunk retires).
