```yaml
proposals:
  - detector: D-tests-coverage
    severity: warning
    section: "6. E2E Test Strategy — Scenario: Restart-suppression incl. one bypass case (Verification signal)"
    change: "Replace the in-scenario hard pass/fail signal with the declare-only landing — [[expected]] retired, row = verdict null / state KnownResidual (non-Blocked) with latency_ms vs the declared slo_tier; the live suppression/bypass claim grades at the HARVEST tier in crates/conductor-run/tests/restart_harvest.rs (baseline/storm precedent) pinning verbatim leg lines; proven bypass set = absolute arm + neither-condition suppressed case (relative arm unreachable at shipped SUT constants); suppression eligibility is the service's young cumulative-sample window, not a spike duration."
    sidecar: "§6 restart-suppression: hard pass/fail → declare-only row + harvest-tier live proof (restart_harvest.rs); bypass = absolute arm + suppressed case."
    rationale: "Report retires both [[expected]] checks; restart_harvest.rs added (10 tests, 7 pinned lines); Outcome row KnownResidual; disproofs 1-2."
  - detector: D-tests-coverage
    severity: warning
    section: "1. Test Scope Summary — Critical paths, Path 3 (Restart-suppression incl. one bypass case)"
    change: "Restate Path 3's signal to match §6: declare-only row + journal/runs.db assertions, suppression/bypass proven at the harvest tier, not an in-scenario hard pass/fail."
    rationale: "The two-site rule; report disproof 1 names §6 + §1 CP3 explicitly."
    dependent-of: D-tests-coverage
  - detector: D-tests-obs-harness
    severity: warning
    section: "3. Test Harness Contract — 5-command implementation, `run` (scenario invocation)"
    change: "Qualify: the harness scenario leg appends --seed <s> ONLY when SEED is explicitly set; unset → the TOML-declared seed governs (arch files-over-env-defaults), identically in .sh/.ps1; envelope records the effective seed."
    sidecar: "§3 run: --seed only on an explicitly set SEED; TOML-declared seed governs otherwise (.sh/.ps1 parity)."
    rationale: "Report Symbols/APIs + Decisions + Outcome (leg ran without SEED; envelope seed 4317015); §3 bakes an unconditional --seed. One-sided: obs-plan carries no seed wording."
```
Notes: D-tests-framework clean (commands match §2/§3/§4; new test bin in crate tests/ per convention; zero deps). D-tests-derived-count clean (no moved value has a test-plan literal; greps cited).
