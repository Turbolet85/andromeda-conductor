# Fan-out results — 2026-08-18-restart-suppression-live-proof wrap

7 doc-agents · 18 detectors · **13 proposals (5 primaries + 8 dependent-of) · 0 detector escalations
· 0 false positives** (playbook: all routine). Raw twins: `.raw-fanout-{doc}.md` (all 7 — every return
carried commentary or fences beyond bare YAML).

| doc | verdict | proposals |
|---|---|---|
| arch | 1 proposal | D-arch-resources: register `CANARY_SERVICE_NAME` (`conductor-canary`) as the preflight canary's emitted OTLP service identity in §Occupied Resources Service/process names (no other occurrence — no dependents) |
| security-plan | clean | D-security-input: TOML re-shape is garde ✓ per the scenario-config row; const is not an input handle; harness change is precedence on an existing handle. D-security-subprocess: spawn/data-dir untouched. D-security-deps: zero dependency delta; the audit DB fault + deny-verified overlap is the prescribed bounded wait |
| design-system | clean | all three `slo_tier` sites name the closed SET (lines 86/319/321) — the detector's carve-out; no other moved count appears |
| layout-templates | 5 proposals | D-layout-derived-count: the five restart-suppression sample rows (HOLD wireframe `<20s`; Run report terminal / cli run / cli suite / Primary content block 2 `<5s`) → set-named tier placeholder (1 primary + 4 dependent-of) |
| test-plan | 3 proposals | D-tests-coverage: §6 restart-suppression signal → declare-only row + harvest tier (primary) + §1 Critical Path 3 restatement (dependent-of — the two-site rule); D-tests-obs-harness: §3 `run` body → `--seed` only on explicitly-set SEED (one-sided by my grep: obs-plan carries no seed wording). D-tests-framework + D-tests-derived-count clean (greps cited) |
| obs-plan | 4 proposals | D-obs-instrumentation: §4 restart row → shipped declare-only instrumentation (`timeline.execute` + 2× `fault.silence`; sample-window semantics; tick counters redacted) (primary) + §1 critical-paths row + §6 scenario-field list + §3 additional-fields example all drop `bypass_triggered` (dependent-of ×3). D-obs-stack + D-obs-redaction clean (the `"<redacted>"` reads are the SUT's own allowlist working) |
| a11y-plan | clean | no interactive element; envelope unchanged — a11y §3's enum reproduction (lines 106/243) carries no per-scenario tier literal; `conductor-canary` is disjoint from a11y's bound identities |

## Orchestrator validation (the 6 checks)
1. **Playbook** — all 13 routine: registry addition (arch — the API-symbol over-reach dismiss does NOT
   apply: an emitted `service.name` is a wire-visible identity Pulse keys per-service state on, not
   internal API surface); derived-count set-naming (layouts ×5 — the detector's own prescribed fix,
   prior-wrap precedent); harvest re-basing (tests ×2 — the v2-11/v2-12 twice-ratified class);
   spec-illustration→sound-impl (tests §3 seed body — report proves it live);
   unbuilt-target-prose retirement (obs ×4 — the per-check-extraction/fingerprint precedent).
2. **Cross-contradiction** — none (disjoint sections; obs×4 and tests×2 are single retirements across
   restatements).
3. **Intent-consistency** — consistent; the redesign deviations were operator-ratified in-session and the
   report justifies each.
4. **Absence-needs-evidence** — orchestrator greps run this wrap: `persistence` across all 7 masters →
   only unrelated senses (Run-History/proptest/file persistence); old-shape tokens
   (`8x/3|12x/4|6x/7|suppressed-15s|sustained-60s|25s stream gap`) → zero hits; `--seed|SEED=` in
   obs-plan → zero hits (the tests agent's one-sided-§3 claim verified, not trusted).
5. **Expected-amendments reconciliation** — plan's four entries all covered: test-plan §6+§1 ✓ (tests),
   obs-plan §4 row + `bypass_triggered` ✓ (obs, wider: §6+§3 too), layout five-site sweep ✓ (layouts),
   architecture CI/CD sentence ✓ no-op as predicted (still true — considered, no edit).
6. **Disproved-claims disposition** — (1) persistence-as-duration → obs/tests/layouts amendments +
   next-visit intake; (2) relative arm unreachable → tests §6 wording + the v2-13 refinement + intake;
   (3) tick counters redacted → obs §4 wording + intake; (4) old TOML encoding → superseded in-repo,
   zero master statements (grep); (5) matrix literals → the v2-13 refine-with-evidence escalation
   (operator dialogue this wrap).

Escalations: **1 staged for dialogue** — the v2-13 coverage refinement (wrap directive 2; not a
detector escalation). Resolution recorded in the wrap report/handoff after the dialogue.
