```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: Standard Contracts — Run report envelope (per scenario check)
    change: The declare-only note names BOTH families — `error-baseline-spike` (retired 2026-08-18) and `pii-scrub` (retired 2026-08-19) — as shipping zero `[[expected]]` checks because no read-back surface can carry them under deterministic L4, each landing `verdict: null` / `state: "KnownResidual"` under the degraded read-back with the live claim graded at the harvest tier.
    sidecar: 2026-08-19 — §Standard Contracts: added `pii-scrub` to the declare-only family note (`[[expected]]` 5 → 0, measurement moved into the TOML header); the note previously named `error-baseline-spike` only.
    rationale: Report §Changes "Schema / config" removes all five `[[expected]]` checks from `scenarios/pii-scrub.toml` (declare-only) and "Counts / qualifiers moved" names this exact site — "architecture §Standard Contracts declare-only family note (currently names error-baseline only)"; leg B measured the shipped shape as `verdict: null` / `KnownResidual`, non-Blocked, matching the note's existing declare-only envelope wording. Sole occurrence in arch — grep for `declare-only` / `[[expected]]` / `harvest tier` hits only this line; line 118's envelope sample and line 217's live-Pulse scenario list carry no graded/declare-only claim about pii-scrub, so no dependent occurrence exists.
```
