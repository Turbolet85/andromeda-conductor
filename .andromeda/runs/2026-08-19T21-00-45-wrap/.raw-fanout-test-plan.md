I have everything I need. Evaluation:

- **D-tests-framework**: report's gates (`cargo nextest run --workspace --profile ci`, `cargo test --workspace --doc`, `cargo clippy --workspace --all-targets -- -D warnings`, `bash scripts/agent-run.sh run`) match §3/§4 exactly; Dependencies bullet says none. No drift.
- **D-tests-obs-harness**: no harness/status-read/log-format change (the `time_unix_nano` stamp is on emitted OTLP log records, not the self-obs JSONL or Run-report envelope); Deviation #2 explicitly reuses the harness's own leg-branch command line. No drift.
- **D-tests-coverage**: both new paths carry tests at the mandated tier (`log_records_carry_distinct_strictly_increasing_stamps` unit + `pii_harvest.rs` 5/5 at the `conductor-run` harvest tier, the documented `baseline/storm/restart_harvest.rs` precedent); pins are committed verbatim captures, live legs stay on the operator gate. No drift.
- **D-tests-derived-count**: `[[expected]]` 5 → 0 hits two sites the report names — §6 line 325 and §1 line 81 — both of which still fold pii-scrub in as an "auto-scenario instance" (read-back-graded). Nextest 633→639 and `<5s`/P-035 have no literal in this doc; Hard-shape 3→2 has no member count here.

```yaml
proposals:
  - detector: D-tests-derived-count
    severity: warning
    section: "6. E2E Test Strategy — Scenario: Headless deterministic scenario run with MCP read-back (`error-baseline-spike`) → Cleanup line's coverage parenthetical"
    change: "Split the parenthetical — `connection-lifecycle` stays an additional auto-scenario instance of this pattern, while `pii-scrub` is now DECLARE-ONLY (all its `[[expected]]` read-back checks retired; row is `verdict` null / `state=\"KnownResidual\"`, non-Blocked, `latency_ms` against its declared `slo_tier`, measurement recorded in the scenario TOML header) with the live scrub claim graded at the HARVEST tier in `crates/conductor-run/tests/pii_harvest.rs` — the `baseline_harvest.rs`/`storm_harvest.rs`/`restart_harvest.rs` precedent."
    sidecar: "test-plan §6: pii-scrub re-based from auto/read-back-graded instance to declare-only + `pii_harvest.rs` harvest-tier proof (2026-08-19-pii-scrub-live-proof)."
    rationale: "Report Counts/qualifiers moved: `scenarios/pii-scrub.toml` `[[expected]]` **5 → 0** (declare-only; measurement in the TOML header), naming `test-plan §6 scenario row` as a doc describing pii-scrub as a graded family; Files/Outcome add `crates/conductor-run/tests/pii_harvest.rs` (NEW — 8 verbatim pins · 5 tests) and leg B's shipped shape `verdict null / KnownResidual`, non-Blocked."
  - detector: D-tests-derived-count
    severity: warning
    section: "1. Test Scope Summary — Critical paths, closing parenthetical (\"7 paths — the maximum …\")"
    change: "Keep the 7-path maximum and `connection-lifecycle`'s auto-instance wording, but re-word the `pii-scrub` clause: it is covered by E2E suite expansion as a DECLARE-ONLY instance of Path 1's pattern (no read-back-graded checks; `verdict` null / `state=KnownResidual`), its live claim graded at the harvest tier."
    sidecar: "test-plan §1 Critical-paths note: pii-scrub twin re-worded from auto-scenario instance to declare-only, tracking the §6 row."
    rationale: "Same retirement, restated site — the report names `§1 Critical-Path-1 twin` alongside the §6 row as docs describing pii-scrub as a graded/Hard family; a §6-only apply would leave §1 asserting the retired read-back-graded shape."
    dependent-of: D-tests-derived-count
```
