# a11y extract

## No domain coverage

This chunk is Rust mutation-killing test work inside `crates/conductor-verify/` (JSON-RPC id-counter correlation, `notify` wire ordering, `ShapeWitness` tracing emission) with no rendered UI surface — a11y-plan §1 scopes every assertion to the single assertable desktop-webview surface and §9 records the unit/cargo-nextest stage as `n/a` for a11y artifacts, so no WCAG criterion, ARIA role, focus/keyboard contract, or contrast token binds here.
