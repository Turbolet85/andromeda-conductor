# layouts extract

## No domain coverage

This is a test-integrity chunk confined to `crates/conductor-verify/` test assertions (mutation-survivor kills for `jsonrpc.rs` id-counter/`notify` and `preflight.rs` `ShapeWitness::list`); it renders no surface, adds no component, focus target, dialog, or cli output element, and layout-templates.md governs only desktop-webview and cli surface structure.
