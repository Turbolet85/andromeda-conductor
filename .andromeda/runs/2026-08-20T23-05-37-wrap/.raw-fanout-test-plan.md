Raw return — test-plan doc-agent (the one return carrying proposals). Preamble/trailing notes present,
so the twin is kept per amendment-flow §Fan-out.

---

Both documents read. Here are my findings.

```yaml
proposals:
  - detector: D-tests-framework
    severity: warning
    section: "§4 Unit Test Strategy — **Mutation instrument:** cargo-mutants 27.1.0 (run discipline, the line beside `Found 0 mutants to test` is a NO-OP)"
    change: "Add one run-discipline item: cargo-mutants' exit code reflects surviving/timeout CLASSES, not run success — gate on the tallies read from `mutants.out/` (`missed.txt` empty + the named survivors in `caught.txt`), never on the exit code; measured **exit 3** on a run that fully met its acceptance (0 missed, all named survivors killed), the non-zero coming from two pre-existing timeouts — the converse half of the already-recorded `Found 0 mutants to test` exits 0 no-op."
    sidecar: "test-plan §4: recorded the second half of cargo-mutants' exit-code semantics — non-zero exit (measured exit 3) reflects surviving/timeout classes, not run failure; gate on tallies, never the exit code."
    rationale: "Report *Decisions & corrections* measures exit 3 on a run whose acceptance was fully met and states the rule is 'Owed as ONE line in this wrap's test-plan §4 amendment, beside last wrap's registrations (operator directive)'; the report's **Spec-master edits** bullet names exactly this as the one edit EXPECTED at P2 (test-plan §4, the cargo-mutants exit-code line). §4 currently records only the exit-0 direction, so an agent following §4 alone would read the chunk's green run as a failure."
  - detector: D-tests-framework
    severity: warning
    section: "§12 Test Decisions Log — `2026-08-20` Mutation instrument adopted, **Run discipline, each item measured not assumed** bullet"
    change: "Extend the run-discipline bullet's exit-code parenthetical so it carries both directions: `Found 0 mutants to test` exits 0 (a silent no-op indistinguishable from a clean pass) AND a non-zero exit (measured exit 3) reflects surviving/timeout classes rather than run failure — the tallies, not the exit code, are the signal in both directions."
    rationale: "§12 restates §4's mutation run discipline verbatim and carries only the exit-0 half ('a silent no-op indistinguishable from a clean pass'), which implies the converse — that a non-zero exit IS distinguishable as a fail — the exact reading the report disproved by measurement (exit 3 on a fully-accepted run). A single-site apply to §4 would leave the one-sided claim alive in the decisions log."
    dependent-of: D-tests-framework
```

Detectors evaluated with no drift:

- **D-tests-coverage** — the chunk adds only test-support and assertions (`WireEntry` / `WireLog` / `DECOY_TOOL` / two `StubConfig` fields, all in `tests/common/mod.rs`), no production `pub` surface. Both new binaries carry tests at the §2/§5 integration tier (rmcp stub over the in-process duplex, no real network, no wall clock), and `tests/readback_shape_witness.rs` is alone in its binary exactly as §11 Integration requires for the process-global `init_observability` singleton. Suite green under both runners (nextest 96/96, `cargo test` 96, workspace 673/673 zero-retry).
- **D-tests-obs-harness** — no harness, status-read, or JSONL log-format change. The shape-witness leg READS the existing self-obs artifact and "adds no span, no field, no OTel SDK", so §3 ↔ obs-plan §3 stay in agreement.
- **D-tests-derived-count** — grepped test-plan for every OLD value in the report's `Counts / qualifiers moved` bullet (669, 92, the 33/5/11/2 mutant baseline, "five survivors", a `Cargo.lock` line count): zero hits in any selector label, fixture count, or tier table. The only numeric near-miss is `SQLite 3.51.1` (§5 boundary table), unrelated to the 51-mutant tally.

One thing I deliberately did **not** propose: the report's Dev-tool bullet names cargo-nextest **0.9.133** while test-plan says **0.9.137** at six sites (lines 112, 120, 206, 269, 593 and §3's `test-runner-install`). That bullet reads "**Dev-tool versions:** none … neither was installed or upgraded here", so nothing moved this chunk, and §4's Tool-version policy already governs it ("reference floors, not exact pins … any install that runs the gate green satisfies it"). Substituting 0.9.133 would be a fresh literal that re-stales. Flagging it here only so the orchestrator can see it was considered.
