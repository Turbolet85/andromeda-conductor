# a11y extract

## Relevance
Partial (narrow) — this chunk adds no UI surface and runs on the not-assertable cli/`conductor-run` path, so a11y ties in only through the verdict/state lamp crosswalk and the obs-envelope reproduction bind.

## Constraints
- The cli / `conductor-run` / `conductor-verify` surface is **not-assertable**: no DOM or widget tree, no axe-core / Lighthouse / pa11y reach; ASCII status prefixes and NO_COLOR text-pairing are output-stream discipline, never an a11y verification path (per a11y-plan §1 A11y Scope Summary + §11 Universal). No a11y gate may be asserted over this chunk's live legs.
- Every `verdict` / `state` value this chunk's leg rows emit must land on exactly one row of the six-lamp state-naming crosswalk, and HOLD is identified **verdict-first** — `verdict == CalibrationRegion` selects the HOLD lamp regardless of `state` (per a11y-plan §6 State color tokens). The family's severity-choice claim is asserted `CalibrationRegion`, so its rows join HOLD, not Manual.
- If a leg needs a value outside the crosswalk's six (a distinct auto-resolved, resolved-summary, or declare-only marker), §6 requires a crosswalk row plus a text label + glyph supplement before any lamp renders it — a token-tinted classification must never imply a seventh lamp state (per a11y-plan §6 State color tokens). Whether the envelope today carries only those six is research's question.
- a11y-plan §3 reproduces the obs §6 Log Format envelope **verbatim**; if this chunk alters that envelope's field set, §3's reproduction must be amended to track it (per a11y-plan §3 Structured violation JSON schema — the a11y↔obs schema bind).
- No WCAG conformance may be claimed without machine-verifiable axe-core / colorjs.io / Lighthouse / WebdriverIO JSON (per a11y-plan §2 Agent-runnable invariants + §11 Strategy). This chunk's leg-verdict / journal evidence is not a11y evidence and must not be worded as such.
- Should any part of this measurement later surface in the run-report view, an emptied active list must render as real prose, and an in-progress run must never be downgraded to a `Fail` treatment (per a11y-plan §11 Visual).

## Patterns to follow
- The §6 crosswalk table as the **join key** between a rendered lamp and its journal row (display label ↔ `verdict` enum ↔ `state` enum, verdict-first for HOLD) — the shape downstream consumers use to trace this chunk's rows (per a11y-plan §6).
- The §3 "fold into existing arrays, add no top-level fields" discipline — a11y violations ride inside `fingerprints[]` rather than widening the envelope; the same restraint applies to any extra dimension a leg wants to carry (per a11y-plan §3 Structured violation JSON schema).
- The not-assertable-surface pattern established for cli in §1: record the measurement as manual/output-stream discipline and exclude it from Sections 2 and 4 rather than inventing a gate (per a11y-plan §1).
- The zero-soft-SLO stance in §10 (no retry-once, no soft threshold; a real signal is never masked) — the domain parallel to this chunk's "never a vacuous green" retirement rule (per a11y-plan §10 SLO Invariants).

## Anti-patterns to avoid
- NEVER treat the cli surface as accessibility-assertable — its text-pairing is not an ARIA verification path (per a11y-plan §11 Universal).
- NEVER let a state/classification be conveyed by color alone, and never let a token-tinted classification cell be read as an uncovered seventh lamp state; the six-label assertion keys on **lamp** display labels (per a11y-plan §11 Visual + §6 State color tokens).
- NEVER claim WCAG conformance from evidence that produces no axe-core / colorjs.io / WebdriverIO JSON (per a11y-plan §11 Strategy).

## Contract bindings
- **a11y §6 State-naming crosswalk ↔ obs §6 envelope** — the `verdict` / `state` enums this chunk writes are the binding join for the six-lamp display labels; a11y narrows, obs owns the schema.
- **a11y §3 violation-schema reproduction ↔ obs §6 Log Format JSON Schema** — verbatim reproduction that must track obs field-set changes (D-a11y-obs-schema).
- **a11y §3/§9 harness ↔ tests §6 `@crabnebula/tauri-driver` + WebdriverIO session and tests' 5-command `logs`** — not exercised by this chunk; noted only so no parallel a11y automation stack is stood up alongside its live legs.

## Acceptance criteria contributions
- Every `verdict` / `state` value emitted by this chunk's leg rows maps to exactly one crosswalk row, and a `CalibrationRegion` verdict joins the HOLD lamp regardless of its `state` (per a11y-plan §6 State color tokens).
- If the obs §6 envelope's field set changes in this chunk, a11y-plan §3's verbatim reproduction is amended in the same wrap (per a11y-plan §3 Structured violation JSON schema).
- No a11y / WCAG conformance is claimed from this chunk's cli legs — the surface is not-assertable and emits no axe-core / colorjs.io / WebdriverIO JSON (per a11y-plan §1 A11y Scope Summary + §11 Universal).
- Any new classification introduced by a retirement or resolution outcome that a lamp or matrix cell would tint carries its own always-rendered text label and does not imply a seventh lamp state (per a11y-plan §6 State color tokens + §11 Visual).

## Relevant amendment history
- **2026-06-21-run-report-envelope-serializer** — §6 crosswalk gained the `CalibrationRegion → ManualCheck` co-occurrence and the verdict-first lamp-precedence rule. Directly relevant: this chunk asserts severity choice as `CalibrationRegion` and may retire checks to declare-only/`ManualCheck`, so the two now co-occur on its rows and HOLD must be identified on `verdict`, never on `state`.
- **2026-08-09-out-of-scope-classification-treatment** — §6 recorded that `--status-residual` also tints the coverage-matrix out-of-scope Mode cell as a coverage-**mode** classification, not a seventh lamp state, with its own `not-conductors` label. Relevant precedent for how this chunk's declare-only / out-of-scope retirements should be treated if they gain a visual treatment.
- **2026-06-16-emission-journal-writer** — §3's envelope reproduction gained `read_back_observed_at` to stay consistent with the obs §6 envelope. Relevant as the standing mechanism: this chunk writes per-leg journal rows through that envelope, and any field-set change there obliges the same tracking amendment.
