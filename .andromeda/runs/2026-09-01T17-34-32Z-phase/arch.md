# arch extract

## Relevance
**Relevant** — arch owns the workspace placement of every file this chunk touches, the frontend→cargo build-order coupling the probe's binary inherits, the port/process/env/command registries a WebDriver stack lands in, and the CARRY amendment to `architecture.md` §Established Decisions [Read-Back Dependency Posture] owed at this chunk's wrap.

## Constraints
- **Placement is fixed and non-Cargo.** The wdio config, the a11y specs and any driver script live under `crates/conductor-tauri/ui/` — an npm asset subtree that is explicitly **NOT a Cargo workspace member** (invisible to cargo), with `node_modules/` + `dist/` git-ignored and `package-lock.json` committed (per architecture.md §Occupied Resources — Frontend asset subtree; §Inherited Defaults — Frontend). Any harness-command edit belongs in `scripts/agent-run.{sh,ps1}`, not a new crate.
- **The probe's binary inherits a compile-time build-order coupling.** `tauri-build`'s `generate_context!` resolves `build.frontendDist` (`ui/dist`) at COMPILE time, so `npm run build` MUST precede any workspace cargo compile of `conductor-tauri`, and a bare `cargo build` does **not** run `beforeBuildCommand` — the ordering is wired as an `ensure_frontend` step into `scripts/agent-run.{sh,ps1}` (`run` verb) and the CI Rust job (per architecture.md §Infrastructure Patterns — Build system). Producing `target/release/conductor-tauri.exe` for the probe is subject to this; whether the shipped harness already sequences it for a release-profile build is research's question.
- **The GUI is convenience, never the release gate.** architecture.md §Design Philosophy ("Headless-drivable core, thin shells") and §Established Decisions [Deployment] require the headless `scripts/agent-run.sh` path to be the source of truth and the release gate, with the Tauri bundle explicitly "convenience, not a release gate". A "headful self-verify ⇒ ui-smoke becomes a Test Commands gate" outcome must not make a built GUI bundle a precondition of the headless release path.
- **The scope law's "no UI automation" non-goal is SUT-scoped, and this chunk sits on the boundary.** architecture.md §Cross-cutting Patterns — Scope law names "no UI automation" as a guard, while §Project Intent requires Pulse's **visual** claims to be verified "via an operator checklist". Driving Conductor's OWN webview is self-verification, not SUT automation — but the self-verify path must not become a route to driving or asserting Pulse's UI. Whether arch's wording needs a wrap qualifier scoping that non-goal to the SUT is a candidate amendment this chunk should record rather than assume.
- **Conductor owns no inbound listener.** architecture.md §Occupied Resources — Ports registers `127.0.0.1:4317` as an **egress target only**, states "no frontend dev-server port — the Tauri webview is bundled", and §Cross-cutting Patterns — Trust boundary names the port-occupier fault as the **single deliberate** case where Conductor binds a port. Any loopback port a `tauri-driver`/`msedgedriver` stack binds is a test-harness resource that must not collide with `:4317` and must not introduce an inbound surface into the shipped app.
- **Three registries take additions at wrap, not silently.** §Occupied Resources tracks launched child processes by name (today: the `andromeda-pulse-mcp` sidecar), the reserved `CONDUCTOR_*` env-var namespace (foreign vars are registered read-only, `ANDROMEDA_PULSE_*`-style, asserted not set by Conductor), and the complete Tauri command / route-equivalent set. A driver child process, a driver-availability handle, or a new `#[tauri::command]` used to expose an affordance each require registration (per architecture.md §Occupied Resources).
- **CI carries build+test only.** architecture.md §Established Decisions [CI/CD] and §Infrastructure Patterns — CI/CD approach pin GitHub Actions to `cargo build` / nextest / clippy on the dev OS target, with dynamic proof requiring live host state as an explicit **operator/local gate, not a CI gate**. A host-bound headful driver leg falls on the operator side of that line by the same reasoning; the chunk's boundary against re-scoping the a11y CI gate (v2-24) is consistent with this.

## Patterns to follow
- **Duplicate-occurrence amendment sweep.** Arch's repeated precedent (the 2026-08-15/16/20/21 and 2026-08-31 twin-site entries) is that a retired claim moves at **every** arch occurrence in one wrap, because a single-site apply leaves the retired reading alive in the twin. The scope's own instruction to RE-SWEEP rather than trust the 2026-09-01 site list is this pattern.
- **Build-order couplings ship as a harness step, not as guidance.** The `ensure_frontend` precedent (2026-06-24) put the frontend-before-cargo ordering into `scripts/agent-run.{sh,ps1}` + the CI job; a Windows self-verify stage should follow the same shape if it becomes a Test Commands gate.
- **Test/harness tooling stays out of arch §Stack.** The 2026-06-27 amendment explicitly **dismissed** registering `@crabnebula/tauri-driver` / axe / lighthouse into arch §Stack / §Inherited Defaults as over-reach — that roster lives in a11y-plan §3.5 and the stack.md distillation, while arch summarizes the frontend at React/Vite/Tailwind/npm grain. A driver devDep added here is therefore not an arch amendment.
- **New occupied resources get an individual §Occupied Resources bullet.** The `conductor-run` crate, `logs/conductor-tauri.jsonl`, and `CONDUCTOR_AGENT_MODE` precedents each landed as their own registry entry at wrap; the same applies to a driver process/port.
- **Frontend dependency gate is `npm audit --omit=dev`** with `package-lock.json` committed — production-dep strict, dev-only test-tooling advisories accepted at dev-tree grain (per architecture.md §Inherited Defaults — Frontend; §Stack — Desktop frontend row). Arch carries this as a derived one-liner of security-plan's authoritative rule.

## Anti-patterns to avoid
- **Do not add a Cargo workspace member (or make `ui/` one) for driver plumbing**, and do not introduce an inbound HTTP/dev-server surface into the shipped app — Conductor exposes no network service of its own (per architecture.md §Established Decisions [Backend Framework]; §Occupied Resources — Ports).
- **Do not generalize a single leg's observation into a plan-wide claim.** This is precisely the defect the CARRY corrects: a workspace-level dedupe claim was generalized from storms that happened to share one tuple (per architecture.md §Established Decisions [Read-Back Dependency Posture]). A probe result — success or failure — must be recorded with its measured mechanism and its scope, never as a platform-wide verdict.
- **Do not relax the platform gate into a silent Linux re-aim or a quietly widened claim** in the shipped specs/config; arch's headless-is-truth + operator-local-dynamic-proof rules mean an unmeasured path must stay declared unmeasured (per architecture.md §Design Philosophy; §Established Decisions [CI/CD]).

## Contract bindings
- **arch §Occupied Resources (Frontend asset subtree / ports / processes) ↔ tests + a11y harness:** the specs and `wdio.conf.ts` live in arch's locked subtree, but the driver tooling roster and the spec content are a11y-plan §3.5 / test-plan's, not arch §Stack (2026-06-27 precedent).
- **arch §Infrastructure Patterns (Build system — `ensure_frontend`, `generate_context!` ordering) ↔ tests harness commands:** a Test Commands gate for ui-smoke must ride the existing ordering in `scripts/agent-run.{sh,ps1}`.
- **arch §Infrastructure Patterns (Build system — `cargo deny` green vs `cargo audit` red on an external advisory-DB fault) ↔ security-plan §Dependency Security:** arch records the split only; the 41st-consecutive audit discharge is security's to own. Likewise the `npm audit --omit=dev` one-liners in arch §Stack / §Inherited Defaults are derived from security-plan and move in lockstep.
- **arch §Established Decisions [Read-Back Dependency Posture] ↔ the `conductor-run/tests/lifecycle_harvest.rs:23` module doc:** the CARRY amendment spans a plan site and a code-comment twin; arch owns the plan half.

## Acceptance criteria contributions
- (arch) All chunk artifacts land under `crates/conductor-tauri/ui/` or `scripts/agent-run.{sh,ps1}`; no new Cargo workspace member, and `ui/` remains a non-Cargo npm subtree with `package-lock.json` committed and `dist/`+`node_modules/` git-ignored (per architecture.md §Occupied Resources — Frontend asset subtree).
- (arch) The shipped app gains no inbound listener; any loopback port bound by the driver stack is recorded and provably distinct from `127.0.0.1:4317`, and no new env var lands outside the reserved `CONDUCTOR_*` namespace (per architecture.md §Occupied Resources — Ports / Environment variables).
- (arch) Any release-profile `conductor-tauri` build the probe consumes is produced with `ui/dist` built first (the `ensure_frontend` ordering), and a headful self-verify leg is declared an operator/local gate rather than added to the CI build+test job (per architecture.md §Infrastructure Patterns — Build system / CI-CD approach).
- (arch) The wrap amendment re-scopes "at most ONE incident is active per workspace" to the `(kind, scope, scope_id)` tuple citing the `inference_runtime.rs:811` predicate, narrows the spared-control and freshness-caveat consequences to WITHIN-tuple (cross-scope recorded as a weighable route option, not a retirement), and applies at every occurrence found by a fresh sweep — not the pre-swept list (per architecture.md §Established Decisions [Read-Back Dependency Posture]).

## Relevant amendment history
- **2026-08-31-p-075-assert-round (three entries)** — these are the entries that INTRODUCED the claim the CARRY retires: the one-active-incident dedupe constraint landed in §Established Decisions [Read-Back Dependency Posture], with twin restatements pushed into §Standard Contracts (readiness-gate carrier sentence) and §Occupied Resources (`ANDROMEDA_PULSE_DATA_DIR`, third occurrence). The wrap sweep should assume the twin sites those entries touched are candidates, since the generalization travelled with them.
- **2026-06-15-design-token-typography-bundle** — registered `crates/conductor-tauri/ui/` as the frontend asset subtree (not a Cargo member; npm + committed lockfile + audit gate). This is the placement authority for every file this chunk touches.
- **2026-06-24-frameless-window-shell** — registered the `generate_context!` → `build.frontendDist` compile-time coupling and its `ensure_frontend` remedy in `agent-run.{sh,ps1}` + CI, plus `logs/conductor-tauri.jsonl` and the Tauri-tree `deny.toml` additions; **dismissed** the Tauri capability perms as arch's concern (security-plan's domain). Directly governs how the probe's binary must be produced.
- **2026-06-27-desktop-a11y-harness-setup** — moved the npm gate to `--omit=dev` in lockstep with security-plan, and **dismissed** registering the a11y/driver devDeps (naming `@crabnebula/tauri-driver`) into arch §Stack / §Inherited Defaults as over-reach. Settles that a driver dependency added here is not an arch amendment.
- **2026-08-16-canary-fingerprint-derivation-aligned (deny.toml entry)** — recorded that `cargo deny` is green while `cargo audit` is separately red on an external advisory-DB fault, and that accepted exceptions are no longer Tauri-only. This is the arch-side framing the PREREQ's 41st-consecutive audit re-check reports against; the discharge itself is security's.
- **2026-08-08-dependency-advisory-remediation (closing note)** — recorded, as deliberately NOT-amended pre-existing gaps, that arch §Stack's Tauri pin reads "v2.10.x / latest 2.10.1" against a resolved 2.11.3 (alongside the tokio 1.48.x vs 1.52.3 gap). Relevant because this chunk works in the Tauri bundle path; it remains a handoff follow-up, not this chunk's to fix.
