```yaml
proposals:
  - detector: D-platform-claim
    severity: warning
    section: "§3 A11y Assertion Harness Contract → Screen reader test pattern → Per-surface test spec (line 268)"
    change: Retire "No automated SR tool exists for the stack." — the desktop-webview SR pass is AGENT-DRIVEN via NVDA's own speech log (`-l 12`) over the SAME `@crabnebula/tauri-driver` + WebdriverIO session, zero new dependencies; the operator's role is REVIEW; the browse-mode rows stay pending OS-level key injection; state the measured platform SET (NVDA/Windows agent-driven, measured 2026-09-02 with all three subjects attached; VoiceOver macOS / Orca Linux declared not-runnable-on-this-host, no pass claimed) and never a bare "Windows-only" capability bound.
    sidecar: "2026-09-02 — §3 SR test pattern: driver verdict \"No automated SR tool exists for the stack\" retired; NVDA's own speech log IS a driver (agent-driven, operator reviews, browse rows pending OS-level injection); macOS/Linux SR remain declared not-runnable-here (v2-23)."
    rationale: "Report `Spec claims disproved by measurement` #1 marks a11y-plan line 268 FALSE — NVDA's speech log plus the existing stack drove 33 of 51 rows `arm: agent` with zero new deps (`evidence/nvda-pass.json`, `attach_observed` true for all three subjects); Decisions record \"a screen reader's built-in speech log COUNTS AS A DRIVER\"; WRAP directive item 3 names this exact replacement wording."
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:47

  - detector: D-platform-claim
    severity: warning
    section: "§1 A11y Scope Summary → A11y assertion harness specification → Screen reader test pattern bullet (line 113)"
    change: Same retirement at the §1 restatement — drop "Manual, supplemental (never sole)" as the NVDA classification and the trailing "No automated SR tool for the stack."; the NVDA leg is agent-driven via its speech log with the operator reviewing, still supplemental to the axe-core baseline (never sole), with VoiceOver/Orca unchanged as declared-not-runnable-here.
    sidecar: "2026-09-02 — §1 harness spec SR bullet: same driver-verdict retirement as §3; NVDA reclassified manual → agent-driven (speech log), \"supplemental, never sole\" standing kept."
    rationale: "Report disproved-claim #1 cites line 113 by name alongside line 268; a single-site apply at §3 would leave the identical verdict standing in §1's harness spec."
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:47
    dependent-of: D-platform-claim

  - detector: D-platform-claim
    severity: warning
    section: "§1 A11y Scope Summary → A11y surfaces &amp; assistive tech reach → desktop-webview → \"Manual verification (supplemental)\" (line 83)"
    change: Split the label so NVDA (Windows) is no longer filed under manual verification — NVDA is an agent-driven SR path (speech log, operator reviews); VoiceOver (macOS) / Orca (Linux) stay the manual/not-runnable-on-this-host half; keep "supplemental to automated, never sole" for both.
    sidecar: "2026-09-02 — §1 assistive-tech reach: NVDA (Windows) moved out of the \"Manual verification\" grouping to agent-driven; VoiceOver/Orca remain manual-declared."
    rationale: "Third restatement of the same retired classification (the report's operator wording is \"agent-driven via NVDA's speech log; operator reviews\"); leaving it makes the surfaces table contradict §3."
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:78
    dependent-of: D-platform-claim

  - detector: D-platform-claim
    severity: warning
    section: "§3 A11y Assertion Harness Contract → Screen reader test pattern → \"Manual pass spec format\" (line 269)"
    change: Retitle to a pass-spec format that is agent-produced (per-state spec rows graded from the speech log, operator review recorded per row) — retire only the "Manual" classifier on this line; leave the per-state naming and the must-announce strings on this line untouched (a different detector owns report items #2/#3).
    sidecar: "2026-09-02 — §3 \"Manual pass spec format\" → pass-spec format (agent-produced rows, operator review); classifier only, strings untouched."
    rationale: "Same retired \"manual\" classification, restated as the section's own format heading; the shipped `nvda-pass-spec.md` + `parse-nvda-log.ts` produce the rows and `operator_review` records the review."
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:47
    dependent-of: D-platform-claim

  - detector: D-platform-claim
    severity: warning
    section: "§3 A11y Assertion Harness Contract → Bootstrap phases → screen-reader-test-spec-setup (line 298)"
    change: "\"manual pass-spec files\" → per-state pass-spec files driven from NVDA's speech log (agent arm) with operator review; VoiceOver/Orca stay declared-only on this host; \"supplemental, not CI-gated\" is unchanged."
    sidecar: "2026-09-02 — §3 bootstrap screen-reader-test-spec-setup: \"manual pass-spec files\" retired to agent-driven pass-spec files (operator reviews); not-CI-gated standing kept."
    rationale: "Report disproved-claim #1 cites line 298 explicitly; route/setup-project read this bootstrap list, so a surviving \"manual\" here re-seeds the retired verdict downstream."
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:47
    dependent-of: D-platform-claim

  - detector: D-platform-claim
    severity: warning
    section: "§3 A11y Assertion Harness Contract → Focus management test harness → Driver (line 252)"
    change: Replace the bare "Runs on the Linux+xvfb target alongside tests' webview E2E." with the measured platform SET this doc already carries at §1/§3-bootstrap/§9/§11 — the focus/keyboard harness runs on Linux under `xvfb` AND headfully on the Windows WebView2 host (its carrier, the operator-only driven arm, re-measured there 2026-09-02 11:55Z); macOS alone lacks a WebDriver; `ubuntu-latest`+`xvfb` is the CI ARRANGEMENT, not a capability bound.
    sidecar: "2026-09-02 — §3 focus-harness Driver: residual single-platform literal \"Runs on the Linux+xvfb target\" retired to the measured platform SET (Linux+xvfb AND Windows WebView2 host; macOS no WebDriver)."
    rationale: "The keyboard/focus half rides the `a11y:driven` suite (§9 line 465), which the report measures running headfully on this Windows host at the wrap light gate (per-suite env, journal and self-obs log all landed under `runs/driven/`). This is the one site the 2026-09-01 platform-SET retirement missed — lines 115, 299, 457 and 565 already name the set, so this literal is a duplicate occurrence that survived a single-site apply. Not the ui-test-README sentence, which report item #8 CARRIES rather than amends."
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:91

# D-a11y-surface — no drift. The report adds no interactive UI element: `.tsx`/`.css`
# untouched and zero `.rs` delta (Files → NOT touched), "Ports / IPC / MCP / commands:
# none added or changed", every new file being harness/spec/evidence under
# `crates/conductor-tauri/ui/test/a11y/screen-reader/`. The detector's trigger
# ("if the report adds an interactive UI element") is not met, so §4/§5/§6 coverage of the
# four must-be-accessible paths is unchanged. (Report items #3/#4/#5 — must-announce
# strings, the absent `contentinfo` footer, the non-shipping second checklist render site,
# the dialog title shape — are shipped-vs-spec gaps on EXISTING elements, not elements this
# chunk adds; out of this invariant's scope.)
#
# D-a11y-obs-schema — no drift. Report Schema/config states verbatim: "No migration, no
# `runs.db` change, no violation-schema change, no obs log-schema change." The new
# `nvda-pass.json` is a chunk evidence record, not the a11y violation envelope, so §1/§3's
# obs-§6-aligned violation schema and the obs log schema are both unmoved and still match.
```
