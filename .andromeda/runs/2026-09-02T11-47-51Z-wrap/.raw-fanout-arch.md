I have everything I need. Here are my findings.

```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — Environment variables
    change: Add a `CONDUCTOR_NVDA` bullet beside `CONDUCTOR_MSEDGEDRIVER` — the portable NVDA executable path the screen-reader leg hands to `startNvda`/`stopNvda`, read ONLY by `crates/conductor-tauri/ui/wdio.conf.ts` (never by a shipped binary), validated at the harness edge exactly like `CONDUCTOR_MSEDGEDRIVER` (existence + `isFile` + shell-metacharacter rejection) and passed as a separate argv element of an array-form spawn, never interpolated into a shell; unset or not-an-existing-file makes the `sr*` suites SKIP at exit 0 with a host-path-free precondition plus a fetch recipe, and the value is never logged, echoed or committed.
    sidecar: Registered `CONDUCTOR_NVDA` in §Occupied Resources env vars (host dev-tool handle for the screen-reader leg; skip-at-exit-0 guard, array-form spawn, value never committed).
    rationale: The report's Changes → Symbols/APIs → "Env handles" bullet lands `CONDUCTOR_NVDA` as a NEW handle in the reserved `CONDUCTOR_*` namespace (guard measured 2026-09-02 11:53Z — `env -u CONDUCTOR_NVDA npm run a11y:sr-empty` → exit 0, no process started); arch's env-var registry lists every other `CONDUCTOR_*` handle but not this one, so a resource the chunk landed is unregistered.
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:19

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — Environment variables (`CONDUCTOR_MSEDGEDRIVER` bullet)
    change: Replace "The one handle in this namespace naming a HOST dev-tool this document does not itself define" with "One of TWO handles in this namespace naming a HOST dev-tool this document does not itself define (`CONDUCTOR_MSEDGEDRIVER` · `CONDUCTOR_NVDA`), both validated and spawned the same way, so neither value is ever committed."
    sidecar: Retired the "one HOST dev-tool handle" singular in the `CONDUCTOR_MSEDGEDRIVER` bullet — the namespace now carries two (`CONDUCTOR_MSEDGEDRIVER` · `CONDUCTOR_NVDA`).
    rationale: Report Counts/qualifiers moved — "Host dev-tool handles in the `CONDUCTOR_*` namespace: 1 → 2 (`CONDUCTOR_MSEDGEDRIVER` + `CONDUCTOR_NVDA`)", naming architecture §Occupied Resources env vars as a doc stating ONE. Without this the singular survives the `CONDUCTOR_NVDA` registration and contradicts it in the same section.
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:32
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — On-disk artifacts (`logs/conductor-tauri.jsonl` bullet)
    change: Replace the per-ARM landing-site sentence with a per-SUITE one — the one tauri-driver spawn site now chooses `CONDUCTOR_RUNS_DIR` per suite, so the sink (`runs_dir.parent()/logs`) lands routine `--e2e` and `sr-empty` at `runs/logs/conductor-tauri.jsonl` (measured 2026-09-02 10:46:10Z), the `a11y:driven` arm at `runs/driven/logs/conductor-tauri.jsonl` (measured 11 457 B, 2026-09-02 11:55:29Z) and `sr`/`sr-error` at `runs/sr-leg/logs/conductor-tauri.jsonl` (measured 8 312 B, 2026-09-02 10:44:45Z); no a11y suite leaves the handle unset any more, so the workspace-root `logs/conductor-tauri.jsonl` is a retired a11y landing site (the unset ⇒ CWD-relative mechanism itself is unchanged), and all sites stay git-ignored.
    sidecar: Retired "the `a11y:driven` arm leaves the handle unset and lands at the workspace-root `logs/`" — the self-obs landing site is now per-SUITE (`runs/logs/` · `runs/driven/logs/` · `runs/sr-leg/logs/`), each measured 2026-09-02.
    rationale: Report Counts/qualifiers moved bullet 3 and Spec-claims-disproved item 7 name this exact architecture bullet as retired by construction; the driven arm's site was re-measured at this wrap's light gate (report Outcome: journal + `runs.db` under `runs/driven/runs/`, self-obs log at `runs/driven/logs/conductor-tauri.jsonl`), so the "derived" qualifier is also gone.
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:91

  - detector: D-arch-decisions
    severity: warning
    section: Cross-cutting Patterns — Scope law
    change: Replace "ships as TWO suites over ONE WebdriverIO + tauri-driver stack — the dev-only `--e2e` self-verify routine arm and the operator-local `a11y:driven` driven arm" with "ships as THREE suite families (four `wdio` suites) over ONE WebdriverIO + tauri-driver stack — the dev-only `--e2e` self-verify routine arm, the operator-local `a11y:driven` driven arm, and the operator-local screen-reader leg (`sr` / `sr-empty` / `sr-error`, NVDA on the Windows dev host)", keeping "never a second automation stack" verbatim.
    sidecar: Scope law: webview suite families 2 → 3 over the SAME one WebdriverIO + tauri-driver stack (the `sr*` screen-reader leg joins); "never a second automation stack" unchanged.
    rationale: Report Counts/qualifiers moved bullet 1 — "Webview suite FAMILIES over the one WebdriverIO + tauri-driver stack: 2 → 3 (+ the `sr*` screen-reader leg, three suites); wdio `suites` entries 1 → 4" — and it names architecture §Cross-cutting Patterns Scope law as a doc stating TWO. The locked "ONE stack / never a second automation stack" clause is NOT contradicted (report Dependencies: none; zero new driver), only the count literal.
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:31

  - detector: D-arch-decisions
    severity: warning
    section: Occupied Resources — Ports (`127.0.0.1:4444` / `:4445` bullet)
    change: Replace "The stack is shared by BOTH arms of that leg" with "The stack is shared by ALL THREE suite families of that leg — the unattended routine arm, the operator-local driven arm, and the operator-local `sr` / `sr-empty` / `sr-error` screen-reader suites (which additionally bracket NVDA's start/quit around the driver) — each binding and releasing the pair with its own session"; the harness-lifetime, workspace-root-`cwd` and "distinct from `:4317`" facts are unchanged.
    sidecar: §Occupied Resources Ports: 4444/4445 are now shared by three suite families, not two — the `sr*` screen-reader suites bind and release the same pair.
    rationale: Same count move (report Counts/qualifiers moved bullet 1 explicitly lists architecture §Occupied Resources Ports "shared by BOTH arms" as a doc stating TWO); the `sr*` suites run the same tauri-driver stack (report Harness/gate surface — "NVDA start/quit bracketing tauri-driver"), so leaving "BOTH arms" would let the retired count survive in the registry section.
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:38
    dependent-of: D-arch-decisions

  - detector: D-arch-decisions
    severity: warning
    section: Cross-cutting Patterns — Trust boundary
    change: Replace "The dev-only webview a11y legs — the routine `--e2e` arm and the operator-local `a11y:driven` arm — additionally spawn tauri-driver / the native WebDriver" with an enumeration of all three families (routine `--e2e`, `a11y:driven`, and the operator-local `sr*` screen-reader suites), noting the screen-reader suites additionally spawn the host NVDA named by `CONDUCTOR_NVDA` and a fixed-argv PowerShell window-activation script — neither opening a listener, so the loopback `4444`/`4445` pair remains the whole added surface and is gone with the harness.
    sidecar: Trust boundary: the webview a11y legs are now three suite families; the `sr*` leg adds host-tool child processes (NVDA, a fixed-argv PowerShell activation script) but no new listener.
    rationale: Same retired two-arm enumeration as the Scope law; this bullet restates it as the trust-boundary inventory, and the report's Harness/gate surface bullet adds NVDA and the activation script as leg-spawned processes (census recorded per subject) with "Ports / IPC / MCP / commands: none added or changed".
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:38
    dependent-of: D-arch-decisions

  - detector: D-arch-decisions
    severity: warning
    section: Occupied Resources — Environment variables (`CONDUCTOR_MSEDGEDRIVER` bullet)
    change: Replace "the ONE config both arms fire, so the handle gates the routine `--e2e` arm and the operator-local `a11y:driven` arm alike" with "the ONE config all three suite families fire, so the handle gates the routine `--e2e` arm, the operator-local `a11y:driven` arm and the `sr*` screen-reader suites alike"; the skip-at-exit-0 and never-shell-interpolated facts are unchanged.
    sidecar: `CONDUCTOR_MSEDGEDRIVER` bullet: the handle now gates three suite families off the one wdio config, not two arms.
    rationale: Third restatement of the retired two-arm count inside the env-var registry — the `sr*` suites fire the same `wdio.conf.ts` and the same `nativeDriver()` path (report: "Unchanged and still the sole callers: `nativeDriver()` / `reportSkip()` … the tauri-driver spawn stays the ONE site"), so a Scope-law-only apply would leave the count contradicted here.
    basis: D:\dev\projects\conductor\conductor-0.2.0\chunks\2026-09-02-screen-reader-manual-spec\report.md:14
    dependent-of: D-arch-decisions
```

Notes on the third detector: **D-platform-claim yields no arch proposals.** I grepped `.andromeda/architecture.md` for every platform/runner/driver verdict (`Linux`, `macOS`, `Windows`, `xvfb`, `headless only`, `only on`, `not runnable`, `no driver`) — the only hits are `Windows`/`Linux` as data-dir platform defaults (line 188), the `run_id` filename-colon constraint (line 138), the gtk-rs Linux-webview advisory path (line 194), and three non-platform `only on` matches in the garde/TOML decisions (lines 50, 51, 84). Arch states no capability verdict about where the a11y or screen-reader leg can run, so the report's `Spec claims disproved` item 8 (the ui test README's "runs only on Linux + xvfb", explicitly a pre-existing non-master falsehood and a route-resolve CARRY) has no arch-side occurrence to retire. Arch's CI statements (line 148 "live Pulse, never CI"; lines 59/225 "not a CI gate, because CI has no Pulse instance") are CI *arrangement*, not capability, and the report's "never a CI stage" for the `sr*` suites agrees with them.
