Verified: the retired strings occur exactly twice in design-system (lines 257 and 263), shipped values confirmed at `App.tsx:245` / `App.tsx:305`, and no platform/driver verdict in design-system is falsified by the report.

```yaml
proposals:
  - detector: D-design-derived-count
    severity: warning
    section: "Surface: desktop-webview → Component Patterns → 3. Coverage matrix (dense single-row-per-P-ID) — Empty state"
    change: The empty state's baked sample `"No scenarios loaded"` becomes the string the app actually renders — `"No scenarios found."` (as shipped, `App.tsx`) — kept as a prose line, never a gray placeholder; attribute the literal to the shipped component so a future rename is a code change, not a doc restale.
    sidecar: "§Component Patterns 3 — empty-state sample updated from `No scenarios loaded` to the shipped `No scenarios found.` (measured, 2026-09-02 SR pass, E0 rows)."
    rationale: "Report §Spec claims disproved by measurement item 3: `\"No scenarios loaded\" → shipped `No scenarios found.` (heard, E0 rows)`, and the same item names design-system §Component Patterns as restating the retired strings. The doc bakes a literal the code no longer produces."
    basis: crates/conductor-tauri/ui/src/App.tsx:245 (heard: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/evidence/nvda-pass.json:912)

  - detector: D-design-derived-count
    severity: warning
    section: "Surface: desktop-webview → Component Patterns → 6. Run-report view — Empty (no run started)"
    change: The empty-state sample `"No run yet — pick a scenario/suite to begin"` becomes the shipped `"No run yet"` (the trailing pick-a-scenario clause does not ship); keep the `--text-tertiary` prose-not-skeleton rule and attribute the literal to the shipped component.
    sidecar: "§Component Patterns 6 — run-report empty-state sample trimmed to the shipped `No run yet` (the `— pick a scenario/suite to begin` tail never shipped; measured 2026-09-02)."
    rationale: "Report §Spec claims disproved by measurement item 3: `\"No run yet — pick a scenario/suite to begin\" → shipped `No run yet``, recorded as a divergence row on the SR pass; design-system §Component Patterns is named there as restating the same strings."
    basis: crates/conductor-tauri/ui/src/App.tsx:305 (heard: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/evidence/nvda-pass.json:413)

  - detector: D-design-derived-count
    severity: warning
    section: "Surface: desktop-webview → Component Patterns → 6. Run-report view — In-progress (run not finished)"
    change: Retire the claim that the report area shows `"Run in progress"` prose — no such prose ships; state instead that during a live run the report area stays empty and the live signal is carried by the titlebar (phase line + count, label `Conductor · live`) plus the matrix, with the "no result yet" state still never downgraded to a red `Fail`.
    sidecar: "§Component Patterns 6 — `Run in progress` report-area prose retired (does not ship); the in-progress signal is named as the titlebar count/phase line + matrix (measured 2026-09-02, S1 rows)."
    rationale: "Report §Spec claims disproved by measurement item 3: `\"Run in progress\" — no such prose ships (the live state carries only the titlebar label `Conductor · live` + the count; S1 rows)`; the SR record carries it as a `node: (none)` row. The doc states a rendered string the code does not produce."
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/evidence/nvda-pass.json:482
```

Notes on the two detectors that did not fire:
- **D-design-tokens** — no new UI this chunk: the report's Files bullet records `.tsx` / `.css` as NOT touched (`git diff --stat`), and every row of `Coverage of new surfaces` reads `tokens n/a` (harness/env/evidence surfaces only). No `hardcoded✗`, no color-only state.
- **D-platform-claim** — design-system's only platform lines are `.andromeda/design-system.md:195` (desktop-webview: Windows + macOS + Linux) and `:282` (cli: Windows / macOS / Linux terminal, headless `conductor-cli`); neither is retired by the report. The verdicts the report retires — "No automated SR tool exists for the stack" (a11y-plan §1/§3) and the ui test README's "runs only on Linux + xvfb" (a pre-existing non-master falsehood, carried) — have no occurrence in design-system (grep for `screen reader|NVDA|VoiceOver|Orca|xvfb` returns only "screen-reader friendliness" at `:321`, a NO_COLOR/text-prefix rule, not a platform verdict).
