Verified against the shipped source. Here are the findings.

```yaml
proposals:
  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: desktop-webview → Primary screens"
    change: name the shipped `RunState` SET as the four screens — `idle` (with idle-with-report as a sub-state, the render the current "Run report (terminal)" bullet describes) · `live` · `hold` · `aborted` — and note that `aborted` currently has no wireframe entry, rather than listing "Run report (terminal)" as the fourth state.
    sidecar: "2026-09-02 — §Primary screens: four-state list retired from `idle / live / HOLD / report-terminal` to the shipped `idle · live · hold · aborted` set; report-terminal recorded as an idle sub-state (v2-23 SR pass)."
    rationale: report "Counts / qualifiers moved" — "the a11y-plan's run-state set (idle / live / HOLD / report-terminal) vs shipped (`idle` / `live` / `hold` / `aborted`, with idle-with-report a sub-state) — 4 → 4 but a different SET"; disproved-claim 2 measured the set off `Titlebar.tsx` and heard `Conductor · aborted` on the S3 rows.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\Titlebar.tsx:4

  - detector: D-layout-derived-count
    severity: warning
    section: "Wireframe — Run report (terminal)"
    change: retitle the wireframe to the state it actually renders (idle-with-report) and drop `report (terminal)` as a state name, so the heading stops asserting a fifth/renamed run state alongside the shipped four.
    sidecar: "2026-09-02 — §Wireframe heading: `Run report (terminal)` re-labelled as the idle-with-report render (shipped RunState set is idle · live · hold · aborted)."
    rationale: same counts-bullet entry as the primary — the wireframe heading restates the retired fourth-state name; docs must not keep a state name the shipped `RunState` union does not carry.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\Titlebar.tsx:4
    dependent-of: D-layout-derived-count

  - detector: D-layout-derived-count
    severity: warning
    section: "Decisions Log → Key layout choices → desktop-webview"
    change: replace `four "screens" are one window in different run states (idle / live / HOLD / report)` with the shipped set `(idle · live · hold · aborted, idle-with-report a sub-state of idle)`.
    sidecar: "2026-09-02 — Decisions Log key-layout-choices: run-state parenthetical updated to the shipped set."
    rationale: the decisions-log bullet restates the same retired four-state naming the primary retires; a single-site apply would leave the stale set standing in the log's standing-choice bullet.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\Titlebar.tsx:4
    dependent-of: D-layout-derived-count

  - detector: D-layout-derived-count
    severity: warning
    section: "Component — Header (frameless titlebar + Paused-count heartbeat) → Left — phase line"
    change: state that the phase line renders one of four fixed state labels (`Conductor · idle` / `· live` / `· HOLD — operator pause` / `· aborted`) from the titlebar's label map — it never names the operational segment, so drop `error-baseline-spike` / `fingerprint-storm` as phase-line samples.
    sidecar: "2026-09-02 — §Component — Header: phase-line sample corrected to the shipped four-label `Conductor · {state}` map (scenario-name samples retired)."
    rationale: disproved-claim 2/3 measured the titlebar labels (`Conductor · live` heard on S1, `Conductor · aborted` on S3; the live state "carries only the titlebar label `Conductor · live` + the count") — the doc bakes a scenario-name phase line the code does not produce.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\Titlebar.tsx:7

  - detector: D-layout-derived-count
    severity: warning
    section: "Wireframes — Run console (idle) / Run console (HOLD) / Run report (terminal) — titlebar rows"
    change: replace the titlebar phase-line cells `error-baseline-spike` (idle) and `run complete` (report) with the shipped label form `Conductor · {state}`; the HOLD row's `HOLD — operator pause` is correct only as the suffix of `Conductor · HOLD — operator pause`.
    sidecar: "2026-09-02 — Wireframe titlebar rows: phase-line samples aligned to the shipped `Conductor · {state}` labels."
    rationale: three wireframe samples restate the phase-line wording the primary retires; `run complete` in particular is not a shipped `RunState` label at all.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\Titlebar.tsx:7
    dependent-of: D-layout-derived-count

  - detector: D-layout-derived-count
    severity: warning
    section: "Component — Primary content block 2 (run-report view + verdict lamp) → Distinct non-result states"
    change: the Empty state renders the shipped `No run yet` (not "No run yet — pick a scenario/suite to begin"), and the In-progress entry must be deleted — no "Run in progress" prose ships; the live state is carried by the titlebar label + count only.
    sidecar: "2026-09-02 — §Primary content block 2: empty-state sample corrected to shipped `No run yet`; never-shipped `Run in progress` state removed (v2-23 S1/E0 rows)."
    rationale: report disproved-claim 3 names this section explicitly — "'No run yet — pick a scenario/suite to begin' → shipped `No run yet`; 'Run in progress' — no such prose ships … layout-templates §Primary content block 2 restate the same strings".
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\App.tsx:305

  - detector: D-layout-derived-count
    severity: warning
    section: "Wireframe — Run console (idle)"
    change: the run-report empty-state row must read the shipped `No run yet`, not `No run yet — pick a scenario/suite to begin.`
    sidecar: "2026-09-02 — idle wireframe: empty-state row updated to the shipped `No run yet` string."
    rationale: the idle wireframe repeats the exact retired literal; a fix confined to §Primary content block 2 leaves the stale sample rendered in the wireframe.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\App.tsx:305
    dependent-of: D-layout-derived-count

  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: desktop-webview → Primary screens (idle bullet + Run report bullet)"
    change: drop the `"Run in progress"` state from the run-report bullet and refer to the empty prose by the shipped string `No run yet` in both bullets.
    sidecar: "2026-09-02 — §Primary screens bullets: `Run in progress` removed, `no run yet` prose aligned to the shipped string."
    rationale: both bullets restate the strings the primary retires — the run-report bullet names "the distinct 'Run in progress' / 'no run yet' states", and the never-shipped one would survive a single-site apply.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\App.tsx:305
    dependent-of: D-layout-derived-count

  - detector: D-layout-derived-count
    severity: warning
    section: "Component — Primary content block 1 (coverage matrix) → States"
    change: the matrix Empty state renders the shipped `No scenarios found.` — replace the baked `No scenarios loaded` prose (cite the component as the string's source rather than restating a second literal).
    sidecar: "2026-09-02 — §Primary content block 1: empty-matrix sample corrected to the shipped `No scenarios found.` (heard on the E0 rows)."
    rationale: report disproved-claim 3 — "'No scenarios loaded' → shipped `No scenarios found.` (heard, E0 rows)"; the doc bakes the pre-ship literal in a state caption.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\App.tsx:245

  - detector: D-layout-derived-count
    severity: warning
    section: "Wireframe — Run console (HOLD) — signature moment (dialog block)"
    change: the hold dialog's shipped title/description are `{p_id} — operator-checklist` and `Observe the operator-checklist claim for this scenario` (backend-supplied hold prompt) — the sample header `HOLD — operator pause` and body `Commit next timeline step: … / Proceeding fires the cue against the live Pulse.` are not what renders.
    sidecar: "2026-09-02 — HOLD wireframe: dialog header/body sample replaced with the shipped operator-checklist hold prompt (heard, S2 rows)."
    rationale: report disproved-claim 5 — "The dialog title shape: shipped `{p_id} — operator-checklist` with description `Observe the operator-checklist claim for this scenario` (heard, S2 rows) where the plans' narrative names it differently".
    basis: D:\dev\projects\conductor\crates\conductor-run\src\lib.rs:538

  - detector: D-layout-derived-count
    severity: warning
    section: "Component — Hero / signature section (operator-pause go/no-go dialog) → Header / Body"
    change: note that Header and Body render the backend-supplied hold prompt (title `{p_id} — operator-checklist`, description `Observe the operator-checklist claim for this scenario`), so the narrative stops implying a "commit next timeline step" header/body of its own.
    sidecar: "2026-09-02 — §Hero: dialog Header/Body described as the backend hold-prompt render (shipped title/description)."
    rationale: this section restates the dialog-copy claim the primary retires; leaving it makes the narrative and the wireframe disagree after a single-site apply.
    basis: D:\dev\projects\conductor\crates\conductor-run\src\lib.rs:538
    dependent-of: D-layout-derived-count

  - detector: D-layout-surface
    severity: warning
    section: "Component — Footer (status strip)"
    change: mark the footer status strip as NOT shipped in the release DOM as of 2026-09-02 — the measured landmark inventory is banner + main + two regions with no `contentinfo` strip, so the seed/phase/roll-up line is a designed-but-unbuilt region, not a described shipped one.
    sidecar: "2026-09-02 — §Component — Footer: recorded as absent from the shipped DOM (no `contentinfo` strip; landmarks = banner + main + two regions), v2-23 SR spec row."
    rationale: report disproved-claim 4 names this section — "a11y-plan §4 landmark inventory (line 320) + §11 (line 546) + layout-templates §Component — Footer: a `contentinfo` footer status strip — ABSENT from the shipped DOM (recorded as a spec row; the landmark list is banner + main + two regions)"; confirmed by no `&lt;footer&gt;`/`contentinfo` anywhere under the ui src tree.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\App.tsx:245

  - detector: D-layout-surface
    severity: warning
    section: "Wireframes — Run console (idle) / Run console (HOLD) / Run report (terminal) — footer rows"
    change: annotate the three footer rows as the unbuilt status strip (not shipped as of 2026-09-02) so the wireframes stop depicting a region absent from the release DOM.
    sidecar: "2026-09-02 — Wireframe footer rows: flagged as the unbuilt status strip pending the shipped `contentinfo` region."
    rationale: each of the three desktop-webview wireframes restates the footer strip the primary retires; a single-site apply on §Component — Footer would leave three wireframes still drawing it as shipped.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\App.tsx:245
    dependent-of: D-layout-surface

  - detector: D-layout-surface
    severity: warning
    section: "Component — Operator-checklist → footer roll-up sentence"
    change: attribute the unticked roll-up to the checklist view's own `role=status` line (where it ships) rather than to the window footer strip, which is not in the shipped DOM.
    sidecar: "2026-09-02 — §Operator-checklist: unticked roll-up re-attributed from the window footer to the checklist view's own status line."
    rationale: "The footer roll-up surfaces the unticked count" repeats the shipped-footer claim the primary retires; the roll-up measurably lives inside the checklist view, not a `contentinfo` strip.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\OperatorChecklistView.tsx:18
    dependent-of: D-layout-surface

  - detector: D-layout-surface
    severity: warning
    section: "Component — Operator-checklist (the `ManualCheck` render — drive+observe surface)"
    change: state that only ONE render site ships in release — the operator-pause dialog — and that the second (run-report) site is not built in the release bundle (the only other mount is the dev gallery), so the "SAME primitive renders in two contexts" framing is a design intent, not a shipped surface.
    sidecar: "2026-09-02 — §Operator-checklist: second (report-site) render recorded as not shipping in release; dialog site is the only shipped mount (row S2-08)."
    rationale: report disproved-claim 4 — "The operator checklist's second (report-site) render site (a11y-plan §4 / layout-templates §Component — Operator-checklist) — does NOT ship in release (only the dialog site; row S2-08 `subject-absent` sibling recorded)"; confirmed by the component's only app mount being `OperatorPauseDialog.tsx:57`.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\OperatorPauseDialog.tsx:57

  - detector: D-layout-surface
    severity: warning
    section: "Component — Primary content block 2 → ManualCheck clause"
    change: qualify "**ManualCheck** → renders as an operator-checklist item … see the Operator-checklist component below" as not shipping at the report site — the checklist primitive mounts only in the hold dialog in release.
    sidecar: "2026-09-02 — §Primary content block 2: ManualCheck clause qualified — the report-site checklist render is unbuilt."
    rationale: this clause restates the report-site render the primary retires; left alone it keeps asserting a run-report checklist surface that row S2-08 recorded as subject-absent.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\OperatorPauseDialog.tsx:57
    dependent-of: D-layout-surface

  - detector: D-layout-surface
    severity: warning
    section: "Wireframe — Run report (terminal) — the `☐ P-035 pii-scrub Manual` row"
    change: annotate the Manual row's checklist rendering as unbuilt at the report site (the lamp/verdict line ships; the induced-state / expected-observation checklist render does not).
    sidecar: "2026-09-02 — Run-report wireframe: Manual row's checklist render flagged as unbuilt at the report site."
    rationale: the wireframe draws the report-site checklist render the primary retires, so the claim would survive a single-site apply on §Component — Operator-checklist.
    basis: D:\dev\projects\conductor\crates\conductor-tauri\ui\src\components\OperatorPauseDialog.tsx:57
    dependent-of: D-layout-surface
```

Notes on the detectors that produced no proposals:

- **D-platform-claim — clean.** The only platform wording in `D:\dev\projects\conductor\.andromeda\layout-templates.md` is the window-control OS convention (line 107, restated line 325: "Standard buttons on Windows/Linux, traffic-light-style on macOS"), which is a design convention, not a runner/driver capability verdict, and is untouched by the report. The doc states no "Linux only" / "headless only" / CI-runner verdict; the `--e2e` bullet (line 187) gates on "a Conductor-owned driver handle", which stays accurate — the `sr*` suites are never an `agent-run` verb, so the 1 → 2 handle move does not falsify it. The retired README claim ("runs only on Linux + xvfb") lives in `crates/conductor-tauri/ui/test/README.md`, is explicitly a route-resolve CARRY per report §Spec claims disproved item 8, and is not this doc's.
- **Suite-count entries** from the counts bullet (2 → 3 families, wdio `suites` 1 → 4, `a11y*` scripts 2 → 5) have **no occurrence** in layout-templates — the report's own list of docs stating TWO does not include it, and the doc states no suite count.
