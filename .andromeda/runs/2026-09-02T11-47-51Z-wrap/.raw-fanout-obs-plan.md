Both documents read, and I've grepped obs-plan for every occurrence of the wordings in play. Here is my assessment.

**D-obs-stack** — clean. Report's **Dependencies** bullet: "none — `package-lock.json` and `Cargo.lock` byte-unchanged (gate 3)"; **Ports / IPC** bullet: "no OTLP change". The leg's driver is NVDA's own `-l 12` speech log plus node built-ins; `@guidepup/guidepup` was rejected. The one OTLP string in the report (`preflight blocked: canary emission failed (OTLP transport error)`, report.md:91) is the PRODUCT fault stream at `:4317` with Pulse down — exactly §3's split. No self-obs SDK/exporter added.

**D-platform-claim** — clean. obs-plan states platforms only as a SET ("Windows/macOS/Linux", lines 49/147) and never carries a "Linux only" / "no driver on Y" verdict. The retired "runs only on Linux + xvfb" sentence lives in the ui test README (report.md:54, an explicit non-master CARRY), not here; §9 CI Integration never claims the webview suites are a CI stage, so "never a CI stage" retires nothing in this doc.

**D-obs-instrumentation** — one drift, duplicated 7 ways. **D-obs-redaction** — one measured exposure.

```yaml
proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§3 Log file location"
    change: "Tauri backend: `&lt;CONDUCTOR_RUNS_DIR&gt;/../logs/conductor-tauri.jsonl` — the file NAME is fixed, the DIRECTORY rides the runs-dir handle (`runs_dir.parent()/logs`), so the sink moves with the suite; measured 2026-09-02 at three sites in one day (`runs/logs/` routine + `sr-empty`; `runs/sr-leg/logs/` 8312 B; `runs/driven/logs/` 11 457 B), never the project-root `logs/`."
    sidecar: "§3 Log file location — Tauri backend sink qualified as runs-dir-relative (`runs_dir.parent()/logs`), naming the three measured landing sites; the bare project-root literal was never the shipped location."
    rationale: "Report Changes / Counts-qualifiers bullet 3 makes the self-obs sink's dependence on `CONDUCTOR_RUNS_DIR` observable for the first time: the chunk moved the driven arm's handle to `runs/driven/runs` and measured the log landing at `runs/sr-leg/logs/` and `runs/logs/`, with `runs/driven/logs/` confirmed at the wrap light gate. §3's bare `logs/conductor-tauri.jsonl` points an agent at a file that exists on no arm. Qualified as a RULE plus the measured set, not a fresh single-site literal."
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:33 (sites) + :91 (driven arm measured)"

  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Log file location"
    change: "Same qualification as §3: Tauri backend `&lt;CONDUCTOR_RUNS_DIR&gt;/../logs/conductor-tauri.jsonl`, directory rides the runs-dir handle."
    sidecar: "§1 Log file location — Tauri backend sink line brought in step with §3 (runs-dir-relative)."
    rationale: "obs-plan.md:105 restates the bare literal the primary retires; a §3-only apply leaves §1 asserting the project-root path."
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\obs-plan.md:105"
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Logging stack — Sink (Tauri backend)"
    change: "Sink (Tauri backend): file `&lt;CONDUCTOR_RUNS_DIR&gt;/../logs/conductor-tauri.jsonl` unconditionally (directory rides the handle); stderr only as an open-failure fallback."
    sidecar: "§1 Logging stack sink — Tauri backend path qualified runs-dir-relative."
    rationale: "obs-plan.md:80 restates the bare literal the primary retires."
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\obs-plan.md:80"
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§1 Telemetry surfaces table — desktop-webview row (backend hooks + Exporter cells)"
    change: "Both cells name the sink as `&lt;CONDUCTOR_RUNS_DIR&gt;/../logs/conductor-tauri.jsonl` (unconditional; directory rides the runs-dir handle; stderr fallback)."
    sidecar: "§1 Telemetry surfaces — desktop-webview row's two sink cells qualified runs-dir-relative."
    rationale: "obs-plan.md:49 carries the bare literal twice in one row; the row is the surface-level summary an agent reads first."
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\obs-plan.md:49"
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§3 Logging stack — Sink (Tauri backend)"
    change: "Sink (Tauri backend): file `&lt;CONDUCTOR_RUNS_DIR&gt;/../logs/conductor-tauri.jsonl` unconditionally (directory rides the handle); stderr only as an open-failure fallback."
    sidecar: "§3 Logging stack sink — Tauri backend path qualified runs-dir-relative."
    rationale: "obs-plan.md:203 restates the bare literal the primary retires, inside the same section as the primary."
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\obs-plan.md:203"
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§6 Sink configuration"
    change: "Sink (Tauri backend): file `&lt;CONDUCTOR_RUNS_DIR&gt;/../logs/conductor-tauri.jsonl` unconditionally (directory rides the handle); stderr only as an open-failure fallback."
    sidecar: "§6 Sink configuration — Tauri backend path qualified runs-dir-relative."
    rationale: "obs-plan.md:489 restates the bare literal; §6 is the section D-obs-instrumentation's check reads, so an unqualified sink here is the load-bearing hit."
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\obs-plan.md:489"
    dependent-of: D-obs-instrumentation

  - detector: D-obs-instrumentation
    severity: warning
    section: "§12 Obs Decisions Log — 2026-06-14 Key decisions / Exporter"
    change: "Append an in-place correction to the Exporter bullet (same form as its 2026-08-22 note): the Tauri backend file is runs-dir-relative (`runs_dir.parent()/logs/conductor-tauri.jsonl`), measured 2026-09-02 at `runs/logs/`, `runs/sr-leg/logs/` and `runs/driven/logs/`."
    sidecar: "§12 Exporter decision — dated correction appended: Tauri backend sink is runs-dir-relative, not project-root."
    rationale: "obs-plan.md:653 restates the bare literal as a live exporter claim; the entry already carries a 2026-08-22 in-place correction, so the doc's own precedent is a dated parenthetical rather than a rewrite of the historical decision."
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\obs-plan.md:653"
    dependent-of: D-obs-instrumentation

  - detector: D-obs-redaction
    severity: escalate
    section: "§11 PII Scrubbing"
    change: "Record the measured gap in the redaction boundary: the enumerated application sites (a) subscriber field filter (b) report-generation JSONL write (c) runs.db insert do NOT cover a process/window TITLE Conductor's own spawn creates — `conductor-verify/src/spawn.rs:80` builds the sidecar with no creation flags, raising a console pane titled with the sidecar's absolute exe path (measured 2026-09-02: NVDA spoke `Terminal` + the path ~200 ms after Start, captured into the leg speech log, row S1-01 `security_finding`); the leg's own scrub (`&lt;host-path&gt;`) kept it out of the committed artifact, and the fix is route-owned (`CREATE_NO_WINDOW` class), not asserted as shipped."
    sidecar: "§11 PII Scrubbing — measured host-path exposure via the sidecar spawn's console-pane title recorded as an uncovered site in the redaction boundary; fix route-owned, no new shipped obligation claimed."
    rationale: "The report's Cross-project bullet measures an absolute host path escaping Conductor at a channel §11's three enumerated scrub sites do not reach, caught only by the leg's TS scrub — the escalate class this detector guards. Recorded as a measured boundary gap because the report routes the fix as a route candidate (`directive item 1`), not as this wrap's code change (zero `.rs` delta)."
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:44 (spawn.rs:80 measurement) + :25 (scrub shape / security_finding)"

  - detector: D-obs-redaction
    severity: escalate
    section: "§11 Project-specific bans — sidecar spawn entry"
    change: "The `NEVER spawn an MCP sidecar via shell with operator-supplied input` ban gains the second hazard measured at the same site: the spawn must also suppress its console window, because a pane titled with the resolved exe path publishes an absolute host path to the OS foreground (`spawn.rs:80`, no creation flags — measured 2026-09-02)."
    sidecar: "§11 Project-specific bans — sidecar spawn ban extended with the console-window/creation-flags hazard measured at the same call site."
    rationale: "This entry is the doc's other rule governing `spawn.rs`; applying only the §11 PII amendment leaves the spawn ban reading as though shell/argv injection is the site's only hazard, so the two must land together."
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:44"
    dependent-of: D-obs-redaction
```

Two notes for the orchestrator on things I deliberately did **not** propose:

- The chunk's new symbols (`wdio.conf.ts`, `parse-nvda-log.ts`, `rows.ts`, `screen-reader.e2e.ts`, `activate-window.ps1`) are all harness-side with zero `.rs` delta and no new `#[tauri::command]` / MCP tool, and the report's **Coverage of new surfaces** marks instrumentation `n/a` for each — so no new must-trace operation exists to be uninstrumented. The `run_id` correlation (`2026-09-02T10-40-35-495`) and `std::time` journal stamps are both intact.
- The leg's `&lt;host-path&gt;` mask token differs from `conductor-core::redact`'s `&lt;redacted&gt;`, but it governs a test-evidence artifact outside the Rust product, so I did not read it as retiring §11's "single-location ownership in `conductor-core::redact`".

ORCHESTRATOR NOTE (post-decode): the primary's "never the project-root `logs/`" over-generalizes — with `CONDUCTOR_RUNS_DIR` unset the sink resolves to the CWD-relative project-root `logs/conductor-tauri.jsonl` (measured 2026-09-01); applied with that scope.
