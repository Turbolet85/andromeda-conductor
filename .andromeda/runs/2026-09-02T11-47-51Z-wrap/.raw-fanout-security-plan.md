```yaml
proposals:
  - detector: D-security-input
    severity: escalate
    section: Input Validation — boundary table (the `CONDUCTOR_MSEDGEDRIVER` row)
    change: Add a `CONDUCTOR_NVDA` row beside `CONDUCTOR_MSEDGEDRIVER` — dev-harness-only host dev-tool path handle read solely by `crates/conductor-tauri/ui/wdio.conf.ts`, never by a Conductor binary; validated by existence + `isFile` + `UNSAFE_PATH` shell-metacharacter rejection at the wdio edge before the NVDA spawn, used only in an array-form spawn and never through a shell; unset or not-a-file ⇒ the `sr` / `sr-empty` / `sr-error` suites SKIP at exit 0 with a host-path-free fetch recipe naming the handle but never its value — and reword the MSEDGEDRIVER row's singular "Deliberately NOT the `std::fs::canonicalize` rule … no Rust code reads it" so it governs the two-handle host-dev-tool SET rather than a lone exception.
    sidecar: Input Validation — `CONDUCTOR_NVDA` added as the second dev-harness host-tool path handle; the wdio-edge guard (existence + isFile + metacharacter rejection, array-form spawn, skip-at-exit-0) now stated for the handle SET, not for `CONDUCTOR_MSEDGEDRIVER` alone.
    rationale: Report §Changes/Env handles — "NEW `CONDUCTOR_NVDA` … validated like `CONDUCTOR_MSEDGEDRIVER` (existence + `isFile` + metacharacter rejection)"; and §Counts/qualifiers moved — "Host dev-tool handles in the `CONDUCTOR_*` namespace: 1 → 2 … Docs stating ONE: … security-plan §Input Validation `CONDUCTOR_MSEDGEDRIVER` row". Under the doc as written the new handle falls under neither the canonicalize rule nor the single named exception, so the boundary is ungoverned.
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:19"

  - detector: D-security-input
    severity: escalate
    section: Threat Model Summary § Attack surface — Vector "CLI input" entry point
    change: The `CONDUCTOR_*` entry-point enumeration gains `CONDUCTOR_NVDA` and its parenthetical becomes the measured leg SET — "dev-harness-only, read by `wdio.conf.ts` on the driver legs (`agent-run … --e2e` routine arm, `npm run a11y:driven` driven arm, and the operator-local `sr` / `sr-empty` / `sr-error` screen-reader suites) and never by a Conductor binary — `CONDUCTOR_MSEDGEDRIVER` · `CONDUCTOR_NVDA`".
    sidecar: Threat Model § Attack surface — CLI-input vector now lists both dev-harness host-tool handles and the three-family driver-leg set.
    rationale: Same report bullets; this vector restates the handle enumeration, so a single-site apply at §Input Validation would leave the threat model claiming one dev-harness handle on two legs.
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:32"
    dependent-of: D-security-input

  - detector: D-security-input
    severity: escalate
    section: Security Anti-Patterns § Input (the canonicalize ban)
    change: "`CONDUCTOR_MSEDGEDRIVER` is the one reserved-namespace path handle this rule does NOT cover" becomes the two-handle set — `CONDUCTOR_MSEDGEDRIVER` and `CONDUCTOR_NVDA` are the reserved-namespace path handles no Rust code reads, whose equivalent duty is discharged at the wdio harness edge (existence + `isFile` + shell-metacharacter rejection before the spawn).
    sidecar: Anti-Patterns § Input — the canonicalize-exception clause widened from one handle to the host-dev-tool handle set.
    rationale: The ban restates the §Input Validation exception as an exclusive singular ("the one"); left unamended it reads as a ban on `CONDUCTOR_NVDA` existing at all, or as leaving it uncovered by either rule.
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\security-plan.md:309"
    dependent-of: D-security-input

  - detector: D-security-input
    severity: escalate
    section: Bootstrap phases — input-validation-library-install
    change: The out-of-scope clause names both handles — "`CONDUCTOR_MSEDGEDRIVER` / `CONDUCTOR_NVDA` are out of this phase's scope — neither reaches Rust; both are validated at the wdio harness edge instead".
    sidecar: Bootstrap phases — input-validation-library-install scope exclusion extended to `CONDUCTOR_NVDA`.
    rationale: Third restatement of the same "one handle never reaches Rust" claim; a single-site apply leaves the bootstrap phase implying `CONDUCTOR_NVDA` is in the Rust canonicalize phase's scope.
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\security-plan.md:205"
    dependent-of: D-security-input

  - detector: D-security-input
    severity: escalate
    section: Secret Management § Storage — Development
    change: Add `CONDUCTOR_NVDA` to the non-secret reserved-namespace handle list beside `CONDUCTOR_MSEDGEDRIVER`, labelled the same way — a local host screen-reader path, not a credential.
    sidecar: Secret Management — `CONDUCTOR_NVDA` recorded as a non-secret local tool path handle.
    rationale: The Development storage bullet enumerates the whole `CONDUCTOR_*` namespace; unamended it omits a handle that now carries a host filesystem path, weakening the "no secrets, all handles accounted for" invariant.
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\security-plan.md:238"
    dependent-of: D-security-input

  - detector: D-security-input
    severity: escalate
    section: Security Anti-Patterns § Code Patterns — rule (b) (any HARNESS spawn)
    change: Rule (b) covers the leg SET (routine `--e2e` arm · `a11y:driven` driven arm · the `sr*` screen-reader suites) and gains the two spawn classes the SR leg ships — a FIXED OS program name with a fixed argv (`spawnSync('powershell.exe', ['-NoProfile','-ExecutionPolicy','Bypass','-File', &lt;repo script&gt;, '-Title', &lt;constant&gt;])`, no operator input anywhere in the vector) and an operator-supplied HOST-TOOL path used as the program (`CONDUCTOR_NVDA`) permitted ONLY after the existence + `isFile` + metacharacter guard, array-form argv, detached, never a shell string — so the current absolute "the PROGRAM must be a repo-derived resolved constant … never a bare PATH name" stays the rule for repo-vendored drivers without banning the two shipped, measured forms.
    sidecar: Anti-Patterns § Code Patterns (b) — harness-spawn rule extended to the `sr*` leg with two governed classes (fixed OS program + fixed argv; guarded host-tool path as program).
    rationale: Report §Changes/Symbols — `screen-reader.e2e.ts` "invokes `activate-window.ps1` through `spawnSync('powershell.exe', [...])` — array-form, fixed args, no operator input", and `startNvda(exe, …)` / `stopNvda(exe)` spawn the `CONDUCTOR_NVDA`-named exe. Rule (b) as written bans both (bare PATH name; operator-supplied value "never as the program itself"), so the doc and the shipped harness disagree.
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:14"
    dependent-of: D-security-input

  - detector: D-security-input
    severity: escalate
    section: Input Validation — boundary table (new row)
    change: Add a row for the SR evidence ingest — NVDA's own speech log (`nvda-speech.&lt;subject&gt;.log`) plus the leg's `actions.&lt;subject&gt;.jsonl` parsed into the committed `nvda-pass.json` — validated by spec-id cross-check against `nvda-pass-spec.md`, closed `outcome` / `arm` / `review_grade` sets, `heard` bounded at 400 chars, and a mandatory host-path scrub (any drive-letter / `%APPDATA%` / `/Users` / `/home` token replaced by `&lt;host-path&gt;`, the row gaining a `security_finding`); the log is a third-party tool's transcript of whatever took the OS foreground, so it is untrusted text, never pasted through unbounded or unscrubbed.
    sidecar: Input Validation — new boundary row for the screen-reader speech-log ingest (bounded `heard`, closed enums, host-path scrub → `&lt;host-path&gt;` + `security_finding`).
    rationale: Report §Changes/Schema-config — the scrub shape and the 400-char `heard` bound; §Coverage — "`parse-nvda-log.ts` → validation ✓ … PII ✓ (host-path scrub → `&lt;host-path&gt;` + `security_finding`)". This is a new external-input surface (text produced outside Conductor, including foreign windows' titles) that §Input Validation names nowhere; the leg measured a real host path arriving through it (row S1-01).
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\evidence\\nvda-pass.json:455"

  - detector: D-security-input
    severity: escalate
    section: Security Anti-Patterns § Logging (the host-path artifact ban)
    change: The artifact enumeration gains the a11y evidence records — "run-report artifacts (`&lt;run_id&gt;.md`, `runs.db` rows, JSONL journals) AND committed evidence records (`nvda-pass.json` and the per-chunk evidence tree)" — with the scrub named as the control for transcribed third-party text.
    sidecar: Anti-Patterns § Logging — host-path ban extended to committed a11y evidence records.
    rationale: The ban restates "artifacts must not leak absolute host paths" over a closed artifact list; the chunk adds a committed artifact class carrying transcribed foreign-process text, so a single-site apply leaves the ban's list claiming the older artifacts are the only host-path-bearing surface.
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\security-plan.md:346"
    dependent-of: D-security-input

  - detector: D-security-subprocess
    severity: escalate
    section: Security Anti-Patterns § Code Patterns — rule (a) (the MCP sidecar)
    change: Rule (a) gains the window-suppression duty — the sidecar spawn keeps the fixed hard-coded program NAME + `.env(...)` + no config-into-argv AND must suppress the child console window (`CREATE_NO_WINDOW`-class creation flag on Windows), because `Command::new(PULSE_MCP_PROGRAM)` with no creation flags raises a terminal pane titled with the sidecar's ABSOLUTE exe path that seizes the OS foreground ~200 ms after `Start` — a host-path disclosure channel outside the `sanitize_error` / artifact-hygiene edges, recorded as a routed product defect.
    sidecar: Anti-Patterns § Code Patterns (a) — sidecar spawn must additionally suppress the child console window; the flagless spawn discloses the sidecar's absolute exe path on-screen (measured 2026-09-02, routed as a defect).
    rationale: Report §Changes/Cross-project — "`crates/conductor-verify/src/spawn.rs:80` builds the sidecar `Command::new(PULSE_MCP_PROGRAM)` with NO creation flags, so starting a run from the GUI raises a Windows Terminal pane titled with the sidecar's exe path that takes the OS foreground … (measured: NVDA announced `Terminal`, the path, `pane` inside the S1-01 window) — a product DEFECT (`CREATE_NO_WINDOW` class)". The spawn matches every stated control (fixed name, `.env(...)`, no argv interpolation) yet still leaks an absolute host path, so §Security Anti-Patterns is incomplete against the measured behaviour.
    basis: "D:\\dev\\projects\\conductor\\crates\\conductor-verify\\src\\spawn.rs:80"

  - detector: D-platform-claim
    severity: warning
    section: Threat Model Summary § Attack surface — Vector "port bind `:4317`" (the dev-only driver-leg clause)
    change: Retire "The dev-only webview driver legs — the routine `agent-run … --e2e` arm and the operator-local `npm run a11y:driven` arm, both firing the same `wdio.conf.ts` driver stack" by naming the measured leg SET — the routine `--e2e` arm, the operator-local `a11y:driven` driven arm, and the operator-local `sr` / `sr-empty` / `sr-error` screen-reader suites, all firing the one `wdio.conf.ts` + tauri-driver stack and bringing up the same harness-lifetime `127.0.0.1:4444` / `:4445` listeners; keep the CAPABILITY note that the `sr*` suites are host-gated on Windows + an NVDA build named by `CONDUCTOR_NVDA` and skip at exit 0 elsewhere, and keep the CI ARRANGEMENT untouched (never a CI stage, no `agent-run` verb).
    sidecar: Threat Model § Attack surface — driver-leg set retired from two arms to the measured three-family set (routine · driven · `sr*`); ports `4444`/`4445` unchanged.
    rationale: Report §Harness/gate surface names the three `sr*` suites as a third runner family over the same stack, and §Changes/Ports records "4444/4445 as before, harness-lifetime" — so the two-arm enumeration is an incomplete runner claim. Retired by naming the set, not by a fresh literal count.
    basis: "D:\\dev\\projects\\conductor\\conductor-0.2.0\\chunks\\2026-09-02-screen-reader-manual-spec\\report.md:38"

  - detector: D-platform-claim
    severity: warning
    section: Security Anti-Patterns § Universal (the no-inbound-listener ban)
    change: The carve-out's runner enumeration — "bound by the `--e2e` routine arm and the `a11y:driven` driven arm alike" — becomes the same measured set (routine `--e2e` arm, `a11y:driven` driven arm, and the `sr*` screen-reader suites), leaving the ban itself and the "absent from `conductor-cli` / `conductor-tauri`" clause unchanged.
    sidecar: Anti-Patterns § Universal — the `4444`/`4445` carve-out now names the three-family driver-leg set.
    rationale: The Universal ban restates the port carve-out's runner list verbatim; a single-site apply at the threat model would leave the ban carving out only two arms while a third leg binds the same ports.
    basis: "D:\\dev\\projects\\conductor\\.andromeda\\security-plan.md:362"
    dependent-of: D-platform-claim
```

Note on `D-security-deps`: no proposal — the report's **Dependencies** bullet reads "none — `package-lock.json` and `Cargo.lock` byte-unchanged (gate 3)"; NVDA 2026.2 arrives as a host tool by env handle and `@guidepup/guidepup` was rejected, so no dependency landed to test against §Dependency Security, and the audit deferral's "no dependency delta" basis still holds. The WebView2 Runtime 151→152 move is recorded under **Dev-tool versions** (a background OS update), not Dependencies, so it is not a chunk dependency change.
