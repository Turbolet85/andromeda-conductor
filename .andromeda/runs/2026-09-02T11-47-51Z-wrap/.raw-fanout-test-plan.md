```yaml
proposals:
  - detector: D-tests-derived-count
    severity: warning
    section: "§6 E2E Test Strategy — Drivers-per-surface table, desktop-webview row (Notes cell)"
    change: &gt;-
      Replace "**TWO arms over this one stack:**" with the measured SET — THREE suite families over the one
      WebdriverIO + tauri-driver stack: the unattended ROUTINE `--e2e` arm (unchanged spec set, now re-seeded
      clean per session), the operator-only DRIVEN arm (`npm run a11y:driven`, now under its own
      `CONDUCTOR_RUNS_DIR=runs/driven/runs` chosen at the one spawn site), and the operator-only SCREEN-READER
      leg (`npm run a11y:sr` / `a11y:sr-empty` / `a11y:sr-error` → `screen-reader.e2e.ts`; Windows + NVDA host
      only, never a CI stage, never an `agent-run` verb) — firing form: `CONDUCTOR_NVDA` naming a portable NVDA
      (unset / not-a-file / metacharacter ⇒ skip at exit 0 with a fetch recipe), NVDA started BEFORE tauri-driver
      and ready on its `NVDA initialized` log line plus a quiet settle, the leg-owned `nvda-config/nvda.ini`
      copied per session, `activate-window.ps1` taking the OS foreground, per-suite runs/scenarios dirs and the
      subject passed by `runs/sr-leg/subject.txt`; stop form: `nvda -q` then a `tasklist` census before
      `tauriDriver.kill()`, evidence written to `nvda-pass.json` — the agent arm IS NVDA's own speech log, the
      operator's role is REVIEW, and the browse-mode rows stay findings pending OS-level key injection (never a
      manual arm, never a hard fail).
    sidecar: "§6 — webview suite families 2 → 3: `sr*` screen-reader leg registered with its firing/stop form; driven arm gains its own runs dir (2026-09-02-screen-reader-manual-spec)."
    rationale: &gt;-
      Report Counts bullet names test-plan §6 explicitly among the docs stating TWO ("Webview suite FAMILIES …
      2 → 3 (+ the `sr*` screen-reader leg, three suites); wdio `suites` entries 1 → 4; npm `a11y*` scripts
      2 → 5"), and the operator's WRAP directive item 3 asks §6 to register the `sr*` suites' firing form and
      their stop form. Harness/gate-surface bullet supplies the platform, gate and census qualifiers.
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/report.md:31,38,83

  - detector: D-tests-derived-count
    severity: warning
    section: "§4 Unit Test Strategy — `conductor-tauri/ui (frontend SPA)` bullet (ESM-loadability paragraph)"
    change: &gt;-
      Replace the two-prover enumeration ("`--e2e` proves the routine suite, `npm run a11y:driven` proves the
      `driven` suite, and a `wdio.conf.ts` member reachable from neither is as unproven as it was under the build
      gate") with the SET of firing paths that LOAD the config — `--e2e` (routine), `npm run a11y:driven`
      (driven) and `npm run a11y:sr` / `a11y:sr-empty` / `a11y:sr-error` (screen-reader) — a member reachable
      from NONE of them being as unproven as under the build gate.
    sidecar: "§4 — wdio.conf.ts prover set widened from two firing paths to three (the `sr*` suites load the new config members)."
    rationale: &gt;-
      The chunk adds `wdio.conf.ts` members (`nvdaExe` / `reportNvdaSkip` / `startNvda` / `stopNvda` /
      `invokedSuites` / `SR_SUITES` / the per-suite spawn env) that no `--e2e` or `driven` invocation loads;
      they are proven only by the three `sr*` sessions (3/3 passing 10:41–10:44Z). Leaving the doc's two-member
      enumeration makes the doc's own "reachable from neither" test read those members as unproven.
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/report.md:14,57
    dependent-of: D-tests-derived-count

  - detector: D-tests-derived-count
    severity: warning
    section: "§11 Test Anti-Patterns — Universal (agent-driven specific), the 'NEVER use real network calls' ban"
    change: &gt;-
      "the WebDriver pair `:4444`/`:4445` spawned by EITHER webview arm" becomes "spawned by ANY webview arm
      (routine, driven, or an `sr*` screen-reader suite)"; the loopback/never-a-CI-network-call ban is otherwise
      unchanged and now covers the `sr*` suites as operator-local legs too.
    sidecar: "§11 Universal — 'EITHER webview arm' → 'ANY webview arm' (three arms now spawn the harness-lifetime WebDriver pair)."
    rationale: &gt;-
      Same retired wording as the §6 primary: the report's Counts bullet moves webview suite families 2 → 3, and
      the `sr*` suites spawn the same harness-lifetime tauri-driver `:4444` / native `:4445` pair with no new
      port (report: "Ports / IPC / MCP / commands: none added or changed (4444/4445 as before)").
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/report.md:20,31
    dependent-of: D-tests-derived-count

  - detector: D-tests-coverage
    severity: warning
    section: "§2 Test Strategy — Agent-runnable invariants, the 'Deterministic' bullet"
    change: &gt;-
      Name the operator-local non-deterministic SET instead of a single carve-out: the `a11y:driven` arm AND the
      three `sr*` screen-reader suites run on real wall clock (a 30 s `NVDA initialized` readiness window, a
      1.5 s quiet settle bounded at 8 s, a 50 ms utterance-to-stamp window, an OS-foreground activation step and
      a ≤ 15 s process-census poll) and additionally drive a host NVDA process, so BOTH are carried only as the
      §9/§11 operator-local-gate exceptions — never CI legs, never the deterministic tier for the frontend paths
      they prove — and the harness-lifetime WebDriver pair is spawned by ANY webview arm rather than "EITHER".
    sidecar: "§2 — determinism carve-out widened from the driven arm alone to the operator-local set (driven + `sr*`), both wall-clock, neither a CI leg."
    rationale: &gt;-
      The chunk adds an executed test path that is not seed-deterministic (real NVDA, log-line readiness, quiet
      settle, foreground race — report: readiness "= the log's `NVDA initialized` line within 30 s, then a 1.5 s
      quiet settle ≤ 8 s"; "the parser assigns utterances by time window"), while §2 currently says the driven
      arm is carried "ONLY" as the exception. A second non-deterministic leg outside the named exception is
      exactly the drift this detector guards.
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/report.md:14,26,38

  - detector: D-tests-coverage
    severity: warning
    section: "§1 Test Scope Summary — Coverage triggers, security-vector-coverage (Vector 1, `CONDUCTOR_*` env path handles)"
    change: &gt;-
      The harness-only branch names BOTH host dev-tool handles in the namespace — `CONDUCTOR_MSEDGEDRIVER`
      (asserted at the e2e tier by the `--e2e` skip arm in both shells) and `CONDUCTOR_NVDA` (identical guard
      shape: existence + `isFile` + shell-metacharacter rejection ahead of an array-form spawn, read only by
      `wdio.conf.ts`, never by a shipped binary; asserted at the e2e tier by the `sr*` suites' skip-at-exit-0
      arm — an npm-script leg, not `--e2e` and not carried in both shells) — with no Rust negative test reachable
      for either and no JS/TS unit runner adopted (§4, §12).
    sidecar: "§1 Vector-1 trigger — host dev-tool handles 1 → 2: `CONDUCTOR_NVDA` registered with its guard shape and its `sr*` skip-arm tier."
    rationale: &gt;-
      Report Counts bullet: "Host dev-tool handles in the `CONDUCTOR_*` namespace: 1 → 2 (`CONDUCTOR_MSEDGEDRIVER`
      + `CONDUCTOR_NVDA`)"; the new handle is "validated like `CONDUCTOR_MSEDGEDRIVER` (existence + `isFile` +
      metacharacter rejection)" and its skip path was measured (2026-09-02 11:53Z, `env -u CONDUCTOR_NVDA npm run
      a11y:sr-empty` → exit 0, no process started). The trigger's per-member rule currently enumerates one
      harness-only member, so the new path's mandated tier is unrecorded.
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/report.md:19,32,56

  - detector: D-platform-claim
    severity: warning
    section: "§6 E2E Test Strategy — Drivers-per-surface table, desktop-webview row (Mode cell, Windows dev host clause)"
    change: &gt;-
      Retire the "matching the host WebView2 Runtime major" requirement on the operator-supplied `msedgedriver`
      — the Windows leg is gated on `CONDUCTOR_MSEDGEDRIVER` alone, and the measured working pair set now
      includes a one-major skew (msedgedriver 151.0.4129.101 driving WebView2 Runtime 152.0.4191.53, green on
      all three final `sr*` sessions and the routine `--e2e` re-run, 2026-09-02), with the driver's own
      "This version of Microsoft Edge WebDriver has not been tested with Microsoft Edge version 152." line the
      sole basis for "unsupported"; refreshing the driver is the operator's host task BEFORE the next leg, never
      inside a wrap.
    sidecar: "§6 — msedgedriver 'matching major' requirement retired: a measured cross-major pair (151 driver / 152 runtime) ran the Windows webview leg green."
    rationale: &gt;-
      Report Dev-tool versions bullet measures the retired requirement false: "a MEASURED cross-major pair
      (driver 151 against runtime 152) that worked on all three final `sr*` sessions and the routine `--e2e`
      re-run, officially unsupported", re-confirmed at the wrap light gate; operator directive item 3 asks it be
      recorded as a measured, unsupported-but-working pair. This is a capability ("requires X") verdict, not a
      CI arrangement, so the measurement falsifies it; the sole occurrence in this doc is the §6 Mode cell
      (§3 bootstrap, §9 matrix and §12 all state the gate without a major-match requirement).
    basis: conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/report.md:37,83,91
```

Notes on what I checked and did **not** propose:

- **D-tests-framework — no drift.** The chunk's runner is `wdio run wdio.conf.ts --suite &lt;name&gt;` (non-interactive, array-form, fixed args) on the already-sanctioned @crabnebula/tauri-driver + WebdriverIO stack; **Dependencies: none** (`package-lock.json` / `Cargo.lock` byte-unchanged, `@guidepup/guidepup` rejected), so no off-spec framework was adopted. The absence of a unit-test file for `parse-nvda-log.ts` is covered by the doc's standing "no JS/TS unit runner is adopted" decision (`.andromeda/test-plan.md:85`, `:244`, `:599`), not a coverage gap.
- **D-tests-obs-harness — no drift.** Report states "no obs log-schema change", no `runs.db` / envelope / 5-command change, and the `sr*` suites are "never an `agent-run` verb". `.andromeda/test-plan.md:188` names `logs/conductor-tauri.jsonl` as an artifact without claiming a workspace-root location, so the sink-relocation entry lands on architecture, not here; §3's routine-arm `CONDUCTOR_RUNS_DIR=runs/e2e-fixture` claim (`:152`, restated `:457`) is still exactly what the config sets for that suite.
- **Not proposed (checked, false positive):** report claim 5 (shipped dialog title `{p_id} — operator-checklist`) does *not* retire test-plan's "HOLD — operator pause" heading-text selector (`:54`, `:308`, `:539`) — that string still ships as the hold titlebar label (`crates/conductor-tauri/ui/src/components/Titlebar.tsx:9`), and the report names no doc for that claim.
- **Not proposed:** the a11y-plan four-state renaming (`report-terminal` → `aborted`) — test-plan never states the retired set; its "idle/live/HOLD" mentions are layout references, not a closed state set.
