# Adaptation record — 0-pending wrap, 2026-09-02T12:57:07Z

Operator directives received after the `2026-09-02-screen-reader-manual-spec` wrap commit (`fff1068`), executed
through the wrap's between-chunks path (no chunk in flight; master has 0 `pending` records; the tree carried only
the expected-transient friction-log append).

## 1. Playbook rules — APPROVED and appended

Both rules proposed in the previous wrap's report were approved as written, with one refinement on (b):
**the directive must NAME the defect, never a class.** Appended to `.andromeda/playbook.md` via the
propose → approve → append channel:

- (a) a new dev-harness-only `CONDUCTOR_*` HOST-TOOL path handle behind the wdio-edge guard, named on the
  plan's expected-amendments list → routine (the §Input Validation row + every singular clause widened to the
  handle SET). Second occurrence of the class (`CONDUCTOR_MSEDGEDRIVER` 2026-09-01, `CONDUCTOR_NVDA` 2026-09-02).
- (b) an escalate-class security/obs detector firing on a MEASURED product defect the operator's wrap directive
  has already routed, naming THAT defect (site · mechanism · symptom) → routine, applied as a record with the fix
  route-owned; a class-level direction satisfies nothing and the halt stays. First occurrence: the sidecar
  console-pane host-path disclosure.

## 2. Route — the msedgedriver PREREQ on _Cross-surface envelope parity_ DISCHARGED

- Direction: the operator refreshed the host driver; the binary `CONDUCTOR_MSEDGEDRIVER` names is now
  `152.0.4191.53` (matching the WebView2 Runtime), the previous binary kept beside it as
  `msedgedriver-151.0.4129.101.exe`.
- Measured before clearing (not taken on the direction alone): `"$CONDUCTOR_MSEDGEDRIVER" --version` →
  `Microsoft Edge WebDriver 152.0.4191.53 (4ee8983fdce2559a0ae8f8376934c5ed353035cd)`; the driver directory
  lists `msedgedriver.exe` and `msedgedriver-151.0.4129.101.exe`. The handle's path is unchanged (the binary was
  replaced in place), so no harness edit is needed; host paths stay out of this record.
- Edit: the second PREREQ clause on the markerless entry _Cross-surface envelope parity_ removed (route-resolve
  §Deferred-gate closure — the reverse clean-up is factual → AUTO, and here operator-directed). The entry keeps
  its first PREREQ (close the rust gate deferral, deferred since `2026-09-02-screen-reader-manual-spec`) and
  its two CARRYs.
- Masters untouched: test-plan §6's record of the 151/152 cross-major pair is a measured historical fact and
  its "refresh after a runtime update is the operator's host task" rule is unchanged; no master states the
  current driver version (a host value).

## 3. Not done (by design)

- No chunk report, no fan-out (nothing measured by a chunk); no curation (the conversation carried a rule
  refinement, which landed in the playbook itself, and a host fact, not a learning).
- The two Epoch-6 Linux+xvfb entries still stand untouched — no direction on them arrived.

Commit: `chore(route): operator-requested adaptation — 0-pending wrap` (carries this record, the playbook
append, the route edit, `state.yaml` session 106, the handoff, and the pending friction-log bookkeeping).
