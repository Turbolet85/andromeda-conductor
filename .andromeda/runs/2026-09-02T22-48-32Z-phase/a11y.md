# a11y extract

## No domain coverage

This chunk is a Rust test-binding + mutation-tier restoration (`crates/conductor-tauri/Cargo.toml` dev-deps / a possible `[[test]]` target, the `#[cfg(test)] mod tests` block in `commands.rs`, `Cargo.lock`, `v2-25`'s matrix ref) with no rendered-DOM surface, no ARIA/focus/keyboard/contrast/motion behavior, and no WCAG criterion in play — a11y-plan §9's per-stage table pins the Unit stage as "no UI a11y tests at unit layer / n/a" and §3 + §9 bind every a11y assertion to the `wdio run` / `@crabnebula/tauri-driver` webview stack rather than `cargo test` / `cargo mutants`; the scope's own closing note confirms the Epoch 6a `A11y CI gate` work is a different, still-markerless entry, and the harness's only `conductor-tauri` touchpoint (the `cargo build --release -p conductor-tauri --features tauri/custom-protocol` bundle build named in the SR leg) is a release build unaffected by dev-dependency or integration-test-target changes.
