# design extract

## No domain coverage

Pure Rust test-harness plumbing (a Cargo binding for the `conductor` binary, the restored `cargo mutants -p conductor-tauri` tier, a fresh-target-dir gate) — it renders nothing and touches no token, type role, motion value, icon, or component pattern; the only design-plan line adjacent to it, `.andromeda/design-system.md` §Surface: cli / Component Patterns 2 + §Per-Surface Bans → cli (the headless agent path is never gated on an interactive `inquire` prompt), governs CLI *behavior* the chunk does not change, and whether the shipped binary already satisfies it is research's question, not this chunk's build.
