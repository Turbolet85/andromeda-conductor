Raw return — design-system doc-agent. Preserved because the return carried proposals AND needed
entity-decoding (`&gt;-` for the YAML block scalar `>-`). Reconstructed twin: the saved/decoded form
lives in fanout-results.md §Proposals.

```yaml
proposals:
  - detector: D-design-tokens
    severity: warning
    section: Design Decisions Log — the `2026-06-14: Initial design system generated by /andromeda-design (Phase 4)` entry, `Surfaces:` bullet
    change: the desktop-webview stack in the `Surfaces:` bullet should read "React 19 + Tailwind v4.1 design tokens on `:root`" (the declaration §Tokens binds), dropping the retired "Tailwind v4.1 `@theme`".
    sidecar: "§Design Decisions Log 2026-06-14 `Surfaces:` — `@theme` → tokens declared on `:root`; the last surviving occurrence of the mechanism §Tokens retired 2026-06-15."
    rationale: &gt;-
      The report's Changes → **Spec-master edits** bullet, item 2, names `.andromeda/design-system.md:406`
      explicitly — retire "Tailwind v4.1 `@theme`" for the `:root` truth §Tokens `:201` already carries.
      §Tokens `:201` states the opposite ("declare these on plain `:root`, NOT `@theme`"), and
      `design-system-amendments.md:5-8` records that retirement as already applied (2026-06-15, attributed to
      D-design-tokens) — but applied at a SINGLE site, so `:406` is the surviving duplicate that makes the doc
      contradict itself. Sweep result: `:406` is the ONLY other occurrence — `:165`, `:197` and `:201` all
      state the `:root` / `@tailwindcss/vite` truth correctly, so no `dependent-of` proposal is owed.
      NOTE for dedupe: the report marks this orchestrator-raised (this wrap's operator directive, item 3),
      "not this chunk's own drift", and lists it as applied at P2 — if that apply landed, this proposal is a
      no-op; the doc as read still carries `@theme` at `:406`.
    basis: .andromeda/design-system.md:406 (retired claim) ↔ .andromeda/design-system.md:201 + .andromeda/design-system-amendments.md:5-8 (the retirement)
```

Clean verdicts recorded by the agent: D-design-tokens (invariant proper — Coverage of new surfaces is
`none`, no `hardcoded✗`, no color-only state), D-design-derived-count (the moved counts are mutation/test
tallies with no palette row / ANSI-map entry / token label / reuse tally counterpart; numeric collisions
`React 19.x` and `12px` are semantic non-matches), D-platform-claim (design-system names no parity arm,
no `tauri::test`, no mock-runtime, no driver; its platform verdicts already name SETS).
