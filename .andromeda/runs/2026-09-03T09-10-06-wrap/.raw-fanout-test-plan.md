Raw return — test-plan doc-agent. Preserved because the return carried proposals AND needed
entity-decoding (`&amp;` in two section titles). Reconstructed twin; the decoded/dispositioned form
lives in fanout-results.md §Proposals.

```yaml
proposals:
  - detector: D-tests-derived-count
    severity: warning
    section: "10. Quality Gates &amp; Coverage Targets — Mutation-survivor disposition (audit tier, non-blocking)"
    change: Replace "the `declares` env-reading edge is the shipped case" with the roster named as a SET — the accepted-deliberate dispositions recorded in §12's mutation-instrument entry, each against its own cited rule (today `conductor-verify`'s `declares` env-reading edge ×6 plus `conductor-tauri`'s three 2026-09-03 survivors) — so a new accepted disposition extends the roster instead of contradicting a single-case literal.
    sidecar: "§10 mutation-survivor disposition: single shipped case (`declares` ×6) → the §12-recorded accepted-deliberate SET, second member landed 2026-09-03 (conductor-tauri triple)."
    rationale: Report `Counts / qualifiers moved` states verbatim "test-plan §10's accepted-deliberate roster gains a second instance (`declares` ×6 was the only one; now plus this triple)"; Outcome records "22 dispositioned: 19 killed, 3 accepted with citations (`evidence/survivor-dispositions.md`)", so the doc's singular "the shipped case" bakes a roster size the code no longer produces.
    basis: .andromeda/test-plan.md:500

  - detector: D-tests-derived-count
    severity: warning
    section: "12. Test Decisions Log — `2026-08-20` — Mutation instrument adopted"
    change: Add a bullet beside the `declares` ×6 entry recording the `conductor-tauri` accepted-deliberate TRIPLE of 2026-09-03.
    sidecar: "§12 2026-08-20 entry: accepted-deliberate roster extended with the conductor-tauri triple (2026-09-03), beside `declares` ×6."
    rationale: The report lists this as Spec-master edit 1 and as the chunk's ONE `Expected amendments (from plan)`; as read, §12's entry still records only `declares` ×6.
    basis: .andromeda/test-plan.md:607
    dependent-of: D-tests-derived-count

  - detector: D-tests-derived-count
    severity: warning
    section: "4. Unit Test Strategy — Mutation instrument (run discipline)"
    change: State the mutants read-out gate as the disposition SHAPE rather than an empty-file literal — gate on `missed.txt` holding exactly this run's accepted-deliberate survivors (empty only when none is accepted) plus every other named survivor present in `caught.txt`, never on the exit code.
    sidecar: "§4 mutants read-out gate: `missed.txt` empty → `missed.txt` == the run's accepted-deliberate set; measured 2026-09-03."
    rationale: An accepted-deliberate survivor (§10) by construction survives, so an "empty `missed.txt`" gate would fail a run this wrap accepted.
    basis: .andromeda/test-plan.md:226

  - detector: D-tests-derived-count
    severity: warning
    section: "7. Test Data &amp; Fixtures — Self-bootstrapping requirement (committed-fixture meaning bullet)"
    change: Widen the meaning-pinner SET from the two `conductor-run` files to one round-trip test per committed fixture, driving it through the production reader / writer / validator.
    sidecar: "§7 committed-fixture meaning-pinner set: two `conductor-run` files → the per-fixture round-trip set, third member added 2026-09-03."
    rationale: Report Files bullet adds the two `conductor-tauri` fixture TOMLs, consumed through `Scenario::from_toml_str_with` and pinned by `the_committed_scenarios_fixture_stays_loadable`.
    basis: .andromeda/test-plan.md:399

  - detector: D-tests-derived-count
    severity: warning
    section: "7. Test Data &amp; Fixtures — Self-bootstrapping requirement (sanctioned provenance bullet)"
    change: Name the committed fixture family as the set of crate-local `tests/fixtures/` trees instead of the single `crates/conductor-run/tests/fixtures/` literal.
    sidecar: "§7 sanctioned provenance: `crates/conductor-run/tests/fixtures/` literal → the set of crate-local committed fixture trees."
    rationale: A committed fixture now lives outside the single path the provenance SET enumerates.
    basis: .andromeda/test-plan.md:398
    dependent-of: D-tests-derived-count
```

Clean verdicts recorded by the agent: D-tests-coverage, D-tests-framework, D-tests-obs-harness,
D-platform-claim (§1/§5/§6 already corrected the parity-arm verdict at 2026-09-02; the
control-panel-LAUNCHED half is recorded still OWED).
