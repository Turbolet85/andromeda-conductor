# design extract

## No domain coverage

This chunk is a Rust mutation-survivor dispositioning exercise in `crates/conductor-run/` (composition root `lib.rs` + `dispatch.rs` and their test modules) plus a `cargo audit` PREREQ re-check — it renders no UI on either the `desktop-webview` or `cli` surface and touches no token, typography, motion, iconography, or component primitive governed by design-system.md.
