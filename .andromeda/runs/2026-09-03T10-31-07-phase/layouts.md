# layouts extract

## No domain coverage

This chunk is a mutation-survivor dispositioning pass over `crates/conductor-run/src/lib.rs` + `dispatch.rs` and their test modules — it creates or modifies no surface element on either surface layout-templates owns (desktop-webview run console, cli output structure); scope.md explicitly forbids production-behaviour change and forbids restructuring the composition root, and any admitted exception is an observability-bound edit (test/obs domain), so no wireframe, region, component placement, focus order, or rendered output shape is in play.
