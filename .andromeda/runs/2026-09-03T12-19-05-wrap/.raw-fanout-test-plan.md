# Raw twin — test-plan doc-agent return (2026-09-03-conductor-run-composition-root-survivors-dispositioned)

Preserved because this return BOTH carried proposals and needed an entity decode. The `section:` values
below are the RAW form as returned — note `&amp;` in the two §10 section names, which the orchestrator
decoded to `&` before parsing. The applied edits used the doc's own text, not these section strings.

```yaml
proposals:
  - detector: D-tests-derived-count
    severity: warning
    section: "12. Test Decisions Log — the `**`declares` x6 classified accepted-deliberate**` bullet"
    change: Retire the `x6` literal — state the accepted-deliberate `declares` class as the SET the disposition ledger enumerates (the env-read arms `lib.rs:358:5` → `false`, `:360:11`, `:360:21`, `:360:26`), and record that the class shrank on 2026-09-03 when `observe_run_contract:346` and `declares:358 → true` were killed by `observe_run_contract_names_the_term_this_environment_does_not_declare`; the `unsafe set_var` rationale is unchanged for the members that remain.
    sidecar: "§12 `declares` bullet — `x6` literal replaced by the named surviving env-read arm set; two former members killed 2026-09-03."
    rationale: "Report Changes → *Counts / qualifiers moved*: “the accepted-deliberate `declares` class: 6 → 4 members. Stated in `test-plan.md` §12”; *Spec claims disproved* #1 measures both former members present in `target/mutants-impl-2026-09-03/mutants.out/caught.txt`. The doc still reads `x6`, so it bakes a count the tier no longer produces."
    basis: conductor-0.2.0/chunks/2026-09-03-conductor-run-composition-root-survivors-dispositioned/evidence/disposition-ledger.md:40-52

  - detector: D-tests-derived-count
    severity: warning
    section: "10. Quality Gates &amp; Coverage Targets — **Mutation-survivor disposition (audit tier, non-blocking)** paragraph"
    change: Drop the `×6` from the inline roster reference — name the `declares` env-reading edge as the set §12's mutation-instrument entry enumerates, with no count carried at this site (§10 already declares the roster to BE that set).
    sidecar: "§10 mutation-survivor-disposition paragraph — `×6` count on the `declares` edge retired in favour of §12's enumerated set."
    rationale: "Same fact as the primary — the report states the 6-member count is “Stated in `test-plan.md` §12 and referenced in §10”. Line 500 restates the retired literal, so a §12-only fix would leave the stale 6 alive at the quality-gate site."
    basis: .andromeda/test-plan.md:500
    dependent-of: D-tests-derived-count

  - detector: D-tests-derived-count
    severity: warning
    section: "10. Quality Gates &amp; Coverage Targets — **Mutation-survivor disposition (audit tier, non-blocking)** paragraph"
    change: Re-attribute the `declares` env-reading edge from `conductor-verify`'s to `conductor-run`'s — the function lives at `crates/conductor-run/src/lib.rs:357`; the cited `.claude/rules/testing.md` 2026-08-10 rule and the “EXTENDS the roster, never contradicts a single-case literal” framing are unchanged.
    sidecar: "§10 mutation-survivor-disposition paragraph — `declares` env-reading edge re-attributed `conductor-verify` → `conductor-run`."
    rationale: "Report Changes → *Spec claims disproved by measurement* #2: “`test-plan.md:500` attributes the `declares` ×6 env-reading edge to `conductor-verify`; it is in `conductor-run`”, measured by `grep -rn 'fn declares' crates/*/src/*.rs` returning a single hit. Swept: `conductor-verify` appears elsewhere in the doc (§1 entity/surface rows, §4, §5, §10 coverage exclusions) only as the read-back client, which is correct — line 500 is the sole site attributing the `declares` edge."
    basis: crates/conductor-run/src/lib.rs:357

  - detector: D-tests-derived-count
    severity: warning
    section: "12. Test Decisions Log — mutation-instrument roster (new bullet beside the `conductor-tauri` triple)"
    change: Record `conductor-run`'s 2026-09-03 accepted-deliberate set so §10's “the SET recorded in §12's mutation-instrument entry” resolves to the whole roster — class B, unreachable past the fixed `http://127.0.0.1:4317` egress gate (`lib.rs:518:27`, `lib.rs:559:25` ×2), cited against architecture.md §Occupied Resources — Ports plus §10 zero-flakiness (a fixed port contended between `--jobs 2` mutant processes; making the endpoint injectable is a production signature change); and class C, `preflight:66:8`, unreachable without the fixed-name `andromeda-pulse-mcp` sidecar on the inherited `PATH`, cited against the same two rules — with the citation home named as the chunk's `evidence/disposition-ledger.md`.
    sidecar: "§12 mutation-instrument roster gains `conductor-run`'s accepted-deliberate classes B and C (2026-09-03)."
    rationale: "Report Changes → *Expected amendments*: “`test-plan.md` §12 roster gains `conductor-run`'s set — carried”, and Outcome row “§12 roster EXTENDED … extended with `conductor-run`'s classes B and C”. §10 states the roster IS §12's set; §12 currently enumerates only the `declares` edge and `conductor-tauri`'s triple, so the doc's roster is 4 members short of the 8 accepted-deliberate survivors the tier now reports (8 missed / 8 accepted, verified per-site against `missed.txt`)."
    basis: conductor-0.2.0/chunks/2026-09-03-conductor-run-composition-root-survivors-dispositioned/evidence/disposition-ledger.md:54-86
```

## Orchestrator note on proposal 4's citation

The proposal cited class C against "the same two rules" as class B (architecture §Occupied Resources —
**Ports**). The applied text cites class C against architecture §Occupied Resources — **Service / process
names** instead, which is where the fixed-program-NAME-through-inherited-`PATH` spawn rule actually lives;
`:66:8`'s barrier is sidecar resolution, not a port bind. Class B's citation was applied as proposed.
