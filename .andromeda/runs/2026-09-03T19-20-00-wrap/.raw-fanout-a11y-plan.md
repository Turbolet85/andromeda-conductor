Raw return — a11y-plan drift-detector (prose analysis preamble + a fenced ```yaml block; stripped to bare YAML for parsing, analysis preserved here as the negative-finding record).

## Negative findings the agent recorded (checked and clean)

- **D-a11y-obs-schema — no drift.** Report's *Schema / config* states "No violation-schema or scrub-shape change"; obs side unwidened ("no new span name", one `info!`/`warn!` on the allowlisted `message` field). a11y-plan's reproduced obs §6 envelope (lines 96-109, 233-246) stays consistent.
- **D-platform-claim — no drift.** The report's `Spec claims disproved` entries name a crate (`anstream`) and a contract term (`sidecar-built`), not a platform/runner/driver; `Harness / gate surface` names both shells with identical semantics and retires no platform verdict. a11y-plan's platform claims (`:115`, `:252`, `:299`, `:457`, `:565`) already name the measured SET (Linux+xvfb AND Windows WebView2 host, macOS manual-only) and correctly separate CI arrangement from capability.
- **Sweep result for the one drift:** line 71 is the ONLY site asserting a verb SET; lines 119/149 reference `conductor run` paths as a class, not an enumeration — not the retired claim. So no dependent proposals.

```yaml
proposals:
  - detector: D-a11y-surface
    severity: warning
    section: "1. A11y Scope Summary — A11y scope (entities needing assertions), the `conductor-cli` entity"
    change: name the entity as the whole `conductor` verb surface (`agent-run` headless binary — every `conductor` verb, whatever the set; six as of 2026-09-03, `preconditions` newest) instead of the three-verb literal `conductor-run`, `conductor-suite`, `conductor-report`, so the not-assertable verdict is set-scoped and covers verbs added later without restaling.
    sidecar: "§1 cli entity: three-verb literal retired for a set-scoped naming of the whole `conductor` verb surface — verb surface measured 5 → 6 (`preconditions` added) in 2026-09-03-live-pulse-preconditions-probed."
    rationale: "Report *Counts / qualifiers moved* records CLI verb surface 5 → 6 and flags the three-verb literal as stale at `architecture.md:33` and `layout-templates.md:251`/`:309`. a11y-plan:71 carries the same literal in hyphenated form (`conductor-suite` is not a crate — no such crate exists — so it is the verb literal, not a crate list). The report's *Coverage of new surfaces* claims a11y n/a for the new verb citing a11y-plan §1, but §1's exclusion is written as an enumeration of three verbs that does not name `preconditions` (nor `preflight`/`coverage`), so the exclusion's scope no longer visibly covers the element the chunk added."
    basis: "report.md:32 (verb surface 5 → 6), :14 (new verb), :59 (a11y n/a citing §1); site is .andromeda/a11y-plan.md:71"
```
