Raw return — architecture drift-detector (fenced in ```yaml; stripped to bare YAML for parsing). 8 proposals: 3 primaries (D-arch-resources) + 5 dependent-of sweeps.

```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — Interface routes / surfaces
    change: Add a CLI-verb bullet registering the shipped operator surface as SIX verbs (`run`/`suite`/`report`/`preflight`/`coverage`/`preconditions`), where `conductor preconditions [--json]` is a non-mutating scheduling-time probe of `:4317` client reachability + `andromeda-pulse-mcp` on the inherited `PATH` + the three `ANDROMEDA_PULSE_*` declarations, exiting 0 iff every subject is satisfied (the documented `conductor preflight` go/no-go exception — it mints no `Verdict`, no `ReportState`, no per-P-ID row), and that `scripts/agent-run.{sh,ps1}` `boot` now runs it as a LEADING arm that short-circuits on non-zero and SKIPS the preflight invocation rather than paying it (harness command count still five, identical semantics in both shells); the adjacent "Route prefixes: none" claim stays unchanged, since a CLI verb is not an HTTP route-equivalent.
    sidecar: Registered the new `conductor preconditions [--json]` verb (CLI surface 5 → 6) and `boot`'s leading probe arm in §Occupied Resources — Interface routes / surfaces.
    rationale: Report *Symbols / APIs* lands a new CLI verb and *Harness / gate surface* lands `boot`'s leading probe arm; neither appears in any arch registry section, and the chunk's own Expected amendment 1 carries this fact.
    basis: report.md:14,40

  - detector: D-arch-resources
    severity: warning
    section: Stack and Technologies — CLI argument parsing row
    change: Replace the three-verb literal "Declarative `conductor run`/`suite`/`report` verb surface" with the shipped six-verb set over the `current_thread` bootstrap.
    sidecar: Retired the stale three-verb literal in the §Stack clap row; the surface is six verbs.
    rationale: Report *Counts / qualifiers moved* states "CLI verb surface 5 → 6" and names `architecture.md:33` as a three-verb literal "already stale before this chunk, now further behind".
    basis: report.md:32
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — Environment variables (`CONDUCTOR_PREFLIGHT_TIMEOUT`)
    change: Qualify "read by `scripts/agent-run.{sh,ps1}` `boot` wrapping `conductor preflight`" — `boot` now runs `conductor preconditions` FIRST and short-circuits on non-zero, so the preflight invocation (and therefore this timeout) is skipped rather than paid when a precondition is unmet.
    sidecar: Restated `boot`'s composition in the `CONDUCTOR_PREFLIGHT_TIMEOUT` bullet.
    rationale: Report *Harness / gate surface* + Smoke ("no `ReadyState` JSON emitted, proving the preflight was skipped rather than merely failed"). This bullet is the doc's other statement of `boot`'s composition and survives a single-site apply.
    basis: report.md:40,103
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: Standard Contracts — Liveness equivalent (OTLP egress check)
    change: Record that the `127.0.0.1:4317` connectability check now has TWO production callers with different dispositions — the timeline engine's pre-emission check, whose refused transport is still a harness error (`Result::Err`), and the scheduling-time `observe_preconditions` probe (same `probe_egress` connect-and-drop, no bind, `:4318` untouched), whose refusal is a reported unmet precondition and a non-zero exit, never a verdict or an `Err`.
    sidecar: Registered the preconditions probe as the second caller of the `:4317` liveness check, with its non-`Err` disposition.
    rationale: Report *Symbols / APIs* — `probe_egress` "gains a SECOND production caller"; §Standard Contracts currently states one caller and one disposition, now true of the emission path only.
    basis: report.md:17,19
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — `contracts/pulse-run-contract.toml`
    change: Record that the `[[term]]` list now carries SIX terms — it gains `mcp-enabled` (`check = "shell-declaration"`, `env = "ANDROMEDA_PULSE_MCP_ENABLED"`, with candidate causes), the same class as the shipped `l4-deterministic` and needing no new machinery — so the observable shell-declaration env set is TWO handles, not one.
    sidecar: Registered the new `mcp-enabled` run-contract term (`[[term]]` 5 → 6) and the two-handle shell-declaration observable set.
    rationale: Report *Schema / config* adds exactly one `[[term]]`; *Counts moved* records "`[[term]]` count 5 → 6; observable env set 1 → 2". Carried as Expected amendment 2.
    basis: report.md:27,33

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — Environment variables (`ANDROMEDA_PULSE_L4_DETERMINISTIC`)
    change: Retire the exclusive article — this handle is now ONE OF TWO `shell-declaration` proxies Conductor reads in its own environment for the run contract.
    sidecar: Retired the singular "the run contract's shell-declaration proxy" wording — the observable set is now two handles.
    rationale: Report *Counts moved* — "observable env set 1 → 2"; this bullet restates the same one-observable claim.
    basis: report.md:33
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — Environment variables (`ANDROMEDA_PULSE_MCP_ENABLED`)
    change: Record that Conductor now READS this handle in its own environment — as the run contract's second `shell-declaration` term and as one of the three probe subjects (handle NAMES only, never values) — while it remains asserted **not** written by Conductor.
    sidecar: Registered Conductor's new read of `ANDROMEDA_PULSE_MCP_ENABLED` beside the standing never-written assertion.
    rationale: Report *Env vars* — "the probe READS three existing Pulse-side handles … and writes none" — and *Schema / config*.
    basis: report.md:21,27
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources — `contracts/pulse-run-contract.toml`
    change: Record beside the `warmup_ms` case that the `sidecar-built` term's `asserted` rationale is measurably FALSE — a `PATH` miss short-circuits to `[BLOCKED]` in ~0s UPSTREAM of term evaluation, so the term is satisfied-by-construction exactly when it is false — and that it was deliberately NOT re-classified this chunk (operator-selected at P4).
    sidecar: Recorded `sidecar-built`'s `asserted` rationale as measurably false (same failure shape as `warmup_ms`), surfaced not re-classified.
    rationale: Report *Spec claims disproved by measurement* 2, measured at `verification-harness.md:54` (2026-08-20). Carried as Expected amendment 2.
    basis: report.md:50
```
