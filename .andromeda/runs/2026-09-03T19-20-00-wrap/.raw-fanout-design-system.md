Raw return — design-system drift-detector (fenced in ```yaml plus a trailing "Notes on what I checked and found clean" block; stripped to bare YAML for parsing, notes preserved here as the negative-finding record).

```yaml
proposals:
  - detector: D-design-derived-count
    severity: warning
    section: "Surface: cli → Tokens (platform-specific) (the `Header style:` / status-prefix line)"
    change: "Status-prefix line keeps the closed per-P-ID six unchanged but names the run-level non-lamp bracket-caption SET rather than one member — currently `[ENVIRONMENT-SUSPECT]` (load-envelope) and `[PRECONDITION]` (scheduling-precondition), both ANSI 246, outside the lamp column, neither a lamp state nor a `ReportState`."
    sidecar: "design-system §Surface: cli Tokens — run-level non-lamp caption enumeration re-stated as a set; `[PRECONDITION]` joins `[ENVIRONMENT-SUSPECT]` (per-P-ID six unchanged)."
    rationale: "Report `Counts / qualifiers moved`: 'cli run-level non-lamp bracket labels 1 → 2 — `[ENVIRONMENT-SUSPECT]` joined by `[PRECONDITION]`. `design-system.md:311` enumerates the closed per-P-ID six PLUS the non-lamp caption by name; that enumeration now omits one.' Coverage for the new cli verb records tokens ✓."
    basis: "crates/conductor-cli/src/render.rs:53,57; :216 renders `[PRECONDITION]`"

  - detector: D-design-derived-count
    severity: warning
    section: "Surface: cli → Tokens (platform-specific) (ANSI map, `Residual mute #9A93A8 → ANSI 246` entry, its non-lamp reuse list)"
    change: "The ANSI-246 non-lamp reuse list gains the `[PRECONDITION]` scheduling caption beside the `hint:` stderr label, the coverage-matrix `not-conductors` Mode cell and the `[ENVIRONMENT-SUSPECT]` load-envelope caption — still named as an open set with no count."
    sidecar: "design-system §Surface: cli ANSI map — `[PRECONDITION]` added to the Residual-mute (246) non-lamp reuse set; set framing kept, no count introduced."
    rationale: "Same report bullet plus 'Residual-mute (ANSI 246) non-lamp reuse 3rd → 4th. Not a stale literal … so no count needs substituting' — the member list, not the count, omits the new caption."
    basis: "crates/conductor-cli/src/render.rs:53"
    dependent-of: D-design-derived-count

  - detector: D-platform-claim
    severity: warning
    section: "Surface: cli → Toolkit / Framework"
    change: "Drop `anstream`/`anstyle` from the cli stack; the shipped styling/TTY gate is `owo-colors` 4.x + `std::io::IsTerminal`, gated independently per stream (stdout and stderr), alongside clap 4.5 / `indicatif` 0.18 / `comfy-table` 7 / `inquire` 0.7."
    sidecar: "design-system §Surface: cli Toolkit — `anstream`/`anstyle` retired from the stack; gate named as shipped (`owo-colors` + `std::io::IsTerminal`)."
    rationale: "Report `Spec claims disproved by measurement` #1 — measured absent from both `Cargo.toml`s and all of `crates/conductor-cli/src/`. Also the plan's Expected amendment #4."
    basis: "crates/conductor-cli/src/render.rs:14, :133, :141"

  - detector: D-platform-claim
    severity: warning
    section: "Surface: cli → Tokens (platform-specific) (ANSI-map preamble sentence)"
    change: "Preamble states the ANSI map is applied via `owo-colors` behind a `std::io::IsTerminal` check — no `anstream` layer in the path."
    sidecar: "design-system §Surface: cli Tokens preamble — `anstream` removed from the ANSI-application path; behaviour unchanged."
    rationale: "The retired mechanism is stated at `design-system.md:288` as well as `:284`."
    basis: "crates/conductor-cli/src/render.rs:133"
    dependent-of: D-platform-claim

  - detector: D-platform-claim
    severity: warning
    section: "Surface: cli → Platform-Specific Notes (first bullet)"
    change: "Bullet keeps the `NO_COLOR` / `TERM=dumb` / piped-stdout requirements and attributes them to the shipped per-stream `std::io::IsTerminal` gate, not to `anstream`."
    sidecar: "design-system §Surface: cli Platform-Specific Notes — `anstream` attribution replaced by the shipped per-stream `IsTerminal` gate."
    rationale: "Third stated site of the disproved mechanism (`design-system.md:331`)."
    basis: "crates/conductor-cli/src/render.rs:141"
    dependent-of: D-platform-claim
```

## Negative findings the agent recorded (checked and clean)

- **D-design-tokens — no drift.** Every Coverage row is `tokens ✓` or `tokens n/a`; no `hardcoded✗`, and `[PRECONDITION]` is an always-rendered ASCII bracket label over a reused tier, so no color-only state.
- **design-system.md:321** (Component Pattern 4, the `[ENVIRONMENT-SUSPECT]` caption "never a seventh lamp or a sixth `ReportState`") NOT proposed: that pattern describes the per-P-ID run/suite output path, and `preconditions_caption` renders only from the new verb, so the statement is not falsified.
- **design-system.md:282** (Platform: Windows / macOS / Linux terminal) NOT proposed under D-platform-claim: the capability claim is already the full platform set; naming `agent-run.sh` there is the arrangement, not a capability.
- **design-system.md:406** (Design Decisions Log) already lists the cli stack without `anstream`, and is history/baseline regardless.
- Counts moved that design-system does not bake: CLI verb surface 5→6, run-contract `[[term]]` 5→6, observable env set 1→2, harness command count unchanged at 5.
