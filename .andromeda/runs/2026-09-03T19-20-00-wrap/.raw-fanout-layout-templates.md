Raw return — layout-templates drift-detector (fenced in ```yaml; stripped to bare YAML for parsing).

```yaml
proposals:
  - detector: D-layout-surface
    severity: warning
    section: "Surface: cli → Primary screens (commands)"
    change: Add a `conductor preconditions [--json]` bullet beside run/suite/report/preflight/coverage — the non-mutating live-Pulse probe (`:4317` reachability as a connect-and-drop client, `andromeda-pulse-mcp` resolved over the inherited `PATH` without spawning, and the three `ANDROMEDA_PULSE_*` handles), exiting 0 iff every subject is satisfied (the documented `preflight` go/no-go exception, not the scenario-verdict rule), printing the `[PRECONDITION]` caption (ANSI 246 Residual-mute reuse) only when a subject is unmet, and minting no `Verdict` / `ReportState` / per-P-ID row.
    sidecar: "§Surface: cli Primary screens — registered the new `conductor preconditions [--json]` verb (probe subjects, exit rule, `[PRECONDITION]` caption, no lamp/ReportState row)."
    rationale: Report §Changes → Symbols / APIs registers a **New CLI verb** `conductor preconditions [--json]` with its own exit rule and render fn, and §Coverage of new surfaces lists it as a cli surface with tokens ✓; the doc's cli Primary-screens enumeration lists only five commands, so the shipped surface maps to no entry.
    basis: crates/conductor-cli/src/cli.rs:56

  - detector: D-layout-surface
    severity: warning
    section: "Surface: cli → Primary screens (commands) — the `conductor preflight [--json]` bullet"
    change: Retire "the `agent-run boot` entrypoint" parenthetical — `boot` now leads with `conductor preconditions` and short-circuits on non-zero, **skipping** the preflight invocation rather than paying it, so preflight is the readiness gate `boot` reaches only once every precondition is satisfied, not its first act.
    sidecar: "§Surface: cli Primary screens — `preflight` no longer named as `agent-run boot`'s entrypoint; `boot`'s leading arm is the preconditions probe, which skips preflight on an unmet subject."
    rationale: Report §Changes → Harness / gate surface — both `scripts/agent-run.sh` and `.ps1` `boot` gain a leading probe arm that runs `conductor preconditions` first and short-circuits, "skipping the preflight invocation rather than paying it"; §Outcome Smoke confirms `boot` drove to exit 1 in both shells with **no `ReadyState` JSON emitted**, proving preflight was skipped rather than failed.
    basis: scripts/agent-run.sh:84
    dependent-of: D-layout-surface

  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: cli → Component — Primary navigation (verb structure)"
    change: Replace the baked three-verb sample `conductor {run|suite|report} {arg}` with the SET named by its source of truth — the verbs clap's `Commands` enum declares in `cli.rs` (six as of 2026-09-03) — keeping the flat one-level verb-noun shape as the invariant and any inline example explicitly illustrative-and-dated rather than the closed list.
    sidecar: "§cli Primary navigation — three-verb literal `run|suite|report` retired; the verb set is now named by its source of truth (clap `Commands`), not baked as a sample."
    rationale: Report §Changes → Counts / qualifiers moved — "**CLI verb surface 5 → 6** … Note `architecture.md:33` and **`layout-templates.md:251`**/`:309` state a **three**-verb literal (`conductor run|suite|report`) — already stale before this chunk, now further behind."
    basis: crates/conductor-cli/src/cli.rs:21-56

  - detector: D-layout-derived-count
    severity: warning
    section: "Decisions Log → Key layout choices → **cli**"
    change: Same three-verb literal restated in the decisions-log bullet — replace `verb-noun (`conductor run|suite|report`)` with the set-naming form (verb-noun over the verbs clap's `Commands` enum declares), leaving the "flat, one level, no cursor manipulation" choice untouched.
    sidecar: "Decisions Log → cli — duplicate three-verb literal `run|suite|report` retired in favour of the named verb set (paired with the §cli Primary navigation fix)."
    rationale: Report's Counts bullet names `layout-templates.md:309` alongside `:251` as a second site of the same stale three-verb literal; a single-site apply would leave the retired count alive in the Decisions Log.
    basis: crates/conductor-cli/src/cli.rs:21-56
    dependent-of: D-layout-derived-count

  - detector: D-layout-derived-count
    severity: warning
    section: "Surface: cli → Component — Primary content block 2 (verdict / report-state lines)"
    change: The closing paragraph's "Each surface additionally carries **one** run-level, non-lamp … qualifier" no longer holds for cli — state cli's non-lamp bracket labels as a SET (`[ENVIRONMENT-SUSPECT]`, the once-per-run load-envelope qualifier, and `[PRECONDITION]`, emitted by `conductor preconditions` for an unmet subject; both ANSI 246 Residual-mute reuse, both outside the lamp column), keep "the lamp set stays closed at six", and tell a parser to read the closed per-P-ID six PLUS that non-lamp set.
    sidecar: "§cli Primary content block 2 — non-lamp bracket labels stated as a set (`[ENVIRONMENT-SUSPECT]` + `[PRECONDITION]`) instead of \"one\" qualifier; lamp set still closed at six."
    rationale: Report §Changes → Counts / qualifiers moved — "**cli run-level non-lamp bracket labels 1 → 2** — `[ENVIRONMENT-SUSPECT]` joined by `[PRECONDITION]`", and §Coverage of new surfaces confirms `[PRECONDITION]` reuses the existing Residual-mute ANSI 246 with "zero new palette rows, no seventh lamp, no sixth `ReportState`".
    basis: crates/conductor-cli/src/render.rs:57

  - detector: D-platform-claim
    severity: warning
    section: "Surface: cli → Tooling context"
    change: Drop `anstream`/`anstyle` from the named cli styling stack — the shipped TTY gate is `owo-colors` 4.x + `std::io::IsTerminal` (two independent decisions, stdout and stderr, each requiring a terminal AND `NO_COLOR` unset AND `TERM != dumb`); the mandated behaviour (auto-strip when piped, `NO_COLOR`/`TERM=dumb` honored) is unchanged, only the named mechanism.
    sidecar: "§Surface: cli Tooling context — `anstream`/`anstyle` retired as the named TTY-gating mechanism; measured stack is `owo-colors` + `std::io::IsTerminal`."
    rationale: Report §Changes → Spec claims disproved by measurement #1 — "`anstream` is named as the cli TTY-gating mechanism and is absent… Stated at `design-system.md:284`/`:288`/`:331` and **`layout-templates.md:170`**"; measured absent from both `Cargo.toml`s and all of `crates/conductor-cli/src/`, with `architecture.md`'s stack table already recording owo-colors correctly. Report Expected amendment #4 carries this for layout-templates §Surface: cli.
    basis: crates/conductor-cli/src/render.rs:14

  - detector: D-platform-claim
    severity: warning
    section: "Surface: cli → Component — Header / banner"
    change: Same mechanism claim restated in the banner component — "All styling routes through `owo-colors` **via `anstream`**, so output auto-strips ANSI when piped (`!isatty(1)`)…" must become the measured mechanism (`owo-colors` gated by `std::io::IsTerminal`, decided independently for stdout and stderr, each also requiring `NO_COLOR` unset and `TERM != dumb`), with the auto-strip/NO_COLOR behaviour and the `[OK]`/`[BLOCKED]` prefix requirement unchanged.
    sidecar: "§cli Header / banner — duplicate `via anstream` mechanism claim retired; routes through `owo-colors` under `std::io::IsTerminal` (paired with the Tooling-context fix)."
    rationale: Sweep of the claim retired above — `:247` asserts the same absent mechanism in wording the report's cite (`:170`) does not cover; a single-site apply would leave `anstream` alive as the doc's stated gate.
    basis: crates/conductor-cli/src/render.rs:154
    dependent-of: D-platform-claim
```
