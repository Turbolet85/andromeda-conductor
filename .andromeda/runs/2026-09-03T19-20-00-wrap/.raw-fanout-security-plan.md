Raw return — security-plan drift-detector (fenced in ```yaml plus a trailing no-drift note; stripped to bare YAML for parsing). 6 proposals, ALL severity `escalate` (both firing detectors carry escalate severity in drift-base).

```yaml
proposals:
  - detector: D-security-input
    severity: escalate
    section: "## Input Validation → the `ANDROMEDA_PULSE_L4_DETERMINISTIC` boundary row (table, line 114)"
    change: Broaden the single-handle row into the declaration-only READ SET Conductor observes in its OWN environment — `ANDROMEDA_PULSE_L4_DETERMINISTIC` · `ANDROMEDA_PULSE_MCP_ENABLED` · `ANDROMEDA_PULSE_DATA_DIR` (`conductor_core::OBSERVED_HANDLES`, 3) — all presence-only via `declares()` with nothing to validate because no value ever becomes a path, an argv element or a log value; the first two additionally fold into run-contract `shell-declaration` terms, the third is read only by `conductor_run::observe_preconditions`, and the probe renders handle NAMES only, never a value.
    sidecar: "Input Validation: declaration-only env-read row generalised from `ANDROMEDA_PULSE_L4_DETERMINISTIC` alone to the three-handle `OBSERVED_HANDLES` set added by the preconditions probe."
    rationale: "Report `Env vars` bullet — the probe READS three existing Pulse-side handles and writes none; `Symbols / APIs` adds `const OBSERVED_HANDLES: [&str; 3]`; `Counts moved` records the observable set 1 → 2. Two of the three reads are covered by NO row in the table, so the boundary is undocumented rather than validated."
    basis: report.md:21,15,27,33,61

  - detector: D-security-input
    severity: escalate
    section: "## Input Validation → the `ANDROMEDA_PULSE_DATA_DIR` boundary row (table, line 120)"
    change: Qualify the row as governing the SPAWN-propagation use specifically — the metacharacter-rejection + `.env(...)`-only duty binds `conductor-verify`'s pre-spawn handling and is NOT relaxed or satisfied by the new presence-only probe read, which never carries the handle's value into a path, an argv element, a log field or a spawn.
    sidecar: "Input Validation: `ANDROMEDA_PULSE_DATA_DIR` row scoped to the spawn path, now that the handle also has a non-spawn presence-only reader."
    rationale: "The handle acquired a SECOND reader (`observe_preconditions`) that never spawns, so the row's `(propagated into the spawned sidecar)` scope qualifier no longer describes every read."
    basis: report.md:21,61,86
    dependent-of: D-security-input

  - detector: D-security-input
    severity: escalate
    section: "## Threat Model Summary → Auth model → **Reason** paragraph (line 81)"
    change: Record that Conductor now also READS `ANDROMEDA_PULSE_MCP_ENABLED` (presence-only) from its own process environment, so an absent declaration surfaces as a named precondition subject and an unmet `mcp-enabled` run-contract term BEFORE any spawn — rather than only downstream as the sidecar starting and immediately exiting (2026-08-13); Conductor still never SETS the flag and inheritance is still the sole channel to the child.
    sidecar: "Auth model: `ANDROMEDA_PULSE_MCP_ENABLED` mechanism updated — absence is now observed upstream as a precondition/contract term."
    rationale: "This paragraph restates the whole mechanism of the same handle the primary re-scopes; the doc's 'absent it, the sidecar starts and immediately exits' is no longer the only way the absence appears."
    basis: report.md:27,40,103
    dependent-of: D-security-input

  - detector: D-security-subprocess
    severity: escalate
    section: "## Security Anti-Patterns → ### Input → the `NEVER spawn the MCP sidecar from an operator-chosen command` bullet (line 314)"
    change: Record the second, NON-SPAWNING resolution site of the same fixed program name — `conductor_verify::sidecar_resolves_on_path()` walks `std::env::split_paths(PATH)` + `PATHEXT` for the compile-time `PULSE_MCP_PROGRAM` const, never constructs a `Command`, and returns a boolean-grade fact and never the resolved path — and that on the `agent-run boot` path a `PATH` miss is now named as a `[PRECONDITION]` subject upstream of the gate instead of only surfacing as the ~0s `[BLOCKED]` row measured 2026-08-20.
    sidecar: "Anti-Patterns/Input: sidecar `PATH` rule extended to the new non-spawning `sidecar_resolves_on_path` predicate and the upstream `[PRECONDITION]` naming of a `PATH` miss."
    rationale: "Report `Symbols / APIs` adds the predicate ('never spawns, returns a boolean-grade fact and never the resolved path'); the doc treats `PATH` solely as a SPAWN-resolution input, so the new resolution surface and its never-return-the-path rule are unrecorded."
    basis: report.md:16,40,50,60,88

  - detector: D-security-subprocess
    severity: escalate
    section: "## Security Anti-Patterns → ### Code Patterns → clause (a) of the child-spawn bullet (line 354)"
    change: Note in the spawn-class taxonomy that the fixed-hard-coded-NAME-over-inherited-`PATH` rule now has a non-spawning sibling which resolves the same const without any `Command`, therefore opens no console window and adds no absolute-exe-path disclosure channel of the `CREATE_NO_WINDOW` class, and must keep returning a boolean-grade fact only.
    sidecar: "Anti-Patterns/Code Patterns: spawn-class taxonomy notes the non-spawning sibling resolution of `PULSE_MCP_PROGRAM`."
    rationale: "Clause (a) restates the same fixed-NAME/inherited-`PATH` mechanism the primary amends, beside the measured host-path-pane defect."
    basis: report.md:16,88,114
    dependent-of: D-security-subprocess

  - detector: D-security-subprocess
    severity: escalate
    section: "## Security Anti-Patterns → ### Universal → the `NEVER silently downgrade a failed preflight` bullet (line 367)"
    change: Scope the ban to preflights that are INVOKED and record the sanctioned upstream arm — `agent-run boot` now runs `conductor preconditions` first and short-circuits on non-zero, skipping the preflight entirely, so an unmet precondition on that path mints no `ReadyState`, no `Verdict`/`ReportState` and no `blocked` row; this is not a downgrade because the probe is non-mutating and exits non-zero naming every unsatisfied subject.
    sidecar: "Anti-Patterns/Universal: preflight-downgrade ban scoped to invoked preflights; `boot`'s upstream precondition short-circuit recorded as the sanctioned skip."
    rationale: "Report `Harness / gate surface` + Smoke. As written the bullet models the gate as always running and always minting a distinct `blocked` state, so a `boot` run with no `blocked` row now reads as a violation of its own doc."
    basis: report.md:40,85,103
```

## Negative findings the agent recorded (checked and clean)

- **D-security-deps — no drift.** Dependencies bullet: "none added, none bumped", `Cargo.lock` byte-unchanged, 564 → 564, `which` explicitly rejected; no dev-tool version moved; the doc's `cargo-audit` ≥ 0.22 / `cargo-deny` ≥ 0.19 floors, toolchain ≥ 1.94.1 and tauri ≥ 2.10.3 pins are untouched, and the doc carries **no running red-audit counter** that the 48th re-pin would stale.
- **D-platform-claim — no drift.** Neither `Spec claims disproved` entry names a platform/runner/driver (`anstream`/`owo-colors` appear nowhere in security-plan.md); the driver/NVDA legs were not involved. The doc's `scripts/agent-run.sh` "source of truth" wording is a CI arrangement, not a capability claim, and line 113 already names `scripts/agent-run.{sh,ps1}` as the dual-shell reader.
