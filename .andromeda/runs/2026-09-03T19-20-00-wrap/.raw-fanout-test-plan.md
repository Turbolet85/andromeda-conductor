Raw return — test-plan drift-detector (prose preamble "I have everything I need…" + a fenced ```yaml block + a trailing clear-detector note; stripped to bare YAML for parsing). 9 proposals: 4 primaries + 5 dependent-of sweeps.

```yaml
proposals:
  - detector: D-tests-derived-count
    severity: warning
    section: §1 Test Scope Summary → Surfaces under test → cli (Signal)
    change: the run-level non-lamp captions cli selectors must expect are a named SET — `[ENVIRONMENT-SUSPECT]` and `[PRECONDITION]` — sitting beside the six-member closed per-P-ID lamp/report-state set, replacing the singular "plus that qualifier".
    sidecar: cli status-label enumeration — run-level non-lamp captions named as a SET after `[PRECONDITION]` joined `[ENVIRONMENT-SUSPECT]`.
    rationale: report §Counts moved records the 1 → 2 label move; the doc bakes ONE non-lamp qualifier in a selector label, so a selector asserting "the six plus that qualifier" now under-enumerates cli stdout. Names the set per the 2026-08-09-sut-load-envelope precedent rather than a fresh literal.
    basis: report.md:34, :18

  - detector: D-tests-derived-count
    severity: warning
    section: §6 E2E Test Strategy → Drivers per surface → cli row
    change: the cli driver row asserts the closed per-P-ID six plus the run-level non-lamp caption SET, both NO_COLOR-stable.
    sidecar: §6 cli driver row — non-lamp caption enumeration widened to the named set.
    rationale: same 1 → 2 move; the 2026-08-09 amendment established that all three cli status-label enumerations carry this claim.
    basis: report.md:34
    dependent-of: D-tests-derived-count

  - detector: D-tests-derived-count
    severity: warning
    section: §6 E2E Test Strategy → Selector strategy
    change: the brand-anchor selector list names the run-level non-lamp qualifier SET instead of the single `[ENVIRONMENT-SUSPECT]`.
    sidecar: §6 Selector strategy — non-lamp qualifier enumeration widened to the named set.
    rationale: third occurrence; the selector strategy is the site an E2E author reads to build assertions.
    basis: report.md:34
    dependent-of: D-tests-derived-count

  - detector: D-tests-obs-harness
    severity: warning
    section: §3 Test Harness Contract → 5-command implementation → `boot`
    change: `boot`'s body now runs `conductor preconditions` as a LEADING arm and short-circuits non-zero on an unmet subject — skipping the preflight invocation rather than paying it, emitting no `ReadyState` JSON — with the preflight gate, its two derived timeout budgets and the readiness signal unchanged behind it; identical semantics in `.sh` and `.ps1`.
    sidecar: §3 `boot` — leading `conductor preconditions` probe arm recorded; command count still five, envelope/status-read/JSONL shapes untouched.
    rationale: report §Harness/gate surface + §Outcome smoke (exit 1 in BOTH shells, no `ReadyState` JSON). Expected amendment 3. The probe mints no `Verdict`/`ReportState`/per-P-ID row, so §3 ↔ obs-plan §3 still agree; one-sided by construction (obs-plan §3 carries no harness command bodies), matching the 2026-08-10-pulse-run-contract precedent.
    basis: report.md:40, :103

  - detector: D-tests-obs-harness
    severity: warning
    section: §1 Test Scope Summary → Test harness requirements → 5-command requirements → `boot`
    change: `boot` is a precondition probe THEN a preflight readiness gate — the live-Pulse prerequisite is now probed and exit-coded ahead of the gate rather than only stated as a Creator-Brief precondition.
    sidecar: §1 5-command `boot` requirement — precondition stated as a probed leading arm, not prose-only.
    rationale: duplicate occurrence of the retired claim "boot reduces to a preflight readiness gate", carrying the very "Precondition — live Pulse + mcp-server feature reachable" line the chunk turned into an exit-coded probe.
    basis: report.md:3, :40
    dependent-of: D-tests-obs-harness

  - detector: D-tests-obs-harness
    severity: warning
    section: §3 Test Harness Contract → opening paragraph
    change: the no-daemon sentence now reads that `boot` is a precondition probe plus a preflight readiness gate, not a preflight readiness gate alone.
    sidecar: §3 intro — `boot` restated as probe-then-preflight; no-daemon / no-listener claim unchanged.
    rationale: token-free restatement of the same mechanism ("so `boot` is a preflight readiness gate"); the sweep for what the claim SAYS finds it here even though it names no command body.
    basis: report.md:40
    dependent-of: D-tests-obs-harness

  - detector: D-tests-obs-harness
    severity: warning
    section: §3 Test Harness Contract → Bootstrap phases → 5-command-discipline-wire
    change: the wiring phase names boot as "precondition probe + preflight gate" in the five-command list, so re-materializing `scripts/agent-run.{sh,ps1}` reproduces the leading probe arm and its non-zero short-circuit in both shells.
    sidecar: §3 5-command-discipline-wire — boot's wiring recorded as probe-then-preflight; still five commands.
    rationale: this is the line setup-project consumes to materialize the harness; left unamended it would re-materialize a probe-less boot. Count stays five.
    basis: report.md:36, :40
    dependent-of: D-tests-obs-harness

  - detector: D-tests-coverage
    severity: warning
    section: §3 Test Harness Contract → Test data bootstrap → Per-test isolation
    change: the `CONDUCTOR_RUNS_DIR` sandbox is owed only where the CLI subprocess under test WRITES run artifacts — the handle is repo-relative by design and an absolute `temp.path()` is rejected by `resolve_under` before the verb dispatches, so a CLI-edge test of a verb that writes nothing sets no handle at all.
    sidecar: §3 Per-test isolation — runs-dir sandbox qualified to artifact-writing CLI tests; handle recorded as repo-relative.
    rationale: report Deviation 1 — the new CLI-edge tests set no handle where the plan prescribed one; with one set, "the probe printed nothing and both new tests failed on empty stdout". §3's own e2e-seed bullet already records the handle as repo-relative, so the two subsections of §3 currently contradict each other.
    basis: crates/conductor-cli/tests/cli_smoke.rs:359-360; report.md:66

  - detector: D-tests-coverage
    severity: warning
    section: §2 Test Strategy → Agent-runnable invariants (Self-bootstrapping fixtures)
    change: the `assert_fs::TempDir`-backed file `runs.db` invariant applies per cross-process test THAT WRITES run artifacts, not to every cross-process test.
    sidecar: §2 self-bootstrapping fixtures — TempDir-backed runs.db invariant qualified to artifact-writing cross-process tests.
    rationale: duplicate occurrence of the universal the §3 amendment retires; a single-site apply would leave §2 asserting that every cross-process test carries a TempDir-backed `runs.db`, which this chunk's two new subprocess tests deliberately and correctly do not.
    basis: report.md:66
    dependent-of: D-tests-coverage
```

## Negative findings the agent recorded (checked and clean)

- **D-tests-framework — clear.** nextest `--profile ci` 691/691 plus the standing `cargo test` runner-portability leg, rstest per-subject matrix, `Command::cargo_bin` inside the owning package per §5, zero new dev-deps, `Cargo.lock` byte-unchanged, no `retries`.
- **D-platform-claim — clear.** The two `Spec claims disproved` entries name a crate mechanism and a contract check-kind, no platform/runner/driver verdict; `anstream` does not appear anywhere in test-plan.md; the harness bullet reaffirms rather than retires the identical-`.sh`/`.ps1` claim — both shells drove to exit 1.
