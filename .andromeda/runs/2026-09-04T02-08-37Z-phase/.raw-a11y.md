# a11y extract — raw twin

<!-- twin reconstructed: inverse-encode of a11y.md (fan-out.md sanctioned form when only the
     decode changed bytes). No preamble or trailing commentary was stripped; the body began at
     "# a11y extract" and ended at the last amendment-history bullet. -->

The return transport HTML-escaped the following, and nothing else:

  "&lt;body&gt;" at three sites (Constraints focus-restoration, Acceptance criteria, amendment history)

Every other byte is identical to `a11y.md`. To reconstruct the raw return, re-encode those
occurrences as their entity forms in the saved copy.
