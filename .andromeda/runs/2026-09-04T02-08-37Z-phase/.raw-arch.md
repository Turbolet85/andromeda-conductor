# arch extract — raw twin

<!-- twin reconstructed: inverse-encode of arch.md (fan-out.md sanctioned form when only the
     decode changed bytes). No preamble or trailing commentary was stripped; the body began at
     "# arch extract" and ended at the last amendment-history bullet. -->

The return transport HTML-escaped the following, and nothing else:

  one "&amp;" in the Relevance line ("CARRY 2a &amp; 2b")

Every other byte is identical to `arch.md`. To reconstruct the raw return, re-encode those
occurrences as their entity forms in the saved copy.
