# design extract
<!-- twin reconstructed: inverse-encode of design.md. The return transport HTML-escaped one ampersand;
     the sole byte difference from the saved copy is the line below, which arrived as
     "§Component Patterns 3 &amp; 4". No other entity appeared in the return. -->

Raw form differed from the saved copy at exactly one site:

    - Status must never be carried by color/border alone — ... (per §Component Patterns 3 &amp; 4, §Anti-Patterns Universal Bans).

Saved copy decodes that to:

    - Status must never be carried by color/border alone — ... (per §Component Patterns 3 & 4, §Anti-Patterns Universal Bans).

Everything else in the return is byte-identical to `design.md`; no preamble or trailing commentary was
stripped (the body began at `# design extract` and ended at the last amendment-history bullet).
