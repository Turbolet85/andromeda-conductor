# layouts extract — raw twin

<!-- twin reconstructed: inverse-encode of layouts.md (fan-out.md sanctioned form when only the
     decode changed bytes). No preamble or trailing commentary was stripped; the body began at
     "# layouts extract" and ended at the last amendment-history bullet. -->

The return transport HTML-escaped the following, and nothing else:

  "&lt;body&gt;" in the 2026-09-01-desktop-a11y-sweep amendment bullet

Every other byte is identical to `layouts.md`. To reconstruct the raw return, re-encode those
occurrences as their entity forms in the saved copy.
