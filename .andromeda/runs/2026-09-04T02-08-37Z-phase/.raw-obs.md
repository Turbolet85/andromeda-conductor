# obs extract — raw twin

<!-- twin reconstructed: inverse-encode of obs.md (fan-out.md sanctioned form when only the
     decode changed bytes). No preamble or trailing commentary was stripped; the body began at
     "# obs extract" and ended at the last amendment-history bullet. -->

The return transport HTML-escaped the following, and nothing else:

  "&lt;name&gt;" in the tauri.command span guards (x2) and "&lt;runs_dir.parent()&gt;" in the sink path (x2)

Every other byte is identical to `obs.md`. To reconstruct the raw return, re-encode those
occurrences as their entity forms in the saved copy.
