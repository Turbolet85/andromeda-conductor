# security extract — raw twin

<!-- twin reconstructed: inverse-encode of security.md (fan-out.md sanctioned form when only the
     decode changed bytes). No preamble or trailing commentary was stripped; the body began at
     "# security extract" and ended at the last amendment-history bullet. -->

The return transport HTML-escaped the following, and nothing else:

  "&lt;host-path&gt;", "&lt;run_id&gt;.md", "&lt;repo script&gt;", "&lt;constant&gt;"

Every other byte is identical to `security.md`. To reconstruct the raw return, re-encode those
occurrences as their entity forms in the saved copy.
