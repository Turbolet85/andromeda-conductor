# arch extract

## Relevance
Relevant — architecture.md §Standard Contracts (Liveness equivalent) is the passage that *defines* the probe, records this exact defect, and names this chunk's fix as route-owned-not-shipped.

## Constraints
- The precondition probe's contract is fixed at THREE graded subjects (`egress-reachable` · `sidecar-resolvable` · `handles-declared`) over the three `ANDROMEDA_PULSE_*` names, and architecture.md §Standard Contracts (Liveness equivalent) states the required fix in terms: **grade the path handle by PRESENCE and keep the truthy gate for the two flags**. This chunk discharges that mandate; it may not renegotiate the subject set or the observed-handle set.
- The probe's disposition is pinned and must survive the change: it reuses the connect-and-drop primitive (no bind, no OTLP written, `:4318` untouched), and an unmet subject is a **REPORTED unmet precondition plus a non-zero exit — never a `Result::Err` and never a verdict**, because the probe runs before any scenario exists to carry one (per architecture.md §Standard Contracts, Liveness equivalent; reinforced by §Cross-cutting Patterns — Verdict/error wall).
- The second `declares()` consumer is a resource architecture.md itself pins: the run contract's OBSERVABLE `shell-declaration` set is **exactly TWO flag handles** (`ANDROMEDA_PULSE_L4_DETERMINISTIC` · `ANDROMEDA_PULSE_MCP_ENABLED`), and `shell-declaration` is the ONLY term kind that may block (per architecture.md §Occupied Resources — `contracts/pulse-run-contract.toml`). Truthiness is the correct grading there, so a fix that loosens a shared predicate would silently widen a gate this plan defines.
- Reporting stays **name-only**: architecture.md §Occupied Resources (`ANDROMEDA_PULSE_MCP_ENABLED`) requires the probe's finding to name handles and never values, and §Standard Contracts requires every precondition string to be host-path-free with `data_dir` redacted. Grading a PATH handle by presence must not put its value on any surface.
- Conductor READS these handles and never WRITES them — the discipline architecture.md §Occupied Resources records for `CONDUCTOR_AGENT_MODE` (`flag || env-set`, main never writes, deliberately avoiding edition-2024 `unsafe std::env::set_var`). The chunk's `[inferred]` test seam must satisfy this rather than mutate process env.
- Crate placement is compiler-enforced: `conductor-run` is the run composition-root library above the seams and below both bins, `conductor-core` the runtime-agnostic library every crate depends on (per architecture.md §Established Decisions [Module Boundaries] + §Occupied Resources — Crate names). Observation-site grading in `conductor-run` and per-handle-kind data beside `OBSERVED_HANDLES` in `conductor-core` both respect that direction; the reverse edge would not compile.
- No new configuration handle: `ANDROMEDA_PULSE_*` are Pulse-side and asserted **not set by Conductor**, and `CONDUCTOR_*` is the reserved namespace for handles this document already defines (per architecture.md §Occupied Resources — Environment variables).

## Patterns to follow
- Read-never-write env consumption via a caller-supplied value — the `CONDUCTOR_AGENT_MODE` `flag || env-set` shape (architecture.md §Occupied Resources — Environment variables) is the in-plan precedent for the lookup-parameter seam the scope infers.
- Two production callers, deliberately different dispositions over ONE shared primitive — architecture.md §Standard Contracts (Liveness equivalent) already models this for the `:4317` connectability check (engine → `Err`; probe → reported precondition). Per-call-site grading policy over a shared read is the same shape the scope's boundary #4 needs.
- Host-path-valued handles are treated as paths, not flags, elsewhere in the plan: `CONDUCTOR_MSEDGEDRIVER` / `CONDUCTOR_NVDA` are validated at the harness edge and produce a host-path-free precondition naming the handle, never its value (architecture.md §Occupied Resources — Environment variables).
- `.sh`/`.ps1` parity for every `agent-run` arm — the headless path is the release gate and the source of truth (architecture.md §Design Philosophy; §Infrastructure Patterns — Build system; §Cross-cutting Patterns — Development Style).
- Verb surface is de-literalized: the CLI's verbs are whichever `Commands` declares, and the enumerating home is layout-templates, not arch (architecture.md §Stack and Technologies — CLI argument parsing).

## Anti-patterns to avoid
- Making the env read testable via `unsafe { std::env::set_var }` — architecture.md §Occupied Resources (`CONDUCTOR_AGENT_MODE`) records avoiding exactly that as the reason the shipped read is structured as it is.
- Converting a probe refusal or unmet subject into `Result::Err` or into a `Verdict`/`ReportState` (architecture.md §Standard Contracts — Liveness equivalent; §Cross-cutting Patterns — Verdict/error wall).
- Emitting a handle's VALUE (or any host path) into a finding, precondition string, or `--json` payload (architecture.md §Standard Contracts — precondition strings host-path-free; §Occupied Resources — `ANDROMEDA_PULSE_MCP_ENABLED`).

## Contract bindings
- **arch ↔ security/obs (redaction):** the probe's name-only reporting is an arch-stated property that security's input/redaction rules and obs's self-obs redaction both consume; whether the shipped finding shape already holds it under a presence-graded path handle is research's question.
- **arch ↔ tests (harness parity):** architecture.md §Infrastructure Patterns / §Design Philosophy make `scripts/agent-run.{sh,ps1}` the release gate, binding the scope's "`boot` reaches the preflight, proven in both shells" to the test plan's .sh/.ps1 parity clause.
- **arch ↔ run contract (`contracts/pulse-run-contract.toml`):** the `shell-declaration` observable set of two flag handles is an arch-registered resource; any predicate change must leave `observe_run_contract`'s grading unchanged. Whether the code today shares one predicate across both sites is research's question.
- **Wrap reconcile surface:** architecture.md §Standard Contracts (Liveness equivalent), §Occupied Resources (`CONDUCTOR_PREFLIGHT_TIMEOUT`), and §Occupied Resources (`ANDROMEDA_PULSE_MCP_ENABLED`) currently RECORD the defect (unsatisfiable subject · unconditional `boot` skip · value-gated check); landing this fix makes all three stale-in-the-other-direction and they must move together — the duplicate-occurrence discipline this plan's amendment history repeatedly enforces.

## Acceptance criteria contributions
- (arch) `handles-declared` becomes satisfiable: the PATH-valued handle is graded by presence and the two flag handles keep affirmative truthiness, so `conductor preconditions` can exit 0 under a correct environment (per architecture.md §Standard Contracts — Liveness equivalent, which names this fix as route-owned).
- (arch) The probe still reports an unmet subject as a finding plus a non-zero exit — never `Result::Err`, never a verdict — and still performs connect-and-drop only: no bind, no OTLP written, `:4318` untouched (per architecture.md §Standard Contracts — Liveness equivalent).
- (arch) Findings and precondition strings name handles only — no env value and no host path on any surface, `--json` included (per architecture.md §Occupied Resources — `ANDROMEDA_PULSE_MCP_ENABLED` + §Standard Contracts host-path-free rule).
- (arch) The run contract's `shell-declaration` grading is unchanged over its two flag handles, no new `CONDUCTOR_*`/`ANDROMEDA_PULSE_*` handle is introduced, no `unsafe std::env::set_var` appears, and code sits in `conductor-run` (observation site) / `conductor-core` (handle-kind data) per crate-per-seam edges (per architecture.md §Occupied Resources — `contracts/pulse-run-contract.toml` + Environment variables; §Established Decisions [Module Boundaries]).

## Relevant amendment history
- **2026-09-04-sr-findings-remediation — "the precondition probe cannot exit 0 (path handle graded by a boolean)"** (§Standard Contracts Liveness equivalent · §Occupied Resources `CONDUCTOR_PREFLIGHT_TIMEOUT` · `ANDROMEDA_PULSE_MCP_ENABLED`): applied as a RECORD of the defect, not a fix, from the 2026-09-04T06:41Z measurement; the entry explicitly names the corrective route entry as owner. Its "Search performed" list (grep for `preconditions` / `observe_preconditions` / `declares` / `handles-declared` / `boot` / `ANDROMEDA_PULSE_` / "short-circuit" / "unmet precondition" / "presence-only") is the reconcile sweep this chunk's wrap inherits — three sites carried the claim.
- **2026-09-03-live-pulse-preconditions-probed — "preconditions probe registered; run contract at six terms"**: established the two-callers/two-dispositions record for the `:4317` check, the `boot` leading arm and its skipped timeout, and the six-term run contract whose observable `shell-declaration` set is TWO handles. It also records **NOT applied**: registering the CLI verb itself in arch (playbook `:61` over-reach — the verb's home is layout-templates §cli), so this chunk should expect no arch verb registration even if the verb's output is touched.
- **2026-08-10-pulse-run-contract — "preflight timeout floor + the L4 declaration Conductor reads"**: first established that Conductor READS a Pulse-side flag in its own environment as a shell-declaration proxy, with an absent declaration surfacing as an unmet term naming candidate causes rather than a claimed measurement of `pulse-app` — the disposition the path handle's presence grading must not disturb.
- **2026-06-24-sanitized-stderr-agent-mode-logging — `CONDUCTOR_AGENT_MODE` registered**: the origin of the read-never-write env discipline (`flag || env-set`, main never writes, avoiding edition-2024 `unsafe set_var`) — the precedent the chunk's inferred test seam should follow.
- **2026-06-23-5-command-agent-run-harness — `CONDUCTOR_PREFLIGHT_TIMEOUT` registered**: recorded the `boot`-wraps-`conductor preflight` path this chunk must prove is reachable again in both shells.
