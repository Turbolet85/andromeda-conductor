# arch extract

## Relevance
Relevant — the chunk edits a shipped seam crate, touches the registered sidecar-spawn resource, and adds a root-level build config file, all of which arch governs.

## Constraints
- The fix belongs in the shared seam crate `crates/conductor-verify` (the MCP read-back seam), NOT in either bin shell: the headless `agent-run` path is the release gate and the GUI is "convenience only", so a suppression that only the GUI inherits would leave the source-of-truth path defective (per architecture.md §Design Philosophy "Headless-drivable core, thin shells" + §Established Decisions [Workspace / Core Structure]).
- The spawn's three recorded hardening properties — fixed program NAME resolved through the inherited `PATH`, the `.env(...)`-only `ANDROMEDA_PULSE_DATA_DIR` pass, and injection-reject — are mandated unchanged; this chunk may only ADD to the construction (per §Established Decisions [MCP Read-Back Client] "the hardened spawn … is unchanged" + §Occupied Resources — Service/process names, which records `PATH` as a spawn-resolution input).
- The child's stdio IS the read-back transport: the client is line-delimited JSON-RPC over the sidecar's stdout/stdin, so any console-suppression construction must leave the inherited/piped stdio intact — a detach-class construction that severs stdio would break the locked read-back decision (per §Established Decisions [MCP Read-Back Client] + §Conventions — Inbound verification).
- No new occupied resource: no new `CONDUCTOR_*` handle (the namespace is an enumerated reservation over artifacts/concepts arch already defines), no new port, no new crate. If the flag cannot be reached through `std` and a dependency is proposed, it inherits the dependency gate — a named `deny.toml` justification per crate and dependency path, with `cargo deny` green (per §Occupied Resources — Environment variables + §Infrastructure Patterns — Build system).
- Cross-platform compile is a workspace property, not a per-file choice: the workspace is one Rust 2024 / MSRV 1.94.1 build and arch's own precedent in this spawn path is platform branching (`%APPDATA%` on Windows vs `$XDG_CONFIG_HOME`/`~` on Linux for the propagated data-dir), so a Windows-only flag must be `cfg`-gated rather than unconditional (per §Occupied Resources — `ANDROMEDA_PULSE_DATA_DIR` + §Inherited Defaults — Language/runtime). Whether CI's matrix compiles a non-Windows target that would catch a `cfg` mistake is research's question — arch §Infrastructure Patterns — CI/CD only says "the dev OS target".
- CARRY 2's `rustfmt.toml` is a NEW workspace-root config artifact absent from arch's canonical directory tree, so landing it is a wrap-amendment candidate to that tree; its content restates an already-locked decision (edition 2024) and introduces no new one (per §Infrastructure Patterns — Directory structure + §Stack and Technologies / §Inherited Defaults — Language/runtime).
- CARRY 1's edits are doc comments on `#[tauri::command]` functions, which arch pins as the internal core↔UI IPC surface; correcting comment text must not move the command contract itself (`run_report` / `run_envelope` return shapes, the `Channel`) (per §Conventions — Interface surfaces "Internal core↔UI" + §Occupied Resources — Interface routes/surfaces).

## Patterns to follow
- Seam-local additive change with no new `Cargo.toml` dependency edge — the crate-per-seam workspace makes a forbidden cross-seam dep fail to compile, so the correct shape is a change contained in `conductor-verify` with its dependency set untouched (per §Established Decisions [Module Boundaries]).
- Platform branching inside the spawn seam, already precedent in the same file per arch's record of the platform-default data-dir resolution (per §Occupied Resources — `ANDROMEDA_PULSE_DATA_DIR`).
- The "route-owned → shipped → retire the stale clause" shape used by the immediately prior pair of chunks: ship the fix, then a wrap amendment retires the "not yet shipped" wording at EVERY site a phrase-grep finds, with a post-edit re-grep returning 0 (per §Established Decisions [Read-Back Dependency Posture] supersession style; amendment entries 2026-09-04 ×2 below).
- Verdict/error wall untouched: a construction-only change adds no error variant — a failed connect still fans into `VerifyError` and reaches the report as `Blocked`, never a new `Err` class (per §Conventions — Error handling + §Cross-cutting Patterns — Verdict/error wall).

## Anti-patterns to avoid
- Do NOT remediate the symptom in the GUI shell (window re-activation / foreground management in `conductor-tauri`) — that would fix the convenience artifact and leave the headless release-gate path raising the pane (per §Design Philosophy "Headless-drivable core, thin shells").
- Do NOT reopen or restructure the read-back client / spawn decision — rmcp is removed and hand-rolled JSON-RPC over the sidecar's stdio is locked; this chunk adds a flag, it does not re-derive the transport or the program resolution (per §Established Decisions [MCP Read-Back Client]).
- Do NOT introduce a `CONDUCTOR_*` (or any) env handle to make the console suppression configurable — the reserved namespace is an explicit enumeration, and each addition has required a registration amendment (per §Occupied Resources — Environment variables).

## Contract bindings
- **arch ↔ security / obs (the same spawn site, four registrations):** security-plan §Anti-Patterns (a) and obs-plan §11 record `spawn.rs:80` as route-owned and not-yet-shipped; arch independently registers the SAME child in TWO places — §Occupied Resources — Service/process names and §Cross-cutting Patterns — Trust boundary — and both describe the launch as "via `TokioChildProcess` (stdio)", a type the scope measured absent from the code since the 2026-06-27 rmcp removal. A wrap sweep of the spawn's arch sites therefore belongs beside the security/obs staleness retirement; whether the arch wording is genuinely stale in code is research's question.
- **arch ↔ a11y/tests (the SR suite registration):** §Cross-cutting Patterns — Trust boundary registers "a fixed-argv PowerShell window-activation script" as a leg-spawned child of the screen-reader suites. Retiring `reactivateWindow()` retires only ONE of that script's two call sites (`bringToForeground()` stays per the scope), so the arch registration stands either way — this chunk owes arch no trust-boundary retirement even if the conditional retire fires.
- **arch ↔ security/obs (host-path discipline):** §Standard Contracts requires every readiness precondition string to be host-path-free with `data_dir` redacted, and §Occupied Resources applies the same name-only rule to operator handles; a window title carrying the sidecar's absolute exe path would sit outside that edge, which is the arch-side reason the suppression is in-scope. Whether the pane in fact raises and carries that title is research's / the live re-run's question.

## Acceptance criteria contributions
- (arch) The creation-flag change is contained in `crates/conductor-verify` with that crate's dependency edges unchanged; neither bin shell carries spawn-suppression logic of its own (per architecture.md §Established Decisions [Module Boundaries] + §Infrastructure Patterns — Directory structure).
- (arch) The spawn still resolves a fixed program NAME through the inherited `PATH`, passes the data-dir via `.env(...)` only, keeps its injection-reject, and keeps the child's stdio as the JSON-RPC transport (per §Established Decisions [MCP Read-Back Client] + §Occupied Resources — Service/process names).
- (arch) No new port, crate, or `CONDUCTOR_*`/`ANDROMEDA_PULSE_*` env handle is added; if any new dependency is pulled, `cargo deny check advisories bans licenses sources` stays exit 0 with a named `deny.toml` entry (per §Occupied Resources — Environment variables + §Infrastructure Patterns — Build system).
- (arch) The workspace still compiles for non-Windows targets — the flag is `cfg`-gated, following the spawn path's existing platform-branch precedent (per §Inherited Defaults — Language/runtime + §Occupied Resources — `ANDROMEDA_PULSE_DATA_DIR`).

## Relevant amendment history
- **2026-06-27-mcp-read-back-result-shape-adapter** — the last structural change to THIS seam: rmcp removed, read-back flipped to hand-rolled line-delimited JSON-RPC over the sidecar's stdio; the entry explicitly records "the hardened spawn + the `2024-11-05` manifest pin + the preflight gate are unchanged" and cleared the security subprocess detector on the grounds that "spawn hardening preserved". That is the precedent shape for this chunk's clearance: a spawn change is acceptable when the hardening set is demonstrably carried through unchanged.
- **2026-09-01-desktop-a11y-sweep** — registered the sidecar's fixed-NAME-through-`PATH` resolution as a spawn-resolution input, measured 2026-09-01 (off-`PATH` ⇒ preflight `BLOCKED` in ~2ms with all four tools `absent`, indistinguishable at row level from a SUT-side gate failure). Directly bounds this chunk: the construction change must not alter name resolution or that blocked-in-~2ms shape.
- **2026-09-02-screen-reader-manual-spec** — the wrap of the very leg whose SR finding S1-01 seeded this chunk; it registered `CONDUCTOR_NVDA`, moved the suite families 2→3, and added the NVDA + fixed-argv PowerShell activation script to §Cross-cutting Patterns — Trust boundary. That registration is what the `reactivateWindow()` retire question touches (and, per the binding above, does not overturn).
- **2026-09-04-sr-findings-remediation** then **2026-09-04-preconditions-probe-reads-path-handles-by-presence** — the immediately prior pair, same "a plan says the fix is route-owned and not shipped → the next chunk ships it → the wrap retires the prior entry at all sites" cycle, including the explicit "Search performed" phrase-grep record and a post-edit re-grep returning 0 for every retired phrase. This chunk's four in-repo staleness sites should discharge the same way.
