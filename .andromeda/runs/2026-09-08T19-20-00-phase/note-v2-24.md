2026-09-08 (2026-09-08-webview2-runtime-152-installed-in-job, phase P5): NOT CLAIMED, cap stays pooled at `planned`. FAILED CONCRETIZATION on CLAIM-REACHABILITY, not on substance. This chunk installs WebView2 Evergreen runtime 152+ in the `a11y` job (signed bootstrapper, `Get-AuthenticodeSignature` gated BEFORE execution) and re-runs probe (a) — the follow-up the prior wrap's note routed. But the acceptance requires the routine specs to RUN IN the `a11y` CI job with any violation failing the gate, and that evidence is authored by a CI run which necessarily follows this chunk's commit and push; no step in this chunk's lifecycle can set `ref`, since /implement writes refs at P2 on LOCAL green. The `requirement` line is even less permissive than the acceptance — "Axe, contrast and keyboard PASS/FAIL is emitted as service-tagged violation JSON and gated in CI" — so no reading admits locally-green specs as satisfying it. PRECEDENT DELIBERATELY NOT REPEATED: this identical cap was CLAIMED at `2026-09-07-sr-findings-fixed` phase P5 and UN-CLAIMED at that same chunk's implement P2 for exactly this reason; claiming again now, on a probe whose outcome is unknown at planning time, would repeat a measured mistake from a weaker basis than the one that failed. Nothing about the capability's substance is in doubt — the routine arm already carries the hold-free Operable pair green locally at 12 passing / 2 skipped — what is missing is a CI run that completes, which is precisely what this chunk probes for. BOTH OUTCOMES COMPLETE THE CHUNK, and the wrap-time disposition is an operator fork stated in advance: if probe (a) reports `DevToolsActivePort` appearing under runtime 152+, the runtime major was the cause and the cap stays `planned` with a follow-up entry owning the claim on a green job; if the endpoint still never opens, the runtime-major hypothesis is FALSIFIED and recorded with its mechanism, `deferred` becomes the honest status since no route forward would remain inside this version, and the surviving unmeasured candidates are the ones the prior verdict named (no interactive desktop session on a hosted runner; a Session-0 / service-account restriction on the WebView2 browser process; an image policy on remote debugging). OPERATOR P5 REVIEW: the chunk's Expected amendment against security-plan covers TWO surfaces, both to be judged at wrap — §Security Anti-Patterns → Code Patterns rule (b), whose verbatim governed-spawn-form count (five) a sixth crossing escalates, AND §Dependency Security, because a signed third-party installer fetched and executed at CI time is a new supply-chain dependency of the `a11y` job that no existing gate bounds (`cargo audit`/`cargo deny` cover the Rust crate graph, `npm audit` the frontend tree, and an Evergreen-endpoint binary appears in neither nor in either lockfile), with the Authenticode check as its admitting control.
