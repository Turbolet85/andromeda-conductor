2026-09-09 (2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate, wrap P7.3): DEFERRED — terminal for this version, re-opened at the next version's route intake. The runtime-major hypothesis is FALSIFIED, so no route forward remains inside 0.2.0.

MEASURED. CI run 34280136892 (2026-09-08) installed WebView2 Evergreen 152.0.4191.66 in-job — Authenticode `Valid`, signer `O=Microsoft Corporation`, re-read seven minutes later by the diagnostics step — and the bare-app probe still reported `DevToolsActivePort first seen: never within 90s` with the app alive (`HasExited=False`) and three `msedgewebview2` children resident in Session 2. The endpoint therefore opens under NEITHER runtime major on the hosted `windows-2025` image; the runtime major was not the cause. Two further measured facts bound the search. (1) The routine arm's own failure has moved UPSTREAM of the endpoint question: tauri-driver never listens on 127.0.0.1:4444, four refused session POSTs inside ~1.5 s, the same shape as runs 34162118841 / 34251573399 / 34256490781 — all at runtime 151 and driver 151 — so it predates both the 152 upgrade and any runtime/driver skew. (2) Probe (b)'s msedgedriver log shows the driver's own bind taking 6.9 s (21:37:29.0 → 21:37:35.9) while wdio's POSTs fire in the first ~1.5 s, which reads as a startup race rather than a capability gap. Related and measured at the same wrap: the runtime × driver PAIR is not the discriminator at major granularity — a 152 × 151 combination both PASSES on the dev host (152.0.4191.53 × 151.0.4129.101) and FAILS on the hosted image (152.0.4191.66 × 151.0.4129.101).

UNMEASURED CANDIDATES, carried for the next version's intake. (a) A hosted-image policy on remote debugging — an Edge/WebView2 policy registry read is the one-line probe and is the leading candidate. (b) A session or service-account property of the hosted runner. (c) msedgedriver 151 against a 152 runtime as a cause of the DRIVER failing to start — an axis the prior chunk never tested, since it retired skew only as a cause of the ENDPOINT failing to open. Self-hosted runners on a public repo are not an option, so that route is closed rather than untried.

The chunk that shipped the probe claimed nothing and this deferral rests on the run's reading, not on an argument that the known obstacles are gone. A cross-version residual carries the re-open.
