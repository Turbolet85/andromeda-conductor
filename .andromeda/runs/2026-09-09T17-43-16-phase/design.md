# design extract

## No domain coverage
This chunk is Rust test-runner hygiene (a process-global tracing subscriber installed by `crates/conductor-faults/tests/port_occupier.rs` racing under shared-process libtest, plus measuring `conductor-verify`/`-run`/`-cli`/`-tauri` under `cargo test -p`) and renders no operator surface, so no token, typography, motion, iconography or component-pattern mandate in `.andromeda/design-system.md` applies — the `obs-plan` §4 allowlist probe and `conductor_core::{init_observability, ObsSink}` are observability/backend contracts, not design ones — though if P3/P4 finds the fix must change human-facing `conductor-cli` output, design-system §Surface: cli (TTY-gated ANSI via `owo-colors` + `IsTerminal`, the closed `[PASS]`/`[FAIL]`/`[HOLD]`/`[MANUAL]`/`[RESIDUAL]`/`[BLOCKED]` prefix set, never color-alone, stdout-data vs stderr-messages split) would then bind and this extract should be re-run.
