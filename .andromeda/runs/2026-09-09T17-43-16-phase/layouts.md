# layouts extract

## No domain coverage

This chunk is entirely test-runner hygiene inside the Rust workspace (`crates/conductor-faults/tests/port_occupier.rs`, a process-global tracing subscriber, and `cargo test -p` measurements across four crates) — it renders no surface, adds no component, changes no wireframe region, focus order, dialog structure or empty state on either the desktop-webview or cli surface; the only cli-adjacent artifacts named (`cargo test`/`cargo nextest`) are build-tool commands, not `conductor-cli` verbs or output structures that `layout-templates.md` templates, and the `P-003 port-occupier` sample rows in that plan are illustrative verdict-line labels the chunk does not move.
