Scenarios keyed to P-067, P-072 and P-079 each drive a live Pulse and yield a non-blocked verdict via MCP read-back, with journal and runs.db evidence per scenario — concretized: each is driven by `conductor run <scenario> --agent-mode` behind a >=150s quiet window (120s Pulse idle + a full 30s resolver tick), the operator's pulse-app launched with the `mcp-server` feature and `ANDROMEDA_PULSE_MCP_ENABLED` + `ANDROMEDA_PULSE_L4_DETERMINISTIC` + an `ANDROMEDA_PULSE_DATA_DIR` equal to that live instance's dir; read-back is reached unconditionally (`conductor-run/src/execute.rs:98` calls `observe()` before any declare-only branch), and each scenario's newest `runs.db` row and its journal envelope line — selected by the envelope-only `seed` key, never the file's last line — carry a `state` other than `Blocked`, measured by the two listed probes reading `non-blocked rows: 3` and `journal envelopes non-blocked: 3`. The per-scenario evidence additionally survives the commit under `conductor-0.2.0/chunks/2026-09-10-live-pulse-in-lane-scenario-round/evidence/` (three envelope lines verbatim plus the three runs.db row values), because `/runs/` is gitignored.
