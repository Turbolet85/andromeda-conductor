2026-09-10 (2026-09-10-live-pulse-in-lane-scenario-round, phase P5): P-079's `constellation-severity-live-wiring` carries a Hard `CountAtLeast >= 1` that is STRUCTURALLY UNSATISFIABLE against a live Pulse, so its non-blocked verdict rests on the grading route rather than on a met floor. Measured across both repos this pass: Pulse writes `EvidenceRefs.span_ids: Vec::new()` at its only production incident-construction site (`andromeda-pulse pulse-app/src/inference_runtime.rs:871`) and nothing mutates it after (whole-repo enumeration; the only non-empty writes are a unit-test fixture at `crates/mcp-server/src/tools.rs:868` and a `vec![]` at `crates/interpretation/src/markdown.rs:507`); `dispatch_retrieve_telemetry_slice` maps `span_refs` 1:1 from it (`crates/mcp-server/src/tools.rs:430-439`); Conductor sums `evidence_count` from `span_refs` (`conductor-verify/src/extract.rs:116`) and grades every `CountAtLeast` against that count (`extract.rs:55`), so `compare` evaluates `0 >= 1` (`conductor-verify/src/slo.rs:53-62`) and an unmet sample floor routes to `ClaimClass::CalibrationRegion` regardless of the declared class (`slo.rs:97-100`). The row is therefore non-blocked (`CalibrationRegion` verdict / `ManualCheck` state) and `v2-04`'s acceptance is met as worded, but that one check asserts nothing about P-079's capability. This is a CLASS, not a P-079 defect, and not this chunk's to fix: exactly three committed scenarios carry a live `kind = "CountAtLeast"` key (`scenarios/constellation-severity-live-wiring.toml:53`, `findings-counter-refresh.toml:49`, `pulse-run-contract.toml:58` — a bare token grep returns seven files because four mention it only in retirement comments), the mechanism is already recorded at `.andromeda/architecture-amendments.md:403` ("`CountAtLeast` grades `span_refs` the incident producer writes empty", 2026-08-21, the ground on which five severity-lifecycle scenarios were retired to declare-only), and `.andromeda/architecture.md:69` records the same outcome as measured and accepted for the still-shipping sibling `findings-counter-refresh`. Owner: a route-entry candidate raised at this chunk's wrap.
