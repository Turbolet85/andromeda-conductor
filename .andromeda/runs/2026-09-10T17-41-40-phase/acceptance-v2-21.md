verification-matrix.json exists with exactly one entry per requirements.md capability, ids unique; no P-NNN appears in an id position anywhere in the matrix (Pulse P-IDs occur only inside acceptance text). Concretely: a committed-artifact `--test` target asserts, over the ACTIVE version directory resolved at run time rather than a baked `conductor-0.2.0` literal (which would rot at 0.3.0), that (a) the set of `v2-NN` capability ids declared in `requirements.md` is EQUAL to the set of `id` values in `verification-matrix.json` — asserted as set equality with both directions named on failure, never as a literal count, per the test-plan §1 signal that forbids a count assertion; (b) the matrix's ids are unique; and (c) no id matches `^P-\d+$`, so no Pulse P-ID occupies an id position. The target follows the `crates/conductor-report/tests/coverage_gate.rs` precedent: paths resolve from `env!("CARGO_MANIFEST_DIR")` rather than a CWD-relative default (a cargo test binary runs with its CWD at the package root), and an explicit non-vacuity guard fails the gate if either id set is empty, so the assertion cannot pass by reading nothing. Measured true at HEAD before the gate exists (32 requirement ids, 32 matrix entries, sets equal, all unique, zero `^P-\d+$`), so the gate makes an already-holding property CHECKED and regression-proof rather than establishing it — which is what makes this claim reachable by /implement's own enumeration, with no wrap-authored artifact in its evidence path.
