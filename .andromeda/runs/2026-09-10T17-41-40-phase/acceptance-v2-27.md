A release binary and a Tauri 2 bundle are produced with cargo-audit and cargo-deny green and Cargo.lock un-drifted, and a final SLO verification pass is recorded against the shipped artifact. Concretely: (1) SUPPLY CHAIN, run BEFORE the build — the local RustSec advisory-db checkout's `git status --porcelain` is empty (a local untracked residue is excluded before any red is classed external), bare `cargo audit` exits 0 (never `--deny warnings`, which exits 1 by construction against the deny.toml-adjudicated allowed set), `cargo deny check advisories bans licenses sources` exits 0, and `cargo metadata --locked` succeeds over the committed lock; (2) BINARY — `cargo build --release --workspace` produces the release binaries, and the bundle-serving GUI binary is built `cargo build --release -p conductor-tauri --features tauri/custom-protocol` after `npm run build`, since the custom-protocol FEATURE and not the profile decides bundle embedding and `generate_context!` resolves `build.frontendDist` at compile time; (3) BUNDLE — a Tauri 2 bundle is emitted under `target/release/bundle/` by the Tauri CLI (a host dev-tool floor, `cargo install tauri-cli --locked`), with its installer paths, sizes and the RESOLVED tauri-cli version recorded in the chunk's evidence/ — the recorded version being what keeps the floor auditable without converting it into a pin; (4) SLO PASS — three bounded in-lane legs are driven through the SHIPPED release binary (`./target/release/conductor run <scenario> --agent-mode`, never preceded by `agent-run boot`, each behind a 150 s quiet window): `live-only-service-truth` (`<20s`), `exception-event-capture` (`<5s`) and `cross-incident-recurrence` (`<20s`), each returning a non-Blocked verdict whose envelope satisfies `latency_ms <= threshold(slo_tier)` against 5000/20000/90000 ms evaluated at report-generation time, with the journal and `runs.db` row committed per scenario under evidence/ because `/runs/` is gitignored; and a just-minted run_id read back through `agent-run.sh status` (mint-then-read — a bare status asserts nothing). The legs deliberately EXCLUDE `constellation-severity-live-wiring`, whose `<20s` tier is unattainable by construction and already ledgered open in `.andromeda/residuals.md` as not blocking the release; that exclusion is about the unattainable TIER only — the capability it stands for was verified non-blocked by `v2-04` at `2026-09-10-live-pulse-in-lane-scenario-round`, so the pass forgoes no coverage this chunk owes.
