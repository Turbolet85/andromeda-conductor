# A11y validation — route draft

## Insert
- Between `Keyboard and focus-order coverage ownership` and `Real-model leg posture and grading rule`: **"Unbuilt a11y surfaces at a terminal — `contentinfo` landmark and report-site checklist render present, or ratified absent with a named owner"** (epoch: `Epoch 3`)
  Reason: a11y-plan §4's landmark table and §3/§5 record both the footer `contentinfo` strip and the report-site checklist render as designed-but-unshipped and explicitly **route-owned** since 2026-09-02, and §11 Screen Reader bans a landmark-less window.

## Rewrite
- `Keyboard and focus-order coverage ownership`: "SC 2.1.1 and SC 2.4.3 owned by a named suite, and what gates in CI instead" → "SC 2.1.1 and SC 2.4.3 owned by a named suite, and the hold-dependent trap/restoration half's CI carve-out stated"
  Reason: a11y-plan §5 run-console-idle and §11 Strategy record the hold-free Operable pair as asserted on the CI-wired routine arm since 2026-09-07 — only the hold-dependent half still needs a stated CI substitute, so "what gates in CI instead" rests on a retired reading.
- `A11y CI gate at an honest terminal`: "routine arm green in CI" → "routine arm's asserted-verdict green in CI"
  Reason: a11y-plan §11 CI requires the strict-mode printed-verdict assertion because a skipped-green run and a real pass are otherwise indistinguishable; `a11y-ci-gate-wire` is the only §3 Bootstrap item this version still owes (the other six realized pre-0.3.0), so this terminal carries the whole bootstrap claim.
- `Full-gate regression over the moved surfaces`: "the `a11y` job" → "the `a11y` job at zero WCAG AA violations"
  Reason: a11y-plan §10 SLO Invariants makes zero WCAG AA violations across the four must-be-accessible paths the job's pass bar, which the chunk's current test-plan-only gating does not state.
