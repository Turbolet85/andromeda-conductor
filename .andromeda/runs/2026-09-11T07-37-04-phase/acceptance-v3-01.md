`conductor-0.3.0/chunks/2026-09-11-hosted-runner-endpoint-cause-probed/evidence/reading.md` records, for each of the three named probes — policy state, loaded module versions, and session identity — what the hosted `windows-2025` runner returned on a named CI run, identifying the job and its steps BY NAME and stating the run by id, event, branch and conclusion. Each hosted-runner reading is stated BESIDE its dev-host control value, captured by the same `scripts/webview2-cause-probe.ps1` rather than hand-approximated, so each pair discriminates a hosted-image property from a universal one and the three together are sufficient to decide whether the endpoint failure is remediable or permanent. A probe that returned nothing — an absent registry key, no live process, or an Access Denied on module enumeration — is recorded as having returned that rather than omitted, and a reading that contradicts the expected cause is recorded unchanged. The reading carries no absolute host path, proven by the committed `grep -cE` hygiene gate over `reading.md` reading `0`, not asserted. The run's red is attributed to the standing WebView2 session-creation failure rather than to this chunk, and no green, conformance or platform-capability claim is drawn from any probe.
