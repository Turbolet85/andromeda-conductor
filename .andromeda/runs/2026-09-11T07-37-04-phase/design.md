# design extract

## No domain coverage
This chunk adds diagnose-only registry/process probes to the `a11y` job in `.github/workflows/ci.yml` plus a recorded evidence artifact — it ships no webview or `conductor-cli` surface (`contracts/`/`crates/`/`scenarios/` untouched, no change to the gate's contrast assertions), so no token, typography, motion, iconography, or component-pattern mandate from `design-system.md` applies.
