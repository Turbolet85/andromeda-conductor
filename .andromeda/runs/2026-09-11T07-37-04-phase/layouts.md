# layouts extract

## No domain coverage

This chunk adds diagnose-only CI steps to the `a11y` job in `.github/workflows/ci.yml` plus a recorded-readings evidence artifact under `conductor-0.3.0/chunks/{marker}/evidence/`; it renders nothing on either surface layout-templates owns (desktop-webview console, `conductor-cli` verb output / `runs/<run_id>.md` report), mints no verb, bracket label, lamp, region or focusable element, and explicitly excludes the console-footer / landmark work that is the one layout obligation nearby (scope §What this chunk does NOT build, deferred to `v3-02`).
