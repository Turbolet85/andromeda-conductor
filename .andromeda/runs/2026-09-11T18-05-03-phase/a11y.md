# a11y extract

## Relevance
Partial — the chunk operates entirely inside the `a11y` CI job (this domain's gate) and its dev-host arm, but adds no UI surface, no ARIA/WCAG criterion, and no new assertion; a11y contributes gate-integrity, artifact-hygiene, and platform-claim discipline only.

## Constraints
- The `a11y` job is a fail-fast gate whose non-zero exit must stay owned by the routine arm's result; every probe added or relocated must remain diagnose-only (`continue-on-error: true` + `if: always()`) and move no verdict (per a11y-plan.md §11 Anti-Patterns → CI). Whether the isolation step at the scope's cited lines already carries that posture is research's question.
- Exit code alone is not a pass: a11y-plan.md §11 Anti-Patterns → CI requires `CONDUCTOR_A11Y_STRICT` (unresolved host-tool handle exits non-zero instead of skipping) **plus** an assertion on the runner's PRINTED verdict — `Spec Files:` failed count, per-spec skip tally against the expected-skip SET, driven-session banner — because a skipped-green run and a real pass are otherwise indistinguishable. Arm B's elevated re-run is graded against that same mechanism, not against exit 0.
- Exactly ONE webview-automation stack (`@crabnebula/tauri-driver` + WebdriverIO); standing up a second automation/CDP/raw-msedgedriver path for probing is banned (per a11y-plan.md §11 Anti-Patterns → CI, second-stack ban).
- Committed a11y/harness artifacts MUST NOT leak absolute host paths or internal seam-crate struct names (per a11y-plan.md §11 Anti-Patterns → SLO, and §3 Structured violation JSON schema, which stores selectors DOM-relative, never filesystem paths). The scope's `ModuleName` + `FileVersion`-only rule (never `FileName`) is the same rule applied to the probe.
- No WCAG conformance may be claimed from this chunk's readings — conformance requires machine-verifiable axe-core / colorjs.io / WebdriverIO JSON evidence (per a11y-plan.md §2 Agent-runnable invariants and §11 Anti-Patterns → Universal). An elevated local routine-arm run is a measurement of the arm executing, not a conformance claim.
- Platform statements must distinguish the measured platform SET from the CI ARRANGEMENT, and must claim only what was measured (per a11y-plan.md §9 CI Integration → Platform, and §1 CI integration). Arm B's null or positive result is dev-host-scoped by construction; asserting a universal retirement of elevation would break this rule.
- a11y-plan.md §11 Anti-Patterns → Strategy records the CI PROOF of the routine hold-free Operable pair as still OWED, with the `a11y` job red at WebView2 session creation and ownership on the *Hosted-runner WebView2 session* route entry — this chunk sits in that lineage and must not be written as discharging it.

## Patterns to follow
- Identify CI jobs and steps BY NAME, never by `ci.yml` line coordinates, which move as the workflow grows (per a11y-plan.md §3 Configuration). The scope's touchpoints are line-cited (`:382`–`:512`, `:411`, `:422`, `:513`); this chunk edits `ci.yml` and will invalidate them.
- Host-tool handle pattern: an unset handle skips at exit 0 locally (`CONDUCTOR_MSEDGEDRIVER`, `CONDUCTOR_NVDA`) while CI declares `CONDUCTOR_A11Y_STRICT` to make it non-zero (per a11y-plan.md §3 Bootstrap `a11y-ci-gate-wire` + §11 CI) — the established model for any section-selectable switch added to the probe script.
- The routine arm needs no live Pulse: it seeds its own committed fixture through the production writer (per a11y-plan.md §9 per-pipeline-stage E2E row), which is what makes arm B's elevated dev-host re-run unattended and repeatable.
- Evidence split — graded/reviewed record committed, raw capture kept out of the repo — is the shipped SR-leg pattern (`nvda-pass-spec.md` graded into `evidence/nvda-pass.json`, per a11y-plan.md §3 Screen reader test pattern); the scope's "raw captures stay under gitignored `runs/`" follows it.
- CI-emitted a11y artifacts carry `service.name` / `deployment.environment` plus `ci.run.id` / `git.commit.sha` (per a11y-plan.md §9 CI-specific resource attributes) — applicable if any probe reading is emitted through the artifact path rather than only into chunk evidence.

## Anti-patterns to avoid
- Never introduce a second browser-automation stack (puppeteer/CDP, or a raw msedgedriver attach) to obtain the probe reading (per a11y-plan.md §11 Anti-Patterns → CI).
- Never let a diagnostic step mask, weaken, or substitute for the gate's verdict, and never allow a skipped run to read as green (per a11y-plan.md §11 Anti-Patterns → CI, the skipping ban and its shipped mechanism).
- Never let committed artifacts or probe output carry absolute host paths or internal struct names (per a11y-plan.md §11 Anti-Patterns → SLO).

## Contract bindings
- **a11y ↔ tests §6** — the `a11y` job rides the one tauri-driver/WebdriverIO webview stack and the binding 5-command harness surface (boot / run / status / cleanup / logs), with results emitted through `logs` (per a11y-plan.md §3 CI integration → Command, §9 Pipeline integration). Changes to the job's steps touch that shared surface.
- **a11y ↔ obs §3/§6** — violation/result artifacts fold into the obs envelope and inherit its resource tags (13 keys local / 15 under CI), with `journal_conformance` asserting key presence (per a11y-plan.md §3 Structured violation JSON schema).
- **a11y ↔ arch §Stack (CI/CD rationale cell)** — a11y-plan quotes that cell verbatim at THREE sites (§1 CI integration, §9 Pipeline integration, §9 ROUTINE-checks bullet); if this chunk alters the `a11y` job's gate set, all three citations move together.

## Acceptance criteria contributions
- (a11y) The `a11y` job's pass/fail remains determined solely by the routine arm's printed verdict; the module-version invocation and every other probe are `continue-on-error: true` + `if: always()` and contribute nothing to the job's exit status (per a11y-plan.md §11 Anti-Patterns → CI).
- (a11y) Arm B's elevated dev-host run is graded by the printed verdict and the expected-skip SET (failed-spec count + per-spec skip tally, against the recorded 12 passing / 2 skipped control), not by exit code alone, with `CONDUCTOR_A11Y_STRICT` semantics preserved so an unresolved handle cannot skip green (per a11y-plan.md §11 Anti-Patterns → CI).
- (a11y) No committed probe artifact or evidence record contains an absolute host path or internal seam-crate struct name — the module reading carries `ModuleName` + `FileVersion` only (per a11y-plan.md §11 Anti-Patterns → SLO + §3 Structured violation JSON schema).
- (a11y) No second browser-automation stack is introduced, and the chunk records its readings as measurements without asserting WCAG conformance or a green routine-arm CI proof (per a11y-plan.md §11 Anti-Patterns → CI second-stack ban + §11 Anti-Patterns → Strategy).

## Relevant amendment history
- **2026-09-07-a11y-ci-gate** — wired the routine arm into CI as job `a11y` on `windows-2025`, and explicitly replaced stale `ci.yml:18`/`:178` coordinates with job NAMES because line coordinates move as the workflow grows. Directly binds this chunk: it edits `ci.yml` inside that job and the scope's touchpoints are line-cited. The same amendment shipped the `CONDUCTOR_A11Y_STRICT` + printed-verdict mechanism behind the skipping ban.
- **2026-09-07-sr-findings-fixed** — moved the hold-free Operable pair (SC 2.1.1, SC 2.4.3) onto the routine arm at 12 passing / 2 skipped with the expected-skip SET unchanged, and recorded the CI PROOF as still OWED with the `a11y` job red at WebView2 session creation, owner *Hosted-runner WebView2 session*. That is the defect lineage this chunk measures, and the source of arm B's control shape.
- **2026-09-08-hosted-runner-webview2-session** — recorded the job's first push-triggered run (34209940695, RED at WebView2 session creation): wired, exercised, never green. Establishes the prior reading this chunk's measurements extend.
- **2026-09-06-coverage-completeness-gate** — retired two false present-tense CI-existence claims after `ci.yml` line coordinates went stale; the operator resolution was NARROW (retire only "a job exists today" claims, leave target-arrangement descriptors standing). Sets the precedent for how a `ci.yml` change here should and should not cascade into the plan.
- **2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate** — moved three verbatim citations of arch §Stack's CI rationale cell, and recorded that a11y-plan's WebView2 runtime/driver literals are DATED DEV-HOST measurements, not provisional claims about the hosted image, so a hosted-image falsification does not retire them. Directly governs how arm B's dev-host elevated reading may be written.
- **2026-09-01-webview-self-verify-windows-host** — retired the Linux+`xvfb`-only capability framing to a measured platform SET and introduced the `D-platform-claim` detector plus the epistemic-status rule (claim only what the leg measured; label the untested half). This is the discipline the scope's own "elevation on the dev host and on a hosted image are not self-evidently the same condition" hypothesis must be written under.
