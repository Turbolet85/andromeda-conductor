RAW return — architecture drift detector (pre-decode; YAML folded-scalar `&gt;-` preserved as returned).

```yaml
proposals:
  - detector: D-arch-decisions
    severity: warning
    section: "Infrastructure Patterns — Build system"
    change: &gt;-
      Retire the always-latest float posture: state that the `a11y` job's in-job WebView2 Evergreen
      install is CONDITIONAL as of 2026-09-12 — it runs only when the pre-step measured runtime major
      is below 152, so on an image already at or above the floor (the hosted `windows-2025` image
      ships 152.0.4191.66) the runtime in effect is the IMAGE's and the job mints no driver/runtime
      major skew of its own; the runtime is floored-at-152, not "always-latest / chosen at job time",
      while the Authenticode-not-lockfile admission and the pin-once-it-gates disposition are unchanged.
    sidecar: "arch §Infrastructure Patterns — Build system: WebView2 runtime float restated as a conditional 152+ floor install (was \"always-latest Evergreen in-job\") — 2026-09-11-hosted-runner-endpoint-cause-closed."
    rationale: &gt;-
      Report **Dependencies**: "The CI-time third-party fetch (the WebView2 Evergreen bootstrapper …)
      becomes CONDITIONAL: it now runs only when the measured pre-step runtime major is below 152, so
      the outbound surface is strictly smaller, never wider"; **Harness / gate surface** (2) "`Install
      WebView2 Evergreen runtime 152+ (gate)` installs only below the floor"; **Counts / qualifiers
      moved** — runtime in effect on the hosted image `153.0.4234.32` → `152.0.4191.66`, "Any doc
      baking a post-install runtime literal for that image is stale". The report's Expected-amendment 1
      names this exact site (`architecture.md:205`) as the architecture co-owner the plan missed.
    basis: D:/dev/projects/conductor/.andromeda/architecture.md:205

  - detector: D-arch-decisions
    severity: warning
    section: "Established Decisions — [CI/CD]"
    change: &gt;-
      Amend the in-job provisioning sentence ("**Since 2026-09-08 the job provisions its own WebView2
      runtime in-job**, ahead of the routine arm … then a post-install assertion that the runtime major
      is ≥ 152 — the step carries no `continue-on-error` and no `if:`") to say the provisioning is now
      conditional on the measured pre-step major (&lt;152 fetches, ≥152 skips), that the pre-execution
      `Get-AuthenticodeSignature` gate guards the only path that fetches, and that the `≥ 152` floor
      assertion sits OUTSIDE the conditional and still runs every job — `continue-on-error` remains
      absent, so the gate never passes by skipping.
    sidecar: "arch §Established Decisions [CI/CD]: in-job Evergreen provisioning restated as conditional-below-152, floor assertion unconditional."
    rationale: &gt;-
      Same Dependencies + Harness/gate-surface change as the primary; this bullet restates the retired
      unconditional-provisioning mechanism in its own words (the actor, the fetch, and the "no `if:`"
      qualifier), so a single-site apply at §Infrastructure Patterns would leave it alive.
    basis: D:/dev/projects/conductor/.andromeda/architecture.md:59
    dependent-of: D-arch-decisions

  - detector: D-arch-decisions
    severity: warning
    section: "Occupied Resources — Ports"
    change: &gt;-
      In the `https://go.microsoft.com/fwlink/p/?LinkId=2124703` entry, qualify the fetch as
      conditional — the `a11y` job reaches this egress target only when the pre-step measured WebView2
      runtime major is below 152, so an already-compliant image performs no fetch at all; the endpoint
      stays registered, the HTTPS + Authenticode admission is unchanged, and the outbound surface is
      strictly narrower than when registered on 2026-09-08.
    sidecar: "arch §Occupied Resources — Ports: fwlink Evergreen egress marked conditional (reached only below the 152 floor)."
    rationale: &gt;-
      Report **Dependencies**: the CI-time third-party fetch "becomes CONDITIONAL … the outbound
      surface is strictly smaller, never wider". The registry entry states the fetch as something the
      job does in that step, which now holds only on the below-floor path.
    basis: D:/dev/projects/conductor/.andromeda/architecture.md:147
    dependent-of: D-arch-decisions

  - detector: D-arch-decisions
    severity: warning
    section: "Cross-cutting Patterns — Trust boundary"
    change: &gt;-
      Change "the `a11y` job's one-shot HTTPS fetch of the WebView2 Evergreen bootstrapper" to a
      CONDITIONAL one-shot fetch — taken only when the job measures a pre-step runtime major below 152
      — keeping the rest of the claim (CI-only, reached by no shipped binary and no dev-host harness
      leg, loopback-only holds for every shipped and local path) intact.
    sidecar: "arch §Cross-cutting Patterns — Trust boundary: third outbound surface restated as a conditional (below-152) fetch."
    rationale: &gt;-
      Third restatement of the same retired claim (the fetch happens on every a11y CI run); report
      **Dependencies** narrows it to the below-floor path only.
    basis: D:/dev/projects/conductor/.andromeda/architecture.md:248
    dependent-of: D-arch-decisions

  - detector: D-platform-claim
    severity: warning
    section: "Established Decisions — [CI/CD]"
    change: &gt;-
      Retire the open-cause verdict this bullet states in the doc's own voice — "**No replacement
      candidate is named:** … an observed difference and NOT a demonstrated cause, since no probe
      varied it, and a third candidate … returned NO reading at all because that probe's step runs
      after the isolation step stops the app (0.645 s later, measured)" (and, upstream, "the cause
      stays OPEN, owned by the `v2-24` deferral note"). Replace with the measured set: the module
      reading WAS taken on the hosted runner inside the isolation step's live window beside a dev-host
      control; elevation WAS varied — an elevated leg on the Windows dev host reproduces `session not
      created: DevToolsActivePort file doesn't exist` with a non-elevated control on the other side —
      so elevation is the established cause, driver/runtime major skew is separately retired by a
      controlled variation (arm C), and the hosted-runner endpoint stays closed with a remedy now
      testable rather than a permanent exclusion. Bound the statement to the two platforms actually
      measured, never to a single fresh literal.
    sidecar: "arch §Established Decisions [CI/CD]: module-probe no-reading and unvaried-elevation verdicts retired — reading taken (arm A) and elevation varied (arm B); cause established as elevation."
    rationale: &gt;-
      Report **Spec claims disproved by measurement** 2 names this exact site. Corroborated by
      **Harness / gate surface** (1), disproved-claim 4, disproved-claim 3, and the Insufficient-fixes
      bullet. Evidence: CI runs `34645345201` / `34654076633` and the chunk's `evidence/reading.md`.
    basis: D:/dev/projects/conductor/.andromeda/architecture.md:59
```

Clean-findings recorded by the agent: D-arch-resources no hit (no new registrable resource; `WEBVIEW2_*`
lifetime at `:197` preserved by construction; the probe script already registered at `:220`).
`:240` ("fails at WebView2 session creation") still TRUE — not a hit. `:59`'s run-pinned historical
literal is past-tense and run-scoped — not a stale current value.
