RAW return — security-plan drift detector (pre-decode; `&lt;` preserved as returned).

- **D-security-input** — the only new input surface is `-Section` on a CI-only PowerShell script, and the report's Coverage bullet records `[ValidateSet]`✓ + `[CmdletBinding()]`✓; no `CONDUCTOR_*` handle, no deserialized struct, no MCP-stdout change. No hit.
- **D-security-subprocess** — zero `.rs` delta, spawn-form count still SIX, `.env(...)`/preflight/data-dir untouched. No hit.
- **D-security-deps** — hit: the Dependencies bullet makes the third dependency class's fetch conditional, and disproved-claim 1 names `security-plan.md:185` directly.
- **D-platform-claim** — no sentence in security-plan.md states a retired platform/runner/driver verdict (`Linux` at :121 is a default-path enumeration; "the runner image's `$env:EDGEWEBDRIVER`" at :116 names a host, states no capability verdict; the report itself records that no spec states the skew claim). No hit.

```yaml
proposals:
  - detector: D-security-deps
    severity: escalate
    section: Dependency Security — "A THIRD dependency class (added 2026-09-08…)" paragraph (the version-disposition sentence)
    change: Replace the "always-latest Evergreen, deliberately and temporarily … the chunk that added it is a PROBE whose subject is the runtime MAJOR" rationale with the measured disposition — the fetch+install is now CONDITIONAL, running only when the step's measured pre-install runtime major is below 152 (strictly narrower outbound surface, never wider), because the probe's subject is no longer the runtime major but the driver/runtime SKEW that the job's own always-latest install created over a coherent 152 image; keep the exit condition, and state that the `≥ 152` floor assertion stays OUTSIDE the conditional with Authenticode verification and the absent `continue-on-error` unchanged.
    sidecar: Dependency Security — third dependency class: always-latest Evergreen rationale EXHAUSTED; WebView2 bootstrapper fetch/install is now conditional on measured pre-step major &lt; 152, floor assertion outside the conditional.
    rationale: Report **Dependencies** bullet — "The CI-time third-party fetch (the WebView2 Evergreen bootstrapper, the third dependency class) becomes CONDITIONAL: it now runs only when the measured pre-step runtime major is below 152, so the outbound surface is strictly smaller, never wider… `continue-on-error` ABSENT, `Get-AuthenticodeSignature` at indent 2 … and the `≥ 152` floor assertion at indent 0 outside the conditional" — plus **Spec claims disproved by measurement 1**, which names `security-plan.md:185` and rules the version disposition "EXHAUSTED, not contradicted" (probe subject is now the SKEW; runs `34645345201` install→153 over a 152 image vs `34654076633` install skipped, all three on 152). The report's **Expected amendments — Amendment 1** carries this site explicitly.
    basis: conductor-0.3.0/chunks/2026-09-11-hosted-runner-endpoint-cause-closed/report.md:27-32,66-72,86-92

  - detector: D-security-deps
    severity: escalate
    section: Threat Model Summary → Infrastructure → **Networking** bullet
    change: Qualify the one non-loopback egress as conditional — the `a11y` job's HTTPS fetch of the WebView2 Evergreen bootstrapper now occurs only when the job's measured pre-install runtime major is below 152, so the CI-only egress is narrower than when the class was admitted 2026-09-08 (still reaching no shipped code).
    sidecar: Threat Model Summary → Infrastructure/Networking: the CI-only WebView2 bootstrapper egress is now conditional on the measured pre-step runtime major &lt; 152.
    rationale: The doc states the fetch as a standing, unconditional property of the `a11y` job ("The one non-loopback egress in the project is CI-only … the `a11y` job's HTTPS fetch of the WebView2 Evergreen bootstrapper"), which the Dependencies bullet's "becomes CONDITIONAL … strictly smaller, never wider" retires; a single-site apply at §Dependency Security would leave the unconditional acquisition alive in the threat model.
    basis: conductor-0.3.0/chunks/2026-09-11-hosted-runner-endpoint-cause-closed/report.md:27-30
    dependent-of: D-security-deps

  - detector: D-security-deps
    severity: escalate
    section: Threat Model Summary → Infrastructure → **CI/CD** bullet
    change: Narrow "that job fetches and executes a signed third-party installer (the WebView2 Evergreen bootstrapper)" to state that it does so only below the 152 floor — the uncovered-by-audit surface is now entered conditionally, still admitted by the same pre-execution Authenticode verification.
    sidecar: Threat Model Summary → Infrastructure/CI-CD: the audited-surface widening is conditional — the `a11y` job fetches/executes the bootstrapper only when the measured pre-install runtime major is below 152.
    rationale: Same retired claim restated in the doc's CI/CD bullet without the `always-latest` token; the report's Dependencies bullet makes the acquisition conditional and its **Harness / gate surface** bullet records "(2) `Install WebView2 Evergreen runtime 152+ (gate)` installs only below the floor."
    basis: conductor-0.3.0/chunks/2026-09-11-hosted-runner-endpoint-cause-closed/report.md:27-32,47-53
    dependent-of: D-security-deps
```
