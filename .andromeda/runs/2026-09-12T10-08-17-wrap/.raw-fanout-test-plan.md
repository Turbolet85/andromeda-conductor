RAW return — test-plan drift detector.

```yaml
proposals:
  - detector: D-platform-claim
    severity: warning
    section: "## 9. CI Integration — Matrix builds → OS bullet (the hosted `windows-2025` cause paragraph, the sentence beginning `**No replacement candidate is named**`)"
    change: "Retire the clause `**No replacement candidate is named**: the run recorded one difference (the runner is elevated, the dev host is not) which is a difference and not a demonstrated cause, and the third candidate — the module version the host processes actually load — returned NO reading, its probe's step running 0.645 s after the isolation step stops the app` and say instead that all three were measured 2026-09-12: the module reading WAS taken inside the isolation step's live window (run `34645345201`, `evidence/reading.md`, `[diag] (2) msedgewebview2` non-zero beside its dev-host control), the driver/runtime major SKEW is retired as a cause by direct variation with a control (run `34654076633`: install skipped, `majors: 2 1`, still `never within 90s`), and ELEVATION is now a demonstrated reproducer — the elevated routine arm on the known-good dev host reproduces `session not created: DevToolsActivePort file doesn't exist` beside a committed non-elevated control, REMEDIABLE IN PRINCIPLE and bounded to the host it was measured on — so the cause is no longer open-with-no-candidate and the remedy is owned by the `v3-02` route entry."
    sidecar: "§9 Matrix builds: replaced the unread-module-probe (0.645 s) and not-a-demonstrated-cause clauses — module reading taken in-window (arm A), major skew retired by control (arm C), elevation demonstrated as the reproducer on the known-good host (arm B), remedy owned by `v3-02`."
    rationale: "Report `Spec claims disproved by measurement` #2 retires both halves by measurement and its `Expected amendments` bullet names **`test-plan.md:469`** as the co-owner site that restates BOTH the 0.645 s no-reading fact and the \"difference and not a demonstrated cause\" clause; the `Harness / gate surface` bullet is what makes the reading possible; #4 records that elevation reproduces the runner's exact signature with a control on both sides."
    basis: "conductor-0.3.0/chunks/2026-09-11-hosted-runner-endpoint-cause-closed/report.md:73-77 · :47-53 · :81-82 · :166-169 · evidence/reading.md"

  - detector: D-platform-claim
    severity: warning
    section: "## 6. E2E Test Strategy — Drivers per surface table, desktop-webview row (the sentence ending `leaving the image open with no named candidate`)"
    change: "Same retirement at its second site: the row's `**and the two candidates this row once named, a policy and a session property, are both MEASURED and retired** (CI run `34586959536`, 2026-09-11 …), leaving the image open with no named candidate` must now NAME elevation as the measured candidate — the elevated routine arm reproduces the hosted signature on the dev host with a control both ways (2026-09-12) — and record that arm C removed the driver/runtime major-skew confound (equal majors, `majors: 2 1`) with the endpoint still closed, so the skew is retired as a cause and the row no longer says the image is open with no named candidate."
    sidecar: "§6 Drivers-per-surface (desktop-webview): `no named candidate` retired — elevation named as the measured reproducer, driver/runtime major skew retired by control."
    rationale: "Duplicate occurrence of the claim the §9 amendment retires; the report's sweep named only `test-plan.md:469`, so a single-site apply would leave `leaving the image open with no named candidate` alive in the §6 driver table. Report disproved-claim #3 states the skew is retired as a cause by direct variation with a control."
    basis: "conductor-0.3.0/chunks/2026-09-11-hosted-runner-endpoint-cause-closed/report.md:78-82 · gate table :208-213"
    dependent-of: D-platform-claim
```

Clean findings: D-tests-coverage no hit (both new paths CI-only and gated red-before-green; the RED
`cargo nextest` is a deterministic PRE-EXISTING failure at `matrix_ledger_gate.rs:49`, landed in `b3b51e6`,
out of the modify-set with a BLOCKING owner minted — not a §10 flakiness break). D-tests-framework no hit.
D-tests-obs-harness no hit (5-command surface, envelope, `status` disk read and JSONL shapes untouched, so
§3 ↔ obs-plan §3 stay in sync). D-tests-derived-count no hit (zero `153` hits; every `152.0.4191.66` at
`:56`, `:307`, `:469` is an attributed sample tied to a named run inside an explicitly-named SET).
