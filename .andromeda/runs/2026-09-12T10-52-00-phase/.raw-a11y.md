# raw twin — a11y

The returned body passed every per-extract check (header, `## No domain coverage` alone, size, no entity
escapes). It is saved verbatim in `a11y.md` with ONE mechanical delta.

**Delta:** the return cited the modify-set file by its absolute host path — a drive-letter-rooted prefix
naming this checkout's location, followed by the same `crates/conductor-report/tests/matrix_ledger_gate.rs`
suffix that the saved copy carries. The saved copy keeps only the repo-relative suffix.

**Why:** `.andromeda/runs/` is tracked (2044 files under version control at this commit), so an extract is a
committed artifact, and the project's artifact-hygiene invariant bars absolute host paths from committed
output. The host rule's prescribed remedy is to change the OUTPUT rather than loosen the guard.

The offending prefix is described here rather than reproduced, because this twin is itself a committed
artifact guarded by the same drive-letter anchor — quoting it would reintroduce exactly the string the
normalization removed.

No other byte differs: same referent, same claim, same sections.
