# raw twin — design

The design distiller's FIRST return failed per-extract check 2 (Sections): it carried a `## Relevance`
section AND `## No domain coverage`, where the contract admits either all 7 sections in order OR
`## No domain coverage` alone — not mixed. It was retried once with the check named; the retry passed and is
what `design.md` holds verbatim.

The judgement was identical across both returns (out-of-scope: the modify-set is one Rust verification-gate
test file whose only outputs are `cargo nextest` assertion messages, so no `desktop-webview` or `cli`
operator surface renders and no design-system token, typography tier, motion, iconography or component
pattern applies). Only the FORM differed.

The first return's additional content, for the record: a one-line `## Relevance` verdict of "out-of-scope",
and a longer reason naming design-system §Surface: cli as governing `conductor-cli` operator output
(`owo-colors` / `indicatif` / `comfy-table` / `inquire`) rather than test-harness assertion text, plus the
observation that no design-system amendment touches the ledger gate, the test assertions, or the version id
space — the nearest entries cite `verification-matrix.json#v2-27` only as a bundle-size citation target, not
as a gate predicate.

That amendment-history observation is preserved here because the passing retry, being
`## No domain coverage` alone, could not carry it.
