# a11y extract

## No domain coverage

The chunk's entire modify-set is one Rust integration test (`crates/conductor-report/tests/matrix_ledger_gate.rs`) generalising a ledger id-space predicate — no rendered UI surface, ARIA role, focus/keyboard sequence, design-token pair, motion rule, or a11y violation artifact is touched, and a11y-plan §9 (CI Integration, per-pipeline-stage artifacts table) maps the cargo-nextest/unit stage to `n/a` for a11y tooling, so no WCAG criterion or harness contract from the plan binds here (the `v3-` a11y capability ids the gate reads are ledger data the chunk is explicitly barred from editing, not an a11y assertion surface).
