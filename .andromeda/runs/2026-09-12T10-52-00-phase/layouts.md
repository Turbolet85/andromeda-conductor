# layouts extract

## No domain coverage

This chunk repairs an id-space predicate inside a Rust workspace test (`crates/conductor-report/tests/matrix_ledger_gate.rs`) and its fixtures/assertion messages — it renders nothing on either surface layout-templates governs (desktop-webview console regions/wireframes or the `conductor` cli verb output structure), adds no verb, bracket label, lamp, column or focusable element, and its acceptance is a `cargo nextest` gate rather than a rendered region.
