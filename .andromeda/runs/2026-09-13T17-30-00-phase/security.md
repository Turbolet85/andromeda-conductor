# security extract

## Relevance
Partial — the refactor/fixture/const work is out of domain, but four of the five unowned mutation survivors sit on security-mandated control points (MCP read-back freshness, the `mark_incident_resolved` write path, contract-manifest fixed-path resolution, the `cleanup` run_id sink) and the `conductor-emit/Cargo.toml` edit moves `Cargo.lock`, which the supply-chain gate binds.

## Constraints
- The `conductor-emit → conductor-core` removal changes `Cargo.lock`; security-plan §Security Anti-Patterns → Universal requires the lock never drift and stay committed, and requires `cargo-audit` (+ recommended `cargo-deny`) green before merge to the dev OS target. Whether the current tree's audit measures green is research's question — §Dependency Security records the 2026-09-05 re-adjudication that closed the red-audit regime, and mandates that a porcelain check on the local `advisory-db` checkout PRECEDE classifying any new red as external.
- The killing assertions for `conductor-verify/src/extract.rs` `observe` (`==`→`!=` at `:98:22` and `:113:22`) must preserve the degrade DIRECTION §Input Validation (MCP child stdout row) mandates: an absent, unparseable or non-fresh `opened_at` stamp contributes nothing and reads as NOT-fresh ⇒ `blocked`, never a false pass. Whether the existing vehicle already pins the negative arm is research's question.
- `ReadbackClient::resolve_incident` (`client.rs:187:9`) is the corpus-MUTATING MCP caller. §Security Anti-Patterns → Data Protection bans Conductor code opening, copying or staging Pulse's `corpus.db` directly — staging a row to induce a SUT state is the named banned route, which is why that arm is required to be stub-proven; §Input Validation requires both the applied `{resolved, incident_id}` response and the DECLINED `ToolDispatchFailed` arm stay under bounded decode + typed `VerifyError`, never a panic.
- `ContractManifest::default_path` (`manifest.rs:35:9`) is the fixed-path resolution §Input Validation governs (pinned MCP contract manifest row + committed-manifests row: fixed `default_path()` through `resolve_under`; read faults carry `e.kind()` only, never the path). A kill that pins a resolved value must not weaken the `CONDUCTOR_CONTRACT_MANIFEST` canonicalize + bounds-check duty §Security Anti-Patterns → Input places at the `conductor-cli` edge.
- `cleanup`'s kill (`cleanup.rs:16:5`) must leave the `run_id`'s ONLY sink a rusqlite `?1` bound parameter, never string-formatted SQL and never a path join — §Security Anti-Patterns → Input plus §Input Validation (CLI arguments row), which states explicitly that neither the canonicalize duty nor the Tauri `resolve_under` duty attaches to this argument because of that sink.
- Single-sourcing the eleven-key envelope array beside `RunRecord` must be key-for-key identical and must not widen the envelope: §Error Handling (run-report artifact sanitization) requires the record carry verdict/state/identity fields only, with no absolute host path and no internal seam-crate struct name, since the artifact is agent-parseable ground truth shared across hosts.
- If the new `conductor-emit/tests/common/mod.rs` or any killing test reads a `CONDUCTOR_*` / `ANDROMEDA_PULSE_*` handle or a committed manifest, §Security Anti-Patterns → Input's per-READER restatement binds: a test-binary reader carries the explicit recorded residual and its failure origin must name the handle and the file's own name, never a resolved path. Whether the five existing clone pairs read any handle today is research's question.

## Patterns to follow
- The stub-proven MCP vehicle — the chunk's own freight table names `serve_stub` in `conductor-verify`'s `tests/common/mod.rs`; §Security Anti-Patterns → Data Protection makes the stub the sanctioned route for exercising `call_tool` arms without touching the corpus file.
- The verdict/error wall — typed `VerifyError::{JsonRpc,Decode,Transport}` as `Result::Err` for harness faults only, with verification outcomes as `Ok(Verdict/ReportState)` values (§Error Handling); killing assertions should target those typed values rather than panics or exit codes.
- The per-READER recording precedent for manifest access from a test binary: `conductor-report --test coverage_gate` resolves `pulse-capabilities.toml` from `CARGO_MANIFEST_DIR` rather than `default_path()`, because a cargo test binary's CWD is its own package root (§Input Validation, committed-manifests row) — the model if a fixture here needs a manifest.
- Host-path-free failure origins — `journal_conformance.rs`'s residual is bounded by an origin naming the handle plus the file's own name (§Security Anti-Patterns → Input), and gate output renders `data_dir` as `<redacted>` (§Input Validation, `ANDROMEDA_PULSE_*` row); both are the model for new shared-fixture error text.

## Anti-patterns to avoid
- NEVER open, copy or stage a row in Pulse's `corpus.db` to make the `resolve_incident` mutant fail — the ban is on FILE access and that arm is required to be stub-proven (§Security Anti-Patterns → Data Protection).
- NEVER build `runs.db` SQL by `format!`/concatenation in the `cleanup` path or a test double introduced to kill its mutant — bound parameters only (§Security Anti-Patterns → Input).
- NEVER let `Cargo.lock` drift or go uncommitted after the `conductor-emit/Cargo.toml` edit — it makes the audit non-deterministic (§Security Anti-Patterns → Universal).

## Contract bindings
- security ↔ tests: the mutation gate itself is the tests domain's (testing.md:19, test-plan §12 roster); security contributes only which survivors sit on mandated control points and what a killing assertion may not do. The `cargo audit` / `cargo deny` gate binds to tests §CI Integration as a job in the single workflow (per focus guide, CI security gate).
- security ↔ obs: the eleven-key envelope / `RunRecord` key set is obs-owned schema; security owns the no-absolute-host-path / no-seam-crate-struct-name rule over it (§Error Handling, run-report artifact sanitization).
- security ↔ wrap/amendment flow: §Input Validation is an exhaustive enumeration of external-input surfaces and readers, so any NEW handle reader or manifest reader introduced by the shared fixture is an amendment-owing boundary-inventory event (precedent: 2026-09-06-coverage-completeness-gate).

## Acceptance criteria contributions
- (security) `cargo audit` exit 0 AND `cargo deny check advisories bans licenses sources` exit 0 over the REGENERATED and committed `Cargo.lock` after the `conductor-emit → conductor-core` removal — verified, never assumed (per security-plan §Dependency Security; §Security Anti-Patterns → Universal).
- (security) the two `extract.rs` `observe` kills pin the NOT-fresh direction as well as the positive one: an absent / unparseable / stale stamp yields `blocked`, never a pass (per security-plan §Input Validation, MCP child stdout row).
- (security) no test, fixture or `common/mod.rs` added or changed by this chunk opens a `corpus.db` file handle or stages a corpus row; the `resolve_incident` kill runs through the MCP tool surface only (per security-plan §Security Anti-Patterns → Data Protection).
- (security) no committed file this chunk touches — shared fixtures, killing tests, the hoisted envelope const, any mutation evidence — carries an absolute host path or an internal seam-crate struct name, and mutation `--output` stays under gitignored `target/` (per security-plan §Security Anti-Patterns → Logging; §Error Handling run-report artifact sanitization).

## Relevant amendment history
- **2026-09-06-run-report-envelope-conformance-gate** — enumerated the `run_id`-class positional at both §Input Validation and §Threat Model with its actual control (rusqlite `?1` bound parameter on `cleanup`, never a path join), restated the canonicalize duty as binding per READER (naming `journal_conformance.rs` as a test-binary reader of `CONDUCTOR_RUNS_DIR`), and widened harness-spawn rule (b) to a fifth form for `conductor cleanup <run_id>`. This is the direct prior work on `cleanup.rs` and on the envelope-conformance family; its in-chunk lesson was that the gate's own failure origin leaked an absolute host path via `path.display()` before being fixed to handle + filename.
- **2026-08-31-p-075-assert-round** — landed the first production caller of the corpus-MUTATING `mark_incident_resolved`, retired "read-back only" at four sites, and clarified that the FILE-access ban is why the DECLINED arm is stub-proven rather than staged. Directly governs the `client.rs:187` `resolve_incident` kill.
- **2026-08-16-canary-fingerprint-derivation-aligned (child-stdout row states the freshness assertion)** — replaced the retired identity claim with the `extract::opened_at_unix_nanos` vs `CanaryMarker.emitted_at_unix_nano` comparison and made the degrade DIRECTION explicit (a missing stamp must never read as satisfied). Directly governs the two `extract.rs` `observe` kills.
- **2026-09-05-audit-corrective** — re-adjudicated the long red audit as a LOCAL advisory-db checkout artifact, closed the "dependency delta under a red audit" regime, and made the porcelain check precede any external classification. This is the operative state for the `Cargo.lock` delta this chunk creates.
- **2026-09-06-coverage-completeness-gate** — recorded a test-binary reader of a fixed-path manifest (`CARGO_MANIFEST_DIR` rather than `default_path()`), establishing that an unrecorded reader is the drift rather than the validation. Precedent for both the `manifest.rs default_path` kill and any manifest access from the new shared fixture.
- **2026-06-27-mcp-read-back-result-shape-adapter** — reconciled the read-back boundary to the hand-rolled JSON-RPC client with bounded decode and `VerifyError::{JsonRpc,Decode,Transport}`; background for what the `client.rs` / `extract.rs` killing assertions must assert against.
