# layouts extract

## Relevance
Partial — the chunk authors an outward-addressed spec artifact (no Conductor reader, no wireframe/region/focus-order change), so layouts contributes only the guard-rails that bind IF any rendered cli/webview/Markdown output appears; the artifact's form and location are stated as a P4 decision in the scope, so whether this chunk emits anything on a rendered surface is research's question.

## Constraints
- The per-P-ID verdict/report-state label set is CLOSED at six (`Pass`/`CalibrationRegion`/`Fail`/`Blocked`/`ManualCheck`/`KnownResidual`); a hard-grade predicate must be assertable through that existing set, minting no seventh lamp, no new bracket label and no new `ReportState` (per layout-templates.md §Surface: cli — Component Primary content block 2; §desktop-webview Component — Primary content block 2).
- Anything that qualifies a RUN rather than reporting a check must ride OUTSIDE the lamp column as a non-lamp qualifier with its ASCII/text label always printed and omitted entirely when inapplicable (per layout-templates.md §Surface: cli — Output structure `conductor run`; §desktop-webview Component — Primary content block 2, load-envelope banner).
- Per-check measurement grain has exactly one documented render home: the indented per-check detail region in `runs/&lt;run_id&gt;.md` — one line per expected check carrying kind, verdict, `latency_ms` and the deadline judged against, with **no new column, no new bracket label, no new ANSI/token entry**, and cli stdout plus the 6-column results/SLO table unchanged (per layout-templates.md §Surface: cli — Component Primary content block 2, per-check detail region).
- The cli verb surface is enumerated HERE and nowhere else, and the verb set is named by mechanism (whatever `Commands` declares), never baked as a literal list; any new verb or stage flag is a layout-templates surface registration (per layout-templates.md §Surface: cli — Primary screens; §Surface: cli — Component Primary navigation).
- Derived counts and sample values must not be baked as literals in this document's wireframes/captions — the coverage roll-up stands in placeholder form `43 auto (N unbacked)` and tier cells as `&lt;slo_tier&gt;` (per layout-templates.md §Surface: cli — Primary screens `conductor coverage [--write]`). Relevant only if the chunk moves any manifest-derived count; the scope claims `v3-07` in Conductor's own verification matrix rather than the SUT capability manifest, so whether any surface tally moves is research's question.
- A never-measured state is never red and its measurement columns render `—`/null with a named precondition string beneath the row — the shape a still-`Blocked` P-025/`v3-08` row would take while Pulse has not implemented the contract (per layout-templates.md §Surface: cli — Component Primary content block 2; §desktop-webview Component — Primary content block 2, Blocked rows).
- Any Markdown/cli/webview rendering of the same fact is adapted per surface, never forked — one shape across Markdown artifact · cli · webview (per layout-templates.md §Surface: cli — IA notes, Multi-surface coordination; §desktop-webview IA notes).

## Patterns to follow
- The `not-conductors` Mode-cell / `[ENVIRONMENT-SUSPECT]` precedent: a REMIT- or run-level statement gets a recessive treatment (`status-residual` ↔ ANSI 246 ↔ Markdown emphasis) with the label always rendered, explicitly not a lamp token, so it never reads as a verdict (per layout-templates.md §desktop-webview Component — Primary content block 1, Mode cell; §Surface: cli — Component Primary content block 2).
- The indented-detail-line precedent — the `Blocked` precondition string and the `KnownResidual` note — is the established way to carry supporting prose under a P-ID line without widening a table (per layout-templates.md §Surface: cli — Component Primary content block 2).
- SET-naming over literals: state the source of truth (the manifest, `Commands`, `conductor_core::UNBACKED_AUTO`) rather than a value that re-stales (per layout-templates.md §Surface: cli — Component Primary navigation; §Primary screens `conductor coverage [--write]`).
- Cross-surface token adaptation by NAME (`count-nominal` ↔ ANSI 114, `status-residual` ↔ ANSI 246, `color-id-cyan` ↔ ANSI 117) if the contract's identifiers ever surface in output (per layout-templates.md §Surface: cli — IA notes, Multi-surface coordination).

## Anti-patterns to avoid
- Do not mint a seventh lamp, a new per-P-ID bracket label, a sixth `ReportState`, or a new `comfy-table` column to carry the contract or its grade predicate — adding a column without a `--format` flag is declared a breaking change for downstream parsers (per layout-templates.md §Surface: cli — IA notes, Command model; §Component Primary content block 2).
- Do not pin a fresh derived literal into any wireframe/caption sample when a count or tier moves — de-literalize instead (per layout-templates.md §Surface: cli — Primary screens `conductor coverage [--write]`).
- Do not render a never-measured or pre-accepted-gap state in `status-fail` red, or signal any state by tint alone without paired text (per layout-templates.md §desktop-webview Component — Primary content block 2, distinct non-result states).

## Contract bindings
- layouts ↔ obs: `.andromeda/obs-plan.md` §Delegated-timing family owns the unmeasurable-instrument finding and the observable's definition; layouts owns only how a resulting verdict/qualifier would RENDER (lamp set, detail-line grain, qualifier placement) — the contract's content is obs's authority, its eventual render shape is this plan's.
- layouts ↔ a11y: status never color-alone / label always rendered / `NO_COLOR`-survivable ASCII prefixes are stated here as requirements from which a11y derives concrete attributes (per layout-templates.md §Decisions Log Notes; §Surface: cli signature placement).
- layouts ↔ architecture: the cli VERB surface is enumerated in layout-templates by architecture's explicit refusal of the role, so a verb/flag registration lands here, not in architecture (per layout-templates.md §Surface: cli — Primary screens; amendment `2026-09-06-run-report-envelope-conformance-gate`).

## Acceptance criteria contributions
- (layouts) The chunk mints no new per-P-ID bracket label, lamp or `ReportState` and adds no column to either `comfy-table` shape — the lamp set stays closed at six (per layout-templates.md §Surface: cli — Component Primary content block 2).
- (layouts) If any run-level annotation or caption ships, it prints outside the lamp column, its text label always renders (surviving `NO_COLOR`/piping), and it is omitted entirely when inapplicable (per layout-templates.md §Surface: cli — Output structure `conductor run &lt;scenario&gt;`).
- (layouts) If the chunk adds any cli verb, flag or rendered region, §cli Primary screens is amended and the verb set stays named by mechanism rather than enumerated as a literal (per layout-templates.md §Surface: cli — Primary screens / Component Primary navigation).
- (layouts) No wireframe, sample row or roll-up caption in layout-templates.md gains a freshly baked derived literal as a result of this chunk (per layout-templates.md §Surface: cli — Primary screens `conductor coverage [--write]`).

## Relevant amendment history
- `2026-08-21-per-check-latency-measurement` — established the `runs/&lt;run_id&gt;.md` per-check detail region (one indented line per expected check: kind, verdict, `latency_ms`, deadline judged against) explicitly with **no new column, no new bracket label, no new ANSI/token**. This is the closest prior art for where a delegated-budget measurement would render once Pulse emits the contracted observable, and it sets the "new measurement grain ≠ new surface element" rule this chunk must not breach.
- `2026-08-09-sut-load-envelope` and `2026-09-01-live-per-p-id-verdict-lamps` — minted and then generalized the run-level NON-LAMP qualifier: outside the lamp column, always-printed label, omitted when inapplicable, and named as a three-surface SET (cli bracket label · Markdown banner · webview banner) adapted never forked. Why it matters here: it is the documented escape hatch for stating something about a run that is not a check verdict — the shape any contract-derived annotation would have to take.
- `2026-08-16-fingerprint-storm-live-proof` (with `2026-08-18-error-baseline-spike-live-proof`, `2026-08-18-restart-suppression-live-proof`, `2026-08-19-connection-lifecycle-live-proof`) — the de-literalization line: a baked derived count/tier went stale three times, each prior fix substituting a fresh literal that re-staled, so sample values are now placeholder-form. Why it matters: if this chunk moves any manifest-derived or tier value, the fix is de-literalization, not re-pinning.
- `2026-09-06-run-report-envelope-conformance-gate` — settled that the cli verb enumeration's home is layout-templates (architecture explicitly refuses the role); the wrap re-aimed a misdirected amendment after grepping both targets. Relevant if the contract work turns out to touch the cli verb surface.
- `2026-09-04-preconditions-probe-reads-path-handles-by-presence` (retiring `2026-09-04-sr-findings-remediation`'s probe half) — precedent for reconciling a documented claim to MEASURED behaviour, including retiring an "unreachable arm" statement once measurement disproved it. Relevant as the amendment posture if this chunk's measured Pulse-side facts change any statement this plan carries.
