proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → On-disk artifacts / database
    change: add a fifth `contracts/` bullet registering `contracts/pulse-p025-measurement-contract.md` — the SUT-facing P-025 measurement contract (which Pulse-emitted observable, at what resolution, over what window, and what constitutes a hard grade), a committed outward-addressed document that is the FIRST `contracts/` member with NO Rust reader: no runtime load path, no bounds check, no ingest channel and no `CONDUCTOR_*` override handle, carrying its provenance (Pulse HEAD `83d4060`, capture date, measurement-not-transcription) on its own face.
    sidecar: registered `contracts/pulse-p025-measurement-contract.md` as the fifth `contracts/` member and the first with no Rust reader (2026-09-13-p-025-measurement-contract-for-pulse).
    rationale: the report's Changes → Schema / config lands "one new committed SUT-facing document, `contracts/pulse-p025-measurement-contract.md` — the fifth `contracts/` member and the first with no Rust reader", and Counts / qualifiers moved states "`contracts/` member count 4 → 5" with §Occupied Resources → On-disk artifacts enumerating only four bullets at `:175`–`:178`; the chunk's own carried expected amendment names architecture.md as the sole owner on a hit basis. An unregistered on-disk artifact is D-arch-resources drift.
    basis: .andromeda/architecture.md:175-178
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → Directory structure
    change: the `contracts/` tree comment now enumerates FIVE members — pinned MCP contract manifest + the SUT capability manifest + the SUT load envelope + the SUT run contract + the P-025 measurement contract (the one member no Rust code reads).
    sidecar: moved the directory-tree `contracts/` comment from four named manifests to five, adding the P-025 measurement contract.
    rationale: same retired claim at a second site — the tree comment at `architecture.md:233` names the `contracts/` set by enumerating exactly the four pre-chunk manifests, so applying the §Occupied Resources bullet alone would leave the four-member enumeration standing here. The report's Counts / qualifiers moved cites this line explicitly ("the directory-tree comment at `architecture.md:233` enumerates four manifests by name") and its site search `grep -nE '^\s*(├|└|│).*contracts'` returned exactly this 1 hit.
    basis: .andromeda/architecture.md:233
    dependent-of: D-arch-resources

# Detectors with no hit, as the agent recorded them:
# D-arch-decisions — Dependencies none (Cargo.lock exit 0); Symbols/APIs none; Crates/modules none.
#   No §Established Decisions entry states a directory-wide "every contracts/ member is runtime-read"
#   rule (each existing bullet states its own load path), so the reader-less member contradicts no
#   locked decision.
# D-platform-claim — Harness/gate surface none; the sole Spec-claims-disproved entry retires a
#   last_seen stamping premise, not a platform/runner/driver verdict; 0 hits in architecture.md.
