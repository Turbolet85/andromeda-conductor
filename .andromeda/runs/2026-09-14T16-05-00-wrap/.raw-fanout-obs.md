proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Scenario: Known-residual classification path → Delegated-timing family (the SCOPE clause closing line 349)"
    change: "Replace the SCOPE clause's 'a SUT change that stamps `last_seen` from span arrival (or copies the activity floor's `last_observed_unix_nanos`) would lift it' with: neither candidate lifts the bound — both address the STALENESS term only; with `last_seen` ingest-stamped a 2/s emitter reports ~500ms and the 2000ms budget starts PASSING while the leaf still emits the age of the newest span at paint time (a green grade over the wrong quantity), and the slowest-wins (max across changed dots) and tick-quantization terms survive either change — so the bound lifts only if Pulse emits a different quantity, which is what the SUT-facing `contracts/pulse-p025-measurement-contract.md` (Pulse HEAD `83d4060`) asks for: EXPOSE a start instant Pulse already holds (rise → `opened_at_unix_nano`, fall → `transitioned_at_unix_nano`), never mint one, and never a journal-relative term (`latency_ms`/`budget_ms`/`effective_deadline_ms`); still Pulse intake, not Conductor work."
    sidecar: "2026-09-14 §4 delegated-timing family — retired the 'ingest-stamped `last_seen` / `last_observed_unix_nanos` would lift it' clause (measured false: staleness term only; slowest-wins + quantization survive) and pointed the SCOPE clause at `contracts/pulse-p025-measurement-contract.md`."
    rationale: "Report **Spec claims disproved by measurement** names `obs-plan.md:349` verbatim and measures the clause false this chunk — both candidates address the staleness term only, ingest-stamping turns the 2000ms budget green over the wrong quantity, and the slowest-wins / quantization terms survive; the report's **Expected amendments** bullet routes exactly this to `obs-plan.md` §4 Known-residual classification path → Delegated-timing family ('add the contract pointer and narrow the \"would lift it\" clause to the measured premise correction'), with `grep -cF 'would lift it'` → 1 hit in obs-plan.md and 0 in the other six masters, so obs-plan.md is the sole owner. The **Files** / **Schema / config** bullets carry the new artifact `contracts/pulse-p025-measurement-contract.md` (no Rust reader, outward-addressed), and **Decisions & corrections** carries the expose-not-mint framing plus the two-case rule after escalation was measured unreachable. No duplicate occurrence exists: sweeping obs-plan.md for the claim's wording and for its mechanism however worded (`would lift`, `span arrival`, `last_observed`, `staleness`, `lift`, `P-025`, `quantization`, `halo`/`constellation`/`hue_update`, `2000`) returns line 349 alone — the other `2000` hits at :409/:414/:561 are SLO-tier thresholds and histogram buckets, not this claim — so no `dependent-of` proposal is owed."
    basis: "conductor-0.3.0/chunks/2026-09-13-p-025-measurement-contract-for-pulse/report.md:29 (claim site: .andromeda/obs-plan.md:349)"

# Detectors with no hit, as the agent recorded them:
# D-obs-instrumentation (new-operation arm) — Symbols/APIs none, Crates/modules none, no new external
#   surface or hot-path op; criterion 4 confirms no span, envelope extra or eighth critical path. The
#   proposal above is filed under this detector only because the falsified claim lives in §4.
# D-obs-stack — Dependencies none; no telemetry symbol, no OTel SDK or OTLP exporter added.
# D-obs-redaction — the only artifact write is the contract document; gate 6 (word-anchored host-path
#   probe, exit 1 / no output) and criterion 7 measure zero host paths and zero internal struct names.
# D-platform-claim — no retired platform/runner/driver verdict; no stating sentence in obs-plan.md.
#
# TRANSPORT: this return arrived HTML-escaped (& as the entity inside `rationale`); decoded on save.
