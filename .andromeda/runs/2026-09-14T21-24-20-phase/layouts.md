# layouts extract

## No domain coverage

Out of domain: this chunk authors mutation-discriminating tests over `conductor-emit`'s `exception.rs`/`latency.rs` (plus a possible `scripts/mutation-gate.py` scoping extension and a `tests/common/mod.rs` fixture fold) and creates or modifies no desktop-webview region, no `conductor` verb or `agent-run` stage flag, and no rendered cli/run-report output shape that layout-templates.md templates — whether any scrubbed host path or `LatencyProfile` percentile ever reaches a rendered surface (the cli sanitized `error:`/`hint:` stream or the `runs/<run_id>.md` artifact) is research's question, and if P3 finds it does, re-invoke this domain.
