The four structurally-dead declarations are retired to declare-only in ONE change: `constellation-severity-live-wiring` (`CountAtLeast`/`Hard`/`"1"`, P-079), `findings-counter-refresh` (`CountAtLeast`/`Hard`/`"3"`, P-045) and `pulse-run-contract` (`CountAtLeast`/`Hard`/`"1"`, P-073), whose floors grade an `evidence_count` summed from a `span_refs` vector Pulse's sole non-test writer sets empty (`pulse-app/src/inference_runtime.rs:871`, HEAD `83d4060`); plus `cross-incident-recurrence` (`Contains`/`Hard`/`"Previously seen"`, P-036), whose declared token differs in CASE from the emitted `"## Previously Seen"` against a case-sensitive comparison. Each retired scenario keeps its `p_ids`, `slo_tier` and phase drive and carries a header comment recording the marker and the measured ground. The committed enumeration at `conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/evidence/assertion-class-enumeration.md` classifies all 12 pre-change `[[expected]]` blocks across 11 of the 36 committed scenarios as satisfiable or dead, with the derivation command recorded beside every number, leaving 8 live blocks across 7 scenarios (`cat scenarios/*.toml | grep -c '^[[expected]]'` prints 8). All five pinning tests that assert a retired declaration — `crates/conductor-core/src/scenario.rs:1188`, `:1207`, `:1303`, `:1408`, `:1429` — are updated in that same change, and both `cargo nextest run --workspace --profile ci` and `cargo test -p conductor-core` are green at the end of it, with no scenario of the class left inconsistent.
