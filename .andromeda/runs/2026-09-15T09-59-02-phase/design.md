# design extract

## No domain coverage

This chunk's entire modify-set is declarative scenario TOML plus an inline `#[cfg(test)]` module in `D:/dev/projects/conductor/crates/conductor-core/src/scenario.rs` — no rendered surface, so no design-system token, typography, motion, iconography, or component-pattern mandate binds (the scope closes the one adjacent design-touching question itself: coverage classification is out of the modify-set and all four P-IDs stay `Auto`, so the closed six-state lamp set and its `Blocked`/`ManualCheck`/`KnownResidual` treatments are untouched).
