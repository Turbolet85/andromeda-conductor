2026-09-15-structurally-dead-assertion-class-retired · pending · Structurally-dead assertion class retired — the four declared checks that cannot pass in any world (three `CountAtLeast` floors graded against an always-empty `span_refs`, one `Hard Contains` on a token no producer emits) retired to declare-only as a class in one pass, with every pinning test updated in the same change — five of them, all in one inline `#[cfg(test)]` module the prescribed companion sweep is structurally blind to, two naming suite-shape properties the retirement dissolves rather than merely breaking · → conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/
