proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Standard Contracts — Run report envelope (per scenario check)"
    change: "The declare-only registry gains the four scenarios retired 2026-09-15 — `constellation-severity-live-wiring` · `findings-counter-refresh` · `pulse-run-contract` · `cross-incident-recurrence` (`findings-counter-refresh` joining the delegated-timing family it was previously excluded from, its parenthetical `is NOT a member: it carries one [[expected]]` struck) — and the corpus tally now reads 29 declare-only of 36 scenarios, with 8 live `[[expected]]` blocks across the 7 that still declare any."
    sidecar: "2026-09-15 — declare-only registry: +4 members (constellation-severity-live-wiring, findings-counter-refresh, pulse-run-contract, cross-incident-recurrence); findings-counter-refresh exclusion parenthetical struck; corpus 25→29 declare-only, 12→8 live [[expected]] blocks across 11→7 scenarios."
    rationale: "Report *Spec claims disproved by measurement* #1 names `architecture.md:135` false — `findings-counter-refresh` now carries zero `[[expected]]`, measured by the retirement itself — and the *Counts / qualifiers moved* bullet lands declare-only 25→29, blocks 12→8, declaring scenarios 11→7. The report carries this as an owed *Expected amendment* against arch §Standard Contracts (Run report envelope), locating `:135` as the registry site."
    basis: "conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/report.md:27 (and :16-:20, :31)"

  - detector: D-arch-resources
    severity: warning
    section: "Established Decisions — [Read-Back Dependency Posture]"
    change: "Re-tense the degraded-read-back example so it stops asserting a live check: \"`findings-counter-refresh` carries one `[[expected]]` and still landed `KnownResidual`, because ITS read-back came back degraded, and its unmet `CountAtLeast` floor graded `CalibrationRegion`\" becomes the 2026-08-21 measurement as taken — it carried one `[[expected]]` THEN, whose `CountAtLeast` floor graded `CalibrationRegion`, and it has been declare-only since 2026-09-15 — leaving the load-bearing point (the degraded route to `KnownResidual` is not gated on declare-only) intact."
    sidecar: "2026-09-15 — [Read-Back Dependency Posture]: the findings-counter-refresh degraded-read-back example re-tensed to its 2026-08-21 reading; the scenario carries zero [[expected]] since the structurally-dead-assertion retirement."
    rationale: "Same retired claim, second site, present tense. The report's own sweep grepped arch for `findings-counter-refresh` and charted `:70` as \"fingerprint derivation, unrelated\" — but that grep line is the merged [Read-Back Dependency Posture] paragraph, and it states the identical claim obs-plan.md:349 is being amended for (*Spec claims disproved* #2, false \"on both halves\"). Applying the `:135` amendment alone would leave arch asserting the same retired `[[expected]]` and the same unmet `CountAtLeast` floor one section up."
    basis: "conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/report.md:28 (claim wording measured at .andromeda/architecture.md:70)"
    dependent-of: D-arch-resources

  - detector: D-arch-resources
    severity: warning
    section: "Established Decisions — [Run-History Persistence]"
    change: "State the fingerprint index's purpose without asserting a declared check: `runs.db` indexes run_id · seed · scenario · P-IDs · verdict · fingerprints · timestamps for cross-run/seed-to-seed queries and for cross-incident fingerprint recurrence, with P-036's `cross-incident-recurrence` scenario declare-only since 2026-09-15 (its `Contains \"previously seen\"` check retired) and its recurrence claim graded at the harvest tier."
    sidecar: "2026-09-15 — [Run-History Persistence]: the present-tense \"P-036 fingerprint-recurrence (\"Previously seen\") check\" restated as an index purpose; P-036's scenario declares no check after the retirement."
    rationale: "Third occurrence of the same retired claim, worded by actor and token rather than by scenario name: the Changes bullet moves `scenarios/cross-incident-recurrence.toml` (P-036, per the Outcome row \"P-079, P-045, P-073, P-036 all intact\") from one `[[expected]]` to zero, and *Spec claims disproved* #3 identifies that retired check as the case-sensitive `Contains` on the \"Previously Seen\" token. Left as-is, arch would register P-036 as declare-only at §Standard Contracts while still naming a live P-036 \"Previously seen\" check in §Established Decisions."
    basis: "conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/report.md:29 (and :10; claim sits at .andromeda/architecture.md:72)"
    dependent-of: D-arch-resources

# Detectors with no hit (agent's own notes):
# D-arch-decisions — no drift. Dependencies "none added, none bumped", Cargo.lock byte-unchanged at 562
#   packages; Crates/modules "none added, removed or changed"; no garde rule, no Scenario::check_* arm, no
#   from_toml_str change. Nothing contradicts §Stack or a locked decision.
# D-platform-claim — no hit. Harness/gate surface "none changed"; none of the three Spec-claims-disproved
#   entries retires a platform, runner or driver verdict. Arch's WebView2 / elevation / hosted-runner
#   verdicts at :60 are baseline prose describing past chunks.

# ORCHESTRATOR DISPOSITION (not the agent's): all three applied. The primary's corpus-tally clause was NOT
# applied — the body carries no such literal today and minting one would re-stale (playbook @106/@127); the
# registry names the SET instead. Applied text was re-derived from the invariant + the report's fact, never
# pasted from the `change` lines.
