proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Scenario: Known-residual classification path → Delegated-timing family bullet"
    change: "Strike the clause \"`findings-counter-refresh` is not declare-only (one `[[expected]]`) and reaches KnownResidual by the same degraded read-back, its unmet `CountAtLeast` floor grading `CalibrationRegion`\" and state instead that `findings-counter-refresh` IS declare-only as of 2026-09-15 (its single `[[expected]]` `CountAtLeast` floor retired as structurally dead), reaching KnownResidual by the same degraded read-back with the declare-only envelope shape this section already specifies — `verdict` null, no `CalibrationRegion` grade from that floor — while its harvest-tier leaf `metric.findings.counter_refresh_ms` (`duration_ms`) is unchanged."
    sidecar: "§4 Delegated-timing family — `findings-counter-refresh` restated as declare-only; the `CountAtLeast`-floor / `CalibrationRegion` grading clause retired (2026-09-15-structurally-dead-assertion-class-retired)."
    rationale: "Report *Spec claims disproved by measurement* #2 names `obs-plan.md:349` (offset 1351) false on BOTH halves after this chunk, and *Expected amendments* carries it as the owed obs-plan §4 edit located by `grep -n 'findings-counter-refresh'` → 1 hit at `:349`. The *Counts / qualifiers moved* bullet measures the scenario into the declare-only set (live `[[expected]]` blocks 12 → 8; declare-only scenarios 25 → 29), and §3's own rule (\"a blocked or declare-only scenario emits no check line at all\") plus §4's declare-only families make `verdict` null the current envelope truth."
    basis: "crates/conductor-core/src/scenario.rs:1298 (`findings_counter_is_declare_only_and_threshold_reload_keeps_its_hard_absent`, pinning `scenarios/findings-counter-refresh.toml` at zero `[[expected]]` through `Scenario::from_toml_str`)"

  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Scenario: Known-residual classification path → Delegated-timing family bullet (opening sentence)"
    change: "Widen the family's declare-only roster from three to four — \"The three declare-only delegated-timing scenarios (`halo-hue-encoding` · `service-constellation-discovery` · `report-render-surface`)\" becomes the four declare-only delegated-timing scenarios, adding `findings-counter-refresh`, all of which reach this classification by this path and add NO critical path of their own."
    sidecar: "§4 Delegated-timing family — declare-only roster widened three → four (`findings-counter-refresh` joined it 2026-09-15)."
    rationale: "Same retired claim, restated as a roster: the opening sentence enumerates the family's declare-only members as exactly the three that are NOT `findings-counter-refresh`, which is only true while the fourth leaf carries its `[[expected]]` block. The report's *Counts / qualifiers moved* bullet measures declare-only scenarios 25 → 29 with `findings-counter-refresh` among the four retired, so restating only the later sentence would leave this enumeration contradicting it inside the same paragraph — the same exclusion-parenthetical shape the report strikes at `architecture.md:135`."
    basis: "crates/conductor-core/src/scenario.rs:1298 (same pin); report *Counts / qualifiers moved*, declare-only scenarios 25 → 29"
    dependent-of: D-obs-instrumentation

# Detectors with no hit (agent's own notes):
# D-obs-stack — no hit. Dependencies "none added, none bumped", Cargo.lock byte-unchanged, 562 packages.
#   No telemetry library in any Changes bullet; no OTel SDK / OTLP exporter surface.
# D-obs-redaction — no hit. Only artifact write is evidence/assertion-class-enumeration.md, measured PII
#   redacted (repo-relative paths only; hygiene grep exit 1 / 0 hits). No logging surface added.
# D-platform-claim — no hit. Harness/gate surface "none changed"; none of the three Spec-claims-disproved
#   bullets retires a platform, runner or driver verdict. obs-plan's platform tokens are surface
#   enumerations and entry-point pointers, stating no verdict.
#
# Agent's sweep note: `findings-counter-refresh` (1 hit), `CountAtLeast` (1), `counter_refresh_ms` (2),
# `[[expected]]` (1) — all on line 349; the same paragraph restates the claim a second time as a roster,
# so a single-sentence apply would leave the roster contradicting the fix.

# ORCHESTRATOR DISPOSITION (not the agent's): both applied. Offsets confirmed by read — roster at
# offset 66, exclusion clause at offset 1402 (post-edit), on a 4,572-character line. Applied text
# re-derived from the invariant + the report's fact, not pasted from the `change` lines.
