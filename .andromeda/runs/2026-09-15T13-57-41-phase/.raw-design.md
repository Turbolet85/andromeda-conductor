# design extract

## No domain coverage

This chunk edits scenario TOML `[[expected]]` declarations, adds prose DECLARE-ONLY headers, and moves Rust pinning tests/guards under `crates/**` — it renders no UI on either design surface (`desktop-webview`, `cli`), introduces no token, glyph, status prefix, lamp state, type role, or component pattern, and the scope's own surfaces table (`<host-path>/conductor-0.3.0/chunks/2026-09-15-remaining-structurally-dead-declarations-retired/scope.md`) marks `.andromeda/` spec masters read-only with only `architecture.md`'s declare-only registry as the plausible wrap-time amendment, so `<host-path>/.andromeda/design-system.md` mandates none of it.

<!-- TWIN NOTE (orchestrator, not the agent's text): this twin is SCRUBBED, not verbatim. The agent's
     return carried two absolute host paths at the two `<host-path>` markers above — the paths this
     orchestrator itself supplied in the distiller prompt. They are elided to the project's standard
     `<host-path>` token because run dirs are committed (2206 tracked files) and security-plan
     §Security Anti-Patterns → Logging forbids an absolute host path in a committed artifact. The
     saved `design.md` carries the same two sites rewritten to their repo-relative form. No other
     byte differs from the return: no preamble, no trailing commentary, no entity escapes. -->
