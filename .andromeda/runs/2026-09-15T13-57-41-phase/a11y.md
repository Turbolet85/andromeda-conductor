# a11y extract

## Relevance
Partial — the chunk builds no UI surface and triggers no WCAG criterion, but its mandated wide sweep `grep -rln "<scenario-name>" crates/` reaches an a11y-owned spec artifact that names one of the five scenarios.

## Constraints
- The scenario-corpus / CLI side of this change is **not-assertable** and owes no a11y verification: `[[expected]]` declarations, `scenarios/*.toml` headers, and Rust pinning guards have no DOM or widget tree, so no axe / Lighthouse / colorjs.io path applies (per a11y-plan §1 A11y Scope Summary → `conductor-cli` entity; §11 Anti-Patterns → Universal).
- The a11y pass spec at `crates/conductor-tauri/ui/test/a11y/screen-reader/nvda-pass-spec.md` is a11y's declared source of truth for the screen-reader leg (per a11y-plan §3 Screen reader test pattern → Pass spec format). Its line 73 names `high-severity-log-capture` inside the `npm run a11y:sr` `CONDUCTOR_SCENARIOS_DIR` fixture set as the load-bearing "short third scenario, which never executes." Whether the SR fixture's behaviour depends on that scenario carrying live `[[expected]]` blocks — or only on its being committed and validated — is research's question; scope asserts retirement deletes no scenario and changes no `p_ids`.
- The CI-gated routine arm **seeds its own committed fixture through the production writer** and needs no live Pulse; the `sr*` legs read `CONDUCTOR_SCENARIOS_DIR` instead (per a11y-plan §9 CI Integration → E2E row; §3 CI integration → Pipeline integration). Whether this chunk's edits reach either seeding path is research's question.
- If any file under `crates/conductor-tauri/ui/test/a11y/` moves in this change, the harness's anti-skip mechanism still binds: `CONDUCTOR_A11Y_STRICT` plus the printed-verdict assertion (`Spec Files:` failed count, per-spec skip tally against the expected-skip SET, driven-session banner), because a skipped-green run and a real pass are otherwise indistinguishable (per a11y-plan §11 Anti-Patterns → CI).
- No WCAG conformance may be claimed from this chunk — it produces no axe / Lighthouse / colorjs.io / WebdriverIO JSON (per a11y-plan §2 Agent-runnable invariants; §11 Anti-Patterns → Strategy).
- Any artifact this chunk causes to be emitted must not leak absolute host paths or internal seam-crate struct names; the DECLARE-ONLY headers' `file:line` citations are scenario-TOML prose, not artifact fields, and must stay that way (per a11y-plan §3 Structured violation JSON schema; §11 Anti-Patterns → SLO).

## Patterns to follow
- Classify a11y-owned sweep hits separately from pins. The `nvda-pass-spec.md:73` occurrence is a **fixture roster line**, not a check-membership assertion, so it is not a pin requiring a minted guard under CARRY 2 and it does not move as a pinning test (per a11y-plan §3 Pass spec format; chunk scope premise 3).
- Identify a11y CI jobs BY NAME (`a11y`), never by `ci.yml` line coordinates, which move as the workflow grows (per a11y-plan §3 Configuration).
- Precedent for correcting an a11y-owned artifact citation rather than editing it in-phase: the pass-spec path correction flow, where the master owned the fix and the sweep control (`grep` for both the filename and the pathed form) prevented a single-site apply (per a11y-plan §3 Pass spec format, as amended 2026-09-04).
- The routine arm's known-good state is 12 passing / 2 skipped with the expected-skip SET unchanged; treat any movement in that set as a signal, not noise (per a11y-plan §11 Anti-Patterns → Strategy, carve-out narrowed 2026-09-07).

## Anti-patterns to avoid
- NEVER treat the cli / scenario-corpus surface as accessibility-assertable — its ASCII status prefixes and NO_COLOR text-pairing are output-stream discipline, not an ARIA verification path (per a11y-plan §11 Anti-Patterns → Universal).
- NEVER claim WCAG conformance without machine-verifiable evidence (per a11y-plan §11 Anti-Patterns → Strategy).
- NEVER let an a11y suite skip green as a pass if any a11y test file is touched — the gate is fail-fast, not a warning (per a11y-plan §11 Anti-Patterns → CI).

## Contract bindings
- **a11y ↔ tests:** a11y runs no stack of its own; the `sr` / `sr-empty` / `sr-error` legs and the routine `--e2e` arm all ride tests §6's single `@crabnebula/tauri-driver` + WebdriverIO session, and their specs live under `crates/conductor-tauri/ui/test/a11y/` — which is why this chunk's `crates/`-wide sweep reaches a11y-owned files (per a11y-plan §3 A11y testing tool pick; §9 E2E row).
- **a11y ↔ obs:** a11y violation artifacts fold into the obs §6 envelope whose `scenario` / `p_ids[]` keys are MAPPED, not measured (`p_ids: []`, `scenario` = the invoked suite). Scope states no `p_ids` change, so no envelope impact is expected (per a11y-plan §3 Structured violation JSON schema).
- **a11y ↔ route:** `contentinfo` landmark and the report-site checklist render are recorded designed-but-unshipped and route-owned; untouched here (per a11y-plan §4 landmark table).

## Acceptance criteria contributions
- (a11y) The CARRY 2 wide-sweep pin inventory explicitly dispositions `crates/conductor-tauri/ui/test/a11y/screen-reader/nvda-pass-spec.md:73` (`high-severity-log-capture` in the `a11y:sr` fixture roster) as a fixture roster line and NOT a pinning guard, so no guard is minted for it and it is not counted in the pin totals (per a11y-plan §3 Screen reader test pattern → Pass spec format).
- (a11y) Retirement leaves all 36 scenarios committed and validated, so the picker catalog is not emptied and the must-announce empty-state prose `No scenarios found.` / `No scenarios match.` is not newly triggered (per a11y-plan §3 Screen reader test pattern → must-announce items).
- (a11y) No WCAG conformance claim and no a11y-tier movement is asserted by this chunk — it emits no axe / colorjs.io / WebdriverIO evidence (per a11y-plan §2 Agent-runnable invariants; §11 Anti-Patterns → Strategy).
- (a11y) If any file under `crates/conductor-tauri/ui/test/a11y/` is modified, the routine arm's expected-skip SET is unchanged and `CONDUCTOR_A11Y_STRICT` plus the printed-verdict assertion still gate (per a11y-plan §11 Anti-Patterns → CI).

## Relevant amendment history
- **2026-09-04-sidecar-spawn-without-a-console-window** — corrected the pass spec's citation to `crates/conductor-tauri/ui/test/a11y/screen-reader/nvda-pass-spec.md` (the repo-root-relative form resolved to nothing). That is precisely the file this chunk's wide sweep will surface, and the amendment establishes both its authoritative path and the two-site sweep control (pathed site vs. bare filename in the §3 bootstrap phase).
- **2026-09-02-screen-reader-manual-spec** — created the SR leg as agent-driven (NVDA speech log), the 51-row per-state pass spec, and the `sr` / `sr-empty` / `sr-error` suite families with `CONDUCTOR_NVDA` as the host-tool handle. The fixture roster containing the never-executing third scenario originates here; it also recorded the `drive_run` abort-flag-before-scenario behaviour that makes that third scenario load-bearing.
- **2026-09-04-sr-findings-remediation** — extended the must-announce inventory to three strings including the picker's filter-miss `No scenarios match.`. Relevant because the picker/catalog surface is the only place a scenario-corpus change could in principle disturb an a11y assertion; scope's "no scenario is deleted" means it should not.
- **2026-09-03-live-pulse-preconditions-probed** — re-scoped the `conductor-cli` entity from a three-verb enumeration to the whole verb SET so the not-assertable verdict does not restale as the surface grows. This is the basis for reading corpus/CLI-side work as out of a11y scope without a per-change re-derivation.
- **2026-09-07-a11y-ci-gate** and **2026-09-08-hosted-runner-webview2-session** — wired the routine arm as CI job `a11y` on `windows-2025`, replaced stale `ci.yml` line coordinates with job names, and recorded that the job is exercised but has never been green (run 34209940695, RED at WebView2 session creation, owner: *Hosted-runner WebView2 session* route entry). Relevant only as the naming and never-claim-green discipline if anything in that job's path moves here.
