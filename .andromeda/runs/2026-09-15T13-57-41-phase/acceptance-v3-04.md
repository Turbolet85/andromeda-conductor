Every committed scenario's declared checks are satisfiable by construction against a live Pulse, or retired to declare-only with their pinning tests updated in the same change — concretized over the FULL twelve-block pre-change population the committed enumeration classifies (`conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/evidence/assertion-class-enumeration.md`), not the four the route entry named.

TEN blocks are retired. FOUR at `2026-09-15-structurally-dead-assertion-class-retired`: `constellation-severity-live-wiring` (`CountAtLeast`/`Hard`/`"1"`, P-079), `findings-counter-refresh` (`CountAtLeast`/`Hard`/`"3"`, P-045) and `pulse-run-contract` (`CountAtLeast`/`Hard`/`"1"`, P-073), whose floors grade an `evidence_count` summed from a `span_refs` vector Pulse's sole non-test writer sets empty (`pulse-app/src/inference_runtime.rs:871`); plus `cross-incident-recurrence` (`Contains`/`Hard`/`"Previously seen"`, P-036), whose declared token differs in CASE from the emitted `"## Previously Seen"` against a case-sensitive comparison. SIX at `2026-09-15-remaining-structurally-dead-declarations-retired`, across five scenarios: `activity-floor` (`Absent`/`Hard`/`"ServiceWentSilent"`) and `service-went-silent` (`Contains`/`Hard`/`"ServiceWentSilent"`), both against a `CueKind` carrying `#[serde(rename_all = "snake_case")]` (`crates/triage/src/contract.rs:181`) that renders `service_went_silent`; `high-severity-log-capture` twice (`Contains`/`Hard`/`"ERROR"` and `Absent`/`Hard`/`"WARN"`) against a `severity_label` rendering `"info"|"warn"|"error"|"critical"` lowercase (`crates/interpretation/src/markdown.rs`); `exception-event-capture` (`Contains`/`Hard`/`"exception"`), absent from the sole report-markdown composer — `assemble_report` and `serialize_report`, both defined in `crates/interpretation/src/markdown.rs` (`:249`, `:115`), reached from `dispatch_retrieve_report` (`crates/mcp-server/src/tools.rs:360-387`) — and absent from the status/severity/title half, which the deterministic fixture pins to `"autonomous"` / `"Deterministic verification incident"` (`pulse-app/src/deterministic_inference.rs:42-43`); and `threshold-hot-reload` (`Absent`/`Hard`/`"RetroactiveReeval"`), an INFERRED token with zero occurrences tree-wide and no snake_case sibling — its ground is the token absence, NOT the serde rename that explains its siblings. All ten grounds measured at SUT HEAD `83d4060`. THREE of the six fail in the vacuous-PASS direction (`activity-floor`, `high-severity-log-capture`'s `Absent`, `threshold-hot-reload`), which test-plan §6 records as the more dangerous half.

TWO blocks remain live, and they satisfy the requirement's OTHER arm by measurement rather than assumption: `root-span-error-scope` and `span-status-error-detection`, each `Contains "error"`, matching the lowercase severity label the same `severity_label` renders. `grep -l '^[[expected]]' scenarios/*.toml | xargs -n1 basename | sort | paste -sd,` prints exactly `root-span-error-scope.toml,span-status-error-detection.toml`.

OPERATOR RULING on "ONE pass" (2026-09-15, at this chunk's P5; requirements.md:16 is IMMUTABLE and is not re-worded or narrowed by this reading): "ONE pass" binds the coupling stated in its own second half — a retirement moves together with the pinning tests that assert it, never staged across chunks. Both chunks satisfy that coupling: the first moved six pinning tests with its four retirements (five existing pins re-shaped plus one minted for `pulse-run-contract`, the class's single unpinned member — re-derived from commit `21edf56`), this one moves eight with its six. What took two chunks was not a decomposed retirement but a corrected SCOPE: the class was ratified at four members (the route entry, v3-04's 2026-09-11 note, the operator's P4 fork), and the first chunk's own committed enumeration measured the true population at ten — the correction the operator ruled must be finished rather than carried. The requirement's headline, "retired as a class", is met by the END STATE: after this chunk every committed `[[expected]]` block is satisfiable or retired, and the two survivors are satisfiable by measurement.

Each retired scenario keeps its `p_ids`, `slo_tier` and phase drive and carries a header comment recording the marker and its own measured ground. Every pinning test asserting a retired declaration is updated in the same change as its retirement — this chunk's eight, all in `crates/conductor-core/src/scenario.rs`, named by SYMBOL rather than line because the prior acceptance's coordinates are pre-change and have moved: `hard_signal_fixtures_load_and_validate` (split, so the two surviving scenarios keep their non-empty assertion), `p007_high_severity_log_capture_asserts_both_sides_of_the_boundary`, `activity_floor_and_silence_fixtures_load_and_validate`, `activity_floor_and_silence_checks_are_all_hard`, `activity_floor_asserts_the_learned_quiet_via_absent`, `presence_scenarios_assert_a_surfaced_incident_via_contains`, `findings_counter_is_declare_only_and_threshold_reload_keeps_its_hard_absent` (re-shaped and renamed — its name and its comment both assert the half this chunk retires), and `scrub_pipeline_degraded_suite_mixes_hard_and_declare_only`. NO surviving assertion is left vacuous: `activity_floor_and_silence_checks_are_all_hard` is retired rather than left green over an empty vec (`all()` returns true on an empty iterator), and the `scrub_pipeline_degraded` mixed-shape suite guard is retired because `threshold-hot-reload` was its last non-empty member — the severity-lifecycle precedent, which retires such a guard rather than weakening it.

Both `cargo nextest run --workspace --profile ci` and `cargo test -p conductor-core` are green at the end of it, with no scenario of the class left inconsistent.
