# design extract

## No domain coverage

This chunk edits scenario TOML `[[expected]]` declarations, adds prose DECLARE-ONLY headers, and moves Rust pinning tests/guards under `crates/**` — it renders no UI on either design surface (`desktop-webview`, `cli`), introduces no token, glyph, status prefix, lamp state, type role, or component pattern, and the scope's own surfaces table marks `.andromeda/` spec masters read-only with only `architecture.md`'s declare-only registry as the plausible wrap-time amendment, so `.andromeda/design-system.md` mandates none of it.
