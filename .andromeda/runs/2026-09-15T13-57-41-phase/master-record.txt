2026-09-15-remaining-structurally-dead-declarations-retired · pending · Remaining structurally-dead declarations retired — the six `[[expected]]` blocks across five scenarios that the PRIOR chunk's own enumeration measured dead (two PascalCase `CueKind` tokens against a `snake_case` serde rename · two uppercase severity tokens against a label Pulse renders lowercase · one token absent from every composed-text field · one with zero occurrences tree-wide), three of them in the vacuous-PASS direction test-plan §6 records as the more dangerous half, each retired to declare-only with its measured ground stated and its pinning tests and guards moved in the same change. The committed enumeration is this chunk's INPUT, not a thing to re-derive; CARRY 2 binds the pin inventory to the WIDE sweep form, the narrow one having returned a false zero for all four names the prior chunk retired. The two satisfiable-but-weak survivors (`root-span-error-scope`, `span-status-error-detection`) are deliberately untouched — retiring live coverage on a false equivalence would be the opposite of solving, and the "what counts as satisfiable" question is owned by the `Scenario-assertion audit gate` entry. Population re-derived at take-up, not inherited: 8 live blocks over 7 scenarios = 6 dead + 2 satisfiable, leaving 2 committed after this chunk; SUT still at Pulse HEAD 83d4060, the commit the grounds were measured against · → conductor-0.3.0/chunks/2026-09-15-remaining-structurally-dead-declarations-retired/
