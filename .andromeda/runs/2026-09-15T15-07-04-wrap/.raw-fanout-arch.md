proposals:
  - detector: D-arch-resources
    severity: warning
    section: Standard Contracts
    change: Replace "NINE families now ship declare-only — `fingerprint-storm` … and the structurally-dead-assertion class (`constellation-severity-live-wiring` · `pulse-run-contract` · `cross-incident-recurrence`, 2026-09-15)" with the SET rather than a fresh count — declare-only is now the default shape: every committed scenario ships declare-only EXCEPT the two that still declare live `[[expected]]` checks, `root-span-error-scope` and `span-status-error-detection` (2 blocks across 2 of 36 committed scenarios, measured 2026-09-15); the structurally-dead-assertion class gained `activity-floor` · `service-went-silent` · `high-severity-log-capture` · `exception-event-capture` · `threshold-hot-reload` (2026-09-15), keeping the existing per-family retirement dates and the "zero `[[expected]]` checks each / `verdict: null` / `state: KnownResidual`" clause unchanged.
    sidecar: 2026-09-15 — §Standard Contracts declare-only registry re-based from the "NINE families" count to the SET: five more scenarios retired to declare-only, leaving `root-span-error-scope` + `span-status-error-detection` as the only checks-bearing pair.
    rationale: The report's fourth Counts/qualifiers bullet names `architecture.md:135` ("NINE families now ship declare-only" plus its enumerated set) as stale in BOTH the count and the set, and its Expected-amendments bullet carries this as an §Standard Contracts amendment with the direction "name the SET, never a fresh count (the 2026-08-19 re-base precedent)". The chunk retires six `[[expected]]` blocks across five scenarios (`scenarios/activity-floor.toml` · `service-went-silent.toml` · `high-severity-log-capture.toml` · `exception-event-capture.toml` · `threshold-hot-reload.toml`), none of which appears in the registry: live `[[expected]]` blocks 8 → 2 and scenarios declaring ≥1 block 7 → 2 (`cat scenarios/*.toml | grep -c '^[[expected]]'` → 2; `grep -l … | wc -l` → 2), with the acceptance criterion printing exactly `root-span-error-scope.toml,span-status-error-detection.toml`. Committed scenarios unchanged at 36.
    basis: .andromeda/architecture.md:135

# Non-hits reported by the same agent:
#   D-arch-decisions — no hit. Dependencies "none added, none bumped", Crates/modules "none",
#     Symbols "no production symbol added, changed or removed". The nearest candidate,
#     §Established Decisions [Probabilistic-Assertion Policy] (architecture.md:71), is not
#     contradicted: it governs the verdict mapping for checks that ARE graded, and arch's baseline
#     already carries declare-only retirements of deterministic families (restart-suppression, 2026-08-18).
#   D-platform-claim — no hit. "Harness / gate surface: none"; "Spec claims disproved by measurement:
#     none in any spec master". No entry retires a platform/runner/driver verdict, so there is no
#     stating sentence in arch to quote.

# ORCHESTRATOR NOTE (not the agent's text): the `change` line's COUNT premise was re-derived and
# found FALSE — the five join the existing structurally-dead-assertion class, so the nine-group count
# does not move; only that class's membership (3 -> 8) and the enumerated scenario total (21 -> 26) do.
# The applied text is re-derived per amendment-flow §Apply and differs from this proposal; the
# wholesale reframing offered here was not taken. See fanout-results.md §Orchestrator correction.
