# a11y extract

## Relevance
partial (narrow) — no rendered-UI/WCAG work here; the chunk touches a11y only through the `slo_tier` enumeration my plan reproduces verbatim and through webview fixture scenarios that sit outside `scenarios/*.toml`.

## Constraints
- The closed tier set is reproduced VERBATIM inside a11y's violation envelope at two sites — a11y-plan §1 (A11y assertion harness specification → Structured violation JSON schema) and §3 (Structured violation JSON schema) — both carrying `"slo_tier": "&lt;5s | &lt;20s | &lt;90s"`. a11y narrows, obs fixed the schema; a11y may never redefine this field. The chunk's boundary ("no change to the tier ladder itself") keeps both reproductions valid, so no a11y amendment is owed — provided re-declarations stay inside the closed set (per a11y-plan §3 Structured violation JSON schema).
- a11y-plan §3 requires a11y artifacts to MAP `slo_tier` to `&lt;90s` and records the scenario-shaped keys as **mapped, not measured** — a re-tier of a committed scenario must not be read as changing a11y artifact emission. Whether the a11y harness derives `slo_tier` from any scenario file (rather than hard-mapping it) is research's question (per a11y-plan §3 Structured violation JSON schema).
- Two distinct "SLO" vocabularies must stay distinct: conductor's `SloTier` deadline for a scenario, versus a11y's OWN budget in §10 (axe-core &lt; 30s routine; 15-minute mocha ceiling on the driven arm). §10 expresses the a11y budget in wall-clock seconds per ARM, never via `SloTier` — this chunk must not unify them (per a11y-plan §10 SLO Invariants &amp; A11y Budgets).
- a11y-plan §3 (CI integration / Pipeline integration) requires the ROUTINE arm to seed its own committed fixture through the production writer so it needs no live Pulse. Those fixture scenarios live under `crates/conductor-tauri/tests/fixtures/scenarios/` — a different directory from the chunk's `scenarios/*.toml` surface. Whether the chunk's re-tier sweep reaches them (and whether they declare `slo_tier` at all) is research's question (per a11y-plan §3 CI integration).
- a11y-plan §1 (A11y scope → `conductor-cli` entity) declares the WHOLE `conductor` verb surface **not-assertable** (no DOM/widget tree, no axe/Lighthouse reach) and excludes it from §2 and §4. Scenario TOMLs, `SloTier`, and the Rust tests pinning deadlines carry no a11y assertion obligation (per a11y-plan §1 A11y Scope Summary).
- a11y-plan §10 scopes the zero-WCAG-AA-violations invariant to the four must-be-accessible paths (scenario pick/start, operator-pause, run-report view, operator-checklist). No element on those paths is a scenario TOML, so this chunk adds no WCAG coverage obligation (per a11y-plan §10 Always-required SLO invariant).
- Naming hazard: `halo-hue-encoding` and `report-render-surface` are conductor SCENARIO names in this chunk's partition, not a11y surfaces; neither is the `--status-*`/`--count-*` token work of a11y-plan §6 nor the run-report view path of §1 (per a11y-plan §1 Critical paths).

## Patterns to follow
- Verbatim-reproduction discipline: when the envelope moves, obs amends first and a11y's reproductions follow at EVERY site — the 2026-06-16 amendment (`read_back_observed_at`) is the worked example. If the tier set ever moved, both a11y reproductions would move together (a11y-plan §3 Structured violation JSON schema).
- Key-PRESENCE conformance, not key exclusivity: a11y-plan §3 records that `journal_conformance` asserts presence, admitting obs resource tags (13 keys local / 15 under CI). A changed `slo_tier` VALUE is therefore not a conformance break by itself.
- Fixture-seeded routine arm: the routine leg proves itself without a live Pulse (a11y-plan §3 Pipeline integration) — consistent with the chunk's "not a live-Pulse chunk" boundary, so the chunk cannot disturb the arm's Pulse posture.
- Identify CI jobs BY NAME (`a11y` on `windows-2025`), never by `ci.yml` line coordinates, which move (a11y-plan §3 Configuration) — applies if the chunk's test changes reach the workflow.

## Anti-patterns to avoid
- NEVER let a11y artifacts leak absolute host paths or internal seam-crate struct names into the obs §6 envelope (a11y-plan §11 → SLO) — any scenario name or stated-reason string that could reach an artifact must stay path-free and struct-name-free.
- NEVER define a soft a11y SLO (a11y-plan §11 → SLO). This chunk's "declare the ceiling plus a stated reason" posture is a SCENARIO-tier remedy; it must not be imported into the a11y gate as a violation-waiver mechanism.
- NEVER skip a11y on failure — the strictness mechanism is `CONDUCTOR_A11Y_STRICT` plus the printed-verdict assertion (failed-spec count, expected-skip SET), because the exit code alone cannot enforce it (a11y-plan §11 → CI). If the chunk moves tests that touch the a11y leg, the expected-skip SET must stay intact.

## Contract bindings
- **a11y ↔ obs §6:** the `slo_tier` field and its closed enumeration are obs-owned; a11y reproduces them verbatim at a11y-plan §1 (harness specification) and §3 (Structured violation JSON schema). Any tier-ladder change would be an obs-first amendment with a two-site a11y follow — this chunk's boundary forecloses that.
- **a11y ↔ tests:** the routine a11y arm rides tests' `wdio run` webview-E2E leg and tests' binding 5-command harness (`logs` emits artifacts), per a11y-plan §3 CI integration. If P3's enumeration of "tests pinning a specific scenario's tier or deadline" reaches the webview fixture scenarios under `crates/conductor-tauri/tests/fixtures/scenarios/`, that binding activates.

## Acceptance criteria contributions
- (a11y) The closed tier set `&lt;5s` / `&lt;20s` / `&lt;90s` is unchanged after the chunk, so both verbatim envelope reproductions still match the shipped artifact's `slo_tier` field — no a11y-plan amendment owed (per a11y-plan §3 Structured violation JSON schema).
- (a11y) No scenario under `crates/conductor-tauri/tests/fixtures/scenarios/` (the routine arm's committed fixture) is re-tiered without re-running the routine a11y leg; P3 states whether those fixtures declare `slo_tier` at all (per a11y-plan §3 CI integration).
- (a11y) The §10 per-arm a11y budget is untouched — no scenario re-declaration alters the axe-core &lt; 30s routine budget or the 15-minute driven-arm mocha ceiling (per a11y-plan §10 Performance budget per a11y CI run).
- (a11y) No WCAG conformance claim is made by this chunk: it renders no DOM element on the four must-be-accessible paths, and a11y-plan §2 bars any conformance claim without machine-verifiable evidence (per a11y-plan §10 Always-required SLO invariant + §11 → Strategy).

## Relevant amendment history
- **2026-06-16-emission-journal-writer** — a11y's violation-schema reproduction gained `read_back_observed_at` to track obs §6 (D-a11y-obs-schema). This is the precedent that governs the `slo_tier` enumeration: a11y's reproduction is a downstream copy that moves only when obs moves. Relevant because this chunk's subject is that exact field's value set.
- **2026-09-07-a11y-ci-gate** — retired the unqualified "no new top-level fields" reading; recorded the shipped artifact as 13 keys local / 15 under CI, `journal_conformance` asserting key PRESENCE not exclusivity, and the scenario-shaped keys (`seed: 0`, `scenario` = invoked suite, `p_ids: []`, **`slo_tier` `&lt;90s`**) as MAPPED not measured. Directly bounds how much this chunk's re-tiering can affect a11y artifacts: the mapping is fixed, not read from a scenario file — which P3 should confirm at HEAD.
- **2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate** — the verbatim-cross-master-citation edge: three sites carried the same quoted arch sentence and the third was found only by a known-positive control grep (`grep -c` returned 3 where 2 had been edited). Method note for any tier-set touch: sweep with a control, since a11y carries the enumeration at two sites and a single-site apply would leave one asserting a retired set.
- No amendment in the history touches scenario `slo_tier` declarations, `SloTier`, or `contracts/pulse-load-envelope.toml` — this chunk's area has no prior a11y precedent beyond the envelope-reproduction thread above.
