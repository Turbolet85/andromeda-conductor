
## 2026-09-16-scenario-assertion-audit-gate — the second static gate over committed data

**Section:** §6 (the static-gate scenario) · §4 (Scenario catalog + coverage-matrix) · §7 (Self-bootstrapping requirement, two sites)
**Change:** §6's `Coverage-matrix completeness gate (static)` scenario gained a sibling leg recording the scenario-assertion audit gate — `crates/conductor-core/tests/scenario_audit_gate.rs`, asserted by `cargo nextest run -p conductor-core --test scenario_audit_gate --profile ci` and CI-enforced by the presence-guarded `Scenario-assertion audit gate` step — with its two axes, its both-directions exact-set grading (`unpinned` / `rotted` / `lost_subject`, no literal-count predicate), its six in-suite negative arms, its closed-set arm and its sweep control, plus the explicit note that a ledger row's `discriminates` flag is RECORDED and never graded because whether a token depends on the scenario's own stimulus is a SUT-behaviour claim §11 routes to the live/operator gate. §4's scenario-catalog bullet now names the catalog half's own crate-local `tests/`-tier gate beside the coverage-matrix half's. §7's committed-fixture family gained its third tree (`crates/conductor-core/tests/fixtures/`) AND — the site the sweep caught, which the body edits had left standing — its per-fixture round-trip enumeration gained the matching round-trip.
**Why:** The chunk shipped the gate, so §6 named one static gate where the tree now runs two, §4 read as though P-ID presence were the only static assertion over the committed catalog, and §7's fixture sample named 2 of 3 trees. Recorded as a sibling LEG rather than an eighth `#### Scenario`, so §6's "(7 scenarios — the test-scope Section 4 maximum.)" qualifier holds, and with NO §9 stage-table row, per the `2026-09-06-coverage-completeness-gate` operator resolution.

**Sweep (cascade step 2), run after every body edit of this pass, over the seven masters + CLAUDE.md + the rule files + `docs/session-learnings.md` + `playbook.md` + `drift-base.md`, with a known-positive control (`scenario-audit-ledger`, 4 hits) confirming the probe fires — and it earned its keep here:**
`tests/fixtures/`: **2 hits** — `test-plan.md:401` (the tree SET, amended with the body edit) and `test-plan.md:402` (the per-fixture ROUND-TRIP enumeration, which the body edit did NOT reach and the sweep did). Both amended; the second is the case the contract's write-the-entry-after-the-sweep rule exists for, since an entry written before it would have claimed a caught-ALL that was one site short.
`coverage_gate`: **7 hits** — `:250` amended here, `architecture.md:60` and `security-plan.md:113` amended in this pass, and four NO-CHANGE: `obs-plan.md:357` (the coverage roll-up's log-field sentence), `playbook.md:64` (a judgment base, routed to propose→approve→append rather than edited), `:80` (§1 Critical Path 6's own row) and `:363` (§6's Verification signal for the coverage gate) — each describes the coverage gate as its own subject and none asserts it is the only static gate, so none went stale.
`binary-edge load sites`, the retired CI gate-set wordings: **0 hits** each, trusted because the control fired.
