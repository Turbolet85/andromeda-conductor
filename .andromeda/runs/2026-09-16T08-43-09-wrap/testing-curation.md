- 2026-09-16: **A fixture that exists to prove a SWEEP FORM sits inside that sweep's own subject, so its explanatory prose must DESCRIBE the guarded token, never spell it.** The wrapped-gloss control for the scenario-audit gate carries a retired gloss split across two comment lines, and its arm asserts BOTH that the comment-marker-stripped join DETECTS it and that a plain single-line search MISSES it — the second half being what proves the strip-and-join is load-bearing rather than decorative. The header written to explain that trick spelled the gloss on one line, which made the second half false; three gate entries went red on a fixture that was correct in the only way anyone had thought to check, and the failure named the fixture rather than the code. `.claude/rules/host-win32.md` (2026-09-12, the drive-letter-anchor entry) already states this shape for a hygiene gate's own command text, and it did not transfer because the domain looked unrelated. The facet that is new: **when a fixture's assertion includes an ABSENCE, the fixture's own commentary is a place that absence can be violated.** Write such a fixture's payload and its header as one act, then re-read the header as the sweep would — and prefer a control whose two halves are asserted together, so a header that breaks one of them cannot pass by satisfying the other.
