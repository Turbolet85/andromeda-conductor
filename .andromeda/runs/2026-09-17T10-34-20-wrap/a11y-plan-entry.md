
## 2026-09-17-a11y-routine-arm-terminal-on-the-measured-configuration — the routine pair's CI proof DISCHARGED, stated with its configuration

**Section:** §1 A11y Scope Summary (CI integration bullet) · §3 A11y Assertion Harness Contract (Configuration · Pipeline integration · Bootstrap phases, a11y-ci-gate-wire) · §9 CI Integration (Platform · E2E artifacts row · Pipeline integration) · §11 A11y Anti-Patterns → Strategy (the 2026-09-07 CARVE-OUT entry)

**Change:** The carve-out's standing debt — "**The CI PROOF of the routine pair is still owed:** the `a11y` job is red at WebView2 session creation on the hosted runner" — is DISCHARGED: the hold-free Operable pair (SC 2.1.1 reachability, SC 2.4.3 focus order) is green in CI on the measured configuration, hosted `windows-2022` at a coherent 131.0.2903.86 driver/runtime pair at High integrity with the driver pinned from the image's own runtime, at 12 passing / 0 failing / 2 skipped, run 35208593666. "CI runs 152 and is unaffected; the pair's CI proof remains owed" is retired, and the 2026-09-16 dev-host regressions of exactly this pair are re-recorded as ASSERTION defects — a walk waiting on a `BODY` sentinel this webview's focus cycle need not contain — rather than a platform property, with the pair now environment-independent across two configurations differing in runtime major, driver major and coherence. "The job is exercised but has never been green" is retired. Six shipped-arrangement literals move `windows-2025` → `windows-2022`, and §9's Pipeline-integration parenthetical re-points the launch path from `scripts/a11y-limited-token-launch.ps1` to the direct `scripts/a11y-token-witness.ps1` entry at native High integrity. §3's "The step is not Linux-bound" sentence gains the measured platform SET (Linux+`xvfb`, the Windows WebView2 dev host, and the hosted Windows image at a coherent pair; only macOS lacks a WebDriver).

**Why:** Report `Spec claims disproved by measurement` 3 and 5, Outcome 1, 2, 3, 11 and 13, and the `Counts / qualifiers moved` bullet. Every discharged claim is written WITH its configuration rather than as a bare verdict — the operator's directive names this explicitly, and the arc that produced it cost six days to an over-general claim in the opposite direction: the next image bump can move the label, the runtime, the driver or their coherence, and the record should make that visible rather than hide it behind "the a11y arm is green in CI". The three premise-correction brackets that bind the RED reading to `windows-2025` at runtime 152/153 are left standing as that configuration's fact.

**Sweep (cascade step 2, run after the last body edit of the pass):** patterns as recorded in the architecture entry of the same marker, over all seven masters, the derived tier, the three preserve-verbatim curation homes and the two judgment bases. In a11y-plan.md after the pass: `windows-2025` 7 lines — the six shipped-arrangement literals were moved and every remaining occurrence is inside a dated premise-correction bracket binding the RED reading to that image at 152/153, or names the external `windows-2025-vs2026` image in the corroborating issue reference; `Evergreen` 1 and `limited-token` 1, both historical. `CI proof … still owed`, `never been green`, `CI runs 152` return 0. Two hits were routed OUT of the cascade rather than edited, per the preserve-verbatim rule: `.claude/rules/testing.md`'s `## Session Additions` carries a 2026-09-07 correction naming `a11y` on the `windows-2025` label, and `.claude/docs/session-learnings.md` carries dated Evergreen/152 entries — both go to P3 curation as in-place extensions, never to a cascade edit.
