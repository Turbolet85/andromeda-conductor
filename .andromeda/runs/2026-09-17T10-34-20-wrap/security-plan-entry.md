
## 2026-09-17-a11y-routine-arm-terminal-on-the-measured-configuration — third dependency class swapped; spawn-rule (b) forms six and seven dispositioned

**Section:** §Threat Model Summary (Infrastructure → Networking · CI/CD) · §Dependency Security (third dependency class · the supply-chain SKIP note) · §Input Validation (`CONDUCTOR_MSEDGEDRIVER` row) · §Security Anti-Patterns → Code Patterns (spawn rule (b), sixth and seventh governed forms)

**Change:** The THIRD dependency class keeps exactly one member and SWAPS it: the WebView2 Evergreen bootstrapper retires with its install step, and Microsoft's version-pinned msedgedriver takes its place, fetched at the version the image's own WebView2 runtime names and admitted by the same sole `Get-AuthenticodeSignature` control. The `≥ 152` floor is retired as falsified on its own terms — not necessary (a coherent 131/131 pair runs the arm green), not sufficient (a coherent 152/152 pair fails, run `34654076633`), destructive on the working configuration (run `35185153012`). The float's stated EXIT CONDITION is rewritten because the mechanism it named does not exist: the Standalone Installer installs Evergreen and takes no version argument, and Fixed Version cannot supply 131; the float retires instead by pinning the DRIVER to the runtime. §Networking re-registers the sole non-loopback egress as the unconditional but version-derived `msedgedriver.microsoft.com` fetch (count one before, one after), and §CI/CD records that the fetching step verifies and stops while the LEG executes the fetched binary. The `CONDUCTOR_MSEDGEDRIVER` row's producer sentence is corrected: `EDGEWEBDRIVER` is now written by the pin gate through `GITHUB_ENV`, not read from the runner image; the `Test-Path` precondition and `wdio.conf.ts`'s sole-reader `isFile` + metacharacter guard stay byte-unchanged. **Spawn rule (b), both ESCALATED and operator-resolved 2026-09-17:** the SIXTH form is RETIRED with its step and its ordinal is not reused, but its distinguishing property — a governed form whose PROGRAM arrives from the network at job time — TRANSFERS to the dev-only driver-stack spawn, because the binary at `CONDUCTOR_MSEDGEDRIVER` is now the fetched driver; the SEVENTH form is RETAINED with its SCOPE corrected to the job's driver-alone diagnostic steps (four `ci.yml` call sites, down from five), never the asserting step, which invokes the leg's entry point directly at native High integrity. A clarifying clause was added where coherence is introduced, distinguishing it from the separately-retired skew cause.

**Why:** Report `Spec claims disproved by measurement` 1, 2 and 4, and the `Dependencies` / `Symbols — Ports/egress` / `Harness / gate surface` bullets. Rule (b)'s class escalates rather than going routine however often it recurs, so both forms were put to the operator rather than applied as drift. Both resolutions were ratified 2026-09-17 with their reasons recorded: a registry silent about a spawn a committed workflow still performs fails in the same way as one describing a spawn it no longer performs (the seventh), and retiring the network-program property outright would orphan the Authenticode control's stated justification while the job still executes a network-fetched binary (the sixth). The egress ratification rests on a measured NET NARROWING — the fwlink carried no version selector, this URL's version derives from the image — re-derived independently at this wrap: `ci.yml` contains exactly two network calls, the driver fetch and `127.0.0.1:9515`, the latter loopback.

**Sweep (cascade step 2, run after the last body edit of the pass):** patterns as recorded in the architecture entry of the same marker, over all seven masters, the derived tier, the three preserve-verbatim curation homes and the two judgment bases. In security-plan.md after the pass: `Evergreen` 5 lines / 7 occurrences, every one historical (what was replaced, what retired, or the disproved Standalone-Installer mechanism); `limited-token` 1 and `runas` 1, both inside the seventh form's corrected scope; `fwlink` 1 (historical); `windows-2025` / `windows-2022` 0 — this document states no runner label, so the label amendments belong to architecture, test-plan and a11y-plan, not here. `FLOORED AT 152`, `exit condition remains UNMET` and `measured-unproven` return 0 across every master.
