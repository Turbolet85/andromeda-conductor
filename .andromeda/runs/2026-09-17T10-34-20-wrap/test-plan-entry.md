
## 2026-09-17-a11y-routine-arm-terminal-on-the-measured-configuration — hosted-runner capability restated as the measured SET; a11y job arrangement re-stated

**Section:** §1 Test Scope Summary (Surfaces under test, desktop-webview Driver) · §2 Test Strategy (Agent-runnable invariants) · §6 E2E Test Strategy (Drivers per surface · Both-surface parity step 2) · §9 CI Integration (Pipeline structure table, E2E-webview row · Matrix builds)

**Change:** Five restatements of one capability verdict — "the dev host, the only member measured to DRIVE a session; CI's `windows-2025` `a11y` runner is wired but drives none on the image as shipped" — are retired and replaced by the measured SET: the dev host AND the hosted `windows-2022` runner at a coherent 131.0.2903.86 driver/runtime pair at High integrity, where the arm is green at 12 passing / 0 failing / 2 skipped (run 35208593666); hosted `windows-2025` at runtime 151/152/153 drives none. "That capability does NOT extend to the GitHub-hosted image AS SHIPPED" is re-scoped to the `windows-2025` image specifically. §6's "**The named candidate is now ELEVATION, and it is the established cause**" is bounded to the configurations that measured it and explicitly does NOT generalize — integrity's SIGN is configuration-bound, the Medium drop helping at runtime 153 on the dev host and breaking the session at 131 — and §9's "REMEDIABLE IN PRINCIPLE — the app must not run elevated" is retired as a general remedy. The §9 stage row and Matrix-builds prose re-state the job's arrangement by SET rather than by count: `windows-2022`, the driver pinned by the job's own gate and published as `EDGEWEBDRIVER` rather than taken from the image, the asserting step invoking `scripts/a11y-token-witness.ps1` directly at native High integrity, and the diagnostic step set minus the removed version-diagnostic. The superseded 11 passing / 1 failing / 2 skipped probe reading stays dated to run 35192876641 and is marked superseded by the green, with `:384` recorded fixed rather than an open defect.

**Why:** Report `Spec claims disproved by measurement` 3 and 5, `Counts / qualifiers moved`, `Harness / gate surface` and Outcome 1, 2, 3 and 13. The detector's own guidance governs the FORM of the fix: retire a platform verdict by naming the measured SET, never by substituting a fresh single-platform literal — which is also why the job step count (17 → 16) is recorded as a step SET rather than a number the next chunk would re-stale.

**Sweep (cascade step 2, run after the last body edit of the pass):** patterns as recorded in the architecture entry of the same marker, over all seven masters, the derived tier, the three preserve-verbatim curation homes and the two judgment bases. In test-plan.md after the pass: `windows-2025` 5 lines, every one either a dated measurement or explicitly bound to that image at its runtime; `Evergreen` 2 and `floor-conditional` 2, all four naming the 2026-09-12 experimental condition under which skew was retired as a cause — historical context, deliberately preserved; `limited-token` 1 and `runas` 1 inside the now-moot remedy-class history; `drives no session` 1, bound to the `windows-2025` image across both its measured runtime majors; `must not run elevated` 1, quoted as retired. `only member measured`, `never been green`, `measured-unproven` and `exit condition remains UNMET` return 0 across every master. One INTRA-document tension was found and closed rather than left: §6 and §9 state that driver/runtime major SKEW is retired as the cause of the hosted `windows-2025` missing endpoint, which a reader could take as contradicting this chunk's coherence finding; the distinction — an incoherent pair is REFUSED with an explicit version error, while the `windows-2025` failure was the silent absence of `DevToolsActivePort` under a pair that already cohered — is stated in security-plan.md where the coherence claim is introduced.
