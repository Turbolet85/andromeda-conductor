# a11y extract

## Relevance
Relevant — the chunk's entire deliverable is text inside my domain authority (`a11y-plan.md` §5 keyboard/focus claims, §11 Strategy carve-out, plus the §1 `:115` repair).

## Constraints
- The claim population to attribute is exactly a11y-plan §5 *Keyboard Navigation*: focus order per layout (`run-console-idle` / `run-console-live` / `run-console-HOLD` / `idle-with-report`, `:353-356`), skip links (`:359`), focus trap (`:361-363`), focus restoration (`:365-367`), per-surface shortcuts (`:370`), plus §5's own harness-pattern bullet (`:372`) which binds the population to SC 2.1.1 / SC 2.4.3 / SC 2.1.2 / SC 2.4.7 — every one needs one owner or an explicit N/A-by-construction (per a11y-plan §5).
- The hold-dependent Operable half — trap entry/containment and restoration — is required to stay operator-local and barred from CI by test-plan §11 + architecture §Established Decisions [CI/CD]; §11 *Strategy*'s CARVE-OUT (`:516`) is the plan's existing home for exactly that statement (per a11y-plan §11 Strategy).
- A carve-out must name a substitute gate, never an absence: §11 *Strategy* requires that POUR coverage is never skipped at Minimal tier ("both axe-core `color-contrast` and WebdriverIO-keyboard must run on every must-be-accessible path") and §10 requires zero WCAG AA violations on the four must-be-accessible paths *including SC 2.1.2 and SC 2.4.7* — an ownership pass must not narrow that invariant (per a11y-plan §10 + §11 Strategy).
- Owners must be named as jobs/suites, not coordinates: §3 *Configuration* requires "identify CI jobs BY NAME, not by `ci.yml` line coordinates, which move as the workflow grows"; §9's E2E artifacts row supplies the canonical arm names (routine `agent-run run --e2e` / driven `npm run a11y:driven` / `sr*` leg) (per a11y-plan §3 Configuration + §9 E2E row).
- Any owner named must be the one webview-automation stack: WebdriverIO `browser.keys()` + `document.activeElement` over `@crabnebula/tauri-driver`, with PASS/FAIL folded into the obs §6 envelope — §11 *CI* bans standing up a second automation stack (per a11y-plan §3 Keyboard test harness + §3 Focus management test harness + §11 CI).
- Whatever gates "in place of" the driven suite must be a mechanism, not a promise: §11 *CI* requires `CONDUCTOR_A11Y_STRICT` plus assertion of the runner's printed verdict (failed count, per-spec skip tally against the expected-skip SET, driven-session banner), because "a skipped-green run and a real pass are otherwise indistinguishable" (per a11y-plan §11 CI).
- CARRY 1's host line is §1's CI-integration sentence, which the plan states **with its configuration** (hosted `windows-2022`, coherent 131.0.2903.86 pair, High integrity, run 35208593666); the repair is a one-clause deletion that must leave every configuration term and the premise-correction brackets intact (per a11y-plan §1 CI integration).

## Patterns to follow
- §5's `run-console-idle` bullet (`:353`) already carries the in-plan shape an ownership sentence takes: claim → arm → marker date → the SCs it covers ("Asserted UNATTENDED on the routine arm since 2026-09-07 (`2026-09-07-sr-findings-fixed`)"). Reuse that sentence form for the remaining bullets (per a11y-plan §5).
- §5's two N/A-by-construction bullets — skip links (`:359`, "no repeated nav blocks to bypass") and no-route-change-surface (`:367`) — are the shape for a claim that has no owner because it has no target; the scope's table marks the same two rows N/A (per a11y-plan §5).
- The §11 *Strategy* carve-out (`:516`) is the shipped template for a scoped, dated, configuration-bound statement: it narrows explicitly, quotes the readings it retires, and names what remains operator-local and why (per a11y-plan §11 Strategy).
- §3 *Keyboard test harness* (`:257-264`) enumerates sequences per ARIA pattern (button / dialog / combobox / checkbox / coverage matrix / first-class shortcuts) — the natural cross-index for a per-claim owner table, and the site the scope pairs with §5 (per a11y-plan §3 Keyboard test harness).
- §9's E2E artifacts row already states per-arm scope prose including the routine arm's context-skip rule; extending attribution there keeps §5, §9 and §11 saying one thing (per a11y-plan §9 E2E row).

## Anti-patterns to avoid
- Do not let attribution silently drop a claim: §11 *SLO* bans a soft a11y SLO and §11 *Strategy* bans skipping POUR coverage — "owner = none" is not an admissible terminal for a claim with a target (per a11y-plan §11 Strategy + §11 SLO).
- Do not re-entrench a retired reading while editing a claim site: §5 `:366` still attributes focus restoration to the "Radix AlertDialog default", which §5 `:362` and §3 *Focus management test harness* both state is **NOT** a Radix default (explicit `onCloseAutoFocus` + `restoreFocusTo`; Radix restored to `&lt;body&gt;` on this Trigger-less Channel-opened dialog). `:366` is inside this chunk's claim population, so the owner sentence must not carry the retired attribution forward (per a11y-plan §5 + §3 Focus management test harness).
- Do not name the gate by workflow line coordinates or by a bare verdict ("the a11y arm is green in CI") — §3 *Configuration* requires job names, and §11 *Strategy* requires green stated with its configuration because "the next image bump can move any of those terms" (per a11y-plan §3 Configuration + §11 Strategy).

## Contract bindings
- **a11y ↔ tests:** the driven suite's CI bar is not a11y's alone — §11 *Strategy* records it as held by test-plan §11 + architecture §Established Decisions [CI/CD], and §9 records the driven arm as REQUIRING a live Pulse plus its full firing form (tests §6). A carve-out worded here must agree with those two masters rather than restate them independently.
- **a11y ↔ obs:** keyboard/focus PASS/FAIL emits into the obs §6 envelope with axe rule-id + WCAG-SC tag + selector inside `fingerprints[]`; §10/§11 SLO forbid leaking absolute host paths or internal seam-crate struct names — so if an owner name ever reaches the artifact it must be a suite/spec name, never a filesystem path (per a11y-plan §3 Structured violation JSON schema + §11 SLO).
- **a11y ↔ amendment flow:** `a11y-plan.md` is a spec master whose history lives in `a11y-plan-amendments.md` (written by wrap P2); the scope's open question about which channel may author the ownership statement is a process fork the scope carries to P4 — my domain adds only that §11 Strategy is the carve-out's correct *section*, whichever channel writes it.

## Acceptance criteria contributions
- (a11y) Every claim in the §5 population — four focus-order-per-layout bullets, focus trap, focus restoration, per-surface shortcuts, skip links — resolves to exactly one named owner or an explicit N/A-by-construction, with no bullet left unattributed (per a11y-plan §5).
- (a11y) The hold-dependent claims (trap entry/containment, restoration) carry a carve-out naming what gates in place of `operator-hold.e2e.ts`, stated with its configuration and its enforcing mechanism (`CONDUCTOR_A11Y_STRICT` + printed-verdict assertion), not as a bare exemption (per a11y-plan §11 Strategy + §11 CI).
- (a11y) No attributed claim narrows the zero-WCAG-AA invariant: SC 2.1.1 / SC 2.4.3 / SC 2.1.2 / SC 2.4.7 remain gated on the four must-be-accessible paths after the pass (per a11y-plan §10).
- (a11y) Owners are named as job/suite names (job `a11y`; routine / driven / `sr*` arms) and never as `ci.yml` line coordinates (per a11y-plan §3 Configuration).
- Note for P3: the plan is target state — whether `accessibility.e2e.ts` and `operator-hold.e2e.ts` currently assert each claim they would be named owner of is research's question, not something this extract can attest.

## Relevant amendment history
- **2026-09-07-sr-findings-fixed** — the closest precedent: narrowed the §11 *Strategy* carve-out to its hold-dependent half across 8 sites, added the routine-arm attribution to §5 `run-console-idle`, and quoted three readings it retired. Why it matters: this chunk continues that same attribution arc and should follow its retire-by-quoting shape.
- **2026-09-07-a11y-ci-gate** — created the Operable CARVE-OUT at §11 *Strategy* (operator-selected, no detector proposed it) and replaced stale `ci.yml:18`/`:178` coordinates with job NAMES. Why: establishes both the carve-out's home and the identify-by-name rule this chunk's owner statements inherit.
- **2026-09-17-a11y-routine-arm-terminal-on-the-measured-configuration** — discharged the routine pair's CI-proof debt inside the §11 carve-out entry and moved six `windows-2025` → `windows-2022` literals; its §1 CI-integration edit is what moved CARRY 1's offsets (866/938 → 2555/2627). Why: the directive "stated with its configuration rather than as a bare verdict" governs any new carve-out wording, and this is the pass CARRY 1's re-measurement is defending against.
- **2026-09-17 / 2026-09-16-medium-integrity-launch-for-the-a11y-routine-arm** — recorded the sweep basis: `grep -oF` per claim phrase, "occurrence-counted with `grep -o`, since a line-granular count collapses multiple corrections on one multi-KB line." Why: this is exactly the counting discipline CARRY 1's dittography needs ("a line-granular read shows one hit and cannot see it").
- **2026-08-22-operator-pause-and-checklist-live-firing** — updated six sites so the HOLD trap contains the operator-checklist `checkbox` rows (Tab/Shift+Tab across rows then actions, Space toggle, `aria-checked`), naming §5 run-console-HOLD focus order + the focus-trap bullet among them. Why it matters here: as read this phase, §5 `:355` and `:362` describe the cycle as "across Proceed/Abort" only, while §1 `:131` and §3 `:253` include the checklist rows — whether that is drift or an intended §5 abbreviation is research's question, and both lines sit inside this chunk's claim population.
- **2026-09-01-desktop-a11y-sweep** — established that focus restoration is an explicit `onCloseAutoFocus` + `restoreFocusTo` contract, NOT a Radix default, naming §5 focus restoration as an amended site. Why: §5 `:366` as read this phase still says "(Radix AlertDialog default…)", and that line is one the chunk must attribute.
- **2026-09-06-coverage-completeness-gate** — operator ruled NARROW over a full sweep (retire only claims that a CI job exists today; eight further sites left standing as target-state). Why: a governing precedent for how far an ownership pass may edit adjacent target-state prose without operator ratification.
