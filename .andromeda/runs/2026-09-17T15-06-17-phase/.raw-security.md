# security extract

## Relevance
**Partial** — a spec-text chunk with no Rust surface, no new input boundary, no spawn and no expected dependency delta; security binds only through committed-artifact hygiene, the a11y CI-gate handle semantics this plan registers, and the no-new-crossing / no-dependency-delta gates.

## Constraints
- Any file this chunk commits — its `evidence/` tree, the CARRY 3 `msedgewebview2` re-census record, any pasted driver/CI output — must carry **no absolute host path and no internal seam-crate struct name**; the ban names the per-chunk `evidence/` tree explicitly and calls these records agent-parseable ground truth shared across hosts (per security-plan.md §Security Anti-Patterns → Logging). The census is the live risk: a process listing that includes an executable-path column is exactly the banned token class.
- The CI carve-out statement must not describe a **skip-at-exit-0** leg as gating. Per security-plan.md §Input Validation (`CONDUCTOR_MSEDGEDRIVER` / `CONDUCTOR_NVDA` rows) an unset or not-a-file host-tool handle SKIPS the requesting leg at exit 0; per the `CONDUCTOR_A11Y_STRICT` row the flag's sole effect is to INVERT that cost, and "stops the CI a11y gate from reporting success by skipping" is the stated reason the strict arm exists.
- Requirement text that references host-gated suites must name the **handle NAME**, never a resolved value or path — the plan requires handle-name-plus-boolean precondition text at every such edge (per security-plan.md §Input Validation, `CONDUCTOR_MSEDGEDRIVER` · `CONDUCTOR_NVDA` · `CONDUCTOR_A11Y_STRICT` rows; §Security Anti-Patterns → Logging).
- This chunk must add **no new governed harness-spawn form and no inbound listener**. Rule (b)'s registry is an exhaustive ordinal enumeration (seven, the sixth retired) and a new crossing is escalate-class, never routine (per security-plan.md §Security Anti-Patterns → Code Patterns rule (b); § Universal). The CARRY 1 python read-modify-write is an agent-local tool invocation; whether the repair leaves any committed script or workflow step behind is research's question.
- A carve-out saying "what gates in their place" must be written so the unrunnable check cannot read as satisfied — the plan's stated shape for an unmet gate is a **distinctly named state, never a false pass** (per security-plan.md §Security Anti-Patterns → Universal, never-silently-downgrade discipline).
- No dependency delta is expected; the standing gates still bind at the merge point — `Cargo.lock` un-drifted with `cargo-audit` (+ recommended `cargo-deny`) green, and `npm audit --omit=dev` = 0 if anything under `crates/conductor-tauri/ui/` moves (per security-plan.md §Security Anti-Patterns → Universal; §Dependency Security, Frontend (npm) supply chain).
- `security-plan.md` itself is **not editable during a phase-loop run** — amendments are wrap's flow (per security-plan.md front-matter, "Do not edit during phase loop runs"). The scope's open question about which channel may write a spec master does not open this master.

## Patterns to follow
- **Handle-NAME-only precondition text**: the CI arm's producer gate prints a handle-named boolean and exits non-zero, never the path; the dev host prints a host-path-free skip naming the handle (per security-plan.md §Input Validation, `CONDUCTOR_MSEDGEDRIVER` / `CONDUCTOR_NVDA` rows). The model for any suite/gate sentence this chunk writes.
- **"An ADDED arm, never a relaxation"**: when the plan registered the strictness flag and the CI producer validation, both were stated as added arms with the existing guards recorded byte-unchanged (per security-plan.md §Input Validation, `CONDUCTOR_A11Y_STRICT` row). An ownership/carve-out statement should say what it does NOT change as explicitly as what it adds.
- **Enumerations are amended at every restating site, never single-site**: this plan's recurring failure mode is an exhaustive claim going stale at one of its restatements (per security-plan.md §Input Validation, committed-manifests per-READER clause). The chunk's quantifier is "for every keyboard/focus claim" — the same exhaustiveness hazard.
- **Record the residual explicitly** where a duty is discharged at a different site than the one that appears to own it (per security-plan.md §Input Validation, `CONDUCTOR_E2E_SEED_DIR` row; §Security Anti-Patterns → Input). A carve-out is structurally that shape: the gate lives elsewhere, so the residual is stated, not implied.
- **`&lt;host-path&gt;` scrub + `security_finding` row** is the established shape for admitting untrusted third-party text into a committed record (per security-plan.md §Input Validation, screen-reader speech-log ingest row) — applies if the ownership table attributes any claim to an `sr*` suite whose evidence is re-cited here.

## Anti-patterns to avoid
- NEVER let a committed record (report, `evidence/` tree, census output) leak an absolute host path or an internal seam-crate struct name (per security-plan.md §Security Anti-Patterns → Logging).
- NEVER widen the governed spawn registry or introduce an inbound listener of Conductor's own as a side effect of a documentation chunk (per security-plan.md §Security Anti-Patterns → Code Patterns rule (b); § Universal).
- NEVER write a carve-out under which a check that cannot run reads as satisfied — the skip-reported-as-success shape the strict arm exists to prevent (per security-plan.md §Security Anti-Patterns → Universal; §Input Validation, `CONDUCTOR_A11Y_STRICT` row).

## Contract bindings
- **security ↔ a11y**: `CONDUCTOR_A11Y_STRICT` is registered in security-plan.md §Input Validation as the fourth handle class, with its justification citing a11y-plan §11 Anti-Patterns → CI. Any sentence this chunk adds to `a11y-plan.md` about what gates in place of an un-run suite must stay consistent with that row's lax-skips / strict-fails semantics.
- **security ↔ tests/CI**: the security gate is a job inside the one workflow that also carries the `a11y` job (per security-plan.md §Dependency Security, CI integration). The chunk's boundary "not a change to what CI runs" makes this a **no-change assertion** to verify, not a surface to extend.
- **security ↔ obs/tests (evidence hygiene)**: the host-path scrub and closed-set bounds on committed evidence bind both the tests harness that writes those records and the obs scrubbing rules (per security-plan.md §Input Validation, speech-log ingest row; §Security Anti-Patterns → Logging).

## Acceptance criteria contributions
- (security) Zero absolute-host-path tokens (drive-letter / `%APPDATA%` / `/Users` / `/home`) and zero internal seam-crate struct names in every file the chunk commits, including the CARRY 3 census record — grep-verified (per security-plan.md §Security Anti-Patterns → Logging).
- (security) Every gate/suite sentence the chunk writes names the handle NAME (`CONDUCTOR_A11Y_STRICT` / `CONDUCTOR_MSEDGEDRIVER` / `CONDUCTOR_NVDA`) and no resolved value, and no skip-at-exit-0 leg is described as gating (per security-plan.md §Input Validation, the three dev-harness handle rows).
- (security) The governed harness-spawn form count and the shipped-binary listener set are unchanged by this chunk — zero new crossing (per security-plan.md §Security Anti-Patterns → Code Patterns rule (b); § Universal).
- (security) Zero dependency delta verified (`Cargo.lock` / `package-lock.json` un-drifted); should any delta land, `cargo audit` + `cargo deny check advisories bans licenses sources` green and `npm audit --omit=dev` = 0 (per security-plan.md §Security Anti-Patterns → Universal; §Dependency Security).

## Relevant amendment history
- **2026-09-17-a11y-routine-arm-terminal-on-the-measured-configuration** (the immediate predecessor) — third dependency class swapped to the version-pinned msedgedriver; spawn rule (b)'s sixth form retired with its step (ordinal not reused) and its network-arriving-PROGRAM property transferred to the driver-stack spawn; the seventh form's SCOPE corrected to the job's driver-alone diagnostic steps. Why it matters here: the a11y job's spawn and egress registry was just re-dispositioned by operator ratification, so any sentence this chunk writes about what the CI a11y leg does must not re-open it.
- **2026-09-16 — a11y-ci-gate-at-an-honest-terminal** — spawn rule (b) SIX → SEVEN, registering the `runas /trustlevel:0x20000` limited-token launch; the operator ruled the chunk's own acceptance criterion had been **over-strict** relative to the master it cited. Why: a precedent that a chunk's restatement of a security mandate can be stricter than the mandate and is corrected downward, not upward.
- **2026-09-07-a11y-ci-gate** — `CONDUCTOR_A11Y_STRICT` registered as a FOURTH handle class after escalation (no playbook rule governed; `:115` and `:121` both failed their clauses). Why: this is the row that governs "what gates in place of a skip", i.e. the exact subject of this chunk's carve-out.
- **2026-09-07-sr-findings-fixed** — the CI arm's PRODUCER of `CONDUCTOR_MSEDGEDRIVER` validates upstream; three of four proposed amendments were REJECTED because they would have written a second reader into places the code has none. Why: a direct precedent against asserting a reader/owner relationship a chunk has not measured — the failure mode an ownership-attribution chunk is most exposed to.
- **2026-09-02-screen-reader-manual-spec** — registered `CONDUCTOR_NVDA`, the speech-log ingest boundary (`heard` bounded, closed enums, host-path scrub → `&lt;host-path&gt;` + `security_finding`) after a real absolute host path arrived through the log at row S1-01. Why: if the ownership table attributes claims to the `sr*` suites, this is the boundary and the hygiene shape those records carry.
- **2026-09-01-desktop-a11y-sweep** — five sites re-scoped from the `--e2e` leg alone to the dev-only driver stack fired by EITHER a11y arm; no control changed, only stale enumerations. Why: the same routine-vs-driven arm distinction this chunk's ownership statement must get right, and a worked example of a scope label that named one leg reading as not covering the other.
