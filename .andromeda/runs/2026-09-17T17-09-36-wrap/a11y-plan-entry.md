
## 2026-09-17-keyboard-and-focus-order-coverage-ownership — §5 keyboard/focus claims given owners; §11 carve-out gains its substitute gate; §1 dittography repaired

**Section:** §5 Keyboard Navigation (focus order per layout · focus trap · focus restoration · per-surface shortcuts) · §11 Strategy (the CARVE-OUT) · §1 CI integration

**Change:** Five amendments, all landed.
(1) §5's five unattributed bullets now carry an owner or a recorded gap. `:355` run-console-HOLD focus order, `:362` the focus trap and `:366` focus restoration name the operator-only DRIVEN arm (`npm run a11y:driven` → `operator-hold.e2e.ts`) as their single owner and point at §11 *Strategy* for what gates in its place. `:354` run-console-live row navigation, `:356` idle-with-report row navigation and `:370` the first-class shortcuts state that **no suite asserts them today**, recorded as gaps at this marker with the route entry *Unasserted keyboard and focus-visible claims closed* named as owner. `:353` already carried its attribution and was not touched.
(2) §5 `:366` no longer attributes restoration to "(Radix AlertDialog default)" — it now states the explicit `onCloseAutoFocus` + `restoreFocusTo` contract with the invoker kept focusable via `aria-disabled`, matching `:362`, §1 `:131`, §3 `:253` and the measurement of 2026-09-01.
(3) §5 `:355` and `:362` now include the operator-checklist `checkbox` rows in the Tab/Shift+Tab cycle, matching §1 `:131` and §3 `:253`, which already did.
(4) §11 *Strategy* `:516` gains the substitute-gate sentence v3-03's second clause requires: the hold-dependent half's owner is named, CI's structural inability to run it is stated with its reason, and the three things CI gates in its place are enumerated — the expected-skip SET held at two under `CONDUCTOR_A11Y_STRICT`, the committed claim→owner enumeration, and its checker — each stated as NOT a substitute for the assertions themselves.
(5) §1 `:115`'s verbatim dittography is repaired: the clause "the one webview-automation stack running on the measured platform SET — " stood twice inside one line, at offsets 2555 and 2627 of a 3691-character line; the first occurrence was removed by offset, leaving the line at 3619 characters with the clause occurring exactly once. Coordinates were re-measured at this wrap before the edit, per the CARRY's own instruction.

**Why:** the chunk measured, against the shipped suites, which arm asserts each §5 claim — and found three that no suite asserts at all. §9 `:465` and §11 already stated the per-arm ownership; §5 did not, so the master disagreed with itself. (4) is what `v3-03`'s acceptance asks the requirement to state and what the carve-out, which already narrowed correctly, still lacked. (2) and (3) are §5 sites left behind by the 2026-09-01 and 2026-08-22 amendments — the only survivors of readings every other site had already retired. (5) rode a route CARRY since the 2026-09-12 wrap for want of a routine channel; the playbook rule minted at this wrap now gives that class one.

**Sweep (run after every body of this pass landed, before this entry):** patterns keyed on what each claim SAYS, over all seven masters, the three preserve-verbatim curation homes, the two judgment bases and the docs leaves. `Radix AlertDialog default` → **0 hits** (retired; a synthetic-positive control confirms the probe can fire). `(across|within across) Proceed/Abort` → **0 hits** (retired). The dittography clause → **1 hit**, a11y-plan `:115`, which is the correct post-repair count. `hold-dependent half` → 3, all inside the amended `:516`. `a11y:driven` → 26 hits across masters, curation homes and leaves, every one NAMING the arm rather than restating a retired claim — no change, and §5 now agrees with them. No hit in `playbook.md` or `drift-base.md`; no hit requiring a P3 curation routing.
