
## 2026-09-17-keyboard-and-focus-order-coverage-ownership — §4's ui/ proving mechanism named as the SET of executing legs

**Section:** §4 Unit Test Strategy — the `conductor-tauri/ui (frontend SPA)` bullet

**Change:** the bullet closed the proving set at the wdio legs — "proven only by EXECUTING the wdio leg that LOADS them", enumerating `--e2e`, `npm run a11y:driven` and the three `a11y:sr*` legs, with anything reachable from none of them "as unproven as it was under the build gate". The chunk shipped a fourth executing leg, so the bullet now names the proving mechanism as the SET of npm legs that EXECUTE a TS member: the four above plus the driver-free, Pulse-free static checker `npm run a11y:ownership` (`tsx test/a11y/check-claim-ownership.ts`), asserted on its printed last line beside its exit. Two qualifiers land with it — the leg runs no test framework (`tsx` is a TypeScript executor, not a runner), so §12's decision that no JS/TS unit runner is adopted stands unchanged; and it is operator-local like the §4 mutation gate, invoked by no CI step and adding no sixth `agent-run.{sh,ps1}` command. The sentence's head clause was corrected in the same pass from "EXECUTING the wdio leg" to "EXECUTING a leg", which the sweep below caught.

**Why:** the chunk committed a gate whose runner the plan named nowhere, into the one surface §4 declares build-gated-plus-wdio-legs. Leaving the set closed would have made a shipped, re-runnable gate read as an unproven member by the doc's own rule. The fix names the SET rather than re-closing the list, per the de-literalization discipline, so the next executing leg does not re-stale it.

**Sweep (run after every body of this pass landed, before this entry):** `proven only by EXECUTING the wdio leg` → **1 hit on the first pass, at `:247` — inside the very line this amendment had just edited.** The retired mechanism survived as the sentence's head clause while the addition contradicted it 400 characters later; the head was corrected and the re-run returns **0 hits**. `` `wdio.conf.ts` member reachable from NONE `` → 0 hits (retired with it). `JS/TS unit[-test] runner` → 3 hits, `:87`, `:247` and `:603` — the INVARIANT this amendment must not disturb, re-read at each site and left standing, since `tsx` carries no test framework and §12's subject is a component/unit runner such as vitest. `a11y:driven` → 8 hits in this master (`:124`, `:155`, `:247`, `:307`, `:465`×2, `:568`, `:582`), every one naming the arm rather than restating the closed set — no change. `:307`, the file's longest line at 10 647 characters, was resolved BY OFFSET rather than from a grep view: `proven only by` → 0, `EXECUTING` → 0, `unit runner` → 0, so it names the arms and their subjects without making a proving-set claim. No hit in `playbook.md` or `drift-base.md`, and none in a preserve-verbatim curation home.
