```yaml
proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources — On-disk artifacts / database"
    change: "Add a registry row after the `contracts/pulse-p025-measurement-contract.md` bullet: `contracts/pulse-real-model-leg-posture.md` — the real-model L4 live-leg posture, grading rule and per-leg quiet window, fixed and committed before any drive; the SECOND reader-less `contracts/` member (no Rust code reads it, so no `default_path()`, no `resolve_under` resolution, no bounds check and deliberately no `CONDUCTOR_*` override handle — the P-025 regime, now n=2 rather than n=1); carries `sut_version` · `captured_at` · `pinned_at` · `provenance` on its face with provenance stated PER CLAUSE (MIXED — which clauses are transcribed SUT records and which is a carried Conductor measurement); it NAMES only already-registered handles (`ANDROMEDA_PULSE_L4_DETERMINISTIC`, `ANDROMEDA_PULSE_MCP_ENABLED`, `ANDROMEDA_PULSE_DATA_DIR`, `ANDROMEDA_PULSE_BASELINE_BOOTSTRAP_SECONDS`) and mints none, and it registers no `agent-run` verb, flag or arm — it describes a future leg's firing form without adding a member to the sanctioned live-leg set."
    sidecar: "arch §Occupied Resources — registered `contracts/pulse-real-model-leg-posture.md` as the second reader-less `contracts/` member (2026-09-18)."
    rationale: "Report Changes → Files lists `contracts/pulse-real-model-leg-posture.md` as **new** (181 lines), and Symbols/APIs records it as a **reader-less** `contracts/` member with zero `.rs` delta, no `default_path()`, no `resolve_under`, no bounds check and no `CONDUCTOR_*` handle. Arch's §Occupied Resources registers every other `contracts/` member individually (architecture.md:175-181); an unregistered on-disk artifact is drift. The report's own Expected-amendments bullet names this section and cites the single P-025 precedent row at architecture.md:181 as the insertion point."
    basis: "conductor-0.3.0/chunks/2026-09-18-real-model-leg-posture-and-grading-rule/report.md:12 (Files) + :19-21 (Symbols/APIs) + :76-82 (Expected amendments); insertion point .andromeda/architecture.md:181"

  - detector: D-arch-resources
    severity: warning
    section: "Infrastructure Patterns — Directory structure (crate-per-seam Cargo workspace)"
    change: "In the tree's `contracts/` comment, extend the member enumeration with the real-model leg posture document and retire the uniqueness claim: `… + the scenario-assertion audit ledger + the P-025 measurement contract + the real-model leg posture (the two members no Rust code reads)`."
    sidecar: "arch §Infrastructure Patterns — directory tree's `contracts/` line now lists the leg-posture document; 'the one member no Rust code reads' → 'the two members' (2026-09-18)."
    rationale: "The same registry claim is restated in the directory tree, which enumerates every `contracts/` member and parenthesizes `(the one member no Rust code reads)` — a uniqueness claim the report's new reader-less member falsifies, and an enumeration the new member is missing from. Left unamended, the primary's single-site apply leaves a contradicting count in arch."
    basis: ".andromeda/architecture.md:238"
    dependent-of: D-arch-resources
```

---

**Orchestrator disposition (both APPLIED after operator ratification):**

- Playbook: **no rule governed.** The two nearest (`playbook.md:100`, `:191`) both carry a "runtime-parsed /
  read at a fixed `default_path()` through `resolve_under`" qualifier that a reader-less member fails, and a
  rule governs only when every qualifying clause holds. Escalated; the operator approved applying both and
  minting the rule (playbook 48 → 49).
- Basis coordinates re-derived by the orchestrator rather than trusted: `grep -c
  'pulse-p025-measurement-contract' .andromeda/architecture.md` → 1 (at `:181`, not the 2 the report first
  asserted — corrected pre-fan-out); `grep -c 'pulse-real-model-leg-posture' .andromeda/architecture.md` → 0,
  confirming the drift.
- The dependent's site `:238` was NOT found by the orchestrator's own verification sweep, whose trailing
  `.{60}` context anchor suppressed a match ending the line; the detector's sweep found it. Recorded as the
  reason the per-occurrence sweep instruction earns its keep.
