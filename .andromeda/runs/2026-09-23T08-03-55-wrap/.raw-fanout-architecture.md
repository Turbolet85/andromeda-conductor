# architecture doc-agent return — 19 proposals. HEAVILY CONDENSED (context ceiling at 91 %): section + change
# essence; rationales cite report.md. D-platform-claim: no hit.

D-arch-resources (warning):
A1  §Standard Contracts — Readiness gate (FIVE named preconditions): register posture-awareness — `l4_posture`
    selects what `preflight_for(manifest, posture)` grades (`evaluate_for`: tagged terms only under their posture,
    untagged under all); `preflight()` delegates Deterministic; `conductor run` passes the scenario's posture,
    suite / GUI `run_thread` / `drive.rs` keep `preflight()`; mismatch → scenario-level Blocked BEFORE the ready check
    (never a sixth precondition — still FIVE); `readiness()` (boot) stays deterministic.
A2  §Standard Contracts — Liveness equivalent (probe's three subjects): register `preconditions --for <SCENARIO>`
    → `observe_preconditions_for(posture)` via `handle_declared_for` (L4 under RealModel must be ABSENT under
    `flag_declared_on_either_side`); "every other name keeps the true/1 gate" now holds for the deterministic posture.
A3  §Standard Contracts — Run report envelope (:135): "2 of 36" → "2 of 37" on the escaped `'^\[\[expected\]\]'`
    basis (as measured 2026-09-23); record `real-model-interpretation` as declare-only from birth, NOT a tenth family,
    success row ManualCheck.
A4  §Occupied Resources — pulse-run-contract.toml entry (:178) term shape + kinds: `{…, posture?, …}`; add
    `shell-absence` (unmet iff declared, union rule); retire "shell-declaration … the only kind that can block" —
    TWO kinds can block, both need `env`; `evaluate` delegates Deterministic.
A5  same entry, term count: SIX → SEVEN as of 2026-09-22 (`l4-real-model` a Conductor posture term, header PROVENANCE
    line; `l4-deterministic` tagged); observed handle set stays TWO (`[L4, MCP]`).
A6  §Occupied Resources — `runs/live-suite/{leg}.jsonl` entry (:171): register `rm.jsonl`, `rm-capture.txt`,
    `rm-capture.err` and the arm's own non-recursive clear of exactly those three; suite glob unchanged.
A7  §Occupied Resources — posture-doc entry (:182) firing-form clause: retire "describes a future leg's firing form …
    no member added" — the leg ships as `agent-run run --live real-model` (a `--live` selector, not a sixth command),
    driven once 2026-09-23. (Not in the plan's list — found by reading the entry.)
A8  same entry contents clause: "the emission profile a hypothesis observable requires" → the profile, its
    single-storm premise corrected in place 2026-09-22 (creation-time attach, inference_runtime.rs:884 @ b2e4cb3).
A9  same entry provenance clause: ~110 s → a DETERMINISTIC-L4 measurement (lifecycle_live.rs:20), mis-carried since
    2026-09-06 plan.md:102; no real-model formation or pickup figure exists — the 2026-09-23 drive measured none;
    the doc carries 11 dated corrections + 1 measurement record.
A10 §Occupied Resources env — `ANDROMEDA_PULSE_L4_DETERMINISTIC` (:198) "ONE OF TWO shell-declaration proxies":
    read (never written) under BOTH postures — `l4-deterministic` (declaration) / `l4-real-model` (absence, union
    rule; the `--for` probe's statement); observed handle set stays TWO.
A11 §Occupied Resources env — NEW entry `RUST_LOG`: not a Conductor handle; registered on the EDGEWEBDRIVER /
    WEBVIEW2_* basis because shipped `agent-run` arms now clear it (and the .ps1 saves/restores it) around the
    real-model arm's test invocations. (Not in the plan's list.)
A12 (dep) §Occupied Resources env — `ANDROMEDA_PULSE_MCP_ENABLED` entry: the L4 handle's true/1 grading qualified to
    the deterministic posture.
A13 (dep) §Infrastructure Patterns directory tree, the `runs/live-suite/` line (:241): name the real-model arm's
    `rm-capture.{txt,err}`, each cleared per invocation of its own arm.
A14 (dep) [Read-Back Dependency Posture] — "Verifying the real thing … needs a non-deterministic live leg": the leg
    now ships and was driven once (NoAttributableIncident, model-side Blocked); v3-09 deferred at P7.3.
A15 (dep) §Design Philosophy — "both … call identically": qualify — only `conductor run` passes `l4_posture` to
    `preflight_for`; suite and GUI keep `preflight()`, so a real-model scenario lands Blocked there.

D-arch-decisions:
A16 (warning) [Probabilistic-Assertion Policy]: add the pre-committed-rule arm — a model-interpretive claim graded
    against a rule committed BEFORE the drive may Pass on a match (`Identified → (Pass, Pass)`), a miss is
    `CalibrationRegion → ManualCheck`, never Fail; ships at test tier in the harvest's `row()`, `classify()`
    unchanged, no new variant. (= plan D3 expected amendment.)
A17 (ESCALATE) [Read-Back Dependency Posture] "Freshness is the carrier because NO read-back field varies with the
    emitted payload": retire the universal — at 83d4060 `grounded_fingerprint_hashes` puts the triggering cue's full-hex
    fingerprint into `fingerprint_refs` (retrieve_telemetry_slice); freshness stays the canary's carrier by choice;
    the capture attributes by fingerprint AND freshness. Code reading only (not live-measured) — operator chooses:
    amend now, or route a live `fingerprint_refs` measurement.
A18 (dep of A17, escalate) same section — "Payload fidelity is unattainable; runtime-STATE fidelity is not": qualify.
A19 (dep of A17, escalate) §Standard Contracts readiness gate — "The carrier is freshness, not payload identity … no
    read-back field varies … `fingerprint_refs` carries the model's evidence_refs (a constant det-* triple)": qualify.
    (Related: obs-plan :131/:314/:315 fingerprint-storm stale claim flagged out-of-detector by the obs agent.)
