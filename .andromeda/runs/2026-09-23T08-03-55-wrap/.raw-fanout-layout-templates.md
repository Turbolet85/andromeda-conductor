proposals:
  - detector: D-layout-surface
    severity: warning
    section: 'Surface: cli › Primary screens (commands) — the `scripts/agent-run.{sh,ps1}` bullet (the `--live` sentence)'
    change: 'State that `--live` takes an optional THIRD-token selector. Bare `--live` stays the operator-gated deterministic live-Pulse suite. `--live real-model` runs the ONE operator-gated real-model leg (`real-model-interpretation`) instead of the suite: it starts with `conductor preconditions --for real-model-interpretation` (an unmet subject refuses at exit 1 and no leg fires) and is reachable from no default or CI path. An unknown selector prints `usage: <script> run --live [real-model]` and exits 2 before any probe, in both shells. The selector is neither a sixth command nor a fifth stage flag.'
    sidecar: 'layout-templates §cli Primary screens, agent-run bullet: documented the `run --live [real-model]` selector (the real-model leg starts with `preconditions --for` and refuses at exit 1; an unknown selector exits 2 in both shells). Chunk 2026-09-22-interpretation-proven-live.'
    rationale: 'Report Harness / gate surface bullet (report.md:38); gates report.md:128-129; layouts acceptance report.md:107. layout-templates.md:190 calls `--live` only "the operator-gated live-Pulse suite" and names no selector. The plan''s expected amendments name this bullet (report.md:62).'
    basis: 'scripts/agent-run.sh:337-338'
  - detector: D-layout-surface
    severity: warning
    section: 'Surface: cli › Primary screens (commands) — the `conductor preconditions` bullet (synopsis)'
    change: 'Make the synopsis `conductor preconditions [--for <SCENARIO>] [--json]` and add: `--for` takes a scenario NAME; a `P-NNN`-shaped value is refused at argument parsing (clap usage error, exit 2). The probe is then graded under that scenario''s declared `l4_posture`. Under `real-model` the L4 handle must be absent or falsy, and its unmet line reads `declared in this environment, but the real-model posture requires it absent or falsy`. Without the flag the probe is unchanged. Under `--for` the probe still exits 0 only when every subject is satisfied, and both `[PRECONDITION]` arms work as already stated.'
    sidecar: 'layout-templates §cli Primary screens, preconditions bullet: documented `--for <SCENARIO>` (scenario name only, a P-ID is refused at exit 2; the real-model posture requires the L4 handle absent). Chunk 2026-09-22-interpretation-proven-live.'
    rationale: 'Report Symbols / APIs (report.md:24, :21); measured green (report.md:130); layouts acceptance (report.md:107). layout-templates.md:187 documents only `conductor preconditions [--json]`. The plan''s expected amendments name this bullet (report.md:62).'
    basis: 'crates/conductor-cli/src/cli.rs:62'
  - detector: D-layout-surface
    severity: warning
    section: 'Surface: cli › Primary screens (commands) — the `conductor preconditions` bullet (the subject-list clause "the three `ANDROMEDA_PULSE_*` handles read for an affirmative declaration")'
    change: 'Make the handle clause depend on posture: under the deterministic posture (no `--for`, or a `--for` scenario that declares deterministic) the three `ANDROMEDA_PULSE_*` handles are read for an affirmative declaration; under `--for` a `real-model` scenario the L4 handle is read for ABSENCE instead.'
    sidecar: 'layout-templates §cli preconditions bullet: the handle subject list now applies to the deterministic posture only; the real-model posture reads the L4 handle for absence. Chunk 2026-09-22-interpretation-proven-live.'
    rationale: 'Depends on the `--for` primary; under `RealModel`, `Preconditions::evaluate_for` requires the L4 handle NOT declared (report.md:21); without the flag `evaluate` delegates Deterministic with byte-identical statements.'
    basis: 'crates/conductor-core/src/preconditions.rs:229-230'
    dependent-of: D-layout-surface
  - detector: D-layout-surface
    severity: warning
    section: 'Surface: cli › Primary screens (commands) — the `conductor preconditions` bullet (the "each handle is graded by its own KIND" sentence)'
    change: 'Mark "the flag-valued members by an affirmative `"true"`/`"1"`" as the deterministic-posture grading, and add the real-model arm: under `--for` a `real-model` scenario the L4 flag is UNMET when either side''s truthiness rule declares it (Conductor''s `true`/`1` plus Pulse''s `1`/`true`/`yes`, trimmed and ASCII-lowercased). Every other handle is graded as before, and exit 0 is still reachable (measured 2026-09-23).'
    sidecar: 'layout-templates §cli preconditions bullet: the per-KIND grading sentence now applies to the deterministic posture, with the real-model L4 absence rule added. Chunk 2026-09-22-interpretation-proven-live.'
    rationale: 'Depends on the `--for` primary; `handle_declared_for` differs from `handle_declared` only for the L4 handle under `RealModel`, graded by `flag_declared_on_either_side` (report.md:21); Deviation 1 (report.md:72); real-model probe exit 0 under the live env (report.md:130).'
    basis: 'crates/conductor-core/src/preconditions.rs:83-93'
    dependent-of: D-layout-surface
# D-layout-derived-count: no hit (no OLD moved-count value in any caption/sample/wireframe/label).
# D-platform-claim: no hit (no entry retires a platform/runner/driver verdict; both shells stay at identical semantics).
# Transcribed from the doc-agent's return; the `basis` fields cite source lines the REPORT does not carry —
# per amendment-flow Validate that is the re-derivation tell, so the orchestrator re-derives the applied text
# from the report's facts, never from these lines.
