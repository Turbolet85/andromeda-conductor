proposals:
  - detector: D-obs-instrumentation
    severity: warning
    section: "§4 Span / Trace Coverage → Scenario: Headless deterministic scenario run with MCP read-back verification"
    change: "Add a posture note: the declare-only `real-model-interpretation` scenario (`l4_posture = \"real-model\"`) adds NO critical path, span name or span attribute — it rides this chain (`scenario.run` → `timeline.execute` → `emit.batch` → `verify.readback` incl. `retrieve_report` → `report.generate` → `db.insert_run`) with the standard eleven-field envelope (`verdict` null; a non-degraded read-back lands `ManualCheck`, not KnownResidual), and its interpretation is graded at the harvest tier (`conductor-run/tests/real_model_harvest.rs`, over the scrubbed `real_model_live` capture), never through the envelope. Inside `scenario.run` and BEFORE the ready check, `execute_scenario` returns a scenario-level `Blocked` when the scenario's declared posture differs from the posture the run's gate evaluated (a real-model scenario reached from the suite or the GUI, which keep the deterministic `preflight`), recorded by one `info` line on the allowlisted `message` field (`scenario blocked: declared L4 posture {a} is not the posture this run's gate evaluated ({b})`) — never a sixth gate precondition. The 2026-09-23 drive was `PreflightBlocked`, so the chain was not observed live."
    sidecar: "§4 CP1 block: recorded that the declare-only real-model-interpretation scenario rides CP1's chain (no new span/attribute; non-degraded → ManualCheck; graded at the harvest tier) and execute_scenario's posture-mismatch Blocked arm with its message-borne info line (2026-09-22-interpretation-proven-live)."
    rationale: "Report Symbols/APIs (the mismatch Blocked + its message-borne line; suite/GUI keep preflight); Schema/config (no envelope key/verdict/state/span/attribute added); Spec claims disproved #4 (ManualCheck); Coverage (message-borne info line); Expected amendment obs-plan §4; Outcome (obs) PreflightBlocked. obs-plan has 0 hits for posture/real-model."
    basis: "crates/conductor-run/src/execute.rs:34-58; scenarios/real-model-interpretation.toml:42"
# Out-of-detector notes (NOT proposals):
# - obs-plan.md:131 (§1 Fingerprint-storm row) and §4 Fingerprint-storm (:314, :315) still state, measured at efabe8e,
#   that Pulse exposes no read-back surface carrying its computed fingerprint and that `fingerprints` are payload-invariant
#   det-* constants; the report's Cross-project claims @83d4060 say fingerprint_refs = model refs ∪ the cue's full-hex
#   fingerprint (retrieve_telemetry_slice), which the capture attributes by.
# - §4 CP7 parity (:368) does not name the L4 posture it covers (GUI keeps the deterministic preflight).
# - Expected amendment §5/§10 pickup: no figure measured; obs-plan has 0 hits for formation/pickup/110 — nothing to record.
# - D-obs-stack / D-obs-redaction / D-platform-claim: no hit.
# Transcribed from the doc-agent's return; `basis` cites source lines the report does not carry — re-derive the applied
# text from the report's facts.
