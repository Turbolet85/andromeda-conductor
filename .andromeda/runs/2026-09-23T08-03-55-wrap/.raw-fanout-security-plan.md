# security-plan doc-agent return — 11 proposals, all D-security-input (detector severity escalate).
# CONDENSED transcription (context ceiling): every proposal's section, change and dependency kept in full
# substance; rationales abbreviated to their report citations. `basis` fields cite source lines the report does
# not carry — re-derive applied text from the report's facts (amendment-flow Validate).

S1 (primary) — §Input Validation, row "the ANDROMEDA_PULSE_* declaration-only READ SET" (security-plan.md:114)
  change: posture-aware How column — `observe_preconditions_for(posture)` grades via `handle_declared_for`
  (= `handle_declared` except the L4 handle under RealModel, which must be ABSENT or falsy under the union rule
  `flag_declared_on_either_side`: Conductor true/1 ∪ Pulse 1/true/yes, trimmed + lowercased, transcribed from
  `deterministic_mode_enabled_for`); `declares(name, absence)` routes shell-declaration terms to `flag_declared`
  and the shell-absence term `l4-real-model` to the union — both value-only, neither reaches the presence arm; the
  union only widens what BLOCKS. "An absent declaration surfaces as unmet" now holds under Deterministic (default,
  every pre-existing scenario); under RealModel a PRESENT L4 declaration is the unmet subject, named by handle only.
  retires: the row's "declares() … retained for the shell-declaration terms alone" / "routes only to flag_declared" /
  "An absent declaration of either boolean surfaces as an unmet contract term". Report: Symbols/APIs.
S2 (dependent-of S1) — §Security Anti-Patterns → Universal, the INVOKED-preflight / probe short-circuit bullet (:378)
  change: scope "(the two flag handles keeping the truthy gate via the value-only `flag_declared`)" to the
  deterministic posture; add that `run --live real-model` leads with `conductor preconditions --for
  real-model-interpretation`, whose `handle_declared_for` requires the L4 handle ABSENT or falsy (union), refusing
  at exit 1 naming each unmet subject before any leg. Report: Harness (a).
S3 (primary) — §Input Validation, row "Committed SUT-facing manifests read at a fixed path" (:113), run-contract clause
  change: "every `shell-declaration` term naming its env var" → every SHELL term names its env var —
  `shell-declaration` and (since 2026-09-22) `shell-absence` (`validate()` keys on `CheckKind::is_shell()`); a term
  may carry an optional `posture` from the closed `L4Posture` set; absent = every posture; unknown = hard parse fault.
  Report: Symbols, Schema/config, Counts (CheckKind 3→4, terms 6→7).
S4 (primary) — §Input Validation, row "Scenario config files (scenarios/, serde deserialize)"
  change: record the optional top-level `l4_posture` key — CLOSED `L4Posture` unit enum, `#[serde(default)]` →
  deterministic, unknown → load-time `CoreError::Config`; its `#[garde(skip)]` is the closed-unit-enum form (the
  parse IS the validation, as for `seed` / `slo_tier` / `FaultSpec.kind`), not the nested-spec skip the dive rule bans.
S5 (primary) — §Input Validation, row "CLI arguments / stdin"
  change: add `conductor preconditions --for <SCENARIO>` (scenario NAME via the validated scenario load; a
  `P-NNN`-shaped value refused by the clap value_parser, exit 2, because a P-ID no longer names one scenario;
  without the flag the probe is unchanged) and the harness selector `run --live [real-model]` (a `case` allowlist;
  `.ps1` binds `$args[2]` by count; unknown → usage + exit 2 before any probe; never reaches an argv or a path).
  Flagged, not proposed: `conductor run P-018` / `SCENARIO=P-018` resolve in unsorted read_dir order (Decisions).
S6 (primary) — §Input Validation, NEW row after "Screen-reader speech-log ingest": real-model capture ingest
  change: `tests/real_model_live.rs` (`live-pulse`, only via `run --live real-model`, never CI) → committed
  `evidence/rm-capture.txt`; ingests untrusted SUT output from (1) MCP re-read over the hardened
  `ReadbackClient::connect` (600 s poll / 10 s; id sweep ≤64, stop after 3 misses) and (2) Pulse's newest
  `agent-latest.jsonl.*` under `ANDROMEDA_PULSE_DATA_DIR`/`logs` — a TEST-binary reader turning that handle's VALUE
  into a path (no canonicalize, whole-file read; a missing log prints the handle NAME only); every line
  `redact_value` → `mask_host_paths`, fields-only, basename → boolean, envelope `fingerprints` elided to counts,
  written once. Reader `real_model_harvest` loads the committed capture from `CARGO_MANIFEST_DIR` + a hard-coded
  path (no CONDUCTOR_* handle) and asserts the pinned literal EQUAL to its section. Host-path gates prove it.
S7 (dependent-of S6) — §Input Validation, row "ANDROMEDA_PULSE_DATA_DIR — the SPAWN-propagation use" (:121)
  change: its reader list gains the test-binary VALUE reader (the capture's Pulse-log join, recorded at S6's row);
  it neither relaxes nor satisfies the spawn duty; the capture's own sidecar spawn goes through the hardened
  `ReadbackClient::connect` → `resolve_data_dir` + `build_command`.
S8 (primary) — §Input Validation, row "Env-var path handles (CONDUCTOR_RUNS_DIR / …)" (:115)
  change: per-READER clause — `CONDUCTOR_RUNS_DIR` gains a TEST-binary reader, `runs_dir()` in
  `tests/real_model_live.rs`, which joins the handle under the workspace root with no `resolve_under` /
  canonicalize (an absolute value replaces the root rather than being rejected) — the same recorded residual as
  `journal_conformance.rs`, bounded by a failure text naming the file NAME + error kind, never a resolved path.
  Noted, not proposed (predates the chunk): `tests/live_suite.rs:60` is an earlier unrecorded reader of the same shape.
S9 (primary) — §Security Anti-Patterns → Data Protection (the corpus.db bullet, :330-333) — REQUIRES RATIFICATION
  change: record the operator's P4 decision — corpus-rendered model text re-read over MCP (the attributed incident's
  `retrieve_report` sections) may enter the committed per-chunk `evidence/` tree ONLY as ratified, and even then
  only through `redact_value` + `mask_host_paths` with envelope fingerprints elided — an explicit, scoped exception
  to "never persist its content into Conductor artifacts"; until ratified the ban binds the capture. The
  2026-09-23 capture carries none (no report was read).
S10 (dependent-of S9) — §Data Protection paragraph "Encryption at rest is out of scope" (:154-157)
  change: ONLY if S9 is ratified — after "never persists or exfiltrates its content" add "except the ratified,
  scrubbed real-model capture evidence (§Security Anti-Patterns → Data Protection)"; refused → no edit.
S11 (dependent-of S9) — Threat Model Summary → Data classification, the corpus.db Note (:46)
  change: ONLY if S9 is ratified — the same scoped exception; refused → no edit.

# D-security-subprocess: no hit (capture's sidecar via the hardened connect; data-dir join is a file read, not argv/shell;
#   protocol pin untouched). D-security-deps: no hit. D-platform-claim: no hit.
