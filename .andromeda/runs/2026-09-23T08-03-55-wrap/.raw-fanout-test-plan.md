# test-plan doc-agent return — 14 proposals. CONDENSED transcription (context ceiling); each proposal's section
# and change kept in substance; rationales cite the report (report.md line refs in the agent's return).

GROUP A — D-tests-obs-harness (warning)
T1 (primary) §3 `run` CI stage selectors bullet, the `--live` clause (:155): retire "`--live` composes the live-Pulse
   suite in one shape" by naming the selector SET — bare unchanged; `run --live real-model` = preconditions --for
   (scenario name; P-ID exit 2; union truthy rule for L4 absence; unmet → exit 1, no leg; `boot` keeps the flagless
   probe) → capture pre-build → non-recursive removal of runs/live-suite/{rm.jsonl,rm-capture.txt,rm-capture.err} →
   `rule_record` before the leg (non-zero stops the arm) → `live_leg rm real-model-interpretation` → the
   `real_model_live` capture (stdout appended, stderr to rm-capture.err, RUST_LOG removed) → reminders; unknown
   selector → usage + exit 2 before any probe, both shells (.sh arm driven live 2026-09-23; .ps1 measured at the
   refusal only). No port/env/CONDUCTOR_* handle; envelope/status/JSONL untouched → §3 ↔ obs-plan §3 still agree.
T2 (dep of T1) §9 Live-Pulse scenarios paragraph (:465) "composed `run --live` stage … ONE invocation": bare drives
   the feature-gated targets + `a11y:driven`; `real-model` drives only `real_model_live` + its `rule_record` around one
   leg, never `a11y:driven`; fired once, never re-driven (only a Conductor-side environment fault may be re-fired);
   pickup recorded as a measurement, never budgeted; real-model formation AND pickup both remain unmeasured (the
   2026-09-23 drive: NoAttributableIncident, model-side Blocked); neither shape is ever a CI gate.
T3 (dep of T1) §2 Deterministic bullet (:124) "The composed `agent-run run --live` suite … joins them": name both
   shapes among the real-wall-clock exceptions — bare (per-leg budgets, fixed 150 s quiet window) and `real-model`
   (one leg against a real-model Pulse whose interpretation is non-deterministic + a bounded capture poll; fired
   once; graded against a rule stated before the drive) — both only §9/§11 operator-local exceptions, never CI.

GROUP B — D-tests-derived-count (warning): "one-per-P-ID" scenario fixtures retired — P-018 is named by 2 scenarios
T4 (primary) §7 Seed strategies table, "Fixture files (scenario config)" row (:394): "each file P-ID-keyed: it names
   the P-IDs it covers, and a P-ID may be named by more than one file (not one-per-P-ID)".
T5 (dep) §1 Coverage scope "Scenario catalog" entity (:39) — P-ID-keyed, a P-ID may be named by more than one.
T6 (dep) §1 Test data strategy (:71) — same qualifier.
T7 (dep) §2 Directory pattern (:128) — same qualifier.
T8 (dep) §3 Test data bootstrap mechanism (:201) — same qualifier.
T9 (dep) §3 Bootstrap phases test-data-bootstrap-wire (:214) — same qualifier.
T10 (dep) §3 `run` command body (:151) `run <scenario|P-ID>` / `SCENARIO=<name|P-ID>`: a P-ID selector is determinate
   only where a single scenario names it; where several do (P-018 at 2026-09-23) it resolves in unsorted `read_dir`
   order — name the scenario (`preconditions --for` already refuses P-IDs).
T11 (dep) §3 Test selection (:154) "`conductor run <P-ID> --seed <s>` for one scenario" → select by scenario NAME.

GROUP C — D-tests-coverage (ESCALATE)
T12 (primary) §1 Critical paths, "Both-surface parity" Path 7 (:81): scope to the deterministic-posture catalog — a
   `real-model` scenario runs only via headless `conductor run` (`preflight_for`); the in-process `{preflight,
   drive_run}` (start_run), Tauri `run_thread` and `conductor suite` keep the deterministic gate and land it
   scenario-level Blocked by design; its envelope follows the model, not the seed; record it against the Creator
   Brief Must-Work (narrowing a Must-Work is the operator's call).
T13 (dep of T12) §1 Coverage triggers, cross-surface-coordination trigger (:94) — parity assertion scoped to
   deterministic-posture scenarios.
T14 (dep of T12) §6 E2E Both-surface parity verification signal (:374) — identical envelope for the same
   deterministic-posture scenario+seed; a real-model scenario excluded by design.
