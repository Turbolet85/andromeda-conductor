Interpretation proven live — NOT proven: the leg SHIPPED and was driven ONCE, and the real model blocked it at the preflight. The L4 posture became scenario data (`l4_posture`; a posture-selected run contract with a `shell-absence` term, a probe graded under the union truthy rule, a posture mismatch recorded as a scenario-level Blocked), `agent-run run --live real-model` and `conductor preconditions --for` shipped, and `real_model_harvest` froze the grading rule before the drive. MEASURED 2026-09-23 (run 2026-09-23T07-39-39-845): the real model answered the canary's one cue-bearing digest and formed no incident, so the scenario never emitted — `NoAttributableIncident → Blocked`, model-side; `v3-09` un-claimed and deferred, never re-driven; no pickup or real-model formation figure exists. The wrap also retired the "no read-back field varies with the emitted payload" universal as measured — ten 2026-09-10 envelopes carry the triggering cue's fingerprint.
