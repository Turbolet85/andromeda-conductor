
## 2026-09-22-interpretation-proven-live — the real-model selector and harvest leg, parity scoped to the deterministic posture, and three stale claims corrected

**Section:** §1 (Coverage scope catalog entity `:39` · untestable zone `:44` · test data strategy `:71` · Critical Path 2 `:76` · Critical Path 7 `:81` · the cross-surface trigger `:94`) · §2 (deterministic-tier exceptions `:124` · directory pattern `:128`) · §3 (`run` scenario invocation `:151` · test selection `:154` · CI stage selectors `:155` · bootstrap mechanism `:201` · bootstrap phase `:214`) · §6 (Fingerprint-storm verification signal `:335` + NEW sibling leg `:336` · skip note `:309` · Both-surface parity signal `:375`) · §7 fixture-files row `:395` · §9 Live-Pulse paragraph `:466`

**Change:**
**(T1–T3)** §3 `--live` now takes an optional third-token selector — bare = the suite, unchanged; `--live real-model` = `preconditions --for real-model-interpretation` → the capture pre-build → the non-recursive clear of `rm.jsonl` / `rm-capture.{txt,err}` → the rule record BEFORE the leg → the leg → the capture (`RUST_LOG` removed) → reminders; an unknown selector exits 2 in both shells. §9 names the selector (drives only `real_model_live`, never `a11y:driven`; fired once; pickup never budgeted; real-model formation AND pickup still unmeasured) and the `live-pulse` target set gains `real_model_live.rs`; §2 lists the selector among the real-wall-clock exceptions.
**(check-5 raise)** §6 — the real-model harvest leg, the plan's Expected amendment no detector proposed: a sibling leg of the Fingerprint-storm scenario (P-018), not an eighth scenario and no §9 stage row, graded at the harvest tier in `real_model_harvest.rs` against the rule stated before the drive (`Identified → (Pass, Pass)`, `NotIdentified → (CalibrationRegion, ManualCheck)`, nothing to grade → `(null, Blocked)`); the 2026-09-23 drive graded `NoAttributableIncident → Blocked`.
**(T12–T14) ESCALATED, operator "Narrow + record"** — Critical Path 7, the cross-surface trigger and the §6 parity signal scoped to DETERMINISTIC-posture scenarios; a real-model scenario is outside parity by design (only headless `conductor run` evaluates its posture), recorded against the Creator Brief Must-Work as a deliberate narrowing.
**(T4–T11) ESCALATED, operator "Correct all + route"** — the "one-per-P-ID" claim retired at six sites (a scenario names its P-IDs; a P-ID may be named by several — already false before this chunk: P-017 · P-019 · P-020 · P-021 · P-022 · P-060, and now P-018); `run <P-ID>` / `SCENARIO=<P-ID>` resolve to the first scenario in directory order (`find_by_pid`), so name the scenario for a determinate run; making `run` refuse an ambiguous P-ID is route-owned (CARRY).
**(A17 cascade) ESCALATED, operator "Amend now, as measured"** — Critical Path 2 and the §6 fingerprint-storm signal: `fingerprints` carries, at `83d4060`, each incident's triggering-cue fingerprint (varies with the emitted payload, measured 2026-09-10) beside the constant `det-*` refs; no shipped check keys on the array.
**(Pre-existing, operator "Fix now")** — `:44` / `:309` "encrypted `corpus.db` (P-049)" → plaintext (security-plan and architecture have recorded it since 2026-06-27); `:44` no longer quotes security-plan wording that plan stopped carrying.

**Why:** report §Changes (Harness, Crates/modules, Counts — "scenarios naming P-018: 1 → 2", Decisions observations) and the plan's Expected amendments (§3, §6, §9); four decisions taken with the operator 2026-09-23.

**Sweep (full ledger `.andromeda/runs/2026-09-23T08-03-55-wrap/reconcile-sweep.md`):**
- **G1 P-ID** — 15 hits: `:151` ×3 amended; `:64` (keying, not cardinality) and `:356` (`run P-032` — P-032 is named by one scenario) left; leaves `testing.md` `:32`/`:38`, `verification-harness.md` `:19`, `commands.md` re-derived; Session Additions `testing.md:59`, `verification-harness.md:49` left (neither states the claim). Companion `per-P-ID` sweep: 49 hits, all P-ID-keyed rows/lamps — left.
- **G4 parity** — `:81` ×4, `:94` ×2, `:375` amended; `:71`/`:200`/`:397` (emission determinism), `:250`, `:289` ×2 (the shipped `cross_surface_parity.rs` leg's own assertion), `:374` (steps under the scoped signal) left.
- **G5 payload** — `:76` ×2, `:335` ×2 amended; `:284`, `:323` left (other claims).
- **G2 encrypted** — 0 remaining test-plan hits. **G3** — `:63` ×2 and `:145` (`boot`, still deterministic), `:87` (another handle), `:611` ×3 (roster history) left. **G6** — `:336`, `:466` amended text only. **G9** — `:64`/`:152` (the Brief's "never hard-failed" still true), `:545`, `:569` left.
- Lateral bind test-plan §3 ↔ obs-plan §3: obs §3 has 0 `--live` hits and the envelope / `status` / JSONL shapes are unchanged — the bind holds.
- Leaves re-derived: `docs/tests-summary.md` (fingerprint-storm + the real-model sibling leg; parity scope), `.claude/rules/testing.md` and `verification-harness.md` bodies.
- Observation, not amended (outside this chunk's report): `:335` still says `retrieve_report` is "permanently `degraded_mode`" under deterministic L4, a universal architecture retired 2026-09-06 (degraded is per-read-back).
