Real-model capture path handles guarded and stale read-back texts corrected — the three recorded path residuals and three stale texts closed, no live leg  CONTEXT (operator relay item 1a, the 2026-09-23 0-pending adaptation): split from the Diagnostic-quality cluster below, which the relay blocked on the real-model preflight canary; this entry takes that cluster's two CARRY blocks verbatim, carries no leg and is sized for one window — the chunk that shipped the real-model leg carried ONE capability and took three windows at effort max (operator-measured from that chunk's transcripts).  CARRY: harden the real-model capture's two unguarded joins — `crates/conductor-run/tests/real_model_live.rs`'s `runs_dir()` joins `CONDUCTOR_RUNS_DIR` with no `resolve_under`, and its `pulse_log()` joins `ANDROMEDA_PULSE_DATA_DIR`'s value with `logs/` with no canonicalize — plus `tests/live_suite.rs`'s identical `runs_dir()`, whose capture-read panic prints the resolved path; security-plan §Input Validation records all three as residuals (operator-routed 2026-09-23).  CARRY: three stale read-back texts — `crates/conductor-verify/src/preflight.rs:202-205` cites fingerprint attribution while `assert_canary` attributes by freshness; `crates/conductor-run/tests/storm_harvest.rs`'s header says `fingerprint_refs` is pinned to `[]`, while at Pulse `83d4060` it carries the cue's fingerprint (measured 2026-09-10, architecture [Read-Back Dependency Posture]); `contracts/pulse-real-model-leg-posture.md` §The launch posture still names the L4 term a `shell-declaration` (it shipped as `shell-absence`, `l4-real-model`).
   ↓
Architecture registries compacted under the read cap — Established Decisions and Occupied Resources each readable whole in one Read, no registered fact lost  CONTEXT (operator relay CE-3, the 2026-09-23 0-pending adaptation): `.andromeda/architecture.md` is 148 508 B and its two registry sections hold 66 % of it — `## Established Decisions` 49 134 B and `## Occupied Resources` 48 859 B (measured at `edc0af8`; the relay records 79.8 KB and 64 % for the pair on 2026-09-15). hypothesis: at the last eight days' pace the larger section reaches the ~64 KB Read cap (itself an estimate) in about two weeks, and a Read past its cap returns page 1 without refusing. How to compact — what moves to `architecture-amendments.md`, what the decisions log's contract allows — is this chunk's research.  CARRY: propose a `D-arch-collision` detector at this chunk's wrap (propose → operator approves → append to `.andromeda/drift-base.md`) — `D-arch-resources` (`drift-base.md:27-31`) states its invariant as REGISTRATION only, so a second owner of an already-registered port, env var or socket passes it today (operator relay CE-3).
   ↓
