# a11y extract

## No domain coverage
The chunk has no rendered-UI, keyboard, focus, contrast, motion, form or live-region surface. Its only changes are path hygiene in the `conductor-run` test binaries (`real_model_live.rs` and `live_suite.rs`), a comment in `preflight.rs`, the header of `storm_harvest.rs`, and prose in `contracts/pulse-real-model-leg-posture.md`. None of these touch the desktop-webview surface that a11y-plan §1 scopes. a11y-plan §1 (around line 119) and §4 (around line 149) exclude headless `conductor run` paths and tests' security-vector coverage as not-assertable. No amendment-history entry covers these files.
