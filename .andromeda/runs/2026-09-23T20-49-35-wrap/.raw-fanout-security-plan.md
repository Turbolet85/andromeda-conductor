proposals:
  - detector: D-security-input
    severity: escalate
    section: "Input Validation (Env-var path handles row, CONDUCTOR_RUNS_DIR / CONDUCTOR_SCENARIOS_DIR / CONDUCTOR_CONTRACT_MANIFEST)"
    change: "Replace the per-READER residual text. It now says journal_conformance.rs has no canonicalize of its own, and that real_model_live.rs and live_suite.rs join with no resolve_under/canonicalize, that an absolute value replaces the root, that live_suite.rs's panic prints the resolved path, and that hardening is route-owned. The new text: since 2026-09-23 all three CONDUCTOR_RUNS_DIR test-binary readers (journal_conformance.rs, real_model_live.rs, live_suite.rs) resolve the handle through the shared test module conductor-run/tests/capture_paths (runs_dir_from → conductor_core::resolve_under) before any read. It rejects absolute and `..` values, and unset or relative values resolve. Every rejection text is path-free (`CONDUCTOR_RUNS_DIR rejected: {fixed reason}`). live_suite.rs panics name only the file name and the error kind, never display(). This is proven by the capture_paths_guard test target (8 cases, both runners). The residual and the route-owned CARRY are closed."
    sidecar: "security-plan §Input Validation path-handle row: the 3 CONDUCTOR_RUNS_DIR test-binary readers are now guarded via tests/capture_paths → resolve_under; the unguarded-reader and path-printing-panic residual is retired (2026-09-23)."
    rationale: "Report Changes → Counts moved: 'CONDUCTOR_RUNS_DIR test-binary readers joining with no resolve_under … 3 unguarded to 0 unguarded' and 'Path-printing panics in live_suite.rs went from 3 to 0'. Symbols/APIs record that runs_dir()/journal_conformance route through the guard. Expected amendments names this row (:115). The row still asserts that the residual exists."
    basis: "D:/dev/projects/conductor/.andromeda/security-plan.md:115"
  - detector: D-security-input
    severity: escalate
    section: "Input Validation (Real-model capture ingest row)"
    change: "Replace 'a TEST-binary reader turning that handle's VALUE into a path, with no canonicalize (the residual is recorded at the data-dir spawn row below)' with: the handle's value is resolved through capture_paths::pulse_logs_dir_from, which canonicalizes it and requires a directory before joining logs/. Failures are path-free (`ANDROMEDA_PULSE_DATA_DIR unset` / `rejected: {io::ErrorKind}` / `rejected: not a directory`) and surface as `pulse-log: none ({reason})` through redact_value → mask_host_paths."
    sidecar: "security-plan §Input Validation real-model capture row: the ANDROMEDA_PULSE_DATA_DIR value reader now canonicalizes and requires a directory; the no-canonicalize residual is retired (2026-09-23)."
    rationale: "Report Counts moved: 'ANDROMEDA_PULSE_DATA_DIR VALUE readers with no canonicalize (real_model_live.rs::pulse_log) went from 1 to 0'. Symbols/APIs: pulse_log() 'canonicalizes the data dir, requiring a directory, before joining logs/'."
    basis: "D:/dev/projects/conductor/.andromeda/security-plan.md:121"
    dependent-of: D-security-input
  - detector: D-security-input
    severity: escalate
    section: "Input Validation (ANDROMEDA_PULSE_DATA_DIR spawn-propagation row)"
    change: "In the parenthetical, replace 'with no canonicalize — a recorded residual, route-owned with the capture's CONDUCTOR_RUNS_DIR join' with: since 2026-09-23 it is canonicalized and required to be a directory through tests/capture_paths, with path-free failures. Keep the statement that this reader neither relaxes nor satisfies the spawn duty."
    sidecar: "security-plan §Input Validation data-dir spawn row: the test-binary VALUE reader's no-canonicalize residual is retired (2026-09-23)."
    rationale: "Same measurement as the capture-row proposal (report Counts moved bullet 2). The report's Expected amendments names :122 explicitly."
    basis: "D:/dev/projects/conductor/.andromeda/security-plan.md:122"
    dependent-of: D-security-input
  - detector: D-security-input
    severity: escalate
    section: "Bootstrap phases (derive for route / setup-project) → input-validation-library-install"
    change: "Drop the claim that journal_conformance.rs, live_suite.rs and real_model_live.rs carry the recorded test-only residual. The new text: these CONDUCTOR_RUNS_DIR test readers resolve through tests/capture_paths (resolve_under) since 2026-09-23. The only Rust test-binary reader that still carries an unguarded residual is CONDUCTOR_E2E_SEED_DIR (envelope_fixture.rs)."
    sidecar: "security-plan §Bootstrap input-validation-library-install: the per-READER residual list loses the three CONDUCTOR_RUNS_DIR test readers; the CONDUCTOR_E2E_SEED_DIR residual is unchanged (2026-09-23)."
    rationale: "Report Expected amendments: ':221 … per-READER residual list loses the three CONDUCTOR_RUNS_DIR test readers and the data-dir value reader. The CONDUCTOR_E2E_SEED_DIR residual is untouched.' Counts moved bullet 1 gives 3 → 0 unguarded."
    basis: "D:/dev/projects/conductor/.andromeda/security-plan.md:221"
    dependent-of: D-security-input
  - detector: D-security-input
    severity: escalate
    section: "Security Anti-Patterns → Input"
    change: "In the CONDUCTOR_RUNS_DIR bullet, replace 'CONDUCTOR_E2E_SEED_DIR is no longer the only Rust test-binary reader — journal_conformance.rs … applies no canonicalize of its own, carrying the same explicit residual … So do the two operator-gated live-pulse captures … hardening both joins is route-owned' with: the three CONDUCTOR_RUNS_DIR test-binary readers (journal_conformance.rs, live_suite.rs, real_model_live.rs) route through tests/capture_paths → resolve_under since 2026-09-23, which rejects absolute and `..` values with path-free errors. CONDUCTOR_E2E_SEED_DIR is again the only unguarded Rust test-binary reader. Keep the rule that the duty binds by READER and still binds every SHIPPED reader."
    sidecar: "security-plan §Anti-Patterns → Input: the unguarded CONDUCTOR_RUNS_DIR test-reader residual and the route-owned hardening note are retired; only the CONDUCTOR_E2E_SEED_DIR residual remains (2026-09-23)."
    rationale: "Report Counts moved bullets 1–2 and Expected amendments (':325 … loses the three CONDUCTOR_RUNS_DIR test readers'). The bullet still asserts these readers are unguarded and their hardening is pending."
    basis: "D:/dev/projects/conductor/.andromeda/security-plan.md:325"
    dependent-of: D-security-input
