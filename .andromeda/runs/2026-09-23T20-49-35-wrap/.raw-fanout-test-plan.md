proposals:
  - detector: D-tests-coverage
    severity: warning
    section: "§1 Test Scope Summary → Coverage triggers → security Vector 1 (CLI input / `CONDUCTOR_*` env path handles) row (line 87)"
    change: "After the FOURTH per-reader class, add a FIFTH class (2026-09-23): the test-binary readers of `CONDUCTOR_RUNS_DIR` (`journal_conformance.rs`, `real_model_live.rs`, `live_suite.rs`) resolve the handle through the shared `crates/conductor-run/tests/capture_paths` guard, which calls `conductor_core::resolve_under` so absolute and `..` values are rejected before any read. The same guard covers the `ANDROMEDA_PULSE_DATA_DIR` value read in `real_model_live.rs::pulse_log`, which it canonicalizes and requires to be a directory. Its required negative test is `capture_paths_guard` (8 unit cases, run by default under both nextest and cargo test), and those cases also assert that no failure message echoes its input or a path."
    sidecar: "test-plan §1 Vector 1: recorded the test-binary CONDUCTOR_RUNS_DIR / ANDROMEDA_PULSE_DATA_DIR reader class and its capture_paths_guard negative test (additive coverage, 2026-09-23 capture-path chunk)."
    rationale: "Report Changes → Files: NEW tests/capture_paths/mod.rs and tests/capture_paths_guard.rs. Counts moved: 3 unguarded CONDUCTOR_RUNS_DIR test-binary readers became 0, 1 un-canonicalized data-dir value reader became 0, and conductor-run gained 1 test target (8 tests). Coverage of new surfaces: 'tests unit (capture_paths_guard, 8 cases, both runners)'. Expected amendments names test-plan §1 Vector 1 (:87) as ADDITIVE coverage. Line 87 lists each per-reader class of the Vector-1 handles and its mandated coverage (THIRD: CONDUCTOR_E2E_SEED_DIR, FOURTH: CONDUCTOR_A11Y_STRICT) but has no entry for this reader class, so the new guarded path's negative test is missing from the trigger row. This adds coverage and corrects no stale claim. The tests are deterministic (they use env values only, with no network or time). Framework (D-tests-framework): plain #[test] rather than rstest #[case] is permitted by §2/§4 naming ('#[test] / #[tokio::test] / rstest #[rstest]'), where rstest #[case] is scoped to the P-ID catalog and garde matrices, and the runner is cargo-nextest --profile ci. Not drift. D-tests-obs-harness: 'Harness / gate surface: none changed' and no status or log format change, so no hit. D-tests-derived-count: none of the moved old values (3 unguarded, 1 no-canonicalize, 3 display() panics, 13 matches, the conductor-run target count) appears as a literal in a selector label, fixture count or tier table (grep returned 0 hits), so no hit. D-platform-claim: the report retires no platform, runner or driver verdict (Spec claims disproved concern read-back comment texts and a posture-doc term), so no hit."
    basis: "D:/dev/projects/conductor/.andromeda/test-plan.md:87; report Changes → Counts / qualifiers moved bullets 1, 2 and 4; Coverage of new surfaces"
