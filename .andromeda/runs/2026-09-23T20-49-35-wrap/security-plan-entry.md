
## 2026-09-23-real-model-capture-path-handles-guarded-and-stale-read-back-texts-corrected — the test-binary path-handle residuals closed
**Section:** §Input Validation (env-var path-handles row `:115`; real-model capture ingest row `:121`; `ANDROMEDA_PULSE_DATA_DIR` spawn row `:122`) · §Bootstrap phases → input-validation-library-install (`:221`) · §Security Anti-Patterns → Input (`:325`)
**Change:** The three `CONDUCTOR_RUNS_DIR` test-binary readers (`journal_conformance.rs`, `real_model_live.rs`, `live_suite.rs`) now resolve the handle through the shared test module `conductor-run/tests/capture_paths` (`runs_dir_from` → `conductor_core::resolve_under`), so an absolute or `..` value is REJECTED with a path-free reason instead of replacing the workspace root; `live_suite.rs`'s failure texts name the file and error kind, never a path. The `ANDROMEDA_PULSE_DATA_DIR` VALUE read in `real_model_live.rs::pulse_log` is canonicalized and required to be a directory before `logs/` is joined. The unguarded-join, path-printing-panic and no-canonicalize residuals (recorded 2026-09-06 / 2026-09-22 / 2026-09-23) are retired at all five sites; the `CONDUCTOR_E2E_SEED_DIR` residual is unchanged. Evidence: `crates/conductor-run/tests/capture_paths_guard.rs`, 8 cases green under nextest and `cargo test`.
**Why:** This chunk shipped the hardening the master had recorded as route-owned (the 2026-09-23 working-route CARRY); `journal_conformance.rs`, recorded as a residual but never routed, was brought in by operator ruling at /andromeda-phase P4. Applied routine under playbook `a chunk SHIPS the fix that a spec master's own body already names as route-owned-not-shipped` for the routed readers, and by the operator's recorded P4 direction for `journal_conformance.rs` (that rule's route-entry clause fails there — no-match branch). Nothing new crosses a boundary: the guard narrows what the readers accept. Cascade sweep (all seven masters + playbook + drift-base + CLAUDE.md + `.claude/rules` + `.claude/docs`, `grep -rnoE` for `REPLACES the root|with no canonicalize|no \`resolve_under\` / canonicalize|hardening both joins|route-owned with the capture|path-bearing panic|DOES print the resolved path|without a guard of its own|joined under the workspace root with no|recorded residuals; their hardening`): 5 hits after the body edits — security-plan `:325` (the still-true `CONDUCTOR_E2E_SEED_DIR` clause, no change); `.claude/rules/security.md:18` ×3 and `.claude/docs/security-summary.md:11` (leaves, re-derived this pass). No other master cites the retired residual.
