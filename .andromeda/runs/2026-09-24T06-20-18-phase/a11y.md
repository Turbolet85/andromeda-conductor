# a11y extract

## Relevance
Partial. The chunk adds no UI surface, focus, keyboard, contrast, motion or live-region behavior, so no WCAG success criterion is triggered. But the two registry sections being compacted hold facts that the a11y domain relies on and cites by anchor: the a11y CI job inside Established Decisions `[CI/CD]` (`architecture.md:60`, the largest single line), plus the webview-driver listeners, the `runs/a11y/<run_id>.jsonl` violation record and the a11y log landing sites in Occupied Resources.

## Constraints
- a11y-plan §11 A11y Anti-Patterns ▸ Strategy (the CARVE-OUT, `a11y-plan.md:516`) cites `architecture §Established Decisions [CI/CD]` by name as the authority for barring the hold-dependent Operable half (trap entry, containment, focus restoration) from CI. It is the only cross-master citation in a11y-plan that points into this chunk's two sections. Compaction has to keep the `[CI/CD]` entry label resolvable. It also has to keep the current-truth statement that CI has no Pulse, so the driven (`a11y:driven`) and screen-reader (`sr*`) arms stay operator-local.
- Per a11y-plan §11 ▸ Strategy, the routine pair's CI proof is "stated with its configuration rather than as a bare verdict", and each term of that configuration is load-bearing. Those terms are: hosted `windows-2022`, a coherent msedgedriver/WebView2 pair pinned from the image's own runtime, and High integrity. Wherever the `[CI/CD]` body keeps the a11y job, it must keep these terms as current truth. The dated `windows-2025` RED runs, the 2026-09-17 move narrative and the run-ID measurements are history and may move to the sidecar. Whether the a11y-plan §11 wording relies on any of those history clauses still being in the arch body is a question for P3 research.
- a11y-plan §3 A11y Assertion Harness Contract ▸ Structured violation JSON schema and ▸ CI integration require that the violation record the webview gate emits stays a registered artifact. That means the `runs/a11y/<run_id>.jsonl` handle, its one-envelope-per-run shape, and its isolation from the scenario-run `*.jsonl` glob. "No registered fact lost" must cover this row.
- a11y-plan §3 ▸ CI integration and §9 CI Integration describe the routine arm as `CONDUCTOR_A11Y_STRICT`-gated, followed by the `journal_conformance` gate over `runs/a11y` and then the violation-record upload. The compacted `[CI/CD]` body must keep this ordering and these gate names as current truth. It may shed the narrative about how the ordering came about.
- a11y-plan §1 A11y Scope Summary (CI integration) and §9 cite the arch **Stack** CI/CD rationale verbatim at three sites. That anchor is outside this chunk's boundary. It becomes a constraint only if compaction relocates or rewords text that the Stack cell shares with `[CI/CD]`. Whether the two texts overlap is P3's question.
- Per a11y-plan §12 A11y Decisions Log and the chunk's own boundary, no a11y decision gets re-decided. The carve-out split (perceivable plus hold-free operable pair in CI, hold-dependent half operator-local) must read the same after compaction as before.

## Patterns to follow
- Use the verbatim cross-master citation sweep with a known-positive control, per the a11y-plan amendment `2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate`. After the body edit, grep every master for the moved or reworded phrases, and run a control grep that must return a known non-zero count. The third citation site in that pass was found only because the control fired.
- Measure coordinates again at edit time instead of trusting recorded line or offset numbers, per the a11y-plan amendment `2026-09-17-keyboard-and-focus-order-coverage-ownership` item (5). Compaction moves line numbers, so any `a11y-plan.md:NNN` or `architecture.md:NNN` citation must be re-resolved, not assumed.
- Record explicit "checked and NOT amended" absence claims with their basis (same 2026-09-09 entry). If the sweep finds a11y-plan needs no edit, say so with the grep evidence.

## Anti-patterns to avoid
- Don't let a relocation leave a stale anchor behind. a11y-plan §11 ▸ Strategy names `§Established Decisions [CI/CD]`. Renaming, splitting or merging that bracketed label without a cascade edit to a11y-plan would leave the carve-out's authority citation dangling.
- Don't collapse the configuration-qualified CI proof into a bare "a11y gate green" verdict. a11y-plan §11 ▸ Strategy explicitly rejects that form, and §11 ▸ CI bans unconditional CI-existence claims.

## Contract bindings
- Architecture ↔ a11y: the `§Established Decisions [CI/CD]` anchor is the authority that a11y-plan §11 ▸ Strategy cites for the operator-local hold-dependent half.
- Architecture Occupied Resources ↔ a11y §3 violation schema ↔ obs: the `runs/a11y/<run_id>.jsonl` row binds a11y-plan §3 ▸ Structured violation JSON schema to obs's structured-record format through the `journal_conformance` gate.
- Architecture `[CI/CD]` ↔ tests: the a11y job, the routine arm and the expected-skip SET under `CONDUCTOR_A11Y_STRICT` bind a11y-plan §9 CI Integration to test-plan (same workflow).

## Acceptance criteria contributions
- (a11y) After compaction, `grep -n '\[CI/CD\]' .andromeda/architecture.md` resolves inside `## Established Decisions`, and a11y-plan `:516`'s citation `architecture §Established Decisions [CI/CD]` still names an existing entry (per a11y-plan §11 A11y Anti-Patterns ▸ Strategy).
- (a11y) The compacted `[CI/CD]` body still states: the a11y job's runner (`windows-2022`), the driver pinned from the image's runtime, `CONDUCTOR_A11Y_STRICT`, the `journal_conformance` gate over `runs/a11y`, the violation-record upload, and the reason the driven and SR arms stay operator-local (no Pulse in CI). Each is checked by grep (per a11y-plan §11 ▸ Strategy and §9 CI Integration).
- (a11y) Occupied Resources still registers `runs/a11y/<run_id>.jsonl` and the dev-only, harness-lifetime tauri-driver/msedgedriver listeners, checked by a before/after handle diff (per a11y-plan §3 A11y Assertion Harness Contract ▸ Structured violation JSON schema).
- (a11y) The post-edit cross-master sweep over a11y-plan returns either zero stale arch citations or a recorded cascade edit, with a known-positive control count reported (per a11y-plan amendments `2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate` pattern / §9 CI Integration).

## Relevant amendment history
- `2026-09-09-workspace-formatting-pass-and-a-fmt-ci-gate`: a11y-plan quotes arch §Stack's CI/CD rationale verbatim at three sites (§1, §9 ×2). When arch amended that cell, all three had to move. The third site was caught only by a known-positive control grep. This is precedent that arch CI-text edits cascade into a11y-plan, and for the sweep-plus-control method.
- `2026-09-17-keyboard-and-focus-order-coverage-ownership`: §11 ▸ Strategy (`:516`) gained the substitute-gate sentence, which cites `architecture §Established Decisions [CI/CD]` as the reason CI cannot run the hold-dependent half. That citation is the live link into this chunk's section. Item (5) also set the rule to measure line and offset coordinates again at edit time.
- `2026-09-17-a11y-routine-arm-terminal-on-the-measured-configuration`: the routine pair's CI proof was discharged "stated with its configuration" (windows-2022, coherent 131 pair, High integrity). This is why those terms are current truth that has to survive compaction, while the `windows-2025` RED runs are history.
- `2026-09-07-a11y-ci-gate` / `2026-09-07-sr-findings-fixed`: these established the violation schema's real shape and the Operable carve-out that the arch `[CI/CD]` entry and the `runs/a11y` Occupied Resources row now register.
