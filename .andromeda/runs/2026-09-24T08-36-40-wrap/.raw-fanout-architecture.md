proposals:
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies
    change: In the "Operator instruments (host runtime)" row, enumerate three committed operator-local instruments — keep the code-graph pipeline (`scripts/code-graph.py` + `scripts/scip_pb2.py` + `scripts/requirements.txt`, since 2026-06-18) and the mutation-tally gate (`scripts/mutation-gate.py` + `scripts/mutation-roster.toml`, since 2026-09-13), and add the architecture-registry size/faithfulness check (`scripts/arch-registry-check.py`, stdlib only, no `requirements.txt` delta, since 2026-09-24); replace "Neither is invoked by any CI step, and neither adds a sixth `agent-run.{sh,ps1}` command" with "None is invoked by any CI step, and none adds a sixth `agent-run.{sh,ps1}` command" — the 5-command harness surface unchanged clause stays.
    sidecar: 2026-09-24 §Stack "Operator instruments (host runtime)" row — `scripts/arch-registry-check.py` (stdlib, operator-local, no CI step, no sixth harness command) registered as the third committed instrument; the two-instrument "Neither … neither" wording becomes "None … none".
    rationale: The report's Symbols bullet adds exactly one new committed operator instrument, `scripts/arch-registry-check.py` (stdlib Python, no CI step invokes it, no `agent-run` verb or flag added, harness keeps its 5 commands), and its Counts bullet states the committed operator-instrument count moves from two to three; its Dependencies bullet says it runs on the host Python 3 already registered in this row. §Stack :39 still enumerates only two instruments and says "Neither", so the row now omits a stack element the chunk uses — the report's planned amendment 3. Sweep for other occurrences of the two-instrument claim in architecture.md found none (`mutation-gate.py`, `code-graph.py` and "Operator instruments" appear only at :39; other "operator-local" hits at :60/:150/:172/:193/:256/:257 are the a11y driven/sr arms, not an instrument count). D-arch-resources: no hit — the report lists no new IPC method / endpoint / port / socket / env var / Rust-TS symbol / crate ("Crates / modules: none"). D-platform-claim: no hit — the only disproved spec claim is a stale `residuals.md:15` line citation, which retires no platform/runner/driver verdict, and the harness surface adds only an operator-local instrument outside CI.
    basis: .andromeda/architecture.md:39
