Over the workspace subject `git ls-files --cached --others --exclude-standard`, none of the 14 content rules (private-key block · AWS access key id · GitHub classic token · GitHub fine-grained PAT · Slack token · Slack webhook · Stripe live key · Google API key · JWT · Anthropic key · OpenAI key · npm token · URL-embedded credentials · generic quoted secret assignment) and no secret file-name class matches outside an allowlist that is empty at claim time. `.gitignore` ignores every secret file class (`.env*`, the `id_*` key files, `*.pem|p12|pfx|key|cer|crt|jks|keystore`). The gate is shown able to FAIL: each rule fires on its runtime-built sample, and a planted sample makes `crates/conductor-core/tests/secret_scan_gate.rs` fail, naming its `path:line` and rule without echoing it. The `rust` job invokes that target from the `Secret-scan gate` step unconditionally, with a presence guard and no `continue-on-error` and no `if:`. The plan's wiring probes (the step named once, the `continue-on-error` and `if:` counts unchanged) prove that wiring. The CI conclusion on the operator's push is a report-only record and not part of this acceptance.
