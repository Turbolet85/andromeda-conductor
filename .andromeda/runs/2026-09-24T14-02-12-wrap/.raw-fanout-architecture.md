proposals:
  - detector: D-arch-resources
    severity: warning
    section: "Occupied Resources — Environment variables"
    change: "Add a bullet after `RUNNER_TEMP`: `GHA_ENV_CONTEXT_PROBE` is not a Conductor handle and is outside the reserved namespace. It is registered on the WEBVIEW2_* basis (a shipped artifact names and sets it). It is CI-only and non-secret: `.github/workflows/ci.yml`'s `rust` job writes it to `GITHUB_ENV` in `GITHUB_ENV context probe (write)` and reads it through `${{ env.GHA_ENV_CONTEXT_PROBE }}` in `GITHUB_ENV context probe (assert)`, which exits 1 on a mismatch. No shipped or test code reads it."
    sidecar: "2026-09-24 secret-scanning-ci-gate: registered GHA_ENV_CONTEXT_PROBE, the rust job's self-probe that pins the premise that GITHUB_ENV keys reach the env context."
    rationale: "Report Changes > Symbols/APIs registers 'New env var: GHA_ENV_CONTEXT_PROBE', written and read only by the rust job's two GITHUB_ENV context probe steps (report.md:25-27, 62-65). §Occupied Resources — Environment variables (architecture.md:184-207) does not list it, even though every other CI-workflow-named variable (EDGEWEBDRIVER, WEBVIEW2_*, RUNNER_TEMP, TEMP/LOCALAPPDATA) is registered."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:202-204"
  - detector: D-arch-decisions
    severity: warning
    section: "Established Decisions — [CI/CD]"
    change: "The `rust` job's gate set now has two kinds. It keeps the static gates over committed data (coverage-completeness 2026-09-06, scenario-assertion audit 2026-09-16), which assert a committed artifact against its production source. It also gains two static REPO gates (2026-09-24): `Secret-scan gate` (`-p conductor-core --test secret_scan_gate`) asserts a repo-wide absence over `git ls-files --cached --others --exclude-standard`, and `Workflow env-context gate` (`-p conductor-core --test workflow_env_gate`) asserts each workflow against its own `env:`/`GITHUB_ENV` declarations. Each is its own named step with no `continue-on-error`, no `if:`, and a `::error::` presence guard. Next to them is the `GITHUB_ENV context probe (write|assert)` pair. There is no new egress and no marketplace action."
    sidecar: "2026-09-24 secret-scanning-ci-gate: [CI/CD] gate set changed kind. Added secret_scan_gate and workflow_env_gate (static repo gates, not over committed data) plus the GITHUB_ENV context self-probe; rust job 23 → 27 steps."
    rationale: "Report Changes > Counts/qualifiers moved: 'The static-gate FAMILY changes KIND, not just size… Neither new gate is of that kind' (report.md:47-52). Harness/gate surface adds four named rust-job steps (report.md:57-67). The locked [CI/CD] decision describes the rust job's static gates only as 'a static gate per committed artifact that a production source derives', so the chunk's gates fall outside it."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:60"
  - detector: D-arch-decisions
    severity: warning
    section: "Stack and Technologies — CI/CD row"
    change: "In the Technology cell, change '+ the static gates over committed data' to '+ the static gates (over committed data · repo-wide secret scan · workflow env-context)'. In the Role cell, keep 'the static gates each asserting a committed artifact against the production source…' for coverage-completeness and scenario audit only, and add 'plus the secret-scan and workflow env-context repo gates (2026-09-24)'."
    sidecar: "2026-09-24 secret-scanning-ci-gate: §Stack CI/CD row now names the two new repo gates next to the committed-data gates."
    rationale: "Same family-kind change (report.md:47-52). The report names :37 as a site of the phrase, and its 'build + test gating' Role cell is the text the a11y-plan quotes (report.md:117-120)."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:37"
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: "Infrastructure Patterns — Directory structure"
    change: "Change the `.github/workflows/` tree comment to: 'GitHub Actions: fmt + build + test + clippy + the static gates (committed data · secret scan · workflow env-context) + the a11y webview e2e gate'."
    sidecar: "2026-09-24 secret-scanning-ci-gate: workflows tree comment now names the new gate kinds."
    rationale: "Same family-kind change (report.md:47-52). The report names :245 as a site."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:245"
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: "Infrastructure Patterns — CI/CD approach"
    change: "After the two committed-data gates, add: 'and two static repo gates (2026-09-24), each its own presence-guarded rust-job step: the secret-scan gate (a repo-wide absence over git's cached+untracked non-ignored listing) and the workflow env-context gate (each workflow's `${{ env.X }}` / `if: env.X` reads against its own declarations), next to the GITHUB_ENV context self-probe'."
    sidecar: "2026-09-24 secret-scanning-ci-gate: CI/CD approach now lists the secret-scan and workflow env-context gates."
    rationale: "Same family-kind change (report.md:47-52, 57-67). The report names :248 as a site."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:248"
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: "Inherited Defaults — CI/CD"
    change: "Change '+ the static gates over committed data (coverage-completeness · scenario-assertion audit)' to '+ the static gates (committed data: coverage-completeness · scenario-assertion audit; repo: secret scan · workflow env-context)'."
    sidecar: "2026-09-24 secret-scanning-ci-gate: Inherited Defaults CI/CD gate list updated."
    rationale: "Same family-kind change (report.md:47-52). The report names :278 as a site."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:278"
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: "Infrastructure Patterns — Build system"
    change: "Add `regex` (1.12, a root `[workspace.dependencies]` entry and a `conductor-core` `[dev-dependencies]` edge; dev-only, already locked transitively, so the package count stays 562) to the dev-test stack list 'rstest/proptest/insta/assert_cmd/assert_fs/predicates'."
    sidecar: "2026-09-24 secret-scanning-ci-gate: regex 1.12 joined the dev-test stack as conductor-core's dev-dependency (no new package)."
    rationale: "Report Changes > Dependencies: '`regex = \"1.12\"` added to root [workspace.dependencies] and as a conductor-core [dev-dependencies] edge… Dev-only' (report.md:30-33). The only dev-test stack enumeration in arch (architecture.md:211) does not include regex, and neither does §Stack or §Established Decisions."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:211"
  - detector: D-platform-claim
    severity: warning
    section: "Occupied Resources — Environment variables (EDGEWEBDRIVER bullet)"
    change: "Replace the rationale 'because GitHub's `${{ env.* }}` expression context holds only workflow/job/step declarations and never a runner-process variable' with: 'The shell read stays correct. The `${{ env.* }}` context does not carry an IMAGE-set runner variable (run 34148079506). It DOES carry a key an earlier step wrote to `GITHUB_ENV`, as measured on `windows-latest` at run 36006370951 (2026-09-24), so it is no longer the reason this value is read in the shell.'"
    sidecar: "2026-09-24 secret-scanning-ci-gate: retired the stale EDGEWEBDRIVER shell-read rationale. GITHUB_ENV-written keys resolve through ${{ env.* }} in a later step (windows-latest, run 36006370951); image-set variables still do not."
    rationale: "Report 'Spec claims disproved by measurement' quotes architecture.md:200. F12 measured that a GITHUB_ENV-written key resolves through ${{ env.* }} in a later step on windows-latest (report.md:77-80, 84-92). The disposition is 'amend the body sentence's rationale'. Stating sentence: '…resolves `CONDUCTOR_MSEDGEDRIVER` from it in the step SHELL (`$env:EDGEWEBDRIVER`), because GitHub's `${{ env.* }}` expression context holds only workflow/job/step declarations and never a runner-process variable.'"
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:200"
  - detector: D-platform-claim
    severity: warning
    section: "Occupied Resources — Environment variables (RUNNER_TEMP bullet)"
    change: "Change 'rather than through a `${{ env.* }}` expression, whose context holds only what a workflow, job or step declared' to 'rather than through a `${{ env.* }}` expression, whose context carries workflow/job/step declarations and `GITHUB_ENV`-written keys (windows-latest, run 36006370951) but no image-set runner variable'."
    sidecar: "2026-09-24 secret-scanning-ci-gate: RUNNER_TEMP bullet's env-context clause no longer says the context holds declarations only."
    rationale: "The same retired claim appears again at architecture.md:203: 'whose context holds only what a workflow, job or step declared'. The report's F12 measurement shows GITHUB_ENV-written keys also resolve (report.md:77-80). The shell read of the image-set RUNNER_TEMP stays correct (report.md:90-91)."
    basis: "D:/dev/projects/conductor/.andromeda/architecture.md:203"
    dependent-of: D-platform-claim

Notes, not proposals:
- D-arch-collision: no hit. The one new env var (`GHA_ENV_CONTEXT_PROBE`) has no prior owner. The `EDGEWEBDRIVER` writer (the a11y pin gate) is unchanged, the egress count stays one (msedgedriver), and no port, socket or artifact path changed owner.
- D-arch-registry-size: tooling detector, so there is no agent-read proposal. The report expects it to trip (186 B / 208 B margin, report.md:108-110). The remedy is to move this wrap's history into the sidecar. The change texts above are kept short for that reason.
- architecture.md:204 (TEMP/LOCALAPPDATA) names `${{ env.* }}` but states nothing about what the context holds, so it is not a hit.
- The report names `ci.yml:351-356` as a non-master site of the same stale rationale. It is out of scope here: the report says route-resolve owns it as a carry.
