proposals:
  - detector: D-tests-derived-count
    severity: warning
    section: "§4 Unit Test Strategy — What unit tests cover, the Scenario catalog + coverage-matrix bullet"
    change: "Keep the closing sentence's definition of the set of crate-local static gates over committed data (whatever `tests/` targets assert a committed artifact against the production source it derives from, never a count). Add that the crate-local static-gate set now holds a second KIND besides that one. The workspace-wide secret-absence gate `crates/conductor-core/tests/secret_scan_gate.rs` asserts that the scanned file set (`git ls-files --cached --others --exclude-standard`) carries no secret-shaped string or file name, against an empty exact-set `ALLOWLIST`. The self-consistency gate `crates/conductor-core/tests/workflow_env_gate.rs` asserts every `.github/workflows/*.yml` `env` read against that file's own `env:` / `GITHUB_ENV` declarations. Name both by kind, never by a count of gates."
    sidecar: "2026-09-24-secret-scanning-ci-gate: §4 widens the crate-local static-gate family beyond 'static gates over committed data' to include the secret-absence gate (secret_scan_gate) and the workflow self-consistency gate (workflow_env_gate), named by kind with no count."
    rationale: "The report's Changes 'Counts / qualifiers moved' bullet says the static-gate family changes KIND, not just size. Neither new gate asserts a committed artifact against its production source: the secret scan asserts a workspace-wide absence, and the env-context gate asserts a workflow file against itself. So every site that names the set as 'the static gates over committed data' now under-describes it. The report counts 1 such test-plan site. The plan's expected amendments also carry 'static gates over committed data 1' for test-plan."
    basis: "D:/dev/projects/conductor/.andromeda/test-plan.md:250 (report.md:47-52, :111-113)"
  - detector: D-tests-derived-count
    severity: warning
    section: "§6 E2E Test Strategy — Scenario: Coverage-matrix completeness gate (static)"
    change: "After the 'Scenario-assertion audit leg', add two sibling static-gate legs that are NOT static gates over committed data. (1) 'Secret-scan leg': `cargo nextest run -p conductor-core --test secret_scan_gate --profile ci`, CI-enforced by the `ci.yml` `rust`-job step `Secret-scan gate`. That step's presence guard is `test -n \"$(git ls-files)\"`, and the step has no `continue-on-error` and no `if:`. The leg has 14 content rules plus a secret-file-name class and an empty `ALLOWLIST` graded as an exact set in both directions. A hit renders repo-relative and never echoes the match. (2) 'Workflow env-context leg': `cargo nextest run -p conductor-core --test workflow_env_gate --profile ci`, enforced by the step `Workflow env-context gate` with a presence guard on `.github/workflows/*.yml`. It checks every `${{ env.X }}` or bare `if: env.X` read against that file's `env:` keys plus its `GITHUB_ENV`-written keys. Each leg has 5 in-suite arms. Like the audit leg, neither takes a §9 stage-table row; both already run inside the Unit stage's `nextest run --workspace --profile ci`."
    sidecar: "2026-09-24-secret-scanning-ci-gate: §6 static-gate scenario gains the secret-scan and workflow env-context sibling legs (a different kind from the committed-data gates, no §9 stage-table row)."
    rationale: "Right now the §6 static-gate scenario lists only the CI-enforced static-gate legs of the committed-data kind: coverage_gate and the 'second static gate over committed data', scenario_audit_gate. That restates the family that §4:250 defines, so fixing only §4 would leave §6 under-describing the set of named static CI gates. The report's 'Harness / gate surface' bullet adds the `Secret-scan gate` and `Workflow env-context gate` steps. 'Symbols' adds the two 5-arm targets. The plan's expected amendments name 'test-plan §6 (two sibling static-gate legs) … no §9 stage-table row → carried'."
    basis: "D:/dev/projects/conductor/.andromeda/test-plan.md:366 (report.md:13-24, :57-67, :111-113)"
    dependent-of: D-tests-derived-count
