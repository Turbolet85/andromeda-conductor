CLAUDE.md ecosystem curated:
  Correction (cap-exempt): .claude/rules/host-win32.md 2026-09-17 entry — "The `${{ env.* }}` context holds only what a workflow, job or step DECLARED" + [corrected 2026-09-24: …or what an earlier step of the same job wrote to `GITHUB_ENV`; an image-set runner variable still never resolves]
    Proof: CI run 36006370951 (sha 6008a68, windows-latest) — the `GITHUB_ENV context probe (assert)` step printed "GITHUB_ENV context probe: the env expression context carries a GITHUB_ENV key" at 13:37:04.2368847Z (chunk evidence/ci-run-record.md).
  Tier 3 (.claude/docs/session-learnings.md): + "Pin an undocumented platform premise with a probe that re-measures it on every run" (confidence 0.8)
    Proof: three fetched GitHub docs pages left the GITHUB_ENV → env-context question unanswered (research.md §Platform issues consulted). The operator chose the probe design at P4, and run 36006370951 measured it. Signals: verified by measurement +0.4 · specific technical detail +0.2 · no other durable home +0.2 (the arch master carries this instance, not the pattern).
  Tier 3 (.claude/docs/session-learnings.md): + "Copying a precedent artifact copies its actor: re-derive every attribution clause" (confidence 0.8)
    Proof: the operator corrected evidence/ci-run-record.md's "The overseer made these" (copied from the fc4a9c2 precedent); the pushed commit 6008a68 carries the same copied line. Signals: explicit user correction +0.4 · specific technical detail +0.2 · no other durable home +0.2.
  Filters: 0 dup · 3 rejected at Filter 4 (0.2 each, one-off sweep-hazard notes: a URL-credential sample written as a `format!` string with `{}` placeholders in the userinfo slot self-matches the rule (and so does quoting that literal in prose — the light gate caught it in this log); `grep -c continue-on-error` counts a comment line; `git ls-files --cached` alone misses untracked new files) · 0 conflict · 0 deferred (cap)
  Recurrence-despite-learning → handoff: (1) host-win32.md "Encoding & heredocs" [corrected 2026-09-23] — a doubled backslash typed through the Bash transport is halved (a python probe for `tr "\\134"` printed False on a present fix). (2) the basis-beside-every-count rule (report-template; CLAUDE.md 2026-08-09) — the report first stated the workspace test total 1067 as if measured, when it was derived.
  No-other-home: "Pin an undocumented platform premise with a probe…" · "Copying a precedent artifact copies its actor…"
  CLAUDE.md size: 137/200 (unchanged; no Tier-1 write)
