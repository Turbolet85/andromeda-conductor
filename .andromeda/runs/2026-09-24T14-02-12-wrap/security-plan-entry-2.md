
## 2026-09-24-secret-scanning-ci-gate — rule (b)'s scope: a cargo test binary's fixed spawn is outside the governed forms
**Section:** §Security Anti-Patterns → Code Patterns, rule (b) (the harness-spawn rule)
**Change:** a closing clause records that a spawn from a cargo TEST binary sits outside rule (b)'s three loci (the driver stack · `scripts/agent-run.{sh,ps1}` · `.github/workflows/ci.yml`) and adds no governed form. The instance is `crates/conductor-core/tests/secret_scan_gate.rs`'s `git -C <repo root> ls-files -z --cached --others --exclude-standard`: a fixed PATH-resolved program, fixed argv, no operator-supplied value, never a shell string. Like the test binaries' `CARGO_BIN_EXE_*` spawns it composes nothing, so the count stays seven.
**Why:** escalated at this wrap (no detector proposed it; the security doc-agent flagged the spawn as outside D-security-subprocess's sidecar scope), and the operator directed "Record: outside rule (b)". It is the workspace's first test-binary spawn of a PATH-resolved external program (the report's Symbols bullet). Recorded so the next reader does not have to re-derive the scope question against the rule's "an EIGHTH crossing escalates" clause. It is NOT an eighth governed form and NOT a boundary widening, since nothing crosses. Leaf re-derived: `.claude/rules/security.md` rule (b) bullet. Sweep for rule (b)'s count wording (`SEVEN` / `seven governed forms` / `EIGHTH`): security-plan 1 site (amended here) · rules/security.md 1 site (re-derived) · no other master states the count.
