
## 2026-09-24-secret-scanning-ci-gate — the secret-scanning CI gate realized; tool selection recorded
**Section:** §Bootstrap phases `secret-scanning-ci-gate` · §Secret Management "Secret scanning in CI" · §Security Anti-Patterns → Secrets (the `.env` NEVER bullet) · §Threat Model Summary → CI/CD · §Security Decisions Log (new dated entry)
**Change:** the gate moves from "optional defense-in-depth, tool selection deferred" to realized. The `rust` job's `Secret-scan gate` runs `crates/conductor-core/tests/secret_scan_gate.rs` (content rules + a secret-file-name class over git's cached + untracked-not-ignored listing, an exact-set allowlist, a hit never echoing its match). `.gitignore`'s secret classes widen to `.env*` plus `*.pem *.p12 *.pfx *.key *.cer *.crt *.jks *.keystore id_*`. The CI/CD threat-model line lists the secret-scan and workflow env-context gates (in-repo test targets, no fetched tool). A new 2026-09-24 Decisions Log entry records the operator's selection of an in-repo Rust static gate over a job-time-fetched scanner, which would have been a second third-class member and a second CI egress. Initial-entry open question (3) is left untouched, as the log requires, and closed by that entry.
**Why:** raised by the orchestrator under Validate check 5: the plan's expected-amendments floor, which no security detector covers. It rests on the report's Changes (Harness / gate surface · Symbols · Schema / config) and the P4 operator decision. Sweep `optional (defense-in-depth )?secret-scanning|No specific secret-scanning tool|tool selection is deferred|not a researched category` over the seven masters + leaves + curation homes + playbook/drift-base: 2 hits, both on `security-plan.md:396` (the historical Initial entry) → no change (historical entries are never modified; the new entry closes them). `:224`, `:259`, `:352` and `:87` amended here. Leaves re-derived: `.claude/docs/security-summary.md:45` ("optional/deferred" → realized) and `.claude/rules/security.md` §Secrets (the ignore classes and the enforcing gate; the env-handle clause re-derived to "every env handle is non-secret"). No egress, spawn-form or dependency-class count moves.
