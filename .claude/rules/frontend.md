---
paths:
  - "crates/conductor-tauri/**/*.tsx"
  - "crates/conductor-tauri/**/*.jsx"
  - "crates/conductor-tauri/**/*.ts"
  - "crates/conductor-tauri/**/*.css"
  - "crates/conductor-tauri/ui/**"
---

# Frontend Rules

Path-scoped rules for the Tauri 2 desktop-webview UI. The GUI is convenience only — the cli/headless path is the release gate; never let GUI work block or replace the headless contract.

**Authoritative source:** `.andromeda/design-system.md` + `.andromeda/layout-templates.md` (desktop-webview surface). Reference design tokens by NAME; raw values live in design-system.md.

## Framework & structure
- React 19.x (Vite 8.x SPA; Preact 10.x size fallback) + Tailwind CSS v4.1 (Oxide via `@tailwindcss/vite`, static zero-runtime stylesheet) + shadcn/ui (Radix Primitives, locally vendored) + Lucide React icons. Tauri 2 (≥2.10.3) frameless window (`decorations:false`, `data-tauri-drag-region` titlebar).
- **Design tokens live on `:root`** (`ui/src/styles/tokens.css`), NOT Tailwind `@theme` — v4 `@theme` tree-shakes non-namespace tokens (drops `--space-*`/`--motion-*`/`--ease-*`) and forbids `@media` nesting; keep `@import "tailwindcss"` for the engine. Bind every token by name (`var(--…)`) — never hardcode hex/px (design-system.md §Tokens).
- **No router, no breakpoints, no browser nav** — the single frameless window IS the surface; the four "screens" (idle / live / HOLD / report) are one window in different run-states. The operator moves by run-state, not routes.

## Design system (expression 0.3 — functional motion only)
- Ground is cool Tokyo-Night slate `--color-base`; color is functional status (Verdict/ReportState), never decoration or mood. Borders-only depth (`--border-subtle` seams, NO drop shadows / `backdrop-filter`).
- Typography: JetBrains Mono is the reserved STATUS tier (count / P-IDs / run_id / SLO timings / fingerprints, `--color-id-cyan`); IBM Plex Sans for phase line + prose. Self-hosted WOFF2 via Fontsource — no runtime CDN.
- Signature: the **Paused-count hold-point** — the titlebar count freezes at the hold value, tints `--count-nominal`→`--count-hold`, resumes on proceed / dims `--count-blocked` on abort. The absence of motion is the event.
- Coverage matrix is a dense single-row-per-P-ID list (Linear instrument-panel density), NOT a KPI-card grid. Its Mode cell's out-of-scope value is recessive via `var(--status-residual)` — never `--status-fail`/`--count-blocked` (reusing any lamp-set token would read as a verdict/report-state), and never a seventh lamp; the label carries the signal.

## Live data
- One Tauri `Channel` streams live counters / target status backend→frontend — no polling/SSE/URL surface. Conductor MUST NOT emit native OS toasts (that's Pulse behavior it observes via the operator checklist).
- Always render explicit loading / empty / in-progress states as real prose — never implicit, never a gray skeleton, never downgrade in-progress to `Fail`.

## Security guardrails (honored at the layout level)
- Deny-by-default Tauri capabilities (only start/stop · picker · run-report · operator-pause + the one Channel); no `shell-open` with scenario-derived strings; no remote-origin iframes; Tauri ≥2.10.3.
- No `alert()`/`confirm()`/`prompt()` — use the styled shadcn `AlertDialog`. Style the webview scrollbar; suppress the Chromium context menu / devtools / text-selection on non-text.

## Hard bans (design Anti-Patterns)
- NEVER use generic fonts (Inter / Roboto / Arial / system-ui), purple-gradient-on-white "AI gradient", glassmorphism, KPI-card grids, flashing/pulsing on `Fail`, or a progress bar that hides/animates-to-100% on the operator-pause (it must freeze).
- NEVER convey state by color alone (see `a11y.md`); NEVER add `framer-motion` or any animation library (CSS transitions only at expression 0.3).

## Accessibility
- See `a11y.md` (auto-loads on the same paths) for the full WCAG contract. Semantic HTML first; keyboard-first; visible focus ring; not-color-alone; reduced-motion respected.

## Session Additions
_This section is owned by `/wrap-session`. setup-project preserves content added here on re-run._
- 2026-06-24: `conductor-tauri`'s `tauri::generate_context!` resolves `build.frontendDist` (`ui/dist`) at COMPILE time — so the webview bundle MUST be built (`npm run build` in `crates/conductor-tauri/ui`) before ANY workspace `cargo build`/`nextest`/`clippy` that compiles `conductor-tauri`, else the compile fails (`ui/dist` is git-ignored → regenerated per environment). A bare `cargo build` does NOT run `tauri.conf.json`'s `beforeBuildCommand` (only the Tauri CLI does), so the order is wired as an `ensure_frontend` step into `scripts/agent-run.{sh,ps1}` (the `run` verb) + the CI Rust job. Also: gitignore `crates/conductor-tauri/gen/` (tauri-build regenerates the ACL schemas each compile) and keep valid `bundle.icon` files present (PNG + ICO) — `generate_context!` validates them at compile time. (frameless-window-shell chunk)
- 2026-06-24: To demonstrate/exercise a **run-state-driven** webview component before its live data source (the Tauri `Channel`, Epoch-9 ch4) is wired, drive it with a typed local state lifted to `App` + a **DEV-only** cycler gated on `import.meta.env.DEV` (stripped from the production bundle), and pass the state as a PROP so the later `Channel` plugs into the SAME contract — don't bake the data source into the component. Gotcha: any use of `import.meta.env.*` requires `crates/conductor-tauri/ui/src/vite-env.d.ts` (`/// <reference types="vite/client" />`), else strict `tsc --noEmit` fails to typecheck it (`ImportMeta.env` is undefined without the vite/client lib reference). (paused-count-hold-point-signature chunk)
- 2026-06-26: In this project, shadcn/ui components are realized by using their underlying Radix/cmdk PRIMITIVES directly + styling with token `.css` / inline `var(--…)` — NOT by vendoring shadcn-cli's Tailwind-utility component files. The `ui/` has no `@/` path alias and styles via tokens-on-`:root` (`tokens.css`) + per-component `.css` (Titlebar/App pattern), so shadcn-cli components (which assume a `--popover`-style palette + Tailwind utility classes) don't fit and would need a full rewrite. The ScenarioPicker is `cmdk` (shadcn Command's engine) used directly — it supplies the combobox/listbox roles + roving focus + type-ahead, styled by `ScenarioPicker.css` binding `--color-*`/`--space-*`/`--radius-*` by name; mark the committed choice with `aria-current` (type-safe on a component's props under strict `tsc`, unlike a bare `data-*`) + a text label, never tint alone. Lucide not yet pulled (a not-color-alone glyph is an inline character). One runtime dep (`cmdk`), `npm audit` 0 — satisfies a11y "use shadcn Command, never hand-roll combobox". (scenario-suite-picker-start-stop chunk)
- 2026-06-26: When the webview renders the six verdict/report-state lamps, mirror `conductor-core/src/lamp.rs` — the SINGLE lamp-truth (its module doc names "desktop" as a reuse target alongside cli/Markdown). `ui/src/lamp.ts` holds a `Lamp` union + `LAMP_META` map carrying the exact spellings (`Pass`/`Fail`/`HOLD`/`Manual`/`Residual`/`Blocked` + the `[PASS]`…`[BLOCKED]` anchors testing.md keys E2E selectors on) + each state's design token + glyph; `StatusLamp` drives the glyph color from `LAMP_META.token` via inline `style={{ color: 'var(--token)' }}` — NOT a per-variant CSS class — so the token→state map stays single-sourced (a CSS copy would silently drift from `lamp.rs`). The glyph is an inline char (filled dot Pass/Hold/Fail, hollow ring Blocked, checkbox Manual, dashed ring Residual); the visible text label is the never-color-alone discriminator. REUSE `StatusLamp` (don't re-spell the lamp set) for the ch6 coverage-matrix + ch7 run-report views. The operator-pause dialog scaffold (`OperatorPauseDialog`) is `@radix-ui/react-alert-dialog` used directly (the cmdk-direct pattern above) — Radix supplies `alertdialog` role + aria-modal + focus-trap + Escape, styled by `OperatorPauseDialog.css` tokens **[corrected 2026-09-01: NOT focus-restore. This dialog opens from a Tauri `Channel`, so it has no Radix `Trigger` to return to and focus lands on `<body>` on close — measured twice, including after the invoker was made focusable. Restoration is app-borne: an explicit `onCloseAutoFocus` handing back a `restoreFocusTo` invoker the owner captured when the hold arrived, with that invoker kept focusable via `aria-disabled` rather than the native `disabled` attribute]**; its fade binds `--motion-micro` (no 200ms token exists). (component-primitives-library chunk)
- 2026-06-27: To surface a `conductor-core` STATIC classification/table in the webview, single-source it through a thin read-only `#[tauri::command]` that returns the core type directly — add `#[derive(Serialize)]` to the existing struct (`CapabilityRow` gained it; `CoverageMode` was already serde) and `invoke<T[]>('cmd')` it from React — NEVER re-author the table in TS (a hand-copied TS table silently drifts from the `conductor-core` source of truth; the data analogue of the lamp-mirror rule above). The command mirrors the `list_scenarios` shape (nullary, read-only, manual `tauri.command.*` span + `count`/`latency_ms`, `sanitize_error` edge) and needs no capability-ACL entry (app commands aren't ACL-gated). The verdict-first `RunRecord`→`Lamp` projection (`lampForRecord`, mirroring `conductor-core` `Lamp::for_record` arm-for-arm — `Blocked`/`KnownResidual` are state-driven and precede the verdict) lives in `ui/src/lamp.ts` beside `LAMP_META`, the same single-lamp-truth file. The coverage-matrix view sourced its 60 P-IDs this way; ch7's run-report view surfaces `RunRecord`s + reuses `lampForRecord` identically. (coverage-matrix-view chunk)
- 2026-06-27: A CONTROLLED Radix `AlertDialog` (its `open` driven by React state) fires BOTH the action/cancel button handler (`onProceed`/`onAbort`) AND `onOpenChange(false)` for a SINGLE Proceed/Abort/Escape — so a handler that delivers a decision once (e.g. `invoke('resolve_operator_hold', { decision })`) DOUBLE-fires. Guard the delivery with a `useRef` idempotency flag: set it true when the hold opens, early-return in the deliver fn unless it's set, then clear it; wire `onOpenChange={(open)=>{ if(!open) resolveHold('NoGo') }}` so Escape/overlay-dismiss resolves to the abort path cleanly (the button click's own handler wins the race; the redundant `onOpenChange` call then no-ops on the cleared ref — and the backend `HoldGate::deliver` take-once is idempotent too, so a double-post is harmless). The shipped `OperatorPauseDialog` (Radix alertdialog: role+focus-trap+Escape+restore) is wired this way in `App.tsx` — drive `open` from a `HoldPrompt | null` state + flip the titlebar `runState` to `'hold'`; reuse the scaffold, don't rebuild it. (operator-pause-go-no-go-dialog chunk)
- 2026-06-27: The desktop a11y/GUI E2E harness (`axe-core@4.12.0` via `@axe-core/webdriverio` + `lighthouse@13.0.3` + `colorjs.io@0.6.1` + `@crabnebula/tauri-driver@2.0.9` + `webdriverio@9` + `@wdio/*` + `tsx`, per a11y-plan §3.5) is DISPLAY-GATED — it runs ONLY on Linux+xvfb + a live Pulse (never the Windows dev host: every scenario is Blocked + the dialogs can't fire without Pulse), so it is authored + typecheck-clean but NOT in the always-on `agent-run run`/CI gate **[corrected 2026-09-01: the gate is DRIVER-availability, not display — the harness drove a real WebView2 session headfully on this Windows host, so "never the Windows dev host" is false; and "authored + typecheck-clean" was never evidence of anything, because the config could not LOAD on any host (`__dirname` in a `"type": "module"` package) while `typecheck:e2e` passed throughout. What genuinely still needs a live Pulse is the two specs whose subject (HOLD dialog / checklist) only exists in a driven run]**
- 2026-09-01: **A harness config that has never been EXECUTED is unverified, whatever its static gates say — and the first execution should be budgeted for several defects, not one.** Driving the shipped `wdio.conf.ts` for the first time (authored 2026-06-27, gated off by a platform assumption) surfaced FOUR independent blockers in one file, none visible to `tsc`: (1) `__dirname` in a `"type": "module"` package — the config could not load on ANY host, Linux included, yet `typecheck:e2e` passed for two months because tsc accepts it under the node types; (2) the bundle-embedding build needs `--features tauri/custom-protocol`, the profile being irrelevant; (3) no readiness synchronisation, so specs ran against an unmounted document — and `browser.waitUntil` treats a THROWING condition as fatal rather than retrying, so a wait whose probe throws while the frame initialises aborts instantly instead of waiting; (4) wdio v9 prefers BiDi when the driver advertises it, and script evaluation over BiDi answers `Page/Frame is not ready` indefinitely against the wry/WebView2 context while the identical calls over classic WebDriver succeed — set `wdio:enforceWebDriverClassic: true` in the capability. Corollary for diagnosis: when a driven page looks empty, dump the raw document (`GET /session/{id}/url` + `/source`) before theorising — that one read separated "the app did not render" from "our reader could not evaluate", which four failing specs could not. (the always-on a11y CI gate is the Epoch-10 "A11y CI gate" chunk). Setup gotchas: (1) keep the wdio specs in a `ui/test/` dir with their OWN `test/tsconfig.json` (`extends ../tsconfig.json`, `types: ["node","@wdio/globals/types","@wdio/mocha-framework"]`) — the app `tsconfig.json` `include` is `["src","vite.config.ts"]`, so `test/` + `wdio.conf.ts` fall OUTSIDE `npm run build`'s `tsc --noEmit` automatically (wdio globals never pollute the app type-graph); add a `typecheck:e2e` script (`tsc -p test/tsconfig.json`) to prove the specs compile with no display. (2) `tauri:options` is a WebDriver vendor capability outside wdio's typed `Capabilities` → assert it `as unknown as WebdriverIO.Capabilities` (justified comment). (3) `autoCompileOpts` is a wdio-v7 config property REMOVED in v9 (tsx auto-detects TS) — don't carry it over. colorjs.io reads `:root` token pairs from the running webview via `getComputedStyle` for WCAG-AA contrast (tokens by name, never literals). New devDeps gate on `npm audit --omit=dev` (dev-only transitive advisories accepted — see `security.md`). (desktop-a11y-harness-setup chunk)
