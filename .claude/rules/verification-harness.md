---
paths:
  - "scripts/agent-run.*"
  - "crates/conductor-cli/**"
  - "crates/conductor-verify/**"
  - "crates/**/tests/**"
---

# Verification Harness Rules

Path-scoped rules for the agent-driven verification harness — the `scripts/agent-run.*` driver, the CLI release-gate path, and the MCP read-back / preflight layer.

**Authoritative sources (bound):** `.andromeda/test-plan.md` §3 (Test Harness Contract) + `.andromeda/obs-plan.md` §3 (log format / service identity). The harness consumes the obs JSON log format; keep both sides in sync.

**Critical adaptation — Conductor has NO daemon and NO inbound listener.** `boot` is a preflight gate (not a daemon start), `status` reads disk artifacts (no HTTP/IPC endpoint), there is NO PID file, and there is no long-running heartbeat. Do not reintroduce daemon/PID/endpoint machinery.

## 5-command discipline (`scripts/agent-run.{sh,ps1}` — exactly 5, identical semantics across .sh and .ps1)
- **`boot`** — MCP preflight readiness gate: `conductor preflight --json` runs the rmcp `initialize` handshake (assert negotiated protocol `2024-11-05` + required-tool presence vs the pinned `contracts/` manifest + a data-dir canary round-trip). `ready:true` → exit 0; `ready:false` ⇒ every dependent scenario reports `Blocked` (never a silent downgrade). CI uses the rmcp stub (returns `ready:true`); the live-Pulse leg is `workflow_dispatch`/local only. Timeout: TWO derived budgets — (a) the in-process canary poll, `CONDUCTOR_PREFLIGHT_TIMEOUT` (default 30s) raised to the run contract's floor (`contracts/pulse-run-contract.toml` `[incident_formation].min_canary_poll_seconds`, which must outlast Pulse's L3 digest cadence), and (b) the wall-clock wrapper both shells apply around the CLI, derived per invocation from the same contract as `warmup_ms/1000 + poll + margin` — the warm-up pre-roll runs BEFORE the poll starts, so a wrapper set to the poll alone kills the run mid-poll. A missing contract term is a hard exit 2, never a silent default; a lowered env value clamps up to the floor. The stub leg still finishes in <1s on `CanaryPoll::immediate()`.
- **`run`** — `cargo nextest run --workspace --profile ci` + `cargo test --workspace --doc` + `cargo clippy --workspace --all-targets -- -D warnings`; scenario leg `conductor run <scenario|P-ID> --seed <s>` / `conductor suite`. Exit 0 = all `Pass`; non-zero = a hard `Fail` (nextest 100). Stage flags `--unit`/`--integration`/`--e2e`.
- **`status`** — read the Run-report envelope from `runs/<run_id>.jsonl` (`jq -e`) or the `runs.db` row (rusqlite bound-param SELECT). No `curl`/HTTP — there is no listener.
- **`cleanup`** — `rm -f runs/<run_id>.{jsonl,md}` + bound-param `DELETE FROM runs WHERE run_id=?1` + release the `:4317` port-occupier bind. Idempotent (re-run on a clean state is a no-op). NEVER touch Pulse (no process management).
- **`logs`** — read `runs/<run_id>.jsonl` (the emission journal — the left side of every SLO check; wall-clock `std::time` stamps) + sanitized stderr.

Do not add a 6th command without a test-plan amendment — the harness is small by design.

## Status envelope contract
- The Run-report envelope (`run_id`, `seed`, `scenario`, `p_ids`, `verdict ∈ {Pass,Fail,CalibrationRegion}`, `state ∈ {Pass,Fail,ManualCheck,KnownResidual,Blocked}`, `latency_ms`, `slo_tier`, `journal_emitted_at`, `read_back_observed_at`, `fingerprints`) is serialized identically into the Markdown report, the `runs.db` row, and each JSONL line.
- A `blocked` row populates identity fields only (measurement fields `null`); cross-run latency/percentile + P-036 fingerprint queries MUST exclude null-measurement rows.
- Artifacts MUST NOT leak absolute host paths or internal struct names.

## Headless invariant (agent-driven)
- The headless source-of-truth path is NEVER blocked on an interactive prompt — `inquire` operator-pause prompts are `isatty`-gated; when stdin is not a TTY the prompt is skipped per the non-interactive policy and the decision recorded to the artifact. An auth/interactive gate there silently breaks the release gate.
- Integration tests drive the same contract as production (rmcp stub over stdio; `Command::cargo_bin` for the cli) — no pixel-scraping, no UI automation depending on screen geometry.

## Determinism
- Same scenario+seed ⇒ same stream shape (seeded `conductor-timeline` + `start_paused` for scheduling). Journal stamps from `std::time::SystemTime`/`Instant`, never the virtual clock. Zero nextest retries.

## Session Additions
_This section is owned by `/wrap-session`. setup-project preserves content added here on re-run._
- 2026-06-21: rmcp 1.7.0 client — pin the handshake to Pulse's `2024-11-05` by passing a `ClientInfo` (it `impl`s `ClientHandler`) built with `.with_protocol_version(ProtocolVersion::V_2024_11_05)` AS the `serve()`/`serve_client()` service; the rmcp client default is `ProtocolVersion::LATEST`, so an unconfigured client silently negotiates UP — the exact mismatch the preflight guards. Read the negotiated version back via `RunningService::peer_info().protocol_version` (`RunningService` derefs to `Peer`, giving `call_tool`/`list_all_tools`). SEP-1319: param/result structs (`CallToolRequestParams`, `ClientInfo`=`InitializeRequestParams`, `Implementation`, `InitializeResult`) are `#[non_exhaustive]` → build via `::new()`/`with_*()`, never struct literals. (mcp-read-back-client chunk)
- 2026-06-21: rmcp 1.7.0 protocol negotiation is server-driven + lenient — the SERVER returns the LOWER of (client-requested, its own) version, negotiating a newer client request DOWN to its version (`service/server.rs`); the CLIENT accepts whatever `InitializeResult.protocol_version` comes back, never rejecting (`service/client.rs`). So `peer_info().protocol_version` equals the client's pinned `2024-11-05` whenever the server is ≥ that — a wrong-version mismatch is OBSERVABLE only when the server reports OLDER. To unit-test the preflight's `negotiated ≠ pinned ⇒ Blocked` branch deterministically, drive the mismatch from the contract manifest's `expected_protocol_version` (pin it to a value the healthy stub won't negotiate), NOT from the stub's reported version. A connect/`initialize` failure is reserved for genuine MCP-unreachable, which the gate maps to `Blocked` (never an `Err`). (preflight-readiness-gate chunk)
- 2026-06-21: For a no-boot-path-change chunk (e.g. a scenario-catalog config + fixture-test chunk), the agent-run smoke is `bash scripts/agent-run.sh run` (the release-gate path: workspace nextest + doctest + clippy), NOT `status`/`logs` — those require a `<run_id>` from a prior live run (`status` exits 2 on a missing arg, 1 on a missing journal), and no live run exists until the Epoch-8 CLI driver materializes scenario execution; `boot` likewise needs a live Pulse / rmcp stub. Plans for such chunks should list `run` as the smoke, not `status`. (hard-signals-scenarios chunk)
- 2026-06-23: To E2E-test the CLI `run`/`suite` verbs WITHOUT a live Pulse — hermetically, never spawning the real `andromeda-pulse-mcp` sidecar — force the read-back path unreachable by setting `ANDROMEDA_PULSE_DATA_DIR` to a value carrying an injection metacharacter (e.g. `pulse;injection`): `conductor-verify`'s `resolve_data_dir` rejects it (`VerifyError::DataDirRejected`) BEFORE `TokioChildProcess` spawns anything, so `ReadbackClient::connect` returns `Err` → the CLI pipeline maps it to a `Blocked` envelope. Deterministic on ANY host (independent of whether the sidecar binary is installed) and it exercises the REAL read-back-unreachable precondition, not a mock/skip. Assert the persisted envelope `state == "Blocked"` + exit 0 (Blocked is not a hard `Fail`); do NOT assert a measured verdict (that needs a live Pulse). Prefer this over relying on "no andromeda-pulse-mcp on PATH" — that is host-fragile (a dev host with Pulse installed would spawn the real sidecar mid-test). The CLI consumes the EXISTING hardened spawn (`ReadbackClient::connect`); it adds no spawn code of its own. (conductor-run-suite-report-verbs chunk, conductor-cli)
- 2026-06-27: **The read-back client is now HAND-ROLLED line-delimited JSON-RPC over the sidecar's stdio** (`conductor-verify/src/jsonrpc.rs` + `client.rs`) — **rmcp was REMOVED**. This SUPERSEDES the 2026-06-21 rmcp-client-pinning notes above (rmcp is no longer a dependency): rmcp's typed `call_tool` deserializes the JSON-RPC `result` into the typed `ServerResult` enum and rejects Pulse's RAW tool payload — Pulse returns `{items,…}` directly as `result`, NOT the MCP `{content:[…]}` envelope (its sidecar is hand-rolled, non-MCP-compliant for `tools/call`) — as `ServiceError::UnexpectedResponse` on EVERY live call (verified rmcp 1.7 `service/client.rs:282-327`; no raw-result escape). The hand-rolled client parses each response line to `serde_json::Value`; version negotiation = reading the `initialize` result's `protocolVersion` (still pinned `2024-11-05` via the manifest); the version-mismatch + hardened-spawn + bounded-decode invariants all still hold. The test stub (`stub_pulse_mcp` + `tests/common/mod.rs`) is likewise hand-rolled JSON-RPC emitting Pulse's raw shapes — the old rmcp-SERVER stub HID this bug (it wrapped results in `CallToolResult`, which the live sidecar never does). `run_preflight`'s `_ => Failed` catch-all was un-masked: a real call/transport/JSON-RPC error → distinct `Blocked` precondition ("MCP read-back call failed: …") vs a marker-absent result ("incident not found in corpus"). (mcp-read-back-result-shape-adapter chunk)
- 2026-06-23: `conductor preflight` (the `agent-run boot` entrypoint) is a go/no-go GATE — it exits **0 iff `ready:true`** and **non-zero on a `ready:false` Blocked precondition** (the full `ReadyState` JSON still prints to stdout; a real harness fault like a missing/bad contract manifest is a separate non-zero `Err` via `main`'s sanitized edge). This is a deliberate EXCEPTION to the run-path rule in the entry above + testing.md ("`Blocked`/`ManualCheck`/… are reported states → exit 0; only a hard `Fail` exits non-zero"): that rule governs the SCENARIO-run verdict surface (`conductor run`), whereas the preflight gate's exit code IS the precondition go/no-go signal (test-plan §3: "exit 0 on ready:true, non-zero otherwise"). CI-safe because CI dogfoods `agent-run run`, not `boot` (with no live Pulse `boot` is always `ready:false` → non-zero, by design). An agent gates with `if agent-run boot; then run…; fi` or by parsing stdout `.ready`. User-decided at /andromeda-phase P4; pre-applies to the Epoch-9 Tauri boot/readiness surface. (5-command-agent-run-harness chunk)
- 2026-06-23: Unit-test the isatty-gated operator-pause resolver at the GATE + DISPATCH, never via a real TTY prompt. Assert `CliResolver::select(None)` returns the `Headless` variant under nextest (nextest captures stdin, so `std::io::IsTerminal` is false → the never-block default fires deterministically in CI), and assert dispatch by resolving a `HoldPoint` through an injected `HeadlessResolver::proceed()`/`abort()` (→ `Decision::Go`/`NoGo`). NEVER spawn a real `inquire` confirm in a test — there is no pty and it would hang against the zero-retry bar. The live interactive prompt is ALSO unreachable in hermetic CI: the hold sits behind a READY preflight gate (`execute_scenario` returns `Blocked` before the hold when `!pf.ready`), so the `ANDROMEDA_PULSE_DATA_DIR=pulse;injection` E2E runs (the entry above) are Blocked-first and never reach it — so the headless-never-block proof is the unit `select_off_tty_is_headless`, NOT a new E2E test (the live prompt is an Epoch-10 operator-gated leg). (isatty-gated-operator-pause chunk, conductor-cli)
- 2026-06-27: **Pulse's incident creation is LLM-in-the-loop NON-DETERMINISTIC — live incident-readback verification cannot meet Conductor's zero-flakiness bar against the real LLM** (verified live). Write-path: OTLP → L1 → L2 RetryStorm cue (deterministic, ≥5 same fingerprint/30s) → L3 digest (20-60s cadence) → **L4 llama.cpp Llama-3.2-3B inference decides Dismiss/Severity** → incident; an incident forms ONLY if the 3B LLM surfaces it (behind a degraded-mode backoff; never cleanly reproduced even in Pulse's own dev). So the canary "emit telemetry → Pulse raises an incident → read it back" round-trip reaches `ready:true` only when the LLM surfaces the canary — it MUST NOT be a hard CI gate, and live family incident-readback (Part B) cannot be deterministic. The canary BRIDGE is shipped + CI-green against the stub and is CORRECT: emit a unique fingerprint-storm (`conductor-run::emit_canary` via conductor-emit `exception_trace_request`+`fingerprint`) → bounded poll `query_incident_list` → assert the fingerprint via `retrieve_telemetry_slice.fingerprint_refs` (Pulse SCRUBS incident titles, so the fingerprint, not a title echo, is the fidelity carrier); `CanaryPoll` budget keeps stub tests fast; failure modes → distinct `Blocked` preconditions. Live it correctly returns `Blocked: incident not found in corpus` because Pulse creates no incident (ingest works — `conductor-canary` reaches `service_registry`), not because the bridge is wrong. **POSTURE DECISION — SETTLED 2026-08-09** (was: OPEN, pending, cross-codebase): how Conductor deterministically verifies an LLM-in-the-loop SUT. The leading option landed Pulse-side — `ANDROMEDA_PULSE_L4_DETERMINISTIC=true` swaps a canned `L4Output` for the inference, so the live legs become reproducible and `Live-Pulse E2E proof` Part B is unblocked as a determinism question. **Its cost is now a recorded deferral, not a silence:** under deterministic L4 every live leg exercises the plumbing and never the interpretation, so **"Conductor green" does NOT mean Pulse's interpretation is trustworthy** — it means the pipeline carried a canned answer end to end. The real-model leg (deterministic mode OFF, a known root cause injected, the top hypothesis asserted to identify it) is **owned by a conductor-0.3.0 entry**, pinned in `.andromeda/residuals.md`, and its four diagnostic-quality capabilities (P-031/P-033/P-034/P-044) sit in `conductor_core::UNBACKED_AUTO` where `check_scenario_backing` asserts the gap rather than leaving it remembered. (live-pulse-e2e-proof chunk; extended by interpretation-correctness-posture) **Extended 2026-08-10 (pulse-run-contract) — there is a precondition UPSTREAM of the L2 cue, and it silently voids the whole write path: Pulse holds no baseline for a service it has never seen, so a storm emitted as a service's FIRST traffic is never evaluated and `cues_emitted` stays 0 no matter how well-shaped the storm is.** The canary storm is exactly that case — a fresh `conductor-canary` service — which is why the three-arm workspace-key probe saw `services_in_bootstrap: 1` / `services_ready: 0` throughout with deterministic L4 confirmed active and a shared data-dir: the storm shape was already correct (6 identical fingerprints, over the ≥5/30s floor) and was never the blocker. Drive a warm-up pre-roll of benign traffic and wait out the bootstrap window BEFORE the counted storm, then allow a poll budget that outlasts L3's 20-60s digest cadence with L4 behind it — both are recorded as `[incident_formation]` terms in `contracts/pulse-run-contract.toml` and applied by the preflight canary. Corollary for diagnosis: `cues_emitted: 0` means the cue evaluator never CONSIDERED the service, which is a different fault from a cue that fired and produced no incident — check `services_ready` before suspecting storm shape. **Extended 2026-08-13 (first-live-green-preflight): the pre-roll prescribed above CANNOT work, and the reason is a constant three orders of magnitude away from what it assumed.** Pulse gates cue evaluation on `BootstrapState::Ready` (`crates/triage/src/cue/evaluate.rs:164`), which needs `now − first_observed_unix_nanos ≥ BOOTSTRAP_WINDOW_SECONDS = 3_600` **per service, wall-clock** (`baseline/activity_floor.rs:33,173-183`), and `baseline_state` persists 0 rows so every `pulse-app` restart resets the anchor — so a 45s (or any seconds-scale) warm-up clears nothing, and "wait out the bootstrap window" means waiting an hour of real time per service with no restart. The DIAGNOSIS still holds exactly as written (no baseline ⇒ the cue evaluator never considers the service); only the remedy is disproved. Reaching a live incident needs a Pulse-side change — a test-mode bootstrap override, or populating the already-present `baseline_state` table — not a Conductor-side retune. Two corollaries measured the same run: `storms_detected_total: 0` is NOT explained by this gate (the detector is fed at the buffer boundary, upstream of it — diagnose it separately via `tracked_fingerprints_count`), and a live leg needs a FIFTH recipe item beyond the four above — `ANDROMEDA_PULSE_MCP_ENABLED=true` in **Conductor's own** environment, inherited by the sidecar (`spawn.rs` passes only the data dir via `.env(...)`), without which every arm silently measures the read-back-unreachable path. **Extended 2026-08-14 (canary-fingerprint-feed-capture), two corrections to the items above. (a) A SIXTH recipe item, and its form is load-bearing: `RUST_LOG=info,conductor_emit=debug` to surface the `emit.batch` wire-shape witness — a BARE per-target directive (`conductor_emit=debug`) REPLACES the default rather than adding to it, filtering every other target out; measured across three forms, the bare one fails the CLI's own `agent_mode_routes_self_obs_to_the_log_file_not_stderr` while the paired one passes. Never set either form for an invocation that also runs the test suite — the environment reaches the runner's child processes, so the witness belongs on the `boot` leg. (b) The diagnosis clause above needs sharpening: `tracked_fingerprints_count` alone CANNOT carry it, because a windowed GAUGE cannot witness an event's absence — it is a 60s window over DISTINCT fingerprints sampled at a 15s tick AFTER eviction, so a working six-occurrence identical-fingerprint storm reads `1` (never 6) and a late sample reads `0` on a perfectly healthy path. Discriminate with the CUMULATIVE counters beside it (`storms_detected_total`, `fingerprints_evicted_total`), which no window can erase. And verify a counter's semantics IN SOURCE before inferring from its shape: the ingest heartbeat's `span_count` is a cumulative `fetch_add`, which is the only reason `1 → 2 → 3 → 9` and holding proves all nine spans arrived — read as a per-tick delta it would have meant nothing.**
- 2026-08-10: A live-Pulse probe has two HOST preconditions a plan's Test Commands will not state, and both decide whether the evidence means anything. (1) The MCP sidecar `andromeda-pulse-mcp` must be BUILT and on PATH (`cargo build -p mcp-server --bin andromeda-pulse-mcp --features mcp-server` in the Pulse repo) — unbuilt, `ReadbackClient::connect` fails and EVERY arm returns the read-back-unreachable precondition, so the probe silently measures the wrong path and produces worthless evidence that still looks like a result. Check the binary before trusting any arm. (2) `pulse-app` emits Specta/TauRPC bindings relative to ITS cwd, so launching it from Conductor's repo root writes a foreign `/ui/src/bindings/` tree into this repo — `/ui/` is now gitignored (recurrence-proof, since it is fallout no Conductor code produces), but prefer launching from a cwd outside the repo. Neither fact is Conductor's code; both are probe-environment facts that gate the probe's validity. (workspace-key-divergence-probe chunk) **Extended 2026-08-14 (canary-fingerprint-feed-capture): precondition (1) is stronger than "the arm measures the wrong path" — with the sidecar unreachable the preflight blocks at read-back BEFORE the canary gate runs, so the canary NEVER EMITS AT ALL. Measured: a scenario run produced a fresh 16-line self-obs artifact with ZERO `emit.batch` spans. The consequence for evidence-reading is the trap: a MISSING wire-shape witness line is evidence that the leg never ran, NOT evidence about what Conductor put on the wire — the two are indistinguishable in the artifact and only the sidecar's reachability separates them. Check the binary before drawing any conclusion from an absence.**
- 2026-08-11: **An artifact-backed check must name the FULL firing invocation and verify the artifact is fresh for THIS run.** `scripts/agent-run.sh run` always executes nextest + doctest + clippy, but fires its SCENARIO leg — the only leg that runs the CLI with `--agent-mode`, and therefore the only one that rewrites `logs/agent-latest.jsonl` — ONLY under a `SCENARIO=<name|P-ID> [SEED=<n>]` env prefix. So a plan listing the bare verb as an artifact's producer verifies nothing: the bare run leaves any prior artifact untouched, and the check reads the PREVIOUS chunk's file as if it were this run's — a pass that proves only that someone once produced a file. Name the env prefix in the command, and confirm freshness (mtime, or a `run_id` the artifact must carry) before drawing any conclusion from it. Same trap for any harness verb whose producing leg is conditional. (faithful-emission-dispatcher chunk — the first smoke pass inspected a six-hour-stale artifact from the previous chunk and would have reported a green criterion on it.)
