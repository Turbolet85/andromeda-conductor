# Session Handoff

**Last Updated:** 2026-06-23T16:33:02Z
**Branch:** build/conductor-0.1.0
**Status:** clean
**Last Commit:** 2026-06-23-scrub-pipeline-degraded-report-surface-scenarios — feat: scrub/pipeline/degraded/report-surface scenarios (P-035, P-037, P-045, P-047..P-056) — closes Epoch 7 catalog, zero model change (conductor-core)

## Position
- Done: **2026-06-23-scrub-pipeline-degraded-report-surface-scenarios** — **Epoch 7 (Scenario catalog) ch8/8 — CLOSES Epoch 7.** 6 scenario TOMLs over 9 drivable P-IDs: `pii-scrub` (P-035/047/048 — Hard `Absent` sentinels + `Contains` structural), `findings-counter-refresh` (P-045 — Hard `CountAtLeast`), `threshold-hot-reload` (P-055/056 — MIXED: P-056 Hard `Absent` + P-055 declare-only), `report-render-surface` (P-037), `cadence-config` (P-052), `degraded-mode-report` (P-053 — declare-only KnownResidual). 4 StaticOnly (P-049/050/051/054) → no TOML (classified). Zero model change (8th consecutive Epoch-7 chunk).
- Next: **Epoch 8 (CLI surface) ch1 — conductor run/suite/report verbs (clap CLI over current_thread bootstrap)** → `/andromeda-phase` to promote + plan. (First chunk of Epoch 8.)

## Work done
6 NEW `scenarios/*.toml`; 1 MOD (`conductor-core/src/scenario.rs` — tests only: +5 fns / +12 rstest cases: 6-case loader [NO blanket `is_empty` — mixed-shape], 3-case declare-only guard, pii-scrub `Absent`+`Contains` guard, findings-counter+threshold Hard guard, suite mixed-shape guard). Gates: conductor-core **163/163** (151→163) · workspace **368/368** (356→368) · clippy `-D` clean · doctest 0 · `agent-run.sh run` exit 0. Goldens **UNCHANGED** (seeds 4317035/037/045/052/053/055 feed none; only 424242/7 feed goldens). Code-graph 1030n/4394e.

## Drift resolved
**drift = 0.** All 7 doc-detectors returned `proposals: []` (cleaner than constellation's 1-dismissed). Playbook pre-emptions held WITHOUT firing: rule 49 (scenario TOMLs + read-back/detector-output tokens — incl. `@example.com`/`sk_live_`/`RetroactiveReeval`/`user.email` — are content in the registered `scenarios/` category), rule 55 (D-obs-instrumentation on scenario-config — no runtime op), rule 46 (plan↔plan bind on pre-existing inconsistency), rule 52 (`CountAtLeast` Hard). 0 amendments, 0 escalations, 0 cascade, 0 playbook/drift-base additions.

## Notes
- **Key decisions:** (1) **P4 Q1** outcome-coherent grouping → 6 files. (2) **P4 Q2** Hard where a deterministic read-back token exists, else declare-only. (3) **Zero model change held (8th consecutive Epoch-7 chunk)** — all 6 realized via existing `ComparisonKind`/`ClaimClass` + `#[serde(default)]` empty `expected`. (4) **First catalog `Absent` for PII-scrub; first MIXED Hard+declare-only single file** (threshold-hot-reload — P-056 Hard `Absent` + P-055 comment-only timing); **first degraded_mode KnownResidual** (declare-only + documented; the residual is the `degraded_mode` CONTRACT per arch §Standard Contracts, vs P-032's `recent_commits` stub). (5) **pii-scrub sentinels:** assert `Absent` on the STABLE generator affixes (`@example.com`/`sk_live_`/`Bearer `/`password=`), NOT the seed-varying full `PiiCorpus` values; `Contains "user.email"` (retained `field_key`) for structure-preserved — a DISTINCT token. JWT/credit-card/SSN have no stable literal → Epoch-8. (6) **P-056 no-fire token `RetroactiveReeval`** is a non-candidate marker (can't collide with the legitimately-surfaced prospective candidate). (7) **StaticOnly four → no TOML** (coverage complete via `coverage.rs` classification; authoring a live scenario for a static claim is wrong, test-plan §11).
- **Curation:** 1 Tier-2 (`testing.md` — the two `Absent`-token-choice traps: stable-sentinel-of-a-seeded-value for scrubbers + non-candidate-marker for no-fire tokens). Filtered: degraded_mode KnownResidual (dup of 2026-06-22 producer-assigned entry), MIXED-single-file / StaticOnly-no-scenario (low-signal / obvious-from-classification), Epoch-7-closed milestone (status, not a rule). 0 conflicts, 0 deferred.
- **Follow-up (carried):** (a) `coverage-matrix.md` not yet at repo root (Epoch-8 cli). (b) **test-plan §3 ↔ obs-plan §3 doc-reconcile** still deferred — D-tests-obs-harness returned CLEAN this chunk (did NOT misfire; the report's "harness/envelope unchanged" framing held), so the pre-existing §3↔§3 gap remains a dedicated-pass follow-up (test-plan §3 is the OWNER per observability.md). (g) **Epoch-8 calibration points for this family:** per-tier SLO MEASUREMENTS (render <2s / counter ≤1s / reload <2s — TOMLs declare only the tier); the pii-scrub exact post-scrub form + the inferred `user.email`/`RetroactiveReeval` tokens; the degraded_mode read-back wiring + Fail→KnownResidual routing + P-053 residual-registry entry; the cadence L4-rate-match measurement; the findings-counter ≤1s refresh latency. (c/e/f) `report.generate` obs span · `opentelemetry-proto default-features=false` trim · Epoch-8/9 reuse of `coverage_matrix()`/`Lamp::for_record`.
- **Epoch 7 (Scenario catalog) CLOSED** — every drivable catalog P-ID now has a scenario; the coverage matrix (all 60 P-IDs) is unchanged.
- **Last failed command:** none.
