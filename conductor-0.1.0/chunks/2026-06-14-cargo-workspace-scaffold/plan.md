# Plan — 2026-06-14-cargo-workspace-scaffold: "Cargo workspace scaffold"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not edit during an /implement run._

## Metadata
- **Marker:** 2026-06-14-cargo-workspace-scaffold · **Version:** conductor-0.1.0 · **Generated:** 2026-06-14 (UTC)
- **Epoch:** 1 — Foundation · **Anchor:** `chunks/2026-06-14-cargo-workspace-scaffold/scope.md`

## Goal
Create the compiling skeleton of the crate-per-seam Cargo workspace — the structural foundation every later chunk extends. This materializes the 8-crate topology the architecture mandates (per arch §Module Boundaries + §Inherited Defaults Workspace crates): six library seams + two binary crates under `crates/`, with `Cargo.toml` dependency edges that ARE the architecture — only seam→core and bin→seam edges are declared, so a forbidden cross-seam `use` fails to compile (per arch §Design Philosophy "compiler-enforced module seams").

The root manifest centralizes every architecture-pinned version in `[workspace.dependencies]` as the single source of truth (per arch §Infrastructure Patterns Build system; echoed by security/obs extracts), and `rust-toolchain.toml` pins the toolchain to ≥1.94.1 — the security-plan's required bump over arch's MSRV 1.88.0 to clear tar-rs CVE-2026-33056 (per security-plan §Dependency Security). `tauri` is pinned ≥2.10.3 (origin-confusion CVE-2026-42184) even though no crate references it until Epoch 9. `Cargo.lock` is generated and committed (binary workspace → reproducible builds + deterministic `cargo-audit`).

This chunk is a fast green skeleton: NO feature logic, NO shared-type bodies (`conductor-core`'s `Verdict`/`ReportState`/scenario model are the NEXT chunk), NO logger/CI/Tauri-build wiring. `conductor-core` is a minimal compiling lib; each seam + bin is a minimal placeholder that compiles.

## Implementation Steps
1. **Root workspace manifest** (`Cargo.toml`): declare `[workspace]` `resolver = "2"` (or "3" for edition 2024) + `members` = the 8 `crates/conductor-*`. Add `[workspace.package]` with `edition = "2024"`, `rust-version = "1.94.1"`, `version = "0.1.0"`, shared `license`/`repository` as desired. Add `[workspace.dependencies]` centralizing the arch-pinned versions (see touchpoints) — exact pins, no floating ranges (per security-plan §Dependency Security).
2. **Toolchain pin** (`rust-toolchain.toml`): `[toolchain] channel = "1.94.1"` (or a dated stable ≥1.94.1), `components = ["clippy", "rustfmt"]`. Confirms edition-2024 capability (per security-plan §Dependency Security + scope).
3. **`conductor-core`** (`crates/conductor-core/`): minimal lib (`Cargo.toml` using `version.workspace = true` / `edition.workspace = true`; `src/lib.rs` empty or a single placeholder item so it compiles). NO `Verdict`/`ReportState` bodies — deferred to the next chunk.
4. **Five seam libs** (`conductor-timeline`, `-emit`, `-faults`, `-verify`, `-report`): each a minimal lib with `Cargo.toml` declaring `conductor-core = { path = "../conductor-core" }` (the seam→core edge) and a placeholder `src/lib.rs`. Do NOT declare any seam→seam edge.
5. **`conductor-cli`** (`crates/conductor-cli/`): a `bin` crate (the `agent-run` binary) depending on `conductor-core`; minimal `src/main.rs` (`fn main()` placeholder). The `#[tokio::main(flavor = "current_thread")]` body + clap verbs come in the CLI epoch — here it just compiles (per arch §Established Decisions [Async Runtime Flavor], deferred).
6. **`conductor-tauri`** (`crates/conductor-tauri/`): a minimal `bin` placeholder depending on `conductor-core`; `src/main.rs` `fn main()`. Do NOT add the `tauri` build pipeline (`build.rs`, `tauri.conf.json`, capabilities, the React webview) — that is Epoch 9 (Desktop control panel). The `tauri` ≥2.10.3 pin sits unused in `[workspace.dependencies]` for now.
7. **Lockfile**: run `cargo generate-lockfile` (or `cargo build`) to produce `Cargo.lock`; ensure it is committed (`.gitignore` already does NOT ignore it).
8. **Verify** the green skeleton: `cargo build --workspace` succeeds; the dependency graph has only seam→core + bin→seam edges; no OTel SDK is present in the tree.

## Codebase touchpoints
### New files
- `Cargo.toml` — root workspace manifest: `[workspace]` members (8) + `[workspace.package]` (edition 2024, rust-version 1.94.1, version 0.1.0) + `[workspace.dependencies]` pins: `tokio = "1.48"`, `tonic = "0.14.6"`, `tonic-prost = "0.14.6"`, `prost = "0.14"`, `opentelemetry-proto = "0.32.0"`, `rmcp = "1.7.0"`, `rusqlite = { version = "0.38.0", features = ["bundled"] }`, `garde = "0.23.0"`, `serde = { version = "1", features = ["derive"] }`, `thiserror = "2.0.18"`, `anyhow = "1.0.102"`, `tracing = "0.1.44"`, `tracing-subscriber = "0.3.23"`, `tauri = "2.10.3"` (versions per arch §Stack + security/obs extract pins; refine exact patch at implement against latest-in-range).
- `rust-toolchain.toml` — `[toolchain] channel ≥ 1.94.1`, `components = ["clippy","rustfmt"]`.
- `crates/conductor-core/Cargo.toml` + `crates/conductor-core/src/lib.rs` — minimal lib (no shared-type bodies).
- `crates/conductor-timeline/{Cargo.toml,src/lib.rs}` — lib; dep `conductor-core` (path).
- `crates/conductor-emit/{Cargo.toml,src/lib.rs}` — lib; dep `conductor-core`.
- `crates/conductor-faults/{Cargo.toml,src/lib.rs}` — lib; dep `conductor-core`.
- `crates/conductor-verify/{Cargo.toml,src/lib.rs}` — lib; dep `conductor-core`.
- `crates/conductor-report/{Cargo.toml,src/lib.rs}` — lib; dep `conductor-core`.
- `crates/conductor-cli/{Cargo.toml,src/main.rs}` — bin (`agent-run`); dep `conductor-core`.
- `crates/conductor-tauri/{Cargo.toml,src/main.rs}` — minimal bin placeholder; dep `conductor-core` (NO tauri build wiring).
- `Cargo.lock` — generated, committed.

### Files to modify
- none — cold-start (per research.md). `.gitignore` already tracks `Cargo.lock` + ignores `/target/`; no change needed.

## Test Commands
```bash
cargo build --workspace                 # green-skeleton gate (primary)
cargo test --workspace                  # compiles all targets + runs 0 tests (built-in; cargo-nextest is installed in a later Foundation chunk)
cargo tree --workspace | grep -i opentelemetry_sdk   # MUST print nothing (obs: no OTel SDK for self-observation)
# Harness smoke (scripts/agent-run.sh) is NOT a gate this chunk — boot=preflight + run=nextest depend on
# later chunks; `cargo build --workspace` is the gate. (test-plan §3 / §10.)
```

## Acceptance Criteria
- (arch) All 8 crates exist with the locked names `conductor-{core,timeline,emit,faults,verify,report,cli,tauri}` and correct crate types — 6 lib, 2 bin (per arch §Occupied Resources).
- (arch/security) `rust-toolchain.toml` pins ≥1.94.1 and the root manifest sets `edition = "2024"` workspace-wide (per arch §Stack and Technologies + security-plan §Dependency Security).
- (security) `tauri` is pinned ≥2.10.3 in `[workspace.dependencies]` (per security-plan §Dependency Security — CVE-2026-42184).
- (arch) `[workspace.dependencies]` centralizes the architecture-pinned versions; no floating semver (per arch §Infrastructure Patterns + security-plan §Dependency Security).
- (arch) Dependency edges are seam→core and bin→seam only; no seam→seam edge is declared, so a cross-seam `use` does not resolve (compiler-enforced seam isolation, per arch §Module Boundaries + §Design Philosophy).
- (obs) No OTel SDK in the dependency tree — `cargo tree --workspace | grep -i opentelemetry_sdk` prints nothing (per obs-plan §11).
- (tests/arch) `cargo build --workspace` returns 0 — the green skeleton (per test-plan §10 + arch §Acceptance).
- (security/tests) `Cargo.lock` is committed and not suppressed by `.gitignore` (per security-plan §Dependency Security + test-plan §9).

## Provenance
- Spec anchors: arch §Module Boundaries / §Design Philosophy / §Stack and Technologies / §Occupied Resources / §Infrastructure Patterns; security-plan §Dependency Security; obs-plan §3 / §11; test-plan §1 / §3 / §9 / §10.
- Extracts: `runs/2026-06-14T23-12-10-phase/{arch,security,tests,obs}.md` (design/layouts/a11y → No domain coverage — pure infra chunk).
- Research files inspected: `.andromeda/context/{dependency-tree,api-surface}.md`, `.gitignore`.

## Implementation notes
- **`[workspace.dependencies]` breadth:** declare all arch-pinned versions now even though most member crates won't reference them until later chunks — declaring an unused workspace dependency is valid and is the intended single-source-of-truth pattern. Member `Cargo.toml`s reference them as `{dep}.workspace = true` when their feature chunk lands.
- **conductor-tauri depth:** keep it a minimal compiling bin; pulling Tauri's build pipeline in now would make the scaffold slow and entangle Epoch 9 work.
- **Forbidden-edge spot-check (optional, revert after):** to confirm seam isolation, temporarily add a `use conductor_emit::…;` inside `conductor-timeline` without the Cargo edge and confirm it fails to compile, then revert. (arch names `cargo-modules`/`cargo-rail` as optional auditors — not required this chunk.)
- **bundled SQLite:** `rusqlite` with `features = ["bundled"]` compiles SQLite C from source → expect a longer first `cargo build` (per `.claude/docs/gotchas.md`).
- **Edition-2024 resolver:** edition 2024 wants the v3 feature resolver; set it at the workspace root.
