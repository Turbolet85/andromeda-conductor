# Codebase Research — 2026-06-14-cargo-workspace-scaffold

## Scope
- **Depth:** minimal (cold-start) · **Reads:** living docs (in-context) · **Globs/Greps:** 1 repo-state probe
- **Cold-start:** the codebase is empty of Rust — no `Cargo.toml`, `rust-toolchain.toml`, `crates/`, or `.rs` files exist. The plan derives entirely from the P2 extracts + arch §Inherited Defaults Workspace crates / §Directory structure. This chunk CREATES the workspace from scratch.

## Files inspected
- `.andromeda/context/dependency-tree.md` (full) — already documents the planned 8-crate structure + the seam→core dependency edges this chunk materializes (seeded by setup-project; this chunk makes it real).
- `.andromeda/context/api-surface.md` (full) — confirms no public API exists yet; `conductor-core` will later export `Verdict`/`ReportState` (NEXT chunk, not here).
- `.gitignore` (full) — already carries the Rust fragment: `/target/` ignored, `Cargo.lock` deliberately NOT ignored (binary-workspace commit policy), `/runs/` ignored. No `.gitignore` change needed this chunk.
- Repo-state probe — top level is `.andromeda/ .claude/ .gitignore CLAUDE.md conductor-0.1.0/ scripts/` only.

## Patterns detected
- **Crate-per-seam topology already documented** (`.andromeda/context/dependency-tree.md`): 8 crates, `conductor-core` is the base, seam libs + bins depend on it, bins compose — the file the first wrap-session will reconcile against actual `Cargo.toml` edges.
- **Cargo.lock commit policy already set** (`.gitignore`): the binary-workspace decision (commit `Cargo.lock`) is encoded; the scaffold must generate + commit it.

## Conventions to follow
- **snake_case source / `conductor-<seam>` kebab crate names** (`.claude/docs/conventions.md`; arch §Conventions) — file + crate naming.
- **`crates/` workspace layout** (arch §Infrastructure Patterns directory tree) — all 8 members under `crates/`.
- **Centralized `[workspace.dependencies]`** (arch §Infrastructure Patterns Build system; all 3 in-domain extracts) — pin versions once; seam crates reference `.workspace = true` later.

## New files to create
- `Cargo.toml` — root workspace manifest: `[workspace]` members (8) + `[workspace.package]` (edition 2024, rust-version, version) + `[workspace.dependencies]` (centralized pins: tokio 1.48.x, tonic 0.14.6, tonic-prost 0.14.6, prost 0.14, opentelemetry-proto 0.32.0, rmcp 1.7.0, rusqlite 0.38.0, garde 0.23.0, serde 1.0.x, thiserror 2.0.18, anyhow 1.0.102, tracing 0.1.44, tracing-subscriber 0.3.23, tauri ≥2.10.3).
- `rust-toolchain.toml` — pin channel ≥1.94.1 (security required bump; tar-rs CVE-2026-33056).
- `crates/conductor-core/{Cargo.toml,src/lib.rs}` — minimal compiling lib (NO shared-type bodies yet — next chunk).
- `crates/conductor-{timeline,emit,faults,verify,report}/{Cargo.toml,src/lib.rs}` — minimal libs, each depending on `conductor-core`.
- `crates/conductor-cli/{Cargo.toml,src/main.rs}` — minimal bin depending on `conductor-core` (`agent-run` binary; full `#[tokio::main(current_thread)]` body comes in the CLI epoch).
- `crates/conductor-tauri/{Cargo.toml,src/main.rs}` — minimal bin placeholder depending on `conductor-core` (NO `tauri` build pipeline / conf / capabilities yet — that is the Desktop epoch; the `tauri` ≥2.10.3 pin lives in `[workspace.dependencies]` unused for now).
- `Cargo.lock` — generated by `cargo build`/`generate-lockfile`, committed.

## Files to modify
- none (cold-start). `.gitignore` already correct (tracks `Cargo.lock`, ignores `/target/`).

## Open questions
- **`[workspace.dependencies]` breadth** — resolved at plan: centralize ALL arch-pinned versions now (arch/security/obs extracts all call for it) even though most are referenced by member crates only in later chunks; declaring an unused workspace dependency is valid and is the intended single-source-of-truth pattern.
- **conductor-tauri scaffold depth** — resolved at plan: minimal compiling bin placeholder only; the heavy Tauri build pipeline (build.rs, `tauri.conf.json`, capabilities, the React webview) is deferred to Epoch 9 (Desktop control panel). Keeps the scaffold a fast green skeleton.
