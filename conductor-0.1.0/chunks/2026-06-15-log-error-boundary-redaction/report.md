# Report — 2026-06-15-log-error-boundary-redaction

**Chunk:** Log + error-boundary redaction — tracing field-allowlist + anyhow-edge sanitization (no host-paths/struct-names/stack-traces)
**Date:** 2026-06-15T18:54:57Z
**Commits:** (none yet — this wrap creates the chunk commit)

## Changes (structured — detectors read this)
- **Files:** `crates/conductor-core/src/redact.rs` (new) · `crates/conductor-core/src/obs.rs` · `crates/conductor-core/src/lib.rs`
- **Symbols / APIs:** new public in `conductor-core`:
  - `fn redact_value(value: &str) -> Cow<'_, str>` — masks absolute host-path file references → `<redacted>`, preserving the rest of the text; `Cow::Borrowed` on the clean path. Std-only (no `regex`).
  - `fn sanitize_error(err: &dyn std::error::Error) -> String` — single-line scrubbed `Display` text (never `Debug`; no struct-field dump, no `{:?}` backtrace); kept **anyhow-free** (arch §Error handling: anyhow only at the binary edges).
  - `fn is_allowlisted(name: &str) -> bool` — **`pub(crate)`**, not exported; the field-name allowlist.
  - **No** IPC methods / HTTP endpoints / ports / sockets / env vars added; **no env var read added** (the redaction config is hardcoded allowlists per arch — not env-driven).
- **Crates / modules:** added module `conductor-core::redact` (private `mod` + flat `pub use redact::{redact_value, sanitize_error}`); no new workspace crate.
- **Dependencies:** **NONE added** — redaction is std-only (`std::borrow::Cow` + string ops); no `regex`, no new crate. `Cargo.lock` un-drifted (50 locked deps, unchanged). cli/tauri `Cargo.toml`s untouched (no `anyhow` wired — lean decision).
- **Schema / config:** the self-obs JSON **log line** now enforces, at the subscriber processor stage (`JsonObsLayer`/`JsonVisitor`): (a) a **field-name allowlist** — permitted = the 7 identity fields + `message` + bounded self-obs fields (`phase`, `count`, `panic`, `location`) + the reserved Run-report envelope fields (obs-plan §6); a non-allowlisted event field NAME is dropped; (b) **value redaction** — every string value masking an absolute host-path file reference → `<redacted>`. This refines the *which-fields-may-appear* + redaction contract of the existing self-obs line; the base field set and the Run-report envelope are unchanged. No migration.
- **Coverage of new surfaces:**
  - `conductor-core::redact` (redaction primitive) → validation n/a (internal sanitizer, NOT an external-input trust boundary) · instrumentation n/a · PII **redacted✓** (host-path masking + field-allowlist; the realization of the artifact-hygiene invariant) · tests **unit✓ (8)** · a11y n/a · tokens n/a
  - `conductor-core::obs` self-obs log line (redaction wired in) → validation n/a · instrumentation ✓ (`run_id` + `std::time` `timestamp_ms` on every line preserved; redaction applied at the processor stage per obs-plan §11, not sink-stage) · PII **redacted✓** (field-allowlist + host-path scrub, incl. the `std::panic` hook line) · tests **unit✓ (3)** · a11y n/a · tokens n/a
  - `redact_value` / `sanitize_error` (new public fns; reusable by the Epoch-6 `runs.db` / JSONL / report writers) → validation n/a · instrumentation n/a · PII **redacted✓** · tests unit✓ · a11y n/a · tokens n/a

## Deviations from intent
1. **`redact_value` anchors on absolute host-path markers; it does NOT blanket-redact `::` type-name tokens** (plan step 1 listed "struct-name `::` chains → `<redacted>`"). **Justification:** obs-plan §11's wording is "remove all `path::` / `module::` / `backtrace` **paths**" — "paths" qualifies all three → absolute *file* paths, which is the artifact-hygiene invariant's "absolute host paths." Blanket `::`-token redaction would gut the **allowlisted** `target` field (always a module path, e.g. `conductor_core::obs` — confirmed preserved in the boot smoke) AND mangle useful std type names in panic payloads (`Option::unwrap`) — the exact over-redaction the plan's own "key correctness edge" warns against. The **internal-struct-name** half of the invariant is instead guarded by (i) `sanitize_error` using `Display` not `Debug` (no struct-field dump), and (ii) the bounded field-name allowlist DROPPING unexpected Debug-dumped fields. In-scope implementation judgment; consistent with §11's "paths" wording. → **candidate obs-plan §11/§6 reconciliation** (record the implemented redaction model: host-file-path-anchored value scrub + allowlist + Display-edge; `target` module paths explicitly preserved). Flagged for D-obs-redaction.
2. **`sanitize_error` returns the scrubbed message text (no `error:` / `hint:` prefix)** (a criterion phrased it as "renders `error:` / `hint:`"). **Justification:** keeps the core primitive surface-agnostic; the design-owned `error:` / `hint:` wrapping is applied at the cli/tauri edge in Epoch 8 (lean split + design-system owns the cli surface shape). Framing refinement, not a behavior gap.
3. **Reserved Epoch-6 Run-report envelope field names pre-listed in the allowlist** (`journal_emitted_at`, `read_back_observed_at`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `latency_ms`, `slo_tier`, `fingerprints`). Forward-listed per obs-plan §6 so the Epoch-6 writers pass the allowlist without a re-edit; harmless (they do not appear on self-obs lines yet). Cited in the code comment.
4. **`lib.rs` module doc-comment left unchanged** (does not yet mention `redact_value` / `sanitize_error`). Per minimal-comment discipline; the living-docs reconcile (P4, `cargo public-api`) captures the new public fns in `api-surface.md`.

## Decisions & corrections
- **P4 scope decisions (user-approved via AskUserQuestion):**
  1. **Lean anyhow-edge** — build + fully test the `sanitize_error` primitive now; DEFER rewiring the cli/tauri `main()`s to a sanitizing reporter until Epoch 8 (the CLI surface that owns error rendering). `scope.md` was amended to record this (val-1 intent-incomplete resolution).
  2. **Scrub-first + bounded allowlist** redaction model — value-masking is the hard guarantee; the field-name allowlist drops only unexpected names (so existing `message`/`phase`/`count` lines stay intact).
- **Implementation judgment (deviation #1):** `redact_value` host-path-anchored, not blanket `::`-redaction — to avoid gutting the allowlisted `target` and std type names; consistent with obs-plan §11's "paths".
- **No new dependency** — the std-only scrubber keeps the audit/deny surface and `Cargo.lock` unchanged (the supply-chain invariant from the dependency-audit-gate chunk).

## Outcome
- **Acceptance criteria: all met.** Host-path / `module::`-file / backtrace value masking → `<redacted>` (benign + repo-relative source paths preserved) ✓ · bounded field-name allowlist drops unexpected names, identity/`message`/`phase`/`count` survive (existing obs tests green) ✓ · panic line `panic`/`location` scrubbed ✓ · `sanitize_error` renders trace-free, host-path-free, single-line scrubbed text, anyhow-free in core ✓ · primitive reusable by Epoch-6 writers (re-exported) ✓ · masked values use the visible `<redacted>` placeholder ✓ · gates green, no new dep, lock un-drifted ✓.
- **Gates (commands run):** `cargo nextest run -p conductor-core` ✓ **36/36** (25 prior + 11 new) · `cargo nextest run --workspace` ✓ 36/36 · `cargo clippy --workspace --all-targets -- -D warnings` ✓ clean (0 fixes) · `cargo audit` ✓ exit 0 (50 deps) · `cargo deny check` ✓ advisories/bans/licenses/sources ok · `Cargo.lock` un-drifted.
- **Smoke (best-effort — formal trigger NOT met; touchpoints are core-lib-only, cli/tauri mains untouched):** `cargo run -p conductor-cli` boots, exits 0, emits a well-formed flat JSON line; `target: "conductor_core::obs"` preserved (the over-redaction key-edge verified in the real binary), all identity fields + `message` present.
