# Plan — 2026-06-21-hard-signals-scenarios: "Hard-signals scenarios (P-005..P-008)"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-06-21-hard-signals-scenarios · **Version:** conductor-0.1.0 · **Generated:** 2026-06-21T23:06:08Z
- **Epoch:** 7 (Scenario catalog) — chunk 2 of 8

## Goal

Author the **hard-signals family** of the `scenarios/` catalog — four declarative TOML scenarios keyed to
Pulse's deterministic hard-signal floor **P-005..P-008** (`pulse-capability-spec.md` §2 "Hard Signal
Detection"). These are the baseline-free, definitional OTel signals Pulse must surface from the first emitted
span regardless of model availability. This chunk is **pure catalog wiring**: it adds config + fixture tests
only, reusing the `Scenario`/`ExpectedCheck` TOML surface wired in ch1 and the emission primitives built in
Epoch 3 (error-spans, exception-events, severity-logs, multi-service topology). **No new emit primitive, no new
inbound bind, and — confirmed by P3 research — no Rust model change.**

The existing model already expresses every recipe (per arch §Established Decisions [Scenario Config Format] +
research): **P-005/006/007 carry `class = "Hard"`** (deterministic `Pass`/`Fail`); **P-007's two-sided severity
boundary uses the existing `ComparisonKind::Absent`** for the "WARN < 17 does NOT contribute" half (the
positive half is `Contains`); and **P-008 carries `class = "CalibrationRegion"`** — the one deviation from the
family, mandated by the **v2.1 spec amendment** (root-vs-deep severity *weighting* is model-side per P-020, not
a deterministic multiplier; per security-plan §Threat Model + obs-plan §4 + the amendment record). Via
`Verdict::default_report_state`, that calibration check routes to `ReportState::ManualCheck`
(`conductor-core/src/verdict.rs:52`).

Scope law holds (per arch §Cross-cutting Patterns): every scenario carries its P-ID; these are pure-egress
scenarios — no port-occupier (that was P-003's ReceiverFailed leg, already in ch1). The live MCP read-back
*run* and the per-check read-back-token extraction are the Epoch-8 CLI driver's job; this chunk proves only
that each TOML deserializes, garde-validates, and builds a valid `PhaseTimeline` through the existing scheduler.

## Implementation Steps

1. **P-005 — `scenarios/span-status-error-detection.toml`.** Header comment cites `pulse-capability-spec.md`
   §P-005 + coverage mode (drive+observe: candidate detection is auto, halo/constellation hue is operator
   checklist). Identity: `p_ids = ["P-005"]`, a fresh `seed`, `slo_tier = "<5s"` (covers the <500ms-p99
   detection budget), `jitter_ms = 50`. One or two trace phases emitting ERROR-status spans (emission defaults
   to `traces`). One `[[expected]]`: `kind = "Contains"`, `class = "Hard"`, `expected = "error"` (the error
   candidate/severity surfaces to read-back).

2. **P-006 — `scenarios/exception-event-capture.toml`.** Header cites §P-006. Identity: `p_ids = ["P-006"]`,
   fresh `seed`, `slo_tier = "<5s"`, `jitter_ms = 50`. Trace phase(s) emitting spans carrying `exception`
   events (emission defaults to `traces` — exception events ride on spans). One `[[expected]]`:
   `kind = "Contains"`, `class = "Hard"`, `expected = "exception"` (the captured exception is accessible to the
   pattern detector / report generator).

3. **P-007 — `scenarios/high-severity-log-capture.toml`.** Header cites §P-007 + the 17 boundary. Identity:
   `p_ids = ["P-007"]`, fresh `seed`, `slo_tier = "<5s"`, `jitter_ms = 50`. **Two phases that emit LOG
   records** across the boundary — each phase sets `emission = { signal = "logs" }` (per
   `phase_spec.rs:62` `Signal::Logs`): a `warn-logs` phase (SeverityNumber < 17) and an `error-logs` phase
   (≥ 17). **Two `[[expected]]` blocks** (the two-sided boundary):
   - `kind = "Contains"`, `class = "Hard"`, `expected = "ERROR"` — ERROR/FATAL (≥17) contributes to the
     hard-signal layer.
   - `kind = "Absent"`, `class = "Hard"`, `expected = "WARN"` — WARN-and-below (<17) does NOT contribute.

4. **P-008 — `scenarios/root-span-error-scope.toml`.** Header cites §P-008 **and the v2.1 amendment**
   (root-vs-deep weighting is model-side ⇒ calibration-region, not a hard assert). Identity:
   `p_ids = ["P-008"]`, fresh `seed`, `slo_tier = "<5s"`, `jitter_ms = 50`. Two trace phases encoding the
   (a)-vs-(b) comparison: a `deep-error` phase (only an internal child span carries ERROR; root succeeds) and a
   `root-error` phase (child + root both ERROR). One `[[expected]]`: `kind = "Contains"`,
   **`class = "CalibrationRegion"`**, `expected = "error"` (the relative b≥a tendency is the Epoch-8
   evaluator's interpretation, not encoded in `ComparisonKind` — consistent with the deferred numeric-tolerance
   precedent in `last-span-ago-tracking.toml`).

5. **Fixture round-trip test** — in `crates/conductor-core/src/scenario.rs` `#[cfg(test)] mod tests`, add a
   `#[rstest]` `hard_signal_fixtures_load_and_validate` mirroring `connection_lifecycle_fixtures_load_and_validate`
   (scenario.rs:300): four `#[case(stem, p_id)]` rows (P-005..P-008) that read `../../scenarios/{stem}.toml`,
   `Scenario::from_toml_str`, and assert `name`, `p_ids`, and `!s.expected.is_empty()`. **Add one P-008-specific
   assertion** that its single expected check carries `ClaimClass::CalibrationRegion` — the behavioral guard for
   the amendment (the one check distinct from ch1). Optionally assert P-007 carries both a `Contains` and an
   `Absent` check.

6. **Gate** — run the workspace test + clippy + doctest gates and the read-only `agent-run.sh status` smoke
   (no boot-path change). Confirm no new dependency entered `Cargo.lock`.

## Codebase touchpoints

### New files
- `scenarios/span-status-error-detection.toml` — P-005, Hard/Contains "error".
- `scenarios/exception-event-capture.toml` — P-006, Hard/Contains "exception".
- `scenarios/high-severity-log-capture.toml` — P-007, `signal = "logs"` phases, Hard/Contains "ERROR" + Hard/Absent "WARN".
- `scenarios/root-span-error-scope.toml` — P-008, CalibrationRegion/Contains "error".

### Files to modify
- `crates/conductor-core/src/scenario.rs` — add the `hard_signal_fixtures_load_and_validate` rstest (4 cases +
  the P-008 `CalibrationRegion` assertion); preserve the existing test-module structure + the
  `env!("CARGO_MANIFEST_DIR")/../../scenarios/{stem}.toml` fixture-path convention (scenario.rs:306).

## Test Commands
```bash
cargo nextest run -p conductor-core            # the new fixture rstest + the existing scenario/expected tests
cargo nextest run --workspace --profile ci     # full workspace gate (no regressions)
cargo clippy --workspace --all-targets -- -D warnings
cargo test --doc -p conductor-core             # doctests nextest skips (expect 0 new)
bash scripts/agent-run.sh status               # read-only smoke (exit 0; no boot-path change this chunk)
```

## Acceptance Criteria
- (arch/tests) The 4 hard-signal TOMLs exist under `scenarios/`, deserialize + garde-validate via
  `Scenario::from_toml_str`, and build a valid `PhaseTimeline` — proven by the new rstest (per arch §Established
  Decisions [Scenario Config Format]; test-plan §7 fixture pattern).
- (arch/security/obs) P-005/P-006/P-007 expected checks carry `class = "Hard"`; **P-008 carries
  `class = "CalibrationRegion"`** and routes to `ManualCheck` — asserted in the fixture test (per the v2.1
  amendment; arch §Probabilistic-Assertion Policy; security-plan §Threat Model; obs-plan §4).
- (arch/obs) P-007 expresses the two-sided boundary with both a `Contains` (≥17 contributes) and an `Absent`
  (<17 does not) check, and its phases emit `signal = "logs"` (per `phase_spec.rs:62`; evaluator support proven
  by `conductor-verify/src/slo.rs:99` `exact_contains_absent_compare_as_expected`).
- (security) No new inbound listener/port bind; loopback-egress-only surface preserved; scenario deserialization
  failures stay `CoreError::Config`/`Validation` (harness `Err`), never a panic (per security-plan §Input
  Validation, §Threat Model; verdict/error wall).
- (tests/determinism) Same scenario+seed ⇒ same stream shape; no new emit primitive; **no new dependency**
  (committed `Cargo.lock` un-drifted) (per test-plan §11; arch §Determinism discipline).
- (gates) `cargo nextest run --workspace --profile ci` returns 0 · clippy `-D warnings` clean · doctest 0 ·
  `agent-run.sh status` exit 0.

## Provenance
- **Spec anchors:** arch §Established Decisions [Scenario Config Format] / [Validation Library] / [Verdict-error
  wall] · §Probabilistic-Assertion Policy · §Timing-Tolerance Model; security-plan §Input Validation · §Threat
  Model Summary · §Error Handling; test-plan §4 / §7 / §11; obs-plan §3 / §4 / §6; design-system §Semantic
  Colors (forward, Epoch 8/9).
- **Requirement SOT:** `pulse-capability-spec.md` §2 (P-005..P-008) + the v2.1 amendment
  (`.andromeda/runs/2026-06-12T18-52-00-spec-amendment-p008-root-weighting-model-side/amendment.md`).
- **Research files:** `chunks/2026-06-21-hard-signals-scenarios/research.md`; inspected
  `crates/conductor-core/src/{expected.rs,scenario.rs,verdict.rs,phase_spec.rs}`,
  `scenarios/{error-baseline-spike,receiver-lifecycle-state,last-span-ago-tracking,receiver-failed-port-conflict,orthogonal-health-domains}.toml`;
  code-graph adoption trace `.andromeda/runs/2026-06-21T23-06-08-phase/tree-query-2026-06-21-hard-signals-scenarios.json`.

## Implementation notes
- **Zero blast radius (P3 code-graph):** this chunk modifies no Rust symbol signature. The new TOMLs are
  consumed by the new in-crate test cases (existing `Scenario::from_toml_str`) and, later, the existing
  `conductor-verify::slo::evaluate_check` (slo.rs:79) — which already handles `Exact`/`Contains`/`Absent`/`CountAtLeast`
  and `Hard`/`CalibrationRegion`. Nothing downstream needs to change here.
- **Read-back tokens are declarative placeholders.** The exact observed string each `Contains`/`Absent` matches
  is finalized when the Epoch-8 driver wires read-back extraction (mirrors ch1, where state-name tokens like
  "Stalled" are mapped by the driver, and `last-span-ago-tracking`'s ±1s tolerance is the evaluator's). Choose
  the values above; expect refinement in Epoch 8 — they need only be non-empty (garde `length(min=1)`) and
  semantically faithful now.
- **Emission realization deferred (ch1 convention).** Phase *names* carry the intent; only P-007 sets an
  explicit `Signal::Logs` because the signal class is semantically load-bearing (logs vs traces). The concrete
  fault content (the actual ERROR spans / exception events / severity mix / root-vs-child linkage) is realized
  by the Epoch-8 driver over the Epoch-3 emit primitives — not authored as emission detail here.
- **Seeds:** pick fresh per-scenario seeds (ch1 used a `4317NNN` scheme keyed to the P-ID, e.g. `4317001`);
  a parallel `…005`/`…006`/`…007`/`…008` scheme keeps them legible and distinct. Determinism does not require
  unique seeds across scenarios, but distinct seeds aid debuggability.
- **TOML nested-table syntax** for P-007 phase emission: use the inline-table form
  `emission = { signal = "logs" }` under each `[[phases]]` entry (serde reads it into `PhaseSpec.emission`).
