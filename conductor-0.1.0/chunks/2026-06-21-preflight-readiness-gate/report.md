# Report — 2026-06-21-preflight-readiness-gate

**Chunk:** Preflight readiness gate — pinned 2024-11-05 + required-tool presence + data-dir canary round-trip, distinct Blocked state on mismatch (conductor-verify)
**Date:** 2026-06-21
**Commits:** (uncommitted at report time — wrap commits in P7; prior wrap: 2026-06-21-mcp-read-back-client)

## Changes (structured — detectors read this)
- **Files:**
  - New: `crates/conductor-verify/src/manifest.rs` · `src/preflight.rs` · `src/bin/stub_pulse_mcp.rs` · `tests/preflight.rs` · `tests/preflight_spawn.rs` · `contracts/mcp-contract.toml`
  - Modified: `crates/conductor-verify/src/lib.rs` · `src/error.rs` · `Cargo.toml` · `Cargo.lock`
- **Symbols / APIs (new public, all in `conductor-verify`):** `ContractManifest` (+ `load`/`default_path`) · `READBACK_TOOLS: [&str;4]` · `ReadyState` (+ `report_state()`) · `ToolPresence` · `CanaryOutcome` · `CanaryMarker` · `run_preflight()` · `preflight_boot()`; new `VerifyError::Manifest { reason }` variant. No new IPC method / endpoint / socket / port.
- **Crates / modules:** `conductor-verify` gains `mod manifest` + `mod preflight`; a `stub-server` cargo feature; a feature-gated `[[bin]] stub_pulse_mcp` (test-support, never in a default/release build). **No new crate; no new seam→seam edge — `conductor-verify` still depends on `conductor-core` only (star topology preserved).**
- **Dependencies:** `serde` (derive) + `toml` added to `conductor-verify` — both already workspace deps (used by `conductor-core`); no new crate enters `Cargo.lock` (still 240 deps), no version bumps; `Cargo.lock` un-drifted (only `conductor-verify`'s entry gained the two). `stub-server` feature = `["rmcp/server", "tokio/io-std"]`. `cargo audit` clean (240 deps, exit 0); `cargo deny` clean (advisories/bans/licenses/sources ok).
- **Schema / config:** `contracts/mcp-contract.toml` — the pinned MCP contract manifest (`expected_protocol_version = "2024-11-05"` + the 4 `required_tools`). `ReadyState` serializes to the arch §Standard Contracts readiness JSON shape (`ready`, `negotiated_protocol_version`, `expected_protocol_version`, `required_tools` map, `data_dir`, `canary_round_trip`, `blocked_precondition`, `checked_at`). Both `contracts/` artifact + the `CONDUCTOR_CONTRACT_MANIFEST` env handle are already registered in arch §Occupied Resources.
- **Coverage of new surfaces:**
  - `contract manifest (TOML external input)` → validation **bounds-checked✓** (`ContractManifest::validate` — non-empty version, all 4 pinned tools present, no empty names; garde n/a — garde is the scenario-config validator, manifest is a pinned ops file) · instrumentation n/a (sync load, not a must-trace op) · PII n/a · tests **unit✓** (4) · a11y n/a · tokens n/a. Path canonicalization (`resolve_under` for a `CONDUCTOR_CONTRACT_MANIFEST` override) is the cli edge's job (Epoch 8); the loader takes an already-resolved path.
  - `MCP preflight read-back (child stdout)` → validation **✓** (MCP call/list failures caught into typed `Blocked` legs; never panic; bounded `serde_json` parse of the result, substring canary match) · instrumentation **span✓** (`verify.readback.preflight` / `.preflight_boot`, bounded `verify.readback*` set) + **info log✓** on the Blocked exit · PII n/a · tests **unit✓ + integ✓** (5 in-proc + 1 live `TokioChildProcess` child) · a11y n/a · tokens n/a. Negotiate-down to `2024-11-05` asserted; spawn hardening unchanged (chunk 1).
  - `ReadyState (serialized readiness artifact)` → validation n/a · instrumentation **✓** (`run_id` on every line via the global subscriber; `checked_at` from `now_rfc3339()` = `std::time`, never the virtual clock) · PII **redacted✓** (`data_dir` passed through `conductor_core::redact_value` → absolute host paths become `<redacted>`; precondition error strings redacted too) · tests **✓** (serialization-shape test) · a11y n/a · tokens n/a
  - `stub_pulse_mcp (test-support binary)` → n/a across all flags — a feature-gated test fixture spawned only by `tests/preflight_spawn.rs`; never part of a default/release build, not a production process.

## Deviations from intent
1. **`assert_fs` dev-dep dropped** (plan step 9 listed it). Manifest tests run against the committed `contracts/mcp-contract.toml` (via `CARGO_MANIFEST_DIR`) + directly-constructed `ContractManifest` values — no temp files needed. Justified: fewer deps, same coverage.
2. **`pub const READBACK_TOOLS` added in `manifest.rs`** (not explicit in plan). DRY for the 4 tool names across the manifest bounds-check, the in-proc stubs, and the stub bin; placed in an in-scope new file rather than editing the unlisted `client.rs`. Justified: avoids out-of-scope edit.
3. **obs `blocked_precondition` realized via the redacted `message` + the allowlisted `state="Blocked"` field**, not a standalone allowlisted `blocked_precondition` log field. `conductor_core::redact::ALLOWLISTED_FIELDS` (owned by the earlier structured-logging chunk, not in this chunk's Files-to-modify) does not include `blocked_precondition`; a non-allowlisted field is dropped at the subscriber. The Blocked exit IS logged at `info` with its precondition visible (in the redacted message) + structured state. Adding the field to the allowlist is an out-of-scope conductor-core edit (candidate for a future obs chunk). Justified: in-scope, no leak, criterion intent met.
4. **Wrong-version test driven from the manifest side, not the stub's reported version.** rmcp's server negotiates a newer client request *down* to its own version (`service/server.rs:236`), so a stub reporting `LATEST` still negotiates to `2024-11-05` (no observable mismatch). The test pins the manifest to a version the healthy server doesn't negotiate, exercising the gate's `negotiated ≠ manifest.expected ⇒ Blocked` branch deterministically. Justified: faithful to the acceptance criterion, no coupling to rmcp's version-ordering.

## Decisions & corrections
- **P4 scope decision (AskUserQuestion): canary = read-back assertion only.** `conductor-verify`'s gate asserts the known canary reads back (identity is a `CanaryMarker` parameter); the canary's *emission* is the Epoch-8 CLI suite runner's job (it will depend on both emit + verify). **No `conductor-verify → conductor-emit` edge** — the workspace star topology (every seam → `conductor-core` only) is preserved.
- **Open questions resolved:** (q2) rmcp accepts the server's reported protocol version post-handshake without rejecting (`service/client.rs:257`), so a version mismatch routes through `run_preflight`, never `preflight_boot`'s connect-Err path — connect-Err is reserved for true MCP-unreachable → `Blocked` with the named precondition. (q3) `data_dir` is `redact_value`-redacted in `ReadyState` (the canary's value is the *fact* of the corpus match, not the path). (q4) stub-child binary = feature-gated `[[bin]] stub_pulse_mcp`, discovered via `env!("CARGO_BIN_EXE_stub_pulse_mcp")`; `stub-server` feature pulls `rmcp/server` + `tokio/io-std` so a default build/clippy/release never compiles it.
- **Spec↔reality nuance (for intent-consistency check):** the obs allowlist gap (deviation 3) — the plan asserted an allowlisted `blocked_precondition` field; the allowlist (earlier chunk) lacks it. Not a leak (precondition logged via redacted message + allowlisted `state`). Worth a glance: whether obs-plan should later add `blocked_precondition` to the §6 allowlist.

## Outcome
- **Acceptance criteria:** all met. Protocol-mismatch / absent-tool / empty-canary each ⇒ `ready:false` with a distinct named precondition; no panic on MCP error/`tonic::Status`/malformed output (caught into `Blocked` legs); `ReadyState` serializes to the readiness envelope; `ReportState::Blocked` reused (no new enum); manifest bounds-checked; chunk-1 spawn hardening unchanged; spans in the bounded `verify.readback*` set; `data_dir` redacted.
- **Gates green:** `cargo nextest run -p conductor-verify` 24/24 · `--features stub-server` 25/25 (incl. live child-spawn) · `cargo nextest run --workspace --profile ci` 199/199 (+9) · `cargo test --workspace --doc` clean · `cargo clippy --workspace --all-targets -- -D warnings` clean (+ `--features stub-server`) · `cargo audit` + `cargo deny` clean · `Cargo.lock` un-drifted.
- **Smoke:** no app boot-path change (the `conductor preflight` verb is Epoch 8; `conductor-cli` untouched). The real-subprocess proof is the green `live_child_spawn_preflight_is_ready` test (spawns the real `stub_pulse_mcp` child over stdio, drives connect→initialize→preflight→canary, asserts `ready:true` + negotiated `2024-11-05`).
