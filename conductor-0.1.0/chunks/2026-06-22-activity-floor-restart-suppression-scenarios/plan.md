# Plan — 2026-06-22-activity-floor-restart-suppression-scenarios: "Activity-floor + restart-suppression scenarios (P-013..P-016, P-057)"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-06-22-activity-floor-restart-suppression-scenarios · **Version:** conductor-0.1.0 ·
  **Generated:** 2026-06-22T16:59:07Z · **Epoch 7 (Scenario catalog) — chunk 4 of 8**

## Goal
Author the **activity-floor + restart-suppression families** of the `scenarios/` catalog — declarative TOML
keyed to Pulse's activity-floor (P-013/P-014) and restart-suppression (P-015/P-016/P-057) capabilities. This
is the first Epoch-7 chunk whose stimulus is **fault helpers** (conductor-faults Epoch-4: `bursty-train-pattern`
P-013, `abrupt-silence-fault` P-014, `emission-gap-resume` P-015) rather than pure emit primitives — but, per
`receiver-failed-port-conflict.toml`, the fault is **Epoch-8-driver-realized via phase-name semantics**, so the
TOMLs only *time + label* phases and declare expected read-back outcomes; they bind nothing and spawn nothing.

Research confirmed the chunk is **zero-model-change**: `ComparisonKind::Absent` already exists (`expected.rs:41`,
used by P-007's `high-severity-log-capture.toml`), so the suppression *absence* legs need no new kind; the
`Scenario`/`ExpectedCheck`/`PhaseSpec` surface (consumed unchanged since ch1) covers train/lunch/silence/gap/
resume/suppression/bypass. Every check is `class="Hard"` — the architecture's Probabilistic-Assertion Policy
names "suppression/bypass logic" and lifecycle timing as deterministic hard pass/fail (the model-interpretive
severity that *consumes* these cues is P-020/P-021, a later chunk).

Per the **P4 file-granularity decision (3 files, outcome-coherent)**: because a scenario's `Vec<ExpectedCheck>`
evaluates against ONE read-back with no per-check service/window scoping, `Absent "ServiceWentSilent"` (P-013)
and `Contains "ServiceWentSilent"` (P-014) cannot share a file — so activity-floor splits into two files, and
**both** P-013 and P-014 get a real Hard check. The same forcing function makes restart-suppression's suppressed
legs (the 15s transient, the 8×/3% sub-threshold) **declare-only** (an `Absent "ErrorRateSpike"` would
contradict the *present* sustained/bypass spikes in the same read-back); those are documented in comments +
chunk notes for the Epoch-8 evaluator, exactly as ch3 deferred its ±%-tolerance baseline-MATCH math.

## Implementation Steps
1. **`scenarios/activity-floor.toml` (P-013).** Header comment: `pulse-capability-spec.md` P-013 (L206-214)
   recipe + `Coverage mode: drive+observe` + the cold-start note + the **OUT** marker for the spec's "restart
   Pulse, verify histogram restoration" leg (Conductor does not manage the Pulse process). `seed = 4317013`,
   `slo_tier = "<90s"`, `jitter_ms = 50`. Phases — a bursty-train baseline then a lunch quiet window:

   | name | gap_ms | meaning |
   |---|---|---|
   | `train-active-1` | 300000 | 5-min active burst (bursty-train-pattern lever) |
   | `train-quiet-1` | 600000 | 10-min quiet |
   | `train-active-2` | 300000 | 5-min active |
   | `train-quiet-2` | 600000 | 10-min quiet |
   | `train-active-3` | 300000 | 5-min active (histogram converges on the 5/10 cadence) |
   | `lunch` | 1800000 | 30-min quiet — **within** the learned pattern ⇒ no false silent |

   One expected check: `kind="Absent"`, `class="Hard"`, `expected="ServiceWentSilent"` (no ServiceWentSilent
   candidate during the expected quiet).

2. **`scenarios/service-went-silent.toml` (P-014).** Header: P-014 (L216-224) recipe + `Coverage mode:
   drive+observe` + note that the *cue* is the Hard signal (the model's resulting incident is P-020 discretion).
   `seed = 4317014`, `slo_tier = "<90s"` (cue at ~30s > the `<20s` tier), `jitter_ms = 50`. Phases:

   | name | gap_ms | meaning |
   |---|---|---|
   | `active` | 300000 | 5-min consistent activity — fresh-service baseline (no learned quiet pattern) |
   | `silence` | 35000 | abrupt stop (abrupt-silence-fault lever); >30s ⇒ cue at the 30s minimum |

   One expected check: `kind="Contains"`, `class="Hard"`, `expected="ServiceWentSilent"`.

3. **`scenarios/restart-suppression.toml` (P-015, P-016, P-057).** Header: P-015 (L232) / P-016 (L242) / P-057
   (L706) recipes + the bypass truth table `bypass = (relative ≥ 10×) OR (absolute ≥ 5%)` + `Coverage mode:
   drive+observe` + the **declare-only** note for the suppressed legs + the Epoch-8 calibration points (the
   bypass *annotation* string; the per-leg magnitudes). **`name = "restart-suppression"`** (must match obs-plan
   §4's must-trace `scenario`). `seed = 4317015`, `slo_tier = "<20s"` (latency-regression persistence
   precedent; RestartEvent ≤2s fits comfortably), `jitter_ms = 50`. One restart-anchored timeline:

   | name | gap_ms | meaning | leg |
   |---|---|---|---|
   | `baseline` | 30000 | consistent traffic + error baseline (the relative-magnitude reference) | — |
   | `restart-gap` | 25000 | 25s stream gap > 20s (emission-gap-resume lever) | P-015 |
   | `resume` | 2000 | resume emission ⇒ RestartEvent ≤2s | P-015 |
   | `suppressed-15s-burst` | 15000 | 15s transient error burst, <30s persistence ⇒ suppressed | P-016a (declare-only) |
   | `sustained-60s` | 60000 | 60s sustained error ⇒ surfaces after 30s persistence | P-016b |
   | `bypass-suppressed-8x-3pct` | 15000 | 8× rel / 3% abs — below both thresholds ⇒ suppressed | P-057a (declare-only) |
   | `bypass-relative-12x-4pct` | 15000 | 12× rel / 4% abs ⇒ relative-bypass surfaces | P-057b |
   | `bypass-absolute-6x-7pct` | 15000 | 6× rel / 7% abs ⇒ absolute-bypass surfaces | P-057c |

   Two expected checks (the coherent positives): `Contains "RestartEvent"` (Hard, P-015) + `Contains
   "ErrorRateSpike"` (Hard, P-016b sustained-surface + P-057b/c bypass-surface). The suppressed legs carry NO
   expected check (would contradict the present `ErrorRateSpike`) — they live in the phase comments.

4. **`crates/conductor-core/src/scenario.rs` — extend the `#[cfg(test)] mod tests` only** (no production code):
   - an **`activity_floor_and_restart_suppression_fixtures_load_and_validate`** `#[rstest]` with `#[case]` rows
     `("activity-floor", &["P-013"])`, `("service-went-silent", &["P-014"])`, `("restart-suppression",
     &["P-015","P-016","P-057"])` — assert `name`/`p_ids`/`!expected.is_empty()` (mirrors the multi-P-ID
     statistical-anomaly loader at `scenario.rs:328-339`);
   - an **all-`Hard` guard** over the three stems (mirrors `scenario.rs:341-363`);
   - an **`Absent`-usage assertion** that `activity-floor` declares a `ComparisonKind::Absent` check and
     `service-went-silent` / `restart-suppression` declare `ComparisonKind::Contains` (mirrors the two-sided
     `p007_…asserts_both_sides_of_the_boundary` at `scenario.rs:379-395`).

5. **Run the gates** (below). Confirm **no determinism golden re-baseline** is needed (the new TOMLs feed no
   seed-named golden — `replay.rs` loads only `error-baseline-spike.toml`, untouched here).

## Codebase touchpoints
### New files
- `scenarios/activity-floor.toml` — P-013 bursty-train + lunch ⇒ `Absent "ServiceWentSilent"`.
- `scenarios/service-went-silent.toml` — P-014 fresh-active → abrupt-silence ⇒ `Contains "ServiceWentSilent"`.
- `scenarios/restart-suppression.toml` — P-015/P-016/P-057 gap→resume→suppression-window→bypass-triple ⇒
  `Contains "RestartEvent"` + `Contains "ErrorRateSpike"`; suppressed legs declare-only.
### Files to modify
- `crates/conductor-core/src/scenario.rs` — add three `#[rstest]`/`#[test]` cases in the existing `mod tests`
  (loader + all-Hard guard + Absent/Contains kind-usage), following the ch1–ch3 per-family pattern verbatim.
  **No change to any non-test symbol** — `Scenario`, `ExpectedCheck`, `ComparisonKind`, `PhaseSpec` are
  consumed as-is (`ComparisonKind::Absent` already has a live user at `scenario.rs:392`).

## Test Commands
```bash
cargo nextest run -p conductor-core            # the new fixture #[case] rows + all-Hard/Absent guards
cargo nextest run --workspace --profile ci     # full zero-retry suite (no regressions)
cargo test -p conductor-core --doc             # doctests nextest skips (expect 0 here)
cargo clippy --workspace --all-targets -- -D warnings
bash scripts/agent-run.sh run                  # smoke = the release-gate path (run, NOT status/logs — no live run_id exists pre-Epoch-8)
```

## Acceptance Criteria
- (arch) All three TOMLs deserialize + garde-validate via `Scenario::from_toml_str` and build a valid
  `PhaseTimeline`, proven by the new fixture `#[case]` rows (per architecture §Scenario Config Format).
- (arch) Every `[[expected]]` check is `class = "Hard"` — no `CalibrationRegion` (per architecture
  §Probabilistic-Assertion Policy: suppression/bypass logic + lifecycle timing are deterministic).
- (arch) The verdict/error wall holds: **zero** change to `Scenario`/`ExpectedCheck`/`ComparisonKind`/`PhaseSpec`
  (per architecture §Error Handling; the `Absent` kind already existed).
- (tests) `cargo nextest run -p conductor-core` passes the loader + all-Hard guard + the `Absent`(activity-floor)
  / `Contains`(service-went-silent, restart-suppression) kind-usage assertions (per test-plan §4 table-driven
  `#[case]`).
- (tests) Determinism preserved — **no seed-named golden re-baseline** (the new seeds 4317013/014/015 feed no
  golden; `replay.rs` is `error-baseline-spike`-only, and this chunk does not touch that fixture) (per test-plan
  §1/§4 + the ch3 seed-named-golden learning).
- (security) garde rejects malformed/out-of-range config at load; **no new inbound listener** — pure-egress
  (silence/gap/train are stops/starts of egress; the port-occupier is P-003/ch1) (per security-plan §Input
  Validation + scope law).
- (obs) `restart-suppression`'s `name` is exactly `"restart-suppression"` (obs-plan §4 must-trace alignment);
  no new span names introduced — the `bypass_triggered`/`path_type` instrumentation is Epoch-8 runtime, not this
  chunk (per obs-plan §4/§11).
- (DoD) The declare-only suppressed legs (P-016 15s-transient, P-057 8×/3%) are documented in the TOML comments
  **and** recorded in the chunk notes for the Epoch-8 evaluator (the absence-evaluation owner).
- (workspace) `cargo nextest run --workspace` returns 0 · clippy `-D warnings` clean · doctest 0 ·
  `bash scripts/agent-run.sh run` exits 0.

## Provenance
- **Spec anchors (from the extracts):** architecture §Probabilistic-Assertion Policy · §Timing-Tolerance Model ·
  §Established Decisions [Scenario Config Format] · §Error Handling (verdict/error wall); security-plan §Input
  Validation · §Threat Model Summary (scope law); test-plan §1 (coverage triggers) · §3 (harness contract) · §4
  (rstest `#[case]` / golden pattern); obs-plan §4 (restart-suppression must-trace) · §10 (SLO tiers) · §11
  (bounded span names); layouts (no constraint — forward-binds to Epoch-9/10 verdict lamps).
- **Requirement source:** `pulse-capability-spec.md` P-013 (L206) · P-014 (L216) · P-015 (L232) · P-016 (L242) ·
  P-057 (L706); the 10×/5% bypass thresholds (L708/L775).
- **Research files inspected:** `crates/conductor-core/src/{scenario,expected,phase_spec}.rs`;
  `scenarios/{high-severity-log-capture,receiver-failed-port-conflict,receiver-lifecycle-state,latency-regression}.toml`;
  `crates/conductor-timeline/tests/{replay,determinism}.rs`.

## Implementation notes
- **Same-token contradiction (the design driver):** never put `Contains X` and `Absent X` in one scenario — the
  scenario-level checks evaluate against one read-back. Hence the 3-file split and the declare-only suppressed
  legs. `Absent` is used only where the absent-token has no present twin in the same file (activity-floor's
  `ServiceWentSilent`), exactly like P-007's `Absent "WARN"` + `Contains "ERROR"`.
- **No golden re-baseline:** `replay.rs` hard-codes `error-baseline-spike.toml`; `determinism.rs` loads no TOML;
  a `crates/` grep found no `read_dir`/glob of `scenarios/` — every loader is an explicit `#[case]` stem, so the
  new files join no test silently (you MUST add the `#[case]` rows). This chunk must NOT edit
  `error-baseline-spike.toml` (editing it is what forced ch3's re-baseline).
- **Bounds:** `MAX_GAP_MS = 3_600_000` (60min) bounds the 30-min lunch (1.8M, OK); phase `name` ≤ 40 chars (all
  planned names ≤ 25); `jitter_ms` ≤ `MAX_JITTER_MS` (60k) — use 50.
- **Bypass magnitudes are NOT structured data:** `EmissionSpec` carries only `signal`; the 8×/3% · 12×/4% ·
  6×/7% pairs + the silence/gap/train fault kind live in **phase names + comments**, realized by the Epoch-8
  driver (the `receiver-failed-port-conflict` "does NOT bind the port itself" pattern).
- **Scope-out (P-013):** the spec's "restart Pulse mid-training, verify histogram restoration" leg is OUT —
  Conductor does not manage the Pulse process; mark it in the header comment.
- **Cold-start nuance:** ServiceWentSilent is universally suppressed for the first 60 min/service while the
  histogram bootstraps (P-013 L214); the training durations are representative, and the spec-faithful
  convergence window is an Epoch-8 live-run concern (compressed vs faithful) — note it in the comment, it does
  not affect the fixture test (which validates structure, not runtime).
- **Epoch-8 calibration points (carry forward):** the latency-regression `<20s` precedent applied to
  restart-suppression; the P-057 bypass-annotation token; the declare-only suppressed-leg evaluation; the
  long-timeline (P-013 ~90-min) live-run compression.
