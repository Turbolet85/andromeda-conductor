# Plan — 2026-06-22-constellation-context-grounding-scenarios: "Constellation + context-grounding scenarios"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-06-22-constellation-context-grounding-scenarios · **Version:** conductor-0.1.0 · **Generated:** 2026-06-22T23:02:34Z

## Goal
Author the constellation + context-grounding scenario catalog family — five declarative `scenarios/*.toml`
entries for P-025 (halo hue), P-026 (halo breathing), P-027 (service-constellation auto-discovery), P-032
(project context grounding), P-036 (cross-incident recurrence) — plus the `conductor-core` test wiring that
loads and guards them. This is Epoch 7 chunk 7/8, and the catalog's inflection for the two remaining report
states: the constellation trio is the **first DriveObserve / operator-checklist (ManualCheck) family**, and
P-032 is the **first declared KnownResidual**.

Research (`research.md`) settled the chunk's central design question decisively: **zero model change.** The
`Scenario.expected` field is already `#[serde(default)]` and its doc comment (`scenario.rs:91-94`) was written
for exactly this case — *"a drive+observe scenario may assert only via the operator checklist."* An
empty-`expected` scenario validates and produces no verdict → `Lamp::Manual` (`lamp.rs:44`). And `ReportState`
is producer-assigned downstream (no scenario `state` field), so P-032's `Fail→KnownResidual` routing is the
Epoch-8 evaluator's job, declared-only here — the established severity-lifecycle / activity-floor precedent
for legs the current model can't yet assert. Per arch §Probabilistic-Assertion Policy + §Read-Back Dependency
Posture; classification per coverage.rs (DriveObserve×3, Auto×2, already present — untouched).

Scope decisions (P4 AskUserQuestion): **(Q1)** the constellation trio is **3 per-P-ID files** (each a distinct
visual claim + emission driver), giving 5 files total; **(Q2)** P-032 is **declare-only** (empty `expected` +
documented residual), refining scope.md's DoD "P-032 expresses KnownResidual" → "declares + documents the
known-residual; the KnownResidual routing is Epoch-8 evaluator-owned."

## Implementation Steps
1. **Constellation trio — 3 DriveObserve / operator-checklist TOMLs (empty `expected`).** Follow the
   `severity-tier-autonomous.toml` idiom (comment header: P-IDs + `pulse-capability-spec.md §` refs +
   capability prose + declare-only notes + Epoch-8 calibration points + a `Coverage mode: drive+observe` line;
   then `name`/`p_ids`/`seed`/`slo_tier`(inline comment)/`jitter_ms`; then `[[phases]]`). OMIT the `[[expected]]`
   block entirely (the operator-checklist path). One descriptive phase `name` per emission lever the Epoch-8
   driver realizes:
   - `scenarios/halo-hue-encoding.toml` — P-025, seed 4317025, `slo_tier="<5s"` (hue shift ≤2s pipeline
     latency, delegated). Phase(s) drive an error-state-shaped stream (the hue lever); document that the halo
     hue render is Pulse's, verified by operator checklist.
   - `scenarios/halo-breathing-encoding.toml` — P-026, seed 4317026, `slo_tier` per a continuous-throughput
     observation. Phase(s) drive the rate-ramp/breathing curve (the `conductor-emit` `rate.rs` primitive
     exists); document the breathing-rhythm operator claim.
   - `scenarios/service-constellation-discovery.toml` — P-027, seed 4317027, `slo_tier="<5s"` (new dot ≤5s).
     Phase(s) drive a multi-service `service.name` topology (the `conductor-emit` `topology.rs` primitive
     exists). Document the declare-only sub-claims: stable dot positions across a Pulse restart, the >20-service
     cap — operator-checklist / Epoch-8.
2. **P-032 context-grounding — Auto→KnownResidual, declare-only (empty `expected`).**
   `scenarios/project-context-grounding.toml` — P-032, seed 4317032. Phase(s) drive an incident inside the
   (real-git-workspace) context. OMIT `[[expected]]`. Comment header documents: runs inside a real git
   workspace with known recent commits (operator/Epoch-10 precondition); the Report context section should
   surface the last-5 commits; **Pulse's production `recent_commits` producer is a stub until v0.3.0 →
   pre-accepted KnownResidual, Conductor is its designated detector**; the expected-commit token is
   live-environment-specific and the `Fail→KnownResidual` routing is Epoch-8 evaluator-owned (declare-only).
3. **P-036 recurrence — Auto, one Hard `Contains` check.** `scenarios/cross-incident-recurrence.toml` — P-036,
   seed 4317036. Phase(s) emit a fingerprinted incident; document that recurrence is realized by running the
   SAME scenario twice (same seed → same fingerprint), the runs.db cross-run index surfacing "Previously seen"
   on the second run (Epoch-8/10). Declare ONE `[[expected]]`: `kind="Contains"`, `class="Hard"`,
   `expected="Previously seen"` — the recurrence is a deterministic runs.db-index lookup (Hard, like the
   fingerprint-storm count). Flag "Previously seen" as an inferred Epoch-8 calibration token (the
   RetryStorm/LatencyRegression precedent).
4. **Test wiring — `crates/conductor-core/src/scenario.rs` `#[cfg(test)] mod tests` ONLY** (mirror the
   `severity_lifecycle_*` block at `scenario.rs:518-632`):
   - `constellation_context_grounding_fixtures_load_and_validate` — a `#[rstest]` `#[case(stem, &[p_ids])]`
     loader over all 5 stems asserting `name` + `p_ids` + `from_toml_str` validity. **Do NOT** assert
     `!expected.is_empty()` here (the existing loaders do; these scenarios invert it).
   - An operator-checklist/declare-only guard: assert P-025/026/027/032 fixtures have `s.expected.is_empty()`
     (the drive+observe path — the inversion of the prior non-empty loader assumption).
   - A P-036 guard: assert exactly the Hard recurrence check
     (`any(kind==Contains && class==Hard && expected=="Previously seen")`).
   - A short suite note (doc comment) recording this as the first family with operator-checklist (empty-expected)
     members + P-032's declared KnownResidual — the documentation analogue of the severity-lifecycle
     `severity_lifecycle_suite_is_mixed_class` guard.
5. **Gates + determinism check.** Run the test commands below. Confirm goldens UNCHANGED: grep
   `crates/conductor-timeline/tests/snapshots/` for seeds `4317025|4317026|4317027|4317032|4317036` → expect
   zero hits (these seeds feed no replay golden; per the 2026-06-22 seed-named-golden testing learning). Do NOT
   touch `coverage.rs` (all 60 P-IDs already classified).

## Codebase touchpoints
### New files
- `scenarios/halo-hue-encoding.toml` — P-025 DriveObserve, empty `expected`, seed 4317025.
- `scenarios/halo-breathing-encoding.toml` — P-026 DriveObserve, empty `expected`, seed 4317026.
- `scenarios/service-constellation-discovery.toml` — P-027 DriveObserve, empty `expected`, seed 4317027.
- `scenarios/project-context-grounding.toml` — P-032 Auto→KnownResidual, empty `expected` (declare-only), seed 4317032.
- `scenarios/cross-incident-recurrence.toml` — P-036 Auto, one Hard `Contains "Previously seen"`, seed 4317036.
### Files to modify
- `crates/conductor-core/src/scenario.rs` — TESTS ONLY (the `#[cfg(test)] mod tests` module). New `#[rstest]`
  loader + operator-checklist/declare-only guard + P-036 Hard-check guard + suite note. **No production code**
  (the model already expresses every leg; `Scenario` @ `scenario.rs:69` is consumed by conductor-{cli,report,
  tauri,timeline,verify} but its public surface is unchanged → zero cross-crate blast radius).

## Test Commands
```bash
cargo nextest run -p conductor-core
cargo nextest run --workspace --profile ci
cargo clippy --workspace --all-targets -- -D warnings
cargo test --doc -p conductor-core
bash scripts/agent-run.sh run
```

## Acceptance Criteria
- (arch) Five TOMLs exist for P-025/026/027/032/036, each carrying its P-ID, `seed`, `slo_tier`, ≥1 phase;
  all load + garde-validate via `Scenario::from_toml_str` (per arch §Established Decisions [Scenario Config Format]).
- (arch/tests) P-025/026/027/032 carry empty `expected` (operator-checklist / declare-only — the drive+observe
  path, `scenario.rs:91-94`); the new loader guard asserts `s.expected.is_empty()` for them and does NOT
  inherit the prior `!expected.is_empty()` assertion (per arch §Probabilistic-Assertion Policy operator-checklist path).
- (tests) P-036 declares exactly one Hard `Contains "Previously seen"` check (recurrence is a deterministic
  runs.db cross-run lookup) (per test-plan §1 recurrence/known-residual paths + §4 rstest catalog fixtures).
- (security) Every new TOML deserializes + garde-validates; no scenario bypasses garde at load (a malformed
  variant still routes to `CoreError::Config`/`Validation`, never a panic) (per security-plan §Input Validation + §Error Handling).
- (tests/determinism) Goldens UNCHANGED — no insta snapshot under `crates/conductor-timeline/tests/snapshots/`
  references seeds 4317025/026/027/032/036 (per the 2026-06-22 seed-named-golden learning + arch §Determinism).
- (scope) No new `ComparisonKind`/`ClaimClass`/scenario field; `coverage.rs` untouched (60 P-IDs still
  zero-gap); the only `conductor-core` source delta is in the test module.
- (tests) `cargo nextest run -p conductor-core` and `cargo nextest run --workspace --profile ci` return 0;
  `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --doc -p conductor-core` 0;
  `bash scripts/agent-run.sh run` exits 0.

## Provenance
- Spec anchors: arch §Probabilistic-Assertion Policy (Hard vs CalibrationRegion; verdict-first lamp; the
  ManualCheck operator-checklist path), §Read-Back Dependency Posture (KnownResidual = pre-accepted residual,
  producer-assigned state), §Established Decisions [Scenario Config Format], §Standard Contracts (run-report
  envelope) · test-plan §1 (partially-testable catalog; untestable visual/operator-checklist zones — no
  human-in-loop CI) + §4 (rstest `#[case]` catalog fixtures) · security-plan §Input Validation + §Error
  Handling (garde at load, `ConfigError` route) · obs-plan §6 (Run-report envelope; `degraded_mode_response`
  is an Epoch-8 forward field) · design-system/layout-templates/a11y-plan ManualCheck+KnownResidual **rendering**
  (lamp glyphs, operator-checklist card, contrast pairs) — **forward Epoch-9 bindings, OUT of this chunk's scope**.
- Research files: `crates/conductor-core/src/{scenario.rs,expected.rs,lamp.rs,coverage.rs}`,
  `scenarios/{severity-tier-autonomous,incident-auto-resolution}.toml`, code-graph crate_edges trace.

## Implementation notes
- **The inverted guard is the crux.** Every existing `*_fixtures_load_and_validate` asserts
  `!s.expected.is_empty()`. The 3 constellation + P-032 scenarios have EMPTY `expected` by design — the new
  loader must not carry that assertion forward; instead assert `is_empty()` for them and the single Hard check
  for P-036. A copy-paste of the severity-lifecycle loader without removing the non-empty assert is the trap.
- **P-036 is one TOML, run twice** — the "two sessions" recurrence is a runs.db cross-run property (Epoch-8/10),
  NOT two TOMLs. The single scenario emits the fingerprint; the cross-run "Previously seen" is the index's job.
- **Empty `expected` is valid** — `#[serde(default)]` + `expected_defaults_to_empty_when_omitted`
  (`scenario.rs:660`) already prove it; garde still enforces non-empty `p_ids`, `P-NNN` format, ≥1 phase,
  bounded jitter on these files.
- **Seeds** follow `4317{NNN}` (P-ID number): 4317025/026/027/032/036 — distinct from severity-lifecycle's
  4317019..023, and feed no golden.
- **No rendering work.** `Lamp` already emits `[MANUAL]`/`[RESIDUAL]` (`lamp.rs:52`); the visual-domain extract
  criteria (operator-checklist card, dashed-ring lamp, contrast pairs) belong to Epoch 9 — do not pull them in.
- **Slight DoD refinement** (surface at P5): scope.md said "P-032 expresses KnownResidual"; per Q2 the TOML
  declares + documents the residual and the KnownResidual routing is Epoch-8 — a faithful, precedent-consistent
  realization, not a defect.
