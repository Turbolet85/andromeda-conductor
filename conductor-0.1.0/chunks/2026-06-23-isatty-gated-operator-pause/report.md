# Report — 2026-06-23-isatty-gated-operator-pause

**Chunk:** isatty-gated operator-pause — CLI interactive PauseResolver (inquire confirm · go/no-go) + tty-gated headless-never-block selector + paused-count spinner mirror, zero model change (conductor-cli)
**Date:** 2026-06-23T21:41:56Z
**Commits:** (pending P7 — this chunk is uncommitted; last_wrap commit was `62bdb7d` line-oriented-output-rendering)

## Changes (structured — detectors read this)
- **Files:** NEW `crates/conductor-cli/src/pause.rs`; MOD `crates/conductor-cli/src/render.rs` · `pipeline.rs` · `commands/run.rs` · `commands/suite.rs` · `main.rs` · `Cargo.toml`; MOD `Cargo.toml` (workspace) · `Cargo.lock`; (route/state) `.andromeda/master-route.md` · `conductor-0.1.0/working-route.md` · `.claude/session-handoff.md`.
- **Symbols / APIs:** NEW `conductor_cli::pause::CliResolver` (enum `{Interactive(PromptResolver), Headless(HeadlessResolver)}`, impl `PauseResolver`) + `CliResolver::select(Option<ProgressBar>) -> CliResolver` (the isatty gate); NEW `conductor_cli::pause::PromptResolver` (impl `PauseResolver`, inquire confirm); NEW `conductor_cli::render::hold_line(&HoldPoint) -> String` (+ private `hold_line_styled`). CHANGED `conductor_cli::pipeline::execute_scenario` — signature gains `resolver: &CliResolver` (callers: `run.rs:18`, `suite.rs:26`). No new IPC method / HTTP endpoint / port / socket / env var. No `conductor-core` change (reads `HoldPoint`/`Decision`/`PauseResolver`/`HeadlessResolver`/`resolve_hold` only).
- **Crates / modules:** NEW module `conductor-cli::pause` (declared `mod pause;` in main.rs). No crate added/removed. `conductor-cli` stays a leaf bin (0 inbound crate edges).
- **Dependencies:** ADDED `inquire = "0.9"` (→ 0.9.4) to `[workspace.dependencies]` + `inquire.workspace = true` in conductor-cli. New transitives in `Cargo.lock`: `crossterm 0.29.0` (already present via comfy-table), `fuzzy-matcher 0.3.7`, `derive_more 2.1.1` (+impl). None bumped.
- **Schema / config:** none (no new `Verdict`/`ReportState`/`Decision` variant; zero model change; no scenario-config / runs.db / journal schema touch).
- **Coverage of new surfaces:**
  - `CLI interactive operator-pause resolver (pause.rs)` → validation n/a (consumes typed `HoldPoint`; no deserialize boundary) · instrumentation: existing `tracing::debug!` on hold resolution in pipeline.rs (decision label only, `run_id`-tagged via the established subscriber) · PII: the `prompt` is redacted at the artifact edge by `resolve_hold`→`HoldResolution` (unchanged); the on-terminal `inquire` prompt is human-only, not an artifact/log · tests: unit (`select_off_tty_is_headless`, `headless_variant_proceeds`, `headless_variant_can_decline`) · a11y: `[HOLD]` ASCII prefix always present (never color-alone), keyboard-driven inquire confirm · tokens: amber via `lamp_code(Lamp::Hold)=179` (reused design-token mapping, not hardcoded).
  - `hold phase-line (render::hold_line)` → validation n/a · instrumentation n/a (presentation) · PII: no host-path/struct-name leak (covered by the shared `renders_leak_no_host_paths_or_struct_names` discipline; line carries only `scenario`/`p_id`/`step`) · tests: unit (`hold_line_plain_keeps_prefix_without_escapes`, `hold_line_colored_overlays_escapes_on_the_prefix`) · a11y: `[HOLD]` prefix always, color is tty-gated overlay · tokens: `lamp_code(Lamp::Hold)` (design-token).

## Deviations from intent
1. **`[HOLD]` phase-line emits from the interactive `PromptResolver` (to stderr), not from `pipeline.rs`** (plan step 3 placed it in pipeline.rs). Justification: an unconditional `pipeline.rs` print would corrupt the machine-parseable STDOUT in headless/piped mode; the design extract wants the line *"above the inquire prompt"* (stderr, part of the interactive presentation), so resolver-emission aligns BETTER with design-system §Component Patterns §1 and keeps the headless path silent. `render::hold_line` remains a tested pure function. **No spec change implied** (the design extract already specified this placement).
2. **No new E2E test added** (plan listed it as OPTIONAL — "cli_smoke.rs *or* new tests/operator_pause.rs"). Justification: the existing `run_by_name_blocks_without_pulse_and_exits_zero` / `suite_runs_the_catalog_and_exits_zero` / `piped_coverage_output_carries_no_ansi_escapes` already prove piped non-blocking + no-escapes; the genuinely-new gate/dispatch logic is unit-tested in `pause.rs`. The smoke (`agent-run.sh run` + direct `conductor run`) is the live non-hang proof.
3. **`deny.toml` untouched** — plan step 6's conditional ("only if `cargo deny` flags one") did not trigger; `inquire`'s tree is permissive-licensed + advisory-clean.

## Decisions & corrections
- **D1** — `execute_scenario` takes a concrete `resolver: &CliResolver` (not generic `<R: PauseResolver>`): both callers live in the same crate and pass the enum; the trait is not object-safe (`-> impl Future`), so `&dyn` was never an option; concrete avoids generic-bound churn.
- **D2** — the `inquire`-error → `Decision` mapping: `Ok(true)→Go`, `Ok(false)→NoGo`, `Err(_)→NoGo` (when `allow_no_go`, else `Go`). Implemented via a catch-all `Err(_)` arm — version-robust, no need to enumerate `OperationCanceled`/`OperationInterrupted`.
- **D3** — `ProgressBar::suspend(f)` is the spinner-freeze primitive (the heartbeat pauses at its current count during the prompt, then resumes) — the faithful CLI mirror of the Epoch-9 Tauri titlebar freeze.
- **Refinement** — the `[HOLD]` phase-line + `inquire` prompt both render to **stderr** (alongside the spinner), keeping STDOUT clean for the machine-parseable results table (consistent with the ch3 spinner-on-stderr decision).
- **Reused, not reinvented** — the isatty gate uses `std::io::IsTerminal` (same primitive as `render::stdout_color()`); the `[HOLD]`/amber mapping reuses `Lamp`/`lamp_code`; zero new status-classification.

## Outcome
- **Acceptance criteria: all met.** Resolver in conductor-cli implementing core `PauseResolver`, zero new core types; non-TTY never blocks (unit `select_off_tty_is_headless` + smoke no-hang + direct `conductor run` exit 0); `[HOLD]` prefix always present, color stripped unambiguous (unit); spinner suspends during prompt; no host-path/struct-name leak; inquire audit+deny green; determinism preserved (pause wall-clock-only; current_thread tests).
- **Gates green:** `cargo nextest run -p conductor-cli` 22/22 · `cargo nextest run --workspace --profile ci` 388/388 (383→388) · `cargo test --workspace --doc` ok · `cargo clippy --workspace --all-targets -- -D warnings` clean (1 fix: `doc_lazy_continuation` reworded) · `cargo audit` exit 0 (only the pre-existing `number_prefix` allow) · `cargo deny check` exit 0 (advisories/bans/licenses/sources ok).
- **Smoke:** `bash scripts/agent-run.sh run` ran the full release gate to completion **without hanging** in a non-tty context (the headless-never-block proof); direct `conductor run error-baseline-spike` → `[BLOCKED] error-baseline-spike` exit 0.
