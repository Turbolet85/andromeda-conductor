# Report — 2026-06-23-line-oriented-output-rendering

**Chunk:** Line-oriented output rendering — owo-colors status lines + indicatif run progress + comfy-table 60-P-ID coverage table over run/suite/report/preflight; mirrors Markdown-report lamp mapping, zero model change (conductor-cli)
**Date:** 2026-06-23T20:40:17Z
**Commits:** (uncommitted at report time — this wrap commits) Epoch 8 ch3/5

## Changes (structured — detectors read this)
- **Files:** NEW `crates/conductor-cli/src/render.rs`, `crates/conductor-cli/src/commands/coverage.rs`; MOD `Cargo.toml`, `crates/conductor-cli/Cargo.toml`, `crates/conductor-cli/src/{main,cli}.rs`, `crates/conductor-cli/src/commands/{mod,suite,report,preflight}.rs`, `crates/conductor-cli/tests/cli_smoke.rs`, `deny.toml`, `Cargo.lock`.
- **Symbols / APIs:** NEW CLI verb **`conductor coverage [--write]`** (clap `Commands::Coverage`). NEW module `conductor-cli::render` (pub `status_line` · `results_table` · `coverage_table` · `spinner` · `paint` · `lamp_code` · const `ID_CYAN`). NEW `commands::coverage`. `print_record` now delegates to `render::status_line`; `report`/`suite` render comfy-tables; `preflight` colorizes its prefix lines. **No new** ports / sockets / IPC methods / events / env vars / workspace crates. The four MCP read-back tools + the `:4317` socket are unchanged.
- **Crates / modules:** added `render` + `commands::coverage` modules inside the existing `conductor-cli` crate. No workspace-member change.
- **Dependencies:** ADDED `owo-colors 4.3.0`, `indicatif 0.17.11`, `comfy-table 7.2.2` (+ transitives `console`, `crossterm`, `crossterm_winapi`, `number_prefix`, `unicode-width`). `cargo audit` exit 0; `cargo deny check` exit 0 (after deny.toml updates below). `Cargo.lock` updated (committed by this wrap).
- **Schema / config:** none (no config keys, no migrations). **`deny.toml` policy:** `[advisories] ignore += RUSTSEC-2025-0119` (number_prefix unmaintained — via THIS chunk's indicatif; non-actionable, no upgrade) · `[licenses] allow += "Zlib"` (foldhash 0.2.0 via rusqlite→hashbrown — **PRE-EXISTING in HEAD's lock**, not introduced here; OSI+FSF permissive).
- **Coverage of new surfaces:**
  - `conductor coverage` verb (cli) → validation n/a (no external input; `--write` writes a FIXED `coverage-matrix.md`, no user-path interpolation) · instrumentation: no new span (presentation, not an obs §4 must-trace op; the scenario emit/verify/report spans + `scenario.run` root are the Epoch-10 driver's) · PII n/a · tests unit+e2e ✓ (render unit + cli_smoke ×3) · a11y: cli not-assertable, never-color-alone ✓ · tokens: design-system ANSI ✓ (Lamp→114/203/179/146/246/60, ID-cyan 117), not hardcoded.
  - results/coverage comfy-tables + colored status lines (cli) → tokens ✓ (Lamp→ANSI per design §Surface: cli) · status never color-alone ✓ (ASCII `[PASS]`/`[FAIL]`/`[HOLD]`/`[MANUAL]`/`[RESIDUAL]`/`[BLOCKED]` prefix always present; color tty-gated via `stdout_color()` = IsTerminal + NO_COLOR + TERM≠dumb) · tests ✓ (render unit both-paths + piped-no-escape E2E + real-boot eyeball: 0 escapes, 60 rows, obs-log on stderr) · redaction ✓ (no host-path/struct-name leak — render test denylist; output is path-free `RunRecord`/`coverage_matrix()` data).
  - indicatif spinner (suite, stderr) → tty-gated, hidden when stderr is not a tty (no stdout/agent corruption); the operator-pause spinner-freeze is ch4.

## Deviations from intent
1. **`deny.toml` edited — NOT in the plan's Files-to-modify.** Two justified entries, both required for the plan's "cargo deny check green" acceptance criterion: (a) `ignore RUSTSEC-2025-0119` — THIS chunk's indicatif transitive (number_prefix), unmaintained-not-vulnerable, no upgrade; in-scope consequence of the dep. (b) `allow "Zlib"` — **pre-existing**: `foldhash 0.2.0` was already on HEAD's committed lock (confirmed via `git show HEAD:Cargo.lock`), so `cargo deny check licenses` was already red before this chunk; fixed here because the chunk + CI gate cannot be green otherwise, and Zlib is OSI+FSF permissive (consistent with the allowlist's "permissive; copyleft/unknown denied" intent). **Latent supply-chain drift surfaced, not a regression this chunk caused.**
2. **Dropped owo-colors `supports-colors` feature** (plan listed it) — comfy-table cannot use owo-colors' `if_supports_color`, so a single `stdout_color()` gate (IsTerminal + NO_COLOR + TERM=dumb) drives BOTH the owo-colors lines and the comfy-table cells, giving one consistent tty decision. `OwoColorize::color()` (default feature) suffices; the feature + its `supports-color` transitive are unneeded (leaner).
3. **comfy-table `ContentArrangement::Disabled`** (vs "terminal-width-detected") — content-sized columns guarantee scenario names + status tokens never wrap/truncate, preserving the `cli_smoke` substring contracts; the right call for a machine-parseable agent-driven tool.
4. **`suite` per-scenario lines → spinner (stderr) + final results table (stdout)** — names live in the table (E2E preserved); `print_record` is now `run`-only.
5. **Small helper duplication** (`wire`/`latency`/`fingerprints`/`ABSENT` mirror `conductor-report`'s private ones) — as the plan anticipated; keeps the chunk zero-report-change. DRY follow-up if a shared row projection is later wanted.

## Decisions & corrections
- **P4 D1 (user):** add a new `conductor coverage` verb (render the static 60-P-ID matrix as a comfy-table + `--write` regenerates `coverage-matrix.md` at repo root) — supersedes the arch extract's "no new verb in Epoch 8" inference; **expected spec amendment: layout-templates §cli Primary screens += `conductor coverage`** (precedented by ch2's `preflight` verb).
- **P4 D2 (user):** `conductor report` stdout switches from raw Markdown to the colored comfy-table (with a `Run report <id>` header preserving the E2E tokens); the on-disk `<run_id>.md` artifact stays Markdown.
- **Carried follow-up CLOSED:** `coverage-matrix.md`-at-repo-root is now regenerable via `conductor coverage --write`.
- **New awareness for the user:** `cargo deny check` was failing on HEAD (foldhash Zlib) before this chunk — the prior session's recorded gates listed `cargo audit` but not `cargo deny`; worth confirming the CI deny step runs.

## Outcome
All 10 plan acceptance criteria met. Gates: `cargo nextest run -p conductor-cli` 17/17 · `cargo nextest run --workspace --profile ci` 383/383 · `cargo test --workspace --doc` ok · `cargo clippy --workspace --all-targets -- -D warnings` clean · `cargo audit` exit 0 · `cargo deny check` exit 0 · `bash scripts/agent-run.sh run` exit 0. Real-boot smoke (boot-path changed): `conductor coverage` renders 60 P-rows, 0 ANSI escapes piped, obs-log on stderr, clean stdout. Zero core/seam model change.
