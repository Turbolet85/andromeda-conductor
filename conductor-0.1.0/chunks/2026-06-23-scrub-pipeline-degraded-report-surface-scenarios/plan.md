# Plan — 2026-06-23-scrub-pipeline-degraded-report-surface-scenarios: "Scrub/pipeline/degraded/report-surface scenarios"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-06-23-scrub-pipeline-degraded-report-surface-scenarios · **Version:** conductor-0.1.0 · **Generated:** 2026-06-23T16-12-27Z

## Goal
Author the final scenario-catalog family — **six declarative `scenarios/*.toml` files** covering the **nine
drivable P-IDs** of the scrub / report-surface / pipeline-ops / degraded-mode groups — plus the `conductor-core`
scenario-model **test wiring** that loads and guards them. The four StaticOnly P-IDs (P-049/050/051/054) get
**no scenario** (their coverage is already complete via the `coverage.rs` classification; authoring a live
scenario for a statically-verified claim would be wrong — per test-plan §11 + security-plan §Threat Model).

This is **chunk 8/8 of Epoch 7 — it closes the scenario catalog.** It is the **eighth consecutive Epoch-7 chunk
to hold zero model change**: every realization uses an existing field — Hard via `ComparisonKind::{Absent,
CountAtLeast,Contains}` + `ClaimClass::Hard`, declare-only via the `#[serde(default)]` empty `expected`
(`expected.rs:14–64`, `scenario.rs:88–97`). The P4 design decisions (resolved with the user): **(Q1)
outcome-coherent grouping → 6 files**; **(Q2) Hard where a deterministic read-back token exists, else
declare-only.**

The realization spectrum (per arch §Read-Back Dependency Posture + the testing.md session learnings):
- **pii-scrub is fully Hard now** — it reuses the shipped `PiiCorpus` (`conductor-emit/src/pii.rs`); the seeded
  PII carries **stable category sentinels** (`@example.com`, `sk_live_`, `Bearer `, `password=`) that appear in
  read-back ONLY if scrubbing fails, so `Absent "<sentinel>"` is a clean Hard negative assertion (P-047/P-048).
- **findings-counter and the P-056 prospective leg are Hard** (deterministic count / retroactive-cue absence).
- **report-render, cadence, the P-055 timing leg, and degraded-mode are declare-only** — pure-latency SLO
  measurements (Epoch-8) and the `degraded_mode` **KnownResidual** (producer-assigned, P-032 precedent).

Per the whole catalog's semantics, a TOML **declares phases + `expected` + `slo_tier`; the emitter binding**
(e.g. `pii_trace_request` → timeline) **and the SLO measurement are Epoch-8** — identical to every prior
catalog entry (error-baseline-spike declares `Contains "ErrorRateSpike"` without naming the error-span emitter).

## Implementation Steps
1. **`scenarios/pii-scrub.toml`** (P-035/047/048, Auto → **Hard**, `<5s`, seed `4317035`). Phases driving a
   PII-laden workload across traces + logs (e.g. `[[phases]]` `emit-pii-traces` signal=`Traces`, `emit-pii-logs`
   signal=`Logs`). `[[expected]]` blocks — all `class="Hard"`:
   - `Absent` on a representative subset of stable sentinels: `"@example.com"` (email), `"sk_live_"` (API key),
     `"Bearer "` (bearer), `"password="` (secret `key=value`) — each proving the raw value was scrubbed.
   - one `Contains "<structural marker>"` (structure preserved, P-035) — an **inferred** substring-tolerant
     token (a surviving service/span name or `field_key`), flagged in a comment as an Epoch-8 calibration point
     (the P-018/P-036 inferred-token precedent). MUST be a **different token** from any `Absent` sentinel
     (testing.md 2026-06-22 Absent/Contains-same-token rule).
   Header comment documents: reuses `PiiCorpus`, the 7 categories, and that P-048 (no raw values stored) is the
   same negative assertion read back from the corpus.
2. **`scenarios/report-render-surface.toml`** (P-037, DriveObserve → **ManualCheck**, `<5s`, seed `4317037`).
   Phases drive one incident; **omit `[[expected]]`** (operator opens the in-app Report surface and confirms
   render <2s + keyboard-nav + section-collapse — a visual/operator-checklist claim; the <2s measurement is
   Epoch-8). Header comment documents the operator-checklist intent.
3. **`scenarios/findings-counter-refresh.toml`** (P-045, Auto → **Hard count**, `<5s`, seed `4317045`). Phases
   emit a known **N** (e.g. 3) unread active incidents. `[[expected]]` `kind="CountAtLeast"`, `class="Hard"`,
   `expected="3"` — the findings counter = `len(query_incident_list | unread/active)` is deterministic. Comment
   notes the ≤1s refresh **timing** is Epoch-8-measured (declared via SLO tier, not asserted as a token).
4. **`scenarios/cadence-config.toml`** (P-052, Auto → **declare-only**, `<20s`, seed `4317052`). Phases drive a
   steady workload; **omit `[[expected]]`**. Header comment: the operator sets a known cadence config
   (baseline/accelerated/reflection) during a go/no-go hold; the L4-invocation-rate match is an Epoch-8
   measurement with no standalone content token (operator-config + deferred-measurement).
5. **`scenarios/threshold-hot-reload.toml`** (P-055/056, Auto → **MIXED**, `<5s`, seed `4317055`). Phases: a
   baseline phase, then a post-reload phase (the operator hot-reloads a threshold mid-run during a hold).
   `[[expected]]` — **one** leg: `kind="Absent"`, `class="Hard"`, `expected="<retroactive cue token>"` — the
   P-056 prospective-only guarantee (the pre-change baseline produces NO retroactive incident; inferred token,
   flag as Epoch-8 calibration). The P-055 <2s reload **timing** is the declare-only leg (comment only). First
   catalog file mixing a Hard leg with a declare-only leg in one scenario.
6. **`scenarios/degraded-mode-report.toml`** (P-053, Auto → **declare-only KnownResidual**, `<20s`, seed
   `4317053`). Phases drive the same hard-signal workload; **omit `[[expected]]`**. Header comment DOCUMENTS the
   residual: the operator disables the model / sets the fallback tier; `retrieve_report(degraded_mode=true)` is a
   **pre-accepted residual** (arch §Standard Contracts lists "a `retrieve_report` result returned under
   `degraded_mode`" as a KnownResidual example), so the Fail→KnownResidual routing is the Epoch-8 evaluator's
   (the scenario model has NO `state` field — ReportState is producer-assigned; P-032 precedent).
7. **`crates/conductor-core/src/scenario.rs`** — **test wiring only**, appended inside `#[cfg(test)] mod tests`
   **before the closing brace at line 774** (after the constellation suite guard), following the established
   per-family shape:
   - **loader rstest** — `#[case]` rows for the 6 stems + their `p_ids`; assert `name`/`p_ids`. **No blanket
     `!expected.is_empty()` assertion** (the family is mixed-shape — the constellation inversion).
   - **Hard-shape guards** — pii-scrub: `any(kind==Absent && class==Hard)` + `any(kind==Contains)`;
     findings-counter: `any(kind==CountAtLeast && class==Hard)`; threshold-hot-reload: `any(kind==Absent &&
     class==Hard)`.
   - **declare-only guard** — `report-render-surface`, `cadence-config`, `degraded-mode-report` assert
     `expected.is_empty()`.
   - **suite guard** — the family exercises BOTH shapes: `any(!expected.is_empty()) && any(expected.is_empty())`
     (the constellation `any(empty) && any(non-empty)` precedent).
   No struct/enum change; no new `ComparisonKind`.

## Codebase touchpoints
### New files
- `scenarios/pii-scrub.toml` — P-035/047/048 Hard PII-scrub (Absent sentinels + Contains structural marker).
- `scenarios/report-render-surface.toml` — P-037 operator-checklist (empty `expected`).
- `scenarios/findings-counter-refresh.toml` — P-045 Hard `CountAtLeast`.
- `scenarios/cadence-config.toml` — P-052 declare-only (operator-config).
- `scenarios/threshold-hot-reload.toml` — P-055/056 MIXED (P-056 Hard `Absent` + P-055 declare-only timing).
- `scenarios/degraded-mode-report.toml` — P-053 declare-only KnownResidual (degraded_mode).
### Files to modify
- `crates/conductor-core/src/scenario.rs` — test wiring only, appended before `mod tests` close @ `scenario.rs:774`; preserve the existing per-family rstest + suite-guard pattern (`scenario.rs:699–773`). No production-symbol change ⇒ zero cross-crate blast (`Scenario::from_toml_str` has 0 production callers per the code-graph trace).

## Test Commands
```bash
cargo nextest run -p conductor-core
cargo nextest run --workspace --profile ci
cargo clippy --workspace --all-targets -- -D warnings
cargo test --doc -p conductor-core
bash scripts/agent-run.sh run    # agent-driven harness smoke (must exit 0); goldens expected UNCHANGED
```

## Acceptance Criteria
- (arch) Six TOMLs exist under `scenarios/`, each loadable via `Scenario::from_toml_str` + garde-valid, carrying
  `p_ids` + `slo_tier` + the resolved `expected` shape (per arch §Established Decisions [Scenario Config Format]).
- (arch/scope) No new `ComparisonKind`, no `Scenario`/`ExpectedCheck` struct change, no new env var (zero model
  change) — existing `Absent`/`CountAtLeast`/`Contains` + empty `expected` serde-default suffice (per scope).
- (security) `pii-scrub` declares Hard `Absent` over the stable PiiCorpus sentinels (`@example.com`/`sk_live_`/
  `Bearer `/`password=`) + a distinct Hard `Contains` structural marker (per security-plan §Input Validation +
  §Data Protection; reuses `conductor-emit/src/pii.rs`).
- (security) The four StaticOnly P-IDs get no TOML; P-049 encryption-at-rest stays covered by the existing
  preflight canary (per security-plan §Threat Model Summary).
- (security/obs) No absolute host path or seam struct-name appears in any new TOML or resulting run-report
  artifact (per security-plan §Error Handling + obs-plan §6).
- (tests) `findings-counter-refresh` declares Hard `CountAtLeast`; `threshold-hot-reload` declares the P-056 Hard
  `Absent` leg (+ P-055 declare-only timing); `report-render-surface`/`cadence-config`/`degraded-mode-report`
  declare empty `expected`; `degraded-mode-report` documents the degraded_mode KnownResidual (per test-plan §4 +
  arch §Read-Back Dependency Posture).
- (tests) `scenario.rs` adds the loader rstest + per-shape guards + a suite guard exercising `any(non-empty) &&
  any(empty)`; the mixed-shape loader carries no blanket `is_empty` assertion (per test-plan §4; testing.md
  constellation precedent).
- (tests) `cargo nextest run --workspace --profile ci` returns 0; `clippy -D warnings` clean; `cargo test --doc`
  0; the determinism-replay insta goldens are **UNCHANGED** (the new seeds feed no golden); `agent-run.sh run`
  exits 0.
- (coverage) Epoch 7 is closed — `coverage.rs` already classifies all 13 P-IDs (Auto×8/DriveObserve×1/
  StaticOnly×4); no classification change.

## Provenance
- **Spec anchors:** arch §Established Decisions [Scenario Config Format] · §Read-Back Dependency Posture
  (degraded_mode ⇒ KnownResidual; ManualCheck via operator-checklist) · §Standard Contracts (MCP tools,
  preflight `blocked`) · §Probabilistic-Assertion Policy. security-plan §Input Validation · §Data Protection
  (PII scrub, 7 categories) · §Threat Model Summary (P-049 canary; StaticOnly out-of-scope) · §Error Handling
  (artifact sanitization). test-plan §1 (coverage tiers) · §4 (unit/rstest table-driven) · §6 (E2E Path 1/7) ·
  §11 (anti-patterns: no scenario for StaticOnly, no extra listener). obs-plan §3/§4/§6 (envelope fields,
  `read_back_observed_at`, conformance — Epoch-8/9 bindings). `coverage.rs:107–130` (authoritative modes).
- **Research files inspected:** `crates/conductor-core/src/{scenario.rs,expected.rs,coverage.rs}`,
  `crates/conductor-emit/src/pii.rs`, `scenarios/{error-baseline-spike,halo-breathing-encoding,
  project-context-grounding,root-span-error-scope}.toml`. Code-graph trace:
  `runs/2026-06-23T16-12-27Z-phase/tree-query-2026-06-23-scrub-pipeline-degraded-report-surface-scenarios.json`.
- **Session learnings applied (`.claude/rules/testing.md`):** Absent/Contains-same-token contradiction (2026-06-22);
  empty-`expected` DriveObserve + `any(empty)&&any(non-empty)` suite guard (2026-06-22 constellation);
  KnownResidual is producer-assigned → declare-only (2026-06-22); inferred substring-tolerant tokens as Epoch-8
  calibration points (2026-06-22 fingerprint-storm); seed-named goldens — grep the seed, not the stem (2026-06-22).

## Implementation notes
- **Seeds `4317<NNN>` per P-ID** (035/037/045/052/053/055/056). Before committing, verify each feeds no golden:
  `ls crates/conductor-timeline/tests/snapshots/replay__fixture_seed_<seed>.snap` returns nothing (the 2026-06-22
  golden-blast learning — goldens are seed-named, not scenario-named). Goldens MUST stay unchanged.
- **pii-scrub emitter binding is Epoch-8.** The TOML declares the PII-laden workload + the Absent/Contains
  expectations; wiring `pii_trace_request`/`pii_logs_request` to the timeline is the Epoch-8 CLI/run path — same
  declared-not-wired stance as every catalog entry. Do NOT add a new `EmissionSpec` variant for PII (would be a
  model change).
- **Inferred tokens** (the pii-scrub `Contains` structural marker; the P-056 retroactive-cue `Absent` token) are
  substring-tolerant and Epoch-8 calibration points — pick a plausible stable token, comment it as inferred, and
  leave exact-value calibration to the evaluator (the `LatencyRegression`/`Previously seen` precedent).
- **Outcome-coherence:** keep each file's tokens non-contradictory — pii-scrub's `Absent` sentinels and its
  `Contains` structural marker are different tokens; threshold-hot-reload carries only the single P-056 `Absent`
  leg (the P-055 timing is comment-only).
- **Mixed-shape loader:** because the family spans Hard (pii-scrub/counter/threshold) and empty (render/cadence/
  degraded), the loader rstest must NOT assert `!expected.is_empty()` for all rows (the constellation inversion);
  the per-shape guards carry the precise assertions.
