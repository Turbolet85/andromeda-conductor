# Report — 2026-06-26-live-counter-channel-stream

**Chunk:** Live-counter Channel stream — Tauri 2 IPC Channel backend→frontend live emission counters + target status driving the titlebar runState; real start_run execution via pipeline-to-library extraction; retire the DEV run-state cycler; zero engine/seam model change (conductor-tauri)
**Date:** 2026-06-26
**Commits:** (uncommitted at wrap — implement does not commit; prior `last_wrap` HEAD = 40a6c7b ch3)

## Changes (structured — detectors read this)
- **Files:** NEW `crates/conductor-run/{Cargo.toml,src/lib.rs}` · MOD `Cargo.toml` (workspace), `Cargo.lock`, `crates/conductor-cli/Cargo.toml`, `crates/conductor-cli/src/main.rs`, `crates/conductor-cli/src/commands/{mod,run,suite,preflight}.rs`, `crates/conductor-tauri/Cargo.toml`, `crates/conductor-tauri/src/{commands,main}.rs`, `crates/conductor-tauri/ui/src/App.tsx` · DEL `crates/conductor-cli/src/pipeline.rs`
- **Symbols / APIs:**
  - NEW pub (conductor-run): `Preflight` · `preflight()` · `readiness()` · `execute_scenario<R: PauseResolver>()` (generic resolver, was bin-local `&CliResolver`) · `persist()` (relocated from cli `commands/mod.rs`) · `RunEvent{stage,count}` · `RunStage{Progress,Blocked,Done,Aborted}` (serde lowercase) · `drive_run<E,A>()`
  - CHANGED (conductor-tauri): `start_run(selection, on_event: Channel<RunEvent>, state)` now drives the real pipeline on a background `current_thread` runtime + streams (was the ch3 validate-only scaffold returning a label); `stop_run(state)` sets a cooperative abort flag; NEW managed `RunControl{abort: Arc<AtomicBool>}` REPLACES ch3's `RunPhase`/`RunState=Mutex<RunPhase>` (retired). `list_scenarios` unchanged.
  - CHANGED (conductor-cli): `commands/mod.rs::persist` removed → re-imported from `conductor_run::persist`; `run/suite/preflight` re-point `crate::pipeline` → `conductor_run` (aliased `pipeline`).
  - **New IPC surface:** the live-counter Tauri 2 `Channel<RunEvent>` (backend→frontend; the architecture's Real-time Strategy `Channel`). No HTTP/socket. No new env var (reuses `CONDUCTOR_RUNS_DIR`/`CONDUCTOR_SCENARIOS_DIR`/`CONDUCTOR_CONTRACT_MANIFEST`). No new port.
- **Crates / modules:** ADDED `conductor-run` (9th workspace member, sits above the seams below the bins). conductor-cli: removed the `pipeline` module. conductor-tauri: gained the run-driver (`run_thread`) in `commands.rs`.
- **Dependencies:** NO new external crate. `conductor-run` deps = existing workspace seams (core/timeline/emit/verify/report) + anyhow/tracing/serde (+ dev tokio/assert_fs). conductor-tauri += `conductor-run`, `tokio` (rt) — both already in the tree. conductor-cli −= conductor-timeline/emit/verify (their last user was pipeline.rs), += conductor-run. npm: none (App.tsx uses the existing `@tauri-apps/api/core` `Channel`). `Cargo.lock`: +`conductor-run` path entry only.
- **Schema / config:** none. `RunRecord` (run-report envelope) UNCHANGED. `RunEvent` is a transient IPC payload, not persisted/indexed.
- **Coverage of new surfaces:**
  - `Tauri Channel start_run + RunEvent stream` → validation: `selection` via `validate_selection` against the catalog + dirs via `resolve_under` ✓ · instrumentation: manual `tauri.command.start_run`/`stop_run` span + `run_id` boundary log ✓ (obs rule 2026-06-26) · PII: errors `sanitize_error`'d at the edge; `RunEvent` carries only `{stage, count:u64}` — no paths/struct names ✓ · tests: `conductor-run::drive_run` deterministic unit (event sequence + abort + persist) ✓; **tauri::test integration ✗ (deferred — operator-gated GUI smoke, zero-flakiness rationale)** · a11y: drives the EXISTING titlebar `aria-live="assertive"` region (ch2) — no new/duplicate live region; count not-color-alone via existing `titlebar__count--${runState}` ✓ · tokens: count rendered via existing `--count-*` tokens (no hardcoded values) ✓
  - `conductor-run library (execute_scenario/persist/drive_run)` → validation: n/a (scenarios garde-validated at load) · instrumentation: relocated `tracing` in preflight/coarse_emit ✓ · PII: relocated `redact_value`/`sanitize` edges ✓ · tests: 4 new unit (blocked path, seed-stable envelope, drive_run stream+persist, abort) ✓ · a11y/tokens: n/a

## Deviations from intent
1. **No `tauri::test` mock-runtime test (plan §7b acceptance criterion).** `start_run` spawns a background thread + streams the Channel asynchronously; asserting the stream would be thread-timing-dependent → violates the zero-flakiness bar (no nextest retries). The run logic is covered deterministically by `conductor-run::drive_run` unit tests instead; the thin Tauri command wrapper is clippy-clean + exercised by the operator-gated GUI smoke (same posture as ch1/ch2/ch3 — GUI boot build-verified, not headless-smoke-tested until the Epoch-9 tauri-driver a11y chunk). Acceptance-criterion INTENT (run logic tested) met via conductor-run; LETTER (a tauri::test) defers to the GUI test-harness chunk.
2. **`drive_run` + `RunEvent`/`RunStage` live in `conductor-run`, not `conductor-tauri/src/run.rs`** — makes the driver unit-testable (Preflight's private fields are crate-local). The plan explicitly permitted "fold into commands.rs."
3. **ch3's `RunPhase` enum retired** for the Channel-driven frontend + minimal `RunControl{abort}` (a backend phase mirror is redundant once the Channel is authoritative; an unused `RunPhase::label` would fail clippy). This realizes the CARRY "reconcile ch3's start/stop→setRunState."
4. **`count` = scenarios-completed**, not per-emission (the Blocked path emits nothing — a per-emission counter is unobservable until Epoch 10; ticking per scenario makes the live Channel genuinely exercisable: 0→1 single, 1→N suite). Documented in `RunEvent`.

## Decisions & corrections
- **P4 (AskUserQuestion):** (1) pipeline library home → NEW `conductor-run` crate (conductor-core architecturally impossible — composition root importing timeline/emit/verify/report, which all depend on core); (2) execution model → background core-owned `current_thread` runtime + live-streaming Channel + real `stop_run` abort (option A).
- **Research correction (scope.md amended):** "&CliResolver → a trait object" → **generic bound `<R: PauseResolver>`** — `PauseResolver::resolve` returns `impl Future`, so the trait is NOT object-safe; matches the existing generic `resolve_hold<R>`.
- **No capability change:** app-command `Channel` args are not ACL-gated (security.md 2026-06-26); the deny-by-default window allowlist stays the 3 entries — confirmed in research, not edited.
- **Name-collision gotcha applied:** `list_scenarios` calls `conductor_core::list_scenarios` fully-qualified; `conductor_run::preflight`/`drive_run` called fully-qualified — avoids `generate_handler!` binding the wrong signature.
- **Parity is structural:** both bins route through `conductor_run::execute_scenario`/`persist`; the existing `cli_smoke` E2E (Blocked envelope) stays green = the CLI is byte-identical (test-plan Path 7).

## Outcome
- **Acceptance criteria:** met, except §7b's literal tauri::test (intent met via conductor-run — see Deviation 1). Determinism, parity, no-new-capability, generic-resolver, core-owned runtime, Channel-streaming, DEV-cycler-retire all delivered.
- **Gates (all green, 1 iteration):** `cargo nextest run --workspace --profile ci` = 402 (+4) · `cargo test --workspace --doc` ✓ · `cargo clippy --workspace --all-targets -- -D warnings` ✓ · frontend `npm run build` + `npx tsc --noEmit` ✓ · `npm audit` 0 · `cargo audit` ✓ (18 allowed warnings) · `cargo deny check` ✓ (advisories/bans/licenses/sources ok).
- **Smoke (boot-path changed):** `bash scripts/agent-run.sh run` → exit 0 (ensure-frontend → nextest → doctest → clippy → scenario legs).
- **Carried:** operator visual check — GUI window boot + live Channel round-trip (count ticking, Start/Stop) build-verified, not headless-smoke-tested; `ui/src/vite-env.d.ts` now unused (harmless, optional cleanup).
