# Plan — 2026-06-27-ci-quality-gate-config: "CI quality-gate config"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-06-27-ci-quality-gate-config · **Version:** conductor-0.1.0 · **Generated:** 2026-06-27T12:53:48Z

## Goal
Promote Conductor's existing GitHub Actions **build + test** pipeline into a **quality-gated** one: enforce a line-coverage floor, assert the zero-retry flakiness budget at the CI level, and upload the JUnit + coverage reports as build artifacts — entirely on the `windows-latest` dev-OS target, with **no live Pulse / Linux+xvfb / webview dependency** (scope.md; arch §CI/CD). This is the `quality-gate-config-emit` step named verbatim in test-plan §9.

The scaffold is already staged, so the change is surgical: `ci.yml:70–71` already carries a `Coverage (measure only)` step whose own comment says *"the coverage threshold + JUnit/coverage artifact upload are Epoch 10"* (this chunk), `cargo-llvm-cov` is already installed (`ci.yml:33`), `.config/nextest.toml` already emits JUnit (`[profile.ci.junit]` → `target/nextest/ci/junit.xml`) and already pins `retries = 0` in both profiles. The work is therefore **gating + uploading**, not introducing coverage tooling or JUnit emission. The binding gate is **line ≥ 60%** via `cargo-llvm-cov --fail-under-lines 60` over the workspace, excluding generated/stub/fixture code (test-plan §10).

The chunk is **CI-config only — zero engine/seam diff** (arch + scope.md boundaries), preserves the discrete `cargo audit` + `cargo deny check` hard-fail steps untouched (security-plan §Dependency Security), and keeps uploaded artifacts free of absolute host paths / internal struct names (security-plan §Error Handling; obs-plan §6). It also establishes the **artifact-upload + gate-step scaffold** the downstream "Obs CI conformance gate" and "A11y CI gate" chunks reuse (arch / tests / obs / security contract bindings) — but implements none of those gates here.

## Implementation Steps
1. **Promote the coverage step → a gate** (`ci.yml`, the `rust` job's `Coverage (measure only)` step). Replace `--summary-only` with an invocation that (a) emits report files — `--lcov --output-path lcov.info` and `--cobertura coverage.xml` (test-plan §9), (b) applies the `--ignore-filename-regex` exclusions for generated code (tonic/prost OTLP codegen), rmcp/tauri stubs, and rstest/insta fixtures (test-plan §10), and (c) enforces **`--fail-under-lines 60`** so CI hard-fails below the floor. Retain a rationale comment, retargeted from "measure only / Epoch 10" to this marker.
2. **Upload the artifacts** (`actions/upload-artifact@v4`): one step for the coverage reports (`lcov.info` + `coverage.xml`, optionally the HTML dir), one for `target/nextest/ci/junit.xml`. Gate both with `if: always()` so a failing coverage/test gate still publishes the diagnostic artifacts (obs-plan §9; test-plan §9). Use stable artifact names (`coverage-report`, `test-results`) the downstream obs gate can reuse.
3. **Assert the flakiness budget at CI level** — add a lightweight step confirming `.config/nextest.toml`'s `[profile.ci]` is `retries = 0` (fail the job if a non-zero retry is ever introduced), per the extracts' "CI-level assertion, do not rely on the local file alone" (arch §Cross-cutting Patterns; test-plan §10). Comment it against the determinism invariant.
4. **JUnit consumption = artifact-only (recommended default; see P5 decision #1).** Do **not** add `dorny/test-reporter` and do **not** elevate `permissions` beyond `contents: read`. (Rationale: solo dev on a long-lived `build/` branch with no PR-review surface to annotate; JUnit/coverage are consumed via the CI artifact API / `agent-run logs`.) The alternative is surfaced at review.
5. **Keep `windows-latest` single-OS (out-of-scope decision; see P5 #2).** Do not add an ubuntu/macos runner matrix in this chunk; it is a separate concern and the webview-E2E leg it implies is display-gated.
6. **`.config/nextest.toml`** — confirm no functional change is needed (JUnit + `retries = 0` already present); at most update the provenance comment on `[profile.ci.junit]` to point at this marker.
7. **Verify on the Windows host** (the chunk's gates): the new `--fail-under-lines 60` coverage command runs green today (current coverage ≥ 60%) and writes the report files; `agent-run.ps1 run` stays green; `cargo audit` + `cargo deny check` stay green; `git diff` shows no `crates/**` change.

## Codebase touchpoints
### New files
- none.
### Files to modify
- `.github/workflows/ci.yml` — promote the coverage step into a `--fail-under-lines 60` gate with file emission + exclusions; add the `actions/upload-artifact@v4` steps (coverage + junit, `if: always()`); add the zero-retry assertion step. Preserve job structure, the per-step rationale-comment house style, pinned `actions/*@vN` versions, and `runs-on: windows-latest`.
- `.config/nextest.toml` — no functional change expected (JUnit + zero-retry already configured); optional provenance-comment update only.

## Test Commands
The gates /implement runs (no Rust unit test exists for workflow YAML; the gate is the coverage command itself + the dogfood run + supply-chain, exercised on the Windows host):
```bash
# the new binding coverage gate (must pass at current coverage; must fail if dropped below 60%)
cargo llvm-cov nextest --workspace --profile ci --fail-under-lines 60 --lcov --output-path lcov.info
# dogfood the source-of-truth release gate (emits target/nextest/ci/junit.xml) — must stay green
scripts/agent-run.ps1 run        # Windows host (or: bash scripts/agent-run.sh run)
# boot smoke / status
bash scripts/agent-run.sh status
# supply-chain gates stay green
cargo audit && cargo deny check
```

## Acceptance Criteria
- (quality) CI **fails** when workspace line coverage < 60% and **passes** at/above it (per test-plan §10, `--fail-under-lines 60`).
- (quality) the nextest `ci` profile emits JUnit XML at `target/nextest/ci/junit.xml` and remains `retries = 0`; a flake surfaces as a hard failure, never retry-masked (per test-plan §10; nextest.toml).
- (tests/obs) the coverage report(s) (`lcov.info` + `coverage.xml`) **and** `junit.xml` are uploaded as CI artifacts on every run, including on gate failure (`if: always()`) (per test-plan §9; obs-plan §9).
- (arch) the flakiness budget is asserted at the CI configuration level (zero-retry confirmed), not relying on the local `.config/nextest.toml` alone (per arch §Cross-cutting Patterns; test-plan §10).
- (security) `cargo audit` and `cargo deny check` remain discrete hard-fail CI steps — not downgraded, suppressed, or removed by this chunk (per security-plan §Dependency Security).
- (security) the uploaded JUnit + coverage artifacts contain no absolute host paths or internal struct/field names — relative crate identifiers only (per security-plan §Error Handling; obs-plan §6).
- (arch) no change to local `agent-run.{sh,ps1} run` green status; **zero engine/seam diff** (no `crates/**` change); `windows-latest` single-OS unchanged (per scope.md boundaries).
- (tests) `scripts/agent-run.ps1 run` returns exit 0.

## Provenance
- **Spec anchors:** test-plan §9 (CI Integration — JUnit + cobertura artifact + build-fail conditions), §10 (Quality Gates — Minimal-tier line ≥ 60%, `--fail-under-lines 60`, generated/stub/fixture exclusions, never-lower-the-bar); arch §Infrastructure Patterns + §CI/CD approach + §Cross-cutting Patterns (zero-retry determinism, no live Pulse in CI); security-plan §Dependency Security (audit/deny gates) + §Error Handling (artifact path/name sanitization); obs-plan §9 (artifact upload) + §6 (no absolute-host-path leak); §9 line 213 `quality-gate-config-emit`.
- **Research files inspected:** `.github/workflows/ci.yml` (full), `.config/nextest.toml` (full), `scripts/agent-run.{sh,ps1}` (grep), `.andromeda/test-plan.md` §9/§10.
- **Code-graph impact:** N/A — config-only chunk (YAML + TOML); no Rust symbol/caller/crate-edge surface (research.md §Graph impact).

## Implementation notes
- The coverage step **re-runs** the suite under instrumentation (a second nextest pass after the `agent-run` dogfood step); cargo-llvm-cov's run re-emits `junit.xml` too, so either copy is uploadable. Keep the coverage gate ordered so a coverage failure does not mask a supply-chain failure (audit/deny stay their own steps).
- cargo-llvm-cov multi-format output: emit via the `report` subcommand after a `--no-report` collection run, or a single pass — an /implement HOW detail; the binding requirement is that `--fail-under-lines 60` runs on the gating invocation and both report files are produced.
- `--ignore-filename-regex` must match the **actual** generated/stub/fixture paths so the 60% rolls up over real product code; confirm the concrete paths (OTLP generated modules, tauri/rmcp stub modules, `tests/`/`fixtures/`) at implement.
- `dorny/test-reporter` (if chosen at review) requires `permissions: checks: write` — a deliberate elevation deliberately omitted by the recommended default.
- Decisions surfaced for P5 review: **#1 JUnit consumption** (artifact-only [rec] vs +`dorny/test-reporter`); **#2 OS matrix** (windows-only [rec] vs 3-OS); **#3 flakiness assertion** = add the lightweight CI check (decided default, minor).
