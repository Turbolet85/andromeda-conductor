# Plan — 2026-08-08-dependency-advisory-remediation: "Dependency advisory remediation"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-08-dependency-advisory-remediation · **Version:** conductor-0.2.0 · **Generated:** 2026-08-08T19:45Z

## Goal

Restore the supply-chain gate to green. Four advisories have accumulated against a `Cargo.lock` that is
byte-identical to HEAD — they were published 2026-06-29 → 2026-07-06, during the 41-day pause, so **the gate
decayed with wall-clock time, not with any diff**. The mechanism is recorded in security-plan's own amendment
history: the RustSec advisory DB is fetched fresh on every run precisely because cargo-audit / cargo-deny are
external CLIs and not `Cargo.lock`-pinnable. Because security-plan §Security Anti-Patterns and test-plan §11
both forbid a release build or a merge while the gate is red, this blocks every downstream chunk that ends in
a release gate.

All four clear by **dependency bumps**, not by exceptions. Three of the four are lock-only; `plist` is the
lever that carries `quick-xml` across its semver-major boundary, so no Tauri bump is needed and
`tauri-utils` (already at its latest, 2.9.3) stays put. Per security-plan §Dependency Security, `deny.toml`
is reserved for the unmaintained-but-non-vulnerable / no-upstream-fix class — every advisory here is
actionable, so **no new ignore entry is added**, and the entries already present in `deny.toml` (17 by
count, per the file itself) stay byte-unchanged.

Two manifest edits ride along, both settled at P4 review. The `anyhow` floor moves to `1.0.104` so the file a
human reads stops naming a version with a known unsound advisory (matching the house style of naming resolved
versions). And the orphan `rmcp = "1.7.0"` is deleted: no member references it, it is absent from
`Cargo.lock`, and arch §Established Decisions [MCP Read-Back Client] records rmcp as removed on 2026-06-27 —
flagged independently by both the arch and security distillers. Its apparent later owner, capability v2-28,
is scoped to three *documents*, so a manifest line would otherwise fall outside every acceptance.

The expected Rust-source delta is **zero**. The real risk surface is compilation, not behavior: quick-xml
0.41 is consumed by `tauri-build`'s build script, so `conductor-tauri` must actually be compiled — and only
through an `ensure_frontend`-preceded path, because `generate_context!` resolves `build.frontendDist` at
compile time and a bare `cargo build` never runs `beforeBuildCommand` (arch §Infrastructure Patterns).

## Implementation Steps

1. **Bump the lock — three commands, four packages.** Run `cargo update -p plist` (moves plist 1.9.0 → 1.10.0
   **and** quick-xml 0.39.4 → 0.41.0), `cargo update -p crossbeam-epoch` (→ 0.9.20), `cargo update -p anyhow`
   (→ 1.0.104). Do **not** run a bare `cargo update` — it would move unrelated locked stack rows, which arch
   §Established Decisions forbids touching opportunistically. `cargo update -p quick-xml` alone is a no-op
   (plist 1.9.0 pins `^0.39`); plist is the lever.
2. **Raise the `anyhow` floor** in `[workspace.dependencies]` to `"1.0.104"` (P4 decision). Members inherit
   via `workspace = true`; no per-crate manifest touches.
3. **Delete the orphan `rmcp` line** from `[workspace.dependencies]`, with its now-dangling `# MCP read-back
   client` comment (P4 decision). Verify absence is unchanged afterward: `rmcp` must still not appear in
   `Cargo.lock` and `cargo tree -i rmcp --target all` must still find no package.
4. **Verify no locked stack row moved.** Diff `Cargo.lock` and confirm the only version changes are the four
   named packages — tokio, rusqlite, tonic, opentelemetry-proto, rand_chacha, garde and tauri must be
   untouched (arch §Stack, §Established Decisions). Confirm the effective MSRV floor still holds: plist 1.10.0
   declares `rust-version 1.88.0`, below the workspace's 1.94.1.
5. **Compile the risk surface through the harness.** Run `scripts/agent-run.sh run` — its `ensure_frontend`
   step (`npm ci` if needed + `npm run build`) precedes the workspace nextest, so `conductor-tauri` and its
   `tauri-build` build script are genuinely exercised against quick-xml 0.41. Never substitute a bare
   `cargo build`.
6. **Re-prove the gate, both tools.** `cargo audit` and `cargo deny check advisories bans sources licenses`
   must each exit 0. If any bump forces a source edit, stop and re-examine scope — the chunk's premise is a
   zero-source-delta change (see Implementation notes).
7. **Assert the lock is complete and un-drifted** (`cargo metadata --locked`), then commit `Cargo.lock`
   together with `Cargo.toml`.

## Codebase touchpoints

### New files
- (none)

### Files to modify
- `Cargo.lock` — four package version changes (plist 1.9.0→1.10.0, quick-xml 0.39.4→0.41.0,
  crossbeam-epoch 0.9.18→0.9.20, anyhow 1.0.102→1.0.104). Committed, un-drifted.
- `Cargo.toml` — line 56 `anyhow = "1.0.102"` → `"1.0.104"`; delete line 45 `rmcp = "1.7.0"` and its
  `# MCP read-back client` comment. No other `[workspace.dependencies]` entry moves.

Deliberately NOT modified: `deny.toml` (no new ignore — these are actionable), `.github/workflows/ci.yml`
(the two supply-chain steps already exist and are correct — see notes), `.config/nextest.toml`, any `.rs`
file, and the `crates/conductor-tauri/ui` npm tree.

## Test Commands

```bash
# Full gate — ensure_frontend (npm build) → nextest --profile ci → test --doc → clippy -D warnings
scripts/agent-run.sh run

# Supply-chain gate — BOTH tools; a single-tool green is not a green gate
cargo audit
cargo deny check advisories bans sources licenses

# Lock completeness / no drift
cargo metadata --locked --format-version 1 > /dev/null

# Harness smoke
bash scripts/agent-run.sh status
```

No `ui-smoke` entry: this is not a UI/visual chunk (zero `ui/src` delta). The display-gated a11y harness
(`npm run a11y`, Linux+xvfb + live Pulse) is **intentionally excluded** per a11y-plan §9 — it is an
operator/local gate, not an always-on one, and its exclusion here is a recorded decision, not a skipped check.

## Acceptance Criteria

- (security) `cargo audit` exits 0 with no un-ignored advisory against `Cargo.lock`, **and**
  `cargo deny check advisories bans sources licenses` exits 0 — verified independently, both tools
  (per security-plan §Dependency Security, CI integration).
- (security) All four advisories — RUSTSEC-2026-0194 / -0195 (quick-xml → 0.41.0), -0204 (crossbeam-epoch →
  0.9.20), -0190 (anyhow → 1.0.104) — are cleared **by version bump**, with **zero new `deny.toml` ignore
  entries** and the existing 17 `[advisories] ignore` entries in `deny.toml` byte-unchanged — per
  security-plan §Dependency Security's accepted-exceptions *mechanism* (each entry carries a justifying
  comment; the count is checked against `deny.toml`, which is the artifact that holds it).
- (security) RUSTSEC-2024-0429 (glib 0.18.5, `unsound`) needs no action and no `deny.toml` entry — its
  disposition is recorded: `cargo audit` classes it `informational: unsound` among its allowed warnings
  (exit 0) and `cargo deny` does not report it at all. The empirical proof is both tools exiting 0
  (per security-plan §Dependency Security, Accepted exceptions).
- (security/arch) `Cargo.lock` is committed and un-drifted — `cargo metadata --locked` succeeds and
  `git diff --exit-code Cargo.lock` is clean after the final gate run (per security-plan §Dependency
  Security Pinning; arch §Inherited Defaults lockfile policy).
- (arch) Zero Rust source change and zero new workspace member / crate / port / env var; the only tracked
  deltas are `Cargo.lock` + the two `Cargo.toml` lines. Any other edit re-opens scope
  (per architecture.md §Occupied Resources).
- (arch) No §Established-Decisions-locked dependency moves — tokio, rusqlite, tonic, opentelemetry-proto,
  rand_chacha, garde, tauri all hold their resolved versions; the workspace still builds on MSRV 1.94.1 /
  Rust 2024 with no toolchain-floor change (per architecture.md §Stack, §Established Decisions).
- (arch/a11y) `conductor-tauri` compiles at least once through an `ensure_frontend`-preceded path
  (`scripts/agent-run.sh run`), never a bare `cargo build`, so the quick-xml 0.41 `tauri-build` build-script
  surface is actually exercised and `ui/dist` still builds (per architecture.md §Infrastructure Patterns —
  Build system; a11y-plan §1 A11y surfaces).
- (tests) `cargo nextest run --workspace --profile ci` passes at the **428-test baseline** with zero retries,
  zero new skips and no `NO_TESTS_RUN` (exit 4) target; `cargo test --workspace --doc` and
  `cargo clippy --workspace --all-targets -- -D warnings` pass as separate stages
  (per test-plan §3 `run`, §9, §10).
- (tests) The dev/fixture tree is exercised, not assumed: the `assert_fs::TempDir` + `CONDUCTOR_RUNS_DIR`
  isolation paths in `conductor-cli`, `conductor-run`, `conductor-report` and `conductor-tauri` all pass
  post-bump — crossbeam-epoch reaches them via `assert_fs` → globwalk → ignore
  (per test-plan §3 Test data bootstrap, §5, §7).
- (tests) No insta snapshot is rewritten and no envelope / journal shape changes — goldens pass in
  fail-don't-write mode, confirming the change is lock-only at the artifact boundary
  (per test-plan §7 Golden artifacts, §3 Log format).
- (obs) The anyhow error edge still redacts: `redact/sanitize_error()` @
  `crates/conductor-core/src/redact.rs:98` keeps producing single-line, host-path-free `Display` output —
  already covered by `sanitize_error_is_single_line_and_host_path_free()` @ `redact.rs:201` plus 24 further
  redaction symbols across `conductor-core` and `conductor-verify`, so the standard nextest run is the proof
  (per obs-plan §11 Error Reporting · PII Scrubbing).
- (obs) `logs/agent-latest.jsonl` still carries the 7 base fields on every line, contains no absolute host
  path, and yields zero `^thread.*panicked` lines in the artifact or captured producer stderr
  (per obs-plan §3, §9, §10).
- (obs) No self-obs OTel SDK is initialized: `opentelemetry` / `opentelemetry_sdk` remain dormant transitive
  deps with no init or exporter call site, and the recorded `default-features = false` follow-up is **not**
  silently absorbed here — it belongs to capability v2-32's polish chunk (per obs-plan §3, §11).
- (a11y) Zero `ui/src` DOM / ARIA-role / token / lamp change is introduced, so no new WCAG AA verification is
  owed; if any webview edit becomes bump-forced, an operator/local `npm run a11y` pass over the four
  must-be-accessible paths must run green first (per a11y-plan §10 SLO Invariants).

## Provenance

- **Spec anchors:** architecture.md §Stack and Technologies · §Established Decisions [Error Handling] /
  [MCP Read-Back Client] / [Module Boundaries] · §Infrastructure Patterns (Build system) · §Occupied
  Resources · §Inherited Defaults — security-plan.md §Dependency Security (Audit tool · Pinning · Update
  policy · Accepted exceptions · CI integration · Critical CVE SLA) · §Security Anti-Patterns § Universal —
  test-plan.md §3 (Test Harness Contract · Test data bootstrap · Log format) · §4 (Tool-version policy) ·
  §7 (Golden artifacts) · §9 · §10 · §11 — obs-plan.md §3 · §9 · §10 · §11 (Error Reporting · PII
  Scrubbing · Telemetry Strategy) — a11y-plan.md §1 · §9 · §10.
- **Amendment history consulted:** 2026-06-15-dependency-audit-gate (advisory DB fetched fresh ⇒ wall-clock
  decay) · 2026-06-23-line-oriented-output-rendering (the only sanctioned `deny.toml` use; the
  gate-red-on-HEAD precedent) · 2026-06-24-frameless-window-shell (`ensure_frontend` compile-order coupling;
  the standing Tauri-tree exception set) · 2026-06-27-mcp-read-back-result-shape-adapter (rmcp removed) ·
  2026-08-08-sut-capability-manifest (the 428 baseline) · 2026-06-17-raw-otlp-message-scaffold (dormant
  transitive OTel SDK is not a violation).
- **Research files inspected:** `Cargo.toml` · `Cargo.lock` · `deny.toml` · `.github/workflows/ci.yml` ·
  `.config/nextest.toml` · `scripts/agent-run.sh` · `crates/conductor-core/src/redact.rs` ·
  `crates/conductor-tauri/ui/dist/`. Code-graph trace:
  `.andromeda/runs/2026-08-08T19-29-55-phase/tree-query-2026-08-08-dependency-advisory-remediation.json`
  (25 rows, `db_state: fresh`). Advisory evidence:
  `.andromeda/runs/2026-08-08T19-29-55-phase/advisory-evidence.md`.
- **Verification matrix:** this chunk claims **no capability**. It partially advances **v2-27** (Release build
  and bundle), whose acceptance requires cargo-audit + cargo-deny green *and* a release binary + Tauri bundle
  + a recorded SLO pass — none of which this chunk produces, so v2-27 stays `chunk: null` per the multi-chunk
  rule. **v2-28** (stale rmcp wording) is scoped to `test-plan.md` / `tests-summary.md` /
  `verification-harness.md` and is unaffected by the manifest-line deletion; it stays `chunk: null` too.
  **v2-32** (indicatif 0.18 + opentelemetry-proto trim) is explicitly out of scope.

## Implementation notes

- **Route-entry premises were corrected at take-up.** The working entry said "crossbeam-epoch ≥0.9.20 and
  quick-xml ≥0.41 **through the Tauri tree**", and the handoff listed four advisories across two packages.
  Both are wrong in three ways, verified live: RUSTSEC-2026-0190 is `anyhow` (not crossbeam-epoch) and is an
  `unsound` advisory that `cargo audit` reports as an exit-0 warning while `cargo deny` denies it;
  crossbeam-epoch is a **dev-dependency** (`assert_fs` → globwalk → ignore → crossbeam-deque), never in the
  release binary; and quick-xml needs no Tauri bump because plist absorbs the major. Full evidence in
  `advisory-evidence.md`. The chunk's *intent* — gate green — is unchanged.
- **Do not "fix" CI's bare `cargo deny check`.** The obs distiller flagged that `.github/workflows/ci.yml`
  invokes it without subcommands while this plan names them explicitly. Verified equivalent: both forms
  report 4 errors, and the bare form's own output shows `advisories FAILED · bans ok · licenses ok ·
  sources ok`. cargo-deny runs every check class when none is named. No workflow edit.
- **Order sensitivity.** Step 1's three `cargo update -p` invocations are independent, but a bare
  `cargo update` is not equivalent and must not be substituted. Run the frontend build (step 5) before any
  workspace cargo command that compiles `conductor-tauri`, or `generate_context!` fails at compile time on a
  missing `ui/dist` — `ui/dist` is present today, so this only bites if the tree is cleaned first.
- **quick-xml 0.41 is a semver-major move** consumed inside `plist`, which `tauri-build` uses for Info.plist
  handling. Conductor never calls quick-xml directly, so breakage — if any — surfaces as a `tauri-build`
  build-script compile error on `conductor-tauri`, not as a behavior change. That is the one place to watch.
- **If a bump forces a source edit, stop.** The scope's premise is zero Rust delta; a forced edit means the
  bump reached behavior and the chunk must be re-scoped rather than quietly widened (security-plan §Threat
  Model — boundaries are re-examined, never widened by fallout).
- **A post-bump flake is a real signal.** `.config/nextest.toml` sets `retries = 0` in both the `ci` and
  `default` profiles and CI separately fails the build on any non-zero retries setting; quarantine and
  root-cause rather than re-running (test-plan §10 Zero-flakiness budget).
- **Wrap-time amendment is owed, and its surface is architecture.md only** — three sites, all naming
  `anyhow 1.0.102`: `architecture.md:31` (§Stack and Technologies, Error handling row), `:52`
  (§Established Decisions [Error Handling]) and `:234` (§Inherited Defaults). **security-plan carries no
  anyhow version** and needs no amendment: its ten `anyhow` mentions (`:26, :55, :115, :198, :251, :264,
  :318, :334, :366`) all describe the anyhow *edge* as an error-sanitization boundary — a property this
  bump does not change — and the version-bearing content in §Dependency Security is the cargo-audit ≥0.22 /
  cargo-deny ≥0.19 tool floors and the `tauri` ≥2.10.3 line, not anyhow. Do **not** propose a lockstep
  security-plan amendment here: that precedent applies when both documents carry the same fact, and only
  one does. The `rmcp` deletion needs no new decision — it aligns the manifest with an already-recorded one.
- **Pre-existing gap, NOT this chunk's drift: `deny.toml` ↔ security-plan §Accepted exceptions.** `deny.toml`
  holds 17 `[advisories] ignore` entries + 8 `[licenses] allow` entries, while security-plan §Dependency
  Security's Accepted-exceptions paragraph names exactly **one** of each (`number_prefix` / RUSTSEC-2025-0119
  and the `Zlib` allow via `foldhash`). The other 16 ignores (5× `unic-*`, 10× gtk-rs, `proc-macro-error`)
  and 7 allows accreted in `deny.toml` without the spec following. This chunk **adds none and changes none**
  of them, so per `.andromeda/playbook.md:46` (a cross-document gap the report shows the chunk did not
  introduce; gap pre-existing → routine dismiss, belongs to a dedicated doc-reconcile pass) it is **not**
  this chunk's amendment and **not** an escalation — record it in the handoff as a named follow-up so wrap
  does not re-derive it.
