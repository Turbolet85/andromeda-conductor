# Plan — 2026-08-09-in-lane-sut-scenarios: "In-lane SUT scenarios (P-067, P-072, P-079)"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-09-in-lane-sut-scenarios · **Version:** conductor-0.2.0 · **Generated:** 2026-08-09T19:52:00Z

## Goal

Author the **first scenario-catalog entries above P-060** — three declarative TOMLs keyed to the three Pulse
capabilities intent §4 F2 places squarely in Conductor's lane — and shrink `conductor_core::UNBACKED_AUTO` by
the one `Auto` capability among them. Until now no scenario names any capability from Pulse's v0.3.0 ledger,
so the in-lane subset (live service truth · Investigate result · single-sourced workspace key) has no
Conductor coverage at all. The 2026-08-08 capability manifest is what makes these ids expressible: garde
asserts the `P-NNN` shape, `Scenario::check_capabilities` asserts membership against
`contracts/pulse-capabilities.toml` (`sut_version = "v0.3.0"`), so all three validate as manifest members
rather than failing a stale `001..=060` range (per architecture.md §Established Decisions [Accepted Capability
Set] + security-plan.md §Input Validation).

Coverage mode drives the shape of each file. **P-067 and P-072 are `DriveObserve`** — Conductor drives the
stimulus, the operator confirms the visual claim — so they carry **no `[[expected]]` block**, which
`#[serde(default)]` makes valid and which routes verdict `None` → `Lamp::Manual` (ManualCheck), never Fail and
never red (per architecture.md §Established Decisions [Probabilistic-Assertion Policy] + a11y-plan.md §6
State-naming crosswalk + layout-templates.md §cli Component: Primary content block 2). **P-079 is `Auto`** —
it claims Conductor drives the stimulus *and* asserts the reaction — so it carries one real check. That single
check is what makes the pin shrink legal: `check_scenario_backing` holds `UNBACKED_AUTO` to exact-set equality
in both directions, so naming P-079 in a scenario's `p_ids` without removing it from the pin fails with *"in
the unbacked ledger but now named by a scenario — shrink the ledger"*, and removing it from the pin without
naming it fails the other way (per test-plan.md §6 scenario-backing leg).

This chunk is the **catalog**, not the live proof. `v2-04`'s full acceptance ("each drive a live Pulse and
yield a non-blocked verdict via MCP read-back") cannot pass here — the faithful per-phase dispatcher (`v2-08`),
real per-check read-back extraction (`v2-09`) and a live `ready: true` preflight (`v2-10`) are all Epoch-2
entries, and `execute_scenario` still grades every check against the literal `"incidents-listed"`
(`conductor-run/src/lib.rs:216`). The chunk therefore **partially advances `v2-04`** and does not claim it.

## Implementation Steps

1. **Author `scenarios/live-only-service-truth.toml` (P-067, DriveObserve).** Follow the header discipline of
   `scenarios/service-constellation-discovery.toml:1-11`: the P-ID + Pulse's claim ("the constellation shows
   only currently-live services; persisted/stale registry entries hidden or marked historical, with honest
   recency labels"), an explicit *Coverage mode: drive+observe* line, and a *Declare-only* list. Identity:
   `name = "live-only-service-truth"`, `p_ids = ["P-067"]`, `seed = 4317067` (the `4317<PPP>` convention;
   verified free), `slo_tier`, `jitter_ms = 50`. Phases drive a multi-service topology in which one service
   goes silent while others keep emitting, so a stale registry entry is distinguishable from a live one.
   Close with the comment recording **why** there is no `[[expected]]` block.
2. **Author `scenarios/investigate-actions-functional.toml` (P-072, DriveObserve).** Same discipline.
   `p_ids = ["P-072"]`, `seed = 4317072`. Phases drive a fault severe enough that an incident becomes
   investigable; the operator triggers Investigate in Pulse's UI and confirms a real L4-backed result.
   **No `[[expected]]` block** — operator-verified (decided at P4). The declare-only list must name both
   deferrals explicitly: the L4-backed result's **quality** is the recorded `v2-05` interpretation deferral
   (owner **conductor-0.3.0**), and `retrieve_report` read-back is `v2-09`.
3. **Author `scenarios/constellation-severity-live-wiring.toml` (P-079, Auto).** Header states Pulse's claim
   ("reconcile the incident workspace key — resolver `data_dir` vs producer detected-root — so per-service
   severity and the incidents panel light up under a live storm"). `p_ids = ["P-079"]`, `seed = 4317079`.
   Phases drive a sustained identical-fingerprint storm. One `[[expected]]` block:
   `kind = "CountAtLeast"` · `class = "Hard"` · `expected = "1"` — the storm's incident is visible through
   read-back **at all**, which is precisely what a diverged workspace key defeats (intent §4 F10:
   `query_incident_list` returns zero rows). Deliberately **not** a candidate token: `RetryStorm` belongs to
   `fingerprint-storm.toml`, and sharing it would make this scenario go red for storm-detection reasons
   unrelated to the key (the 2026-06-22 outcome-coherence discipline). Record the token choice and the
   `slo_tier` as calibration points, per the `fingerprint-storm.toml:16-18` precedent.
4. **Shrink the pin.** Remove `"P-079"` from `UNBACKED_AUTO` in `crates/conductor-core/src/drift.rs:60`
   (11 → 10 entries). Leave `P-073` and `P-074` in place (owned by the Epoch-2 "Pulse run contract" and
   Epoch-3 "fingerprint-storm live proof" entries) and leave the four interpretation-cluster ids
   `P-031`/`P-033`/`P-034`/`P-044` untouched — they are the recorded `v2-05` deferral owned by
   conductor-0.3.0.
5. **Correct the pin's doc comment.** `drift.rs:58-59` currently closes "…`P-073`/`P-074`/`P-079` are already
   owned by the in-lane SUT-scenarios entry." After this chunk only P-073/P-074 remain owed, and their owners
   are different route entries. Re-word to name the two remaining ids and their actual owners.
6. **Add the loader + mode-shape tests** in `crates/conductor-core/src/scenario.rs` `mod tests`, in the shipped
   idiom (`scenario.rs:780-806`): `#[rstest]` `#[case]` rows over the three new stems asserting `s.name` and
   `s.p_ids`; an `expected.is_empty()` assertion for the two `DriveObserve` stems; a `CountAtLeast`/`Hard`
   membership assertion for the P-079 stem; and one suite guard asserting the new family exercises **both**
   shapes (`any(empty) && any(non-empty)`), mirroring
   `constellation_context_grounding_suite_has_operator_checklist_members` at `scenario.rs:825`.
7. **Run the PREREQ supply-chain check** (see Implementation notes) — `cargo audit` re-run under the bounded
   wait, with `cargo deny check` observed green.

**No roll-up renderer change.** The code-graph shows all 13 `UNBACKED_AUTO` consumers derive the count
(`.len()` or the slice); `summary_line(rows, unbacked)` takes it as a parameter and the three surface tests
assert against `UNBACKED_AUTO.len()`, never a literal. Grep confirmed **zero** hardcoded `11` in `crates/`.
The 11 → 10 shrink therefore propagates to the Markdown report, the CLI table and the webview with no
arithmetic edit — this corrects `scope.md` deliverable 3, which anticipated three surface edits.

## Codebase touchpoints

### New files
- `scenarios/live-only-service-truth.toml` — P-067, DriveObserve, empty `expected`, seed 4317067.
- `scenarios/investigate-actions-functional.toml` — P-072, DriveObserve, empty `expected`, seed 4317072.
- `scenarios/constellation-severity-live-wiring.toml` — P-079, Auto, one `CountAtLeast "1"` / `Hard` check,
  seed 4317079.

### Files to modify
- `crates/conductor-core/src/drift.rs` — remove `"P-079"` from `UNBACKED_AUTO` (line 60) and re-word the
  doc-comment's closing ownership sentence (lines 58-59). **Graph impact:** `UNBACKED_AUTO` @
  `conductor-core/src/lib.rs:36` (re-export) · `drift.rs:310` (the live gate) ·
  `conductor-report/src/coverage.rs:13,28,164,178,189,191` · `conductor-cli/src/render.rs:17,208,209` ·
  `conductor-tauri/src/commands.rs:140,357` — 13 references, every one count-derived, so the data edit has
  zero code blast radius. `check_scenario_backing` @ `drift.rs:310,317,328,340,351,364,370` — all 8 call
  sites are its own tests; there is no production caller, which is why the gate runs in CI without a Pulse.
- `crates/conductor-core/src/scenario.rs` — add the per-stem loader cases, the mode-shape assertions and the
  new family's suite guard to `mod tests`; preserve the `format!("{}/../../scenarios/{stem}.toml", env!(…))`
  path idiom.

## Test Commands
```bash
cargo nextest run -p conductor-core
cargo nextest run --workspace --profile ci
cargo test --doc
cargo clippy --workspace --all-targets -- -D warnings
cargo deny check advisories bans licenses sources
cargo audit
bash scripts/agent-run.sh status
```

## Acceptance Criteria

- (arch) The three new kebab-case TOMLs live in `scenarios/`, each naming exactly its P-ID, and all three load
  through the manifest-validating path with membership accepted — no new crate, seam or dependency delta
  (per architecture.md §Infrastructure Patterns directory structure · §Established Decisions [Scenario Config
  Format] / [Accepted Capability Set]).
- (arch) Each scenario declares an explicit `seed` and an `slo_tier` from the closed set `<5s`/`<20s`/`<90s`;
  garde bounds hold (per architecture.md §Conventions Data model + Config conventions · obs-plan.md §5/§6).
- (tests) `cargo nextest run -p conductor-core` passes with the new `#[rstest]` cases: all three stems load,
  each names its P-ID, and each P-ID is accepted by `contracts/pulse-capabilities.toml` (per test-plan.md
  §7 / §4).
- (tests) Mode-shape assertions hold — the two `DriveObserve` stems assert `expected.is_empty()`, the `Auto`
  stem asserts its `CountAtLeast`/`Hard` check, and the suite guard asserts the family exercises both shapes
  (per test-plan.md §6 · §11 E2E).
- (tests) `the_committed_catalog_matches_the_unbacked_ledger` is green with `UNBACKED_AUTO` at **10** entries —
  exact-set equality in both directions, with `P-073`/`P-074` and the four interpretation-cluster ids
  untouched (per test-plan.md §6 scenario-backing leg).
- (arch) `check_sut_drift` stays green: a rotted or newly-unbacked pin surfaces as `CoreError` (`Err`), never
  as a report state (per architecture.md §Established Decisions [Accepted Capability Set] · §Cross-cutting
  Patterns Verdict/error wall).
- (obs/layouts) After the pin shrinks, all three coverage roll-up surfaces render the unbacked count derived
  from `UNBACKED_AUTO.len()` with **no literal**, and the four per-mode summands still sum exactly to the row
  total — the qualifier is not a fifth mode (per obs-plan.md §4 denominator semantics · layout-templates.md
  §cli Primary screens).
- (security) All three TOMLs load through the garde-validating path and a malformed variant is rejected as
  `ConfigError` — a harness fault, non-panicking, never a verdict (per security-plan.md §Input Validation
  scenario-config row · §Security Anti-Patterns → Input).
- (security) No new scenario TOML, comment or roll-up output contains an absolute host path, a stack trace or
  an internal seam-crate struct/field name (per security-plan.md §Error Handling · obs-plan.md §11 Logs).
- (security) `cargo deny check advisories bans licenses sources` is run and **observed** green (output
  inspected, not assumed), and `cargo audit` is re-run with its result recorded — with no floor raise, no
  `deny.toml` ignore and no CI edit (per security-plan.md §Dependency Security two-fault split).
- (design) The two `DriveObserve` P-IDs surface on the `ManualCheck` treatment — never red, never a bare gray
  placeholder — and the coverage rendering stays the 4-column table with no verdict column (per
  design-system.md §Color Palette Verdict-vs-ReportState · §cli Component Patterns #3).
- (obs) No new span name and no instrumentation on the config-load / catalog path (per obs-plan.md §11
  Spans/Traces).
- (tests) `cargo nextest run --workspace --profile ci` returns 0 with zero retries; `cargo clippy --workspace
  --all-targets -- -D warnings` is clean; `cargo test --doc` passes.

## Provenance
- Spec anchors: architecture.md §Established Decisions [Accepted Capability Set] / [Scenario Config Format] /
  [Probabilistic-Assertion Policy] · §Cross-cutting Patterns Verdict/error wall · §Conventions Data model +
  Config conventions · security-plan.md §Input Validation · §Error Handling · §Dependency Security ·
  §Security Anti-Patterns · test-plan.md §4 · §6 scenario-backing leg · §7 fixtures · §10 · §11 ·
  obs-plan.md §4 denominator semantics · §5/§6 · §11 · layout-templates.md §cli Primary screens ·
  §cli Component: Primary content block 1/2 · §desktop-webview Component: Operator-checklist ·
  design-system.md §Color Palette · §Iconography · §cli Component Patterns #3/#4 · a11y-plan.md §1 Critical
  paths → Operator-checklist · §4 · §6 State-naming crosswalk.
- Requirement provenance: **`v2-04`** (`verification-matrix.json#v2-04`) — **partially advanced, not claimed**:
  its acceptance requires a live-Pulse non-blocked verdict via MCP read-back, which depends on `v2-08`/`v2-09`/
  `v2-10` in Epoch 2. The entry stays `chunk: null` per `verification-matrix-contract.md` §chunk ownership.
- Research files inspected: `crates/conductor-core/src/drift.rs`, `crates/conductor-core/src/scenario.rs`,
  `crates/conductor-core/src/expected.rs`, `crates/conductor-report/src/coverage.rs`,
  `crates/conductor-cli/src/render.rs`, `crates/conductor-tauri/src/commands.rs`,
  `scenarios/service-constellation-discovery.toml`, `scenarios/fingerprint-storm.toml`,
  `crates/conductor-timeline/tests/replay.rs`.

## Implementation notes

- **PREREQ — `cargo audit` is a bounded wait, nothing more.** It has been red at three consecutive checks and
  is **proven an advisory-DATABASE fault, not a tool fault**: cargo-audit 0.22.2 (the latest published)
  reproduced `duplicate advisory ID: RUSTSEC-2026-0244` byte-identically, so the duplicate id is committed
  data in RustSec's advisory-db and there is nothing to raise a floor to. Re-run it; **verify `cargo deny
  check` actually ran green** as the overlapping signal rather than assuming it. Do **not** raise the floor,
  do **not** add a `deny.toml` ignore, do **not** edit CI. Close the deferral the moment it parses. CI's own
  `cargo audit` step hits the same wall until upstream heals.
- **Order sensitivity.** Steps 1-3 (author the TOMLs) and step 4 (shrink the pin) must land **together**:
  `check_scenario_backing` is exact-set in both directions, so either half alone turns the gate red — the
  TOMLs alone fail with "shrink the ledger", the pin edit alone fails with "author a scenario".
- **The replay goldens are not at risk.** `crates/conductor-timeline/tests/replay.rs` enumerates only
  `scenarios/error-baseline-spike.toml`, and its two goldens are seed-named
  (`replay__fixture_seed_424242.snap` / `replay__fixture_seed_7.snap`). New scenarios with new seeds add no
  golden and move none — but keep the seeds free (`4317067`/`4317072`/`4317079` verified unused) so the
  seed-named-golden trap stays shut.
- **`CountAtLeast` softening is the evaluator's job.** `conductor-verify::slo` softens an *unmet*
  `CountAtLeast` to the calibration region at evaluation time, so declaring `class = "Hard"` is correct for
  the met floor — this is the established treatment (P-009 `"10"`, P-011 `"50"`).
- **P-067 and P-072 do not participate in the backing gate.** Only `Auto` rows do; `DriveObserve` is
  operator-confirmed by definition (`drift.rs:147-148`). They are authored because `v2-04` requires them, not
  because a gate compels them.
- **No UI automation.** The P-067 and P-072 visual claims are operator-checklist items with no invented
  driver — the standing non-goal, and test-plan.md §1's untestable zone.
- **Expected amendments (wrap):** none anticipated. The `(N unbacked)` shape, the four-mode roll-up and the
  scenario-backing gate were all documented by the immediately-prior chunk; this chunk changes the number,
  not the shape. If the pin's doc-comment re-wording (step 5) surfaces a spec that also names the three ids
  as one entry's ownership, record it for wrap's detectors rather than editing the spec here.
