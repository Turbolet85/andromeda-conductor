# Plan — 2026-08-18-error-baseline-spike-live-proof: "error-baseline-spike live proof"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-18-error-baseline-spike-live-proof · **Version:** conductor-0.2.0 · **Generated:** 2026-08-18

## Goal

Prove the statistical-anomaly family (P-009..P-012) against a live deterministic-L4 Pulse, on the surfaces
that can actually carry the proof. Research settled the surface question at source (research.md §Key
findings 1-3): neither authored token can ever reach read-back (`Contains` would hard-Fail forever;
`CountAtLeast` grades a structurally-empty `span_ids`), both cues top out at Suggested tier so no incident
forms from this family on a cpu-primary host, and the real live surface is Pulse's own `triage.cue.emit`
line (kind · priority · magnitude · absolute_value · confidence · persistence_seconds ·
suppression_bypassed). So this chunk applies the v2-11 disposition with per-surface evidence: retire both
scenarios' read-back token checks to declare-only, move the family's assertions to a test-only harvest of
the cue line (per test-plan §6 harvest substitution + architecture §Read-Back Dependency Posture), drive
both scenarios on operator-gated live legs (fresh data dir per leg), and record journal + runs.db +
leg-verdict evidence.

Two inherited obligations discharge on the same legs. The token-leading fingerprint transcription's LIVE
equality (unproven — the 2026-08-17 chunk proved it at unit tier only) is tested via the preflight canary:
Conductor computes the 32-hex value at emit (`conductor-run/src/lib.rs:227`) but logs it nowhere, so one
message-borne witness line plus a prefix-8 comparison against Pulse's harvested `fingerprint_hex` completes
the test (per architecture §RBDP — a disagreement is a TRANSCRIPTION defect, never a scenario failure). And
the standing cargo-audit bounded deferral is re-checked at the gates with the deny overlap VERIFIED (per
security-plan §Dependency Security).

## Implementation Steps

1. **PREREQ first: supply-chain re-check.** Run `cargo audit` (expect the byte-identical
   `duplicate advisory ID: RUSTSEC-2026-0244` DB fault, true exit 1 — record the result either way; close
   the deferral the moment it parses) and VERIFY `cargo deny check advisories bans licenses sources` true
   exit 0 across all four classes. `Cargo.lock` must stay un-drifted (this chunk has zero dependency
   delta). Do NOT raise a floor, add a `deny.toml` ignore, or edit CI.
2. **Retire the read-back token checks in both TOMLs** (`scenarios/error-baseline-spike.toml`,
   `scenarios/latency-regression.toml`): remove the `[[expected]]` blocks (`CountAtLeast "10"`/`"50"`,
   `Contains "ErrorRateSpike"`/`"LatencyRegression"`), replacing each with a comment that names the
   per-surface evidence — the token can reach no read-back surface (degraded report renders fixture
   constants + no cue kind while any incident is open; `evidence_count` reads `span_ids`, which
   `inference_runtime` never populates), and the live assertion now lives at the harvest tier
   (`conductor-run/tests/baseline_harvest.rs`). Re-declare `slo_tier = "<90s"` in both (the honesty-
   maximizing bucket; whole-run latency still exceeds it by construction — say so in the comment, naming
   v2-19 as the per-check-latency owner). Update each header's assertion prose. **gap_ms / seed /
   jitter_ms / `[phases.emission]` are NOT touched** — the seed-named replay golden `fixture_seed_424242`
   consumes exactly those fields.
3. **Add the canary-fingerprint witness**: one `tracing::info!` in `emit_canary`
   (`crates/conductor-run/src/lib.rs:224-236`) carrying the computed 32-hex fingerprint in the MESSAGE
   (the `log_observed_keys` precedent — no new allowlist field; hex survives `redact_value`). Keep it
   allowlist-clean and host-path-free.
4. **Author the harvest tier** — new `crates/conductor-run/tests/baseline_harvest.rs`, following
   `storm_harvest.rs`'s pattern exactly (window-slice by pre-leg line count; target-filtered JSONL parser;
   malformed lines skipped, never fatal; predicates return `Result<_, String>`): parse `triage.cue.emit`
   lines into {kind, priority, magnitude, absolute_value, confidence, persistence_seconds,
   suppression_bypassed}; predicates at minimum — `cue_fired(kind)` (existence proves the sample floor:
   the evaluator skips below-floor snapshots), magnitude ≥ 3.0 with confidence ≥ 0.7 (the convergence +
   floor witness), `priority == "suggested"` observed (the tier the shipped magnitudes reach), and for the
   error-spike cue `suppression_bypassed == true` (the P-057 absolute-arm witness at 0.35 > 0.05). Author
   the parser from the source-derived shape first (`emitter.rs:194-205`); PIN the fixture to the leg's
   verbatim captured line in step 8 (the storm_harvest precedent). Add the prefix-8 equality comparator
   (Conductor's logged 32-hex vs a harvested 8-hex `fingerprint_hex`) here or in `storm_harvest.rs` —
   implement's call.
5. **Take the ride-along golden** (folded CARRY, leaned take): one new seeded `dispatch_wire` stream
   golden driven by `scenarios/error-baseline-spike.toml` (seed 424242) covering the Error shape including
   its plain-slot branch (`dispatch.rs:95-98` — the exact branch the CARRY cites), in the committed golden
   family shape (per test-plan §7: one file per test-file family × seed, `*_time_unix_nano` excluded,
   CI fail-don't-write).
6. **Deterministic gates green** before any leg: `cargo nextest run --workspace --profile ci` (zero
   retries), `cargo test --workspace --doc`, `cargo clippy --workspace --all-targets -- -D warnings`.
   Confirm the replay golden `fixture_seed_424242` EXECUTED by name in the runner output and stayed
   byte-identical (the producer-ran rule).
7. **Live leg A — error-baseline-spike** (operator-gated, never CI): fresh `ANDROMEDA_PULSE_DATA_DIR`;
   launch `pulse-app` under the SIX-item recipe (sidecar built + on PATH ·
   `ANDROMEDA_PULSE_L4_DETERMINISTIC=true` + data dir in the launching shell ·
   `ANDROMEDA_PULSE_MCP_ENABLED=true` in Conductor's env · poll ≥ the contract floor · `pulse-app` cwd
   outside this repo, Conductor at its repo root · the paired `RUST_LOG` form only if the wire witness is
   wanted, boot-leg only); record the pre-leg line count of `{data_dir}/logs/agent-latest.jsonl.<date>`;
   run `SCENARIO=error-baseline-spike bash scripts/agent-run.sh run`; post-leg, harvest the Pulse slice +
   Conductor's `logs/agent-latest.jsonl`. Diff the `verify.readback.observe` key-set witness lines for
   `retrieve_report` / `retrieve_telemetry_slice` against the pinned baseline in
   `conductor-verify/tests/readback.rs` BEFORE trusting any measured verdict (first non-empty-corpus reach
   of those readers).
8. **Live leg B — latency-regression**: restart `pulse-app` on a FRESH data dir (open-incident dedupe is
   not by fingerprint; storm retention carries over), same recipe and harvest procedure.
9. **Pin + grade**: pin the captured `triage.cue.emit` line(s) verbatim into `baseline_harvest.rs`
   fixtures; run the predicates green; run the prefix-8 equality comparison against the canary storm line
   from either leg. A disagreement there is raised as a TRANSCRIPTION defect finding, never a scenario
   failure.
10. **Evidence + read-back**: `bash scripts/agent-run.sh status` under MINT-THEN-READ (verify the printed
    `run_id` is the leg's); read each leg's `runs.db` row (bound-parameter SELECT) + `runs/<run_id>.jsonl`
    envelope — expect state `KnownResidual` (degraded read-back; verdict None on the declare-only path),
    never Blocked. Compose `evidence/leg-verdict.md` citing harvested lines with any workspace/path-bearing
    fields stripped (committed evidence is host-path-free). If ANY harvested reality contradicts the
    research chain (a cue that never fires, an unexpected tier, a shape mismatch at the readers) — STOP,
    record the measurement, and surface it; measurement wins over the plan.

## Codebase touchpoints

### New files
- `crates/conductor-run/tests/baseline_harvest.rs` — the family's harvest tier (parser + predicates + the
  equality comparator, placement per step 4).
- `crates/conductor-run/tests/snapshots/dispatch_wire__error_baseline_spike__*.snap` (exact name per the
  committed family convention) — the ride-along Error-shape stream golden.
- `conductor-0.2.0/chunks/2026-08-18-error-baseline-spike-live-proof/evidence/leg-verdict.md` (+ harvested
  slices) — the two-leg record.

### Files to modify
- `scenarios/error-baseline-spike.toml` — checks → declare-only, `slo_tier` → `<90s`, header prose; timing
  untouched.
- `scenarios/latency-regression.toml` — same.
- `crates/conductor-run/src/lib.rs` — the one witness line in `emit_canary` (`:224-236`).
- `crates/conductor-run/tests/dispatch_wire.rs` — add the golden-driving test for step 5.
- (`crates/conductor-run/tests/storm_harvest.rs` — only if the comparator lands there.)
- Verification-only (no expected edit): `crates/conductor-timeline/tests/replay.rs` golden byte-identical +
  confirmed-ran; `crates/conductor-core/src/scenario_catalog.rs` (asserts this family at NAME level only —
  `:88`, `:96`); `crates/conductor-core/src/scenario.rs` family-name occurrence (check at implement).

Graph impact: the grading path is consumed unchanged — `evaluate_check` callers are
`conductor-run::execute_scenario @ crates/conductor-run/src/lib.rs:395` + conductor-verify internals/tests
(18 rows, P3 trace); this chunk adds no caller. `baseline_harvest` name is free (collision query, 3 rows
all in `storm_harvest.rs`).

## Test Commands

```bash
cargo nextest run -p conductor-run                                   # harvest predicates + canary wire + goldens
cargo nextest run --workspace --profile ci                           # full zero-retry gate
cargo test --workspace --doc
cargo clippy --workspace --all-targets -- -D warnings
cargo audit                                                          # PREREQ: record result (expected red: RUSTSEC-2026-0244 DB fault)
cargo deny check advisories bans licenses sources                    # PREREQ: must be true exit 0, all four classes
# live legs (operator-gated, NEVER CI) — fresh data dir per leg, pulse-app restarted between:
SCENARIO=error-baseline-spike bash scripts/agent-run.sh run
SCENARIO=latency-regression bash scripts/agent-run.sh run
bash scripts/agent-run.sh status                                     # MINT-THEN-READ: verify the printed run_id is the leg's
```

## Acceptance Criteria

- (arch) Every change lands in-seam — `scenarios/` + `conductor-run` (one self-obs line + tests) — with no
  new workspace member, no new `contracts/` manifest, no new `CONDUCTOR_*` env var, no listener; the
  re-declared `slo_tier` stays inside the closed `<5s`/`<20s`/`<90s` enum (per architecture.md §Occupied
  Resources; §Conventions — Data model conventions).
- (arch/tests) No vacuously-passable check remains in either family TOML: both token checks retired to
  declare-only with comments naming the per-surface evidence, assertions moved to the harvest tier (per
  architecture.md §Read-Back Dependency Posture; test-plan §6 Scenario: Fingerprint-storm, amended
  2026-08-16).
- (tests) `cargo nextest run --workspace --profile ci` green with `retries = 0`; no live-Pulse CI gate
  added; the seed-named replay golden `fixture_seed_424242` CONFIRMED EXECUTED by name and byte-identical
  (per test-plan §9/§10; the producer-ran rule).
- (tests) `baseline_harvest` predicates are pinned to the legs' VERBATIM captured `triage.cue.emit` lines
  (never invented shapes); a malformed harvest line is skipped, never fatal (per test-plan §6).
- (tests/live) Per scenario, an operator-gated leg yields a `runs.db` row + `runs/<run_id>.jsonl` envelope
  (11 fields) with state non-Blocked — expected `KnownResidual` under the degraded read-back — read via
  bound-parameter SELECT / `jq`, produced by the listed `SCENARIO=…` commands (per test-plan §3/§6;
  obs-plan §6). Cites `verification-matrix.json#v2-12`.
- (live/harvest) Each scenario's harvested cue line names its kind (`error_rate_spike` /
  `latency_regression`) at `priority = "suggested"`, magnitude ≥ 3.0, confidence ≥ 0.7; the error-spike cue
  carries `suppression_bypassed = true` (P-057's absolute arm); the cue fires in the spike/ramp window, not
  the baseline window (per the harvest-tier substitution, test-plan §6; SUT source cited in research.md).
  Cites `verification-matrix.json#v2-12`.
- (live/equality) The prefix-8 comparison between Conductor's logged canary fingerprint (the new witness)
  and Pulse's harvested `fingerprint_hex` EXECUTED on ≥1 leg; a mismatch is raised as a transcription
  defect, never a scenario failure (per architecture.md §Established Decisions [Read-Back Dependency
  Posture]).
- (obs) The witness line and all harvested-evidence handling stay inside the two-record-shape discipline;
  the key-set witness lines for `retrieve_report`/`retrieve_telemetry_slice` are recorded and diffed
  against the pinned baseline before any verdict is trusted (per obs-plan §3/§6).
- (obs/security) No committed evidence artifact (`leg-verdict.md`, harvested slices, journal/`runs.db`
  rows) carries an absolute host path or an internal seam-crate struct name; the legs produce zero
  unstructured panic lines (per security-plan §Error Handling; obs-plan §10/§11).
- (security) `cargo audit` re-run + result recorded (deferral closed iff it parses); `cargo deny check`
  VERIFIED true exit 0 across all four classes; `Cargo.lock` un-drifted (per security-plan §Dependency
  Security).
- (design/layouts) Leg transcripts stay inside the closed six-label bracket set with no new lamp state or
  table column; declare-only/residual rows render `[RESIDUAL]`-class treatment, never red; captured
  evidence legible with ANSI stripped (per design-system §Surface: cli; layout-templates §Surface: cli).
- (a11y) No accessibility conformance claim is made from the cli/headless legs; envelope
  `verdict`/`state`/`slo_tier` values stay inside the reproduced enum domains (per a11y-plan §1/§3).
- Affordance criterion: not applicable — no user-operated control is in scope (cli/headless surface;
  operator-pause is isatty-gated and the headless leg never blocks).

## Provenance

- Leaned `<90s` for both TOMLs per the v2-11 precedent + the inherited slo_tier re-check CARRY (whole-run
  latency exceeds every tier by construction; per-check latency is v2-19's — stated in the TOML comments).
- Leaned TAKE on the ride-along golden per the operator's pre-authorized candidacy in the folded CARRY and
  its named `dispatch.rs:95` branch.
- Spec anchors: architecture.md §Established Decisions [Read-Back Dependency Posture] + [Timing-Tolerance
  Model] + [Probabilistic-Assertion Policy]; §Occupied Resources (contracts, env vars); test-plan §3/§6/§7/
  §9/§10; obs-plan §3/§4/§6/§10/§11; security-plan §Dependency Security + §Error Handling + §Input
  Validation; design-system §Surface: cli; layout-templates §Surface: cli; a11y-plan §1/§3/§6.
- Research files: scenarios/*.toml (both), conductor-run dispatch.rs + lib.rs + tests/storm_harvest.rs,
  conductor-verify extract.rs + slo.rs, conductor-core scenario.rs + redact.rs + scenario_catalog.rs,
  conductor-timeline tests/replay.rs; SUT (read-only, HEAD efabe8e): triage cue/{evaluate,thresholds,
  classify,emitter}.rs + cadence/coordinator.rs, pulse-app {deterministic_inference,inference_runtime}.rs,
  mcp-server tools.rs, interpretation/markdown.rs. Full chain: research.md.

## Constraints & rejected approaches

- Re-shaping emissions to force Autonomous-tier cues / incidents (error_percent ≥ 60, p99 ≥ 5×) — REJECTED:
  it would test a different claim than P-010/P-012's ≥3.0×/≥2.5× detection thresholds; the acceptance's
  "candidate" is the cue, proven on the harvest surface.
- Carrying the canary fingerprint via a new `ReadyState` field or a new redact-allowlist field — REJECTED:
  contract-shape churn for a witness; the message-borne form is the established key-set precedent.
- Shortening phase durations to fit a tier deadline — REJECTED: buys nothing (no graded check remains to
  consume the tier) and would re-baseline the seed-named replay golden.
- Leaving the `Contains`/`Absent`-class or `CountAtLeast` checks in place — BANNED: the vacuous-green /
  permanent-CalibrationRegion class measured at v2-11 and settled at source here.
- Asserting service attribution on cue lines — IMPOSSIBLE by SUT design (no `scope_id` on the line, PII
  discipline); attribution rides fresh-dir isolation only.
- No Pulse-side edits, no CI edits, no `deny.toml` changes, no new dependencies.

## Implementation notes

- Sequencing: the harvest parser is authored from the source-derived line shape but its committed fixture is
  pinned from the LEG's verbatim capture (steps 4 → 9) — the storm_harvest precedent; do not invent lines.
- The scenario legs run headless under `agent-run.sh` (CONDUCTOR_AGENT_MODE) — the empty-expected
  operator-checklist hold resolves via the never-block headless resolver; no prompt blocks the leg.
- Pulse's `Incident.fingerprint` now carries the real L1 hash (repaired 2026-08-17) but reaches NO MCP tool
  — the log-harvest route is the only equality surface; do not go looking for a read-back carrier.
- Committed harvest slices must be selected/stripped: Pulse lines carrying `workspace`/data-dir values are
  host-path-bearing — cite them redacted in leg-verdict.md, never verbatim-commit those fields.
- Expected amendments (wrap): obs-plan §4 (Fingerprint-storm required-fields row) + §1 — envelope
  `fingerprints` "pinned `[]` under deterministic L4" is stale at SUT HEAD `efabe8e` (the fixture now
  populates 3 `det-*` evidence refs); test-plan §6 (Scenario: error-baseline-spike) + its §1 restatement —
  the scenario's verification signal presumed gradeable read-back checks (`verdict="Pass"`, `[PASS]`); the
  live signal is now the harvest tier + a `KnownResidual` row (two-site rule per the tests extract).
- If a cue does NOT fire on a live leg (EWMA convergence slower than modeled, tick alignment), that is a
  measurement finding — record it in leg-verdict.md and surface at wrap; do not chase it by inflating the
  scenario's emissions mid-leg.
