# Codebase Research — 2026-08-18-error-baseline-spike-live-proof

## Scope
- **Depth:** deep · **Reads:** 15 files (7 Conductor, 8 SUT read-only) · **Globs/Greps:** 13

## Files inspected
- `scenarios/error-baseline-spike.toml` (full) — phases: baseline 90s `kind="error"` 60 occ @ `error_percent 10`, spike 60s 40 occ @ 35; `[[expected]]` = `CountAtLeast "10"` Hard + `Contains "ErrorRateSpike"` Hard; `slo_tier "<5s"`; seed 424242.
- `scenarios/latency-regression.toml` (full) — baseline/ramp 90s each, `kind="latency"` 3 occ × 60 samples (180 spans/phase), p99 800→2400; `CountAtLeast "50"` Hard + `Contains "LatencyRegression"` Hard; `slo_tier "<20s"`; seed 4317011.
- `crates/conductor-run/tests/storm_harvest.rs` (full) — the harvest pattern this chunk extends: window-slice by pre-leg line count, target-filtered JSONL parser pinned to a REAL captured line, malformed lines skipped never fatal, predicates return `Result<_, String>`; test-only, nothing wired into the run path.
- `crates/conductor-run/src/dispatch.rs` (full) — `EmissionShape::Error` → `error_trace_request` / plain `trace_request` (`:87-99`; the plain branch at `:95-98` is the same builder the Plain-path CARRY cites); `Latency` → `latency_trace_request` (`:115-123`); **no `exception` events outside the Exception shape**; everything emits under `DEFAULT_SERVICE_NAME = "conductor"` (`conductor-emit/src/message.rs:23`).
- `crates/conductor-run/src/lib.rs` (`:180-300`, `:299-434`) — `emit_canary` computes the canary fingerprint (`let fp = fingerprint(&spec)`, `:227`) and logs NEITHER marker nor fp (grep of every `tracing::` line); `execute_scenario` stamps `emitted_ms` BEFORE the timeline (`:338`) and `observed_ms` after read-back (`:369`); empty `expected` → operator-checklist hold → `manual_record` (verdict None); `state_for` overrides state to `KnownResidual` on a degraded read-back (`:425-427`); `EmptyCorpus`/`CallFailed` → Blocked.
- `crates/conductor-verify/src/extract.rs` (`:1-130`) — `Observation.text` = list fields + each report's markdown; `evidence_count` = **`span_refs` count summed** (`:113`); `fingerprints` = `fingerprint_refs`; `observed_for(CountAtLeast)` = evidence_count as decimal; key-set witness SHIPPED (`log_observed_keys` at `:84`, `:96`, `:111`).
- `crates/conductor-verify/src/slo.rs` (`:1-45`, `:60-90`, tests) — `within_tolerance = (0..=tier.deadline_ms()).contains(&latency)`; unmet `CountAtLeast` → CalibrationRegion regardless of declared class (`:79-83`).
- `crates/conductor-core/src/scenario.rs:65-71` — `deadline_ms`: 5_000 / 20_000 / 90_000.
- `crates/conductor-core/src/redact.rs:21-63` — allowlist has no singular `fingerprint` field; `message` is allowlisted (the witness channel).
- SUT `andromeda-pulse` @ HEAD `efabe8e` (read-only; same HEAD the 2026-08-17 alignment transcribed — no new SUT drift):
  - `crates/triage/src/cue/evaluate.rs` — `evaluate_thresholds`: ErrorRateSpike per-service, gated `samples < min_ewma_samples → skip` (`:38`); LatencyRegression per-operation, gated `samples < min_latency_samples` (`:90`); **both cues carry `fingerprint: None`** (`:74`, `:141` — "a statistical condition, not a specific fault"); only `evaluate_service_went_silent` checks `BootstrapState` (`:167`).
  - `crates/triage/src/cue/thresholds.rs` — `DEFAULT_ERROR_RATE_MULTIPLIER 3.0` (`:65`) · `DEFAULT_LATENCY_MULTIPLIER 2.5` (`:70`) · base floors 0.01 / 100ms (`:75`, `:80`) · `MIN_EWMA_SAMPLES 10` (`:99`) · `MIN_LATENCY_SAMPLES 50` (`:107`) · tick 1s (`:84`). Also `ANDROMEDA_PULSE_BASELINE_BOOTSTRAP_SECONDS` override exists (`:12`) — silence-family only, irrelevant here.
  - `crates/triage/src/cue/classify.rs` — Autonomous requires magnitude ≥5.0 AND confidence ≥0.9 AND persistence ≥30 (`:23`); Suggested ≥3.0 AND ≥0.7 (`:25`); dual bypass 10× / abs 0.05 / 1000ms.
  - `crates/triage/src/cue/emitter.rs` — per-cue **`triage.cue.emit`** line (`:194-205`): `kind`, `priority`, `scope`, `magnitude`, `absolute_value`, `persistence_seconds`, `confidence`, `suppression_bypassed`, message "attention cue emitted"; deliberately NO `scope_id` (PII discipline, `:171-175`). Also `triage.cue.tick` heartbeat (`cues_emitted`, `cadence_triggers_emitted`, …) and `triage.cue.suppression_check` per cue. Consts: `cue/mod.rs:42-44`, `:51`, `:55`.
  - `crates/triage/src/cadence/coordinator.rs` — Tier-1 consumes ONLY `PriorityTier::Autonomous` cues (`:389-390`); **Tier-2 cycles are skipped wholesale on `HardwareProfile::CpuPrimary`** (`:154-172`), logging `cadence.trigger` with `tier="tier2"`, `cue_kind`, `priority`, "tier2 skipped: cpu-primary profile disables acceleration".
  - `pulse-app/src/deterministic_inference.rs` (full) — `CANNED_L4_OUTPUT_JSON`: title "Deterministic verification incident", fixture symptom/hypotheses/steps; `evidence_refs` = 3 constants (`det-span-…`, `det-template-0007`, `det-fingerprint-…`); `is_resolution_summary: false`.
  - `pulse-app/src/inference_runtime.rs` (`:650-760`) — incident creation: `title`/`detail` = scrubbed canned L4 fields; `fingerprint` = the TRIGGERING CUE's L1 fingerprint (storms only; baseline cues supply None); **`evidence_refs.span_ids = Vec::new()` always** (`:733`); `fingerprint_hashes = parsed.evidence_refs` (`:734`); `resolution_summary_text = None` at creation (`:742`), attached only by `attach_resolution_summary_to_incident` at resolution (`:512-519`); re-emission dedup on (kind, scope, scope_id) (`:686-707`).
  - `crates/mcp-server/src/tools.rs` (`:300-460`) — list items `{incident_id,status,severity,title,opened_at_unix_nano}`; `retrieve_report`: `degraded_mode = parse(resolution_summary_text).is_none()` (`:374-378`); `retrieve_telemetry_slice`: `span_refs` ← `span_ids`, `fingerprint_refs` ← `fingerprint_hashes` (`:430-441`).
  - `crates/interpretation/src/markdown.rs` (`:249-329`) — degraded branch renders title/status/severity/symptom(=`incident.detail`)/`span:`+`fp:` evidence lines/workspace; NO branch renders a cue kind.

## Graph impact (trace: `.andromeda/runs/2026-08-18T17-47-56-phase/tree-query-2026-08-18-error-baseline-spike-live-proof.json`, 2 queries)
- **`evaluate_check`** — 18 caller rows: `conductor-run::execute_scenario` (`lib.rs:395`) + conductor-verify internals/tests. The grading path is shared and this chunk adds no caller — the per-check extraction and dispatcher suffice unchanged (scope premise 4).
- **`%harvest%`** — only `storm_harvest.rs` symbols exist → the `baseline_harvest` module name is free (collision check).

## Patterns detected
- **Harvest-surface substitution** (`storm_harvest.rs`, whole file; test-plan §6 as amended 2026-08-16): retired read-back token → declare-only + a test-only harvest of Pulse's own telemetry line, graded at the `conductor-run` unit tier on the operator gate.
- **Message-borne witness** (`extract.rs::log_observed_keys`): variable observed values ride the allowlisted `message` field — the channel for the new canary-fingerprint witness (no new allowlist field needed; hex survives `redact_value`).
- **Fresh-dir-per-leg + restart between legs** (verification-harness.md 2026-08-16): open-incident dedupe is NOT by fingerprint; storm retention carries over — both dissolve on a fresh dir.
- **MINT-THEN-READ** for `status` (verification-harness.md 2026-08-16): run the producing leg (`SCENARIO=<name> bash scripts/agent-run.sh run`), then read the minted `run_id` back.

## Conventions to follow
- Declare-only retirement documents the retired check in TOML comments and moves the assertion to the harvest tier (the fingerprint pair's exact treatment; testing.md 2026-06-22 chain).
- **Timing fields untouched**: `crates/conductor-timeline/tests/replay.rs:23` loads `scenarios/error-baseline-spike.toml` verbatim and freezes `fixture_seed_424242` (`:35`) — the golden depends on gaps/seed/jitter only, so `[[expected]]`/`slo_tier`/comment edits leave it byte-identical, and the golden tests must be confirmed to have RUN (the vacuous-green third form).
- `scenario_catalog.rs` asserts this family at NAME level only (`:88`, `:96`) — emptying `expected` breaks no catalog test; `scenario.rs`'s mention of the family name is to be checked at implement (likely a doc/test fixture string).

## Key findings (the decisive chain)
1. **Neither authored token can reach any read-back surface — settled at source, both halves of the partially-corrected CARRY.** (a) Composed text = list fields + report markdown; during a leg every incident is OPEN, so `resolution_summary_text` is None → `degraded_mode: true` → the DEGRADED report branch renders fixture-constant title/symptom + refs, and no branch anywhere renders a cue kind (Pulse's spelling would be snake_case `error_rate_spike` regardless). `Contains` would hard-Fail forever. (b) `CountAtLeast` grades `evidence_count` = `span_refs` count = `span_ids.len()` = **0 by construction** (no writer exists) → permanent CalibrationRegion measuring a structurally-empty field. The 2026-08-17 "de-vacuumed" correction is real but lands on `fingerprint_refs` (3 fixture constants) → the envelope's `fingerprints` will be NON-empty for the first time (obs-plan §4's "pinned `[]`" statement is stale at SUT HEAD — a wrap amendment candidate), while helping neither the count nor the tokens.
2. **The family's live-proof surface is `triage.cue.emit`** — kind (`error_rate_spike` / `latency_regression`), priority, scope, magnitude, absolute_value, persistence_seconds, confidence, suppression_bypassed; corroborated by the `cadence.trigger` tier2-skip line (which carries `cue_kind` + `priority` on cpu-primary) and the `triage.cue.tick` counters. Attribution rides fresh-dir isolation (the line carries no scope_id by design; the only traffic is Conductor's, service "conductor").
3. **Both scenarios top out at Suggested tier — no incident forms from this family on this host.** Shipped magnitudes: spike ≈ 3.5× (0.35 vs max(≈0.10, 0.01)·3.0 threshold — fires), ramp ≈ 3.0× (2400 vs max(≈800,100)·2.5=2000 — fires); Autonomous needs ≥5× + conf ≥0.9 + persistence ≥30. Tier-1 accepts Autonomous alone; Tier-2 cycles are skipped on cpu-primary. Read-back therefore observes ONLY the preflight canary's incident: "candidate produced" is proven on the harvest surface, and "non-blocked" holds because the canary incident keeps the corpus non-empty while `degraded` routes the rows to `KnownResidual` (`state_for`). The spike's absolute rate 0.35 > 0.05 also trips the dual-condition bypass → `suppression_bypassed: true` expected on its cue line (a P-057 witness for free).
4. **Whole-run latency exceeds every tier by construction.** `emitted_ms` predates the timeline, so `latency_ms` ≈ 150s (error) / 180s (latency) + poll > Tier90s' 90_000. Once both checks retire, nothing grades the tier (manual/residual rows evaluate no SLO); the honest available move is re-declaring both TOMLs `<90s` (the v2-11 precedent, minimizing the gap), with real per-check latency owned by v2-19.
5. **The transcription-equality carry discharges via the CANARY, not the family.** Error/Latency shapes emit no `exception` events and both cues carry `fingerprint: None`, so no family storm line can exist — but every leg's preflight fires the 12-occurrence canary exception storm, which writes `triage.pattern.storm.detected` with Pulse's 8-hex `fingerprint_hex`. Conductor computes the matching 32-hex at emit (`lib.rs:227`) and logs it nowhere. One message-borne `info!` witness in `emit_canary` + a prefix-8 comparison predicate at the harvest tier completes the live equality test. (Pulse's `Incident.fingerprint` now carries the real L1 hash — repaired 2026-08-17 — but still reaches no MCP tool, so the log-harvest route remains the only comparison surface.)
6. **Floors + convergence are provable on the cue line itself**: a cue's existence proves its floor (the evaluator skips below-floor snapshots), `confidence` encodes samples/100 (≥0.6→0.7+ at spike, 1.0 at ramp), and `magnitude` is the live baseline-relative ratio — so baseline convergence "within tolerance over the sample floors" is concretely assertable as bounds on the harvested cue fields.

## New files to create
- `crates/conductor-run/tests/baseline_harvest.rs` — the family's harvest tier: `triage.cue.emit` parser (target-filtered, malformed-skipped, pinned to the leg's real captured line post-leg per the storm_harvest precedent) + predicates: `cue_fired(kind)` / magnitude ≥ 3.0 / confidence ≥ 0.7 (floor witness) / `suggested` priority observed / spike-cue `suppression_bypassed == true` (P-057 witness) — exact predicate set is plan's.
- `conductor-0.2.0/chunks/2026-08-18-error-baseline-spike-live-proof/evidence/leg-verdict.md` — the two-leg record + harvested slices.

## Files to modify
- `scenarios/error-baseline-spike.toml` — retire both `[[expected]]` to declare-only (comments carry the per-surface settlement), `slo_tier` `<5s` → `<90s` (lean), header prose re-aimed at the harvest tier. **gap_ms/seed/jitter untouched.**
- `scenarios/latency-regression.toml` — same treatment (`<20s` → `<90s`).
- `crates/conductor-run/src/lib.rs` — ONE `tracing::info!` in `emit_canary` carrying the computed canary fingerprint on `message` (allowlist-clean; the §5 witness).
- `crates/conductor-run/tests/storm_harvest.rs` — the prefix-8 equality predicate (Conductor's logged 32-hex vs harvested 8-hex `fingerprint_hex`); MAY fold into `baseline_harvest.rs` instead — implement's call.
- Verification-only touchpoints (no expected edit): `crates/conductor-timeline/tests/replay.rs` golden must stay byte-identical AND be confirmed to have run; `crates/conductor-core/src/scenario_catalog.rs` (name-level assertions); `crates/conductor-core/src/scenario.rs` family-name occurrence (check at implement).

## Open questions
- none blocking plan-decision. One implementation-scope: whether the equality comparator lives in `storm_harvest.rs` or `baseline_harvest.rs` → blocks: implementation-scope (file list provisional on that point only).
