# Plan — 2026-08-19-connection-lifecycle-live-proof: "connection-lifecycle live proof"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-19-connection-lifecycle-live-proof · **Version:** conductor-0.2.0 · **Generated:** 2026-08-19T22:15:00Z

## Goal
Write the port-occupier driver that does not exist (the CARRY from 2026-08-16-fault-application-spans:
`conductor-faults` is a code-graph island — 0 crate edges, 0 external references), then live-prove the
Connection & Health Awareness family (P-001..P-004) at the harvest tier. The driver is three small,
compiler-enforced pieces: a declarative phase-level fault (`PhaseSpec.fault`, serde + garde — per
architecture.md §Conventions Config conventions), an explicit `conductor-run` → `conductor-faults`
`Cargo.toml` edge (per architecture.md §Established Decisions [Module Boundaries]), and a `PhaseGuard`
returned from the existing `on_phase` closure — `run_timeline_observed` is already generic over the guard
type and holds it to the phase boundary (research.md §Patterns), so the occupier's RAII release IS the
boundary drop and no timeline change exists.

Research determined (not merely suspected) that the family's four Hard `Contains` read-back checks are
structurally ungradeable: Pulse's connection FSM surfaces only via a TauRPC resolver, a Tauri broadcast
topic, and tracing lines — no MCP tool carries it, and `Observation.text` composes incident-list + report
text only (research.md §Files inspected). The four TOMLs therefore retire declare-only with the measurement
recorded (the fingerprint-storm → error-baseline → pii-scrub precedent, per architecture.md §Standard
Contracts declare-only family note), and the live claim grades over Pulse's OWN lines — the
`connection.state.transition` FSM ledger, the `app.boot.otlp.grpc.bind` bind/bound pair, and the
`connection.tick` heartbeat — pinned verbatim in a new `connection_harvest.rs` (test-plan §6 harvest chain).
Two live legs (the pii-scrub A/B shape): leg A measures the shipped grading (structural fails) on the old
TOMLs; leg B lands the shipped declare-only shape, the driven port-conflict choreography, and the harvest
capture. The chunk also carries the 31st `cargo audit` PREREQ re-check — on a RESTATED basis, because this
chunk's new crate edge ends the "zero dependency delta" footing (security-plan §Dependency Security,
admitting-a-dependency precedent; scope.md §3 premise-correction).

Claim target: `verification-matrix.json#v2-15` (P-001..P-004 are all `DriveObserve` — the visual halves,
connection-dot hue and halo, stay operator-checklist; the live rows land as non-Blocked declare-only
ManualCheck/KnownResidual, which IS that contract).

## Implementation Steps
1. **Phase-level fault model** (`crates/conductor-core/src/phase_spec.rs`): add
   `FaultSpec { kind: FaultKindSpec }` with the closed enum `FaultKindSpec::PortOccupier`
   (serde `kebab-case` → `kind = "port_occupier"`; the closed enum is itself the kind validation), and
   `PhaseSpec.fault: Option<FaultSpec>` with `#[serde(default)]` + `#[garde(dive)]`. Add the cross-field
   invariant via the established garde `Context` pattern (architecture.md §Established Decisions
   [Validation Library]): a fault-declaring phase MUST declare `occurrences == 0` — emitting into a window
   where the occupier holds `:4317` (or Pulse is deliberately absent) would send OTLP into the occupier.
   Unit tests: the TOML table parses; an unknown kind is a `CoreError::Config`; a fault phase with
   occurrences > 0 is a `CoreError::Validation`; absent table defaults `None`.
2. **Thread the new field** through the 13 `PhaseSpec` literal-constructor sites (graph-enumerated,
   research.md §Graph impact): `crates/conductor-core/src/{error,load_envelope,phase_spec,scenario}.rs`,
   `crates/conductor-run/tests/dispatch_wire.rs`, `crates/conductor-timeline/src/convert.rs` — each gains
   `fault: None` (mechanical; `EmissionSpec` is untouched — it is `#[non_exhaustive]` and the occupier is
   not an emission).
3. **The driver** (`crates/conductor-run`): add `conductor-faults.workspace = true` to `Cargo.toml` (the
   root `[workspace.dependencies]` row already exists; `Cargo.lock` gains the member-dep row, ZERO new
   external crates). In `lib.rs`, replace the `on_phase` closure's `Option<tracing::Span>` with a
   `PhaseGuard { span: Option<tracing::Span>, occupier: Option<PortOccupier> }` (single call site —
   `fault_span @ crates/conductor-run/src/lib.rs:532`, closure at `:357`; `fault_span` folds into the
   guard builder). At phase open, a fault-declaring phase attempts ONE `PortOccupier::occupy(port)` via a
   private port-parameterized builder — the production call site passes `OTLP_INGEST_PORT`, unit tests pass
   `0` (tests must NEVER bind the real 4317, per `conductor-faults/tests/port_occupier.rs` discipline).
   **Ratified policy (P4 fork): harness Err after the timeline** — the `on_phase` hook is infallible by
   design, so a failed occupy is captured in a closure-captured slot, the timeline completes (every phase
   is 0-occurrence, nothing emits), and `execute_scenario` returns a sanitized `Err` ("port occupier could
   not bind: {io-kind}" — no host paths) BEFORE `observe()`: no RunRecord row is written for a run whose
   declared fault never applied. The occupier's own `fault.port_occupier` span (already §4-conformant,
   opened created-not-entered inside `occupy`) rides the guard and closes at the boundary drop — the RAII
   release, with the realized hold on the debug `message`.
4. **Guard-mechanics tests** (in `lib.rs`'s existing `mod tests`): a fault-declared fixture phase produces
   a guard whose boundary drop releases the (ephemeral-port) bind — assert via rebind; the captured-failure
   path returns `Err` after the timeline (pre-hold the ephemeral port); the walk fixture (no fault) is
   byte-identical in behavior to today (goldens unchanged — the fault field does not alter
   `PhaseTransition` timing).
5. **Re-base the four TOMLs** (each header gains the measurement note, the pii-scrub precedent; every
   `[[expected]]` block retires — declare-only; tiers stay inside the closed `<5s`/`<20s`/`<90s` set and
   are SET FROM MEASURED leg-B latency, never widened):
   - `scenarios/receiver-failed-port-conflict.toml` — the 3-phase operator choreography (research.md §Key
     run-path facts; forced by ready-gate ∧ Pulse-holds-:4317 ∧ no-retry-bind): `pre-conflict` (~20s,
     occurrences 0 — the operator stops pulse-app on cue) → `port-held` (~40s, `fault.kind =
     "port_occupier"`, occurrences 0 — occupy at phase open; the operator starts pulse-app into the
     conflict) → `recovery` (~15s, occurrences 0 — boundary drop released the bind; the operator restarts
     pulse-app, which now binds). Total ≤ ~85s; tier `<5s` → `<90s`.
   - `scenarios/receiver-lifecycle-state.toml` — phases already correct (emission tables shipped); retire
     the `Contains "Stalled"` check; tier `<90s` kept (phases sum ~69s; latency ≈ duration).
   - `scenarios/last-span-ago-tracking.toml` — make the three intervals' emission explicit
     (`kind = "plain"`, occurrences 1 — today's default, now declared); retire `Contains "Receiving"`;
     tier predicted `<20s` (measured at leg B).
   - `scenarios/orthogonal-health-domains.toml` — `error-burst` gains a REAL emission table
     (`kind = "error"`, occurrences ~10, `error_percent = 100`, inside the load envelope's sustained
     terms); `clean-stop` declared occurrences 0; retire both checks; tier re-measured (predicted `<90s`
     kept).
6. **Catalog-guard re-bases** (`crates/conductor-core/src/scenario.rs`): add the family guard
   (`connection_lifecycle_family_is_declare_only_at_the_harvest_tier`, 4 stems, asserting zero expected +
   the fault declaration on the port-conflict TOML) and re-base whichever existing guards assert these
   TOMLs' expected counts / suite membership (the pii chunk's 2-test shape: declare-only guard +
   suite-guard membership).
7. **The harvest test** (NEW `crates/conductor-run/tests/connection_harvest.rs`, pii_harvest mold —
   parsers + hard predicates + verbatim doc-comment pins landed from leg B; runs in CI over the PINNED
   lines, deterministic): parsers for `connection.state.transition`
   (`from_state`/`to_state`/`last_span_ago_ms`/`trigger_reason`/`severity`), `app.boot.otlp.grpc.bind`
   ("OTLP gRPC receiver bound" / "bind failed" + `reason`), `connection.tick`
   (`state`/`last_span_ago_ms`/`severity`). Predicates:
   - **Walk leg (P-001):** the REAL ledger — boot-time `Listening→Receiving` (the preflight canary
     advances the app-wide `last_ingest`), then `Stalled→Receiving→Idle→Stalled` across the walk phases,
     with `last_span_ago_ms` straddling the 10s/60s thresholds (± the 1s poller tick).
   - **Port-conflict leg (P-003):** `→ReceiverFailed` with `trigger_reason = bind_failed` (error level)
     while Conductor's occupier held the bind; the paired `app.boot.otlp.grpc.bind` "bind failed"; after
     the boundary release, "OTLP gRPC receiver bound" from the relaunched pulse-app + a recovery
     transition — the release proven by the SUT's own successful rebind.
   - **P-002:** transition/tick `last_span_ago_ms` values consistent with the emission intervals (the ±1s
     tracker claim witnessed at threshold crossings and ticks).
   - **P-004:** `Receiving→Idle` during clean-stop with NO `ReceiverFailed` line anywhere in the walk/P-004
     leg windows — the no-false-alert orthogonality negative (the sentinel-absence style).
8. **Live legs** (operator-gated, never CI; fresh dirs under `%TEMP%/pulse-legs/`; deterministic-L4 Pulse;
   capture `{data_dir}/logs/agent-latest.jsonl.<date>` sliced by pre-leg line count): **Leg A** — old
   TOMLs, checks intact: measure the structural grading (the four `Contains` checks fail against corpus
   text that cannot carry the tokens; the port-conflict occupy against a live Pulse errs AddrInUse → the
   ratified Err path measured live). **Leg B** — re-based TOMLs: the four declare-only rows land
   non-Blocked (ManualCheck; KnownResidual under degraded read-back) with journal + runs.db evidence; the
   port-conflict choreography runs (stop → conflict → recovery on the phase cues); harvest lines captured
   and pinned. Evidence to `chunks/2026-08-19-connection-lifecycle-live-proof/evidence/leg-verdict.md`.
9. **PREREQ — the 31st `cargo audit` re-check, restated basis:** probe `cargo audit` standalone (expect
   the byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` at true exit 1); run
   `cargo deny check advisories bans licenses sources` over the NEW `Cargo.lock` (expect true exit 0); the
   re-pin states the changed footing — an intra-workspace member edge, zero new external crates — per the
   2026-08-16 admitting-a-dependency precedent. No floor raise, no `deny.toml` ignore, no CI edit; close
   the moment it parses.

## Codebase touchpoints
### New files
- `crates/conductor-run/tests/connection_harvest.rs` — the family's harvest-tier proof (parsers +
  predicates + verbatim leg-B pins + the no-ReceiverFailed negative).
### Files to modify
- `crates/conductor-core/src/phase_spec.rs` — `FaultSpec`/`FaultKindSpec` + `PhaseSpec.fault` + the
  occurrences-0 cross-field invariant + tests.
- `crates/conductor-core/src/error.rs` · `crates/conductor-core/src/load_envelope.rs` ·
  `crates/conductor-core/src/scenario.rs` · `crates/conductor-timeline/src/convert.rs` ·
  `crates/conductor-run/tests/dispatch_wire.rs` — `fault: None` at the 13 literal sites (+ scenario.rs
  catalog-guard re-bases per step 6).
- `crates/conductor-run/Cargo.toml` (+ `Cargo.lock`) — the run→faults edge.
- `crates/conductor-run/src/lib.rs` — `PhaseGuard`, the occupy-at-phase-open builder (port-parameterized,
  private), the captured-failure → post-timeline `Err`, `fault_span` folded in; guard tests in `mod tests`.
- `scenarios/receiver-failed-port-conflict.toml` · `scenarios/receiver-lifecycle-state.toml` ·
  `scenarios/last-span-ago-tracking.toml` · `scenarios/orthogonal-health-domains.toml` — step 5.
- `conductor-0.2.0/verification-matrix.json` — the v2-15 claim (P5 link step; PREMISE-CORRECTION note in
  `notes` at claim, the v2-14 phase-time ratification shape).

## Test Commands
```bash
cargo nextest run --workspace --profile ci
cargo test --workspace --doc
cargo clippy --workspace --all-targets -- -D warnings
bash scripts/agent-run.sh run
bash scripts/agent-run.sh status
cargo deny check advisories bans licenses sources
cargo audit   # expected true exit 1 — the standing RUSTSEC-2026-0244 DB fault; probed standalone, never a chunk gate
```
Live legs (operator-gated, NOT CI — produce the leg evidence + harvest pins; per the leg-verdict precedent):
```bash
# fresh dir per leg under %TEMP%/pulse-legs/; operator launches pulse-app with:
#   ANDROMEDA_PULSE_MCP_ENABLED=1  ANDROMEDA_PULSE_L4_DETERMINISTIC=true  ANDROMEDA_PULSE_DATA_DIR=<leg dir>
# same three exported in Conductor's shell (the run-contract shell-declaration proxy), then per scenario:
CONDUCTOR_RUNS_DIR=<leg-runs> bash scripts/agent-run.sh boot && cargo run -p conductor-cli -- run <scenario> --agent-mode
# leg B port-conflict: stop/start/restart pulse-app on the three phase cues (pre-conflict/port-held/recovery)
```

## Acceptance Criteria
- (arch) The driver reaches `conductor-faults` through a declared `Cargo.toml` edge from `conductor-run` —
  no bin-local shim, no duplicated occupier (per architecture.md §Established Decisions [Module
  Boundaries]); cites `verification-matrix.json#v2-15`.
- (arch) `127.0.0.1:4317` remains the SOLE inbound bind, loopback-only, released at the phase boundary; no
  new listener, port, or `CONDUCTOR_*` env handle (per architecture.md §Cross-cutting Patterns Trust
  boundary · §Occupied Resources).
- (arch) Every live-leg row conforms to the run-report envelope: closed five-state set, closed tier set,
  journal-relative latency; re-based TOMLs keep `check_load_envelope` / `check_sut_drift` /
  `check_scenario_backing` green with the `[[exempt]]` ledger EMPTY (per architecture.md §Standard
  Contracts · §Occupied Resources load-envelope).
- (core) A fault-declaring phase with `occurrences > 0` fails garde validation; an unknown `fault.kind`
  fails parse; the four re-based TOMLs load via `from_toml_str_with` (per security-plan §Input Validation,
  scenario-config row).
- (run) A fault-declared phase's failed occupy yields a sanitized harness `Err` AFTER the timeline
  completes — no RunRecord row for an unapplied fault; a succeeded occupy releases at the boundary drop,
  proven by rebind in the guard tests (per architecture.md §Cross-cutting Patterns Verdict/error wall;
  ratified at the P4 fork).
- (obs) The driven leg's self-obs carries `fault.port_occupier` span_event `new`+`close` at info with
  exactly `fault_type` + `port`, observed beneath `timeline.execute` — or §4's CONDITIONAL wording is
  re-based on the measurement as a wrap amendment (per obs-plan §4 Fault-injection spans).
- (obs) Zero unlogged panics across both legs including the Err path; no absolute host path (`%TEMP%`
  leg dirs included) in any log, journal, or runs.db field (per obs-plan §9 · §11).
- (security) `cargo deny check advisories bans licenses sources` true exit 0 over the NEW lock; `cargo
  audit` probed standalone; the 31st pin restates its basis on the member-edge footing (per security-plan
  §Dependency Security).
- (security) No absolute host path or internal struct name in `<run_id>.md`, `runs.db`, or the JSONL
  journal from either leg — grep-verified (per security-plan §Error Handling).
- (tests) `cargo nextest run --workspace --profile ci` green (incl. phase_spec, guard, catalog-guard, and
  harvest tests) + doctests + clippy -D warnings as their own stages; zero retries (per test-plan §3 · §10).
- (tests) The live legs exit 0 with non-Blocked declare-only rows (ManualCheck / KnownResidual) parsed from
  `runs/<run_id>.jsonl`; the harvest predicates grade the pinned leg-B lines in CI: the walk ledger, the
  ReceiverFailed(bind_failed) witness while the occupier held, the post-release "OTLP gRPC receiver bound"
  rebind proof, and the no-ReceiverFailed negative on the walk/P-004 legs (per test-plan §6 harvest chain ·
  §9 Live-Pulse scenarios).
- (design/layouts) No new color token, ANSI entry, or bracket label; re-declared tiers stay inside the
  closed set and render from the declared value (per design-system §Surface: cli · layout-templates §cli
  Component blocks).
- (a11y) Every emitted `verdict`/`state` value maps to the existing six-label crosswalk under verdict-first
  precedence — no new enum value (per a11y-plan §6 State-naming crosswalk).

## Provenance
- Spec anchors: architecture.md §Established Decisions [Module Boundaries] · [Validation Library] ·
  [Timing-Tolerance Model] · §Standard Contracts (envelope + declare-only note) · §Cross-cutting Patterns
  (Trust boundary · Verdict/error wall · Determinism) · §Occupied Resources; security-plan §Input
  Validation · §Dependency Security · §Error Handling · §Security Anti-Patterns; test-plan §3 · §6 · §9 ·
  §10 · §11; obs-plan §3 · §4 · §6 · §9 · §11; design-system §Surface: cli; layout-templates §cli;
  a11y-plan §3 · §6.
- Research files: research.md §Files inspected (16 files, both repos) · §Graph impact (3 queries, trace in
  the run dir) · §Key run-path facts.
- Leaned (decisive, per artifact): the 3-phase port-conflict choreography (forced by ready-gate ∧
  Pulse-holds-:4317 ∧ no-retry-bind — research.md §Key run-path facts); declared-data `PhaseSpec.fault`
  over a phase-name convention (architecture.md §Conventions — config is declared data, garde-validated);
  the A/B two-leg structure (the pii-scrub ratified precedent); no `port` field in `FaultSpec` (scope law —
  no listener widening; tests inject via the private port-parameterized builder); harvest-tier grading
  (research determined no MCP surface carries the FSM state).
- Ratified at the P4 fork: occupy-failure = harness Err after the timeline (operator choice, Recommended
  option).

## Constraints & rejected approaches
- `EmissionShape::PortOccupier` — REJECTED: a fault application is not an emission; the dispatcher stays
  untouched (0-occurrence phases never dispatch).
- Phase-name convention ("port-held" triggers the occupier) — REJECTED: stringly magic vs the declared-data
  model garde can validate.
- An occupy retry-loop across the window (or a spawned retry task) — REJECTED: the on_phase hook is
  synchronous and the 3-phase choreography already solves the ordering; a task adds concurrency for
  nothing.
- A `port` field on `FaultSpec` — REJECTED: listener widening; 4317 is THE exception. Tests reach `:0` via
  the private builder parameter, never config.
- Changing `run_timeline_observed`'s signature (fallible on_phase) — REJECTED: it is already guard-generic;
  the captured-failure slot keeps the timeline crate untouched.
- Any CI-run test binding the real `:4317` — REJECTED (test ban; ephemeral `:0` only).
- warn+continue on a failed occupy — REJECTED at the P4 fork (silent-downgrade class; a row would exist for
  a run whose declared fault never applied).
- Spec masters as touchpoints — REJECTED by rule; see Expected amendments below.

## Implementation notes
- **Expected amendments (wrap):** obs-plan §4 — `fault.port_occupier` `timeline.execute` parentage
  CONDITIONAL → unconditional on the driven-leg measurement; test-plan §1 Critical-paths closing note + §6
  Cleanup parenthetical — the connection-lifecycle Path-1 wording re-bases to declare-only + harvest (BOTH
  sites, the two-site rule); architecture.md §Standard Contracts — the declare-only family note gains the
  connection family; layout-templates §Wireframe idle-console P-001/P-002/P-003 sample rows — de-literalize
  to `<slo_tier>` IF tiers move (the 2026-08-18 precedent); a11y-plan §3/§6 — no delta expected (no new
  enum value).
- The FSM is app-wide and the preflight canary advances `last_ingest` — the walk leg's witnessable ledger
  starts from Stalled (research.md §Key run-path facts); predicates assert the REAL ledger, never the naive
  Listening-first walk.
- P-004's `error-burst` emission must stay inside the load envelope's two sustained terms (per-emitting-
  phase judgment; a silence window is not an emitting phase).
- Tier predictions (latency ≈ scenario duration, the v2-11/v2-12 precedent): walk `<90s` keep (~69s
  phases); port-conflict `<90s` (≤85s phases); last-span-ago `<20s`; orthogonal `<90s` — all SET FROM
  MEASURED leg-B values inside the closed set.
- Windows capture: Pulse's log is `agent-latest.jsonl.<date>` (rolling); slice the leg window by pre-leg
  line count (the pii-scrub mold).
- The walk TOML's phases are already emission-correct — do not reshape them; only the check retires.
- Leg A's port-conflict run measures the ratified Err path live (occupy vs a live Pulse → AddrInUse →
  sanitized Err, no row) — record it in leg-verdict as the policy's live witness.
