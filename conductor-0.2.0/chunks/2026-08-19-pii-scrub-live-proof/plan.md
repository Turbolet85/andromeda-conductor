# Plan — 2026-08-19-pii-scrub-live-proof: "pii-scrub live proof"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-19-pii-scrub-live-proof · **Version:** conductor-0.2.0 · **Generated:** 2026-08-19T19:41:39Z

## Goal
Drive the pii-scrub family (P-035/P-047/P-048, `scenarios/pii-scrub.toml`, seed 4317035) end-to-end against a
live deterministic-L4 Pulse and land the family's honest live claim. Research source-proved at SUT HEAD
`efabe8e` that NO MCP read-back surface can carry scrub evidence (span attributes are never stored; the
value-bearing stored fields live in the in-memory buffer no corpus tool reads; `retrieve_telemetry_slice`
projects only the constant `evidence_refs`; `retrieve_report` renders only L4-fixture/incident fields), so the
TOML's four Hard `Absent` sentinel checks can pass only VACUOUSLY and the Hard `Contains "user.email"` can only
FAIL STRUCTURALLY. The chunk therefore (a) measures that behavior live once, (b) retires the five checks to
declare-only WITH the measurement recorded (the fingerprint-storm / error-baseline precedent, per test-plan §6
retirement precedent + architecture §Standard Contracts declare-only note), and (c) grades the live claim at
the harvest tier: all three PII carriers (span attributes / exception event / log records) measured INGESTED by
Pulse via its own `duckdb.append` lines, with scrub semantics byte-verified at source and pinned.

Two defects/gaps the research surfaced ride the chunk: a Conductor-side fix — `pii_log_record` stamps all
seven same-severity records in a tight loop, colliding on Pulse's `log_records` PK `(ts_unix_nano,
resource_hash, severity_number)` (the 2026-08-15 PK-collision class on a different table) — and two SUT intake
items (the bare `sk_live_…` ApiKey value matches NO pattern in Pulse's scrub catalog; Pulse exposes no external
scrub observability at all). The chunk also proves the hygiene half of the intent: no raw excerpt value in any
Conductor artifact or committed pin, via a sentinel-absence negative over recomputed corpora. Claims **v2-14**
under the operator-ratified concretized acceptance (P4 fork, 2026-08-19).

## Implementation Steps
1. **PREREQ — the 30th consecutive `cargo audit` re-check** (before any dependency-adjacent work; basis =
   zero dependency delta this chunk). Run `cargo audit` STANDALONE and capture its true exit; expect the
   byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` advisory-DB parse fault at exit 1 — if it PARSES,
   close the deferral this chunk. Verify the overlap, never assume it: `cargo deny check advisories bans
   licenses sources` standalone, expect true exit 0 over the untouched `Cargo.lock`. No floor raise, no
   `deny.toml` ignore, no CI edit (security-plan §Dependency Security, two-fault split). Record both exits in
   the chunk evidence.
2. **Fix the log-record PK collision (emit-local)** — `crates/conductor-emit/src/pii.rs::pii_log_record`:
   derive one base stamp per request and give each record a DISTINCT `time_unix_nano` (base + per-category
   index offset in nanos; `observed_time_unix_nano` equal to it; `std::time` only — never tokio's clock).
   Companion unit test in `pii.rs::tests`: seven records ⇒ seven distinct `time_unix_nano` values, order
   stable. Graph: `pii_logs_request` is the only caller; no external threading (research §Graph impact).
3. **New harvest module** `crates/conductor-run/tests/pii_harvest.rs` (the `restart_harvest.rs` shape:
   per-target parse structs, skip-don't-fail parsing, witness predicates, pinned verbatim leg lines,
   TEST-ONLY — nothing wired into the run path):
   - Parser for `duckdb.append` lines (`target` / `table_name` / `rows_appended` — the production
     `append_table_traced` shape, consumer.rs:165-210).
   - Witness predicates over the leg window: two `spans` appends of 2 rows (the two pii-span occurrences),
     two `span_events` appends of 1 row (the exception event per occurrence), two `log_records` appends of
     7 rows (the PK fix proven live when 7/7 lands twice), plus the canary's 12-row storm batch and incident
     formation — matching the emitted counts with zero collision drops.
   - **Sentinel-absence negative**: recompute both per-occurrence corpora per phase from seed 4317035 (pin
     the four `emission_seed(4317035, phase, occurrence)` values as computed constants with the formula
     cited from `dispatch.rs:197-201` — do NOT widen conductor-run's public API) and assert NO corpus value
     appears in any pinned line (security-plan §Anti-Patterns → Logging applied to test pins).
4. **Leg A — measure the as-shipped grading** (fresh data dir under `%TEMP%/pulse-legs/<ts>`): run
   `SCENARIO=pii-scrub scripts/agent-run.sh run` with the TOML's five checks INTACT. Expected measurement:
   the `Contains "user.email"` check fails structurally (worst verdict `Fail`), the four `Absent` checks pass
   vacuously — the row records the ungradeable-surface behavior. Capture the row + observation verbatim; this
   is the retirement's live measurement (test-plan §6 precedent: never retire on source-proof alone when one
   cheap leg can measure it).
5. **Retire `scenarios/pii-scrub.toml` to declare-only**: remove all five `[[expected]]` blocks; rewrite the
   header comments to record the measurement (source citations: tools.rs:422-443 slice projection ·
   schema.rs spans/log_records columns · appender.rs whole-field markers; plus leg A's observed row); re-tier
   `slo_tier` `<5s` → `<90s` (declare-only rows measure latency to read-back completion, the sibling
   families' measured class — confirm from leg A's `latency_ms` before committing the value). Catalog load
   tests must stay green (all 35 TOMLs validate); grep loader/goldens for pii-scrub-specific pins (tier or
   expected-count) and re-base any found (the restart chunk re-shaped 4 such sites).
6. **Leg B — the shipped shape, harvested** (fresh data dir): run the retired scenario; assert the row lands
   like the siblings — verdict `null`, state `ManualCheck` overridden to `KnownResidual` under a degraded
   read-back, never `Blocked` (lib.rs:383-401 routing); in-envelope; headless never blocks on the operator
   checklist. Slice Pulse's `{data_dir}/logs/agent-latest.jsonl.<date>` by pre-leg line count; extract the
   witness lines; PIN them verbatim into `pii_harvest.rs`; run the hygiene grep over ALL leg artifacts
   (journal · report · runs.db dump · Conductor self-obs · the pins) for all 14 corpus values + absolute host
   paths → expect zero hits; record everything in `chunks/2026-08-19-pii-scrub-live-proof/evidence/leg-verdict.md`.
7. **Record the SUT intake items** (leg-verdict §New measurements, for the next Pulse visit — never spec
   edits): (1) bare provider-key `sk_live_…` matches no pattern in `scrubber.rs` — a P-047 recall gap for the
   realistic bare-key shape (do NOT re-shape Conductor's corpus to mask it); (2) no external scrub
   observability exists — suggest a redaction-count field on `duckdb.append` or `buffer.tick`; (3)
   `log_records` PK `(ts_unix_nano, resource_hash, severity_number)` silently drops same-severity
   same-nanosecond records — Conductor now works around it emit-side.
8. **Gates**: full workspace nextest (ci profile, zero-retry) + doctests + clippy -D warnings; the envelope
   catalog gate (`check_load_envelope`) stays green with `[[exempt]]` exactly empty (no re-shape — the pii
   phases sit orders of magnitude inside the terms).

## Codebase touchpoints
### New files
- `crates/conductor-run/tests/pii_harvest.rs` — parsers + witness predicates + pinned verbatim leg lines +
  the sentinel-absence negative (step 3/6).
- `conductor-0.2.0/chunks/2026-08-19-pii-scrub-live-proof/evidence/leg-verdict.md` — leg A + leg B record,
  hygiene grep, intake items.
### Files to modify
- `crates/conductor-emit/src/pii.rs` — `pii_log_record` distinct per-record stamps + unit test (step 2;
  preserve the module's seeded-determinism doc contract).
- `scenarios/pii-scrub.toml` — declare-only retirement + measurement comments + tier re-declare (step 5).
- `conductor-0.2.0/verification-matrix.json` — v2-14 `chunk` link at P5; `ref`/`status` at implement/wrap
  (the matrix is a ledger, not a spec — the sanctioned carve-out).
- (Possible, discovered at step 5's grep: any loader/golden test pinning pii-scrub's tier/expected-count —
  re-base in place, the restart-chunk precedent.)

## Test Commands
```bash
cargo nextest run -p conductor-emit                       # step-2 unit (stamp distinctness + corpus units)
cargo nextest run -p conductor-run --test pii_harvest     # step-3/6 harvest pins + sentinel negative
cargo nextest run --workspace --profile ci                # full zero-retry gate (all 35 TOMLs still load)
cargo test --doc --workspace                              # doctests nextest skips
cargo clippy --workspace --all-targets -- -D warnings     # lint gate
cargo audit ; cargo deny check advisories bans licenses sources   # PREREQ, standalone true exits (step 1)
# Live legs (operator/local gate, never CI) — same shell exports for pulse-app AND the harness:
#   ANDROMEDA_PULSE_DATA_DIR=%TEMP%/pulse-legs/<ts> · ANDROMEDA_PULSE_MCP_ENABLED=1 · ANDROMEDA_PULSE_L4_DETERMINISTIC=true
SCENARIO=pii-scrub bash scripts/agent-run.sh run          # leg A (checks intact), then leg B (retired shape)
bash scripts/agent-run.sh status                          # smoke: runs.db row + journal present
```

## Acceptance Criteria
- (arch) All code lands in existing workspace members (`conductor-emit` / `conductor-run` tests / scenario
  TOML); no new crate, no new cross-seam edge, no new `CONDUCTOR_*` env var, no new `runs.db` column, `state`
  stays the closed five-variant set (per architecture.md §Established Decisions [Module Boundaries] ·
  §Occupied Resources · §Standard Contracts "Run report envelope").
- (arch) P-035/P-047/P-048 remain members of the accepted capability set (manifest lines 48/60/61), and every
  retired check lands in a NAMED state with the measurement recorded — never a silent pass (per
  architecture.md §Established Decisions [Accepted Capability Set] · §Cross-cutting Patterns "Verdict/error
  wall").
- (security) Zero raw PII excerpt values in any leg artifact or committed pin: grep the journal, Markdown
  report, `runs.db`, self-obs streams and `pii_harvest.rs` pins for all 14 per-occurrence corpus values —
  zero hits; zero absolute host paths and internal struct names likewise (per security-plan §Error Handling ·
  §Security Anti-Patterns → Logging + Data Protection).
- (security) The PREREQ re-check recorded with standalone true exits and the audit↔deny overlap VERIFIED
  (deny true exit 0), deferral re-pinned on the zero-dependency-delta basis or closed if the DB parses (per
  security-plan §Dependency Security).
- (tests) `cargo nextest run --workspace --profile ci` green with `pii_harvest` included, zero retries,
  workspace line coverage ≥ 60% (per test-plan §9 Unit stage · §10 Quality Gates).
- (tests) The leg journal parses to the Run-report envelope with a matching `runs.db` row via bound-parameter
  `SELECT`; the family's non-`Fail` terminal state is reported in the envelope, never a non-zero exit (per
  test-plan §6 Headless deterministic scenario run · §3 `run` exit-code semantics).
- (tests) No `[[expected]]` check left standing that passes vacuously — all five retired to declare-only with
  leg A's measurement recorded in the TOML header (per test-plan §6 Fingerprint-storm / error-baseline
  re-base precedent).
- (obs) Harvest evidence carries key names, counts and Pulse's own `[REDACTED:{category}]` convention only —
  no value content; no new span name (the harvest is test-only) and no allowlist widening; zero unlogged
  panics across both legs; every self-obs line carries the §3 base set with `run_id` (per obs-plan §11 PII
  Scrubbing · §6 Boundary-call wrappers · §10 Always-required SLO invariant · §3 two record shapes).
- (design/layouts) The pii row renders inside the closed six-label/lamp set (expected landing: `[RESIDUAL]`
  non-red, or `[MANUAL]` when undegraded) with `—`/null measurement cells where unmeasured and non-content
  detail sub-lines; zero new tokens, labels or table columns (per design-system §Surface: cli → Tokens +
  Component Patterns #3/#4 · layout-templates §cli Component — Primary content block 2).
- (a11y) The recorded verdict/state pair maps to exactly one six-lamp crosswalk row, chosen deliberately
  (`KnownResidual` vs `ManualCheck`), verdict-first precedence intact (per a11y-plan §6 State-naming
  crosswalk).
- **Capability claim** — `verification-matrix.json#v2-14`: the concretized acceptance (operator-ratified at
  the P4 fork; text at P5 step 4) is fully satisfied by leg B + the harvest pins + the source-pinned scrub
  semantics + the hygiene negative.
- Affordance criterion: N/A — no user-operated control in this chunk (v2-14 is `dynamic-external`; the P-035
  operator-checklist render is exercised headless-unconfirmed by design, not claimed as an affordance).

## Provenance
- Spec anchors: architecture.md §Established Decisions [Read-Back Dependency Posture] · [Module Boundaries] ·
  [Accepted Capability Set] · [Probabilistic-Assertion Policy] · §Standard Contracts (Readiness gate ·
  Run-report envelope · Corpus access) · §Occupied Resources; security-plan §Input Validation · §Dependency
  Security · §Error Handling · §Security Anti-Patterns; test-plan §1 · §3 · §6 · §7 · §9 · §10 · §11;
  obs-plan §3 · §4 · §6 · §9 · §10 · §11; design-system §Color Palette · §Surface: cli; layout-templates §cli;
  a11y-plan §1 · §3 · §6 · §11.
- Research files inspected: research.md §Files inspected (Conductor 8 · SUT 6, all at HEAD `efabe8e`);
  graph trace `.andromeda/runs/2026-08-19T19-17-45-phase/tree-query-2026-08-19-pii-scrub-live-proof.json`.
- Leaned decisions (material, per artifacts): PK fix emit-local in `pii_log_record` (graph shows no external
  callers — research §Graph impact); two legs A/B (the storm precedent measures before retiring); corpus
  keeps the bare `sk_live_` shape (leaned per scope Boundaries — the corpus tests the catalog, not vice
  versa); tier re-declare `<90s` pending leg A confirmation (sibling-measured class).

## Constraints & rejected approaches
- REJECTED: re-shaping Conductor's ApiKey value to `api_key=sk_live_…` so Pulse's key=value pattern matches —
  that tunes the corpus to the scrubber and masks a genuine recall gap; the bare shape stays, the gap is
  intake.
- REJECTED: reading the buffer DB or `corpus.db` directly for scrub evidence — the buffer is in-memory
  (unreachable cross-process) and direct corpus reads are banned outside one-off operator-sanctioned
  diagnosis (security-plan §Data Protection); harvest grades Pulse's own tracing lines only.
- REJECTED: adding a Conductor-side scrub-evidence span/attribute or envelope extra — no allowlist widening,
  no span outside the bounded set, no envelope schema change (obs-plan §11; the envelope is test-plan-owned).
- DO NOT gate the headless leg on the P-035 `inquire` checklist (isatty-gated, recorded unconfirmed —
  layout-templates §cli Headless invariant).
- DO NOT touch `deny.toml`, CI workflows, or any version floor for the audit deferral (bounded wait alone).
- DO NOT write any spec master under `.andromeda/` — expected amendments route through wrap (below).

## Implementation notes
- `emission_seed` is private to `dispatch.rs` — pin its four computed values for (seed 4317035, phases 0/1,
  occurrences 0/1) as constants in `pii_harvest.rs` with the formula cited (`dispatch.rs:197-201`), rather
  than widening any API.
- The exception-event fingerprint is computed by Pulse over RAW pre-scrub content (appender.rs:354-366) —
  each occurrence's stacktrace embeds different seeded values, so the two pii exceptions land DISTINCT
  fingerprints (no storm interaction; the storm threshold is untouched by 2 occurrences).
- Leg B's expected `duckdb.append` shape (fixed emitter): `spans` 2+2 (pii) beside the canary's 3 (warm-up) +
  12 (storm); `span_events` 1+1; `log_records` 7+7. A `log_records` line below 7 post-fix falsifies the fix —
  halt and diagnose, do not pin.
- Preflight budgets: the wrapper derives its wall-clock from `pulse-run-contract.toml` (`warmup_ms/1000 +
  min_canary_poll_seconds + margin`); a missing term is exit 2 (test-plan §3 `boot`).
- Windows leg housekeeping: data dirs under `%TEMP%/pulse-legs/<ts>` (one-sweep cleanup, operator convention).
- Expected amendments (wrap): architecture.md §Standard Contracts — extend the declare-only family note with
  pii-scrub + the no-scrub-observability measurement; test-plan §6 pii-scrub scenario row + §1 Critical-Path-1
  twin re-base (the two-site rule); design-system §Surface: cli Component Patterns #4 — the `P-035`
  ManualCheck sample literal if the family's rendered state moves; layout-templates §cli — the five P-035
  sample sites if tier/state move; obs-plan §4 — the pii-scrub scenario row's read-back wording if one
  asserts read-back evidence.
