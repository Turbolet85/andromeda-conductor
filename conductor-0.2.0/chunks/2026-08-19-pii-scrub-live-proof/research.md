# Codebase Research — 2026-08-19-pii-scrub-live-proof

## Scope
- **Depth:** deep · **Reads:** 14 targeted file/section reads (Conductor 8, SUT 6) · **Globs/Greps:** ~12 · **Graph queries:** 1

**SUT anchor:** andromeda-pulse HEAD is **`efabe8e`** — unchanged since every prior live measurement (2026-08-16/17/18 legs), so all prior read-back-surface measurements remain anchored to the live HEAD. The TIME-axis re-verification obligation is discharged for this chunk.

## Files inspected
- `scenarios/pii-scrub.toml` (full) — 2 phases (`emit-pii-spans` traces / `emit-pii-logs` logs; `kind = "pii"`, `occurrences = 2`, all 7 categories, `gap_ms = 2000`), `seed = 4317035`, `slo_tier = "<5s"`; **5 `[[expected]]` checks: 4 Hard `Absent` sentinels (`@example.com` · `sk_live_` · `Bearer ` · `password=`) + 1 Hard `Contains` (`user.email`)**; header comments name "Report excerpts + corpus" as the graded surface and flag `user.email` as an Epoch-8 calibration point.
- `crates/conductor-emit/src/pii.rs` (full) — `PiiCorpus::seeded` (ChaCha8, 7 structurally-valid synthetic values; sentinels confirmed in `generate()`); `pii_trace_request` → root `pii.attributes` span (7 attrs under `field_key()`s incl. `user.email`) + child `pii.exception` span whose `exception` event embeds ALL values in `exception.message` + `exception.stacktrace` (type `PiiLeak`); `pii_logs_request` → one record per category, value in BOTH body (`request payload: {value}`) and attribute. Values synthetic by construction (doc header), seed-deterministic.
- `crates/conductor-run/src/dispatch.rs:55-145, 197-201` — the `Pii` arm (:124) seeds the corpus from `emission_seed(scenario.seed, phase_index, occurrence)` — **per-occurrence corpus AND span identity, so the 2026-08-15 constant-identity PK defect does NOT recur on the spans table** (verified: `emission_seed` distinct per slot, test :255-260).
- `crates/conductor-run/src/lib.rs:355-470` — the grading path: unusable read-back ⇒ `Blocked`; **empty `expected` ⇒ `manual_record` (`ManualCheck`, verdict `None`; `degraded` observation overrides state to `KnownResidual`)** — the declare-only routing the sibling families land on; non-empty `expected` ⇒ every check evaluated against `observation.observed_for(kind)`, worst verdict wins.
- `crates/conductor-verify/src/extract.rs:1-110` — `Observation.text` = `query_incident_list` JSON + each incident's `retrieve_report` markdown (+ slice segments), union across incidents; `Absent`/`Contains` grade against this composed text. Doc header re-verifies the three tool shapes against Pulse's `tools.rs`.
- `crates/conductor-run/tests/restart_harvest.rs:1-90` — the harvest-module pattern to copy: per-target parse structs, skip-don't-fail line parsing, witness predicates, pinned verbatim leg lines, TEST-ONLY affordance; capture source `{data_dir}/logs/agent-latest.jsonl.<date>` sliced by pre-leg line count.
- `crates/conductor-report/src/journal.rs` — `JournalWriter::append(&RunRecord)`: journal lines ARE run-report envelopes; **no emitted payload content reaches the journal by construction**.
- `contracts/pulse-capabilities.toml:48,60,61` — P-035/P-047/P-048 all in the accepted set. `contracts/pulse-load-envelope.toml:25-26` — `max_sustained_rate_spans_per_s = 10000` · `max_sustained_storm_ms = 600000`: the pii phases (2 occurrences · 2s gaps) are orders of magnitude inside; **no re-shape needed, `[[exempt]]` stays empty**.

**SUT files (andromeda-pulse @ `efabe8e`):**
- `crates/security/src/scrubber.rs` (full, 180 lines) — 7 first-match-wins regexes; `ScrubbedValue::Redacted` carries the category label ONLY. **Category walk vs Conductor's corpus: email ✓ jwt ✓ bearer ✓ credit_card ✓ ssn ✓ secret_kv ✓ (`password=`) — but `api_key` requires a key-name token INSIDE the value (`(api[_-]?key|access[_-]?token|secret[_-]?key|auth[_-]?token)[\s=:]+[\w-]{12,}`), and Conductor's bare `sk_live_…` value matches NO pattern → it would persist RAW wherever value-bearing storage exists. 6/7 recall against this corpus — a real SUT recall gap (bare provider-key shape).**
- `crates/buffer/src/appender.rs:280-375` — `scrub_otlp_field` applies at persistence with **WHOLE-FIELD semantics**: any match replaces the entire string with `[REDACTED:{category}]`. Exception fingerprint computed over RAW pre-scrub type+stacktrace (dedupe stability); `exception.message`/`exception.stacktrace` stored scrubbed; `exception_type` stays raw. Log body scrubbed at build (`extract_log_body`).
- `crates/buffer/src/schema.rs:135-210` — **the `spans` table stores NO attributes at all** (ids · ts · service_name · end_time · status_code only) — span-attribute values are never persisted, so P-048 holds BY SCHEMA for span attributes; `log_records` stores `body` (scrubbed) and NO attributes column. **`log_records` PK = `(ts_unix_nano, resource_hash, severity_number)`** — Conductor's 7 pii records share resource_hash + severity 0 and stamp `unix_nanos()` per record in a tight build loop, so same-nanosecond records VIOLATE the PK and are logged-and-skipped (the 2026-08-15 defect class on a different table; real risk at Windows clock granularity).
- `crates/mcp-server/src/tools.rs:360-469, 596-668` — **`retrieve_telemetry_slice` returns ONLY the incident's `evidence_refs` projections** (`span_refs` hex, `fingerprint_refs`, timestamps) — under det-L4 the constant `det-*` triple; **`retrieve_report`** renders `assemble_report(incident, parsed_l4, previously_seen)`: all sections L4Output/incident-derived (scrub-at-projection), evidence section renders ref STRINGS only. **Neither corpus tool carries any stored-telemetry content** — no span names, no attributes, no bodies.
- `crates/interpretation/src/markdown.rs` (grep-anchored) — six report sections all fixture/incident-sourced; no telemetry-excerpt section exists on the sidecar path (P-035's "Report excerpts" surface is the in-app Report UI, not MCP).
- `crates/buffer/src/consumer.rs:165-210` — the PRODUCTION `duckdb.append` info line (`append_table_traced`): fields `rows_appended` + `duration_ms` + `table_name`. **Grep across `crates/buffer` + `crates/security`: Pulse emits ZERO scrub-specific telemetry** — no redaction counter, no per-redaction line, nothing naming a category.

## Graph impact
- **`PiiCorpus`** — 47 caller rows: `conductor-emit` internals + its unit/integration tests + exactly ONE production external caller, `Dispatcher::dispatch` at `crates/conductor-run/src/dispatch.rs:124`. Zero further cross-crate blast. Trace: `.andromeda/runs/2026-08-19T19-17-45-phase/tree-query-2026-08-19-pii-scrub-live-proof.json`.

## Patterns detected
- **Harvest module** (`restart_harvest.rs:1-90`): structs per tracing target, `filter_map` skip-don't-fail parsing (verdict/error wall applied to evidence), pinned verbatim lines, nothing wired into the run path.
- **Declare-only routing** (`lib.rs:383-401` + test :752): empty-`expected` → `ManualCheck`/`None`, degraded → `KnownResidual` — the exact row shape the sibling families measured live.
- **Per-slot emission seed** (`dispatch.rs:197-201`): identity + content vary per (seed, phase, occurrence).

## Conventions to follow
- **Verdict/error wall on evidence**: read-back faults are `Outcome` values (`extract.rs`); harvest lines that fail to parse are skipped, never fatal.
- **Non-content witnesses**: key-names/counts only on `message` (obs precedent); Pulse's own `[REDACTED:{category}]` markers and category labels are safe to pin — they carry no value content by construction.
- **Journal = RunRecord envelopes** (`journal.rs:54`): no payload channel exists.

## The central measurement (closes the scope's main premise)
Under deterministic L4 at SUT HEAD `efabe8e`, **no MCP read-back surface can carry scrub evidence for this family**, for four independent, source-proven reasons: (1) span attributes are never stored (schema); (2) the value-bearing stored fields (exception message/stacktrace, log body, template content) live in the IN-MEMORY buffer DuckDB no corpus tool reads and the sidecar cannot see; (3) `retrieve_telemetry_slice` projects only `evidence_refs` (constant `det-*` under det-L4); (4) `retrieve_report` renders only L4-fixture/incident fields. Therefore the TOML's 4 `Absent` checks can only pass VACUOUSLY and the `Contains "user.email"` check can only FAIL STRUCTURALLY — the exact storm-family shape whose remedy (retire to declare-only WITH the measurement recorded, grade the live claim at the harvest tier) is the established precedent. Additionally **no scrub observability exists at all externally** (no telemetry, no readable stored surface), so the harvest tier can witness INGESTION (per-table `duckdb.append` `rows_appended` matching the emitted counts, incident formation from the preflight canary) but cannot witness scrubbing itself — scrub semantics are provable only at SOURCE (scrubber.rs + appender.rs, pinnable as cited constants/behaviors) and by Pulse's own negative-canary tests.

## New files to create
- `crates/conductor-run/tests/pii_harvest.rs` — parsers (`duckdb.append` per-table counts; incident-formation lines), witness predicates (spans=4 · span_events=2 · log_records=14 rows appended for the two phases under the fixed emitter; zero collision-drops), pinned verbatim leg lines, and a **sentinel-absence negative** over every pinned line + the leg's Conductor artifacts (recompute the per-occurrence corpora from seed 4317035 via `emission_seed` and assert no value appears).

## Files to modify
- `crates/conductor-emit/src/pii.rs` — `pii_log_record`: distinct `time_unix_nano` per record (base + per-category offset) so 7 same-severity records cannot collide on the `log_records` PK; companion unit test. (Callers per graph: only `pii_logs_request` internally; no external threading.)
- `scenarios/pii-scrub.toml` — post-measurement: retire the 5 checks to declare-only with the measurement recorded in comments (the storm/baseline comment pattern); re-tier `<5s` → `<90s` candidate (row latency is read-back-completion-bound like both siblings — confirm from the leg).
- `conductor-0.2.0/verification-matrix.json` — v2-14 per the P4/P5 fork (see Open questions).
- (No load-envelope, no run-contract, no harness, no capability-manifest change.)

## Open questions
- **v2-14 concretization fork** — the cap's outcome-level acceptance ("read back scrubbed in both corpus and report excerpts…") is source-proven unattainable under det-L4 + the 4-tool surface (the central measurement above), and not by THIS chunk only but by ANY chunk until Pulse ships a scrub observable. Contract §Acceptance lifecycle: surface to the operator now → blocks: **plan-decision** (claim with a refined concrete acceptance mirroring the twice-ratified v2-12/v2-13 shape, vs leave pooled with a failed-concretization note).
- **Log-record PK fix shape** — offset stamps inside `pii_log_record` (lean: yes, emit-side, keeps dispatch untouched) vs a dispatch-level stagger → blocks: **implementation-scope** (file list provisional on this).
