# Plan — 2026-08-20-latency-regression-re-proof: "latency-regression re-proof"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-20-latency-regression-re-proof · **Version:** conductor-0.2.0 · **Generated:** 2026-08-20T16:26:14Z

## Goal

Prove P-011/P-012 live: drive a re-shaped `latency-regression` scenario against a live
deterministic-L4 Pulse until Pulse's own `triage.cue.emit` line carries `kind: "latency_regression"`
at suggested tier, and pin the shipped-but-live-unexercised latency predicates in
`baseline_harvest.rs` to that leg's verbatim capture (the storm/error harvest precedent, per
test-plan §6 + §1). The shipped 90/90 shape is measured undetectable (CARRY, leg B); research pinned
the actual mechanism at SUT HEAD `efabe8e` — the long baseline is a 60s-rotating t-digest pair read
as union(current, previous), the short read is current-only on a 15s pair, and the floor (50) +
confidence (samples/100) both read the long pair's current-window count — so the re-shape makes the
regression short relative to the last ~60–120s of dense history, delivered as TWO narrow spike
pulses offset ≡30 (mod 60) seconds so the uncontrollable rotation phase φ cannot kill both
(research.md §The detectability mechanism).

The second CARRY obstacle — the canary incident auto-resolves (120s idle / 30s ticks) across any
benign window this scenario can produce — is closed by the ratified option (b): grading stays purely
at the harvest tier, and the row's non-Blocked clause is re-thought as a scoped mapping change in
`conductor-run`: a DECLARE-ONLY scenario whose post-green-preflight read-back finds an empty active
list lands the pre-accepted auto-resolve residual (`KnownResidual`, verdict null) instead of
`Blocked` — the existing degraded-override shape (`state_for`, lib.rs:445) extended to the one case
where emptiness is SUT-designed lifecycle, never a harness precondition. Checks-bearing scenarios
and `CallFailed` keep `Blocked` (security-plan §Input Validation: never a false pass-as-empty — a
declare-only scenario grades nothing, so no check can falsely pass). The absorbed PREREQ (32nd
consecutive `cargo audit` re-check) runs the ratified bounded-wait protocol per security-plan
§Dependency Security.

## Implementation Steps

1. **Re-shape `scenarios/latency-regression.toml`** (declare-only, seed 4317011, `slo_tier "<90s"`,
   p_ids P-011/P-012 all unchanged) to the four-phase two-pulse recipe:
   - `baseline` — gap 200000ms, kind latency, occurrences 200, samples 20, operation "checkout",
     profile p50 100 / p95 400 / p99 800 (≈20 samples/s wire; ≥3 long rotations of dense history).
   - `spike-a` — gap 40000ms, occurrences 10, samples 1, operation "checkout",
     profile p50 3000 / p95 3100 / p99 3200 (narrow: every sample ≥ 3.75× baseline p99; ≈0.25/s so
     the union stays < 1% spike while the cue window is open).
   - `baseline-b` — gap 110000ms, occurrences 110, samples 20, profile 100/400/800 (re-purifies the
     union; places spike-b's start exactly 150s ≡ +30 (mod 60) after spike-a's).
   - `spike-b` — gap 40000ms, occurrences 10, samples 1, profile 3000/3100/3200.
   Total emission 390s; every phase far inside `max_sustained_storm_ms = 600000` and
   `max_sustained_rate_spans_per_s = 10000` (envelope basis: occurrences/gap ≤ 1/s). Re-base the
   header prose to the measured mechanism (the "5-min window" comment is stale against the 60s
   rotation) and state the two-pulse φ-robustness rationale + the expected cue window (first
   ~10–35s of whichever pulse draws φ ≥ ~3.5s; dead zone ~6% per pulse, disjoint by construction).
2. **Land the row-state mapping change in `crates/conductor-run/src/lib.rs`** (`execute_scenario`'s
   `ReadBackOutcome::EmptyCorpus` arm): when `scenario.expected.is_empty()`, proceed to the
   declare-only path with an empty degraded observation (no fingerprints) so the existing
   `state_for`/`manual_record` machinery lands `state: KnownResidual`, `verdict: null`, latency
   stamped from the read-back attempt; log the residual attribution (post-green-preflight emptiness
   = the active-incident surface cannot outlive a benign window; auto-resolve 120s/30s is
   SUT-designed P-020-family behavior). `CallFailed` and checks-bearing `EmptyCorpus` keep the
   `Blocked` return unchanged. Preserve the verdict/error wall (states as `Ok(...)`).
3. **Extend the `conductor-run` unit tests** beside the existing `state_for` tests (lib.rs:735+):
   declare-only + empty read-back → KnownResidual/verdict-null; checks-bearing + empty → Blocked
   (unchanged); CallFailed → Blocked (unchanged).
4. **Align the source-derived latency fixture** in `baseline_harvest.rs` to the evaluator's real
   line (`scope: "operation"` per andromeda-pulse `cue/evaluate.rs:120-128` — the shipped builder
   says "service"), keeping the leg-A error pins byte-untouched.
5. **Run the PREREQ**: `cargo audit` recorded verbatim (expect the 32nd byte-identical
   `duplicate advisory ID: RUSTSEC-2026-0244` exit 1 unless upstream healed — close the deferral
   the moment it parses); `cargo deny check advisories bans licenses sources` observed true exit 0
   over the committed lock (verified overlap, never assumed). No floor raise, no `deny.toml`
   ignore, no CI edit.
6. **Drive the live leg** (operator gate, /implement P3): fresh `%TEMP%` data dir, six-item recipe
   per `.claude/rules/verification-harness.md`, `SCENARIO=latency-regression` with `SEED` unset
   (TOML seed governs, per the 2026-08-18 §3 harness fix). Harvest Pulse's
   `agent-latest.jsonl.<date>` sliced by pre-leg line count; also capture the `baseline tracker
   tick` timestamps so φ for each pulse is computable post-hoc (diagnostic if a pulse misses).
7. **Pin the leg verbatim**: add captured-line tests to `baseline_harvest.rs` (the leg-A precedent)
   asserting the harvested `latency_regression` cue satisfies `cue_fired` + `convergence_witness`
   (magnitude ≥ 3.0, confidence ≥ 0.7) + `suggested_tier` + the absolute-arm
   `suppression_bypassed` (3200ms > 1000ms bypass floor); write
   `evidence/leg-verdict.md` with the quoted lines, the row (`KnownResidual`/verdict-null via the
   new mapping), offsets, and the φ readings. If the φ draw lands in a pulse's ~6% dead zone AND
   the other pulse also misses (possible only via non-φ causes), diagnose from the tick lines
   before re-running — never adjust thresholds to fit.
8. **Re-run the catalog gates** (they ride nextest): `check_load_envelope` over the re-shaped
   phases (expect pass unaided, `[[exempt]]` exactly empty), the scenario catalog guards
   (`scenario.rs:441-467` — load+validate, p_ids, declare-only all preserved). Check whether any
   seeded-stream golden (`replay__*` / `pacing__*` / `dispatch_wire__*`) pins this scenario's
   stream; if so re-pin as committed artifacts, never via `cargo insta review`.

## Codebase touchpoints

### New files
- `conductor-0.2.0/chunks/2026-08-20-latency-regression-re-proof/evidence/leg-verdict.md` — the live-leg evidence (implement P3).

### Files to modify
- `scenarios/latency-regression.toml` — the four-phase two-pulse re-shape + header re-base (step 1).
- `crates/conductor-run/src/lib.rs` — the declare-only empty-read-back mapping (step 2) + unit tests (step 3); no signature changes (graph: no caller threading — the change is internal to `execute_scenario`/`manual_record`).
- `crates/conductor-run/tests/baseline_harvest.rs` — fixture scope alignment (step 4) + verbatim leg pins (step 7); leg-A error pins untouched.

## Test Commands
```bash
cargo nextest run -p conductor-run --profile ci        # harvest predicates + the new mapping tests
cargo nextest run -p conductor-core --profile ci       # catalog guards + check_load_envelope over the re-shaped TOML
cargo nextest run --workspace --profile ci             # zero-retry full gate
cargo clippy --workspace --all-targets -- -D warnings
cargo audit                                            # PREREQ: expect exit 1 byte-identical (record verbatim; close if it parses)
cargo deny check advisories bans licenses sources      # PREREQ overlap: must be true exit 0 over the committed lock
bash scripts/agent-run.sh boot                         # preflight against the live Pulse (fresh-dir recipe)
SCENARIO=latency-regression bash scripts/agent-run.sh run   # the live leg (SEED unset -> TOML seed 4317011); produces the row + journal + harvest window
bash scripts/agent-run.sh status                       # smoke: the leg's row landed in runs.db/JSONL
```

## Acceptance Criteria
- (arch) Every re-shaped emitting phase passes `check_load_envelope` unaided against
  `max_sustained_storm_ms` + `max_sustained_rate_spans_per_s`, `[[exempt]]` exactly empty in both
  directions (per architecture.md §Occupied Resources — pulse-load-envelope.toml).
- (arch/security) The re-shaped TOML loads through garde: `kind` present, p50≤p95≤p99 per phase,
  `occurrences`/`samples` within `MAX_OCCURRENCES`, no `#[garde(skip)]` on any `PhaseSpec` child
  (per architecture.md §Conventions Config conventions + security-plan §Input Validation).
- (tests) `cargo nextest run -p conductor-run --profile ci` green with the latency predicates
  pinned VERBATIM from THIS chunk's leg capture; leg-A error pins byte-unchanged; workspace ci
  profile green, zero retries (per test-plan §3 run + §7 + §10).
- (tests/obs) The live leg's row is non-Blocked under the new mapping: `state "KnownResidual"`,
  `verdict null`, `latency_ms` recorded (INTEGER, wall-clock journal-relative), `slo_tier "<90s"`,
  `runs/<run_id>.jsonl` parsing to the eleven-field envelope; checks-bearing scenarios and
  CallFailed still land Blocked in the unit tests (per test-plan §6 Scenario 1 + obs-plan §5/§10 +
  security-plan §Anti-Patterns → Input).
- (obs) The harvested `triage.cue.emit` line(s) carry `kind "latency_regression"`, `priority
  "suggested"`, `magnitude ≥ 3.0`, `confidence ≥ 0.7`, `suppression_bypassed true`,
  `scope "operation"`, quoted verbatim into `evidence/leg-verdict.md` with zero absolute host
  paths and zero unstructured panic lines in the self-obs streams (per obs-plan §3/§9 +
  security-plan §Error Handling).
- (security) The 32nd `cargo audit` re-check is recorded verbatim; if still the advisory-DB fault,
  `cargo deny check advisories bans licenses sources` observed true exit 0 over the committed
  un-drifted `Cargo.lock`, deferral re-pinned with basis stated — no floor raise, no `deny.toml`
  ignore, no CI edit (per security-plan §Dependency Security).
- (design/layouts — by construction) No new lamp state, ReportState, color, or table column: the
  re-thought row renders through the existing `[RESIDUAL]` treatment; `[ENVIRONMENT-SUSPECT]`
  stays absent for an in-envelope run (per design-system §Anti-Patterns + layout-templates §cli).

## Provenance
- Spec anchors: architecture.md §Occupied Resources (load envelope · service split · env handles) +
  §Established Decisions [Timing-Tolerance Model] [Accepted Capability Set]; security-plan §Input
  Validation + §Dependency Security + §Error Handling; test-plan §3 (harness `run`/SEED) + §6/§1
  (harvest precedent, two-site rule) + §7 (goldens) + §10 (gates); obs-plan §3 (two line shapes) +
  §4 (span constraints) + §5/§10 (latency fields) + §9; design-system §Surface: cli; layout-templates
  §cli; a11y-plan §6 (crosswalk — no new enum member).
- Research: `research.md` (16 files incl. andromeda-pulse at HEAD `efabe8e`; graph trace
  `tree-query-2026-08-20-latency-regression-re-proof.json`, 119+47 rows).
- **Leaned** (decisive, per artifacts): harvest-tier + row-mapping over scenario-raised incidents —
  option (a) is measured dead on this host (Tier-2 cadence skipped on cpu-primary,
  coordinator.rs:154-172, settled at v2-12); the CARRY itself names option (b) with "the row's
  non-Blocked clause re-thought".
- **Leaned** (design synthesis, previewed for ratification): the two-pulse ≡30 (mod 60) shape over
  a single spike — the long-pair rotation anchor is pulse-app's persist clock (uncontrollable φ),
  a single pulse dies for φ < ~3.5s (~6%), and two 30s-offset draws cannot both land there; cost is
  ~2 min of leg time.
- **Matrix note**: NO cap is claimable by this chunk — the routed-forward latency half lives inside
  v2-12 (already `verified`; its notes name this route entry), no unclaimed cap mentions
  P-011/P-012, v2-19 is excluded by the working entry. Completion = master `complete` flip + a
  v2-12 `notes` addendum at wrap recording the forward pointer resolved.

## Constraints & rejected approaches
- **Rejected: scenario-raised incident read-back** — suggested-tier cues form no incident on this
  host (Tier-2 cycles skipped wholesale on cpu-primary); building on it would re-create leg B's
  Blocked with extra steps.
- **Rejected: single-pulse shape** — φ-lottery: floor+confidence read the long pair's
  current-window count, which resets on a 60s rotation anchored to pulse-app boot; φ < ~3.5s kills
  the pulse and nothing in the TOML controls φ.
- **Rejected: threshold/multiplier tuning or Pulse-side changes** — the SUT is fixed at HEAD; no
  threshold multiplier fixes a denominator the anomaly feeds (CARRY).
- **Rejected: envelope `[[exempt]]` entry / term edit / tier change / new `CONDUCTOR_*` env var /
  new crates** — never-widen (security-plan §Input Validation); tier stays `<90s` (v2-12 narrowing
  precedent covers whole-run latency exceeding every tier).
- **Rejected: converting `CallFailed` or checks-bearing `EmptyCorpus` to KnownResidual** — transport
  trouble is not a pre-accepted residual, and a graded `Absent` check over an empty observation
  would be the exact false pass the wall forbids.
- Do NOT touch the leg-A error-spike pins or `storm_harvest.rs`; do NOT grade any Pulse-side
  counter VALUE (`<redacted>` precedent); do NOT set `RUST_LOG` for the leg (cue lines are INFO);
  do NOT sleep-sync the leg (key waits on harvest signals); goldens re-pin fail-don't-write, never
  `cargo insta review`.

## Implementation notes
- The cue re-emits every 1Hz tick while its condition holds — expect a BAND of
  `latency_regression` lines during each detectable window, not one; pin the clearest (the leg-A
  "last suggested line" precedent).
- Expected line values when clean: magnitude ≈ 4.0 (3200/800), decaying as the union absorbs the
  pulse; confidence = min(1, φ·20/100); `absolute_value` ≈ 3100–3232; `persistence_seconds` =
  the long pair's current-window sample count (a COUNT, despite the name).
- The `baseline tracker tick` lines in the harvest give the rotation timestamps — compute each
  pulse's φ for the evidence; a missed pulse with φ in the dead zone is the DESIGNED miss (the
  other pulse carries the leg), any other miss is a finding to diagnose before re-running.
- The error half of `baseline_harvest.rs` shares the parser — the new pins reuse
  `parse_cue_emitted`/`cue_fired` untouched.
- `manual_record` currently takes `&Observation` — the empty-degraded observation for the
  EmptyCorpus/declare-only path should reuse the existing struct (degraded=true, fingerprints
  empty), not a new type; the operator-hold still fires on the declare-only path (headless
  resolver never blocks).
- Expected amendments (wrap): test-plan §6 latency-regression scenario + §1 Critical Path — two-site
  re-base to the two-pulse shape + the KnownResidual row; security-plan §Input Validation read-back
  row — record the declare-only empty-corpus carve-out (post-green-preflight, zero-checks-only);
  architecture.md §Standard Contracts run-report note — the six-family declare-only sentence gains
  the auto-resolve-residual landing for this family; obs-plan §4 statistical-anomaly row — measured
  cue offsets from this leg (if the row carries offsets, per the restart precedent).
