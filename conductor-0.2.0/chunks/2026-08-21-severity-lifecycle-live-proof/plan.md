# Plan — 2026-08-21-severity-lifecycle-live-proof: "severity-lifecycle live proof"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-08-21-severity-lifecycle-live-proof · **Version:** conductor-0.2.0 · **Generated:** 2026-08-21T09:35:00Z

## Goal
Live-prove the severity-lifecycle family (P-019..P-023, P-059, P-060) against a live deterministic-L4 Pulse at
SUT HEAD `efabe8e`, in the six-family precedent shape: measure what read-back actually carries, grade the live
claims at the harvest tier over Pulse's own tracing lines, retire the structurally-ungradeable checks
declare-only WITH measurements recorded, and land the evidence trail (per test-plan §6 "Severity-lifecycle
full pass" + §1 Critical Path 4; obs-plan §4 CP4; arch §Probabilistic-Assertion Policy).

The headline observable is REAL and mechanically verified at source: Pulse auto-resolves an incident after a
120s no-reemission window on 30s `AutoResolveObserver` ticks, persists the transition to the corpus, and the
active-only `query_incident_list` then stops returning it, while a post-resolve same-fingerprint retrigger
creates a NEW incident (`list_active`-scoped dedup — new-not-reopen HOLDS). The two structural negatives are
equally verified and become recorded premise corrections rather than silent greens: the P-059 resolution
summary is UNREACHABLE under deterministic L4 (nothing constructs `DigestKind::ResolutionSummary`; the canned
fixture pins `is_resolution_summary:false` — the only two attach triggers), and severity choice cannot be
discriminated through read-back (fixture-pinned `severity:"autonomous"` → list string `"error"`; vocabulary
disjoint from the tier tokens) — the tier ladder is instead graded at the cue tier, where Pulse's own
`priority_tier_label` lines carry "autonomous"/"suggested"/"curious" (research.md §Files inspected).

Zero production-source changes are planned: the shipped `route_read_back` arms, extraction, and envelope carry
every outcome. The chunk's delta is scenario TOMLs (re-shape + measured retirement), one new harvest test
binary, live-leg evidence, the v2-16 matrix claim, and the folded 35th-audit PREREQ.

## Implementation Steps
1. **PREREQ first — 35th consecutive audit re-check (PROBE-AUTO-SATISFY).** Run `cargo audit` and
   `cargo deny check advisories bans licenses sources`. Exact signature (audit true exit 1, first diagnostic
   `duplicate advisory ID: RUSTSEC-2026-0244`, AND deny true exit 0) → the one-line record
   `probe unchanged, 35th consecutive`, close. ANY deviation → the FULL form per the working-route annotation
   (basis re-derivation; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit)
   (per security-plan §Dependency Security).
2. **Re-shape `scenarios/incident-auto-resolution.toml` for the live path** (declared phase data only; garde
   boundary intact, no `skip`):
   - trigger: ≥12 identical-fingerprint exceptions inside one 60s storm window (Autonomous band ≥10; the
     canary's 12 precedent) so an incident actually forms under the Tier-1 autonomous-only gate;
   - a NEW OK-span dilution tail phase between trigger and silence, dense enough that the 30s short-term
     error EWMA ends BELOW the error-rate cue threshold at cease (threshold = max(long-term,
     `base_error_rate`) × multiplier — implement derives the shape from the SUT constants), so no
     `error_rate_spike` cue refreshes the incident after silence (the 2026-08-20 375-cue mechanism);
   - cease + resolve wait ≥150s (120s window + 30s tick + margin);
   - retrigger: ≥12 identical (same fingerprint) → the NEW incident;
   - DROP the 10-min sustain phase (its P-059 interpretation-continuity target is ungradeable under det-L4;
     it alone puts the phase at the `max_sustained_storm_ms` bound);
   - RETIRE all three `[[expected]]` checks declare-only, each with its measurement in the TOML header:
     `Contains "Resolved"` (active-only list + lowercase status labels), `CountAtLeast "2"` (grades
     `evidence_count` = Σ `span_refs`, which the incident producer writes as `Vec::new()` — observed is
     always "0"), `Contains "resolution summary"` (no producer at HEAD `efabe8e`).
3. **Re-shape the three tier TOMLs** (`severity-tier-autonomous/-suggested/-curious.toml`) onto the proven
   converged-baseline + spike recipe (2026-08-18/2026-08-20 precedent), each spike targeting its cue band
   derived from SUT constants: autonomous ≥5× + confidence ≥0.9 (conf = samples/100 ⇒ ≥90 error samples in
   the window), suggested ≥3×, curious <3× (the 2026-08-20 leg measured 2.895 → curious). RETIRE each
   `Contains` tier-token check declare-only with the fixture-pinned-severity measurement in the header. Keep
   `p_ids`, seeds, and the closed-set `slo_tier` declarations (placeholder-over-literal if any tier moves,
   per the layouts/design amendment precedent).
4. **Retire `ack-cooldown.toml`'s check declare-only** with the measurement header: no ack tool exists in the
   four-tool contract (the TOML's own Q4-era Q2 note), the five phases declare no emission (the
   `PhaseSpec.emission` default emits one plain trace each — nothing forms), and `CountAtLeast` grades
   span_refs-always-0. Keep the phase structure verbatim as drive documentation.
5. **New harvest test binary `crates/conductor-run/tests/severity_harvest.rs`** (own binary per test-plan §11
   Integration singleton ban; five precedents): pins over the leg-captured verbatim Pulse lines —
   - the auto-resolve witness: `triage.incident.auto_resolve.tick` with `resolved_count ≥ 1` + the lifecycle
     event line (to_state = Resolved), and the timing bound (resolve instant − last re-emission instant
     within 120s + 30s + margin) asserted hard pass/fail;
   - the new-not-reopen witness: two distinct incident-created outcomes for the same fingerprint, the second
     opened after the first's resolution;
   - the tier-ladder witness: cue lines with `priority` per tier leg, each scenario's measured band recorded;
   - the resolution-summary ABSENCE witness: the leg window carries no attach/resolution-summary line
     (asserted over the captured slice, with the structural grounds cited in the test doc).
6. **Live legs** (operator-gated, fresh `%TEMP%/pulse-legs/<n>` data dir each, sidecar built + on PATH,
   pulse-app launched from a cwd outside this repo, Pulse's `{data_dir}/logs/agent-latest.jsonl.<date>`
   harvested post-leg and sliced by pre-leg line count):
   - Leg A — `incident-auto-resolution` (the headline): green preflight → scenario run → row lands
     `verdict:null` / `KnownResidual` (declare-only) → harvest the auto-resolve + new-not-reopen lines.
   - Legs B/C/D — the three tier scenarios: each records its measured cue band (or measured absence — also a
     result); rows land KnownResidual.
   - Leg E — `ack-cooldown`: the declare-only row's live landing recorded.
   - Leg A addendum (bounded measurement, NOT acceptance-load-bearing): after the scenario row is recorded,
     drive `mark_incident_resolved` against every remaining active incident via the leg script (the
     registered lifecycle write's FIRST live exercise; tests §5 names it for CP4), then observe
     `query_incident_list` twice ~60-90s apart — measuring (a) the write lands (list empties), (b) whether
     the pulse-app persist loop resurrects the sidecar-resolved rows or a Tier-3-digest cue re-creates a
     canary-service incident. Record the outcome either way; the `AutoResolved` routing arm's live firing is
     structurally unreachable at current identities (the preflight canary precedes every scenario read-back
     and its frozen EWMA never lets its incident idle out) unless this measurement surprises — the
     2026-08-20 honest-limit precedent stands otherwise.
7. **Evidence** — `evidence/leg-verdict.md` + captured leg slices under the chunk folder; envelope rows
   quoted from `runs/<run_id>.jsonl` + `runs.db`; redaction-clean (no host paths / struct names).
8. **Gates** — workspace nextest (ci profile) + `cargo test -p conductor-run` (runner portability) + clippy +
   doctests green, zero retries; the new harvest binary green under both runners.

## Codebase touchpoints
### New files
- `crates/conductor-run/tests/severity_harvest.rs` — the family's harvest-tier pins (own test binary).
- `conductor-0.2.0/chunks/2026-08-21-severity-lifecycle-live-proof/evidence/leg-verdict.md` (+ leg slices).
### Files to modify
- `scenarios/incident-auto-resolution.toml` — re-shape + measured declare-only retirement (step 2).
- `scenarios/severity-tier-autonomous.toml` / `-suggested.toml` / `-curious.toml` — re-shape + retirement (step 3).
- `scenarios/ack-cooldown.toml` — measured declare-only retirement, phases kept (step 4).
- `conductor-0.2.0/verification-matrix.json` — v2-16 claim (P5 writes the link; wrap flips status on the leg evidence).

## Test Commands
```bash
# PREREQ probe (step 1; record exits + first diagnostic line)
cargo audit; cargo deny check advisories bans licenses sources

# CI-visible gates
cargo nextest run --workspace --profile ci
cargo test -p conductor-run
cargo clippy --workspace --all-targets -- -D warnings
bash scripts/agent-run.sh run            # the bundled release-gate path (nextest + doctest + clippy)

# Live legs (operator-gated; fresh data dir per leg; sidecar built + on PATH; SEED unset — TOML seed governs)
ANDROMEDA_PULSE_L4_DETERMINISTIC=true ANDROMEDA_PULSE_MCP_ENABLED=true \
  ANDROMEDA_PULSE_DATA_DIR="$LEG_DIR" SCENARIO=incident-auto-resolution bash scripts/agent-run.sh run
ANDROMEDA_PULSE_L4_DETERMINISTIC=true ANDROMEDA_PULSE_MCP_ENABLED=true \
  ANDROMEDA_PULSE_DATA_DIR="$LEG_DIR" SCENARIO=severity-tier-autonomous bash scripts/agent-run.sh run
ANDROMEDA_PULSE_L4_DETERMINISTIC=true ANDROMEDA_PULSE_MCP_ENABLED=true \
  ANDROMEDA_PULSE_DATA_DIR="$LEG_DIR" SCENARIO=severity-tier-suggested bash scripts/agent-run.sh run
ANDROMEDA_PULSE_L4_DETERMINISTIC=true ANDROMEDA_PULSE_MCP_ENABLED=true \
  ANDROMEDA_PULSE_DATA_DIR="$LEG_DIR" SCENARIO=severity-tier-curious bash scripts/agent-run.sh run
ANDROMEDA_PULSE_L4_DETERMINISTIC=true ANDROMEDA_PULSE_MCP_ENABLED=true \
  ANDROMEDA_PULSE_DATA_DIR="$LEG_DIR" SCENARIO=ack-cooldown bash scripts/agent-run.sh run
# (each leg: mint-then-read — conclude only from THIS run's run_id artifacts, never a bare `status`;
#  RUST_LOG=info,conductor_emit=debug belongs on a boot-only diagnostic leg, never on a suite-running invocation)
```

## Acceptance Criteria
- (tests) Every retired check lands declare-only WITH its measurement in the scenario TOML header; each
  family row lands `verdict:null` / `state="KnownResidual"` (non-Blocked) and the run exits 0
  (per test-plan §6 Cleanup parenthetical + §11 E2E reported-state ban); cites `verification-matrix.json#v2-16`.
- (tests) The live claims are graded at the HARVEST tier in `crates/conductor-run/tests/severity_harvest.rs`
  (own binary), green under BOTH `cargo nextest run --workspace --profile ci` and
  `cargo test -p conductor-run`, zero retries (per test-plan §4 runner-portability + §10 Zero-flakiness +
  §11 Integration singleton ban).
- (tests/arch) The auto-resolve witness is asserted HARD pass/fail on Pulse's own ledger lines — transition
  within the 120s no-reemission window + 30s tick bound — and the new-not-reopen witness pins two distinct
  incidents for one fingerprint; severity choice is graded at the cue tier and NEVER hard-failed on exact
  values (per test-plan §6/§1 CP4 + architecture §Probabilistic-Assertion Policy).
- (arch) Zero new crates, zero new cross-seam `Cargo.toml` edges, zero new occupied resources; consumed MCP
  tools stay within the registered four — `mark_incident_resolved`'s first live exercise uses the shipped
  client method (per architecture §Module Boundaries + §Occupied Resources).
- (arch) Every live row conforms to the run-report envelope: `state` in the closed five, declare-only
  `verdict:null`, `slo_tier` from the closed tier enum, latency journal-relative from `std::time`
  (per architecture §Standard Contracts + §Timing-Tolerance Model).
- (security) The PREREQ probe runs BOTH gates and records exits; auto-satisfy only on the byte-exact
  signature; any deviation restores the full form (per security-plan §Dependency Security).
- (security) No `#[garde(skip)]` on any nested spec field; every retained emission block validates under its
  shape's rules; every non-pass leg outcome is a typed value or a named-precondition `Blocked` — never the
  generic corpus-empty string, never a false pass-as-empty (per security-plan §Input Validation +
  §Security Anti-Patterns → Universal).
- (security/obs) Committed evidence (TOML headers, harvest pins, leg-verdict, journal/runs.db quotes) is
  redaction-clean: no absolute host paths, no internal struct/field names (per security-plan §Error Handling +
  obs-plan §11 Logs/PII Scrubbing).
- (obs) The leg's read-back boundary log carries tool name + latency + result key-set for every tool called,
  including `mark_incident_resolved`'s first live call; obs-plan §4 CP4's never-built span chain is recorded
  as measured-absent with the Expected amendment noted — never a vacuous green (per obs-plan §6 Boundary-call
  wrappers + §4 Severity-lifecycle scenario + §11 Spans).
- (design/layouts/a11y) Every outcome renders inside the closed six lamp/bracket set with its always-rendered
  text label — no new lamp, token, ANSI entry, or bracket label; declare-only rows render `[RESIDUAL]`-class
  with the measurement shown, never Fail-red, never silent green; any `slo_tier` cell reads the declared
  closed-set value (per design-system §Surface: cli Tokens + §Color Palette; layout-templates §cli Component
  — Primary content block 2; a11y-plan §6 State color tokens).
- **Affordance criterion:** N/A — this chunk ships no user-operated control (CLI/headless legs only; the
  operator-checklist hold resolves headless by design) — per `verification-matrix-contract.md` §Affordance honesty.

## Provenance
- Spec anchors: test-plan §1 CP4 / §3 / §4 / §6 / §9 / §10 / §11 · obs-plan §1 / §3 / §4 / §6 / §11 ·
  security-plan §Dependency Security / §Input Validation / §Error Handling / §Security Anti-Patterns ·
  architecture §Established Decisions [Read-Back Dependency Posture] [Probabilistic-Assertion Policy]
  [Accepted Capability Set] [Timing-Tolerance Model] / §Standard Contracts / §Occupied Resources ·
  design-system §Surface: cli / §Color Palette · layout-templates §cli Components / §Primary screens ·
  a11y-plan §1 / §3 / §6 / §10 / §11.
- Research files inspected: see `research.md` §Files inspected (14 reads: 7 Conductor + 7 Pulse @ HEAD `efabe8e`).
- Leaned decisions (decisive material leans, per artifact): re-shape over leg-as-shipped for the four
  stimulus TOMLs (the shipped shapes sit under the measured SUT floors/bands — 2026-08-15 canary lesson +
  2026-08-18 recipe + coordinator/evaluator constants); drop the sustain-10min phase (its P-059 target is
  ungradeable under det-L4 and it alone rides the envelope bound); keep ack-cooldown's phases verbatim as
  drive documentation (connection-family precedent); mark_incident_resolved exercised as a bounded leg
  measurement (tests §5 names it for CP4), never an acceptance dependency.

## Constraints & rejected approaches
- REJECTED: extending `observe()` to remember/query resolved incident ids (a second read-back pass) — new
  production plumbing to chase deterministic-L4 constants, against the six-family retirement precedent; the
  resolved incident's absence + the retrigger's presence carry the read-back corroboration.
- REJECTED: grading auto-resolve via a `Contains "resolved"` lowercase re-word — the resolved incident LEAVES
  the active-only list, so no token can carry it; absence is the observable.
- REJECTED: making the `AutoResolved` arm's live firing an acceptance clause — structurally unreachable at
  current identities (preflight canary inside every leg + frozen canary EWMA); it stays a bounded measurement
  with the 2026-08-20 honest-limit precedent.
- REJECTED: `deny.toml` ignore / tool-floor raise / CI edit for the red audit — the advisory-DATABASE fault
  remedy stays the bounded wait (security-plan §Dependency Security).
- REJECTED: direct `corpus.db` reads to observe resolved rows — MCP read-back ONLY (security-plan §Data
  Protection); the harvest tier covers what read-back cannot.
- DO-NOT: no new env vars, no `CONDUCTOR_*` override for contracts, never SET `ANDROMEDA_PULSE_*` from
  Conductor code (leg SCRIPTS export them — that is the operator's shell, the run-contract's
  shell-declaration proxy); no OTel SDK / self-obs OTLP; no `sleep(N)`-style sync in tests — wait on
  envelope/journal/exit signals; no `info` logging in per-transition inner loops.
- DO-NOT: never read `tracked_fingerprints_count` as absence evidence; discriminate storms via the cumulative
  counters and tier via `severity_hint`/`priority` labels (arch §Occupied Resources).

## Implementation notes
- **Leg recipe (accumulated)**: sidecar built + on PATH (check FIRST — an unbuilt sidecar blocks preflight
  and the canary never emits, so a missing wire witness means the leg never ran); `ANDROMEDA_PULSE_MCP_ENABLED`
  in Conductor's OWN environment; pulse-app launched outside this repo (Specta bindings fallout); pulse-app is
  GUI-subsystem — harvest `{data_dir}/logs/agent-latest.jsonl.<date>` post-leg, sliced by pre-leg line count;
  no Windows-form `D:/…` PATH entries in the leg shell (the drive-letter-colon split resolves the sidecar
  nowhere and fakes a 0s `[BLOCKED]`); a fast `[BLOCKED]` must be disambiguated before being read as a
  lifecycle finding.
- **Timing**: Leg A ≈ 4-6 min post-re-shape (trigger + dilution + 150s wait + retrigger + read-back); every
  emitting phase stays far under `max_sustained_storm_ms=600000` and the trivial rate bound; `slo_tier` stays
  `<90s` — the declare-only row records its real latency (the 2026-08-20 373784ms precedent shows the shape).
- **ReflectionTrend hazard**: a workspace-global ReflectionTrend incident (config-cadenced) may appear inside
  a leg and joins the active list — read leg lists with identity awareness (kind/scope), never bare counts.
- **Expected amendments (wrap):** test-plan §6 Severity-lifecycle scenario + §1 CP4 twin — re-base onto the
  harvest tier + retire the stale `conductor run severity-lifecycle --seed <s>` step (no such scenario name;
  seed-precedence form) [two-site rule]; obs-plan §4 CP4 + §1 row + §6 extras (`lifecycle_phase` /
  `severity_choice_calibrated`) — re-base the never-built family chain onto shipped instrumentation with
  measured constraints (restart-suppression precedent, four sites); arch §Standard Contracts — the
  declare-only families list grows by this family (+ §Read-Back Posture AutoResolved-arm status if the
  measurement surprises); layout-templates — verify + correct the P-022 sample-row label ("port-occupier /
  Blocked" vs coverage.rs "Auto-Resolution and Lifecycle"; P-003 is the port-occupier).
- **Matrix at wrap**: v2-16 flips on the leg evidence; the P-059 clause carries the PREMISE-CORRECTION
  narrative in `notes` (the v2-14 phase-time ratification precedent — operator ratifies at P5 via the
  concretization preview below).
