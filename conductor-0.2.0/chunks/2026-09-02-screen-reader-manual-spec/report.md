# Report — 2026-09-02-screen-reader-manual-spec

**Chunk:** Screen-reader manual spec — per-state NVDA must-announce pass spec over the four shipped run states, with one operator pass recorded against it (conductor-tauri/ui, v2-23)
**Date:** 2026-09-02T12:00:00Z
**Commits:** none since `last_wrap` (`76550a4` is the previous wrap's own commit); every change below is uncommitted and lands with this wrap's commit.

## Changes (structured — detectors read this)
- **Files:**
  - Modified: `crates/conductor-tauri/ui/wdio.conf.ts` (+173/−15) · `crates/conductor-tauri/ui/package.json` (3 script lines) · `crates/conductor-tauri/ui/test/README.md` (one pointer paragraph) · `conductor-0.2.0/verification-matrix.json` (v2-23 ledger: `chunk` by phase, `ref` + `status: implemented` by implement, notes) · `conductor-0.2.0/working-route.md` (the phase stamp on line 87) · `.andromeda/master-route.md` (the phase's `pending` record).
  - New: `crates/conductor-tauri/ui/test/a11y/screen-reader.e2e.ts` (the SR spec, 25 KB) · `crates/conductor-tauri/ui/test/a11y/screen-reader/nvda-pass-spec.md` (the per-state pass spec) · `…/screen-reader/rows.ts` (the 51-row table shared by spec and parser) · `…/screen-reader/parse-nvda-log.ts` (speech log + action timeline → the pass record; node built-ins only) · `…/screen-reader/activate-window.ps1` (window activation: `FindWindow` with an `IntPtr` class arg, `Get-Process` fallback, synthetic ALT via `keybd_event`, `ShowWindow 9`, `SetForegroundWindow`; exit 0 / 3 not-found / 4 not-foreground) · `…/screen-reader/nvda-config/nvda.ini` (the leg-owned NVDA profile: welcome dialog off, `silence` synth, auto language switching off, auto say-all off) · `conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/{scope,research,plan}.md` · `…/evidence/nvda-pass.json` (51 rows + `operator_review`) · `…/evidence/leg-verdict.md`.
  - NOT touched (measured by `git diff --stat`): any `.rs`, any `.tsx` / `.css`, `Cargo.toml` / `Cargo.lock`, `package-lock.json`, `scripts/agent-run.{sh,ps1}`, `.github/`, `test/a11y/accessibility.e2e.ts`, `test/a11y/operator-hold.e2e.ts`, the routine default `specs: ['./test/a11y/accessibility.e2e.ts']`, any `.andromeda/` master, any `contracts/` manifest.
  - Staged under gitignored `runs/` (never committed): `runs/sr-leg/{scenarios/,runs/,empty/,bad/broken.toml,nvda-config/,subject.txt,census-before.<subject>.txt,nvda-speech.<subject>.log,actions.<subject>.jsonl,nvda-pass.json,logs/}` · `runs/driven/runs/`.
- **Symbols / APIs:**
  - `wdio.conf.ts` — new `suites.sr` / `suites['sr-empty']` / `suites['sr-error']` (all → `./test/a11y/screen-reader.e2e.ts`); `SR_SUITES` (subject · runs dir · optional scenarios dir per suite); `SR_LEG_DIR = 'runs/sr-leg'`; `DRIVEN_RUNS_DIR = 'runs/driven/runs'`; `invokedSuites(config)` (reads `suite` reflectively off the hook's config + the launcher argv); `nvdaExe()` (the `CONDUCTOR_NVDA` guard: unset / `UNSAFE_PATH` metacharacter / not an existing file ⇒ `undefined`) + `reportNvdaSkip()` (names the handle, never a value; `process.exit(0)`); `startNvda(exe, logPath)` (array-form detached spawn `[-m, --no-sr-flag, -c <configDir>, -l 12, -f <log>]`; readiness = the log's `NVDA initialized` line within 30 s, then a 1.5 s quiet settle ≤ 8 s; copies the committed `nvda.ini` into `runs/sr-leg/nvda-config/`); `stopNvda(exe)` (`[-q]` then a `tasklist` census poll ≤ 15 s); `onPrepare` is now `async (config)` and chooses the app env PER SUITE at the ONE tauri-driver spawn site (routine ⇒ `CONDUCTOR_RUNS_DIR=runs/e2e-fixture`; `driven` ⇒ `runs/driven/runs`; `sr` ⇒ `runs/sr-leg/runs`; `sr-empty` ⇒ the fixture dir + `CONDUCTOR_SCENARIOS_DIR=runs/sr-leg/empty`; `sr-error` ⇒ `runs/sr-leg/runs` + `CONDUCTOR_SCENARIOS_DIR=runs/sr-leg/bad`), writes `subject.txt` + `census-before.<subject>.txt`, starts NVDA BEFORE the driver; `onComplete` is now `async`: `stopNvda` → `tauriDriver.kill()` → `writeNvdaPass(...)`; `seedFixtureRuns()` now `rmSync` + `mkdirSync` before the copy (a clean fixture dir per session). Unchanged and still the sole callers: `nativeDriver()` / `reportSkip()` / the `before` mount wait / the capabilities block; `seedFixtureRuns` keeps its one caller (`onPrepare`); the tauri-driver spawn stays the ONE site.
  - `package.json` — scripts `a11y:sr` / `a11y:sr-empty` / `a11y:sr-error` = `wdio run wdio.conf.ts --suite <name>` (the `a11y*` script set 2 → 5).
  - `parse-nvda-log.ts` — exports `readSpeechLog` · `readStamps` · `grade` · `writeNvdaPass` · `census`; types `PassRow` (with `review_grade?`) · `OperatorReview` · `ReviewGrade` · `Timeline`; CLI `tsx parse-nvda-log.ts <speech-log> <actions.jsonl> <out.json> [--subject]`.
  - `rows.ts` — `type Subject = 'live' | 'empty' | 'error'`; `interface SpecRow`; `ROWS` (51: live S0-01…S0-16 · S1-01…S1-05 · S2-01…S2-08 · S3-01…S3-07 · T-01 = 37; empty E0-01…E0-10; error R0-01…R0-04); `rowsFor(subject)`.
  - `screen-reader.e2e.ts` — reads the subject from `runs/sr-leg/subject.txt`; stamps every row into `runs/sr-leg/actions.<subject>.jsonl` plus the timeline records `@session` (browserVersion) · `@foreground` (activation result, NVDA named-window, initial focus, `tabsToStart`) · `@reactivate` · `@probe` · `@end`; invokes `activate-window.ps1` through `spawnSync('powershell.exe', ['-NoProfile','-ExecutionPolicy','Bypass','-File', script, '-Title', WINDOW_TITLE])` — array-form, fixed args, no operator input.
  - Env handles: NEW `CONDUCTOR_NVDA` — the portable NVDA's path, a HOST dev-tool handle read ONLY by `wdio.conf.ts` (no shipped binary reads it), validated like `CONDUCTOR_MSEDGEDRIVER` (existence + `isFile` + metacharacter rejection), passed as a separate argv element of an array-form spawn, never through a shell; unset or not-a-file ⇒ the `sr*` suites SKIP at exit 0 with a fetch recipe (measured 2026-09-02 11:53Z: `env -u CONDUCTOR_NVDA npm run a11y:sr-empty` → exit 0, the `error:`/`hint:` lines, no process started). READ (existing): `CONDUCTOR_MSEDGEDRIVER` (unchanged guard), `CONDUCTOR_RUNS_DIR` and `CONDUCTOR_SCENARIOS_DIR` (now SET by the config per suite as above; the live `sr` subject's trimmed catalog comes from the shell), `ANDROMEDA_PULSE_*` (the driven arm's live form, shell-declared). No `SR_SUBJECT` env (the plan named one — see Reverted).
  - Ports / IPC / MCP / commands: none added or changed (4444/4445 as before, harness-lifetime; no `#[tauri::command]`, no MCP tool, no OTLP change).
- **Crates / modules:** none added, removed or changed in the Cargo workspace (zero `.rs` delta). The ui test tree gains the `test/a11y/screen-reader/` subdirectory.
- **Dependencies:** none — `package-lock.json` and `Cargo.lock` byte-unchanged (gate 3). NVDA 2026.2 is a host tool arriving by env handle, not a dependency; `@guidepup/guidepup` was rejected (zero-dependency form chosen; route candidate).
- **Schema / config:**
  - The evidence record `nvda-pass.json` (Conductor's own artifact, inside the redaction boundary): identity block `spec` · `recorded_at` · `nvda_version` (from the log's own header) · `webview2_runtime` (the session's `browserVersion`) · `webview2_runtime_registry` (the EdgeUpdate key) · `build_commit` · `bundle_build` · `stimulus_runs {fixture_run_id, live_run_id}` · `attach_observed {live, empty, error}` · `subjects{<subject>: recorded_at, census_before/after, foreground, pre/post-session utterance counts}` · `rows[]` (`id` · `subject` · `state` · `class` ∈ {focus, live, browse} · `item` · `node` · `sc` · `expected` · `tokens` · `arm` ∈ {agent, operator} · `outcome` ∈ {announced-as-expected, announced-differently, not-announced, not-run-here, subject-absent} · `heard` (≤ 400 chars) · `note?` · `security_finding?` · `review_grade?` ∈ {accepted-as-heard, finding, accepted-with-reason}) · `findings[]` · `operator_review {reviewed_at, reviewer, verdict, basis[], grades{…}, notes}`.
  - Scrub shape: any drive-letter / `%APPDATA%` / `/Users` / `/home` token in an utterance is replaced by `<host-path>` and the row gains a `security_finding` string (one row, S1-01, carries it: the sidecar console pane's title — a foreign window, not Conductor text).
  - The action timeline `runs/sr-leg/actions.<subject>.jsonl` (gitignored; `{ts, id, state, cls, item, tokens}` + the `@…` records); the parser assigns utterances by time window (stamps ≤ 50 ms apart share one window; `@end` clips the last).
  - The leg-owned NVDA profile `nvda.ini` (committed) copied per session into `runs/sr-leg/nvda-config/`.
  - No migration, no `runs.db` change, no violation-schema change, no obs log-schema change.
- **Spec-master edits:** none by this chunk (implement authored nothing; wrap's P2 applies the amendments listed under Spec claims disproved + the plan's Expected amendments + the operator's directive item 3).
- **Counts / qualifiers moved:**
  - Webview suite FAMILIES over the one WebdriverIO + tauri-driver stack: 2 (routine `--e2e` arm · driven arm) → 3 (+ the `sr*` screen-reader leg, three suites); wdio `suites` entries 1 → 4; npm `a11y*` scripts 2 → 5. Docs stating TWO: architecture §Cross-cutting Patterns Scope law ("ships as TWO suites over ONE … stack"), §Occupied Resources Ports ("shared by BOTH arms"), CLAUDE.md Critical Warnings, `.claude/rules/a11y.md` §Testing ("TWO suites over that ONE stack"), a11y-plan §9 (line 465 "TWO arms"), test-plan §6.
  - Host dev-tool handles in the `CONDUCTOR_*` namespace: 1 → 2 (`CONDUCTOR_MSEDGEDRIVER` + `CONDUCTOR_NVDA`). Docs stating ONE: architecture §Occupied Resources env vars ("The one handle in this namespace naming a HOST dev-tool"); security-plan §Input Validation `CONDUCTOR_MSEDGEDRIVER` row ("is the exception: no Rust code reads it").
  - The driven arm's `CONDUCTOR_RUNS_DIR`: the fixture dir `runs/e2e-fixture` → `runs/driven/runs` (set by the config; before this chunk the spawn forced the fixture dir on EVERY suite). Consequence for the self-obs sink (`runs_dir.parent()/logs`): the `sr` / `sr-error` sessions land `runs/sr-leg/logs/conductor-tauri.jsonl` — MEASURED (8312 B, written 10:44:45Z by the `sr-error` session); the `sr-empty` session and the routine arm share `runs/logs/conductor-tauri.jsonl` (measured 10:46:10Z, the routine re-run); the driven arm's landing site becomes `runs/driven/logs/conductor-tauri.jsonl` — DERIVED at authoring (the driven arm is re-running at wrap; see Outcome). Doc stating the OLD value: architecture §Occupied Resources `logs/conductor-tauri.jsonl` bullet ("the operator-local `a11y:driven` arm leaves the handle unset and lands at the workspace-root `logs/conductor-tauri.jsonl`").
  - The post-session fixture dir (`runs/e2e-fixture`) holds the committed `lamps-fixture.jsonl` PLUS a `runs.db` the app's own storage bootstrap creates on open (28 KB, zero tables) — never a foreign run journal; each session starts from exactly the committed journal (rm + copy, measured 2026-09-02).
  - The a11y-plan's run-state set (idle / live / HOLD / report-terminal) vs shipped (`idle` / `live` / `hold` / `aborted`, with idle-with-report a sub-state) — 4 → 4 but a different SET.
  - Spec rows 51 (37 live · 10 empty · 4 error); outcomes 33 announced-as-expected · 1 announced-differently · 14 not-run-here (browse class, findings) · 2 subject-absent · 1 not-run-here by design; operator review 33 accepted-as-heard · 15 findings (S0-16 + 14 browse rows) · 3 accepted-with-reason.
- **Dev-tool versions:** NVDA 2026.2 portable (host tool; present on this host before the leg, named by `CONDUCTOR_NVDA`); WebView2 Runtime 151.0.4129.107 → 152.0.4191.53 (a background Windows update during the day — not the chunk's doing; both the registry key and the session's `browserVersion` read 152 on the final sessions); msedgedriver 151.0.4129.101 unchanged — a MEASURED cross-major pair (driver 151 against runtime 152) that worked on all three final `sr*` sessions and the routine `--e2e` re-run, officially unsupported; refreshing the driver is the operator's host task BEFORE the next leg, never inside this wrap (operator directive item 3).
- **Harness / gate surface:** the three `sr*` suites (operator-local, Windows/NVDA-only by nature, never a CI stage, never an `agent-run` verb; the `sr` subject fires exactly one preflight canary, `sr-empty` / `sr-error` none); the `CONDUCTOR_NVDA` skip-at-exit-0 guard; NVDA start/quit bracketing tauri-driver; per-suite subject env at the one spawn site + the clean fixture re-seed (the routine arm's spec set unchanged, its subject now guaranteed clean per session); the driven arm's own runs dir; the evidence record's closed outcome set + per-row arm + the three-value review grade; the process census recorded per subject inside the record (`nvda.exe` / `conductor-tauri.exe` / `msedgedriver.exe` / `andromeda-pulse-mcp.exe` terminated by the leg; `pulse-app.exe` left running — operator stops it). Platform verdicts stated: VoiceOver (macOS) / Orca (Linux) declared not-runnable-on-this-host in one spec section, never as rows.
- **Cross-project / external claims:**
  - NVDA 2026.2 (the tool itself, measured on this host): `-m --no-sr-flag -c <dir> -l 12 -f <log>` writes every utterance as `Speaking [...]` lines; `-q` quits; the `NVDA initialized` log line is the readiness point (a window created between `Starting NVDA` and it is never bound); NVDA follows the OS FOREGROUND, binds a window on its first focus event, and its browse-mode commands are reached only through its OS keyboard hook — WebDriver-injected keys never deliver them (measured on every session: injected `h` / `d` / arrows produced no speech while the same session's focus events did). Basis: the speech logs under `runs/sr-leg/` + `evidence/nvda-pass.json` rows.
  - WebView2 / msedgedriver: the cross-major pair above (basis: the session `browserVersion` + the EdgeUpdate registry key + `msedgedriver --version`).
  - WebdriverIO 9: `--suite` is merged onto the runtime config but is absent from the typed `Testrunner` surface (basis: `tsc -p test/tsconfig.json` rejected `config.suite`; `Reflect.get` shipped).
  - Pulse (SUT): no new SUT claim; the driven arm's live form (fresh data dir under `%TEMP%\pulse-legs`, deterministic L4, MCP enabled, the sidecar on `PATH`) is the operator's shell declaration.
  - Conductor-side, outside this chunk's files: `crates/conductor-verify/src/spawn.rs:80` builds the sidecar `Command::new(PULSE_MCP_PROGRAM)` with NO creation flags, so starting a run from the GUI raises a Windows Terminal pane titled with the sidecar's exe path that takes the OS foreground ~200 ms after `Start` (measured: NVDA announced `Terminal`, the path, `pane` inside the S1-01 window) — a product DEFECT (`CREATE_NO_WINDOW` class), routed as a route candidate (directive item 1), not fixed here.
- **Reverted / negative API facts:** the plan's `SR_SUBJECT` env for the spec — not shipped; the config writes `runs/sr-leg/subject.txt` and the spec reads it (the app env and the worker env are different processes; a file is the one handle both sides see). The plan's readiness signal "the log file appearing" — replaced by `NVDA initialized` + settle (the file appears before NVDA can bind a window; measured: a whole subject went silent). One shared speech log / actions file — replaced by per-subject files (`nvda-speech.<subject>.log`, `actions.<subject>.jsonl`) so a session never reads another's speech. `blur()` and a programmatic selection collapse to move the focus start off the picker — tried, no effect, removed; the Tab-cycle to the host-chrome stop (`BODY`) shipped. Silent browse rows first graded `not-announced` — reverted to `not-run-here` (arm `agent`, a finding, per the operator's review). The `@end` stamp moved from the walk's tail into the `after` hooks so a failing walk still closes its timeline. The plan's step-1 two-scenario catalog — replaced by three (a third, never-executed `high-severity-log-capture.toml` copy), because `Stop` during the LAST scenario ends `Done` → `idle`, never `Aborted`.
- **Spec claims disproved by measurement:**
  1. a11y-plan §1 (line 113 "Screen reader test pattern: Manual, supplemental … No automated SR tool for the stack") + §3 Screen reader test pattern (line 268 "No automated SR tool exists for the stack"; §3 Bootstrap line 298 "manual pass-spec files") — FALSE: NVDA's own speech log (`-l 12`) plus the existing WebdriverIO + tauri-driver stack drove 33 of 51 rows agent-driven with zero new dependencies (`evidence/nvda-pass.json`: `arm: agent`, `attach_observed` true for all three subjects; the operator's role is REVIEW), and `@guidepup/guidepup` 0.34.0 exists on npm (research.md). What remains manual-class is the 14 browse-mode rows, pending OS-level key injection (CARRY) — never a "no tool exists" verdict. Operator's wording for the pattern: "agent-driven via NVDA's speech log; operator reviews; browse-mode rows pending OS-level injection".
  2. a11y-plan §1 / §3 / §5 (lines 66, 135, 269, 356) four-state naming `idle / live / HOLD / report-terminal` — the shipped `RunState` set is `idle` · `live` · `hold` · `aborted` (`Titlebar.tsx`); "report-terminal" is idle with a report (research.md §Scope premise closure 1; the `sr-empty` subject rendered it; S3 rows heard `Conductor · aborted`).
  3. a11y-plan §3 must-announce prose (line 268/269): "No scenarios loaded" → shipped `No scenarios found.` (heard, E0 rows); "No run yet — pick a scenario/suite to begin" → shipped `No run yet`; "Run in progress" — no such prose ships (the live state carries only the titlebar label `Conductor · live` + the count; S1 rows). Design-system §Component Patterns 6 / 7 and layout-templates §Primary content block 2 restate the same strings.
  4. a11y-plan §4 landmark inventory (line 320) + §11 (line 546) + layout-templates §Component — Footer: a `contentinfo` footer status strip — ABSENT from the shipped DOM (recorded as a spec row; the landmark list is banner + main + two regions). The operator checklist's second (report-site) render site (a11y-plan §4 / layout-templates §Component — Operator-checklist) — does NOT ship in release (only the dialog site; row S2-08 `subject-absent` sibling recorded; S1-05 / E0-10 `subject-absent` are the count live-region and the envelope banner respectively).
  5. The dialog title shape: shipped `{p_id} — operator-checklist` with description `Observe the operator-checklist claim for this scenario` (heard, S2 rows) where the plans' narrative names it differently.
  6. Chunk-internal (plan.md) claims measured false, recorded for the ledger: hold order in the trimmed catalog is sorted-filename order (P-026 `halo-breathing-encoding` BEFORE P-025 `halo-hue-encoding`), not hue-first; two scenarios do NOT suffice for `aborted` (`drive_run` polls `should_abort` only before a scenario starts, `crates/conductor-run/src/lib.rs:936`); NVDA attaches to the driven WebView2 window only after the leg brings it to the OS foreground and after `NVDA initialized` (the plan's open attach question, answered YES with those two conditions); `Stop` during the last scenario reads `aborted` client-side then settles to `idle` (the backend records `Done`) — a product semantic inconsistency, a finding.
  7. architecture §Occupied Resources `logs/conductor-tauri.jsonl` bullet: "the operator-local `a11y:driven` arm leaves the handle unset and lands at the workspace-root `logs/`" — retired by construction (the config now sets `runs/driven/runs` for the driven suite); the `sr` leg's landing site measured (`runs/sr-leg/logs/`), the driven arm's re-measured at this wrap's light gate (see Outcome).
  8. `D-platform-claim` scope: the SR leg is Windows-only by NVDA's nature; no macOS/Linux SR pass is runnable here (declared, not a claimed pass). The ui test README's "runs only on Linux + xvfb" sentence is a pre-existing non-master falsehood — a route-resolve CARRY (owner: the Epoch-6 _A11y CI gate_), not this wrap's amendment.
- **Coverage of new surfaces:**
  - `CONDUCTOR_NVDA` (env handle, wdio edge) → validation ✓ (existence + `isFile` + `UNSAFE_PATH` rejection; array-form spawn) · instrumentation n/a (harness, no shipped binary; console lines name the handle only) · PII ✓ (the value is never logged, echoed or committed) · tests e2e (the skip path measured exit 0 with recipe; the resolved path on 3/3 sessions) · a11y n/a · tokens n/a
  - `sr` / `sr-empty` / `sr-error` suites + `screen-reader.e2e.ts` → validation n/a · instrumentation n/a · PII ✓ (the record scrubs; gate 1 grep clean over all committed artifacts) · tests e2e (3/3 sessions passing 2026-09-02 10:41–10:44Z on WebView2 152.0.4191.53) · a11y ✓ (this IS the a11y evidence; supplemental to `v2-22`) · tokens n/a
  - `parse-nvda-log.ts` → validation ✓ (spec-id cross-check against `nvda-pass-spec.md`, closed outcome / arm sets asserted by gate 2) · instrumentation n/a · PII ✓ (host-path scrub → `<host-path>` + `security_finding`; heard truncated at 400 chars) · tests: gate 2 (record shape) + gate 7 (CLI re-run over a captured pair) — no unit-test file ✗ (node built-ins, exercised end-to-end only) · a11y n/a · tokens n/a
  - `activate-window.ps1` → validation ✓ (fixed argv, no operator input; `-NoProfile -ExecutionPolicy Bypass -File`) · instrumentation n/a · PII ✓ (window title only) · tests e2e (exit 0 on every session; the `@foreground` record) · a11y n/a · tokens n/a
  - `nvda-config/nvda.ini` → n/a across all flags (a data file; copied per session)
  - `seedFixtureRuns` clean re-seed + per-suite runs dirs → tests e2e (routine `--e2e` re-run twice after the change: 8 passing / 3 designed skips, exit 0; `sr` sessions persisted under `runs/sr-leg/runs`; the driven arm re-running at wrap) · other flags n/a
  - `evidence/nvda-pass.json` + `leg-verdict.md` → PII ✓ (gate 1: no drive letter / `%APPDATA%` / `/Users` / `/home` / struct name) · tests: gate 2 · other flags n/a

## Deviations from intent
- **`runs/sr-leg/subject.txt` instead of an `SR_SUBJECT` env** (plan step 3c) — the config sets the APP's env at the spawn site while the spec runs in wdio's worker; a file under the leg dir is the one handle both processes see, and it lands beside the other per-subject artifacts.
- **Readiness on `NVDA initialized` + a quiet settle, not on the log file appearing** (plan step 3b) — measured: the file exists before NVDA can bind a window, and a window created in that half-second was never announced (one whole subject silent).
- **Three new files the plan did not list** — `rows.ts` (the row table is read by both the spec and the parser; one source), `activate-window.ps1` (NVDA follows the OS foreground, which a tauri-driver-launched window never takes; measured on the first sessions, which logged Pulse's window) and `nvda-config/nvda.ini` (NVDA's defaults read the whole 83-row matrix as one utterance on load and spoke `English (not supported)` on every language change). All three are harness files under the same tree, zero dependencies.
- **A third scenario in the trimmed live catalog** (plan step 1 named two) — `drive_run` polls the abort flag only before a scenario starts; a `Stop` during the last scenario ends `Done` → `idle` (heard on the 10:31 session). A never-executed third copy makes the backend `Aborted` stage reachable after the stopped one.
- **Hold #1 is P-026 (`halo-breathing-encoding`), hold #2 is P-025** — sorted-filename order; the plan's narrative had hue first. Rows and spec corrected to the measured order.
- **Re-activation after `Start`** — the sidecar console pane steals the foreground; the leg re-activates the app window before the S1 focus rows (a leg workaround; the product defect is routed).
- **Browse rows are `not-run-here` findings on the AGENT arm with no manual arm this chunk** (the plan's fallback was the operator's manual pass per row) — the operator's review ruling: OS-level key injection makes them agent-driven; owned by the CARRY, never a manual arm and never a hard failure.
- **`operator_review` carries per-row `review_grade` + a `grades` map + a `basis[]`** beyond the plan's `reviewed_at` / `verdict` / `notes` — the operator's review was per-row, so the record carries it per row.
- **The pass ran three times to a green leg version** (the plan foresaw one live session) — the first sessions measured the foreground binding, the readiness point, the focus start and the console pane; only the final leg version's three sessions are the recorded evidence (one canary each `sr` session, each under the quiet window).
- **The driven arm was not in the plan's Test Commands but its spawn env CHANGED** (step 3d) — re-run at this wrap's light gate as a changed-surface gate, not deferred (a config change to a suite that never re-ran is the 2026-09-01 never-executed-config class).

## Decisions & corrections
- **Route ownership (operator, session start):** rewording a markerless working-route entry is NOT an ownerless direct edit — wrap's route-resolve owns the markerless tail; a recorded operator pre-direction satisfies the trajectory gate; a 0-pending wrap runs it as an operator-requested adaptation. "Touch nothing by hand."
- **The NVDA directive CONFIRMED as written, then re-shaped by relay:** the spec is the chunk's authored artifact, ONE recorded pass is its proof, the chunk HOLDS `pending` across the review seam — never wrap-with-owed. Re-shape: a screen reader's built-in speech log COUNTS AS A DRIVER — the pass is agent-driven with zero new dependencies and the operator's role becomes review (remote); the manual pass is the fallback per row, and the evidence records which ARM produced every row.
- **Guidepup:** rejected for this chunk (zero-dependency form suffices), surfaced as a ROUTE CANDIDATE at route-resolve — agent-driven is the standing default wherever a driver exists; natural owner the Epoch-6 _A11y CI gate_.
- **Operator finding at the second P5 card:** the spawn forced `runs/e2e-fixture` on EVERY suite and `seedFixtureRuns` never cleaned — a latent flake in the routine arm's lamps assertions; the driven suite gets its own runs dir and the re-seed starts clean (belt and braces).
- **Operator review grades (2026-09-02):** the 33 announced-as-expected rows accepted as heard; S0-16 a FINDING (initial-focus placement — focus lands in the picker input at load, so NVDA correctly announces the focused element; the spec's presentation-role expectation was written for an unfocused element; rides wrap as finding #2); the 14 browse-class rows `not-run-here` with the reason "browse-mode commands are unreachable by WebDriver-injected keys (measured every session); OS-level key injection (SendInput/SendKeys — NVDA hooks the OS keyboard) makes them agent-driven — owned by the CARRY in B.2" — findings, never passes, never hard failures, no manual arm this chunk; the 2 subject-absent + 1 not-driven-by-design rows accepted with their reasons. Transcribed verbatim; v2-23 flips to `verified` only once transcribed (done).
- **Process hygiene (standing):** NVDA is the leg's to stop (`-q`), Pulse is the operator's — every leg report ends with the census and "Pulse left running: operator stops it"; a pre/post `tasklist` census brackets every leg; host paths arrive by env handle and never enter a committed artifact.
- **WRAP directive (operator, four items):** (1) the nine a11y findings are DISPOSITIONED at route-resolve, not absorbed — the sidecar console foreground steal is a Conductor spawn DEFECT (`CREATE_NO_WINDOW` class), a route candidate of its own; the rest (initial focus in the picker · scroll-region reads the whole table · assertive flips cancelling focus-restore · the silent load-time alert · `aria-current` spoken as "current" · filter-miss prose silent · the unlabeled count · the Stop-then-idle announce) group into ONE route candidate "SR findings remediation" in Epoch 6 beside the _A11y CI gate_, each with its evidence row id; (2) two CARRYs on _A11y CI gate_: Guidepup as the agent-driven SR option (weighable, not adopted) and OS-level key injection for the 14 browse-mode rows (makes the manual arm zero), both citing `nvda-pass.json`'s per-row arm; (3) amendments beyond the plan's list: a11y-plan §3 SR pattern wording "agent-driven via NVDA's speech log; operator reviews; browse-mode rows pending OS-level injection"; test-plan §6 registers the `sr*` suites (firing form with `CONDUCTOR_NVDA`, the activation script, the leg-owned NVDA profile, the clean re-seed) and their stop form; WebView2 152.0.4191.53 against msedgedriver 151 recorded as a measured, unsupported-but-working cross-major pair, with the driver refresh as the operator's host task BEFORE the next leg (never inside this wrap); (4) coverage gate: v2-23 flips to `verified` only with `operator_review` non-null and the 14 not-run-here rows carrying their reason — hold otherwise.
- **Measured facts worth keeping (candidates for curation):** NVDA follows the OS foreground, not WebDriver's document focus, and binds a window on its first focus event after `NVDA initialized`; a PowerShell `$null` bound to a `string` P/Invoke parameter arrives as `""` (`FindWindow` needs an `IntPtr` class argument); the app's sequential-focus start sits at the picker input when the catalog is populated; `drive_run` polls the abort flag only before a scenario; a screen reader's own log is a driver.

## Outcome
- **Acceptance criteria:** all 11 met — the spec (51 rows over the shipped four states + the idle-with-report sub-state, every plan-vs-shipped gap a row), the evidence record with `operator_review` transcribed (`verification-matrix.json#v2-23`), the supplemental / no-conformance standing with VoiceOver / Orca declared not-runnable-here, the design / layouts rows as shipped, one new `CONDUCTOR_*` handle and no port / crate / MCP tool / second stack, gate-1 hygiene clean with the one HEARD host path recorded as a `security_finding` (a foreign window's title), the record parsing from a paste and correlating the live rows by `run_id` (`2026-09-02T10-40-35-495`), the harness extended never relaxed (both existing specs + the routine default byte-unchanged, `typecheck:e2e` green, routine arm exit 0, `sr*` skip at exit 0 unset), the routine subject isolated (clean re-seed; `sr` journals under `runs/sr-leg/runs`; the driven arm's isolation measured at the light gate), the census recorded per subject, and the workspace gates deferred under the zero-delta rule.
- **Gates (run by /implement to green, 2026-09-02):** gate 1 hygiene ✓ (both greps exit 1) · gate 2 evidence record ✓ (parses; identity block; closed outcome/arm sets; `operator_review` present) · gate 3 harness-extended ✓ (`git diff --stat` empty for the two specs + lockfile; the routine default line present; `npm run typecheck:e2e` ✓) · gate 4 routine `--e2e` ✓ (`scripts/agent-run.ps1 run --e2e`: 8 passing / 3 designed skips, exit 0 — run twice, before and after the config changes) · gate 5 `cargo nextest` / `cargo clippy` DEFERRED (zero `.rs` / manifest / lockfile delta — origin THIS chunk; re-run at the next source-touching chunk; pinned as the next entry's PREREQ at route-resolve) · gate 6 the SR leg ✓ (`sr-empty` 10:42:59Z · `sr` 10:41:57Z · `sr-error` 10:44:50Z, all three subjects attached; NVDA 2026.2; pre/post census: every leg-started process terminated, `pulse-app.exe` 50164 left running — the operator's) · gate 7 parser CLI ✓.
- **Smoke:** the routine arm ran as a P2 gate (recorded, not re-run); the `sr*` sessions booted the release bundle (`--features tauri/custom-protocol`) themselves on every session.
- **At this wrap (light gate):** the driven arm (`npm run a11y:driven`) is being re-run in its live firing form as the changed-surface gate for step 3(d) — result recorded below before the fan-out read this report.
- **Driven arm re-run (2026-09-02 11:55Z, wrap light gate) — env MEASURED, hold assertions NOT re-runnable:** the driven suite's app ran under `CONDUCTOR_RUNS_DIR=runs/driven/runs` (the config's per-suite env): its run journal `runs/driven/runs/2026-09-02T11-55-27-224.jsonl` + `.md` + `runs.db` landed there, and its self-obs log at `runs/driven/logs/conductor-tauri.jsonl` (11 457 B, 11:55:29Z) — the landing site is now measured, not derived; the fixture dir afterwards held exactly the committed `lamps-fixture.jsonl`. The run itself BLOCKED in 2 s — `preflight blocked: canary emission failed (OTLP transport error)` — because Pulse was already down at wrap (no `pulse-app.exe`, no `:4317` listener; the operator's process, stopped after the leg), so the hold dialog never arrived and the driven spec ran out its 12-minute wait. The hold path under a suite-chosen runs dir is proven by the SAME code path in the `sr` live session (two holds under `runs/sr-leg/runs`); the driven spec's own assertions last ran green on 2026-09-01 and re-run at the next live-Pulse leg. msedgedriver's own line on this session: `This version of Microsoft Edge WebDriver has not been tested with Microsoft Edge version 152.` — the cross-major pair's "unsupported" is the driver's statement, "working" the sessions'.
