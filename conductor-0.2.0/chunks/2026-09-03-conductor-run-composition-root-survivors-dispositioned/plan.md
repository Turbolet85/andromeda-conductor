# Plan — 2026-09-03-conductor-run-composition-root-survivors-dispositioned: "conductor-run composition-root survivors dispositioned"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-09-03-conductor-run-composition-root-survivors-dispositioned · **Version:** conductor-0.2.0 · **Generated:** 2026-09-03T11:02:00Z

## Goal

Close `conductor-run`'s standing mutation-survivor ledger: every named survivor ends either **killed by a
test** or **classified accepted-deliberate against a cited standing rule**, per test-plan §10. The score is a
consequence of that dispositioning and is never itself the acceptance.

The survivor set is **measured, not inherited**. P3 re-ran the tier at HEAD — 118 mutants, **25 missed, 65
caught, 28 unviable, 0 timeout** — and the 25-row result is composition-identical to the 2026-09-02 code
audit's table (`research.md` §The decisive measurement). Two facts that measurement added change the plan's
shape. First, **0 timeouts**: every survivor here is a MISSED, so the remedy class is uniformly *strengthen
the assertion*, never the sibling chunk's *bound the await* (`.claude/rules/testing.md` 2026-09-03). Second,
the tier needs **no build-graph repair** — 118 mutants enumerate, the baseline is 156/156 exit 0, and
`ui/dist` is present — which is the one place this crate differs sharply from `conductor-tauri`, whose tier
could not score at all until its build graph was fixed.

Of the 25, five are carried forward as already-ratified accepted-deliberate (the `declares` env-reading edge).
The remaining **20 are this chunk's work**: the 19 with no recorded disposition, plus `observe_run_contract:346`
— which P3 found may be killable with no env mutation at all, since the env read lives in `declares` rather
than in its caller. Scope had said carry all six as-is; the operator ruled at P4 to attempt the kill and shrink
the roster to five if it lands. The audit's own explanation for the concentration — that the file "grows every
epoch while its own test coverage does not follow" — was **falsified** at P3 (test lines +94% against
production's +72%), so this is not a catch-coverage-up chunk: each survivor gets its own cause read at its own
site.

**Expected end state, refined at the P5 review against HEAD:** of the 20, **17 killed** and **3 classified
accepted-deliberate** — `execute_scenario`'s interior trio (`:518:27`, `:559:25` ×2), which sits past a
fixed-endpoint egress gate no test can cross without binding Pulse's own port (step 3). With the 5 carried
`declares` members, `missed.txt` should end holding **exactly 8** entries. That split is the plan's
expectation, not its gate: the gate is that `missed.txt` matches whatever accepted-deliberate set this run
actually records, each member against a cited rule.

## Implementation Steps

1. **Preflight/contract cluster — kill 8 in the `lib.rs` `#[cfg(test)]` module (`lib.rs:956`+).**
   - `:66:8` (`delete ! in preflight`) — the guard chooses which branch logs `preflight blocked: readiness
     gate not satisfied`. Assert that line's presence on the not-ready path and its absence on the ready path.
     **No tracing-capture helper exists in this module** (verified at HEAD): capture with
     `tracing::subscriber::with_default` over a `Vec` sink scoped to the test — **never `init_observability`**,
     which is process-global and first-install-wins (test-plan §11 Integration). Per
     `.claude/rules/testing.md:72`, scope the observation count to this call site's own message text so a
     shared helper's tally cannot contaminate it.
   - `:346:5` (`observe_run_contract -> Default::default()`) — **the operator-ratified attempt.** The kill does
     **not** pass a declared set in: `observe_run_contract` derives it internally from `declares` (env read at
     `lib.rs:357`). It rests instead on a term the runner's environment cannot satisfy — construct a
     `RunContract` carrying a `CheckKind::ShellDeclaration` term whose `env` name is **guaranteed unset** under
     the test runner, so `evaluate(&empty)` returns a **non-empty `unmet`** while the mutant's
     `Default::default()` returns an empty one (`conductor-core/src/run_contract.rs:101` for the `Default`
     shape, `:153-166` for the filter). Pick an unset name deliberately; **no `unsafe { set_var }`, no
     process-env write** (`.claude/rules/testing.md:67`). If no such assertion proves reachable, classify it
     accepted-deliberate against that same rule and record why — a sanctioned exit, not a failure.
   - `:401:5`, `:402:24`, `:402:29`, `:402:55`, `:405:64` ×2 (`warm_up_canary_service`, 6 mutants) — assert the
     early-return guard on both arms (`warmup_ms == 0`, `warmup_emissions == 0`, and the `||` joining them) and
     the gap arithmetic `warmup_ms / warmup_emissions`.
     **Home and driver, both settled by measurement:** the function is **crate-private** (`lib.rs:396`), so no
     `tests/*.rs` can reach it and the existing OTLP stubs in `tests/canary_wire.rs` /
     `tests/canary_obs_witness.rs` / `tests/dispatch_wire.rs` are **not** available to it — the kill lives in
     the in-file `#[cfg(test)]` module, which today has **no receiver** (its only `TcpListener` uses at
     `lib.rs:1176`/`:1178` are the port-occupier guard's bind assertions, not a stub). Stand up a receiver
     there on an **ephemeral port**: `TraceEmitter::connect` takes any endpoint
     (`conductor-emit/src/client.rs:55`), unlike `execute_scenario`'s fixed constant, so nothing needs to bind
     `:4317`.
     **The gap arithmetic is observable only through the sleep gap**, so drive it on a **paused tokio clock**
     with `warmup_emissions >= 2` and `warmup_ms` chosen so that `ms/em`, `ms%em` and `ms*em` are mutually
     distinct — otherwise `%` or `*` satisfies the same assertion as `/`.
     These assertions pin the function's **shipped behaviour**; they deliberately do not endorse its original
     rationale, which architecture.md records as disproved-but-harmless (see §Constraints).

2. **Read-back/lifecycle cluster — kill 5 in the same module.**
   - `:197:21` (`< with <=` in `attribute_by_liveness`) — add a case sitting **exactly on**
     `AUTO_RESOLVE_IDLE_SECONDS`, where `<` and `<=` disagree, and assert the returned `LifecycleVerdict`
     variant. Assert the typed value, never a panic or a string (arch extract §Acceptance).
   - `:250:5` ×4 (`active_incident_ids` → `Ok(vec![])` / `[0]` / `[1]` / `[-1]`) — the function is
     **crate-private** (`lib.rs:249`) and takes `&ReadbackClient`, so the kill lives in the in-file
     `#[cfg(test)]` module. **Driver:** `ReadbackClient::connect_transport` is `pub`
     (`conductor-verify/src/client.rs:71`) and is driven over an **in-process duplex** by conductor-verify's own
     tests (`tests/jsonrpc_correlation.rs:23`, `tests/jsonrpc_line_bound.rs:46`) — reproduce that shape locally
     with a responder returning a fixed `items` array. Do **not** reach for `stub_pulse_mcp`: it is a
     `stub-server`-feature-gated **binary** in conductor-verify, not a library helper, and pulling it in would
     mean a new dev-dependency and a spawned child for a unit-tier kill.
     Assert the returned ids equal a set that contains **at least two** ids and is not `[]`, `[0]`, `[1]` or
     `[-1]`, so all four replacements differ from it simultaneously. Per `.claude/rules/testing.md:72`(a) the
     responder must supply those ids **independently of the request**, since a stub echoing what it was given
     is blind to a mutation of that value. **Reach the corpus only through the MCP tool surface** — never stage
     a corpus row (security extract §Anti-patterns).
     If the duplex needs a `tokio` dev-dependency feature this crate does not yet enable (e.g. `io-util`), that
     manifest edit is in scope — see §Codebase touchpoints.

3. **Run cluster — kill 3, classify 3 accepted-deliberate.**

   **`:518:27` (delete field `degraded`) and `:559:25` ×2 (`- with +` / `- with /`) → ACCEPTED-DELIBERATE,
   not killed.** Both sit **inside `execute_scenario`, past its egress gates**, and the drive-the-path exit was
   researched and found unreachable:
   - `execute_scenario` reaches them only after `probe_egress(DEFAULT_OTLP_ENDPOINT)` (`lib.rs:488`) and
     `Dispatcher::connect(scenario, DEFAULT_OTLP_ENDPOINT)` (`:493`), where `DEFAULT_OTLP_ENDPOINT` is the
     **fixed** `http://127.0.0.1:4317` (`conductor-emit/src/client.rs:23`) — not an injectable parameter.
     Reaching them therefore requires binding **Pulse's own ingest port** inside a unit test: a second
     deliberate inbound bind architecture.md does not sanction (the port-occupier fault is the only one), which
     would also collide with this crate's own occupier guard test (`lib.rs:1176`) and with any live Pulse on
     the dev host — flaky by construction against the zero-retry bar (test-plan §10).
   - No integration test can drive it either: `Preflight`'s fields are crate-private, as
     `tests/operator_pause_harvest.rs:20` already records, and no `tests/*.rs` calls `execute_scenario`.
   - The two in-file drivers (`:1369`, `:1384`) go through `blocked_preflight()` and stop at the Blocked arm —
     which is why `execute_scenario`'s *other* mutants are caught and only these three survive.
   - Making the endpoint injectable would be a production **signature change**, barred by §Constraints.

   Record all three against the cited rules above (architecture.md §Occupied Resources — Ports / §Cross-cutting
   Patterns — Trust boundary; test-plan §10 zero-flakiness). **Do not** extend
   `a_declare_only_empty_read_back_routes_to_the_auto_resolve_residual` (`lib.rs:989`) to chase `:518`: that
   test calls `route_read_back` and then `manual_record` **directly**, with its own literal
   `Observation { degraded: true, .. }` and `3_000` — it never enters `execute_scenario`, so an assertion added
   there would pin a literal the test itself wrote and pass while the mutant lives.

   **Then kill the remaining 3 in the in-file module:**
   - `:715:9` ×2 (`FaultKind::label -> ""` / `"xyzzy"`) — assert the **literal** `fault_type` values pinned by
     obs-plan §4: `"silence"` for `FaultKind::Silence` and `"ramp"` for `FaultKind::Ramp`. The existing
     classification tests (`lib.rs:1446`, `:1456`) assert the *variant* and never the emitted string, which is
     exactly why these two survive.
   - `:753:5` (`fault_span -> None`) — assert a fault-declaring phase yields a span at all, and that a
     non-fault phase yields `None`. Assert only bounded span names and allowlisted attributes; **do not widen
     the span-name set or `ALLOWLISTED_FIELDS`** (obs extract §Anti-patterns).

4. **Dispatcher — kill `dispatch.rs:163:26` (`== with !=`).** Assert the P-008 root-vs-deep placement:
   `error_depth: Some(0)` yields `ErrorPlacement::Root` and `Some(n)` for `n > 0` yields
   `ErrorPlacement::DeepChild { depth: n }`. Home it either in a `#[cfg(test)]` module in `dispatch.rs` or in
   the existing `crates/conductor-run/tests/dispatch_wire.rs`, which already captures against the loopback
   OTLP stub — /implement's choice; both are sanctioned homes (research.md §Open questions).

5. **Re-run the tier and read out the gate.** Use a **fresh** `--output` directory and read the **nested**
   `mutants.out/` tallies (`.claude/rules/testing.md:77` — a pre-existing output dir is stale by construction
   and once produced a confident, wholly false clean sweep). The run must report **`Found 118 mutants`** for
   its score to be comparable with P3's pre-state; a different count means the tree moved and the comparison
   is void. Gate on the tallies, never the exit code: `missed.txt` holds **exactly** this run's
   accepted-deliberate survivors, and every other named survivor appears in `caught.txt` (test-plan §4, as
   reconciled at the sibling chunk's wrap).

6. **Record the disposition ledger** under
   `conductor-0.2.0/chunks/2026-09-03-conductor-run-composition-root-survivors-dispositioned/evidence/` — one row per survivor
   naming its exit (killed, with the test that kills it; or accepted-deliberate, with the cited rule). No
   absolute host path in the committed record (security extract §Acceptance). This is the artifact wrap reads
   to extend test-plan §12's roster.

7. **Discharge the standing PREREQ** — the 47th `cargo audit` re-check, per §Test Commands and
   §Implementation notes.

## Codebase touchpoints

### New files
- `conductor-0.2.0/chunks/2026-09-03-conductor-run-composition-root-survivors-dispositioned/evidence/` — the
  disposition ledger (step 6). No new source or fixture file is anticipated: every survivor is reachable from
  the existing in-file test module or the existing `tests/` files.

### Files to modify
- `crates/conductor-run/src/lib.rs` — **`#[cfg(test)]` module only** (from `:956`). 19 of the 20 kills land
  here. Production source expected byte-unchanged (see §Constraints).
- `crates/conductor-run/src/dispatch.rs` **or** `crates/conductor-run/tests/dispatch_wire.rs` — the
  `dispatch.rs:163` placement kill (step 4), whichever /implement selects.

**Caller threading: not applicable.** No survivor's remedy changes a signature, so no caller set is threaded.
For the record, `execute_scenario` is the only survivor-bearing symbol with cross-crate callers —
7 sites / 3 files, into `conductor-cli` `commands/run.rs` and `commands/suite.rs` (research.md §Graph impact)
— and that boundary would come into play only if a signature moved, which this plan does not do.

- `crates/conductor-run/Cargo.toml` — **conditional, step 2 only.** No new dev-*dependency* is anticipated
  (`tokio` with `macros`/`rt`/`net`/`test-util`, `assert_fs`, `opentelemetry-proto`, `tonic`, `tokio-stream`,
  `insta`, `serde_json` are all present), but the in-process duplex may need a `tokio` **feature** this crate
  does not yet enable (e.g. `io-util`). A feature addition normally moves no package. Either way the result is
  reported as a **package-count delta**, never as "byte-unchanged" (`.claude/rules/security.md` 2026-09-02),
  with `cargo deny` verified green over the resulting lock.

## Test Commands

```bash
# unit + integration (the crate under work), and the standing runner-portability gate
cargo nextest run -p conductor-run --profile ci
cargo test -p conductor-run

# workspace regression + lint
cargo nextest run --workspace --profile ci
cargo clippy --workspace --all-targets -- -D warnings

# the mutation tier — the PRODUCER of this chunk's acceptance evidence.
# Fresh --output dir; tallies read from the NESTED mutants.out/ (.claude/rules/testing.md 2026-09-03).
cargo mutants -f crates/conductor-run/src/lib.rs -f crates/conductor-run/src/dispatch.rs \
  --test-tool=nextest --jobs 2 --output target/mutants-impl-2026-09-03
cat target/mutants-impl-2026-09-03/mutants.out/missed.txt
cat target/mutants-impl-2026-09-03/mutants.out/caught.txt

# supply chain — the standing PREREQ (the 47th) + its overlap. Read each exit DIRECTLY, never through a pipe.
cargo audit  # EXPECTED RED (exit 1): standing deferral since 2026-08-09, ratified at the 2026-08-10 wrap;
             # satisfied by signature match (advisory-DB parse fault, duplicate RUSTSEC-2026-0244) —
             # route-resolve §Probe-auto-satisfy. Any deviation restores the FULL form and is reported.
cargo deny check advisories bans licenses sources
```

No live-Pulse leg and no UI surface in this chunk, so no live-leg firing form, no headful self-verify, and no
harness smoke verb are listed — none would produce evidence any criterion below rests on.

## Acceptance Criteria

- (tests) The tier re-run reports **`Found 118 mutants`** — the same count as P3's pre-state, without which
  the two scores are not comparable (per test-plan §4).
- (tests) `missed.txt` holds **exactly** this run's accepted-deliberate survivors and every other named
  survivor is present in `caught.txt`; the verdict is read from the nested `mutants.out/` and never from the
  exit code, which carries no verdict in either direction (per test-plan §4).
- (tests) Each of the 20 in-scope survivors ends **killed** or **accepted-deliberate against a cited standing
  rule**; the §12 roster is EXTENDED, not contradicted, and no numeric threshold is used as the acceptance
  (per test-plan §10).
- (tests) `cargo nextest run -p conductor-run` AND `cargo test -p conductor-run` are both green — the standing
  runner-portability gate — with zero retries and no runner-dependent result (per test-plan §4, §10).
- (tests) The CI coverage threshold is left untouched — no workflow or `.config` edit lowers
  `--fail-under-lines`; this is asserted from the chunk's diff, not by running the coverage gate, which is a
  CI stage and not a listed command here (a test-only chunk cannot reduce line coverage). New tests add no
  wall-clock or shared-mutable-state dependence (per test-plan §10, §11).
- (arch) All added tests live in `crates/conductor-run`; no new workspace member and no new cross-seam
  dependency edge (per architecture.md §Established Decisions [Module Boundaries]).
- (arch) No new `CONDUCTOR_*` or `ANDROMEDA_PULSE_*` env handle is introduced and no test writes process
  environment; env is read at the caller and passed in as a typed value (per architecture.md §Occupied
  Resources — Environment variables).
- (arch) Killing tests assert typed `Verdict` / `ReportState` / `ReadBack` / `LifecycleVerdict` values, with
  `Result::Err` reserved for harness faults (per architecture.md §Cross-cutting Patterns — Verdict/error wall).
- (obs) Assertions use only bounded span names and attributes already in
  `conductor-core::redact::ALLOWLISTED_FIELDS`; no span name is added and no critical-path span is removed or
  downgraded to make a mutant die (per obs-plan §11 Spans/Traces, §4).
- (obs) The `FaultKind::label` disposition pins the literal `fault_type` values `"silence"` and `"ramp"` (per
  obs-plan §4 Fault-injection spans).
- (security) `cargo deny check advisories bans licenses sources` exits 0, read directly rather than through a
  pipe, and recorded as verified rather than assumed (per security-plan §Dependency Security).
- (security) `cargo audit` re-checked as the 47th: either the pinned signature is matched and the deferral
  re-pinned as a database fault, or the deviation is reported in FULL form — with no floor raise, no
  `deny.toml` ignore and no CI edit (per security-plan §Dependency Security).
- (security) `Cargo.lock` un-drifted and committed; any dependency delta reported as a package count with deny
  green over the NEW lock (per security-plan §Dependency Security).
- (security) No committed record of this chunk contains an absolute host path (per security-plan §Security
  Anti-Patterns → Logging).
- (a11y) `crates/conductor-run/tests/envelope_fixture.rs`, `tests/lamps_fixture.rs`, `tests/fixtures/` and the
  envelope production-writer path are **absent from this chunk's diff** — asserted from the diff, since the
  a11y routine arm that would otherwise prove it is not a listed command here (no UI surface in this chunk).
  That is what keeps the arm's two seeded subjects intact and stops any routine-arm spec context-skipping as a
  consequence of this chunk (per a11y-plan §4, §9).

**No capability is claimed.** All six unclaimed matrix entries (`v2-04`, `v2-21`, `v2-24`, `v2-26`, `v2-27`,
`v2-32`) belong to other route entries — live-Pulse scenarios, the a11y CI gate, the coverage completeness
gate, release/bundle, and dependency polish. Mutation dispositioning is a quality-audit activity, matching the
sibling chunk, which also claimed none.

## Provenance
- Spec anchors: test-plan §4 (mutation instrument + read-out), §9 (scoped audit, never a CI stage), §10
  (survivor disposition + roster-as-a-SET), §11 (unit/integration discipline), §12 (roster entries) ·
  architecture.md §Established Decisions [Module Boundaries] / [Error Handling], §Cross-cutting Patterns
  (verdict/error wall, determinism), §Occupied Resources (env vars, crate names), §Infrastructure Patterns
  (build system) · obs-plan §3 (self-obs line shape), §4 (fault-injection spans), §6 (levels, boundary-call
  wrappers), §11 (bounded span set) · security-plan §Dependency Security, §Input Validation, §Error Handling,
  §Security Anti-Patterns · a11y-plan §4, §9, §11.
- Rules: `.claude/rules/testing.md:19` (procedural disposition rule), `:67` (env-at-the-caller), `:72`
  (assert the mutant's difference on the observed surface), `:77` (fresh output dir + nested tallies), `:78`
  (MISSED vs TIMEOUT read differently) · `.claude/rules/security.md` (2026-09-02, package-count delta).
- Research files inspected: `crates/conductor-run/src/lib.rs`, `crates/conductor-run/src/dispatch.rs`,
  `crates/conductor-run/Cargo.toml`, `crates/conductor-run/tests/` + `tests/fixtures/`,
  `.claude/rules/testing.md`.
- Code-graph: `.andromeda/runs/2026-09-03T10-31-07-phase/tree-query-…json` (43 rows, plane `rust`,
  `db_state: fresh`).

## Constraints & rejected approaches

- **Splitting `lib.rs` along the cluster seam — REJECTED for this chunk.** The source proposal offers it as
  direction (a) and states the disposition half "does not depend on" it and "is the cheaper one". A split is
  also a §Module Boundaries decision and a registered-resource change (arch extract §Anti-patterns). Do not
  restructure the composition root to make a mutant convenient to kill.
- **Production-code change — NOT expected, and not licensed by default.** Every survivor here is a MISSED,
  meaning the harness observed the mutant and shrugged; the remedy is a stronger assertion. The sibling
  chunk's bounding remedy applied only to TIMEOUT survivors, of which this crate has **zero**. Any production
  edit must be named and justified in the report, never taken silently.
- **`unsafe { std::env::set_var }` in a shared-process test module — BANNED.** It is the exact hazard the
  2026-08-20 chunk removed, and the ground of the `declares` acceptance (`.claude/rules/testing.md:67`).
- **Staging a Pulse `corpus.db` row to induce a state — BANNED.** Reach the corpus only through the MCP tool
  surface (security extract §Anti-patterns).
- **A numeric `--fail-under` mutation threshold — REJECTED.** Explicitly not the gate; the score is a
  consequence of dispositioning (test-plan §10).
- **Widening the bounded span-name set or `ALLOWLISTED_FIELDS` to make a survivor observable — BANNED**; the
  set is a deliberate contract and a 2026-09-01 operator ruling declined even a `db.*` read-span wildcard.
- **Asserting `warm_up_canary_service`'s rationale — OUT.** architecture.md records its stated purpose as
  disproved on two axes (a 45s pre-roll against a 3,600s bootstrap gate, aimed at a path the canary does not
  ride) but harmless and shipped. Step 1 pins what the function DOES, not why it was written.

## Implementation notes

- **Read the classes correctly.** MISSED = the harness observed the mutant and shrugged → strengthen the
  assertion. TIMEOUT = the harness never got to observe → bound the await. This crate is 25 MISSED / 0
  TIMEOUT, so every remedy is the first kind (`.claude/rules/testing.md:78`).
- **Two mutants at one site need one assertion that separates all three states** (correct, and both
  replacements). `FaultKind::label` → `""` and `"xyzzy"` both die to pinning the literal; `active_incident_ids`
  needs an id set that differs from `[]`, `[0]`, `[1]` and `[-1]` simultaneously; `:559`'s `-` → `+` and `/`
  need stamps whose three results are mutually distinct.
- **Order sensitivity:** run the tier (step 5) only after steps 1-4 are green under both runners; a tier run
  over a red tree wastes ~12 minutes and its tallies mean nothing.
- **Budget from the measured cost:** P3's full run was **12 minutes for 118 mutants at `--jobs 2`** on this
  tree. Budget from that, not from a guess.
- **Expected amendments (wrap):**
  - `test-plan.md` §12 — the mutation-instrument entry's accepted-deliberate roster gains `conductor-run`'s
    set, each member against its own cited rule; if step 1's `observe_run_contract:346` kill lands, the
    `declares` class shrinks 6 → 5 and the roster records that.
  - `test-plan.md` §10 — the roster reference tracks the same change.
  - `test-plan.md` §500 — **correction:** it attributes the accepted-deliberate `declares` ×6 env-reading edge
    to `conductor-verify`; `fn declares` is at `crates/conductor-run/src/lib.rs:357` (verified at take-up,
    single hit workspace-wide). Direction: re-attribute to `conductor-run`.
