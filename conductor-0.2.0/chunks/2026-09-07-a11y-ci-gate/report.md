# Report — 2026-09-07-a11y-ci-gate

**Chunk:** A11y CI gate — the a11y specs as a CI gate without Pulse, failing on any violation, emitting redacted service-tagged violation JSON
**Date:** 2026-09-07
**Commits:** none yet — this wrap authors the chunk's first commit (branch `build/conductor-0.2.0`, `@{u}..HEAD` = 0 at Setup; the session's earlier push landed, so this commit makes it 1 ahead)

## Changes (structured — detectors read this)

- **Files:** `.github/workflows/ci.yml` · `crates/conductor-tauri/ui/wdio.conf.ts` · `crates/conductor-tauri/ui/test/a11y/accessibility.e2e.ts` · `crates/conductor-tauri/ui/test/README.md` · `scripts/agent-run.sh` · `scripts/agent-run.ps1` · **new** `crates/conductor-tauri/ui/knip.json`
- **Symbols / APIs:**
  - `wdio.conf.ts` (all module-private, not exported): `strictMode()` · `exitUnresolvedHandle()` · `writeA11yEnvelope(results)`; constants `A11Y_RUNS_DIR = 'runs/a11y'`, `VIOLATION_SIDECAR`; module state `a11yRunId` / `a11yStartedAt` / `a11ySuite`. `onComplete` gained the wdio-supplied `(exitCode, config, capabilities, results)` parameters (previously nullary).
  - `accessibility.e2e.ts` (module-private): `recordViolationTuples(violations)`; `VIOLATION_SIDECAR`.
  - `agent-run.sh`: shell function `assert_a11y_verdict()` + constant `A11Y_EXPECTED_SKIPS=2`. `agent-run.ps1`: `Assert-A11yVerdict([string]$LogPath)` + `$A11yExpectedSkips = 2`.
  - **Env vars:** **`CONDUCTOR_A11Y_STRICT` is NEW** — reserved-namespace, flag-valued (affirmative `"true"`/`"1"`, mirroring `conductor_core::flag_declared`), read ONLY by `wdio.conf.ts` and by the two shells' assertion helpers; never by a shipped binary. No other handle added or changed.
  - **Ports/sockets:** unchanged — `4444`/`4445` remain dev-only and harness-lifetime; no shipped binary gained a listener.
  - **Remaining-caller facts:** `nativeDriver()` / `nvdaExe()` / `reportSkip()` / `reportNvdaSkip()` are BYTE-UNCHANGED; only their two `process.exit(0)` call sites in `onPrepare` were replaced by `exitUnresolvedHandle()`. Basis: the P3 code-graph impact query (`tree-query-2026-09-07-a11y-ci-gate.json` entry 1, `rows: 6`, plane `ts`, `db_state: fresh`) — every caller of those guards is `wdio.conf.ts` itself, so these are sole-caller sites, not a default assumption.
- **Crates / modules:** none added, removed, or changed. Zero `.rs` delta.
- **Dependencies:** **none added, none bumped.** `package-lock.json` and `Cargo.lock` are both untouched (basis: `git status --short` lists neither). `knip.json` is configuration, not a dependency.
- **Schema / config:**
  - **New artifact: the a11y violation record**, one envelope line per run at `runs/a11y/<run_id>.jsonl` (gitignored via `/runs/` at `.gitignore:17`; NOT matched by the harness's non-recursive `"$RUNS_DIR"/*.jsonl` glob at `agent-run.sh:156`, so `status`/`report` cannot mistake it for a scenario run). Shape = the **eleven-field run-report envelope** (a11y-plan §3 `:234-246`) **PLUS** the obs resource tags `service.name` (`conductor-ui`) and `deployment.environment`, and under CI additionally `ci.run.id` + `git.commit.sha`. **Measured top-level key count: 13 locally, 15 under CI.** Basis: the emitted record `runs/a11y/2026-09-07T15-48-39-a11y.jsonl`.
  - Field mapping (scenario-shaped keys are MAPPED, not measured): `seed: 0` · `scenario` = the invoked suite (`routine`) · `p_ids: []` · `slo_tier: "<90s"` · `read_back_observed_at` = suite end. `verdict`/`state` = `Fail` when any violation tuple was recorded OR any spec failed, else `Pass`. `fingerprints[]` = `{axe-rule-id}|{wcag-sc-tag}|{selector}`, **one entry per NODE**.
  - **Scrub/redaction shape:** selectors are DOM-relative / `data-testid` by construction; the record carries no filesystem path. Verified by the conformance gate's own host-path arm over every string at either depth.
  - New config file `knip.json`: `entry` = `test/a11y/**/*.e2e.ts`; `ignoreDependencies` = `@wdio/local-runner`, `axe-core`, `lighthouse`.
- **Spec-master edits:** none in this report — P2 applies them.
- **Counts / qualifiers moved:**
  - knip findings **20 → 12** (basis: `npm run knip` before and after the config; before = 3 unused files + 5 devDeps + 3 exports + 9 exported types, after = 3 exports + 9 exported types).
  - a11y violation-record top-level keys: the spec states eleven; the shipped artifact carries **13** (see Schema, and the UNMET criterion in Outcome).
  - New constant: expected routine-arm skips = **2** (basis: `grep -nE 'this\.skip\(\)' accessibility.e2e.ts` → `:239`, `:251`, `:295`, `:301`, of which `:239`/`:251` are fixture-seeded, over 12 `it()` blocks; measured runs read 10 passing / 2 skipped).
- **Dev-tool versions:** none installed or upgraded.
- **Harness / gate surface:**
  - `--e2e` in BOTH shells now captures wdio output to `$RUNS_DIR/a11y-e2e.log`, reads the exit from the BARE command (no pipe), and asserts the PRINTED verdict: `Spec Files:` failed-count · the per-spec skip markers against the expected set · the `[webview2 … windows]` driven-session banner. Identical semantics both shells; command count stays five; no new stage selector.
  - **New CI job `a11y`** in `.github/workflows/ci.yml`, `runs-on: windows-2025` (explicit label, not `-latest`), with steps: checkout → `rustup show` → `Swatinem/rust-cache@v2` → `taiki-e/install-action` (cargo-nextest) → `actions/setup-node@v4` → `.\scripts\agent-run.ps1 run --e2e` under `CONDUCTOR_A11Y_STRICT=1` / `CONDUCTOR_MSEDGEDRIVER=${{ env.EDGEWEBDRIVER }}\msedgedriver.exe` / `CONDUCTOR_ENV=dev` → the conformance gate → `actions/upload-artifact@v4`. The existing supply-chain steps (`:86` cargo audit, `:89` cargo deny, `:209` npm audit) are untouched.
  - **The conformance gate REUSES an existing test** rather than re-listing the schema: `CONDUCTOR_RUNS_DIR=runs/a11y cargo nextest run -p conductor-run --test journal_conformance --profile ci`, the `ci.yml:172-179` form. No new gate code, no second copy of the key list.
- **Cross-project / external claims:** the GitHub runner image contents — `windows-latest` = Windows Server 2025 shipping Microsoft Edge 152.0.4191.53 + Microsoft Edge Driver 152.0.4191.53 at `EDGEWEBDRIVER=C:\SeleniumWebDrivers\EdgeDriver` (windows-2022 at 151.0.4129.x). Ground truth lives in `actions/runner-images` (main); **operator-measured 2026-09-07, not verifiable from this repo**. One `hypothesis:` remains unmeasured — the WebView2 Evergreen runtime's presence on that image; the job's first run measures it.
- **Reverted / negative API facts:** the plan's step 7 specified an `install tauri-driver` CI step. **Not shipped** — `wdio.conf.ts:49` resolves `@crabnebula/tauri-driver/cli.js` from `node_modules`, so `npm ci` already supplies it and a cargo install would be dead weight.
- **Insufficient fixes (written, kept, not the remedy):** none.
- **Spec claims disproved by measurement:**
  1. **CARRY 5's "20 findings, all 20 false positives of ONE class"** (working-route `:121`) — measured false. Only 8 were the WebdriverIO-discovery class and are now gone; `lighthouse` and `axe-core` are TRUE positives of a different kind (a11y-plan §3-mandated *by name and pin*, neither imported anywhere — `axe-core` reaches code only transitively via `@axe-core/webdriverio`); `@wdio/local-runner` is a plugin-conditional sub-class; and 12 of 20 are an exported-but-unimported class the CARRY never mentions. Evidence: the before/after `npm run knip` runs.
  2. **The plan's stated wdio tally format** — `Spec Files:` **omits** the `failed` term entirely when nothing failed (real line: `Spec Files:  1 passed, 1 total (100% completed)`), so a parser requiring `N failed` finds nothing on a green run. Evidence: the calibration and overseer runs.
  3. **This chunk's own implement-stage friction record** claimed mocha prints no skip summary at all. **Measured false by the operator:** mocha prints `2 skipped` (overseer log `:135676`). What is genuinely absent is the token `pending` — which is what the implement stage grepped for, a proxy for the reporter's actual wording. The marker-counting assertion is correct either way; the retraction is recorded in the friction ledger.
  4. **a11y-plan §3 `:248`'s "so no new top-level fields are introduced"** — the shipped artifact carries 13 top-level keys. See Outcome; the extension point that admits them is documented (`journal_conformance.rs`: `// (b) key presence — extra keys are allowed (obs-plan §3 keeps the extension point open)`), so the spec sentence, not the artifact, is what needs to move.
- **Expected amendments (from plan):**
  - `a11y-plan` §11 Operable carve-out — **carried**; fact in *Harness / gate surface* (the gated arm is the routine one) + *Outcome*. Located by `grep -c 'WebdriverIO-keyboard' a11y-plan.md` → 5 hits; the STATING sentence is `:516` ("both axe-core `color-contrast` and WebdriverIO-keyboard must run on every must-be-accessible path").
  - `a11y-plan` §9 Platform / §3 Configuration (CI arrangement + stale `ci.yml` coordinate) — **carried**; fact in *Harness / gate surface*. Located by `grep -n 'ci.yml:18' a11y-plan.md` → 2 stating sentences, `:218` and `:457`, both asserting "no such CI job exists today" with the now-stale `:178` frontend coordinate (frontend is at `:188`).
  - `a11y-plan` §11 `:565` (CARRY 4's reduced-motion clause) — **carried**; fact in *Spec claims disproved* item 4's neighbourhood and in the chunk's scope. Located by `grep -c 'one platform-dependent assertion' a11y-plan.md` → 1 hit.
  - `a11y-plan` §3 `:248` violation-schema shape — **carried** (added at this wrap per operator directive item 1); fact in *Schema / config* + *Counts moved*. Located by `grep -c 'no new top-level fields' a11y-plan.md` → 1 hit.
  - `test-plan` §9 Matrix builds + Pipeline E2E row — **carried**; fact in *Harness / gate surface*. Located by `grep -n 'ubuntu-latest' test-plan.md` → 1 hit at `:469`, which names the *A11y CI gate* route entry as the owner of the arrangement this chunk now ships.
  - `architecture` §Occupied Resources — Environment variables (register `CONDUCTOR_A11Y_STRICT`) — **carried**; fact in *Symbols / APIs → Env vars*. Located by `grep -c 'CONDUCTOR_MSEDGEDRIVER' architecture.md` → 3 hits (the handle-registry neighbourhood).
  - `security-plan` §Input Validation (the `CONDUCTOR_A11Y_STRICT` row) — **carried**; fact in *Symbols / APIs → Env vars*. Located by `grep -c 'CONDUCTOR_MSEDGEDRIVER' security-plan.md` → 5 hits.
  - `obs-plan` §9 (register the a11y violation artifact among uploaded CI artifacts) — **carried**; fact in *Schema / config* + *Harness / gate surface*. Located by `grep -n 'agent-latest.jsonl' obs-plan.md` → 4 hits (`:48`, `:79`, `:104`, `:147`), the artifact-list neighbourhood.
- **Coverage of new surfaces:**
  - `CONDUCTOR_A11Y_STRICT` (env handle) → validation n/a (flag-valued, never a path — no canonicalize duty; the truthiness test mirrors `flag_declared`) · instrumentation n/a · PII n/a (never logged; the precondition names the handle, never a value) · tests `e2e` (both arms exercised: strict+unresolved → exit 1, lax+unresolved → exit 0) · a11y n/a · tokens n/a
  - `runs/a11y/<run_id>.jsonl` (violation artifact) → validation `✓` (asserted by `journal_conformance`'s key-presence + closed-set + host-path arms) · instrumentation n/a (it IS the artifact) · PII `redacted✓` (DOM-relative selectors only; host-path arm green) · tests `integration` (the conformance gate, run against the real record) · a11y n/a · tokens n/a
  - CI job `a11y` (gate surface) → validation n/a · instrumentation n/a · PII `redacted✓` (no handle value in any emitted string) · tests `e2e` (the leg ran 4× on this host) · a11y `✓` (it IS the a11y gate) · tokens n/a — **`unrunnable-here` for the CI job itself**: the job's own first execution requires a push, which is the operator's act.

## Deviations from intent

1. **The emitter is split across two files, not confined to `wdio.conf.ts`.** wdio runs specs in a WORKER and `onComplete` in the LAUNCHER, so a module global is not shared across that boundary — the axe tuples are written by the spec to a sidecar file and assembled by the config. Both files were already touchpoints; no new file, no new env handle.
2. **`fingerprints[]` carries one entry per NODE, not per violation.** The plan said per violation; `target` is a per-node field, and the 2026-09-01 sweep recorded a single `color-contrast` violation spanning 18 nodes — per-violation would drop 17 selectors. a11y-plan §3 `:248` names the "node `target` selector", which supports the finer grain.
3. **No `tauri-driver` install step in CI** (plan step 7) — the driver is an npm package resolved from `node_modules`.
4. **The tally assertion was calibrated by RUNNING the leg first**, not written from the plan's stated format — which was wrong in two specifics (Spec-claims-disproved items 2 and 3).
5. **Resource attributes ride as extra top-level keys**, making the artifact 13 keys where the plan said eleven with "no new top-level fields". Plan step 4 itself asked for the tags, so the two clauses were in tension as written; the conformance gate's documented superset tolerance is what admits them. Routed as an UNMET criterion in Outcome rather than reported met.

## Decisions & corrections

- **Operator, P5 review (phase):** four fixes — reuse the existing `journal_conformance` gate rather than re-listing the schema in CI (the repo's own `ci.yml:167-171` comment rejects a third copy); name the three tally assertions distinctly; de-provisionalise the runner (image ships the Edge driver; pin `windows-2025`; add `rust-cache`); word the CI criterion as met-in-mechanism-locally with the GitHub run as the operator's act.
- **Operator, P4 forks (phase):** record the Operable carve-out rather than add routine keyboard specs (CARRY 3(2)'s focus defect is open, so such specs would gate on a known-red subject); land the printed-verdict assertion HERE in both shells rather than deferring to the sibling entry.
- **Operator, this wrap:** the (obs) eleven-key criterion is UNMET as worded — never a silent MET; CARRY 5 discharged with its premise corrected; CARRY 3 carried forward as a new route entry; the BLOCKED-ON's dead clauses rewritten now while its clearing event is pinned as a CARRY.
- **Correction to this chunk's own record:** the implement-stage claim "mocha prints no skip summary" is false; it prints `2 skipped`. The grep was for `pending`, a proxy for the reporter's wording — the same proxy-token class this project has recorded before.
- **Measured, not inherited:** every CARRY coordinate was re-verified at promotion; CARRY 4's four line numbers proved correct, CARRY 1's README problem proved to be three sites not one, and CARRY 2's account of the prior workaround proved wrong (the sibling guard took `[role="alertdialog"]`, not `aria-live`).

## Outcome

**Acceptance criteria, re-asserted against the DIFF:**

- (a11y) routine arm runs with no live Pulse, non-zero on violations, no retry/warning-only — **MET in mechanism locally**; the GitHub run that proves it end-to-end is the operator's act and is this entry's `BLOCKED-ON` clearing event. Not claimed as done.
- (a11y) the gate cannot report success by skipping — **MET, measured both ways**: strict + unresolved handle → exit 1; lax + unresolved → exit 0 with the dev-host skip preserved; a third skip fails the whitelist.
- (obs) "exactly the eleven envelope keys … **no new top-level field**" — **UNMET as worded.** The emitted artifact carries **13** top-level keys (the eleven plus `service.name` + `deployment.environment`, which plan step 4 itself required), and 15 under CI. The keys are admitted by design — `journal_conformance.rs` states `extra keys are allowed (obs-plan §3 keeps the extension point open)` — so the artifact is correct and the *criterion wording* and a11y-plan §3 `:248` are what must move. No capability is matrix-linked to this criterion, so it routes as a **P2 escalation**, not a post-claim disproof.
- (obs) service identity + CI resource attributes — **MET** (`service.name`, `deployment.environment` present; `ci.run.id`/`git.commit.sha` gated on `GITHUB_ACTIONS`, unexercised locally by construction).
- (security) 0 absolute host paths / 0 struct names in committed or uploaded artifacts; unset-handle precondition names the handle not its value — **MET** (conformance host-path arm green; skip text carries no value).
- (security) `ci.yml` still runs cargo audit / cargo deny / npm audit with fail-on-hit — **MET** (steps `:86`/`:89`/`:209` untouched; all three green this session).
- (arch) no live-Pulse dependency, no shipped-binary listener, no second automation stack — **MET**.
- (tests) both shells assert the printed verdict at identical semantics; five commands; zero retries — **MET**.
- (design) no new token, hex, raw duration or pixel — **MET** (no `.css`/token file touched).
- (tests) workspace nextest + clippy return 0 — **MET** (902/902; clippy exit 0).

**Gates green (commands run):** `cargo nextest run --workspace --profile ci` (902/902) · `cargo clippy --workspace --all-targets -- -D warnings` (0) · `npm run build` (0) · `npm audit --omit=dev` (0 vulns) · `npm run knip` (20→12) · `cargo audit` (0, 17 allowed warnings; advisory-db porcelain 0 lines read FIRST) · `cargo deny check advisories bans licenses sources` (0) · `CONDUCTOR_RUNS_DIR=runs/a11y cargo nextest -p conductor-run --test journal_conformance` (8/8 against the real record) · `bash scripts/agent-run.sh run --e2e` (asserted: 0 failed · 2 skipped · banner present). **No gate deferred** — despite zero Rust source delta permitting it, the workspace gates were run.

**Smoke:** the headful self-verify is listed in Test Commands and ran as a P2 gate — recorded, not re-run (4 leg invocations: calibration, asserted gate run, two negative arms).

**Outcome basis:** implement's P4 report, PLUS this wrap's operator directive (four items, measured on this host) — which changed three things: the (obs) criterion from met to **UNMET as worded**; the mocha-summary fact (a `2 skipped` line DOES print, correcting the implement friction record); and the disposition of CARRY 3 / CARRY 5 / the BLOCKED-ON. Post-implement artifacts named: `runs/a11y-overseer-20260907T160516Z.log`, `runs/a11y-overseer-neg-20260907T160516Z.log`, `runs/a11y/2026-09-07T16-05-48-a11y.jsonl` (Pass, 5 900 ms) — all gitignored under `/runs/`, declared by the operator, and the light gate's own re-run adds a third journal. On CI the dir is fresh per run.

**Process hygiene:** implement's census recorded `tauri-driver` / `msedgedriver.exe` / `conductor-tauri.exe` / `node.exe` all started by this chunk's leg runs and all `terminated` via wdio's `onComplete` → `tauriDriver.kill()`; `pulse-app` `none started` (the routine arm needs no live Pulse). Re-measured at this wrap: **0 conductor-tauri · 0 msedgedriver · 0 node**, no `4444`/`4445` listeners. The six `msedgewebview2.exe` visible on this host belong to `SearchHost.exe` (Windows Search), **not to the leg** — operator-measured, and not this chunk's orphans.
