# Codebase Research — 2026-09-07-a11y-ci-gate

## Scope
- **Depth:** deep · **Reads:** 21 · **Globs/Greps:** 16 · **Code-graph queries:** 2 (`ts` plane, `db_state: fresh`)
- **Harness rules consulted:** `.claude/rules/verification-harness.md` — read in full as a STRUCTURAL extraction (60 lines / 58,643 bytes; `## Session Additions` indexed at 21 per-entry introducers, spans L49 + L58 read offset-bounded). **This rule does not auto-load for this chunk** — its `paths:` are `scripts/agent-run.*` · `conductor-cli/**` · `conductor-verify/**` · `crates/**/tests/**`, and the webview specs live at `crates/conductor-tauri/ui/test/` (singular `test`). Its retrieval rides this slot, exactly as the rule's own caveat states. Also indexed: `.claude/rules/a11y.md` (41 lines), `.claude/rules/frontend.md` (53 lines).

## Files inspected
- `.github/workflows/ci.yml` (full map + `:43-58`, `:68-79`, `:188-213`) — TWO jobs, `rust` (`:16`, `runs-on` `:18`) and `frontend` (`:188`, `runs-on` `:190`), both `windows-latest`. No a11y job. The rust job already builds the webview bundle (`:43-47`, `npm ci && npm run build`) and dogfoods `.\scripts\agent-run.ps1 run` (bare — no stage flag, so no wdio leg) under `$PSNativeCommandUseErrorActionPreference = $true`. Supply-chain steps at `:86` (`cargo audit`) / `:89` (`cargo deny`); frontend job runs `npm ci` → `npm audit --omit=dev` → `npm run build` with `working-directory: crates/conductor-tauri/ui`.
- `crates/conductor-tauri/ui/wdio.conf.ts` (`:54-70`, `:131-160`, `:213-230`, `:228-300`) — ONE config, ONE stack. `UNSAFE_PATH = /[;&|\`$<>\r\n"']/` (`:54`); `nativeDriver()` (`:56`) and `nvdaExe()` (`:150`) return `undefined` on unset-or-unsafe. Default `specs: ['./test/a11y/accessibility.e2e.ts']` (`:230`) = the routine arm; `suites` (`:231`) = `driven` → `operator-hold.e2e.ts`, `sr`/`sr-empty`/`sr-error` → `screen-reader.e2e.ts`. **The skip is `process.exit(0)` inside `onPrepare` at `:269` (driver) and `:288` (NVDA)**, with the in-file comment stating the intent: "a throw here would surface an unconfigured host as a failed gate — the one reading the guard must prevent."
- `crates/conductor-tauri/ui/test/a11y/accessibility.e2e.ts` (`:12`, `:72`, `:145`, `:164-306`) — the routine spec: **13 `it()` blocks**, `AxeBuilder` + `withTags(WCAG_TAGS)` (`:72`), `colorjs.io` token pairs, colour-alone, compiled reduced-motion rule (`:145`, `:224`). **`browser.keys` occurrences: 0.** Two specs context-skip: `:293` (operator-pause dialog, guard `[role="alertdialog"]` at `:294`) and `:299` (operator-checklist, guard `[role="status"]` at `:300` — CARRY 2's target).
- `crates/conductor-tauri/ui/test/a11y/operator-hold.e2e.ts` (`:78-100`) — carries the keyboard/focus-trap traversal (`browser.keys('Tab')`, `focusInsideDialog()`, Space toggle). This is the `driven` suite.
- `scripts/agent-run.sh` (`:186-198`) / `scripts/agent-run.ps1` (`:206-225`) — the `--e2e` arm: `ensure_frontend` → `cargo build --release -p conductor-tauri --features tauri/custom-protocol` → `npm run a11y`. The `.sh` arm rides `set -euo pipefail`; the `.ps1` arm reads `$LASTEXITCODE` and `exit`s it. Both propagate npm's status.
- `.andromeda/a11y-plan.md` (`:94-110`, `:228-262`, `:218`, `:424`, `:565`, `:597`) — the violation schema, verbatim, twice.
- `.andromeda/test-plan.md` (`:68`, `:184`, `:189`, `:191`) — the envelope shape and the self-obs/journal separation rule.
- `crates/conductor-tauri/ui/package.json` (`:16`, `:24`, `:42`) · `crates/conductor-tauri/ui/test/README.md` (`:8`, `:12`, `:26`) · `scripts/code-graph-cookbook.md` (`:17-25`, `:46-70`).

## Graph impact (from the code-graph query)
- **Query 1 — existence**, `rows: 8`: every guard symbol is indexed on the `ts` plane (`nativeDriver` `:56`, `seedFixtureRuns` `:90`, `invokedSuites` `:135`, `nvdaExe` `:149`, `reportNvdaSkip` `:155`, `reportSkip` `:213`, `config` `:223`), all in `wdio.conf.ts`.
- **Query 2 — impact**, `rows: 6`: every caller of every guard function is `wdio.conf.ts` itself, inside `onPrepare` (`:263`, `:267`, `:270`, `:284`, `:286`). **Zero cross-file callers.** A strict-mode change to the guard arms therefore has zero blast radius outside that one file — no threading, no registration sites, no spec-side plumbing.

## Patterns detected
- **Skip-at-exit-0 is deliberate and documented at the site** (`wdio.conf.ts:265-269`): exit rather than throw, because wdio has no "skip the whole run" result and a throw would read as a failed gate on an unconfigured dev host. Any CI-strict behaviour must be an ADDED arm over this, not a rewrite of it (matches arch's "amendment, not a silent swap" and security's "assertion outside the guard, never relaxing it").
- **CI already knows how to build the webview subject** (`ci.yml:43-47` + `agent-run.{sh,ps1}` `--e2e`): bundle first, then `cargo build --release -p conductor-tauri --features tauri/custom-protocol`. The feature — not the profile — decides bundle embedding.
- **A named `ci.yml` step is the established gate shape** (`ci.yml:79` coverage-completeness, `:144` obs conformance, `:172` run-journal conformance): red on violation, no §9 stage-table row required (operator precedent at `2026-09-06-coverage-completeness-gate`).
- **The committed machine-readable a11y record already has a template** — `test/a11y/screen-reader/parse-nvda-log.ts` (`writeNvdaPass`): closed enums, bounded text, host-path → `<host-path>` plus a `security_finding` row.

## Conventions to follow
- **Violation JSON = the eleven-field run-report envelope, verbatim** (`a11y-plan.md:228-250`): `journal_emitted_at`, `read_back_observed_at`, `run_id`, `seed`, `scenario`, `p_ids`, `verdict`, `state`, `latency_ms`, `slo_tier`, `fingerprints`. `state = Fail` when `violations.length > 0` else `Pass`; axe `id` + `impact` + WCAG `tags` + node `target` ride inside `fingerprints[]`; **no new top-level fields**; selectors DOM-relative / `data-testid`, never filesystem paths; `service.name` ∈ {`conductor-tauri`, `conductor-ui`} + `deployment.environment` tag each artifact.
- **Assert the SPEC LIST, never `$?`** (`verification-harness.md:58`, measured 2026-09-02) — see Open question 1.
- **Selector discipline** (`test-plan.md §6`): role / text / `data-testid` / `aria-live`; never xpath, never hashed classes. The corrected CARRY-2 guard follows the in-file precedent at `accessibility.e2e.ts:294`.

## New files to create
- `crates/conductor-tauri/ui/knip.json` — knip entry-point config declaring `wdio.conf.ts` + `test/a11y/**/*.e2e.ts` as entries (knip ships a `wdio` plugin for exactly this), retiring CARRY 5's 20/20 false positives.

## Files to modify
- `.github/workflows/ci.yml` — add the a11y job/step (build bundle → build the `custom-protocol` binary → install tauri-driver → run the routine arm → assert the spec tallies → upload the violation JSON).
- `crates/conductor-tauri/ui/wdio.conf.ts` — the CI-strict arm over the `:269` skip (added arm, guard untouched). **Only file in the graph's caller set; no threading files.**
- `crates/conductor-tauri/ui/test/a11y/accessibility.e2e.ts` — CARRY 2: `:300` guard `[role="status"]` → `[role="alertdialog"]`.
- `crates/conductor-tauri/ui/test/README.md` — CARRY 1: the falsified platform verdict at `:8`, `:12`, `:26`.
- `crates/conductor-tauri/ui/package.json` (+ `package-lock.json`) — only if a knip wdio plugin is a separate package; `npm audit --omit=dev` must stay at 0 production vulns.
- `scripts/agent-run.{sh,ps1}` — only if the spec-tally assertion lands in the shells (see Open question 1); identical semantics both shells, command count stays five.

## Scope premise closure
All 17 `[inferred]` bullets in `scope.md` re-checked. **VERIFIED (tag dropped):** the without-Pulse boundary; the one-stack ban; the `ci.yml` job coordinates; the spec-file set; `wdio.conf.ts` as the single config; the npm scripts; `v2-24`'s matrix state; the block's live half; CARRY 1's three sites; CARRY 2's target + `:294` precedent; `cmdk ^1.1.1`; CARRY 4's four line numbers (all correct); CARRY 5's every particular. **PREMISE-CORRECTED:** (a) the BLOCKED-ON's `ci.yml:164` and a11y-plan `:218`'s `ci.yml:178` both name a frontend job that now sits at `:188`; (b) the BLOCKED-ON's "49 commits ahead / CI not run in a month" is dead — origin at `e21361a`, 0 ahead / 0 behind, CI green `34128631720`; (c) CARRY 2's "worked around by taking `aria-live`" is wrong — `:294` took `[role="alertdialog"]`, and the specs carry no `aria-live` guard (sole `aria-live` mention is a comment at `:12`). None of the corrections changes the work; all three sharpen it.

**Mechanism-claim re-derivation.** CARRY 3's three claims are `measured at`-marked and their evidence pointers spot-check still-true at HEAD (`rows.ts:303`/`:311`, `parse-nvda-log.ts:55`/`:64`/`:459`). They bear on the SR leg, which is CI-ineligible either way, so none is load-bearing for this chunk's design — recorded, not re-measured. CARRY 5's mechanism (WebdriverIO discovers specs by config, not import, so knip's graph cannot see them) is re-established structurally at HEAD: `wdio.conf.ts:230-236` declares `specs`/`suites` as string paths, and no `import` of any `*.e2e.ts` exists anywhere in the package.

## Open questions
1. **Does the gate assert the spec list itself, and does that assertion land HERE or at the sibling entry?** → blocks: **plan-decision**. `verification-harness.md:58` measured that `run --e2e` exits 0 both on a full pass and on a total skip, prescribing "read the SPEC LIST … reconcile passing/skipped tallies". a11y-plan §11 forbids a gate that can pass by skipping, so this chunk's gate is hollow without it — yet the *Release build and bundle* entry carries a CARRY asking for exactly this assertion in both shells. Overlapping ownership; P4 must resolve who lands it.
2. ~~**Is `msedgedriver` present on the `windows-latest` runner, and at what path?**~~ → **CLOSED at the P5 review (2026-09-07).** Measured against `actions/runner-images` (main): `windows-latest` = Windows Server 2025, shipping Microsoft Edge 152.0.4191.53 + Microsoft Edge Driver 152.0.4191.53 at `EDGEWEBDRIVER=C:\SeleniumWebDrivers\EdgeDriver` (windows-2022 at 151.0.4129.x). No install step; the job pins `windows-2025` explicitly. The residual `hypothesis:` is the WebView2 Evergreen runtime's presence, with the fallback order fixed in `plan.md` §Implementation notes.
3. **Can a without-Pulse gate satisfy a11y-plan §11's POUR requirement?** → blocks: **plan-decision**. Measured: the routine arm has **zero** `browser.keys` calls — all Operable coverage (keyboard traversal, focus trap, restoration) lives in `operator-hold.e2e.ts` = the `driven` suite = live-Pulse = CI-banned by test-plan §11 and arch §CI/CD. §11 Anti-Patterns → Strategy requires both contrast AND keyboard on every must-be-accessible path. Either a recorded carve-out or a plan amendment is needed; neither is phase's to author. Related: the two routine specs that context-skip (`:293`, `:299`) are the "expected skips" the tally reconciliation must whitelist — and per `test-plan §6` a context-skip is never a pass.
