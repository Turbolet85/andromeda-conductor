# Plan — 2026-09-07-dependency-polish: "Dependency polish"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-09-07-dependency-polish · **Version:** conductor-0.2.0 · **Generated:** 2026-09-07T11:10Z

## Goal

Two dependency moves the route entry names, plus seven folded CARRYs of accumulated upkeep. The indicatif
bump goes to **0.18.6**: design-system §Surface: cli / Toolkit-Framework already names `indicatif 0.18` as
target state, and the 2026-09-03 amendment assigns that line to this entry by name. Research measured the
risk away — all four indicatif APIs Conductor uses are signature-identical across 0.17.11 and 0.18.6,
`suspend` included, and `suspend` *is* the stop-in-place freeze that design-system §Component Patterns 1 and
layout-templates §Signature placement both mandate. The `opentelemetry-proto` trim lands
`default-features = false` at **both** dep sites (per obs-plan §3's Transitive note as extended by the
folded CARRY), and ships the evaluation's honest result: the trim cannot drop the dormant
`opentelemetry`/`opentelemetry_sdk` crates, because at 0.32.0 the `trace`/`logs` features gate the generated
message modules `conductor-emit` imports *and* the SDK transforms through one flag. What it does drop is
`metrics`, `zpages`, `with-serde` (→ `serde`, `const-hex`, `base64`) and `internal-logs`. (`profiles`
is NOT in the drop list: `full` does not include it and `profiles = []`, so it was never enabled.)

The CARRY freight is four doc-vs-artifact reconciliations (tokio → the lock's 1.52.3, Tauri → 2.11.3,
`deny.toml`'s exception counts, the 9-crate workspace), the `conductor-verify` standalone-build red that
architecture §Established Decisions [Module Boundaries] names this entry as owner of, `conductor-run`'s seven
rustdoc intra-doc link warnings, `knip` on the never-served web plane, and Pulse's eighth P-047 category.

This chunk **claims `verification-matrix.json#v2-32`** (Dormant dependency maintenance), whose acceptance
this work satisfies clause-for-clause. It runs entirely offline — no live Pulse, no scenario, no read-back.

## Implementation Steps

**Order is load-bearing: step 1 decides step 8's content.** The indicatif bump determines whether
RUSTSEC-2025-0119's `deny.toml` ignore still has a subject, so the supply-chain reconciliation must read the
post-bump tree, never the pre-bump one.

1. **Bump indicatif to 0.18** — change the `[workspace.dependencies]` pin at `Cargo.toml:64` from `"0.17"`
   to `"0.18"` and refresh `Cargo.lock`. `crates/conductor-cli/Cargo.toml:21` keeps `indicatif.workspace = true`.
2. **Verify the spinner surface compiles unchanged** — `crates/conductor-cli/src/render.rs:138-149` and
   `crates/conductor-cli/src/pause.rs:26/40/77` are the only two files touching the crate. Expect no edit:
   research measured `ProgressBar::with_draw_target`, `enable_steady_tick`, `suspend` and
   `ProgressStyle::with_template` byte-identical between versions. **If an edit IS required, it may not
   change the freeze**: `pause.rs:40`'s `bar.suspend(render_and_prompt)` stays a `suspend` call.
3. **Add a test pinning the stop-in-place freeze** in `crates/conductor-cli/src/pause.rs`'s existing
   `#[cfg(test)]` module: assert `PromptResolver` holding `Some(bar)` routes its render+prompt closure
   through `ProgressBar::suspend` (the bar's position is unchanged across the call, and the bar is neither
   finished nor cleared). This is v2-32's spinner clause made assertable rather than argued.
4. **Trim `opentelemetry-proto` at `crates/conductor-emit/Cargo.toml:10`** — add `default-features = false`,
   keeping `features = ["gen-tonic", "trace", "logs"]` exactly as they are. `trace` and `logs` are
   non-optional (they gate the `tonic::trace::v1` / `tonic::collector::trace::v1` / `tonic::logs::v1` /
   `tonic::collector::logs::v1` modules the crate imports); `metrics` stays absent, as it is today.
5. **Trim the second dep site at `crates/conductor-run/Cargo.toml:33`** — the dev-dep is bare
   `opentelemetry-proto.workspace = true` today and therefore rides `default = [full]`, which workspace
   feature unification would push back onto `conductor-emit`. Give it `default-features = false` and
   `features = ["gen-tonic", "trace", "logs"]` — **all three are required**. `tests/canary_wire.rs`,
   `tests/dispatch_wire.rs` and `src/canary.rs:305-333` import `tonic::collector::trace::v1`,
   `tonic::trace::v1` and `tonic::common::v1`; and `tests/common/mod.rs:12` imports
   `tonic::collector::logs::v1`, which is what makes `logs` mandatory rather than conditional. (Research's
   import scan globbed `tests/*.rs` and missed `tests/common/mod.rs`, so the earlier "add `logs` only if the
   compile demands it" was wrong — it is demanded.) Do not add `metrics`.
6. **Fix the `conductor-verify` standalone build** — add `"time"` to the tokio feature list in
   `crates/conductor-verify/Cargo.toml:10` (`[dependencies]`), the feature `src/preflight.rs:336`'s
   `tokio::time::sleep` needs. Leave `[dev-dependencies]:31` alone: `test-util` staying dev-only is what
   keeps obs-plan §11's virtual-clock ban structurally enforced.
7. **Sweep every other workspace member for the same dev-only-feature masking** — architecture
   §[Module Boundaries] requires it explicitly ("the same sweep for any crate whose feature is dev-only").
   **Sweep each member on ITS OWN targets, not uniformly `--lib`:** `conductor-cli` and `conductor-tauri`
   have no library target (measured — `src/lib.rs` absent, `src/main.rs` present, `conductor-cli` declares
   `[[bin]]`), so `--lib` exits 101 "no library targets" on both and a uniform loop reports two false reds.
   Use `--lib` for the seven library crates and `--bins` for those two. **Never `--all-targets`** — it
   re-unifies dev-dependencies, which is the exact masking this sweep hunts. `hypothesis:` the
   `conductor-tauri --bins` check may need `ui/dist` present, because `tauri::generate_context!` resolves
   `build.frontendDist` at compile time — measure that before recording any red there, and run
   `npm --prefix crates/conductor-tauri/ui run build` first if so. Fix any member that genuinely fails alone
   by declaring the missing feature in its own `[dependencies]`. Record the per-member result — a clean
   sweep is the finding that lets architecture's E1 qualifier be narrowed at wrap.
8. **Reconcile the supply-chain exception prose, reading the POST-bump tree.** `number_prefix 0.4.0` has
   exactly one dependent in the current lock (`indicatif 0.17.11`) and is absent from 0.18.6's dependency
   list, so after step 1 RUSTSEC-2025-0119's `[advisories] ignore` loses its subject. **Remove that entry
   from `deny.toml`.** **Authority — `deny.toml`'s own text, not a master rule.** Its `[advisories]` header
   (`deny.toml:6-7`) states that accepted advisories are listed "**WITH a justifying comment** — never a
   silent skip", and this entry's comment (`:9-11`) justifies it as "`number_prefix 0.4.0` (**indicatif
   0.17** → conductor-cli progress rendering)". Once the bump removes that path the comment justifies
   nothing, so the entry can no longer satisfy the header's own condition. A follow-up recorded at
   `security-plan-amendments.md:33` anticipated the retirement ("a future `indicatif` 0.18 bump may drop
   `number_prefix`"), which is a pointer, not an obligation. **No security-plan §Dependency Security or
   §Security Anti-Patterns rule requires this** — see Implementation notes. Then record the reconciled
   counts (expected: 16 ignores + 9 allows) as the shape for wrap. **If the lock still carries
   `number_prefix` after the bump, do NOT remove the entry** — record the measurement instead.
9. **Fix the seven rustdoc intra-doc links in `conductor-run`** — re-path or `crate::`-qualify at the link.
   Confirmed sites: `src/execute.rs:28` (`[\`persist\`]`), `src/preconditions.rs:56` (`[\`canary_gate\`]`),
   `src/preconditions.rs:62` (`[\`observe_run_contract\`]`, a public-doc-links-to-private-item). The
   remaining links are named by the gate's own output; fix each where it is reported. Public API is
   unaffected — doc comments only, no signature or item change.
10. **Add `knip` to the web plane** — a devDependency plus a `knip` script in
    `crates/conductor-tauri/ui/package.json`, with `package-lock.json` moving alongside. Do NOT rename or
    displace the five existing a11y script names (`a11y`, `a11y:driven`, `a11y:sr`, `a11y:sr-empty`,
    `a11y:sr-error`). Do NOT wire it into CI — this chunk adds no gate. **Run it once and disposition every
    finding in writing without deleting anything**: a11y-plan §11 forbids acting on a static dead-code
    report against harness dependencies, and design-system §Component Patterns 7 protects the
    Operator-checklist primitive, whose run-report mount is designed-but-not-built.
11. **Add the eighth `PiiCategory`, appended LAST.** In `crates/conductor-emit/src/pii.rs`: a new
    `ProviderKey` variant appended after `SecretKeyValue`; `all() -> [PiiCategory; 8]`; a `field_key()` arm;
    a `generate()` arm; `PiiCorpus { values: [String; 8] }`; an eighth sequential draw at the END of
    `seeded()`'s array. **Append-last is mandatory, not stylistic** — `seeded()` draws every value from one
    `ChaCha8Rng` in discriminant order (`pii.rs:97-106`), so a mid-order insert changes every later corpus
    value and breaks every committed expectation.
12. **Give the new variant an UNCOVERED provider-key shape.** Pulse's `provider_key` regex
    (`andromeda-pulse crates/security/src/scrubber.rs:99` @ `83d4060`) matches
    `[sr]k_(?:live|test)_[A-Za-z0-9]{16,}` among five alternatives, and Conductor's existing `ApiKey`
    already emits `sk_live_` + 24 alnum — i.e. a value Pulse classifies as `provider_key`, not `api_key`.
    Generate one of the shapes Conductor does not yet cover: `ghp_` + 36 alnum, or `AKIA` + 16 uppercase
    alnum. **Do NOT re-shape the existing `ApiKey` generator** — it would move committed corpus values,
    which is exactly what append-last exists to prevent.
13. **Update the arity pins the compiler will not catch.** `pii.rs:325` and `:329` (the arity test's two
    `assert_eq!`, and its NAME `exposes_exactly_seven_distinct_categories`), `:444` (span-attribute count),
    `:486` (log-record count), `crates/conductor-emit/tests/pii_payload_corpus.rs:155`, and
    `crates/conductor-run/tests/pii_harvest.rs:191`
    (`assert_eq!(needles.len(), 33, "4 corpora x 7 values + 5 stable affixes")` → 37, or 38 with a new
    affix added at `:188`). **Do NOT touch** `PiiCorpus::seeded(7)` at `pii.rs:334`/`:437`/`:482`/`:506` —
    that 7 is a SEED, numerically equal to the arity by coincidence. **Do NOT touch** `pii_harvest.rs`'s
    `rows_appended: 7` at `:96`/`:97`/`:150`/`:151` — those are historical witness values byte-identical to
    the 2026-08-19 capture and stay 7 permanently.
14. **Record the doc reconciliation shapes** (values only; the amendment write is wrap's — see
    Implementation notes). No `.andromeda/` master is edited by this chunk.

## Codebase touchpoints

### New files
- None.

### Files to modify
- `Cargo.toml` — indicatif pin `0.17` → `0.18` (step 1).
- `Cargo.lock` — moves; stays committed and un-drifted.
- `crates/conductor-emit/Cargo.toml` — `default-features = false` on `opentelemetry-proto` (step 4).
- `crates/conductor-run/Cargo.toml` — same at the dev-dep site, with explicit features (step 5).
- `crates/conductor-verify/Cargo.toml` — `"time"` into `[dependencies]` tokio features (step 6).
- Any member the step-7 sweep finds failing standalone — manifest only.
- `deny.toml` — remove the stranded RUSTSEC-2025-0119 ignore (step 8, conditional on the measurement).
- `crates/conductor-cli/src/pause.rs` — the freeze-pinning test (step 3); production code only if step 2
  finds an API delta, which research predicts it will not.
- `crates/conductor-run/src/execute.rs`, `src/preconditions.rs`, and any further site the rustdoc gate
  names — doc comments only (step 9).
- `crates/conductor-tauri/ui/package.json` + `package-lock.json` — `knip` devDep + script (step 10).
- `crates/conductor-emit/src/pii.rs` — the eighth variant and its arity pins (steps 11–13).
- `crates/conductor-emit/tests/pii_payload_corpus.rs` — arity pin at `:155` (step 13).
- `crates/conductor-run/tests/pii_harvest.rs` — `:188` affix list and `:191` needle count only (step 13).

## Test Commands

No live leg: this chunk drives no external process, so no firing-form invocation and no stop form are owed.
No headful UI gate either — the webview self-verify's subject (`ui/dist`) is untouched, and the one surface
this chunk does change is the cli, which a11y-plan §1/§11 record as not accessibility-assertable and for
which no headful driver exists. Recorded, not silently skipped.

```bash
# --- supply chain: the porcelain probe PRECEDES any external classification (security-plan §Dependency Security)
git -C "${CARGO_HOME:-$HOME/.cargo}/advisory-db" status --porcelain   # must print nothing
cargo audit                                                            # expect exit 0
cargo deny check advisories bans licenses sources                      # expect exit 0
git status --porcelain -- Cargo.lock                                   # must print nothing (un-drifted)

# --- the standalone per-seam build: currently RED on conductor-verify, the discriminating gate for step 6
cargo check -p conductor-verify --lib
# Per-target sweep: the seven library crates on --lib, the two bin-only crates on --bins.
# conductor-cli and conductor-tauri have NO lib target, so --lib exits 101 there (false red).
# --all-targets is banned here: it re-unifies dev-deps, the exact masking this hunts.
for p in conductor-core conductor-timeline conductor-emit conductor-faults conductor-verify \
         conductor-report conductor-run; do
  cargo check -p "$p" --lib || echo "STANDALONE-RED: $p"
done
for p in conductor-cli conductor-tauri; do
  cargo check -p "$p" --bins || echo "STANDALONE-RED: $p"
done

# --- runner portability: BOTH runners green per test-plan §4 / §10
cargo nextest run --workspace --profile ci
cargo test -p conductor-emit
cargo test -p conductor-run
cargo test -p conductor-verify
cargo test -p conductor-cli
cargo test --doc

# --- lint
cargo clippy --workspace --all-targets -- -D warnings

# --- rustdoc: a bare `cargo doc` exits 0 even WITH warnings (measured at HEAD: exit 0 over
#     `generated 7 warnings`). RUSTDOCFLAGS promotes them to errors, so the EXIT CODE carries the
#     verdict and no log file or grep is needed — nothing is written outside the gitignored target/.
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps -p conductor-run   # expect exit 0

# --- secret hygiene: no full corpus VALUE committed (affixes are committed by design — see criteria)
git grep -nE 'sk_live_|sk-proj-|ghp_|AKIA' -- crates scenarios contracts scripts

# --- web plane
npm --prefix crates/conductor-tauri/ui audit --omit=dev   # 0 production vulnerabilities
npm --prefix crates/conductor-tauri/ui run knip           # run once; disposition findings, delete nothing
npm --prefix crates/conductor-tauri/ui run typecheck
npm --prefix crates/conductor-tauri/ui run build

# --- harness smoke (test-plan §3 5-command discipline; no live Pulse needed)
bash scripts/agent-run.sh status
```

## Acceptance Criteria

- (matrix) `verification-matrix.json#v2-32` — indicatif resolves at **0.18** with the operator-pause spinner
  still stopping in place (never hiding, never animating to completion), `opentelemetry-proto` builds with
  `default-features = false`, `Cargo.lock` is un-drifted and cargo-audit plus cargo-deny stay green.
  **`ref` prescription for /implement (P2, on green).** `acceptance` is already concrete and was
  premise-checked at P5 — do not reword it. Phase owns `chunk`, implement owns `ref` (contract §Location &
  ownership), so write `ref` as ONE string naming the test path first and the by-construction half beside it,
  so the wrap light gate re-proves every clause:
  `crates/conductor-cli/src/pause.rs::<the step-3 freeze test> (spinner clause, asserted); by-construction: Cargo.lock resolves indicatif 0.18.x; conductor-emit + conductor-run both declare default-features = false on opentelemetry-proto; cargo audit + cargo deny check exit 0 over the committed un-drifted lock`
  — substituting the real test name. The spinner clause is the only one a test asserts; the other three are
  gate outcomes the listed Test Commands produce.
- (arch) `cargo check -p conductor-verify --lib` exits 0 with tokio's `time` declared in that crate's own
  `[dependencies]`, and all nine members are swept EACH ON ITS OWN TARGETS — `--lib` for the seven library
  crates, `--bins` for `conductor-cli` and `conductor-tauri`, which have no lib target — with the per-member
  result recorded (per architecture.md §Established Decisions [Module Boundaries]).
- (arch) Over the moved, committed, un-drifted lock: `cargo deny check advisories bans licenses sources`
  exit 0 and `cargo audit` exit 0, with `git status --porcelain` on the advisory-db checkout read BEFORE any
  parse failure is classified external (per architecture.md §Infrastructure Patterns — Build system).
- (arch) After `default-features = false`, `conductor-emit` still builds the raw OTLP message structs and
  the tonic clients; the retained feature list is `["gen-tonic","trace","logs"]` and any divergence from the
  set §Stack registers is surfaced as registry drift rather than absorbed (per architecture.md §Stack).
- (design) The operator-pause spinner stops in place at the exact count value — not hidden, not cleared, not
  animated to 100% — asserted by the step-3 test rather than argued (per design-system.md §Surface: cli /
  Component Patterns 1).
- (layouts) Post-bump cli output stays line-oriented and pipe-safe, the spinner is still `ProgressDrawTarget`-
  gated on `stderr().is_terminal()`, and the headless `agent-run` legs complete without blocking on a prompt
  (per layout-templates.md §Surface: cli — Expression level · §IA notes Headless invariant).
- (layouts) The chunk mints no new bracket label, lamp, `ReportState`, ANSI entry or token; the eighth
  `PiiCategory` is corpus data, not a rendered state (per layout-templates.md §Surface: cli — Component
  Primary content block 2).
- (tests) Every touched crate is green under BOTH runners — `cargo nextest run --workspace --profile ci` and
  `cargo test -p <crate>` for `conductor-emit`, `conductor-run`, `conductor-verify`, `conductor-cli`
  (per test-plan.md §4 / §10).
- (tests) No committed golden changes; same scenario + seed still reproduces the same stream shape. The three
  `dispatch_wire__*` snapshots carry no PII corpus values, so the eighth category must not move them
  (per test-plan.md §7 / §2).
- (obs) After the trim, `conductor-emit`'s emission surface still compiles and its suite is green
  (`cargo test -p conductor-emit` + the workspace nextest run), and no OTel SDK is initialized or used for
  self-observation. **By-construction for the span half:** no step touches `emit.batch` /
  `emit.logs_batch` or their §6 wire-shape witness, and the witness is a `debug`-gated line requiring the
  additive `RUST_LOG=info,conductor_emit=debug` form that no gate here runs — so the plan asserts the
  emitters' compilation and tests, and records the span names as untouched rather than claiming a
  measurement no listed command makes (per obs-plan.md §3 OTel SDK init + §11 Spans/Traces).
- (obs) `conductor-cli` still declares **no `tracing` dependency** after the indicatif work — checked in
  `crates/conductor-cli/Cargo.toml`, which keeps obs-plan §4's cli row and §6's per-module row true. **This
  is the assertable form**: the self-obs stream's JSON-per-line shape is produced only by an agent-mode run,
  which no gate here invokes, so the criterion asserts the dependency edge the steps could break rather than
  a stream no listed command emits (per obs-plan.md §4 Span/Trace Coverage · §6 Log Coverage · §11 Logs).
- (security) `npm audit --omit=dev` reports 0 production vulnerabilities with `package-lock.json` committed
  after `knip` lands (per security-plan.md §Dependency Security, Frontend (npm) supply chain).
- (security) No full corpus VALUE is committed, and the eighth category follows the existing generator
  convention — a committed AFFIX with an RNG-derived body. Producer: the listed
  `git grep -nE 'sk_live_|sk-proj-|ghp_|AKIA' -- crates scenarios contracts scripts`; every hit it returns is
  read and must be an affix constant or an assertion on one, never a whole generated value
  (per security-plan.md §Secret Management and §Security Anti-Patterns → Secrets). **Refined from the
  security extract's contribution**, which asked that no `sk_live_`/`ghp_`/`AKIA` literal appear in any
  committed file; research measured that already false at HEAD by design (`pii.rs:245`, `:346`,
  `pii_harvest.rs:188` — 5 hits in 3 tracked files before any eighth category lands).
- (a11y) `crates/conductor-tauri/ui/package.json` + `package-lock.json` still declare and resolve
  `axe-core@4.12.0`, `@axe-core/webdriverio`, `colorjs.io@0.6.1`, `lighthouse@13.0.3`, `webdriverio` and
  `@crabnebula/tauri-driver`, and the five a11y script names are present and unrenamed after `knip` lands
  (per a11y-plan.md §3 Bootstrap phases · §9 CI Integration → E2E row).
- (a11y) Every `knip` finding naming an a11y harness dependency or spec file is dispositioned in writing
  rather than acted on by deletion (per a11y-plan.md §11 Anti-Patterns → Strategy + CI).
- (tests) `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps -p conductor-run` exits 0 (baseline at HEAD: the
  bare command exits 0 while printing `generated 7 warnings`, which is why the flag carries the verdict).

## Provenance
- Spec anchors: architecture.md §Stack and Technologies · §Established Decisions [Module Boundaries] ·
  [OTLP Emission Strategy] · [Determinism RNG] · §Infrastructure Patterns — Build system · §Occupied Resources;
  security-plan.md §Dependency Security · §Secret Management · §Security Anti-Patterns;
  design-system.md §Surface: cli / Component Patterns 1 · / Tokens · / Toolkit-Framework · §Motion;
  layout-templates.md §Surface: cli — Signature placement · Expression level · IA notes · Tooling context;
  test-plan.md §2 · §4 · §7 · §9 · §10 · §11; obs-plan.md §3 · §6 · §9 · §11; a11y-plan.md §3 · §9 · §11.
- Research files inspected: `research.md` §Files inspected (21 reads incl. three vendored crates and the SUT
  repo at `83d4060`), §Graph impact (1 rust-plane query, `rows: 13`, `db_state: fresh`).

## Constraints & rejected approaches
- **Rejected: re-shaping the existing `ApiKey` generator to a key=value form** to match Pulse's `api_key`
  regex — it would move committed corpus values for every seed, which append-last exists to prevent, and the
  gain is unverifiable here (no live leg). Recorded as a finding instead.
- **Rejected: trimming `conductor-emit` alone** — workspace feature unification pushes `conductor-run`'s
  dev-dep `default = [full]` straight back onto it. Both sites or neither.
- **Rejected: dropping `trace` or `logs` to shed the dormant SDK crates** — those same two features gate the
  generated message modules `conductor-emit` imports. The dormant footprint is not removable at 0.32.0; the
  evaluation's result is the deliverable.
- **Rejected: adding a rustdoc gate to CI or the harness shells.** The CARRY asks for the links to be fixed,
  not for a gate, and this chunk's scope carries "No new gate". The 2026-09-06 precedent (a chunk shipping a
  CI gate step took no §9 stage row) is about a gate the route asked for; none is asked for here.
- **Do NOT act on `knip` output by deleting.** a11y-plan §11 bans it for harness deps; design-system
  §Component Patterns 7 protects the Operator-checklist primitive; obs-plan §1 protects the frontend's
  console-JSON telemetry exports. Disposition in writing, delete nothing.
- **Do NOT amend any `.andromeda/` spec master.** Every reconciliation is recorded as an Expected amendment.

## Implementation notes

**Decisive leans taken at P4** (no operator round; each rests on an artifact that already answers it):
- **`deny.toml` is writable after all**, against scope's P1 draft which called it read-only — but the
  authority is `deny.toml`'s OWN text, not a spec master. **Correction made at the P5 review:** the P4 lean
  cited "security-plan §Dependency Security forbids leaving an ignore whose subject a bump removed". **No
  such rule exists.** security-plan.md carries exactly four `ignore` lines — `:119` (screen-reader speech
  log), `:176` (the advisory-DATABASE fault remedy), `:181` (the Accepted-exceptions paragraph, which
  RECORDS the `number_prefix` ignore), `:211` (the secret-scanning gate) — and none states the rule. The
  sentence exists only in this run's security extract (`.andromeda/runs/2026-09-07T10-43-05-phase/security.md:23`),
  where the distiller anchored an inference to the master. Re-based on what does exist: `deny.toml:6-7`
  requires every accepted advisory to be listed WITH a justifying comment, and `:9-11`'s comment justifies
  this one via `indicatif 0.17` — a justification the bump voids. `security-plan-amendments.md:33` records a
  Follow-up anticipating the retirement, which is a pointer, not an obligation. **Class:** the 2026-08-09 T1
  rule's own case — grep A before asserting A says X, including when a distiller supplies the anchor.
- **All sites of each doc claim are reconciled, not only the CARRY-named arch sites** — leaned per the
  CARRY's own 2026-08-21 extension ("Fixing only the two obs-plan sites would leave test-plan stale, so treat
  the site list as three").
- **indicatif targets 0.18** — leaned per design-system §Toolkit-Framework, which names it as target state.

**Stated numbers and their basis** (never a bare count):
- 13 `all()` call sites — `scripts/code-graph.py` rust-plane query, `rows: 13`, trace at
  `.andromeda/runs/2026-09-07T10-43-05-phase/tree-query-2026-09-07-dependency-polish.json`.
- 7 rustdoc warnings — `cargo doc --no-deps -p conductor-run` measured 2026-09-07, printed
  `generated 7 warnings` at exit 0.
- 17 advisory ignores / 9 license allows — `deny.toml` @ HEAD, unique-RUSTSEC-id count and allow-array count.
- 9 workspace members — `Cargo.toml` members list.
- `number_prefix` has exactly 1 dependent — `Cargo.lock` @ HEAD, dependency-block scan.
- Pulse's regex + case set — `andromeda-pulse crates/security/src/scrubber.rs:77`/`:99`/`:148-152` @ `83d4060`.

**Order sensitivity:** step 1 before step 8 (the bump decides whether the ignore is stranded); steps 1/4/5/6
before every supply-chain and runner gate (they all read the moved lock); step 11 before step 13 (the
compiler enumerates the exhaustive matches, the literals it will not).

**Expected amendments (wrap):**
- `Expected amendments (wrap): architecture.md §Stack and Technologies — indicatif 0.17 → the resolved 0.18`
- `Expected amendments (wrap): architecture.md §Stack · §Established Decisions [Language / Runtime] · §Inherited Defaults — tokio 1.48.x → the resolved lock version (sites :14, :42, :253)`
- `Expected amendments (wrap): architecture.md §Stack · §Infrastructure Patterns — Deployment model · §Inherited Defaults — Tauri v2.10.x / latest 2.10.1 → the resolved 2.11.3 (sites :27, :200, :260); the ≥2.10.3 FLOOR statements are untouched`
- `Expected amendments (wrap): security-plan.md §Dependency Security — the Accepted-exceptions paragraph reconciled to deny.toml's post-bump set, and the Tauri version statement at :84`
- `Expected amendments (wrap): obs-plan.md §1 Obs Scope Summary · §12 Decisions Log — "8 workspace crates" → 9 (sites :25, :654); §1 instrumentation-scope table — tokio 1.48.x → the resolved lock version (sites :32, :39)`
- `Expected amendments (wrap): obs-plan.md §3 OTel SDK init — the Transitive note's scope corrected to TWO dep sites, and its default-features follow-up closed with the measured result (the dormant SDK crates are not removable at 0.32.0)`
- `Expected amendments (wrap): test-plan.md §1 — "8 workspace crates" → 9 (site :25); §4 · §12 — tokio 1.48.x → the resolved lock version (sites :30, :434)`
- `Expected amendments (wrap): design-system.md §Surface: cli / Toolkit-Framework — indicatif 0.18 confirmed resolved (the line becomes true), and inquire 0.7 → the resolved 0.9.4`
- `Expected amendments (wrap): layout-templates.md §Surface: cli — Tooling context — indicatif 0.17 → the resolved 0.18`
- `Expected amendments (wrap): architecture.md §Established Decisions [Module Boundaries] — the E1 standalone-build qualifier narrowed, conditional on the step-7 sweep coming back clean`

**Pitfalls:**
- `cargo doc` exits 0 with warnings. The gate's asserted signal is the printed `generated N warnings` line.
- `PiiCorpus::seeded(7)` is a seed, not an arity. Four false positives on any bare `7` sweep near pii.
- `pii_harvest.rs`'s `rows_appended: 7` is a historical capture value and stays 7.
- The `knip` run's `package-lock.json` move must not disturb the six a11y-mandated devDeps or the five a11y
  script names.
