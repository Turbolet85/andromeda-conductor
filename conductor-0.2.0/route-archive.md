# Route archive — conductor-0.2.0

<!-- Verbatim working-route entry lines, captured at the wrap that flip-compacted them.
Writer: /andromeda-wrap-session P7 ONLY. Read by NO loop skill.
This is COLD HISTORY: each line is superseded at take-up by the chunk's scope.md fold, and at
flip by the master record's desc. NEVER cite it for current truth.
NOT a complete pin chronicle — tail annotations cleared before an entry froze live only in git
and in the wrap's P5 summary. -->

## 2026-08-08-sut-capability-manifest — archived at the 2026-08-20 wrap

[2026-08-08-sut-capability-manifest] SUT capability manifest — validated versioned refs/ Pulse artifact as the accepted P-ID source, replacing the compile-time 001..=060 bound, malformed reporting blocked

## 2026-08-09-sut-drift-check — archived at the 2026-08-20 wrap

[2026-08-09-sut-drift-check] SUT-drift check — loud failure when Pulse's ledger advances past the capability set Conductor knows   CARRY: the accepted set now ships as `contracts/pulse-capabilities.toml` (`sut_version`/`captured_at`/`capabilities[]`) with `CapabilityManifest::load` + `accepts` — build the drift check over THAT, don't re-source it

## 2026-08-08-dependency-advisory-remediation — archived at the 2026-08-20 wrap

[2026-08-08-dependency-advisory-remediation] Dependency advisory remediation — crossbeam-epoch ≥0.9.20 and quick-xml ≥0.41 through the Tauri tree, restoring cargo-audit and cargo-deny green

## 2026-08-09-current-sut-coverage-classification — archived at the 2026-08-20 wrap

[2026-08-09-current-sut-coverage-classification] Current-SUT coverage classification — capabilities in the manifest as auto, drive+observe, static-only or not-Conductor's, the boundary a recorded decision   CARRY: `coverage.rs` is a `&'static str` `[CapabilityRow; 60]` documented "no runtime IO" — manifest-sourcing it is a representation change, and its two exactly-sixty tests move with it   CARRY (from 2026-08-09-sut-drift-check): this chunk OWNS retiring `conductor_core::KNOWN_UNCLASSIFIED` — the drift check asserts the observed gap equals that pin by exact set, so classifying P-061..P-082 without shrinking the pin to `[]` fails `drift::tests::committed_artifacts_match_the_known_gap` with "already classified — shrink the known gap". The empty-pin shape is already proven by `in_sync_with_an_empty_known_gap_passes`, so this is a data edit, not a redesign   CARRY (doc wording, verified first-hand at that wrap, NOT that chunk's drift — dismissed under playbook:46): three specs still name the superseded 60 while the manifest carries 82 — `architecture.md:33` "60-P-ID coverage tables"; `design-system.md:7, :257` "all 60 capabilities" / "the full 60-row wall"; and `layout-templates.md` contradicts itself, `:37` reading "82 loaded (manifest set)" while `:121` still says "the full 60-row wall (P-001..P-060)" — residue of the 2026-08-08 de-hardcoding sweep, which updated the header strip but not the component prose. Reconcile all three here, where the classification becomes manifest-sourced

## 2026-08-09-out-of-scope-classification-treatment — archived at the 2026-08-20 wrap

[2026-08-09-out-of-scope-classification-treatment] Out-of-scope classification treatment — not-Conductor's rows distinct in coverage matrix, footer roll-up and CLI table, never Blocked or Fail   CARRY (from 2026-08-09-current-sut-coverage-classification): the mode itself SHIPPED — `CoverageMode::NotConductors` (wire `not-conductors`), 16 rows classified, rendering as text in the Markdown / CLI / webview surfaces and counted by `tally()`; this chunk owns only the VISUAL treatment. `layout-templates.md` §Component — Primary content block 1 now documents the Mode cell and its four values (added at that chunk's wrap by operator decision, accepting this chunk will revisit the same block) — EXTEND that entry with the out-of-scope treatment, don't re-author it. Nit to sweep while there: `crates/conductor-tauri/ui/src/components/CoverageMatrix.css:1` still comments "all 60 capabilities"

## 2026-08-09-interpretation-correctness-posture — archived at the 2026-08-20 wrap

[2026-08-09-interpretation-correctness-posture] Interpretation-correctness posture — one real-model leg with a known root cause and asserted top hypothesis, or a recorded deferral naming its owner   PREREQ (from 2026-08-09-out-of-scope-classification-treatment): **re-check `cargo audit`** at this chunk's gates. It exited 1 there on `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244` — the RustSec DB itself would not parse (reproduced with a fresh fetch AND with `-n`), against a zero-dependency-delta chunk with both lockfiles un-drifted, so it was taken as a BOUNDED DEFERRAL with `cargo deny check` (all four classes ok) as the overlapping signal. If it is green again, note the heal and close the deferral. **If it still fails, raise the cargo-audit FLOOR to the fixed release** — not a `deny.toml` entry, not a CI edit (`.claude/rules/security.md` 2026-08-09 + `playbook.md` external-decay rule). CI's own `cargo audit` step hits the same wall until upstream heals.

## 2026-08-09-in-lane-sut-scenarios — archived at the 2026-08-20 wrap

[2026-08-09-in-lane-sut-scenarios] In-lane SUT scenarios — live service truth, Investigate result and single-sourced workspace key (P-067, P-072, P-079)   PREREQ (from 2026-08-09-interpretation-correctness-posture): **re-check `cargo audit`** — still red at a THIRD consecutive check, and now proven to be an advisory-DATABASE fault, not a tool fault: installing the latest published 0.22.2 reproduced `duplicate advisory ID: RUSTSEC-2026-0244` byte-identically, so the duplicate id is committed data in RustSec's advisory-db and there is nothing to raise a floor to. Per the amended rule (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09 · security-plan §Dependency Security), the remedy is the **bounded wait alone** — re-run it, and verify `cargo deny check` actually ran green as the overlapping signal. Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses.   CARRY (from 2026-08-09-interpretation-correctness-posture): **P-073/P-074/P-079 sit in `conductor_core::UNBACKED_AUTO`** — the pin of `Auto`-classified capabilities no scenario names, asserted by `check_scenario_backing` at EXACT-SET equality. Authoring a scenario that names any of them WITHOUT removing it from the pin fails the gate with "in the unbacked ledger but now named by a scenario — shrink the ledger". Shrinking the pin is part of this chunk's work, not a follow-up.

## 2026-08-09-sut-load-envelope — archived at the 2026-08-20 wrap

[2026-08-09-sut-load-envelope] SUT load envelope — proven-good storm bounds for scenario authors plus environment-suspect flagging of over-envelope runs   PREREQ (from 2026-08-09-in-lane-sut-scenarios): **re-check `cargo audit`** — now red at a FOURTH consecutive check, unchanged. Re-proven this chunk on cargo-audit **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, so it stays an advisory-DATABASE fault with nothing to raise a floor to. Remedy is the **bounded wait alone** — re-run it, and verify `cargo deny check` actually ran green as the overlapping signal (it did here: all four classes ok, exit 0). Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09 · security-plan §Dependency Security.)

## 2026-08-10-workspace-key-divergence-probe — archived at the 2026-08-20 wrap

[2026-08-10-workspace-key-divergence-probe] Workspace-key divergence probe — two-launch verdict plus a host-path-free named precondition replacing the opaque corpus Blocked   PREREQ (from 2026-08-09-sut-load-envelope): **re-check `cargo audit`** — now red at a FIFTH consecutive check, unchanged. Re-proven this chunk on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1, so it stays an advisory-DATABASE fault with nothing to raise a floor to. Remedy is the **bounded wait alone** — re-run it, and verify `cargo deny check` actually ran green as the overlapping signal (it did here: advisories/bans/licenses/sources all ok, exit 0). Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09 · security-plan §Dependency Security.)

## 2026-08-10-pulse-run-contract — archived at the 2026-08-20 wrap

[2026-08-10-pulse-run-contract] Pulse run contract — deterministic-L4 mode and shared data-dir asserted at preflight, unmet terms named (P-073)   PREREQ (from 2026-08-10-workspace-key-divergence-probe): **re-check `cargo audit`** — SEVENTH consecutive check, **deferred since 2026-08-08-sut-capability-manifest**. **RATIFIED** by the operator at the 2026-08-10 wrap under the L5 age trigger: the bounded wait continues and **re-pins silently from here** until the advisory DB heals — no further ratification HALT. Re-proven this chunk on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1, so it remains an advisory-DATABASE fault with nothing to raise a floor to. Remedy is the **bounded wait alone** — re-run it, and verify `cargo deny check` actually ran green as the overlapping signal (it did here: advisories/bans/licenses/sources all ok, exit 0). Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)   CARRY (from 2026-08-10-workspace-key-divergence-probe): **the live workspace-key question is still UNANSWERED, and this entry owns its precondition.** The named precondition shipped and is proven at the stub tier, but the three-arm live probe could not exercise the key axis: under deterministic L4 with a shared data-dir, Pulse ingested the canary yet `cues_emitted` stayed 0 with the service in baseline bootstrap, so **no incident formed under any key** and all three arms blocked for the no-incident reason. Arm 3 (`cwd == the data dir`) therefore leaves the Windows `\\?\` canonicalization question open — `canonicalize` returns the verbatim prefix while the sidecar uses the raw `data_dir` string. This entry is what makes an incident actually form, so **re-run the three-arm probe once it does** and record which reason each arm blocks for. Evidence + full mechanics: `conductor-0.2.0/chunks/2026-08-10-workspace-key-divergence-probe/two-launch-verdict.md`; the falsified marker-less-temp-dir premise is recorded in `verification-matrix.json#v2-17` `notes`.   CARRY (from 2026-08-09-in-lane-sut-scenarios): **`P-073` is in `conductor_core::UNBACKED_AUTO`** (now 10 entries after `P-079` retired). `check_scenario_backing` holds the pin at EXACT-SET equality in BOTH directions, so a scenario naming `P-073` without removing it from the pin fails with "in the unbacked ledger but now named by a scenario — shrink the ledger", and shrinking without naming fails the other way. The two halves must land in ONE commit. Proven mechanic: this chunk did exactly that for `P-079` — the pin edit needs no renderer change, since all 13 `UNBACKED_AUTO` consumers derive the count.

## 2026-08-10-scenario-run-root-span-tree — archived at the 2026-08-20 wrap

[2026-08-10-scenario-run-root-span-tree] scenario.run root span tree — the must-trace self-observation spans beneath each scenario run   CARRY (from 2026-08-10-workspace-key-divergence-probe): **this entry now also owns the two ORPHANED `conductor-report` spans.** `crates/conductor-report/src/` emits **zero** `tracing::` calls across all five files, yet obs-plan §4 Critical Path 1 mandates `report.generate` → `db.insert_run` in the scenario-run span chain. `playbook.md` (2026-06-20/21) records both as route-sequenced to land "with the Epoch-8 cli/timeline caller" — **Epoch 8 is complete and they never landed**, so the deferral outlived its promised owner while being tracked only in a `note` field, which is not an owning channel. Operator-assigned here at the 2026-08-10 wrap because these ARE must-trace spans beneath a scenario run: this entry's scope covers the report seam, not just the run root. Do not re-dismiss the D-obs-instrumentation detector on `conductor-report` as build-sequencing — the sequencing already ran out.   PREREQ (from 2026-08-10-pulse-run-contract): **re-check `cargo audit`** — EIGHTH consecutive, **deferred since 2026-08-08-sut-capability-manifest**, re-pinning silently under the operator's L5 ratification at the 2026-08-10 wrap (no further ratification HALT). Re-proven this chunk on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1 — an advisory-DATABASE fault with nothing to raise a floor to. Remedy is the **bounded wait alone**: re-run it, record the result, and verify `cargo deny check` ran green as the overlapping signal (it did here: advisories/bans/licenses/sources all ok). Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)

## 2026-08-11-faithful-emission-dispatcher — archived at the 2026-08-20 wrap

[2026-08-11-faithful-emission-dispatcher] Faithful emission dispatcher — per-phase telemetry shaping over the emit primitives, replacing one-signal-per-phase   CARRY (from 2026-08-10-scenario-run-root-span-tree): **this is the first entry that will want a POST-OPEN span attribute, and the layer drops those silently.** `JsonObsLayer` records span attributes on the `on_new_span` record ONLY and implements no `on_record`, so `tracing::field::Empty` + a later `Span::current().record(...)` vanishes with no error — the same silent-field-loss class this chunk just fixed one layer down. It bites HERE specifically: obs-plan §4 CP1 (`obs-plan.md:298`) requires `timeline.execute` to carry `phase_count` AND `emission_count`; the shipped span carries only `phase_count` (`scheduler.rs:34`), and `emission_count` is knowable only AFTER the phase has emitted — which is exactly what this entry produces. Either implement `on_record` (deciding whether a late value amends the `new` record or emits its own line) or compute the count before opening the span. Do not discover this by watching a field disappear.   PREREQ (from 2026-08-10-scenario-run-root-span-tree): **re-check `cargo audit`** — NINTH consecutive, **deferred since 2026-08-08-sut-capability-manifest**, re-pinning silently under the operator's L5 ratification at the 2026-08-10 wrap (no further ratification HALT). Re-proven this chunk on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1 — an advisory-DATABASE fault with nothing to raise a floor to. **The deferral's standing basis is now "audit SURFACE unchanged (no new `[[package]]`) + `cargo deny check` verified green" — NOT "zero dependency delta"**: this chunk DID move the tree (a `tracing` edge inside `conductor-report`'s existing package entry), so the older wording no longer holds literally. Remedy is the **bounded wait alone**: re-run it, record the result, and verify `cargo deny check` ran green as the overlapping signal (it did here: advisories/bans/licenses/sources all ok). Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)

## 2026-08-13-dispatcher-determinism-goldens — archived at the 2026-08-20 wrap

[2026-08-13-dispatcher-determinism-goldens] Dispatcher determinism goldens — same scenario and seed yielding an identical emission stream shape over the new per-phase dispatcher   CARRY (from 2026-08-11-faithful-emission-dispatcher): **this entry owns the load-envelope close-out, and the contract itself names it.** `contracts/pulse-load-envelope.toml:40-42` states that both `[[exempt]]` entries AND the switch from total-duration to EMITTING-PHASE duration "retire at the Epoch-2 Faithful emission dispatcher entry" — that chunk deliberately declined it (plan step 8, ratified at its P5 review) to stop the version's largest chunk widening further, and shipped only the derivability re-wording. What is now true: `EmissionSpec::occurrences` exists, so a per-phase rate IS computable, and `crates/conductor-core/src/load_envelope.rs` + arch §Occupied Resources both record the terms as **derivable but not yet asserted**. This entry owns three linked pieces: (a) re-scope `check_load_envelope` from total `gap_ms` to the sum of gaps for phases with `occurrences > 0`; (b) retire BOTH `[[exempt]]` entries — `activity-floor` (50 of its 65 minutes are now literally `occurrences = 0`) and `incident-auto-resolution` (its 731s wait is likewise declared quiet), which is exactly why the contract predicted they would pass on their own merits; (c) reconcile the contract's own sentence + the arch/`load_envelope.rs` wording once the assertion is real. Note the ledger is EXACT-SET in both directions, so (b) must land in the same commit as (a) or `check_load_envelope` fails as ledger rot.   PREREQ (from 2026-08-11-faithful-emission-dispatcher): **re-check `cargo audit`** — ELEVENTH consecutive, **deferred since 2026-08-08-sut-capability-manifest**, re-pinning silently under the operator's L5 ratification at the 2026-08-10 wrap (no further ratification HALT). Re-proven again on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1 — an advisory-DATABASE fault with nothing to raise a floor to. The standing basis held **literally** at the last check: audit SURFACE unchanged (`Cargo.lock` moved 4 lines, all dependency edges inside `conductor-run`'s existing package entry, **zero new `[[package]]`**) and `cargo deny check` verified green (advisories/bans/licenses/sources, exit 0) as the overlapping signal. Remedy is the **bounded wait alone**: re-run it, record the result, verify `cargo deny` ran green. Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)

## 2026-08-13-per-check-read-back-extraction — archived at the 2026-08-20 wrap

[2026-08-13-per-check-read-back-extraction] Per-check read-back extraction — observed values from the corpus tools feeding the unchanged evaluate/classify path, degraded_mode mapping to KnownResidual   PREREQ (from 2026-08-13-dispatcher-determinism-goldens): **re-check `cargo audit`** — TWELFTH consecutive, **deferred since 2026-08-08-sut-capability-manifest**, re-pinning silently under the operator's L5 ratification at the 2026-08-10 wrap (no further ratification HALT). Re-proven again on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1 — an advisory-DATABASE fault with nothing to raise a floor to. **The standing basis was RE-VERIFIED this chunk rather than echoed**: `Cargo.lock` moved exactly 1 line — an `insta` edge inside `conductor-run`'s existing package entry — so audit SURFACE unchanged with **zero new `[[package]]`**, and `cargo deny check` observed green (advisories/bans/licenses/sources, exit 0) as the overlapping signal. Remedy is the **bounded wait alone**: re-run it, record the result, verify `cargo deny` ran green. Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)

## 2026-08-13-first-live-green-preflight — archived at the 2026-08-20 wrap

[2026-08-13-first-live-green-preflight] First live green preflight — ready:true against a real Pulse with journal and runs.db evidence   CARRY (from 2026-08-10-pulse-run-contract, re-pinned from the workspace-key probe): **the three-arm workspace-key probe still owes its live re-run, and this entry is where a live preflight actually runs.** The mechanism that was missing has now SHIPPED but is untested live — `[incident_formation]` in `contracts/pulse-run-contract.toml` drives a warm-up pre-roll (benign traffic carrying the canary service out of Pulse's baseline bootstrap before the counted storm) plus a poll floor outlasting L3's 20-60s digest cadence. **The live leg is the empirical test of that mechanism: until it runs, the warm-up rests on one falsification (`cues_emitted: 0` with a correct storm shape), never on a confirmation.** Re-run `conductor preflight --json` from the three cwd roles — the Conductor repo root · a marker-less temp dir · the data dir itself — and record which reason each arm blocks for, appending to `chunks/2026-08-10-workspace-key-divergence-probe/two-launch-verdict.md`. Only if an incident actually forms can arm 3 answer the open Windows `\\?\` question (`canonicalize` returns the verbatim prefix while the sidecar uses the raw `data_dir` string). **Full operator recipe, so the gated leg never rediscovers it:** (1) the sidecar must be BUILT *and* reachable — `cargo build -p mcp-server --bin andromeda-pulse-mcp --features mcp-server` in the Pulse repo, then on PATH, because `spawn.rs` resolves the fixed program name `andromeda-pulse-mcp` from PATH; unreachable, every arm measures the read-back-unreachable path and yields evidence that looks like a result but is not (this chunk hit exactly that — a DIFFERENT failure from the probe chunk's unbuilt sidecar, same worthless outcome); (2) `ANDROMEDA_PULSE_L4_DETERMINISTIC=true` and `ANDROMEDA_PULSE_DATA_DIR` exported in the SAME shell that launches both, since Conductor observes the L4 term only as a declaration in its own environment; (3) allow at least the contract's `min_canary_poll_seconds` — `CONDUCTOR_PREFLIGHT_TIMEOUT` can raise it but no longer lower it; (4) launch `pulse-app` from a cwd OUTSIDE this repo (it writes Specta/TauRPC bindings relative to its own cwd).   CARRY (from 2026-08-13-per-check-read-back-extraction): **the per-check extraction is stub-proven only, and its failure mode against a real response is SILENT.** `conductor_verify::observe` composes the graded observation from `query_incident_list` + `retrieve_report` + `retrieve_telemetry_slice`, and its readers were written against Pulse's SOURCE (`crates/mcp-server/src/tools.rs`) — never against an observed live response. Every reader deliberately degrades to empty on an unexpected shape rather than erroring (the verdict/error wall), so a live field-name or nesting mismatch does NOT surface as a fault: it yields an empty observation, which reads downstream as an ordinary `Blocked` or a failed `Contains`. This entry is the first moment live Pulse responses reach those readers. When ready:true lands, dump one raw `retrieve_report` / `retrieve_telemetry_slice` result and diff its keys against `extract.rs`'s expectations (`markdown`, `degraded_mode`, `span_refs`, `fingerprint_refs`, `items[].{id|incident_id,status,severity,title}`) BEFORE trusting any measured verdict — a green run over an empty observation looks identical to a green run over a real one.   PREREQ (from 2026-08-13-per-check-read-back-extraction): **re-check `cargo audit`** — THIRTEENTH consecutive, **deferred since 2026-08-08-sut-capability-manifest**, re-pinning silently under the operator's L5 ratification at the 2026-08-10 wrap (no further ratification HALT). Re-proven again on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1 — an advisory-DATABASE fault with nothing to raise a floor to. **The standing basis was RE-VERIFIED this chunk, and held stronger than at any prior pin**: `Cargo.lock` moved **ZERO lines** (un-drifted — audit SURFACE unchanged, zero new `[[package]]`), and `cargo deny check` observed green (advisories/bans/licenses/sources, true exit 0) as the overlapping signal. Remedy is the **bounded wait alone**: re-run it, record the result, verify `cargo deny` ran green. Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)

## 2026-08-14-canary-fingerprint-feed-capture — archived at the 2026-08-20 wrap

[2026-08-14-canary-fingerprint-feed-capture] Canary fingerprint-feed capture — why Pulse's storm detector tracks zero fingerprints while nine spans arrive and both sides agree on the attribute keys, settled by a capture taken while Pulse is running   CARRY (from 2026-08-13-first-live-green-preflight): **this is the ONE open Conductor-side piece of the live-path fix-scope, and it is answerable without an incident** — it needs `pulse-app` running, not a corpus, so the 3,600s bootstrap gate does not block it. What is established: Conductor emitted 9 `emit.batch` spans without error (3 warm-up + 6 storm), Pulse logged `span_count: 9`, and the attribute contract matches exactly (`conductor-emit/src/exception.rs:156-160` emits event `exception` with `exception.type`/`.message`/`.stacktrace`; `crates/buffer/src/appender.rs:346-350` extracts those three keys; `fingerprint.rs:71-79` returns `None` only on an absent/empty `exception_type`) — yet `tracked_fingerprints_count` stayed 0, which rules out both scattered-fingerprint and detector-missed-the-group. The remaining discriminator is whether the six exception spans reach the appender with their events intact, and it **cannot be settled post-mortem**: `corpus.db` carries no span or exception table (`baseline_state`/`digest_archive`/`incident_events`/`incidents`/`pipeline_metrics`/`service_registry` only), so received spans never persist and the in-process buffer dies with the process. If they do not arrive intact the defect is Conductor-side and this entry owns the fix; if they do, the question moves to Pulse and this entry closes by handing it over. **Live-leg operator recipe — FIVE items, not the four on record:** (1) sidecar built AND on PATH; (2) `ANDROMEDA_PULSE_L4_DETERMINISTIC=true` + `ANDROMEDA_PULSE_DATA_DIR` exported in the launching shell; (3) **`ANDROMEDA_PULSE_MCP_ENABLED=true` in CONDUCTOR's own environment** — `spawn.rs` passes only the data dir via `.env(...)`, so inheritance is the sole channel, and without it the sidecar starts and immediately exits and every arm measures the read-back-unreachable path; (4) poll budget at or above the contract floor (`agent-run boot` now derives it); (5) `pulse-app` launched from a cwd OUTSIDE this repo, with Conductor itself always run from its repo root (all four `contracts/*.toml` resolve cwd-relative). **Open design question for the operator, deliberately undecided:** `spawn.rs` could set `ANDROMEDA_PULSE_MCP_ENABLED` itself the way it already sets the data dir, making the sidecar's own gate unmissable — but that would have Conductor enable a Pulse-side feature flag on its behalf, which `architecture.md` §Occupied Resources currently asserts it does not do.   PREREQ (from 2026-08-13-first-live-green-preflight): **re-check `cargo audit`** — FIFTEENTH consecutive, **deferred since 2026-08-08-sut-capability-manifest**, re-pinning silently under the operator's L5 ratification at the 2026-08-10 wrap (no further ratification HALT). Re-proven again on **0.22.2** (the latest published): byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1 — an advisory-DATABASE fault with nothing to raise a floor to. The standing basis was RE-VERIFIED rather than echoed: `Cargo.lock` moved **zero lines** (audit surface unchanged, zero new `[[package]]`) and `cargo deny check` observed green (advisories/bans/licenses/sources, true exit 0) as the overlapping signal. Remedy is the **bounded wait alone**: re-run it, record the result, verify `cargo deny` ran green. Do NOT raise the floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. (`playbook.md` external-decay · `.claude/rules/security.md` 2026-08-09/-08-10 · security-plan §Dependency Security.)

## 2026-08-15-canary-storm-autonomous-band — archived at the 2026-08-20 wrap

[2026-08-15-canary-storm-autonomous-band] Canary storm inside Pulse's Autonomous band — the preflight canary forms a Tier-1 incident and preflight reaches ready:true against a live Pulse   CARRY (from the 2026-08-15 Pulse consolidated visit): **the canary undershoots Pulse's incident-forming threshold BY DESIGN, and this entry owns the correction.** `CANARY_STORM_COUNT = 6` (`crates/conductor-run/src/lib.rs:111`) sits in Pulse's Suggested band. Verified in Pulse's own source this session: `DEFAULT_SUGGESTED_THRESHOLD = 5` and `DEFAULT_AUTONOMOUS_THRESHOLD = 10` (`crates/triage/src/pattern/storm.rs:73`, `:78`); the emit test is `count >= detector.autonomous_threshold` (`:245`), so TEN OCCURRENCES SUFFICE — the margin sits above 10, not above 11; and the trigger population is bounded by `DEFAULT_DETECTION_SUB_WINDOW_SECONDS = 30` (`:69`), so the raised count must land inside ONE 30s detection sub-window. Pulse's Tier-1 coordinator accepts ONLY Autonomous cues (`crates/triage/src/cadence/coordinator.rs:390`; its own test at `:861-864` documents the band), so at 6 no Tier-1 incident ever forms — measured at Pulse's Tier-1 chunk (2 incidents at >=10, zero at 6). **The exact margin is the PLAN's judgment.** The constant's doc comment (`lib.rs:109-110`) states the now-insufficient rationale ("over Pulse's `>=5 in 30s` retry-storm floor (P-018) so the gate's incident is raised deterministically") and must move WITH the constant; test call sites are `crates/conductor-run/tests/canary_wire.rs:20,101,130,182`. **The `fingerprint-storm` SCENARIO does NOT move** — `scenarios/fingerprint-storm.toml` is already two-phase by design (occurrences 6 -> Suggested, then 12 -> Autonomous); only the preflight canary constant changes. **This entry also owns the live preflight-green attempt.** **Expected amendment at this chunk's wrap:** `architecture.md:174` encodes the now-false "the read-back tools filter incidents by the `workspace` column (= data_dir; `andromeda-pulse-mcp.rs:74` + `tools.rs:336`)" — since Pulse's workspace-key chunk the sidecar reads the key the APP publishes under the data dir, raw data_dir being the fallback only. **Ride-along record closure:** `conductor-0.2.0/chunks/2026-08-10-workspace-key-divergence-probe/two-launch-verdict.md` §Re-run gains the arm-zero closure (the feed is healthy 1:1:1 on a second driver; the zero was tier-band arithmetic, not a defect), and `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/fingerprint-feed-verdict.md` gains its §5 closure pointer — cite Pulse's chunk-folder evidence paths (workspace-key-alignment · corpus-key-persistence · tier-1-incident-path `evidence/`), never copy bodies. **`v2-10`** ("Live ready:true preflight", pooled with a CONCRETIZATION DECLINED note) becomes CLAIMABLE at this entry's P5 if research confirms `ready:true` is now provable — the decline note's OWN re-claim path, never retroactive; all three causes it names (workspace-key divergence, the 3,600s `BootstrapState::Ready` gate, the unreachable data_dir fallback) are dissolved Pulse-side.   PREREQ: re-check `cargo audit` (standing deferral since 2026-08-08-sut-capability-manifest; ratified at the 2026-08-10-workspace-key-divergence-probe wrap; basis: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` re-proven on 0.22.2, the latest published, so no floor exists to raise; re-verified at this wrap with `Cargo.lock` zero lines vs HEAD; overlap: `cargo deny check` advisories/bans/licenses/sources true exit 0, re-observed green at this wrap; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`). The same upstream advisory-DB fault reached Pulse the same day — one event, two projects. Close the deferral the moment it parses.

## 2026-08-15-canary-spans-pulse-fingerprints — archived at the 2026-08-20 wrap

[2026-08-15-canary-spans-pulse-fingerprints] Canary spans that Pulse fingerprints — the buffer enumerates the canary's span events and its fingerprint observer runs, as it already does for inject_demo   CARRY (from 2026-08-15-canary-storm-autonomous-band): **the count fix landed and was necessary but NOT sufficient — this is the remaining preflight-green blocker, and its first diagnostic step is already answered.** That chunk raised `CANARY_STORM_COUNT` to 12 and the live leg proved the storm reaches Pulse intact (`span_count: 15` = 3 warm-up + 12 storm, sidecar dispatching, all four tools present, workspace key published), yet `incidents: 0` and no storm was detected at any tier. **The `buffer.tick` trio discriminates it and rules out the fingerprint stage entirely:** across 15 identical ticks `span_events_seen: 0` · `observer_invocations: 0` · `fingerprints_computed: 0`, with `rows_ingested: 1` against `span_count: 15`. `observer_invocations: 0` means the observer is **never invoked**, not invoked-and-empty — so the gap sits between **OTLP ingest receipt and the buffer's span-event enumeration**, upstream of fingerprinting. It is **producer-dependent**: `inject_demo`'s spans traverse the same path and fingerprint (Pulse tracked `c33df842`), Conductor's canary spans do not — so the question is no longer *where* the gap is but **what differs between those two producers' spans at the buffer boundary** (`inject_demo` batches 2 ops × 3 spans per 500ms and keeps going; the canary sends 12 single-span unary exports and stops). Read the detector telemetry correctly when re-testing: `tracked_fingerprints_count` is a 60s-windowed gauge over DISTINCT fingerprints, so a healthy 12-occurrence identical-fingerprint storm reads **1, never 12**, and `storms_detected_total` increments on BOTH the Suggested and Autonomous branches — it discriminates storm PRESENCE, never TIER; `severity_hint` on `triage.pattern.storm.detected` names the tier. Evidence: `conductor-0.2.0/chunks/2026-08-15-canary-storm-autonomous-band/report.md` · `andromeda-pulse-0.3.0/chunks/2026-08-15-tier-1-incident-path-investigation/evidence/premise-check.md`. **`v2-10` returned to the pool unclaimed** with the measurement in its `notes`; this entry inherits the preflight-green blocker role and `v2-10` becomes claimable again only on a leg that actually reaches `ready:true`.   PREREQ: re-check `cargo audit` (standing deferral since 2026-08-08-sut-capability-manifest; ratified at the 2026-08-10-workspace-key-divergence-probe wrap; **19th pin**, re-deferred by 2026-08-15-canary-storm-autonomous-band which was itself zero-delta; basis: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, no floor exists to raise; re-verified at that chunk with `Cargo.lock` zero lines vs HEAD; overlap: `cargo deny check` true exit 0 across all four classes, re-observed green; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`). Close the deferral the moment it parses.

## 2026-08-16-canary-fingerprint-derivation-aligned — archived at the 2026-08-20 wrap

[2026-08-16-canary-fingerprint-derivation-aligned] Canary fingerprint derivation aligned — Conductor's expected fingerprint is what Pulse actually derives, so the round-trip's last precondition can pass   CARRY (from 2026-08-15-canary-spans-pulse-fingerprints): **this entry inherits the preflight-green blocker role — the THIRD generation of it (count → primary key → derivation), and the first whose obstacle is proven by construction rather than by symptom.** The 2026-08-16 live leg drove the canary through every prior stage: ingest counted 15 spans, the buffer appended all of them (**27 `duckdb.append` lines, ZERO `reject_reason`**), the `buffer.tick` trio read `12/12/12`, `storms_detected_total` reached 2 with `severity_hint: "autonomous"` at `occurrence_count: 10`, and an incident formed (`item_id: 1`) — then the gate stopped at `canary fingerprint not found in telemetry slice`. **The two derivations cannot agree as they stand:** Conductor computes FNV-1a 64-bit rendered as 16 hex chars over `exception_type` + each frame's `function` (`conductor-emit/src/exception.rs:115-123`); Pulse computes blake3 truncated to 16 bytes over `exception_type` + `\0` + `normalize_stacktrace(stacktrace)` — first 3 non-empty lines, path tokens / hex addresses / `:line(:col)` stripped — and reads it back as an **8-char hex prefix of the first 4 bytes** (`andromeda-pulse crates/buffer/src/fingerprint.rs:79-110`, HEAD `d090314`). Equality is impossible by WIDTH alone. Conductor adopts Pulse's derivation; **whether the comparison surface is the 8-char prefix or the full width is THIS chunk's research question**, not one the wrap settled. **Expect a manifest touchpoint:** a hashing dependency becomes a NORMAL (not dev/test-only) dependency of the emit path, so the dep-kind boundary rule applies — a test-scoped dep cannot back a shipped signature. **`v2-10` ("Live `ready:true` preflight") claims HERE**, on a leg that actually reaches `ready:true`, with `conductor-0.2.0/chunks/2026-08-15-canary-spans-pulse-fingerprints/evidence/leg-verdict.md` as the recorded basis its matrix `notes` already points at. **Two source twins ride this chunk with the fix** (wrap does not edit source — the `scheduler.rs` comment precedent): `crates/conductor-run/src/lib.rs:218` (`emit_canary`'s doc restating the retired match claim) and `warm_up_canary_service`'s doc, which still teaches the disproved bootstrap rationale. **Live-leg recipe correction — the console tee is retired:** `pulse-app` is a Windows GUI-subsystem binary and never attaches to a console, so a tee yields 0 bytes; its real telemetry sink is `{data_dir}/logs/agent-latest.jsonl.<date>`, harvested post-leg and sliced to the leg window by pre-leg line count. Capturing Pulse's own ERROR-level `duckdb.append` `reject_reason` lines stays mandatory — no Conductor log can supply them.   PREREQ: re-check `cargo audit` (standing deferral since 2026-08-08-sut-capability-manifest; ratified at the 2026-08-10-workspace-key-divergence-probe wrap; **21st pin**; basis: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, re-verified true exit 1 at this chunk with `Cargo.lock` zero lines and no dependency delta, so no floor exists to raise; overlap: `cargo deny check advisories bans licenses sources` re-observed true exit 0 across all four classes; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`). Close the deferral the moment it parses — and note the basis will CHANGE next chunk if the hashing dependency lands, which restores the full pin form.

## 2026-08-16-fault-application-spans — archived at the 2026-08-20 wrap

[2026-08-16-fault-application-spans] Fault-application spans — silence, ramp and port-occupier phases observable beneath the timeline span with duration and journal offset   CARRY (from 2026-08-13-dispatcher-determinism-goldens): **ride-along — align one stale doc-comment in `crates/conductor-timeline/src/scheduler.rs:69`.** It states "Total elapsed time per phase is exactly the jittered gap either way", which holds for the reported `PhaseTransition` stream but NOT for clock consumption: `paced_slices` divides a gap into sub-millisecond slices and `tokio::time` rounds each sleep UP to its 1ms tick, so a phase's emissions overshoot its window by up to one tick each and the excess carries forward (measured: phase 0 boundary 12010ms, its last emission 12012ms across 6 emissions). Not amendment-flow material — all seven masters grep clean of the claim, the wording is source-only, and wrap does not edit source. The true bound is already asserted by `crates/conductor-timeline/tests/pacing.rs::each_phase_last_emission_sits_on_its_boundary_within_timer_resolution`, so this is a comment fix riding along with the next timeline-touching work, **not a work item**.   PREREQ: re-check `cargo audit` (standing deferral since 2026-08-08-sut-capability-manifest; ratified at the 2026-08-10-workspace-key-divergence-probe wrap; **22nd pin, and the BASIS CHANGED at 2026-08-16-canary-fingerprint-derivation-aligned — the compact ratified form no longer applies**). Fault: advisory-DATABASE — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1, re-measured at that chunk; no released tool version can read the DB, so there is no floor to raise. **What changed: `Cargo.lock` MOVED** — `blake3` 1.8.6 plus `arrayref` / `arrayvec` / `constant_time_eq` / `cpufeatures` landed as a normal dependency of `conductor-emit`, so the deferral can no longer rest on "no dependency delta" and the tree is no longer static. Overlap: `cargo deny check advisories bans licenses sources` **true exit 0 across all four classes over the NEW lock**, after a justified `[licenses] allow` of `BSD-2-Clause` for `arrayref` — deny is now the SOLE coverage for the added packages and must be VERIFIED green, never assumed (security-plan §Dependency Security, admission condition; playbook rule appended 2026-08-16). Full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`. Close the deferral the moment it parses.

## 2026-08-16-fingerprint-storm-live-proof — archived at the 2026-08-20 wrap

[2026-08-16-fingerprint-storm-live-proof] fingerprint-storm live proof — identity triple, storm cue thresholds and exactly-one-incident coalescing (P-017, P-018, P-074)   CARRY (from 2026-08-09-in-lane-sut-scenarios): **`P-074` is in `conductor_core::UNBACKED_AUTO`** (now 10 entries after `P-079` retired) — this entry is its named owner. `check_scenario_backing` is exact-set in BOTH directions, so naming `P-074` in a scenario's `p_ids` and removing it from the pin must land in ONE commit or the gate fails either way. Note `fingerprint-storm.toml` already exists naming P-017/P-018 — adding `P-074` to its `p_ids` is what backs the claim.   CARRY (from 2026-08-13-first-live-green-preflight): **two thirds of the read-back key-diff are still un-retired, and this is the first entry that can retire them.** The live leg witnessed exactly one shape — `query_incident_list returned keys [items, next_cursor, total]`, matching the committed baseline in `conductor-verify/tests/readback.rs::the_read_back_key_sets_are_pinned_as_the_live_diff_baseline` exactly — because `retrieve_report` and `retrieve_telemetry_slice` are only reached with a NON-EMPTY corpus, which no arm produced. Their readers (`markdown` · `degraded_mode` · `span_refs` · `fingerprint_refs` · `items[].{id|incident_id,status,severity,title}`) therefore remain **stub-proven only**, and every reader degrades to empty on an unrecognized shape rather than erroring — so a live field-name divergence there is SILENT and reads downstream as an ordinary `Blocked` or a failed `Contains`. The witness is already wired: each tool's key set is logged once per pass on the existing `verify.readback.observe` / `verify.readback.preflight` spans. **Diff those two key sets against the pinned baseline BEFORE trusting any measured verdict from this family**, and land any divergence as a finding plus a stub test, never a silent reader fix. The five-item operator recipe for reaching a live leg at all is pinned on the `Canary fingerprint-feed capture` entry above.   CARRY (from 2026-08-14-canary-fingerprint-feed-capture): **the fingerprint feed does not reach Pulse's storm detector, and this entry is the first family that depends on it.** Settled by capture: Conductor's storm reaches the wire with its `exception` events intact (asserted on spans a collector actually received — `crates/conductor-run/tests/canary_wire.rs`), and Pulse receives and counts all nine spans, yet its fingerprint table stays empty across 31 tick lines spanning three arms, twelve of them INSIDE the 60s retention window, with `storms_detected_total` and `fingerprints_evicted_total` both 0 throughout. So the gap is inside Pulse between OTLP ingest receipt and the per-span-event fingerprint observer — a REGION, not a named defect, since no read-back surface exposes that path. **Consequence for this entry: the storm-cue thresholds (6→Suggested / 12→Autonomous) cannot surface until that gap is closed Pulse-side — it is a THIRD blocker, independent of the 3,600s bootstrap gate and of workspace-key alignment.** Read the detector's telemetry correctly when re-testing: `tracked_fingerprints_count` is a 60s-windowed gauge over DISTINCT fingerprints sampled at a 15s tick AFTER eviction, so a working six-occurrence identical-fingerprint storm reads **1, never 6**; the window-immune discriminators are the two cumulative counters beside it. Full evidence + the optional one-run live leg that would put the wire witness and the tick counters in a single capture: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/fingerprint-feed-verdict.md` §5.   CARRY (from 2026-08-16-canary-fingerprint-derivation-aligned): **the THIRD blocker named in the CARRY above is RETIRED — the feed is healthy and preflight is green.** The ingest→fingerprint gap was Conductor-side (a constant span identity against Pulse's `spans` PRIMARY KEY), fixed at the predecessor chunk; the 2026-08-16 leg measured 27 `duckdb.append` lines with ZERO `reject_reason` and `severity_hint: "autonomous"` at `occurrence_count: 10`, and `conductor preflight` reached `ready:true`. This family is no longer blocked on Pulse. **Two things this entry now owns.** (1) **`scenarios/fingerprint-storm.toml`'s header prose states the P-017 claim measurement falsified** — it says the fingerprint is "INSENSITIVE to source path + line"; the path half is FALSE. Pulse's `normalize_frame` strips ABSOLUTE paths only, and Conductor's frames are relative by construction (guarded by `stacktrace_carries_no_absolute_host_path`), so a relative-path change IS identity-significant; the "first 3 normalized stack frames" half of that same sentence is correct and is the second narrowing. Source/config is not wrap's to edit (the `scheduler.rs` precedent), so the fix rides this entry. (2) **The identity triple's expectations must be re-derived against the shipped derivation** — `FingerprintVariant::PathVariant` now yields a DIFFERENT fingerprint, so a scenario asserting the triple aggregates to one storm must not include a path variant among the same-fingerprint members. Both narrowings are recorded in `architecture.md` §Established Decisions [Read-Back Dependency Posture] and pinned by `crates/conductor-emit/src/exception.rs` unit tests.   PREREQ: re-check `cargo audit` (standing deferral since 2026-08-08-sut-capability-manifest; ratified at the 2026-08-10-workspace-key-divergence-probe wrap; **23rd pin**; basis RE-VERIFIED at 2026-08-16-fault-application-spans — advisory-DATABASE fault, byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` at true exit 1, and although `Cargo.lock` moved (+2 lines) both were dependency EDGES inside `conductor-faults`' existing entry with **zero new `[[package]]`**, so the audit SURFACE is unchanged and no floor exists to raise; overlap: `cargo deny check advisories bans licenses sources` observed true exit 0 across all four classes; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`). Close the deferral the moment it parses.

## 2026-08-17-fingerprint-semantics-token-leading — archived at the 2026-08-20 wrap

[2026-08-17-fingerprint-semantics-token-leading] Fingerprint semantics re-aligned to token-leading — identity-triple premise, variant tests, the P-017 clause and storm grading against the SUT's current derivation   CARRY (from the 2026-08-17 Pulse visit, SUT HEAD `efabe8e`): **Pulse changed exception-fingerprint normalization to TOKEN-LEADING, so this repo's alignment surfaces are stale BY DESIGN — owned debt, not a regression.** A path token counts as absolute only when it STARTS a token (`is_token_boundary` now guards `is_absolute_path_start`, `crates/buffer/src/fingerprint.rs`); relative path structure is identity-significant at EVERY depth, so the below-leading-segment distinction this repo encodes is dead — `src/a.rs` and `src/b/c.rs` are now TWO identities, and Pulse's own doc comment names the old behavior as the bug it fixed. FOUR surfaces carry the superseded semantics: `scenarios/fingerprint-storm.toml:9-13` (the prose plus the variant-mix arithmetic it justifies) · `conductor-emit/src/exception.rs` (`same_fingerprint_variants_match_the_base` at :382-390 asserts `PathVariant` keeps the base fingerprint, and `PathVariant::derive` at :414-421 stays relative by construction — which is exactly what now separates it) · the amended P-017 clause in `architecture.md` §Read-Back Dependency Posture · and `conductor-run/tests/storm_harvest.rs`'s single-`fingerprint_hex` grading. Measured failure shape at SUT HEAD: the 18-occurrence triple SPLITS ~12 base (>=10 → Autonomous) / ~6 path (>=5 → Suggested) into TWO storms; incident COUNT probably still reads one (L2 coalesces per kind/scope/scope_id and both land on the same service); and the per-phase tier boundaries go false (phase 1's 6 splits ~4/~2, so neither reaches >=5 where the phase name claims it does — exact per-phase counts depend on the dispatcher's variant distribution, which was not read). This entry is the drive-half unblock for Pulse's P-075 blocker 2: Pulse DECLINED that capability on it and records that the re-alignment is executed HERE, never from there. `v2-10`/`v2-11` STAY verified — they measured a state that really held; the semantics change invalidates only FUTURE legs. Evidence (read-only, cite never copy): `andromeda-pulse-0.3.0/chunks/2026-08-16-fault-identity-semantics-decided/decision.md` · `.../2026-08-17-incident-fingerprint-producer-repaired/` · `.../2026-08-17-conductor-e2e-verification-closure/` · Pulse matrix P-075 `notes` (both blockers).   PREREQ: re-check `cargo audit` — **25th consecutive**, standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap (re-pins silently; no further ratification HALT). Basis RE-VERIFIED this wrap and UNCHANGED: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` at true exit 1, and no released cargo-audit can read the DB, so there is no floor to raise. `Cargo.lock` un-drifted with zero source delta since the last pin, so the +1-edge basis change that forced the previous FULL form has itself stabilized — compact form resumes. Overlap VERIFIED, not assumed: `cargo deny check advisories bans licenses sources` observed true exit 0 across all four classes this wrap. Remedy is the bounded wait alone — do NOT raise a floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. Full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`.

## 2026-08-18-error-baseline-spike-live-proof — archived at the 2026-08-20 wrap

[2026-08-18-error-baseline-spike-live-proof] error-baseline-spike live proof — baseline convergence, ramp and candidate persistence over the sample floors (P-009..P-012)   CARRY (from 2026-08-15-canary-spans-pulse-fingerprints): **the Plain-trace dispatch path has NO stream-golden coverage, and it is the path that carried a catalog-wide defect undetected.** Both committed `dispatch_wire__*` goldens are driven by `scenarios/fingerprint-storm.toml` (Exception shape — all 36 golden spans are `name: "root"` carrying an `exception` event), so `EmissionShape::Plain` with `Signal::Traces` (`dispatch.rs:82`, and the non-error branch at `:95`) is asserted only by occurrence-count tests, which a constant-identity collision passes unchanged because every span still arrives. That is exactly how `ok_span`'s shared constant `(trace_id, span_id)` reached every scenario phase unnoticed. This entry's baseline phases emit Plain-shape traces, so it is the natural owner: consider adding a seeded stream golden over the Plain path. **Not a work item on its own** — a new-coverage candidate riding along, per the operator's scoping.   CARRY (from 2026-08-16-fingerprint-storm-live-proof): **every remaining live-proof family will find its `[[expected]]` read-back token checks UNGRADEABLE under deterministic L4, and the `Absent` side fails silently.** Measured: `retrieve_report` returns `degraded_mode: true` PERMANENTLY in this mode (computed `parsed_l4.is_none()`; the fixture is never parsed as an L4 output), so its `markdown` carries no scenario-specific token — a `Contains` always fails and an `Absent` passes VACUOUSLY. The fingerprint pair retired both to declare-only and moved the assertions onto Pulse's own `triage.pattern.storm.detected` line; expect the same disposition for this family's `ErrorRateSpike` / `CountAtLeast` tokens, and check FIRST whether the surface can carry the token before trusting any absence. Also inherited: `slo_tier` must be re-checked against measured `latency_ms`, which spans the scenario's own emission window (both fingerprint scenarios moved `<20s` → `<90s` for that reason).   **PREMISE PARTIALLY CORRECTED (2026-08-17 Pulse visit): this CARRY's blanket form no longer holds — one read-back surface was de-vacuumed.** Pulse's deterministic-L4 fixture now populates `evidence_refs` (`pulse-app/src/deterministic_inference.rs`), which reaches `retrieve_telemetry_slice.fingerprint_refs` and the Report Evidence section through a single join, so that surface CAN carry content under deterministic L4. What is NOT settled is the `retrieve_report` half this CARRY was measured on: `degraded_mode = parsed_l4.is_none()` reads `resolution_summary_text` (`crates/mcp-server/src/tools.rs:374-378`), a DIFFERENT field from `evidence_refs`, so the permanent-`degraded_mode` finding may well still stand. Settle which surfaces can carry a token BEFORE choosing this family's `[[expected]]` checks — do not inherit either the blanket pessimism or a blanket reversal.   CARRY (from 2026-08-17-fingerprint-semantics-token-leading): **the re-aligned derivation's LIVE equality is unproven, and it lands here by construction.** That chunk proved the token-leading transcription at unit tier only — its scenario leg took the Blocked spine with no live Pulse and emitted nothing, so no stacktrace reached the journal and Conductor's computed fingerprint was never compared against one Pulse actually derived. The next family leg's storm harvest is where that comparison happens for free: `conductor-run/tests/storm_harvest.rs` grades on Pulse's OWN `fingerprint_hex` from `triage.pattern.storm.detected`, so any live leg that forms a storm confirms or refutes the transcription with no added assertion. Nothing is owed beyond running one; but if the harvested `fingerprint_hex` disagrees with what `conductor-emit` computes for the same base spec, that is a TRANSCRIPTION defect to raise, never a scenario failure.   PREREQ: re-check `cargo audit` — **26th consecutive**, standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap (re-pins silently; no further ratification HALT). Basis RE-VERIFIED at `2026-08-17-fingerprint-semantics-token-leading` and UNCHANGED: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` at true exit 1, and no released cargo-audit can read the DB, so there is no floor to raise. That chunk carried real Rust source delta but **zero dependency delta** (`Cargo.toml` / `Cargo.lock` untouched), so the audit SURFACE is unchanged and the compact form carries forward. Overlap VERIFIED, not assumed: `cargo deny check advisories bans licenses sources` observed true exit 0 across all four classes. Remedy is the bounded wait alone — do NOT raise a floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. Full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`.

## 2026-08-18-restart-suppression-live-proof — archived at the 2026-08-20 wrap

[2026-08-18-restart-suppression-live-proof] restart-suppression live proof — gap/resume restart event, surgical suppression window and the bypass triple (P-015, P-016, P-057)   CARRY (from 2026-08-18-error-baseline-spike-live-proof): **three live-leg facts this entry inherits.** (1) `scripts/agent-run.sh` FORCES `SEED=424242` onto a scenario leg when `SEED` is not given, overriding the TOML's declared seed (latency-regression declared 4317011 and ran at 424242) — pass `SEED=<the TOML's seed>` explicitly on every leg, and this entry OWNS the default's disposition (stop the harness overriding TOML-declared seeds, or record the override as intended; a one-line change either way). (2) Pulse auto-resolves an idle incident on 30s ticks, so a window emitting only non-cue-raising traffic for ≥~2-3min outlives the canary incident and the read-back honestly Blocks — this family's silent gap phases are exactly that class, while its error-spike traffic re-raises identities (the bypass triple rides ErrorRateSpike): budget which phases keep the corpus alive. (3) Ride-along: `crates/conductor-run/src/lib.rs:223`'s doc comment still reads "fingerprint_refs is L4-authored and empty under deterministic L4" — stale since the SUT's fixture populates a `det-*` triple (measured 2026-08-18); align it with the next conductor-run edit (wrap does not edit source).   PREREQ: re-check `cargo audit` — **28th consecutive**, standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap (re-pins silently; no further ratification HALT). **The BASIS CHANGED at 2026-08-18-error-baseline-spike-live-proof — the compact zero-dependency-delta wording is DEAD**: RUSTSEC-2026-0258 (`h2` 0.4.15, low, patched upstream) landed mid-chunk and was remediated by the sanctioned lock-only bump to 0.4.16 (`Cargo.lock` moved 8/8 lines, ZERO new `[[package]]`), so the deferral now rests on "audit SURFACE unchanged (zero new packages) + `cargo deny check` VERIFIED true exit 0 across all four classes over the NEW lock" — both re-verified at that wrap, never echoed. The audit fault itself is unchanged: byte-identical `duplicate advisory ID: RUSTSEC-2026-0244`, true exit 1, no floor exists to raise. Remedy stays the bounded wait alone — do NOT raise a floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. Full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`.

## 2026-08-19-pii-scrub-live-proof — archived at the 2026-08-20 wrap

[2026-08-19-pii-scrub-live-proof] pii-scrub live proof — seven PII categories scrubbed across corpus and report excerpts, verdicts recorded without persisting excerpt content (P-035, P-047, P-048)   PREREQ: re-check `cargo audit` — **30th consecutive**, standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap (re-pins silently; no further ratification HALT). Basis RE-VERIFIED at `2026-08-18-restart-suppression-live-proof` and RESTORED to the compact premise: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` at true exit 1 (probed standalone in-chunk after a pipeline `$?` first read tail's exit — capture true exits standalone), and ZERO dependency delta this chunk (`Cargo.toml`/`Cargo.lock` untouched), so the audit SURFACE is unchanged. Overlap VERIFIED, not assumed: `cargo deny check advisories bans licenses sources` observed true exit 0. Remedy is the bounded wait alone — do NOT raise a floor, do NOT add a `deny.toml` ignore, do NOT edit CI. Close the deferral the moment it parses. Full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`.

## 2026-08-19-connection-lifecycle-live-proof — archived at the 2026-08-20 wrap

[2026-08-19-connection-lifecycle-live-proof] connection-lifecycle live proof — Listening/Receiving/Idle/Stalled walk plus the :4317 port-occupier leg (P-001..P-004)   CARRY (from 2026-08-16-fault-application-spans): **there is no port-occupier driver anywhere — this entry writes one, it does not merely invoke one.** The code-graph measured `conductor-faults` as an island: 0 crate edges in either direction and 0 references to `PortOccupier`/`AbruptSilence`/`EmissionGap`/`BurstyTrain` from outside the crate, so the "Epoch-8 driver" named in `scenarios/receiver-failed-port-conflict.toml:8` does not exist and that scenario's `port-held` phase currently emits one plain span while nothing binds `:4317`. Two consequences ride it. (1) **`v2-15` is claimable only here** — its acceptance asserts the occupier drives the receiver-failed state AND releases its bind on cleanup, which needs the driver plus a live read-back; the decline is recorded in the matrix `notes`. (2) **The `fault.port_occupier` span already ships** (RAII at `occupy`/`release`, carrying `fault_type` + `port`, proven by `crates/conductor-faults/tests/port_occupier.rs`), but obs-plan §4 records its `timeline.execute` parentage as CONDITIONAL because nothing runs it beneath the timeline — wiring the driver is what makes that parentage real, and the §4 wording becomes unconditional in the same wrap. Note the run→faults crate edge does not exist yet either; adding it is an explicit `Cargo.toml` edge under the crate-per-seam rule, never a bin-local shim.   PREREQ: re-check `cargo audit` — **31st consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-VERIFIED at `2026-08-19-pii-scrub-live-proof`: advisory-DATABASE fault — byte-identical `duplicate advisory ID: RUSTSEC-2026-0244` at true exit 1, probed standalone, and ZERO dependency delta (`Cargo.toml`/`Cargo.lock` untouched); overlap VERIFIED, not assumed: `cargo deny check advisories bans licenses sources` observed true exit 0; remedy is the bounded wait alone — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-20-latency-regression-re-proof — archived at the 2026-08-20 wrap

[2026-08-20-latency-regression-re-proof] latency-regression re-proof — a regression short relative to accumulated history, read back on a surface that survives the window (P-011, P-012)   CARRY (from 2026-08-18-error-baseline-spike-live-proof): **this entry owns the latency half v2-12's refined acceptance routed forward, and it starts from two measured obstacles, not a blank page.** (1) The shipped 90/90 shape can never fire the cue: the long-window t-digest p99 ABSORBS a ramp contributing ~half the window (threshold ≈ long·2.5 ≈ 6000ms vs short ≈ 2400ms; ZERO `latency_regression` cue lines across the whole leg) — detectability needs the regression SHORT relative to accumulated history (a longer/denser baseline, a sharper shorter spike, or both), and no threshold multiplier fixes a denominator the anomaly itself feeds. (2) The canary incident auto-resolves across a benign window (30s resolver ticks), so a long latency-only scenario needs a read-back design that does not depend on the canary incident surviving (its own cue-raising traffic, or grading purely at the harvest tier with the row's non-Blocked clause re-thought). Evidence: `conductor-0.2.0/chunks/2026-08-18-error-baseline-spike-live-proof/evidence/leg-verdict.md` §Leg B. The harvest parser + predicates already ship (`crates/conductor-run/tests/baseline_harvest.rs`; the latency predicates are live-unexercised, fixtures source-derived — pin them verbatim from THIS entry's leg). Relationship: v2-19 owns per-check latency / sub-5s representation — this entry owns ONLY the P-011/P-012 regression-detection re-proof; `check_load_envelope` re-runs on any re-shaped phases.   PREREQ: re-check `cargo audit` — **32nd consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-DERIVED at `2026-08-19-connection-lifecycle-live-proof`: advisory-DATABASE fault — `cargo audit` probed standalone true exit 1 byte-identical (`duplicate advisory ID: RUSTSEC-2026-0244`); the zero-dep-delta footing ENDED this chunk — lock delta = one member-dep line (the run→faults edge), zero new external crates; overlap VERIFIED, not assumed: `cargo deny check advisories bans licenses sources` observed true exit 0 over the NEW lock this chunk; remedy stays the bounded wait alone — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-20-verifier-self-hardening — archived at the 2026-08-20 wrap

[2026-08-20-verifier-self-hardening] Verifier self-hardening — the mutation-audit survivor families gain killing assertions and the suite passes under either test runner (code-audit baseline 2026-08-20, §B1–§B3)   CARRY (from the 2026-08-20 Epoch-3 code audit, `.andromeda/runs/2026-08-20T18-06-29-code-audit/`): four named survivor families, each verified at source in the audit — **`declares` ×5** (`conductor-run/src/lib.rs:218-222`, the run-contract `shell-declaration` predicate behind a NAMED preflight precondition; a `declares` stuck at `true` lets an unmet term read as satisfied, where the architecture requires a distinct `Blocked` naming it) · **`MAX_LINE_BYTES` ×3** (`conductor-verify/src/jsonrpc.rs:79`, the read-back per-line size bound `.claude/rules/security.md` REQUIRES; replacing the guard with `false` is currently unnoticed) · **the canary boundaries ×5** (`preflight.rs:385` `assert_canary` freshness `>`, `preflight.rs:335` `poll_canary` deadline arithmetic — the boundary semantics `2026-08-16-canary-fingerprint-derivation-aligned` proved live are themselves unasserted) · **the two journal stamp sources ×6** (`lib.rs:517` `now_ms`, `lib.rs:631` `now_unix_nanos` — journal-relative SLO arithmetic rests on them). Full survivor lists with coordinates: `proposals.md` §B1–§B2 + `c-mutation-conductor-run.json` / `c-mutation-conductor-verify.json` (kept FULL). Plus the runner-portability CAUSE fix (§B3): a per-test temp obs sink for `conductor-run::canary_wire the_wire_shape_witness_reaches_the_self_obs_artifact`, which passes under nextest and fails under `cargo test` — the audit ranks the process-global-sink shape SUSPECTED-not-proven, so confirm the cause before fixing; removing the cause is preferred over documenting the runner. Rider: `conductor-cli`’s unused `tracing` dependency (cargo-machete, graph-corroborated). ACCEPTANCE: a SCOPED `cargo-mutants` re-run over the two audited units, `--test-tool=nextest`, `stub_pulse_mcp` EXCLUDED per the operator ruling (record the exclusion in the next audit ledger record), shows the NAMED survivors killed; nextest stays zero-retry. NOT IN SCOPE (rulings on record): `shape_is_realizable` (no action indicated) · the `conductor-emit`→`conductor-core` doc-link dependency (deliberate keep) · the `conductor-run/lib.rs` split (watch only). Dictated coordinates are hypotheses — re-verify each against the artifacts at take-up per the promotion rule.   PREREQ: re-check `cargo audit` — **33rd consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis: an advisory-DATABASE fault, not a Conductor defect — the zero-dep-delta footing was RE-VERIFIED at `2026-08-20-latency-regression-re-proof` with `Cargo.toml`/`Cargo.lock` byte-untouched; overlap: `cargo deny check advisories bans licenses sources` observed true exit 0 over the current lock, VERIFIED not assumed; PROBE-AUTO-SATISFY signature = `cargo audit` true exit 1 whose first diagnostic line is `duplicate advisory ID: RUSTSEC-2026-0244` AND `cargo deny` true exit 0 — reproduce that byte-identically and the pin is satisfied by the one-line record `probe unchanged, 33rd consecutive`, no basis re-authoring; ANY deviation (changed diagnostic, moved exit code, overlap shift, dependency delta) restores the FULL form; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-20-read-back-seam-survivors-closed — archived at the 2026-08-20 wrap

[2026-08-20-read-back-seam-survivors-closed] Read-back seam survivors closed — the five out-of-family audit survivors gain killing assertions (code-audit baseline 2026-08-20, §B2 remainder)   CARRY (from the 2026-08-20 Epoch-3 code audit, remainder after `2026-08-20-verifier-self-hardening`): the five survivors OUTSIDE that chunk’s four named families, all measured still-alive by its scoped re-run — **`jsonrpc.rs:41:22` ×2** (`+=` → `-=` / `*=` on the request-id counter in `JsonRpcSession::request`: the id is what correlates a response to its request, so a mutated increment must break correlation, not just arithmetic) · **`jsonrpc.rs:65:9`** (`JsonRpcSession::notify` → `Ok(())`, i.e. the `notifications/initialized` step silently not sent) · **`preflight.rs:357:9` + `:357:12`** (`ShapeWitness::list` → `()` and its `!` deleted — the one-shot key-set witness that must record the FIRST answering call, never once per attempt and never attempt-0-only). All five sit in `conductor-verify`, whose seam is reachable through the PUBLIC `ReadbackClient::connect_transport` duplex (the line-bound tests are the worked precedent), so no visibility widening is needed. Acceptance: a scoped `cargo mutants -p conductor-verify --test-tool=nextest -f crates/conductor-verify/src/jsonrpc.rs -f crates/conductor-verify/src/preflight.rs` shows these five killed with the previously-killed set still dead; nextest stays zero-retry. With this entry every audit-surfaced survivor is either killed or classified accepted-deliberate before the next boundary’s trend measurement. Dictated coordinates are hypotheses — re-verify each against `c-mutation-conductor-verify.json` at take-up.   PREREQ: re-check `cargo audit` — **34th consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-DERIVED at `2026-08-20-verifier-self-hardening`, which moved the lock for the first time in the chain: the delta was ONE removed dev-edge line with **zero package-set change**, and `cargo deny check advisories bans licenses sources` was observed true exit 0 over the POST-removal lock, so the advisory-DATABASE fault remains the sole cause and the overlap remains verified rather than assumed; PROBE-AUTO-SATISFY signature = `cargo audit` true exit 1 whose first diagnostic line is `duplicate advisory ID: RUSTSEC-2026-0244` AND `cargo deny` true exit 0 — reproduce it byte-identically and the pin is satisfied by the one-line record `probe unchanged, 34th consecutive`, no basis re-authoring; ANY deviation (changed diagnostic, moved exit code, overlap shift, a dependency delta that ADMITS a package) restores the FULL form; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-21-severity-lifecycle-live-proof — archived at the 2026-08-21 wrap

[2026-08-21-severity-lifecycle-live-proof] severity-lifecycle live proof — auto-resolve and resolution summary observed through read-back (P-019..P-023, P-059, P-060)   PREREQ: re-check `cargo audit` — **35th consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-DERIVED at `2026-08-20-read-back-seam-survivors-closed`, which moved the lock a second time in the chain: the delta was ONE ADDED dev-edge line (`assert_fs` under `conductor-verify`) with **zero package-set change**, and `cargo deny check advisories bans licenses sources` was observed true exit 0 over the POST-addition lock, so the advisory-DATABASE fault remains the sole cause and the overlap remains verified rather than assumed; that wrap also SHARPENED the playbook dependency-under-red-audit rule to key the pin's form on PACKAGE ADMISSION rather than on whether the lock moved, which is why an edge-only delta left this pin compact; PROBE-AUTO-SATISFY signature = `cargo audit` true exit 1 whose first diagnostic line is `duplicate advisory ID: RUSTSEC-2026-0244` AND `cargo deny` true exit 0 — reproduce it byte-identically and the pin is satisfied by the one-line record `probe unchanged, 35th consecutive`, no basis re-authoring; ANY deviation (changed diagnostic, moved exit code, overlap shift, a dependency delta that ADMITS a package) restores the FULL form; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-21-per-check-latency-measurement — archived at the 2026-08-21 wrap

[2026-08-21-per-check-latency-measurement] Per-check latency measurement — sub-5s budgets beneath the closed slo_tier set, per-check latency_ms carried in the run-report envelope   PREREQ: re-check `cargo audit` — **36th consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-VERIFIED at `2026-08-21-severity-lifecycle-live-proof`, which moved NO dependency at all — `Cargo.toml` and `Cargo.lock` byte-untouched, zero package admission — so the advisory-DATABASE fault remains the sole cause and the overlap stays verified rather than assumed; that wrap discharged the 35th in the PURE auto-satisfy form the 34th predicted, the first such fire; PROBE-AUTO-SATISFY signature = `cargo audit` true exit 1 whose first diagnostic line is `duplicate advisory ID: RUSTSEC-2026-0244` AND `cargo deny check advisories bans licenses sources` true exit 0 — reproduce it byte-identically and the pin is satisfied by the one-line record `probe unchanged, 36th consecutive`, no basis re-authoring; ANY deviation (changed diagnostic, moved exit code, overlap shift, a dependency delta that ADMITS a package) restores the FULL form; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-21-delegated-timing-budgets-proven — archived at the 2026-08-21 wrap

[2026-08-21-delegated-timing-budgets-proven] Delegated timing budgets proven — halo hue, constellation discovery, report render and counter refresh at real values (P-025, P-027, P-037, P-045)   CONTEXT: the external block cleared 2026-08-21 — Pulse SHIPPED all three missing observables (chunk `2026-08-21-delegated-timing-observables`; absorbed at Pulse HEAD `f0c38f5`): `metric.report.render_ms` (P-037, pre-existing) · `metric.constellation.hue_update_ms` (P-025) · `metric.constellation.discovery_ms` (P-027) · `metric.findings.counter_refresh_ms` (P-045) — each a `telemetry.frontend` procedure behind an EXACT allowlist leaf, firing proven by Pulse webview tests (positive + paired negative). These four names are the chunk's coordinates. Evidence: that chunk's report + its P-075 `notes` — cite, never copy.   PREMISE (P-025, research not task): the Halo State Pulse canvas is ORPHANED at Pulse HEAD (zero non-test JSX, verified twice), so the severity→hue semantic lives on the CONSTELLATION DOT and `hue_update_ms` measures THAT; `halo-hue-encoding.toml:27` still grades the hue by operator observation in halo wording — expect a premise refine at the chunk, capability semantic UNCHANGED. The canvas's own fate is Pulse's "Halo State Pulse canvas disposition" entry — never scope it here.   PREREQ: re-check `cargo audit` — **37th consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-VERIFIED at `2026-08-21-per-check-latency-measurement`, which admitted ZERO packages — `Cargo.toml` and `Cargo.lock` byte-untouched — so the advisory-DATABASE fault remains the sole cause and the audit↔deny overlap stays verified rather than assumed; that wrap discharged the 36th in the PURE auto-satisfy form, the second such fire; PROBE-AUTO-SATISFY signature = `cargo audit` true exit 1 whose first diagnostic line is `duplicate advisory ID: RUSTSEC-2026-0244` AND `cargo deny check advisories bans licenses sources` true exit 0 — reproduce it byte-identically and the pin is satisfied by the one-line record `probe unchanged, 37th consecutive`, no basis re-authoring; ANY deviation (changed diagnostic, moved exit code, overlap shift, a dependency delta that ADMITS a package) restores the FULL form; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-08-22-operator-pause-and-checklist-live-firing
PREREQ: re-check `cargo audit` — **38th consecutive** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap, re-pins silently; basis RE-VERIFIED at `2026-08-21-delegated-timing-budgets-proven`, which admitted ZERO packages — `Cargo.toml` and `Cargo.lock` byte-untouched — so the advisory-DATABASE fault remains the sole cause and the audit<->deny overlap stays verified rather than assumed; that wrap discharged the 37th in the PURE auto-satisfy form, the THIRD such fire; PROBE-AUTO-SATISFY signature = `cargo audit` true exit 1 whose first diagnostic line is `duplicate advisory ID: RUSTSEC-2026-0244` AND `cargo deny check advisories bans licenses sources` true exit 0 — reproduce it byte-identically and the pin is satisfied by the one-line record `probe unchanged, 38th consecutive`, no basis re-authoring; capture the audit exit code BEFORE any pipe, since `$?` after `| head` reports the pipeline's last stage and reads as a false deviation; ANY real deviation (changed diagnostic, moved exit code, overlap shift, a dependency delta that ADMITS a package) restores the FULL form; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)


## 2026-08-31-p-075-assert-round — archived at the 2026-09-01 wrap

[2026-08-31-p-075-assert-round] P-075 assert round — the read-back fidelity Pulse's last 0.3.0 capability can honestly claim, and the payload-level premise measured false   CONTEXT (from the 2026-08-31 boundary adaptation): the operator directed both this entry and its disposition — mint at the Epoch-5 head, ahead of Webview self-verify — against a premise measured at Pulse HEAD `83d4060` and re-verified independently at this wrap. (1) The round must NOT re-assert the three delegated budgets: `v2-20` is ALREADY `verified` by `2026-08-21-delegated-timing-budgets-proven`, its ref naming three shipped tests in `crates/conductor-run/tests/delegated_timing_harvest.rs` (P-027 discovery <=5s · P-037 render <=2s · P-045 refresh <=1s). P-025's <=2s clause was excluded there by the 2026-08-21 operator ruling after measurement disproved its premise (staleness, not latency) and is owned by Epoch 6's _Halo hue budget re-driven_. P-075's Pulse-side claim closes by QUOTING those fixtures as its external evidence reference — exactly what v2-20's acceptance text already anticipated. (2) Payload-level content fidelity stays unattainable, so this entry RECORDS a second premise disproof rather than building an assert: `incident_events` (new in the five-table corpus at `user_version 2`) is written by `crates/triage/src/incident/persistence.rs::save_incident_event` and read only corpus-side (`crates/corpus/src/disposition.rs`, plus an `event_kinds` test helper in `contract.rs`) — ZERO references in `crates/mcp-server`, whose eight wire tools are pinned at `crates/mcp-server/src/tools.rs:44-51`, so no MCP tool surfaces the table at any width. The 2026-08-16 posture in architecture §Read-Back Dependency Posture therefore holds unchanged: under deterministic L4 every L4-authored field is a fixture constant and freshness stays the carrier. (3) What IS runtime-real through MCP today, and is this entry's positive content: `mark_incident_resolved`'s applied/declined round-trip and `query_incident_list` active-set membership — runtime-STATE fidelity, never payload fidelity. (4) `incident_events`-through-MCP is recorded as a PULSE 0.4.0 residual candidate for the operator (it pairs with the big-brother MCP direction there) — noted, deliberately not Conductor work. This entry claims NO verification-matrix capability: its content is evidence-quoting plus premise recording, so its wrap's coverage gate is a no-op by construction.   ↓


## 2026-09-01-webview-self-verify-windows-host — archived at the 2026-09-01 wrap

[2026-09-01-webview-self-verify-windows-host] Webview self-verify on the Windows host — agent-driven affordance proof through the real Tauri window; operator legs reserved for judgment items   CONTEXT (from the 2026-08-22 boundary adaptation): two operator rulings set this entry's premise — (1) this host is Windows-only and no Linux runner exists or is planned, so platform-foreign legs moved to the Epoch-6 tail; (2) agent-driven GUI verification is the DEFAULT, the operator's eyes reserved for judgment items (the ManualCheck class) and an occasional holistic pass, never routine affordance proof. Measured basis at this wrap: `test-plan.md` §2 (:54) names tauri-driver as the desktop-webview driver and `crates/conductor-tauri/ui/package.json` carries the stack (`@crabnebula/tauri-driver ^2.0.9` · `@axe-core/webdriverio` · wdio 9), but NO driver binary exists on this host — `tauri-driver` and `msedgedriver` are both absent from PATH, and the installed npm package ships Rust source rather than a binary. The "Linux+xvfb only" framing is a CI-runner assumption, not a Windows verdict: test-plan's own justification (:465) is macOS-specific ("no WKWebView WebDriver on macOS") and Windows is never named in the E2E job. Tauri documents Windows support via msedgedriver — NOT yet measured here; this entry's research owns that probe. Once a driver exists the pipeline's own rules take over: "headful self-verify available" makes the ui-smoke a Test Commands gate.   CARRY (from 2026-08-31-p-075-assert-round, operator correction post-commit): **an over-scoped claim is live in a spec master and this entry's wrap owes the amendment.** `architecture.md` §Read-Back Dependency Posture states "At most ONE incident is active per workspace" — WRONG. Pulse's dedupe key is the `(kind, scope, scope_id)` TUPLE: `.find(|inc| inc.kind == kind && inc.scope == scope && inc.scope_id == scope_id)` over `registry.list_active(&digest.workspace)`, measured at `pulse-app/src/inference_runtime.rs:811`, HEAD `83d4060` — the workspace scopes the CANDIDATE SET, the tuple is the key. The originating leg saw workspace-level behaviour only because its storms shared one tuple; the leg evidence stays valid, the generalisation does not. Fold THREE consequences: (a) re-scope to the tuple and cite the predicate; (b) "spared-control unattainable" narrows to WITHIN-tuple — a CROSS-SCOPE two-incident control is possible and untested, so record it as a weighable route option, never a retirement (the liveness attribution stands as delivered); (c) the freshness-caveat retirement narrows the same way — a concurrent unrelated incident cannot arise within the tuple but can across scopes. Two sites are amendable (`architecture.md:67` · the `lifecycle_harvest.rs:23` module doc) and two are known-stale with no sanctioned channel (`master-route.md:107`, which is `@import`ed into every session, and the chunk's own `report.md:79`). Site list swept 2026-09-01 — RE-SWEEP rather than trusting it.   PREREQ: re-check `cargo audit` — **41st consecutive**, re-pinned in FULL form because the BASIS CHANGED (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap). The 40th was discharged at the `2026-08-31-p-075-assert-round` wrap and the signature reproduced byte-identically — `cargo audit` true exit 1, first diagnostic `duplicate advisory ID: RUSTSEC-2026-0244`; `cargo deny check advisories bans licenses sources` true exit 0 — but that chunk TOUCHED a manifest (`crates/conductor-run/Cargo.toml`, +6 lines), so the prior basis "every manifest byte-untouched" no longer holds and the compact form is not available. The CONCLUSION is unchanged and re-verified: the delta declares an EMPTY cargo feature (`[features] live-pulse = []`) with no dependency, so it admits ZERO package nodes, `Cargo.lock` is byte-unchanged, and `cargo deny`'s coverage set is identical — deny was run and VERIFIED green over that state, not assumed (playbook 2026-08-20: the pin's form keys on whether the delta ADMITS A PACKAGE, not on whether a manifest moved). The advisory-DATABASE fault therefore remains the sole cause and the audit<->deny overlap stays the coverage. PROBE-AUTO-SATISFY signature is unchanged; capture the audit exit code BEFORE any pipe, since `$?` after `| head` reports the pipeline's last stage; ANY further deviation restores the full form again; remedy stays the bounded wait — no floor raise, no `deny.toml` ignore, no CI edit; close the moment it parses; full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`)

## 2026-09-01-desktop-a11y-sweep
- CONTEXT (from the 2026-08-22 boundary adaptation): the entry previously read "operator-gated live-Pulse pass over the four accessible paths: axe, contrast, not-color-alone, keyboard-trap escape, focus restoration, reduced-motion". Under the agent-driven-by-default ruling that list SPLITS: axe, contrast, not-color-alone, keyboard-trap escape, focus restoration and reduced-motion are agent legs through the Webview self-verify entry at the head of this epoch; what stays operator-gated is the judgment class — the ManualCheck items and an occasional holistic pass. The display-gate question this entry inherited is resolved at route level HERE, not per chunk.
- CARRY (from 2026-09-01-webview-self-verify-windows-host, operator route pre-direction): **this entry moved ABOVE _Live per-P-ID verdict lamps_ so the harness is calibrated to zero BEFORE any UI chunk develops against it** — the harness exists to verify UI per-chunk, so every later UI chunk should land holding zero green under its own ui-smoke gate. The `--e2e` leg is **driven-red until this entry closes it**, and the red has exactly two contents, both measured 2026-09-01: (a) **1 genuine axe violation** on the Minimal-tier baseline, and (b) **2 specs whose SUBJECT does not exist outside a driven live-Pulse run** — the operator-pause `alertdialog` and the operator-checklist rows. Give those two the **context-skip shape**: subject absent ⇒ skip-with-reason, never fail. Two specs already pass live (Blocked-vs-Fail token distinctness; WCAG AA contrast 4.5:1 / 3:1), so the sweep starts from 2/5 passing, not from zero. Until it lands the light-gate reading is: **skip-arm green without `CONDUCTOR_MSEDGEDRIVER`; driven red = expected per this record, never a silent skip.**
- PREREQ: re-check `cargo audit` — **42nd consecutive, COMPACT form** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap). The compact form is available again because the BASIS is restored: `2026-09-01-webview-self-verify-windows-host` touched no Cargo manifest, admitted zero package nodes and left `Cargo.lock` byte-unchanged, so "every manifest byte-untouched" holds once more (the 41st needed the full form only because its predecessor had touched `crates/conductor-run/Cargo.toml`). Discharge unchanged: reproduce the PROBE-AUTO-SATISFY signature — `cargo audit` true exit 1 with first diagnostic `duplicate advisory ID: RUSTSEC-2026-0244`, `cargo deny check advisories bans licenses sources` true exit 0 — capturing each exit code BEFORE any pipe; any deviation restores the full form; remedy stays the bounded wait (no floor raise, no `deny.toml` ignore, no CI edit); close the moment it parses. Full rationale: `conductor-0.2.0/chunks/2026-08-14-canary-fingerprint-feed-capture/report.md`.

## 2026-09-01-live-per-p-id-verdict-lamps — archived at the 2026-09-02 wrap

[2026-09-01-live-per-p-id-verdict-lamps] Live per-P-ID verdict lamps — coverage rows carrying each capability's actual verdict state, not-yet-run rows visibly distinct from failed   CARRY (from 2026-08-09-sut-load-envelope): **the webview never got the load-envelope banner** — the cli caption and the Markdown report banner shipped, the third surface did not, so **a GUI user sees no envelope signal at all**: an over-envelope run renders in the desktop run-report exactly like a clean one, with nothing saying its read-back is not evidence about the SUT. Landing it needs (a) a NEW read-only `#[tauri::command]` exposing the run's `EnvelopeStatus` — `RunsDb::get_envelope` already returns it, but `conductor-tauri/src/commands.rs` has no reader — and (b) a run-level banner in `ui/src/components/RunReport.tsx` reusing `var(--status-residual)` with an always-rendered DOM text label, **outside the lamp column and never a seventh lamp** (the `not-conductors` Mode-cell precedent; `ReportState` stays five, `LAMP_META` stays six). Its axe / contrast / not-color-alone verification then rides the Epoch-5 desktop a11y sweep ABOVE this entry (reordered 2026-09-01 by operator pre-direction, so the sweep calibrates the harness to zero first and this chunk develops against a green gate). Deferred because at authoring time a UI change could not be verified on this host — **that premise is now retired: the Webview self-verify entry above resolved the gate on 2026-09-01, and the harness drives a real WebView2 session here (the gate is driver-availability, not display), so this chunk CAN verify its own UI change through `--e2e`** and `v2-07`'s acceptance names the Markdown report, which shipped.   PREREQ: close rust gate deferral (deferred since `2026-09-01-desktop-a11y-sweep`) — that chunk's modify-set was CSS/TS/JSON only (zero `.rs`, zero manifest, zero lockfile delta), so `cargo nextest run --workspace --profile ci` and `cargo clippy --workspace --all-targets` were deferred rather than re-run on a cold rebuild. Re-verify the premise before echoing it: a dependency or manifest delta arriving here falsifies the zero-delta rationale independently of the deferral.   PREREQ: re-check `cargo audit` — **43rd consecutive, COMPACT form** (standing deferral since `2026-08-08-sut-capability-manifest`; ratified at the `2026-08-10-workspace-key-divergence-probe` wrap). The compact basis holds: `2026-09-01-desktop-a11y-sweep` touched no Cargo manifest and left `Cargo.lock` byte-unchanged. Discharge unchanged: reproduce the PROBE-AUTO-SATISFY signature — `cargo audit` true exit 1 with first diagnostic `duplicate advisory ID: RUSTSEC-2026-0244`, `cargo deny check advisories bans licenses sources` true exit 0 — capturing each exit code BEFORE any pipe; any deviation restores the full form; remedy stays the bounded wait (no floor raise, no `deny.toml` ignore, no CI edit); close the moment it parses.

## 2026-09-02-screen-reader-manual-spec — archived at the 2026-09-02 wrap

[2026-09-02-screen-reader-manual-spec] Screen-reader manual spec — per-state NVDA/VoiceOver/Orca must-announce pass spec for the four run states

(the frozen line carried no annotation freight at its flip — its PREREQ-free promotion left nothing to strip; archived verbatim for the chronicle)

## 2026-09-02-cross-surface-envelope-parity — archived at the 2026-09-02 wrap

[2026-09-02-cross-surface-envelope-parity] Cross-surface envelope parity — CLI and Tauri identical envelope for one seed, with the stale rmcp wording reconciled   CARRY (from 2026-08-31-p-075-assert-round): the rmcp sweep this entry owns is now ONE site smaller and one site sharper. `obs-plan.md:35` ("MCP read-back client via rmcp 1.7.0") was corrected at that wrap because it sat inside a sentence the chunk's own amendment was rewriting — operator-ratified, on the reasoning that leaving a known-false clause in an authored line is worse than the ordinary pre-existing-drift dismissal. What REMAINS for this entry: `contracts/mcp-contract.toml`'s header comment still reads "the rmcp client negotiates DOWN to it". Verified 2026-09-01 that the other derived-tier mentions are already correct (`docs/stack.md`, `docs/conventions.md`, `docs/gotchas.md`, `rules/verification-harness.md:18`, `rules/testing.md:35` all record the 2026-06-27 removal), and that the dated `## Session Additions` entries naming rmcp are historical records to leave standing, not drift.   CARRY (from `2026-09-01-live-per-p-id-verdict-lamps`, operator wrap directive): **the desktop load-envelope banner's VISUAL proof has no owner, and this entry is where it lands.** The banner shipped with that chunk but no rendered DOM has ever asserted it: the a11y sweep ran before the surface existed, and the routine `--e2e` arm context-skips it because the seeded fixture journal records no `run_envelope` row, so its contrast rides the render-independent token-pair spec (`--status-residual` verified at 4.5:1 over both `--color-raised-1` and `--color-base`, both themes) and its populated render rides `conductor_run::read_envelope`'s round-trip plus the `run_envelope` mock-runtime IPC test. What this entry adds: seed an OVER-ENVELOPE run on the desktop (an `EnvironmentSuspect` row in the fixture runs dir's `run_envelope` table, which the journal-only seed does not create), then assert the banner's DOM label and run axe against that state — the one assertion the routine arm structurally cannot reach today.   PREREQ: close rust gate deferral (deferred since 2026-09-02-screen-reader-manual-spec) — `cargo nextest run --workspace --profile ci` + `cargo clippy --workspace --all-targets -- -D warnings` were deferred under the zero-`.rs`/manifest/lockfile-delta rule (that chunk shipped TypeScript, Markdown, a PowerShell script and an NVDA profile only); this entry touches Rust (the over-envelope fixture seed + the envelope parity), so they run here as mandatory.

## 2026-09-02-mutation-tier-restored-for-conductor-tauri — archived at the 2026-09-03 wrap

[2026-09-02-mutation-tier-restored-for-conductor-tauri] Mutation tier restored for conductor-tauri — the parity test's CLI binary declared in the build graph rather than found by chance   CONTEXT (operator WRAP directive 2026-09-02, item 1 — the corrective FIRST chunk of Epoch 6a, inserted so every later 6a chunk runs on hardened machinery): source `.andromeda/runs/2026-09-02T15-49-17-code-audit/proposals.md:35-77` (`mutation-tier-aborted`). `cargo mutants -p conductor-tauri` planned 43 mutants and tested **0** (exit 4, `cargo test failed in an unmutated tree`), leaving the epoch's most-touched unit — 13 of the 22 changed source files — with no test-quality measurement at all. Cause measured at that proposal's end-to-end resolution chain: the failing test `commands::tests::path7_the_two_surfaces_write_an_equal_envelope_into_one_runs_db` (`crates/conductor-tauri/src/commands.rs:487`) is a unit test inside a BIN target that reaches the CLI through `assert_cmd`'s `legacy_cargo_bin` fallback to `target/debug/conductor.exe`, while `conductor-tauri` declares no dependency on `conductor-cli` at all (`crates/conductor-tauri/Cargo.toml:26` is a comment). It is genuinely green under `--workspace` (which builds the sibling bin) and green under `-p conductor-tauri` against a PRE-EXISTING artifact dated 16:11; it aborts on a fresh target dir. ACCEPTANCE: the tier yields a score for `conductor-tauri`, and a runner-portability gate proves the test on a FRESH target dir. The FORM (escargot-style build · the test moved to the bin-owning package · an artifact dependency) is deliberately unnamed — P3-research territory.   PREREQ (standing, external decay — operator directive 2026-09-02): **re-check `cargo audit`; the advisory DB has not parsed since 2026-08-09.** Run it with the exit captured BEFORE any pipe (`cargo audit; AUDIT=$?`) — expect exit 1 + `error loading advisory database: … duplicate advisory ID: RUSTSEC-2026-0244` — and run `cargo deny check advisories bans licenses sources` UNCONDITIONALLY as the overlap probe (expect 0), never gated on whether the lockfile moved. **Chain, recorded honestly: 43 consecutive re-pins through `2026-09-01-live-per-p-id-verdict-lamps`; ONE un-probed chunk (`2026-09-02-screen-reader-manual-spec`, which ran no supply-chain probe under its zero-delta gate deferral, so its wrap re-pinned nothing); the 44th probe ran at `2026-09-02-cross-surface-envelope-parity`; this is the 45th.** Compact/auto-satisfy stays available ONLY while the signature reproduces byte-identically — a different error, or a parsing audit, ENDS the deferral and is reported, not re-pinned. Basis form for a dependency delta under this red gate: assert the PACKAGE COUNT (that chunk measured 564 → 564 with the lock moving one edge line) and `cargo deny` green over the NEW lock — never lockfile byte-identity. **This is an external-decay standing pin, NOT a source-delta gate deferral: never clear it on a rust-gate closure.**

## 2026-09-03-conductor-tauri-survivors-dispositioned — archived at the 2026-09-03 wrap

[2026-09-03-conductor-tauri-survivors-dispositioned] conductor-tauri survivors dispositioned — the crate's first mutation score turns into a killing test or a cited accepted-deliberate entry per survivor   CONTEXT (operator WRAP directive 2026-09-03, recorded pre-direction naming this entry and its slot): the tier was restored at `2026-09-02-mutation-tier-restored-for-conductor-tauri` and its FIRST score is the basis — 43 planned, 43 tested, **21 stable standing survivors** (18 missed + 3 timeout) across `commands.rs` 14 · `pause.rs` 4 · `main.rs` 3, enumerated in that chunk's `evidence/mutation-tally.md`. A 22nd — `pause.rs:85` `TauriResolver::kind -> "xyzzy"` — classified **missed on one run and unviable on another over an identical tree** (measured across that chunk's implement run and its wrap light gate), so treat `kind`'s two mutants as one disposition unit and expect the headline count to read 21 or 22. That chunk dispositioned NONE by design: the plan's criterion 2 was authored when the survivor set was unmeasurable by construction, and 8 of the 22 sit outside its touchpoints. SIX are in the security-plan §Input Validation forbidden domain and must be KILLED, never accepted-deliberate — `resolve_handle:38` IS the `resolve_under` traversal guard, plus its callers `scenarios_dir:44` / `runs_dir:48` / `manifest_path:52`, and `run_report:168` / `run_envelope:224`; `run_envelope:224 → Ok(None)` additionally breaks the layout-templates always-rendered-qualifier invariant. LIKELY SHAPE (hypothesis: the path helpers and catalog commands are unobservable without a staged repo root, and the run-data commands are currently tested at exactly their empty case — which IS the mutants' return value): killing them needs seeded fixtures inside `conductor-tauri`'s test module — a staged repo root plus a populated runs dir, the shape `.claude/rules/testing.md`'s committed-fixture + production-reader-round-trip entry already prescribes. ACCEPTANCE: the plan's criterion 2 verbatim — every NAMED survivor ends killed or classified accepted-deliberate against a cited standing rule, no numeric `--fail-under` threshold. BUDGET BASIS: the measured 4 m 18 s for 43 mutants at `--jobs 2` (measured at that chunk), never the 5400 s the plan had guessed.   PREREQ (standing, external decay — operator directive 2026-09-02; ratified at the 2026-08-10 wrap): **re-check `cargo audit`; the advisory DB has not parsed since 2026-08-09.** Run it with the exit captured BEFORE any pipe (`cargo audit; AUDIT=$?`) and run `cargo deny check advisories bans licenses sources` UNCONDITIONALLY as the overlap probe, never gated on whether the lockfile moved. SIGNATURE (probe-auto-satisfy): `cargo audit` exit **1** + first diagnostic `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244`, overlap `cargo deny` exit **0**. Reproduced byte-identically at the 2026-09-02-cross-surface-envelope-parity wrap (44th) and again at `2026-09-02-mutation-tier-restored-for-conductor-tauri` (45th, with deny green over a lock that moved 2 edge lines at 564 → 564 packages); **this is the 46th**. ANY deviation — a changed diagnostic line, a moved exit code, an overlap shift — restores the FULL form and is reported, never re-pinned. Basis form for a dependency delta under this red gate: assert the PACKAGE COUNT plus `cargo deny` green over the NEW lock, never lockfile byte-identity. **External-decay standing pin, NOT a source-delta gate deferral: never clear it on a rust-gate closure.** Full rationale: the `2026-08-09-interpretation-correctness-posture` chunk report.

## 2026-09-03-conductor-run-composition-root-survivors-dispositioned — archived at the 2026-09-03 wrap

[2026-09-03-conductor-run-composition-root-survivors-dispositioned] conductor-run composition-root survivors dispositioned — every standing mutation survivor gains a killing test or a cited accepted-deliberate entry   CONTEXT (operator WRAP directive 2026-09-02, item 2): source `.andromeda/runs/2026-09-02T15-49-17-code-audit/proposals.md:78-154` (`monotonic` · sizes.file_max). `crates/conductor-run/src/lib.rs` is the SOLE `monotonic` hit among the five scalars tested and grew 898 → 1012 → **1115** code lines across the Epoch 3/4/5 boundaries (raw 1196 → 1352 → 1530). An independent metric converges on the same file: 24 of the crate's 25 mutation survivors live in it, measured at that proposal's 25-row survivor table. Of the 18 carried survivors, 6 are ALREADY ratified accepted-deliberate (the `declares` class, ratified 2026-08-21 against `.claude/rules/testing.md:19`), leaving **12 carried with no recorded disposition**; the epoch killed ZERO standing survivors while the score rose 70.59 → 72.22 on a denominator grown by 26 mutants. SPLIT the work along the survivor clusters the proposal names — the preflight/contract cluster (`warm_up_canary_service` · `declares` · `observe_run_contract`, survivors 7-18) and the run cluster (`execute_scenario` · `fault_span` · `FaultKind::label`, survivors 19-24). Whether the FILE itself also splits is research territory; the disposition half does not depend on it and is the cheaper one.   PREREQ (standing, external decay): re-check `cargo audit` (standing deferral since 2026-08-09; ratified at the 2026-08-10 wrap; basis: the RustSec advisory DB itself will not parse — a DATABASE fault, not a tool fault, so no floor raise exists to make; overlap: `cargo deny check advisories bans licenses sources` runs green every chunk; full rationale: the `2026-08-09-interpretation-correctness-posture` chunk report). SIGNATURE: `cargo audit` exit **1** with first diagnostic `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244`, overlap `cargo deny` exit **0** — reproduced byte-identically at the `2026-09-03-conductor-tauri-survivors-dispositioned` wrap (the 46th); **this is the 47th**. Read the exit DIRECTLY, never through a pipe. ANY deviation restores the FULL form and is reported.


## 2026-09-03-live-pulse-preconditions-probed — archived at the 2026-09-03 wrap

[2026-09-03-live-pulse-preconditions-probed] Live-Pulse preconditions probed before a leg is scheduled, so an absent SUT is named once rather than absorbed per chunk   CONTEXT (operator WRAP directive 2026-09-02, item 6 — placement RATIFIED by the operator at the 2026-09-03-conductor-tauri-survivors-dispositioned wrap: kept as placed, ahead of the entries whose legs need a live Pulse. The adaptation record's open point is closed; the enumerated alternatives at mint were folding into an existing entry, or `.andromeda/residuals.md`): source `.andromeda/runs/2026-09-02T15-24-02-evolve-diagnose/proposals.md:548` (L3 band-aid). The diagnosis counted **5 in-epoch facts** absorbing live-Pulse unavailability one chunk at a time (implement/smoke · fix-loop · gates ×2 · report) plus **4 more in Epoch 4**, one of them a soft-exit. Probe subjects: `127.0.0.1:4317` reachable · `andromeda-pulse-mcp` resolvable on the inherited `PATH` · the three `ANDROMEDA_PULSE_*` handles declared. hypothesis: naming the precondition up front keeps a cold gate re-run from measuring the absence and false-redding, which is the treatment the light-gate rule already prescribes case by case. Conductor does no Pulse process management by scope law, so the probe REPORTS — it never launches.   PREREQ (standing, external decay): re-check `cargo audit` (standing deferral since 2026-08-09; ratified at the 2026-08-10 wrap; basis: the RustSec advisory DB itself will not parse — a DATABASE fault, not a tool fault, so no floor raise exists to make; overlap: `cargo deny check advisories bans licenses sources` runs green every chunk; full rationale: the `2026-08-09-interpretation-correctness-posture` chunk report). SIGNATURE: `cargo audit` exit **1** with first diagnostic `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244`, overlap `cargo deny` exit **0** — reproduced byte-identically at the `2026-09-03-conductor-run-composition-root-survivors-dispositioned` wrap (the 47th); **this is the 48th**. Read the exit DIRECTLY, never through a pipe. ANY deviation restores the FULL form and is reported.

## 2026-09-04-sr-findings-remediation — archived at the 2026-09-04 wrap

[2026-09-04-sr-findings-remediation] SR findings remediation — the eight remaining screen-reader findings from the NVDA pass fixed at their defects, then the sr* leg re-run against the spec   CONTEXT (operator WRAP directive 2026-09-02, item 1 — ONE entry beside the A11y CI gate; evidence `conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/evidence/nvda-pass.json` + `leg-verdict.md` §Findings, row ids per finding): (1) initial focus sits in the picker input when the catalog is populated, so a keyboard user's first Tab skips the titlebar controls — S0-16 (review cause) + the `@foreground.initialFocus` record; (2) focusing a scroll region reads its whole content in one utterance (`Coverage rows` / `Run report rows` — the `tabindex=0` group carries the table as its accessible content) — S0-09, E0-05, E0-06; (3) assertive phase-line flips cancel the focus-restore announcement after a hold resolves — S2-07, S3-04; (4) a load-time `role="alert"` is silent until NVDA binds the window, heard only on reload — R0-01; (5) `aria-current` is spoken as "current", the ` · selected` text only on re-navigation — S0-06, S1-03; (6) the picker's filter-miss prose (`role="presentation"`) is never announced — S0-16; (7) the titlebar count is unlabeled and unannounced — S0-12, S1-02; (8) `Stop` during the LAST scenario announces `aborted` client-side then settles to `idle` while the backend records `Done` — S3-05 / T-01 and the 10:31 session; hypothesis: `drive_run` polls the abort flag only before a scenario starts (`conductor-run/src/lib.rs:936`). Fix each at its defect, never by relaxing a spec row; the sidecar console pane is the SEPARATE entry above, and the 14 browse-class rows are the A11y CI gate's CARRY (OS-level key injection), not this entry's.   CARRY (from `2026-09-02-screen-reader-manual-spec`, operator WRAP directive item 2a): **Guidepup as the agent-driven SR option — weighable here, NOT adopted.** `@guidepup/guidepup` 0.34.0 (+ `@guidepup/setup` 0.25.2) exists on npm and is maintained; the chunk's zero-dependency form (NVDA's own `-l 12` speech log over the existing WebdriverIO session) drove 33 of 51 rows `arm: agent` (evidence: `conductor-0.2.0/chunks/2026-09-02-screen-reader-manual-spec/evidence/nvda-pass.json`, per-row `arm`), so a driver package earns its place only if it reaches what the log cannot — chiefly the browse-mode rows in the next CARRY — and it would be a second automation stack plus a dependency delta under the standing advisory-DB deferral (test-plan §6, security-plan §Dependency Security). The default stays agent-driven wherever a driver exists; the manual arm is the interim, never the design.   CARRY (from `2026-09-02-screen-reader-manual-spec`, operator WRAP directive item 2b): **OS-level key injection for the 14 browse-mode rows — makes the manual arm zero.** The rows recorded `not-run-here` on the agent arm (S0-08, S0-10, S0-11, S0-12, S0-13, S0-14, S0-15, S1-02, S2-06, S3-06, S3-07, E0-01, E0-07, E0-08 — `nvda-pass.json` per-row `arm` + `note`) are static text NVDA reaches only through its browse-mode commands, which its OS keyboard hook sees and WebDriver-injected keys never deliver (measured on every session); `SendInput` / `SendKeys` from the leg's own process (or its activation script) would deliver them, turning the browse class agent-driven with no new dependency. Findings, never passes, never a manual arm, until this lands.   PREREQ (standing, external decay): re-check `cargo audit` (standing deferral since 2026-08-09; ratified at the 2026-08-10 wrap; basis: the RustSec advisory DB itself will not parse — a DATABASE fault, not a tool fault, so no floor raise exists to make; overlap: `cargo deny check advisories bans licenses sources` runs green every chunk; full rationale: the `2026-08-09-interpretation-correctness-posture` chunk report). SIGNATURE: `cargo audit` exit **1** with first diagnostic `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244`, overlap `cargo deny` exit **0** — reproduced byte-identically at the `2026-09-03-live-pulse-preconditions-probed` wrap (the 48th, package count 564 unchanged); **this is the 49th**. Read the exit DIRECTLY, never through a pipe. ANY deviation restores the FULL form and is reported.

## 2026-09-04-preconditions-probe-reads-path-handles-by-presence — archived at the 2026-09-04 wrap

[2026-09-04-preconditions-probe-reads-path-handles-by-presence] Preconditions probe reads path handles by presence — `handles-declared` is satisfiable and `boot` reaches the preflight   CONTEXT (operator WRAP directive 2026-09-04, item 1; measured at `conductor-0.2.0/chunks/2026-09-04-sr-findings-remediation/report.md` §Spec claims disproved #1): `observe_preconditions` passes all three `ANDROMEDA_PULSE_*` names through `declares()` (`crates/conductor-run/src/lib.rs:359`, truthy-only — it accepts a value of `"true"`/`"1"`), so the PATH-valued `ANDROMEDA_PULSE_DATA_DIR` can never declare; `conductor preconditions` therefore exits 1 under a FULLY CORRECT environment (measured 2026-09-04T06:41Z: `egress-reachable` and `sidecar-resolvable` satisfied, `handles-declared` unmet naming DATA_DIR alone, with a child process independently confirmed to receive the value intact), and `agent-run boot`'s leading arm has short-circuited before EVERY preflight since `480bc66`. SCOPE: presence-grading for the path handle, the truthy gate kept for the two flags; a unit test over the ENV READ (the existing tests construct the `declared` set directly and cannot catch it); both shells' `boot` proven to reach the preflight. The five masters amended at this chunk's wrap record the defect, not a fix — they are the re-verification surface when it lands.   PREREQ (standing, external decay): re-check `cargo audit` (standing deferral since 2026-08-09; ratified at the 2026-08-10 wrap; basis: the RustSec advisory DB itself will not parse — a DATABASE fault, not a tool fault, so no floor raise exists to make; overlap: `cargo deny check advisories bans licenses sources` runs green every chunk; full rationale: the `2026-08-09-interpretation-correctness-posture` chunk report). SIGNATURE: `cargo audit` exit **1** with first diagnostic `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244`, overlap `cargo deny` exit **0** — reproduced byte-identically at the `2026-09-04-sr-findings-remediation` wrap (the 49th, package count 564 unchanged); **this is the 50th**. Read the exit DIRECTLY, never through a pipe. ANY deviation restores the FULL form and is reported.

## 2026-09-04-sidecar-spawn-without-a-console-window — archived at the 2026-09-05 wrap

[2026-09-04-sidecar-spawn-without-a-console-window] Sidecar spawn without a console window — the GUI-launched MCP sidecar stops raising a foreground terminal pane that steals focus and shows its path   CONTEXT (operator WRAP directive 2026-09-02, item 1 — a Conductor spawn DEFECT, `CREATE_NO_WINDOW` class, a route candidate of its own): measured at `2026-09-02-screen-reader-manual-spec` (SR pass row S1-01, `security_finding`) — `conductor-verify/src/spawn.rs:80` builds `Command::new(PULSE_MCP_PROGRAM)` with no creation flags, so a Windows Terminal pane titled with the sidecar's absolute exe path takes the OS foreground ~200 ms after `Start`; any keyboard or screen-reader user loses focus to it, and the title is a host-path disclosure outside every redaction edge (recorded route-owned in security-plan §Anti-Patterns (a) + obs-plan §11). The a11y leg works around it by re-activating the app window; the fix belongs at the spawn (Rust, `conductor-verify`) — afterwards re-run the `sr` subject and retire the leg's re-activation step. Slot: beside the A11y CI gate with its sibling below, the orchestrator's placement — move it if the operator prefers another.   CARRY (from `2026-09-03-conductor-tauri-survivors-dispositioned`, operator WRAP directive 2026-09-03 item 5 — pinned here as the next entry touching conductor-tauri source): the doc comments above `run_report` and `run_envelope` in `crates/conductor-tauri/src/commands.rs` say an absent runs dir yields an empty list / `None` and "never an error". Measured true for `run_id = None` only — `latest_run_id` returns `Ok(None)` when `read_dir` fails (`conductor-core/src/run_journal.rs`) — and FALSE for a supplied id, which reaches `resolve_under` against a base that need not exist and errors. Both commands behave correctly; the comments read wider than they are. Recorded unfixed at that chunk (production code was out of its scope) in its `evidence/survivor-dispositions.md` §Adjacent finding; correct the two comments while editing this file.   CARRY (from `2026-09-03-live-pulse-preconditions-probed`, operator WRAP directive item 2, verified first-hand): the rustfmt EDITION mismatch — the PostToolUse hook runs bare `rustfmt "$f"` with no `--edition` and no `rustfmt.toml` exists, so an edition-2024 workspace gets 2015-style import sorting on every hook pass. Measured at that chunk: `render.rs` 222+/43−, `spawn.rs` 182+/6−, `cli_smoke.rs` 221+/37− are largely re-wraps of pre-existing lines, and it defeated one anchored Edit mid-burst by rewrapping between Read and Edit. The seeded hook ROW is a pipeline defect the overseer encodes — do NOT edit it. Instance remedy here: a one-file `rustfmt.toml` carrying `edition = "2024"`, which both bare `rustfmt` and `cargo fmt` honour and a setup re-render cannot overwrite. Note `cargo fmt --check` is not a gate in this project (absent from CI and `agent-run.sh`) and already flags ~745 spots workspace-wide, so the fix bounds FUTURE churn rather than promising a clean tree.   CARRY (from `2026-09-04-sr-findings-remediation`, operator WRAP directive 2026-09-04 item 2 — this entry re-runs the `sr` subject by its own text): **finding 1, initial focus.** The defect is CONFIRMED, not fixed: `tabs_to_start` measured live 3 · empty 6 · error 5, so the populated-catalog walk does not begin at the document start and the first Tab skips the titlebar controls (`evidence/nvda-pass.json`). The leg records only the COUNT, so measure WHICH element holds Chromium's sequential-focus starting point, then fix. Mechanism unidentified and nothing guessed: cmdk 1.1.1 has no `autofocus` of any spelling, both its `.focus()` calls are guarded on focus already being inside cmdk, and no `src/` file focuses at mount.   CARRY (from `2026-09-04-sr-findings-remediation`, operator WRAP directive 2026-09-04 item 2): **finding 4, the load-time alert.** The fix shipped (the four `role="alert"` regions mount empty at first paint, so the error arrives as a change) but is UNPROVEN: the leg's R0-01 action still reloads the document, so a first-load announcement and a post-reload one are indistinguishable. Drop the reload from that action to discriminate it; the spec row and its `expected` were corrected at the chunk to say so.   PREREQ (standing, external decay): re-check `cargo audit` (standing deferral since 2026-08-09; ratified at the 2026-08-10 wrap; basis: the RustSec advisory DB itself will not parse — a DATABASE fault, not a tool fault, so no floor raise exists to make; overlap: `cargo deny check advisories bans licenses sources` runs green every chunk; full rationale: the `2026-08-09-interpretation-correctness-posture` chunk report). SIGNATURE: `cargo audit` exit **1** with first diagnostic `error loading advisory database: parse error: duplicate advisory ID: RUSTSEC-2026-0244`, overlap `cargo deny` exit **0** — reproduced byte-identically at the `2026-09-04-preconditions-probe-reads-path-handles-by-presence` wrap (the 50th, package count 564 unchanged); **this is the 51st**. Read the exit DIRECTLY, never through a pipe. ANY deviation restores the FULL form and is reported.

## 2026-09-05-audit-corrective — archived at the 2026-09-05 wrap

[2026-09-05-audit-corrective] Audit corrective — render.rs survivors dispositioned, civil_from_unix boundary-date kill, jsonrpc.rs timeouts owned, M2's three dedups, and conductor-run/src/lib.rs split along named seams with its inline tests moved to tests/   CONTEXT (from the 2026-09-05 boundary-#4 code audit, `.andromeda/runs/2026-09-05T08-35-52-code-audit/proposals.md`; minted by operator directive at the 2026-09-05 0-pending adaptation as Epoch 6b's corrective head — the founder ruled the `lib.rs` split INTO this chunk, not a separate entry): the audit is a TREND record at HEAD `59d5b7c` against baseline `29c30b0` (Epoch 5), the 4th record in `code-metrics.ndjson`. Its code-facing findings, each with its coordinate: (1) `conductor-cli` first-ever mutation score **58.59** (117 mutants: 58 caught / 41 missed / 18 unviable) with **32 of the 41 survivors in `render.rs`** — the pure string builders (`paint` / `hold_line` / `envelope_caption` / `coverage_summary` / `wire` / `latency`), the `stdout_color` / `stderr_color` tty gates and the `coverage_summary_styled` comparisons (full list: proposals.md §First-audited units) — disposition each: kill, or accept-deliberate against a cited rule, in the shape the two Epoch 6a disposition chunks set; (2) `conductor-core` shard 1/4 scored 95.24 with all 6 survivors at `obs.rs:224-226` `civil_from_unix` — `-`/`+` swaps in the days-to-civil date arithmetic that no current test distinguishes; a BOUNDARY-DATE assertion (a month end, a leap day, an epoch edge whose civil date moves under each swap) kills them; shards 2–4 (378 mutants) are untested in this record; (3) `conductor-verify` `jsonrpc.rs:49:30` (`!=`→`==` in `JsonRpcSession::request`) and `:70:9` (`write_message` → `Ok(())`) are TIMEOUT mutants that have crossed two boundaries unowned — a timeout has no slot in the score formula, so name them: bound the await they hang, then kill or accept; (4) M2's three dedups — clone pairs 84 → 86 → 90 (proposals §M2): (a) a shared command prologue helper in `conductor-tauri/src/commands.rs` for the repeated resolve-and-map-error shape (self-clones 13 lines at 173/232, 12 at 409/508); (b) the CLI ↔ report coverage rendering pair (`conductor-cli/src/render.rs` ↔ `conductor-report/src/coverage.rs`, 9 + 8 + 8 lines) — a shared formatter in `conductor-report` the CLI calls, or two-surface parity accepted by construction and RECORDED as such; (c) a `tests/common` fixture in `conductor-run` for the canary/dispatch wire setup shared by `dispatch_wire.rs` / `canary_wire.rs` / `canary_obs_witness.rs` (13, 8, 7 lines); (5) `conductor-run/src/lib.rs` 1012 → 1115 → **1423** lines (+27.6 % at this boundary, hotspot #1, home of all 5 accepted-deliberate `conductor-run` survivors; max cognitive 8, so breadth not depth) — split along the seams the file already names (the preconditions / run-contract observation half, the canary warm-up half, the lifecycle / harvest helpers) into sibling modules of `conductor-run`, and move its inline `#[cfg(test)]` module to `tests/`. Expected amendment for THIS chunk's wrap: test-plan §12's accepted-deliberate roster coordinate `lib.rs:362:5` (`declares` → `false`; `test-plan.md:609`) MOVES with the split — re-point it to the new site in the same wrap; the tauri (`main.rs:18` / `start_run` / `run_thread`) and `spawn.rs` rosters are untouched. The `conductor-cli` render survivors are an ABSOLUTE first-score finding, not a drop — no prior cli record exists to trend against.   PREREQ (standing, external decay): re-check `cargo audit` (standing deferral since 2026-08-09; ratified at the 2026-08-10 wrap; MIGRATED here from *Operator-gated live suite* at the 2026-09-05 0-pending adaptation, where its basis was RE-VERIFIED and measured FALSE — so the compact form is retired and this pin stands in FULL form until the corrective chunk's wrap closes it). Measured 2026-09-05: the RustSec advisory-db upstream has carried exactly ONE `RUSTSEC-2026-0244` file (`crates/gettext-rs/`) since commit `e12b689b` (2026-08-09 16:57 +0200, "Move RUSTSEC-2026-0244 to correct crate") — the deferral's FIRST day; the duplicate that all 52 byte-identical readings reproduced (51 wrap re-pins + the 2026-09-05 dashboard probe) was an UNTRACKED leftover `crates/gettext-sys/RUSTSEC-2026-0244.md` in the LOCAL `$CARGO_HOME/advisory-db` checkout (`D:/dev/rust/cargo` on this host), whose git HEAD (`5a0ebedf`) matched upstream throughout — cargo-audit's fetch INTO the existing copy never removed it, so the old basis ("the DB itself will not parse — a DATABASE fault") described the cache, never the world; CI's clean checkout passed the same `cargo audit` step (run 33954347685, 2026-09-05T08:06Z) and a fresh clone under `--db` exited 0. The cache was cleaned 2026-09-05 (`git -C $CARGO_HOME/advisory-db status --porcelain` prints nothing) and `cargo audit` now exits **0** — 1239 advisories loaded, 564 crate dependencies scanned, 0 vulnerabilities, `warning: 18 allowed warnings found` (17 `unmaintained` + 1 `unsound`, all `deny.toml`-justified); overlap `cargo deny check advisories bans licenses sources` exit 0. NOT closed here — a 0-pending wrap runs no light gate. THIS chunk's plan lists the probe in `## Test Commands` as TWO steps in order: `git -C $CARGO_HOME/advisory-db status --porcelain` (must print nothing — untracked residue is what a fetch leaves behind), then `cargo audit` (expected exit **0** with the 18-warning summary; exit read DIRECTLY, never through a pipe; output redirected INTO `target/`), plus the deny overlap; its wrap closes the deferral per route-resolve §Deferred-gate closure and, in the same pass, re-bases the two master sites that still state the retired "external advisory-DB fault" — `architecture.md` §Infrastructure Patterns *Build system* (:196) and `security-plan.md` §Dependency Security (:177) — to the measured cache-residue cause. Any RED at take-up is a NEW event (a fresh advisory against the static tree, or fresh residue), never a continuation of the old signature.

## 2026-09-06-operator-gated-live-suite — archived at the 2026-09-06 wrap

[2026-09-06-operator-gated-live-suite] Operator-gated live suite — re-runnable live-Pulse proof invocation carrying its evidence, never a CI gate   CARRY (from `2026-09-01-desktop-a11y-sweep`, operator wrap directive): the driven a11y arm's live precondition is SELF-COLLIDING — `conductor preflight` fires its own canary storm, and Pulse dedupes a new incident against any OPEN one on the same `(kind, scope, scope_id)` tuple (as measured at `andromeda-pulse pulse-app/src/inference_runtime.rs:811`, HEAD `83d4060`), so a second canary inside the ~120s idle + 30s resolver window forms no FRESH incident and preflight blocks. The RELIABLE shape is the quiet window, and it is already recorded as part of the leg's firing form (test-plan §6). The DESIGN option this entry may weigh — **not decided** — is a PER-RUN CANARY IDENTITY: salt the canary's `scope_id` so the preflight tuple and the driven-run tuple differ by construction, removing the collision instead of waiting it out. Weighable here because this entry's stated job is a re-runnable live-Pulse proof invocation; it stays **never a CI gate** either way, and test-plan §9's live-leg ban is unchanged.   CARRY (from `2026-09-03-conductor-run-composition-root-survivors-dispositioned`, operator WRAP directive item 2): three `conductor-run` mutation survivors are accepted-deliberate at the TIER because no hermetic test can reach their sites, but their OBSERVABLES are live-path facts a driven run can assert from persisted records — so the coverage is owned here rather than parked inside an accepted class. Assert, on a live-Pulse driven run: (1) `Observation.degraded == true` on a declare-only scenario whose read-back finds an empty active set (the `ReadBack::AutoResolved` arm, `conductor-run/src/lib.rs:518`); (2) the persisted `latency_ms` equals `read_back_observed_at − journal_emitted_at` on the manual path (`:559`) — a subtraction, not a sum or quotient; (3) the `preflight blocked: readiness gate not satisfied` self-obs line appears on a NOT-ready gate and is absent on a ready one (`:66`), which needs a connected sidecar and therefore a live leg. Each is unreachable from a unit test for a measured reason (a fixed `http://127.0.0.1:4317` endpoint const contended under `--jobs 2`; a crate-private `Preflight`; a spawned `andromeda-pulse-mcp` absent on a hermetic host) — see the chunk's `evidence/disposition-ledger.md` classes B and C. Landing these does NOT retire the tier acceptances: the mutants stay accepted-deliberate (a live leg is never a CI gate, test-plan §9), and this CARRY buys the behavioural coverage the accepted class cannot.

## 2026-09-06-run-report-envelope-conformance-gate — archived at the 2026-09-06 wrap

[2026-09-06-run-report-envelope-conformance-gate] Run-report envelope conformance gate — every run journal row schema-complete and host-path-free, build failing on violation   CARRY (from 2026-08-21-per-check-latency-measurement): **the 5-command harness's `cleanup` verb deletes from `runs` alone and now leaves ORPHANS.** `scripts/agent-run.{sh,ps1}` run `DELETE FROM runs WHERE run_id = '$id'` only, while a run also writes `run_check` (this chunk, grain `(run_id, scenario, check_index)`) and `run_envelope` (pre-existing since `2026-08-09-sut-load-envelope`, never cleaned). The CONTRACT was corrected at that wrap — test-plan §3 cleanup body + verification and `.claude/rules/verification-harness.md` now name all three tables — so this entry owns only the CODE fix: add the two bound-parameter deletes to both shells and extend the count-zero verification. Note the shells interpolate the id into the SQL string rather than binding it (`sqlite3` CLI has no bind facility); it is gated by `valid_run_id`, so this is a recorded observation, not a live injection path.
## 2026-09-06-coverage-completeness-gate — archived at the 2026-09-06 wrap

[2026-09-06-coverage-completeness-gate] Coverage completeness gate — zero-gap classification over the current SUT set with every CI gate green   CARRY (from 2026-08-09-interpretation-correctness-posture): **the gate now has TWO axes, and this entry owns only the first.** `check_sut_drift` (classification↔manifest set-equality) is what "zero-gap classification" means; `check_scenario_backing` (`Auto`-classification↔scenario-catalog backing, over the `UNBACKED_AUTO` pin) already SHIPPED and is live in nextest — do not rebuild it, and do not fold it into this gate's acceptance. Note the roll-up it feeds: the auto term carries a derived `(N unbacked)` qualifier that is NOT a fifth summand (obs-plan §4 denominator semantics), so a gate reading those fields must keep the four per-mode counts summing to the row total.   CARRY (from 2026-08-09-current-sut-coverage-classification): **decide whether `coverage-matrix.md` becomes a real committed artifact.** `architecture.md` §Occupied Resources registers it as the definition-of-done artifact and `test-plan.md` §1 Critical Path 6 names it as the gate's surface, but the file has NEVER existed in this repo — `conductor coverage --write` generates it on demand and that chunk deleted the one it produced while verifying, since a plan declaring zero new files would otherwise have had `git add -A` commit it. This entry owns the call (commit it + gate on it · keep it on-demand and re-word the two specs · or gitignore it); the count/mode staleness on that same test-plan line was already amended, only the artifact question is open   CARRY (from 2026-08-09-out-of-scope-classification-treatment): **second consecutive chunk to do the create-inspect-delete dance** — that chunk's smoke also had to generate `coverage-matrix.md` to verify the Markdown surface, then delete it for the same reason. The cost is now recurring, not incidental: any chunk touching the Markdown coverage render pays it. Also note the gate's denominator is now pinned — `obs-plan.md` §4 records that `coverage_percent` is computed over the **in-scope** count (manifest set minus out-of-scope rows) while `p_id_count_expected` stays the full manifest count, so build the gate to that split, not to a single total   CARRY (from 2026-08-20-latency-regression-re-proof): **the load envelope's rate term measures DISPATCHES, not spans.** `max_sustained_rate_spans_per_s` is computed `occurrences / gap_ms` (`conductor-core/src/load_envelope.rs`), while a `Latency`/`Ramp` phase emits `samples`/`windows` spans per dispatch — `latency-regression` now puts 50 wire spans/s through a term that counts it as 1/s. No verdict moves today (the catalog sits ~200x under the bound) and the gate/caption shared basis is unaffected, but the term as named tells a reader it bounds wire load when it bounds dispatch rate, and a denser scenario could breach the real bound while passing the gate. Fix is Conductor-side: count `occurrences x samples`, or rename the term to what it measures — and whichever lands, `check_load_envelope` and `LoadEnvelope::classify` must move TOGETHER (they read one shared basis) with the `[[exempt]]` ledger re-verified still-empty. Recorded in arch §Occupied Resources; SURFACED-not-authored at that chunk.   CARRY (from the 2026-08-22 boundary adaptation): **Pulse's P-047 catalog is now EIGHT categories, Conductor's corpus is still seven.** Pulse HEAD `70344d5` ("a bare provider key no longer reaches stored fields") added `provider_key` to `crates/security/src/scrubber.rs` — anchored on published issuer prefixes (`[sr]k_(live|test)_…` · `sk-…` · `gh[pousr]_…` · `AKIA…` · `xox[baprs]-…` · `AIza…`) — and changed that module's own doc from "the 7 P-047 categories" to "the 8". Conductor's `conductor-emit/src/pii.rs:32` still documents "The seven P-047 PII categories Pulse's scrubber must detect and redact" over a seven-variant `PiiCategory`, with `all() -> [PiiCategory; 7]` fixing the arity and `PiiCorpus` storing in discriminant order — so an eighth arm moves the enum, the array type and the corpus, not just a doc line. This closes a loop Conductor opened: `2026-08-19-pii-scrub-live-proof` recorded the bare `sk_live_` recall gap as SUT intake, and Pulse has now discharged it. **`v2-14` stays VERIFIED** — it measured a state that genuinely held against the then-current SUT, and a SUT change invalidates only FUTURE legs; this is owned debt, never a re-opened chunk. Without the eighth category Conductor's P-047 claim is structurally narrower than the SUT it verifies.

## 2026-09-06-halo-hue-budget-re-driven — archived at the 2026-09-07 wrap

[2026-09-06-halo-hue-budget-re-driven] Halo hue budget re-driven — an error stream sustained through incident formation so the severity tier flips while the service is still emitting (P-025)   CONTEXT (from 2026-08-21-delegated-timing-budgets-proven): the <=2s bound measured 35581ms and 36705ms on two legs and the cause is structural, not slowness — `metric.constellation.hue_update_ms` computes `now - item.last_seen_unix_nano` at the tier change, i.e. STALENESS, so a service that goes quiet before the flip reports Pulse's own L2->L3(20-60s)->L4 formation latency. The identical formula passed at 702ms for `metric.constellation.discovery_ms`, which fires while `last_seen` is fresh — so the metric is sound and the pairing was wrong. Second cause: `halo-hue-encoding.toml` declares ZERO `[phases.emission]`, so it drives no tier change of its own and BOTH samples were the preflight canary's (`severity_tier: autonomous`, within a second of its incident). Attaining the bound needs a sustained emission window spanning formation, which interacts with the load envelope's `max_sustained_storm_ms` term — size it against `contracts/pulse-load-envelope.toml`, never by eyeballing. v2-20 was refined to the three proven budgets at that chunk (operator-ratified) and P-025 returned to the pool: it is UNVERIFIED, never un-verified. Evidence: that chunk's report + `v2-20` notes PREMISE-CORRECTION 2 + `crates/conductor-run/tests/delegated_timing_harvest.rs::p025_hue_update_is_measured_over_budget_by_a_staleness_mechanism`. **CARRY (from 2026-08-22-operator-pause-and-checklist-live-firing): the scenario's DECLARED TIER is unreachable and must be re-tiered in the same pass.** `halo-hue-encoding` declares `slo_tier = "<5s"` (its own header calls it "the fast tier") while its two phases total 6s of `gap_ms`, so `latency_ms` measured 6081 and 6080 on the two attended legs — over its own declared deadline, twice, from its own phase data. Nothing grades it today (declare-only, `verdict: null`), so it is latent rather than broken; it becomes a real failure the moment the scenario gains an `[[expected]]` check. Size the tier against the re-driven shape alongside the load-envelope sizing this entry already names.   CARRY (from `2026-09-06-operator-gated-live-suite`, 0-pending adaptation 2026-09-06): re-prove that chunk's observable 1 — the `ReadBack::AutoResolved` arm, which needs an EMPTY active set at read-back — on a FRESH Pulse data dir and INSIDE the canary service's bootstrap hour. Its `scenarios/auto-resolve-idle-window.toml` header (:29-32) states a MARGIN model that is known stale; the 200 s window value itself is fine, and the harvest literals stay as the record of the arm when it was reached. TWO distinct causes were measured, and they tune differently — a re-attempt that knows only one will mis-tune. (a) PAST the hour the arm is unreachable at any window length: `service_went_silent` cues raise NEW autonomous incidents during the silent phase itself, so a longer silence yields more of them — as measured at `pulse-legs/a11y-20260906-110201/logs/agent-latest.jsonl.2026-09-06`, run `2026-09-06T10-58-18-536` (`ManualCheck`, `latency_ms 200048`, 6 fingerprints) with incidents created 11:00:33 and 11:02:02 after cues of magnitude 3.0 → 5.0 → 7.03, the day's FIRST emitted silence cue being 10:11:09.723Z — one hour after the canary service's first span (~09:11Z), per `BOOTSTRAP_WINDOW_SECONDS = 3_600` at `andromeda-pulse crates/triage/src/baseline/activity_floor.rs:36` with the evaluator running each emit cycle at `crates/triage/src/cue/emitter.rs:186` (HEAD `83d4060`). (b) INSIDE the hour the window must clear 150 s (120 s idle + up to a full 30 s observer tick) measured from the LAST canary incident, not the first — leg A's own preflight can form more than one: as measured at run `2026-09-06T10-07-08-396` (`ManualCheck`, 3 fingerprints), which ran wholly inside the hour with ZERO silence cues in its window yet still failed, because incidents formed at 10:07:18 AND 10:07:53 and the later was only ~122 s old at read-back. Reached once, at 09:17Z, ~6 min into that hour.

## 2026-09-07-dependency-polish — archived at the 2026-09-07 wrap

[2026-09-07-dependency-polish] Dependency polish — indicatif bump preserving the stop-in-place hold spinner, plus opentelemetry-proto default-features trim   CARRY (from 2026-08-08-dependency-advisory-remediation): three PRE-EXISTING doc-vs-artifact gaps, none introduced by that chunk, all dismissed there under playbook:46 — arch §Stack/§Established Decisions/§Inherited Defaults name `tokio 1.48.x` (architecture.md:14, :41, :229) while the lock resolves **1.52.3**; arch §Stack/§Inherited Defaults name Tauri `v2.10.x / latest 2.10.1` (:26, :236) while it resolves **2.11.3**; and `deny.toml` carries 17 `[advisories] ignore` + 8 `[licenses] allow` entries while security-plan §Dependency Security's Accepted-exceptions paragraph names exactly one of each (`number_prefix` / RUSTSEC-2025-0119 and the `Zlib` allow). Reconcile the docs to the resolved artifacts here, where the dependency surface is already open   CARRY (from 2026-08-09-sut-drift-check): a fourth doc-vs-artifact gap of the same class, verified first-hand and likewise not that chunk's drift — `obs-plan.md:25` and `:645` both justify the Minimal tier with "8 workspace crates" while the workspace has **9** (stale since `conductor-run` was extracted on 2026-06-26). The tier conclusion is unaffected; only the count is wrong. **Extended at the 2026-08-21 setup re-run: there is a THIRD site this CARRY did not name — `test-plan.md:25` also justifies its Minimal tier with "8 workspace crates".** Fixing only the two obs-plan sites would leave test-plan stale, so treat the site list as three. Corollary held at that re-run: the derived leaves inheriting this count (`.claude/docs/obs-summary.md`, `.claude/docs/tests-summary.md`) were deliberately NOT corrected — a leaf re-derives what its master says, and fixing it ahead of the master would mint a fresh divergence; they self-heal at the next cascade once these three sites land   CARRY (from 2026-08-11-faithful-emission-dispatcher): **the `opentelemetry-proto default-features` trim now spans TWO dep sites, not one.** obs-plan §3's Transitive note scopes the dormant `opentelemetry` / `opentelemetry_sdk` footprint (and this trim) to `conductor-emit`'s dep alone; the emission-dispatcher chunk added `opentelemetry-proto` + `tonic` as **dev-dependencies** on `conductor-run` (the loopback wire stub for the per-family dispatch assertions, since `conductor-emit` cannot depend on `conductor-run`). Trimming default-features on `conductor-emit` alone will therefore NOT drop the dormant crates from the dev tree. The obs-plan §3 amendment was deliberately NOT taken at that wrap — dismissed under playbook 2026-06-21 (test-only use of a library the spec narrows to production scope; never in a release build, production posture unchanged) — so the fact is carried here, where the trim is actually performed, rather than as spec drift. No runtime edge was added: a draft `export_logs` helper naming the wire type was reverted in favour of an accessor that names none.   CARRY (from `2026-09-04-sidecar-spawn-without-a-console-window`, operator WRAP directive item 3): **`crates/conductor-verify` does not compile standalone.** `cargo check -p conductor-verify --lib` is RED at HEAD — `tokio::time::sleep` at `preflight.rs:336` while tokio's `time` feature sits in that crate's `[dev-dependencies]` (`Cargo.toml:31`) and NOT its `[dependencies]` (`:10`). Workspace feature unification hides it, so the crate builds in the workspace and passes `cargo test -p conductor-verify` (dev-deps supply the feature) while failing alone; no CI job builds a member standalone, so nothing catches it. PRE-EXISTING at HEAD (`git show HEAD` confirms both the call site and the identical feature list) and out of that chunk's scope. Remedy: declare `time` in `[dependencies]` and re-verify with `cargo check -p conductor-verify --lib`; then sweep the other members the same way, since the same masking applies to any crate whose feature is dev-only. Note architecture §Established Decisions [Module Boundaries] was QUALIFIED at that wrap (escalation E1) to stop promising a standalone per-seam build — landing this fix is what lets that qualifier be narrowed back.   CARRY (from the 2026-09-05 boundary-#4 code audit, operator directive at the 2026-09-05 0-pending adaptation): **install `knip` as a `crates/conductor-tauri/ui` devDependency plus a `knip` npm script** — the audit's A5 web column (`dead-code-web`) is `tool-missing: knip absent (not in package.json, not in node_modules)` (proposals.md §Skips), so the web plane has had no dead-code series at any boundary; `package-lock.json` moves with it and `npm audit --omit=dev` must stay clean (dev-tree advisories accepted at dev-tree grain, security.md). **StrykerJS is DECLINED by the founder (2026-09-05)** — small UI; the e2e/a11y legs cover it — so the `mutation-web` skip is not a gap to fill: the NEXT code-audit record lists it under `skips[]` as `declined` (that reason verbatim), never `tool-missing`.   CARRY (from `2026-09-05-audit-corrective`): the `conductor-run` split left **7 rustdoc intra-doc link warnings** — doc comments that resolved inside the one 1944-line module now point across sibling modules (`[RunRecord]`, `[HeadlessResolver]` ×2, `[persist]`, `[canary_gate]`, plus two public-doc-links-to-private-item on `dispatch` and `observe_run_contract`). Measured at that chunk's wrap light gate: `cargo doc --no-deps -p conductor-run` exits 0 and NO gate enforces rustdoc warnings (no `cargo doc` / `RUSTDOCFLAGS` in `ci.yml` or either `agent-run` shell), so nothing is red — this is owned housekeeping beside this entry's `cargo check -p conductor-verify --lib` red, not a regression to hot-fix. Fix at the link (re-path or `crate::`-qualify); the public API is unaffected (27 items, diff-identical across the split).   CARRY (from `2026-09-06-coverage-completeness-gate`, deferred by operator decision at that chunk's P4; placement ruled at this wrap's P5 armed halt — no tail entry naturally owns it, this is the nearest-plausible): **Pulse's P-047 catalog is EIGHT categories, Conductor's corpus is still seven.** Verified at Pulse HEAD `83d4060` (2026-09-06): `crates/security/src/scrubber.rs:3` reads "the 8 P-047 categories" with a live `provider_key` arm (`:107-108`, cases at `:149-152` — `sk_live_…` / `sk-proj-…` / `ghp_…` / `AKIA…`). Conductor-side, `crates/conductor-emit/src/pii.rs:32` still documents "The seven", over a 7-variant `PiiCategory` with `all() -> [PiiCategory; 7]` (`:53`), `PiiCorpus { values: [String; 7] }` (`:90`), a 7-element `seeded()` array literal (`:97-106`) and a `field_key()` match. Blast radius measured from the code-graph: 13 `all()` call sites — 10 in `pii.rs` tests, 2 in `conductor-emit/tests/pii_payload_corpus.rs` (`:101`, `:140`), 1 cross-crate in `conductor-run/tests/pii_harvest.rs:185` — plus the test `exposes_exactly_seven_distinct_categories` (`:325`) whose NAME pins the arity. **Determinism constraint the original CARRY never named (measured at `conductor-emit/src/pii.rs:97-106`): `PiiCorpus::seeded` draws all seven values sequentially from ONE `ChaCha8Rng`, and `all()` is documented as discriminant order = storage order = draw order — so the eighth variant must be APPENDED LAST; inserting it mid-order changes every subsequent corpus value and breaks every committed expectation.** `v2-14` stays VERIFIED (it measured a state that genuinely held; a SUT change invalidates only FUTURE legs) — this is owned debt, never a re-opened chunk. Subject fit is acknowledged imperfect: this entry is otherwise indicatif + opentelemetry-proto upkeep.

## 2026-09-07-a11y-ci-gate — archived at the 2026-09-07 wrap

[2026-09-07-a11y-ci-gate] A11y CI gate — the a11y specs as a CI gate on whatever runner the project gets, without Pulse, failing on any violation; redacted service-tagged JSON aligned to the obs-owned format. Linux+xvfb was an ASSUMPTION, not a capability verdict — the Windows WebView2 path is the measured one (2026-09-01/02), so the gate is runner-agnostic and only the CI arrangement is open   BLOCKED-ON: one green CI run of the a11y job after a push — the push is the operator's act. Two facts, both measured at HEAD `480bc66` (2026-09-04): both `ci.yml` jobs already run on `windows-latest` (`.github/workflows/ci.yml:18` rust gate, `:164` frontend gate), so a CI runner EXISTS; and the a11y job itself is absent from that workflow, **[dead clauses rewritten 2026-09-07, factual re-verify: the origin/ahead/no-run-in-a-month support was FALSE by take-up — `origin/build/conductor-0.2.0` had advanced to `e21361a`, the branch measured 0 ahead / 0 behind, and CI ran green on 2026-09-05, 2026-09-06 and again 2026-09-07 (run 34128631720). The block itself still stood on its ONE live clause — no a11y job existed in the workflow — which none of those three facts ever bore on.]** This SUPERSEDES the 2026-08-22 wording ("a CI runner (none exists; Windows-only host)"), which was inherited from the retired Linux-runner block (`.andromeda/runs/2026-08-22T21-17-21Z-wrap/adaptation-record.md`) and is measurably false; corrected by operator directive at the 2026-09-04 wrap (`.andromeda/runs/2026-09-04T01-35-00Z-wrap/adaptation-record.md`). The trajectory question stays dispositioned; /andromeda-phase Setup refuses promotion until that run exists, which is intended.   CARRY (from `2026-09-02-screen-reader-manual-spec`, plan §Implementation notes): the ui test README (`crates/conductor-tauri/ui/test/README.md`) still opens "This leg runs only on Linux + xvfb against a live Pulse" — a non-master project doc the 2026-09-01 sweeps measured false (the leg runs headfully on the Windows host; only macOS is driver-less) and that chunk deliberately left standing, editing only its pointer sentence. Reword it at THIS entry, which touches the ui test tree, to the measured platform set.   CARRY (from `2026-09-04-sr-findings-remediation`, operator WRAP directive 2026-09-04 item 3 — this entry owns the a11y suite): tighten the operator-checklist spec's subject-absent guard from a bare `[role="status"]` to `[role="alertdialog"]`. Measured: the bare token stands in for "a hold is raised" and was sound only while the checklist roll-up was the app's only `role="status"`; two a11y-plan-mandated live regions added at that chunk satisfied it, the skip stopped firing, and the spec asserted checkboxes it found none of. Worked around there by taking `aria-live` instead, which leaves the guard still loose for the next `role="status"` anyone adds.   CARRY (from `2026-09-04-sidecar-spawn-without-a-console-window`, operator WRAP directive items 1+2, placement ruled at that wrap's P5 halt): **three SR-leg findings, all measured, none fixed.** (1) **The load-time alert is NOT announced** — R0-01 graded `not-announced` with `heard: []` once the reload was dropped, so the prior chunk's fix (four `role="alert"` regions mounting EMPTY at first paint) is necessary but NOT sufficient: NVDA binds a window on its FIRST FOCUS EVENT, which necessarily follows a load-time error, so the text arrives in a region nobody is listening to yet. The remedy is a RE-ANNOUNCE mechanism (re-assert the message after first focus), not another mount tweak — that shape is disproved. Row R0-01 and its `expected` already say silence here is a FINDING, never a pass. (2) **The sequential-focus start point is still wrong** — `tabs_to_start` measured live 3 · empty 6 · error 5 with `initial_focus: "BODY"`, unchanged by that chunk. NEW fact from it: `browser.refresh()` had been RESETTING the start point, which is why dropping the reload exposed the defect at R0-02 (`the first Tab landed on "Close window", not "Minimize window"`). The holder is STILL UNMEASURED — cmdk 1.1.1 has no `autofocus`, both its `.focus()` calls are guarded, no `src/` file focuses at mount, yet the leg's own comment attributes it to "the picker input at mount". Measure WHICH element holds Chromium's sequential-focus-navigation starting point before designing any fix; the leg's `tabsToStart` cycling loop would mask one. (3) **The leg has real run-to-run announcement variance** — a harness-reliability finding against test-plan §10's zero-flake bar, recorded here rather than passed: two identical runs graded S0-01/S0-02 `not-announced` then `announced-as-expected`, and three `empty`-subject rows (E0-06/E0-07/E0-09) moved the same way, correlating with that subject's `nvda_named_window` flipping. Consequence for anyone reading this leg: ONE observation cannot separate variance from regression — take a second before concluding, as that chunk did to exonerate its own change.   CARRY (from `2026-09-06-operator-gated-live-suite`, wrap P2 fan-out): a PRE-EXISTING internal inconsistency in `a11y-plan.md`, surfaced by that wrap's a11y doc-agent as explicitly out of its detectors' scope and therefore proposed by nobody — `:565` still reads "reduced-motion emulation is the one platform-dependent assertion", a verdict `:218`, `:424` and `:597` already retired in an earlier chunk. Not that chunk's drift (its report changed no a11y fact), so it had no amendment channel; recorded here because this entry owns the a11y surface. Re-verify the four line numbers before acting — they are the agent's, unre-derived at that wrap.   CARRY (from `2026-09-07-dependency-polish`, operator wrap-directive item 2 — this entry named as the owner, the do-it call staying the founder's at promotion): **`knip` is installed on the web plane but its series is unusable — a `knip.json` entry-point config is OWED.** That chunk added `knip` as a `crates/conductor-tauri/ui` devDependency plus a `knip` npm script (report-only, no gate, `npm audit --omit=dev` still 0 production vulns and knip added ZERO advisories — 38 dev-tree before and after). Its first run returned **20 findings, all 20 false positives of ONE class**: WebdriverIO discovers specs through `wdio.conf.ts`'s `specs`/`suites` config and loads devDependencies through its own plugin resolution, and neither is an import edge, so knip's reachability graph cannot see the package's largest consumer — it flagged the three `test/a11y/*.e2e.ts` specs as unused files and five devDeps as unused, four of which (`@axe-core/webdriverio`, `axe-core@4.12.0`, `colorjs.io@0.6.1`, `lighthouse@13.0.3`) are a11y-plan §3-mandated BY NAME AND PIN. Nothing was deleted; every finding is dispositioned in that chunk's `evidence/knip-first-report.md`, and a11y-plan §11 forbids acting on such a report against the harness. This entry is the named owner because it owns the ui test tree and those specs ARE the false positives; knip ships a `wdio` plugin for exactly this. **Until the config lands, the next code-audit record's A5 `dead-code-web` column must read "tool present, series unusable (100 % FP)" — never a usable series, and never `tool-missing`, which is now false.**
