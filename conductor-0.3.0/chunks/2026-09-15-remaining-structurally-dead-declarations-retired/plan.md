# Plan — 2026-09-15-remaining-structurally-dead-declarations-retired: "Remaining structurally-dead declarations retired"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-09-15-remaining-structurally-dead-declarations-retired · **Version:** conductor-0.3.0 · **Generated:** 2026-09-15T14:25Z

## Goal

Retire the six `[[expected]]` declarations that cannot pass against a live Pulse, completing the class the
previous chunk opened. Each of the five affected scenarios keeps its `p_ids`, `slo_tier` and phase drive and
gains a `DECLARE-ONLY` header recording its own measured ground; the eight pinning tests that assert the
retired declarations move in the same change.

The grounds are not re-derived here — the previous chunk's committed enumeration is this chunk's input, and
P3 confirmed all six against Pulse HEAD `83d4060` (unchanged since the enumeration was written). Three fail
in the **vacuous-PASS** direction, which test-plan §6 records as the more dangerous half: `activity-floor`'s
`Absent`, `high-severity-log-capture`'s `Absent`, and `threshold-hot-reload`'s `Absent` can never fail, so
nothing ever alerts you.

This chunk completes `verification-matrix.json#v3-04`'s universal. After it, **two** `[[expected]]` blocks
remain committed — `root-span-error-scope` and `span-status-error-detection` — and both are satisfiable
(each `Contains "error"` matches the lowercase severity label `interpretation/src/markdown.rs`'s
`severity_label` renders). Every committed block is then satisfiable or retired, per the requirement's own
wording. That those two discriminate the deterministic fixture's severity constant rather than their own
stimulus is a weaker defect, routed by operator ruling to the `Scenario-assertion audit gate` entry
(`working-route.md:30`) and explicitly **not** resolved here.

## Implementation Steps

1. **Retire `activity-floor`'s block.** Remove the `[[expected]]` table at `scenarios/activity-floor.toml:67-70`
   (`Absent`/`Hard`/`"ServiceWentSilent"`). Add a `DECLARE-ONLY` header in the precedent shape
   (`scenarios/cross-incident-recurrence.toml:24-36`) attributing the marker and stating the ground: `CueKind`
   carries `#[serde(rename_all = "snake_case")]` (`andromeda-pulse crates/triage/src/contract.rs:181`, HEAD
   `83d4060`), so every render emits `service_went_silent` and the PascalCase spelling exists only as a Rust
   identifier — the `Absent` therefore passes **vacuously**.
2. **Retire `service-went-silent`'s block.** Remove `scenarios/service-went-silent.toml:35-38`
   (`Contains`/`Hard`/`"ServiceWentSilent"`). Same ground, opposite direction: a permanent FAIL. Header states
   both the ground and the direction.
3. **Retire both `high-severity-log-capture` blocks.** Remove `scenarios/high-severity-log-capture.toml:34-37`
   (`Contains`/`Hard`/`"ERROR"`) and `:40-43` (`Absent`/`Hard`/`"WARN"`). Ground: `severity_label`
   (`andromeda-pulse crates/interpretation/src/markdown.rs`) maps every severity to lowercase
   (`"info"|"warn"|"error"|"critical"`), so the uppercase tokens can never render — the `Contains` fails
   permanently and the `Absent` passes vacuously. One header covers both, naming each direction.
4. **Retire `exception-event-capture`'s block.** Remove `scenarios/exception-event-capture.toml:24-27`
   (`Contains`/`Hard`/`"exception"`). Ground, stated at the width P3 established: `dispatch_retrieve_report`
   (`andromeda-pulse crates/mcp-server/src/tools.rs:360-387`) composes report markdown through
   `assemble_report` → `serialize_report`, **both defined in `crates/interpretation/src/markdown.rs`**
   (`:249`, `:115`), so that one file is the sole composer; `git grep -c --fixed-strings 'exception' --
   'crates/interpretation/src/markdown.rs'` exits 1 (no matches), and the token is absent from the
   status/severity/title half too (the deterministic fixture pins `"autonomous"` /
   `"Deterministic verification incident"`, `pulse-app/src/deterministic_inference.rs:42-43`). Permanent FAIL.
5. **Retire `threshold-hot-reload`'s block.** Remove `scenarios/threshold-hot-reload.toml:37-40`
   (`Absent`/`Hard`/`"RetroactiveReeval"`). **State the tree-wide absence, not the serde-rename mechanism that
   explains its siblings** — the two grounds are different and only this one holds:
   `git grep -c --fixed-strings 'RetroactiveReeval' -- '*.rs'` exits 1 tree-wide, no snake_case sibling
   exists, and the only `retroactive` occurrences are two doc comments
   (`crates/triage/src/lifecycle/mod.rs:57,103`) stating Pulse applies threshold changes forward. The token
   was INFERRED at authoring and no producer ever emits it, so the `Absent` passes vacuously. The header also
   records that Pulse's prospective-only behaviour — what P-056 wanted to assert — is real and simply has no
   read-back token.
6. **Split `hard_signal_fixtures_load_and_validate`** (`crates/conductor-core/src/scenario.rs:551-567`). Its
   four `#[case]` rows mix two retirees with two survivors. Keep `span-status-error-detection` and
   `root-span-error-scope` on the existing `!s.expected.is_empty()` assertion; move
   `exception-event-capture` and `high-severity-log-capture` to a sibling asserting `s.expected.is_empty()`
   with a declare-only message, following the `statistical_anomaly_fixtures_load_and_validate` shape
   (`:569-586`). Both halves keep the `s.name` and `s.p_ids` assertions.
7. **Re-shape `p007_high_severity_log_capture_asserts_both_sides_of_the_boundary`** (`:631-651`). It asserts
   `any(Contains)` + `any(Absent)`, both retired. Replace with a declare-only assertion carrying the
   measurement in its doc comment, in the `pii_scrub_is_declare_only_with_the_measurement_recorded` shape
   (`:1278-1295`).
8. **Invert `activity_floor_and_silence_fixtures_load_and_validate`** (`:653-671`): `!s.expected.is_empty()`
   becomes `s.expected.is_empty()` with a declare-only message. Keep the `s.name` / `s.p_ids` assertions.
9. **Retire `activity_floor_and_silence_checks_are_all_hard`** (`:673-689`). `all(|c| c.class == Hard)` is
   **vacuously true** on an empty vec, so leaving it would ship a green assertion that tests nothing — the
   project's own vacuous-green discipline. Remove it; its declare-only standing is carried by step 8.
10. **Retire `activity_floor_asserts_the_learned_quiet_via_absent`** (`:723-738`) and
    **`presence_scenarios_assert_a_surfaced_incident_via_contains`** (`:740-756`, whose only case is
    `service-went-silent`). Both assert a retired check directly.
11. **Re-shape and RENAME `findings_counter_is_declare_only_and_threshold_reload_keeps_its_hard_absent`**
    (`:1297-1332`). Its name and its `:1325-1331` assertion both claim threshold-hot-reload keeps a `Hard
    Absent` this chunk removes. Its `:1299-1302` comment additionally states that check "remains satisfiable",
    which **contradicts the same chunk's committed enumeration** classifying it structurally dead in the
    vacuous-PASS direction; P3's re-derivation settles it in the enumeration's favour. Assert `is_empty()`
    for both scenarios, rename to drop the retired claim, and correct the comment — do not reproduce the
    "remains satisfiable" wording anywhere.
12. **Retire `scrub_pipeline_degraded_suite_mixes_hard_and_declare_only`** (`:1334-1368`).
    `threshold-hot-reload` is that family's last non-empty member, so `assert!(any(|empty| !*empty))`
    (`:1364-1367`) becomes false. Follow the severity-lifecycle precedent (`testing.md` 2026-06-22, RETIRED
    2026-08-21): when a family goes wholly declare-only the mixed-shape guard is retired, not weakened, and
    replaced by an `is_empty()` assertion across all six members.
13. **Leave `scrub_pipeline_degraded_fixtures_load_and_validate` (`:1239-1259`) unchanged** — it carries no
    blanket expected-assertion by design (`:1257-1258`). Its comment naming the family as mixed-shape is
    superseded by step 12 and is corrected there, not here.
14. **Mint no pinning guard.** CARRY 2's guard-minting arm has no subject: the wide sweep measured all five
    scenarios already pinned in `scenario.rs`. Record the discharge rather than silently dropping it.

## Codebase touchpoints

### New files
- None.

### Files to modify
- `scenarios/activity-floor.toml` — remove one `[[expected]]` block (`:67-70`); add DECLARE-ONLY header.
- `scenarios/service-went-silent.toml` — remove one block (`:35-38`); add header.
- `scenarios/high-severity-log-capture.toml` — remove two blocks (`:34-37`, `:40-43`); add header.
- `scenarios/exception-event-capture.toml` — remove one block (`:24-27`); add header.
- `scenarios/threshold-hot-reload.toml` — remove one block (`:37-40`); add header.
- `crates/conductor-core/src/scenario.rs` — the eight test changes of steps 6-12 (one file; all pins are
  inline `#[cfg(test)]`).

**Graph impact.** No production caller threads a value this chunk changes. `check_scenario_backing` /
`check_sut_drift` @ `crates/conductor-core/src/drift.rs` have 19 caller rows, all tests (15 in `drift.rs`,
2 in `crates/conductor-report/tests/coverage_gate.rs:42,59`, 2 crate-root re-exports at
`crates/conductor-core/src/lib.rs:40`); `UNBACKED_AUTO` has 14 references, none reading a scenario's
`expected`. The coverage-completeness gate is unaffected **by construction**: `check_scenario_backing`
builds its `covered` set only from `scenario_p_ids` (`drift.rs:165`), and this chunk changes no `p_ids`.

## Test Commands

No live leg and no boot path this chunk — every gate is `cargo`-tier over committed fixtures, so there is
no `smoke`, no `probe`-guarded `leg`, and no `self-verify` entry (not a UI/visual chunk; no frontend
delta). The supply-chain entries run despite a zero-dependency delta, per the standing rule. If `cargo
audit` goes red, the diagnostic that PRECEDES classifying the fault as external is
`git -C $CARGO_HOME/advisory-db status --porcelain` printing nothing — the corrected rule from the
deferral closed 2026-09-05; it is not listed as a gate because no deferral is standing.

```toml
[[gate]]
run = 'cargo nextest run -p conductor-core --profile ci'
role = 'unit'
note = 'the sole pin site — steps 6-13 all land here'

[[gate]]
run = 'cargo test -p conductor-core'
role = 'unit'
note = 'standing runner-portability gate (test-plan §4; v3-04 names it) — the suite must be green under BOTH runners'

[[gate]]
run = 'cargo nextest run -p conductor-report --test coverage_gate --profile ci'
role = 'unit'
note = 'both axes of the coverage-completeness gate; expected unaffected by construction (no p_ids change)'

[[gate]]
run = 'cargo nextest run --workspace --profile ci'
role = 'unit'

[[gate]]
run = '''grep -l '^\[\[expected\]\]' scenarios/*.toml | xargs -n1 basename | sort | paste -sd,'''
role = 'probe'
new = true
expect = ['exit 0', 'last line root-span-error-scope.toml,span-status-error-detection.toml']
baseline = 'red — prints all seven declaring scenarios (activity-floor,exception-event-capture,high-severity-log-capture,root-span-error-scope,service-went-silent,span-status-error-detection,threshold-hot-reload) at exit 0; the last-line atom fails. RED is the correct direction: the gate discriminates this chunk own work, and a green baseline would be the vacuity.'
note = 'the end-state SET, not a count (test-plan §6 forbids pinning a literal count as a gate)'

[[gate]]
run = 'cargo clippy --workspace --all-targets -- -D warnings'
role = 'lint'

[[gate]]
run = 'cargo audit'
role = 'probe'

[[gate]]
run = 'cargo deny check advisories bans licenses sources'
role = 'probe'
```

## Acceptance Criteria

- (unit) All six structurally-dead declarations are retired to declare-only in ONE change, each scenario
  keeping its `p_ids`, `slo_tier` and phase drive and carrying a header recording the marker and its own
  measured ground — completing `verification-matrix.json#v3-04` over the full twelve-block population
  (four retired by `2026-09-15-structurally-dead-assertion-class-retired`, six here).
- (unit) `grep -l '^\[\[expected\]\]' scenarios/*.toml | xargs -n1 basename | sort | paste -sd,` prints
  exactly `root-span-error-scope.toml,span-status-error-detection.toml` — every committed `[[expected]]`
  block is then satisfiable or retired (per test-plan §6).
- (tests) `cargo nextest run --workspace --profile ci` returns 0 AND `cargo test -p conductor-core` returns
  0 — the runner-portability gate (per test-plan §4 Framework + §10).
- (tests) All eight pinning tests asserting a retired declaration are updated in the same change, and no
  surviving assertion is vacuous: `activity_floor_and_silence_checks_are_all_hard` is retired rather than
  left green over an empty vec (per test-plan §11 Unit).
- (arch) No scenario is deleted and no `p_ids` edited: all five TOMLs load cleanly through
  `Scenario::from_toml_str_with` against a byte-unchanged `contracts/pulse-capabilities.toml`
  (per architecture.md §Cross-cutting Patterns — Scope law).
- (arch) Each retired scenario validates as a zero-`[[expected]]` shape at load — garde plus
  `check_capabilities`, `check_budgets` and `check_checklist` all pass (per architecture.md §Established
  Decisions [Validation Library]).
- (tests) `cargo nextest run -p conductor-report --test coverage_gate --profile ci` is green in all three
  of its arms — `every_accepted_capability_is_classified` (`coverage_gate.rs:35`),
  `removing_a_real_classification_row_fails_the_gate` (`:47`) and
  `the_committed_artifact_is_the_rendered_classification` (`:72`) (per test-plan §6 Scenario:
  Coverage-matrix completeness gate).
- (tests) The scenario-backing leg's `UNBACKED_AUTO` set-equality holds in both directions — its tests are
  inline in `crates/conductor-core/src/drift.rs` (`the_committed_catalog_matches_the_unbacked_ledger` and
  its four sibling arms), so the entry that proves this is `cargo nextest run -p conductor-core --profile
  ci`, NOT the `coverage_gate` entry (per test-plan §6 Scenario-backing leg).
- (layouts) No new bracket label, lamp, `ReportState`, Mode value, ANSI entry or table column appears on any
  surface — the lamp set stays closed at six (per layout-templates §cli Component — Primary content block 2).
- (obs) No new span name and no new span-attribute name is introduced (per obs-plan §11 Spans / Traces).
- (a11y) The pin inventory dispositions
  `crates/conductor-tauri/ui/test/a11y/screen-reader/nvda-pass-spec.md:73` as an `a11y:sr` fixture roster
  line and NOT a pinning guard — no guard is minted for it (per a11y-plan §3 Pass spec format).
- (security) `cargo audit` exits 0 and `cargo deny check advisories bans licenses sources` exits 0 over an
  un-drifted `Cargo.lock` (562 packages, unchanged — the basis is the package count, not byte-identity),
  and every committed DECLARE-ONLY header carries zero absolute host paths (per security-plan §Security
  Anti-Patterns → Universal and → Logging).

## Provenance
- Spec anchors: `architecture.md` §Standard Contracts (Run report envelope · Per-check record),
  §Established Decisions [Validation Library] / [Accepted Capability Set] / [Module Boundaries];
  `test-plan.md` §4 Framework, §6 (declare-only series · Coverage-matrix completeness gate), §7
  Self-bootstrapping, §10 Coverage thresholds, §11 Unit; `security-plan.md` §Input Validation,
  §Security Anti-Patterns → Input / Logging / Universal, §Dependency Security; `obs-plan.md` §3 Log format
  JSON schema (two record shapes), §6 Required fields, §11 Spans / Traces; `layout-templates.md` §cli
  Component — Primary content block 2; `a11y-plan.md` §3 Screen reader test pattern → Pass spec format.
- Research files inspected: `crates/conductor-core/src/scenario.rs`, `crates/conductor-core/src/drift.rs`,
  the five scenario TOMLs, `scenarios/cross-incident-recurrence.toml`, and at `andromeda-pulse@83d4060`
  `crates/interpretation/src/markdown.rs`, `crates/mcp-server/src/tools.rs`, `crates/triage/src/contract.rs`,
  `crates/triage/src/lifecycle/mod.rs`, `pulse-app/src/deterministic_inference.rs`.
- **Decisive material leans** (stated, not asked): retiring the `scrub_pipeline_degraded` mixed-shape suite
  guard rather than weakening it — leaned on the severity-lifecycle precedent, where the identical situation
  (a family's last non-empty member retiring) retired the guard (`testing.md` 2026-06-22, RETIRED
  2026-08-21). And correcting the prior chunk's "remains satisfiable" comment — leaned on that chunk's own
  committed enumeration plus P3's re-derivation at HEAD, which agree against the comment.

## Constraints & rejected approaches
- **Do not touch `root-span-error-scope` or `span-status-error-detection`** — both are satisfiable and their
  weaker defect is `v3-06`'s question. Retiring live coverage on a false equivalence would be the opposite
  of solving.
- **Do not correct a dead token into a live one** — it makes a check reachable, not proven, and can hard-Fail
  a live run on an unproven premise (the `cross-incident-recurrence` case-fix the prior chunk declined).
- **Do not mint a guard under CARRY 2** — measured: nothing is unpinned.
- **Do not borrow the serde-rename ground for `threshold-hot-reload`** — its ground is a tree-wide token
  absence; the rename explains its siblings only.
- **Do not pin a literal block/test count as a gate** — assert the SET (test-plan §6 de-hardcode line).
- **Do not edit any `.andromeda/` spec master** — registry movement rides wrap's amendment flow.

## Implementation notes
- **Order sensitivity:** do the five TOML retirements (steps 1-5) before the test re-shapes (6-13), so a
  test run between them names the retirement that broke each pin rather than failing on a missing fixture.
- **`all()` over an empty iterator is `true`** — this is why step 9 retires rather than keeps that guard,
  and why no re-shaped test should assert `all(...)` over `expected`.
- **Forecast to MEASURE, not to restate:** workspace tests were last measured at 983 (prior chunk's wrap
  record). This chunk removes six declarations and re-shapes eight tests, so the count will move; record
  what the re-run measures rather than predicting a number. No step freezes that producer.
- **Expected amendments (wrap):** `architecture.md` §Standard Contracts — the declare-only family registry
  grows past NINE; state the SET, never a count (the 2026-08-19 re-base precedent).
- **Expected amendments (wrap):** `obs-plan.md` §4 — the declare-only roster gains this chunk's families.
- **Expected amendments (wrap):** `test-plan.md` §6 — the declare-only series gains these five scenarios;
  note that the sidecar carries no entry for the preceding chunk, so §6's silence is not evidence about what
  that chunk shipped.
- **Expected amendments (wrap):** `matrix#v3-04 notes` — after the status flip, carry the COMPLETION
  NARRATIVE as the operator directed at this chunk's P5: the "ONE pass" ruling verbatim from the claimed
  acceptance's `OPERATOR RULING` paragraph (the coupling reading, and the corrected-scope explanation of why
  the class took two chunks), plus the end state — every committed `[[expected]]` block satisfiable or
  retired, the two survivors satisfiable by measurement. One canonical text: copy the acceptance's
  paragraph rather than re-wording it, so the ledger and the acceptance cannot drift. `requirements.md` is
  IMMUTABLE and untouched; the ruling is a READING recorded in the ledger, never a narrowing of the
  requirement.
