# Report — 2026-09-15-remaining-structurally-dead-declarations-retired

**Chunk:** Remaining structurally-dead declarations retired — the six `[[expected]]` blocks across five scenarios the prior chunk's own enumeration measured dead, each retired to declare-only with its measured ground stated and its pinning tests moved in the same change.
**Date:** 2026-09-15
**Commits:** `21edf56 feat(2026-09-15-structurally-dead-assertion-class-retired)` (the prior chunk, committed after `last_wrap`; this chunk is uncommitted at report time)

## Changes (structured — detectors read this)

- **Files:** `scenarios/activity-floor.toml` · `scenarios/service-went-silent.toml` · `scenarios/high-severity-log-capture.toml` · `scenarios/exception-event-capture.toml` · `scenarios/threshold-hot-reload.toml` · `crates/conductor-core/src/scenario.rs`. Six files, none new; +161/−131 (`git diff --stat scenarios/ crates/`).
- **Symbols / APIs:** no production symbol added, changed or removed — every edit is inside `scenario.rs`'s inline `#[cfg(test)] mod tests`. Test fns: 5 added / 3 removed by name (`git diff … | grep -cE '^[+-].*fn [a-z_]+\('` → 5 / 3), net rstest CASES −4. Added: `hard_signal_declare_only_fixtures_load_and_validate`, plus four renames (`p007_high_severity_log_capture_is_declare_only_with_the_measurement_recorded` ← `…_asserts_both_sides_of_the_boundary`; `findings_counter_and_threshold_reload_are_both_declare_only` ← `findings_counter_is_declare_only_and_threshold_reload_keeps_its_hard_absent`; `scrub_pipeline_degraded_suite_is_uniformly_declare_only` ← `…_mixes_hard_and_declare_only`). Removed outright: `activity_floor_and_silence_checks_are_all_hard`, `activity_floor_asserts_the_learned_quiet_via_absent`, `presence_scenarios_assert_a_surfaced_incident_via_contains`. No public API touched; no crate-root re-export needed (nothing new is visible outside the crate).
- **Crates / modules:** none added, removed or changed.
- **Dependencies:** none added, none bumped. `Cargo.lock` byte-unchanged (`git diff --stat Cargo.lock` → no entry), 562 packages (`grep -c '^\[\[package\]\]' Cargo.lock`).
- **Schema / config:** six `[[expected]]` tables removed from five committed scenario TOMLs; no key added, no shape changed. Every scenario keeps its `name`, `p_ids`, `seed`, `slo_tier`, `jitter_ms` and all `[[phases]]` — verified by `tomllib` load of all five (expected=0 for each; p_ids and slo_tier unchanged).
- **Spec-master edits:** none — this chunk edits no `.andromeda/` master (wrap's amendment flow owns them).
- **Counts / qualifiers moved:**
  - Live `[[expected]]` blocks **8 → 2** (`cat scenarios/*.toml | grep -c '^\[\[expected\]\]'` → 2).
  - Scenarios declaring ≥1 block **7 → 2** (`grep -l '^\[\[expected\]\]' scenarios/*.toml | wc -l` → 2). Committed scenarios unchanged at 36.
  - Workspace tests **983 → 979** (`cargo nextest run --workspace --profile ci` summary line; 983 is the prior chunk's wrap record). Net −4 reconciles: `hard_signal` split −2, new declare-only sibling +2, three removed pins −4.
  - **The declare-only registry's SET moves; its COUNT does not.** `architecture.md:135` states "NINE families now ship declare-only" followed by an enumerated SET (`grep -noE '(EIGHT|NINE|TEN|ELEVEN) (declare-only|families)' .andromeda/architecture.md` → 1 hit, `:135`; the literal is single-sited, confirmed independently by the test-plan detector). **Corrected at P2 validate:** an offset read of that 1 701-char line enumerates **21 scenarios in 9 groups**, and this chunk's five join the EXISTING structurally-dead-assertion class — so the nine-group count stays correct while that class's membership grows **3 → 8** and the enumerated scenario total **21 → 26**. An earlier draft of this bullet claimed both the count and the set were stale; that was wrong on the count half and the arch detector inherited it. Basis: `python` offset read of `architecture.md:135`, groups counted from the clause's own separators.
- **Dev-tool versions:** none — no host tool installed, upgraded, or read changed this chunk.
- **Harness / gate surface:** none — no `agent-run` script, xtask verb, CI step, or status/verdict shape touched. One NEW plan gate entry exists (a `role = 'probe'` corpus census over `scenarios/*.toml`), but it lives in the chunk plan, not in the project's harness surface.
- **Cross-project / external claims:** all six retirement grounds are facts about the SUT, read at `andromeda-pulse` HEAD `83d4060` (unchanged since the prior chunk's enumeration measured them; `git -C ../andromeda-pulse rev-parse --short HEAD`). Basis per ground: `crates/triage/src/contract.rs:181` (`#[serde(rename_all = "snake_case")]` on `CueKind`) · `crates/interpretation/src/markdown.rs` `severity_label` (lowercase mapping) · `crates/interpretation/src/markdown.rs:249,115` (`assemble_report`/`serialize_report` — the sole report-markdown composer, reached from `crates/mcp-server/src/tools.rs:360-387`) with `git grep -c --fixed-strings 'exception' -- 'crates/interpretation/src/markdown.rs'` → exit 1 · `git grep -c --fixed-strings 'RetroactiveReeval' -- '*.rs'` → exit 1 tree-wide, read from the BARE command · `crates/triage/src/lifecycle/mod.rs:57,103` (forward-only threshold application) · `pulse-app/src/deterministic_inference.rs:42-43` (the pinned severity/title constants).
- **Reverted / negative API facts:** none.
- **Insufficient fixes (written, kept, not the remedy):** none.
- **Spec claims disproved by measurement:** **none in any spec master.** The claim this chunk disproves lives in CODE, not a spec: `crates/conductor-core/src/scenario.rs:1299-1302` (authored by the prior chunk, 2026-09-15) stated P-056's `Hard Absent` "is untouched … and remains satisfiable", while that same chunk's committed enumeration classified it structurally dead in the vacuous-PASS direction. Re-derived at SUT HEAD `83d4060`: `git grep -c --fixed-strings 'RetroactiveReeval' -- '*.rs'` exits 1 — zero occurrences tree-wide, no `snake_case` sibling — so the check was true in every world. Corrected in place by this chunk (the comment is rewritten and the test renamed). Master search confirming no spec carries the claim: `grep -rn 'RetroactiveReeval' .andromeda/*.md` → 0 hits; `grep -rnc 'P-056' .andromeda/*.md` → hits only in `input.md` (2) and `master-route.md` (1), neither asserting satisfiability.
- **Expected amendments (from plan):**
  - `architecture.md` §Standard Contracts (declare-only registry) — **carried**; its fact is the fourth Counts/qualifiers bullet above. Search: `grep -noE '(EIGHT|NINE|TEN|ELEVEN) (declare-only|families)' .andromeda/architecture.md` → **1 hit, `:135`**; `grep -c 'declare-only' .andromeda/architecture.md` → 6. Direction, as applied: extend the structurally-dead-assertion class's enumeration with the five and add a durable anchor naming the two scenarios that still declare live checks — the count is NOT touched, because no new family was created (the plan predicted it would "grow past NINE"; measurement at P2 says otherwise). This honours the 2026-08-19 re-base precedent's intent (never re-pin a literal that re-stales) by anchoring on the complement, which shrinks toward a fixed point rather than growing.
  - `obs-plan.md` §4 (declare-only roster) — **not carried**; this chunk changes no value obs-plan states. Search: each of the five scenario names over `.andromeda/obs-plan.md` → **0 hits each**; the only roster (`:349`, "The four declare-only delegated-timing scenarios") names the delegated-timing family, none of which is this chunk's. obs-plan's generic clause (`:229`, "a blocked or declare-only scenario emits no check line at all") stays TRUE and is reinforced, not falsified. The fact is stated here anyway per the contract.
  - `test-plan.md` §6 (declare-only series) — **fact stated, disposition is the detector's**; this chunk measured no false claim there. Search: each of the five names over `.andromeda/test-plan.md` → **0 hits each**, so §6's series is INCOMPLETE with respect to this chunk, not WRONG. Its sidecar carries no entry for the preceding chunk either (newest 2026-09-14), so §6's silence is not evidence about what that chunk shipped.
  - `matrix#v3-04 notes` — **ledger-note — owner P7.3.** Carries the operator's "ONE pass" ruling verbatim from the claimed acceptance's `OPERATOR RULING` paragraph plus the end state, copied rather than re-worded so ledger and acceptance cannot drift.
- **Coverage of new surfaces:** none — this chunk introduces no external surface, hot-path op, or UI element. Every edit removes a declaration or re-shapes an existing inline test. (validation n/a · instrumentation n/a · PII n/a · tests — the six retirements are pinned by six named tests, listed under Outcome · a11y n/a · tokens n/a)

## Deviations from intent

- **Plan step 13's comment correction was executed here, not where the plan assigned it.** Step 13 said to leave `scrub_pipeline_degraded_fixtures_load_and_validate` unchanged and that its "Mixed-shape family" comment is "corrected there [step 12], not here" — but step 12 edits a DIFFERENT function, so following the letter would have shipped a comment asserting a mixed-shape family this chunk had just made uniform. The comment was corrected minimally in place. **Justification:** shipping a knowingly-false claim is the exact defect class this chunk exists to retire. The step's substantive instruction (add no blanket expected-shape assertion to that loader) was followed unchanged.
- **Step 12's replacement assertion is stronger than specified.** The plan said "replaced by an `is_empty()` assertion across all six members"; the shipped form collects the NAMES of any member still declaring checks and asserts that list is empty, so a failure reads `…these still declare checks: ["x"]` rather than a bare boolean. **Justification:** the project's standing rule that an assertion reporting only a count names no defect. Same property proven, self-describing failure.
- **Step 7's re-shaped test pins `slo_tier` alongside `is_empty()`.** The plan named the `pii_scrub_is_declare_only_with_the_measurement_recorded` shape, which asserts only emptiness. **Justification:** the sibling declare-only precedents (`restart_suppression_…`, `statistical_anomaly_…_at_the_harvest_tier`) pin the tier too, and the tier is a property retirement must preserve.
- **Considered and declined:** adding a drive-shape pin (asserting the two severity emission phases) to the P-007 test — beyond the plan's named shape, so left out rather than taken as an in-flight widening.

## Decisions & corrections

- **Operator ruling on "ONE pass" (P5 review, recorded in the claimed acceptance).** `requirements.md:16` is IMMUTABLE and says "…or are retired to declare-only in ONE pass with their pinning tests updated in the same change" — verified byte-exact at that line. The ruling: "ONE pass" binds the COUPLING stated in its own second half (a retirement moves together with the pinning tests that assert it, never staged across chunks), which both chunks satisfy; what took two chunks was a corrected SCOPE, not a decomposed retirement. Nothing about the requirement is re-worded or narrowed; `requirements.md` is untouched.
- **A dictated figure in that ruling was re-derived and corrected before it landed.** The ruling said the first chunk "moved five pins"; `git show 21edf56 -- crates/conductor-core/src/scenario.rs` shows five `-`/`+` test-fn pairs plus one unpaired `+`, i.e. **six** tests moved (five existing pins re-shaped, one minted for `pulse-run-contract`), and that chunk's own commit message says six. The precise form went into the committed acceptance and the sharpening was reported rather than applied silently; the ruling's substance is unaffected.
- **Sweep hazard found this chunk (curation raw material):** `grep -rln "activity-floor" crates/` returns **9 files**, of which only `scenario.rs` is a check-membership pin. The other eight are doc comments naming the P-013/P-014 fault FAMILY (`conductor-faults/src/{train,silence,lib}.rs`, `conductor-core/src/phase_spec.rs`), load-envelope duration fixtures (`conductor-core/src/load_envelope.rs`), and envelope-exemption note STRINGS inside test fixtures (`conductor-cli/src/render.rs`, `conductor-report/src/{report,db}.rs`). A scenario name that is also a domain-concept noun over-returns; the hits must be read, never counted. The other four names return exactly 1 file each.
- **Confirmation that the narrow companion sweep is unsound here:** `grep -rln "<name>" crates/**/tests` returns nothing for all five names, because every pin lives in an inline `#[cfg(test)]` module under `src/`. The wide `crates/` form is the only correct one — CARRY 2 said so and measurement agrees.

## Outcome

**Acceptance criteria — re-asserted against the DIFF:**

- ✅ Six declarations retired in ONE change, each scenario keeping `p_ids`/`slo_tier`/phase drive and carrying a header with the marker and its own measured ground — verified by `tomllib` load of all five and by reading the five headers.
- ✅ `grep -l '^[[expected]]' scenarios/*.toml | xargs -n1 basename | sort | paste -sd,` prints exactly `root-span-error-scope.toml,span-status-error-detection.toml`.
- ✅ Workspace and runner-portability gates both green.
- ✅ Eight pinning tests updated in the same change; no surviving assertion vacuous — `activity_floor_and_silence_checks_are_all_hard` retired rather than left green over an empty vec (`all()` is true on an empty iterator), and the `scrub_pipeline_degraded` mixed-shape guard retired because its last non-empty member retired.
- ✅ No scenario deleted, no `p_ids` edited; `contracts/pulse-capabilities.toml` byte-unchanged (absent from `git status --short`).
- ✅ Each retired scenario validates as a zero-`[[expected]]` shape at load — the five loader tests pass through `Scenario::from_toml_str`, exercising garde plus `check_capabilities` / `check_budgets` / `check_checklist`.
- ✅ `coverage_gate` green in all three arms; the scenario-backing `UNBACKED_AUTO` set-equality holds (its tests are inline in `drift.rs`, proven by the `conductor-core` entry — the criterion was split at P5 to attribute each to the entry that actually proves it).
- ✅ No new bracket label, lamp, `ReportState`, Mode value, ANSI entry or table column — the diff introduces no surface at all.
- ✅ No new span name or span-attribute name.
- ✅ `nvda-pass-spec.md:73` dispositioned as an `a11y:sr` fixture roster line, not a pin; no guard minted for it.
- ✅ `cargo audit` and `cargo deny` exit 0 over an un-drifted `Cargo.lock` (562 packages); the five new headers carry zero absolute host paths (`grep -nE '[A-Za-z]:[\\/]'` over all five → no match).

**Gates** (the plan's `[[gate]]` entries, in order):

| `run` | verdict |
|---|---|
| `cargo nextest run -p conductor-core --profile ci` | exit 0 · 326/326 |
| `cargo test -p conductor-core` | exit 0 · green on all four targets (312 + 6 + 8 + 0) |
| `cargo nextest run -p conductor-report --test coverage_gate --profile ci` | exit 0 · 3/3 |
| `cargo nextest run --workspace --profile ci` | exit 0 · **979/979**, 0 FAIL |
| `grep -l '^[[expected]]' scenarios/*.toml \| xargs -n1 basename \| sort \| paste -sd,` | exit 0 · `last line` atom held — **red→green against its P5 baseline** |
| `cargo clippy --workspace --all-targets -- -D warnings` | exit 0 · 0 warnings |
| `cargo audit` | exit 0 · 7 allowed warnings |
| `cargo deny check advisories bans licenses sources` | exit 0 · advisories/bans/licenses/sources ok |

No `defer` entry, no `leg` entry, no red. Smoke: **skipped — no boot-path / UI-surface change** (no touchpoint hits a binary, daemon, entry-point or UI surface; the plan lists no `smoke`/`self-verify` entry).

**Outcome basis:** `/andromeda-implement`'s P4 report as given, in this same session's conversation — the gate readings, the file list, the deviations and the process census are all first-hand from that run. No operator directive intervened between implement and this report; no `/clear` or context wall, so the chunk's evolve records were not read as a substitute.

**Process hygiene:** implement's census recorded `cargo`/`rustc` gate builds started by that run and terminated, everything else `none started`. Re-measured at this wrap: `Get-Process -Name cargo,rustc,nextest,conductor,andromeda-pulse-mcp,msedgedriver,tauri-driver` returns an empty table — no survivors.
