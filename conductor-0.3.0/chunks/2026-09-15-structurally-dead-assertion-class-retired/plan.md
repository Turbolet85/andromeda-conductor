# Plan — 2026-09-15-structurally-dead-assertion-class-retired: "Structurally-dead assertion class retired"

_Generated by /andromeda-phase P4. Read by /andromeda-implement. Manual edits between runs are fine; do not
edit during an /implement run._

## Metadata
- **Marker:** 2026-09-15-structurally-dead-assertion-class-retired · **Version:** conductor-0.3.0 · **Generated:** 2026-09-15T10:30:00Z

## Goal

Retire the structurally-dead assertion class from the committed scenario corpus — four `[[expected]]`
declarations across four scenarios that cannot pass in any world — to the established **declare-only** shape,
as a class in one pass, with every pinning test updated in the same change. The scenario keeps its drive
(`p_ids`, `slo_tier`, phases) and carries zero `[[expected]]` blocks plus a header comment recording the
retirement, its marker and the measured ground (per test-plan §6; the shipped idiom at
`scenarios/severity-tier-autonomous.toml:11`). Declare-only is an already-governed config shape, so no garde
rule, `Scenario::check_*` arm or `from_toml_str` change is owed (per architecture.md §Established Decisions
[Validation Library] + security-plan §Input Validation).

Three members (`constellation-severity-live-wiring` P-079, `findings-counter-refresh` P-045,
`pulse-run-contract` P-073) declare a `CountAtLeast` floor graded against `evidence_count`, which Pulse
derives from a vector it writes empty at its only production incident-construction site — re-derived at
Pulse HEAD `83d4060` this phase (research.md §Scope premise closure). The fourth
(`cross-incident-recurrence` P-036) declares a `Hard Contains` on a token whose emitted form differs from the
declared one by one character against a case-sensitive comparison. **Its recorded ground is corrected by this
chunk, not inherited**: the token IS emitted and DOES reach the graded text, so "no producer emits it" — the
residual's attribution — is false. The operator ratified retiring all four rather than correcting the case,
because this chunk drives nothing and a corrected token would ship reachable-but-unproven.

Because `v3-04` asserts a UNIVERSAL ("every committed scenario's declared checks are satisfiable … or are
retired"), the chunk also commits the enumeration that makes the universal true: all 12 live `[[expected]]`
blocks classified satisfiable-or-dead, with derivations recorded. That enumeration is the evidence
`v3-06`'s audit gate later mechanizes; this chunk does not ship a gate for it beyond its own count probe.

## Implementation Steps

1. **Retire the three `CountAtLeast` declarations.** In `scenarios/constellation-severity-live-wiring.toml`
   (block at `:52-55`), `scenarios/findings-counter-refresh.toml` (`:48-51`) and
   `scenarios/pulse-run-contract.toml` (`:57-60`): delete the `[[expected]]` block and replace the P-ID
   comment immediately above it with a `DECLARE-ONLY (2026-09-15-structurally-dead-assertion-class-retired)`
   header recording the ground —
   `evidence_count` is summed from `retrieve_telemetry_slice.span_refs`, which Pulse's sole non-test writer
   (`pulse-app/src/inference_runtime.rs:871`, HEAD `83d4060`) sets empty, so the floor is false in every world
   and routes to `CalibrationRegion` regardless of the declared `Hard`. Leave `name`, `p_ids`, `seed`,
   `slo_tier`, `jitter_ms` and every `[[phases]]` table byte-identical. **`slo_tier` is not this chunk's to
   touch** (see Constraints).

2. **Retire `cross-incident-recurrence` with the corrected ground.** Delete the `[[expected]]` block at
   `scenarios/cross-incident-recurrence.toml:42-45`. The header comment records what was actually measured:
   Pulse emits `"## Previously Seen"` (`crates/interpretation/src/markdown.rs:221`, HEAD `83d4060`), that
   markdown reaches Conductor's graded text (`conductor-verify/src/extract.rs:101-103`), and `Contains` is
   case-sensitive (`conductor-verify/src/slo.rs:51`) — so the declared `"Previously seen"` never matched on
   CASE, not for want of a producer. Record that correcting the case is deliberately NOT done here (this
   chunk drives nothing, so a corrected token would be reachable but unproven) and that the scenario's own
   `:15` header already flagged the token as inferred. Keep the existing `:11-16` P-036 capability prose.

3. **Re-point the two direct pinning tests** in `crates/conductor-core/src/scenario.rs`. Both assert a
   declaration that will no longer exist, so each is inverted to assert the retired shape, following the
   shipped declare-only idiom (`severity_lifecycle_family_is_declare_only` `:844`,
   `restart_suppression_is_declare_only_at_the_harvest_tier` `:699`,
   `pii_scrub_is_declare_only_with_the_measurement_recorded` `:1284`):
   - `cross_incident_recurrence_asserts_previously_seen_via_hard_contains` (`:1188`) → asserts the scenario
     is declare-only, its comment carrying the case-mismatch measurement from step 2.
   - `constellation_severity_live_wiring_asserts_incident_visibility_via_hard_count_floor` (`:1408`) →
     asserts the scenario is declare-only, its comment carrying the empty-`span_refs` ground.

4. **Narrow `findings_counter_and_threshold_reload_carry_their_hard_checks`** (`:1303`). Drop only its first
   assertion (P-045's `CountAtLeast`/`Hard`); the P-056 `Absent`/`Hard` assertion over
   `threshold-hot-reload` stays live and correct. Rename the test to its narrowed subject and add a
   declare-only assertion for `findings-counter-refresh`, so the pair the test names is still both covered.

5. **Re-scope the two suite-shape tests** (`:1207`, `:1429`). Each asserts that a scenario FAMILY carries both
   a non-empty-`expected` member and an empty one; after step 1/2 each family is uniformly empty, so the
   property is dissolved rather than merely broken. Re-scope each to assert the family's NEW true shape —
   every member declare-only — computed set-valued over the family's stems rather than against a literal
   (per test-plan §1 Critical Path 6's de-hardcode style). Neither test is deleted: test-plan §11 Quality
   forbids absorbing a removed assertion by dropping a guard.
   - `constellation_context_grounding_suite_has_operator_checklist_members` (`:1207`) — its stem list is the
     four constellation members plus `cross-incident-recurrence`.
   - `in_lane_sut_suite_mixes_operator_checklist_and_auto` (`:1429`) — its stems are `live-only-service-truth`,
     `investigate-actions-functional`, `constellation-severity-live-wiring`.

6. **Commit the class enumeration** to
   `conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/evidence/assertion-class-enumeration.md`:
   all **12** pre-change `[[expected]]` blocks across **11** scenarios (of **36** committed), each classified
   satisfiable or dead with the evidence, plus the derivation command beside every number. The four retired
   members carry their grounds from steps 1–2; the eight survivors (`activity-floor`,
   `exception-event-capture`, `high-severity-log-capture` ×2, `root-span-error-scope`, `service-went-silent`,
   `span-status-error-detection`, `threshold-hot-reload`) each carry the producer that makes them
   satisfiable. Repo-relative paths only — no absolute host path, no internal seam-crate struct name (per
   security-plan §Security Anti-Patterns → Logging).

7. **Run the gates** in the `[[gate]]` order below and record each reading.

## Codebase touchpoints

### New files
- `conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/evidence/assertion-class-enumeration.md` — the committed "as a class" record (step 6).

### Files to modify
- `scenarios/constellation-severity-live-wiring.toml` — remove `[[expected]]` (`:52-55`) + retirement header. Drive untouched.
- `scenarios/findings-counter-refresh.toml` — remove `[[expected]]` (`:48-51`) + retirement header. Drive untouched.
- `scenarios/pulse-run-contract.toml` — remove `[[expected]]` (`:57-60`) + retirement header. Drive untouched.
- `scenarios/cross-incident-recurrence.toml` — remove `[[expected]]` (`:42-45`) + retirement header carrying the CORRECTED ground.
- `crates/conductor-core/src/scenario.rs` — the five pinning tests at `:1188`, `:1207`, `:1303`, `:1408`, `:1429` (steps 3–5). The file's garde annotations and `Scenario::check_*` arms are NOT touched.

## Test Commands

No `smoke` entry: this chunk has **no boot-path touchpoint and no live leg** — the modify-set is declarative
scenario config plus an inline test module, it drives no external process and no SUT, and `test-plan.md`
declares no boot-smoke trigger-path list (grepped this phase: no `boot-smoke`/`trigger path` declaration), so
nothing is owed and no `role = 'probe'` liveness entry applies. No `self-verify` entry: no frontend path is in
the modify-set. No `leg = 'live'` entry, which is why research.md's `Harness rules consulted` slot records
"none — no live leg in this chunk".

```toml
[[gate]]
run = 'cargo fmt --all -- --check'
role = 'lint'

[[gate]]
run = 'cargo clippy --workspace --all-targets -- -D warnings'
role = 'lint'

[[gate]]
run = 'cargo nextest run --workspace --profile ci'
role = 'unit'
note = 'the coverage/backing gate (conductor-report/tests/coverage_gate.rs) rides this entry — it asserts exact-set equality over UNBACKED_AUTO keyed on which scenarios NAME which P-IDs, so it is the mechanical proof that retiring checks moved no classification row.'

[[gate]]
run = 'cargo test -p conductor-core'
role = 'unit'
note = 'runner-portability standing gate (test-plan §4 + §10 zero-flakiness): green under BOTH runners. This chunk edits only the crate whose inline test module holds all five pinning tests.'

[[gate]]
run = 'cargo test --workspace --doc'
role = 'unit'
note = 'the doctests nextest skips (test-plan §3 5-command run body).'

[[gate]]
run = '''cat scenarios/*.toml | grep -c '^\[\[expected\]\]''''
role = 'probe'
new = true
expect = ['exit 0', 'last line 8']
baseline = 'red — printed 12, exit 0. Red-before-green on a property THIS chunk creates: the four retirements are what take 12 to 8, so the untouched tree must fail this atom.'
note = 'the class FORECAST, stated as a prediction to measure: 12 live [[expected]] blocks today, predicted 12 -> 8 after the four retirements (step 1 + step 2 produce it). Record what the re-run measures; a different number means a member was missed or an extra block moved. Derivation for the pre-change 12: this same command on the untouched tree.'

[[gate]]
run = '''grep -rnE '[A-Za-z]:[\/]|/home/|/Users/|%APPDATA%' conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/evidence | wc -l'''
role = 'probe'
new = true
expect = ['exit 1', 'last line 0']
baseline = 'red — exit 2 ("No such file or directory"), last line 0. Red because the evidence directory is created by step 6; red-before-green on a subject this chunk builds. KNOWN-POSITIVE CONTROL run at P5 against minted inputs in the phase run dir: the pattern matched all three leak classes in a leaky file (D:/, /home/, %APPDATA%; exit 0) and cleared a repo-relative coordinate line (exit 1, last line 0) — so it is provably capable of failing, which is the property a silent-pass grep lacks.'
note = 'committed-evidence host-path hygiene (security-plan §Security Anti-Patterns → Logging), scoped to the evidence DIRECTORY alone and never to the chunk folder — this plan and its report contain the pattern text itself, and a sweep including them matches the gate string rather than the content (host-win32.md, 2026-09-14). The grep-exits-1 idiom is the project shape: exit 1 under pipefail IS the no-match verdict.'

[[gate]]
run = 'git -C "$CARGO_HOME/advisory-db" status --porcelain'
role = 'probe'
expect = ['exit 0', 'no output']
note = 'the corrected standing rule (security-plan Session Additions, 2026-09-05): a red audit is trusted only after the LOCAL advisory-db checkout is shown clean — untracked residue is what a fetch leaves behind and it produced 51 false re-pins. Runs BEFORE cargo audit.'

[[gate]]
run = 'cargo audit'
role = 'probe'

[[gate]]
run = 'cargo deny check advisories bans licenses sources'
role = 'probe'
note = 'a green cargo audit does not imply a green cargo deny (security-plan Session Additions, 2026-06-23). Zero dependency delta is expected this chunk: no manifest is in the modify-set, so Cargo.lock must be byte-unchanged and the package count must hold at 562.'
```

## Acceptance Criteria

- (tests) `cargo nextest run --workspace --profile ci` returns 0 AND `cargo test -p conductor-core` returns 0 — both runners, no runner-dependent result, with all five affected pinning tests updated in the SAME change as the TOML edits (per test-plan §4 Framework · §10 Zero-flakiness budget) — `verification-matrix.json#v3-04`.
- (tests) Every edited scenario TOML is exercised in a Rust test through the production validator, so a parseable-but-inert edit fails at nextest rather than silently weakening a downstream assertion (per test-plan §7 Self-bootstrapping requirement).
- (tests) No retired declaration is re-expressed as a vacuous `Absent`/`Contains`, and neither suite-shape test is deleted to absorb the change (per test-plan §6 Scenario: Fingerprint-storm · §11 Quality).
- (unit) The four target scenarios each carry zero `[[expected]]` blocks and the corpus-wide count measures **8** blocks across **7** scenarios — recorded as measured, whatever it reads (per test-plan §6) — `verification-matrix.json#v3-04`.
- (arch) Each retired scenario keeps its `p_ids`, `slo_tier` and phase drive; no garde rule, `Scenario::check_*` arm, `from_toml_str` change, coverage-classification row or `UNBACKED_AUTO` pin edit ships (per architecture.md §Established Decisions [Validation Library] + [Accepted Capability Set]).
- (arch) The retirement mints no report state, verdict value or envelope key — `verdict ∈ {Pass, Fail, CalibrationRegion}` and `state ∈ {Pass, Fail, ManualCheck, KnownResidual, Blocked}` stay closed (per architecture.md §Standard Contracts).
- (obs) The change mints no new span name, span attribute or envelope field; the bounded span-name set and `ALLOWLISTED_FIELDS` are unchanged (per obs-plan §11 Spans / Traces).
- (layouts) Retirement mints no new bracket label, lamp glyph, `ReportState` or ANSI entry on any of the three surfaces — the per-P-ID set stays closed at six (per layout-templates §Surface: cli — Component — Primary content block 2).
- (security) `cargo audit` exit 0 and `cargo deny check advisories bans licenses sources` exit 0 over an un-drifted, committed `Cargo.lock` (per security-plan §Security Anti-Patterns → Universal).
- (security) The committed class-enumeration evidence contains zero absolute host paths and no internal seam-crate struct names, proven by the hygiene grep scoped to the evidence directory (per security-plan §Security Anti-Patterns → Logging).
- (security) Every retired scenario still declares its P-ID(s) — scope law holds by construction (per security-plan §Security Anti-Patterns → Universal).
- (a11y) No file under `crates/conductor-tauri/ui/test/a11y/` is in the modify-set, and the chunk's "gates green" claim asserts no WCAG conformance and does not cover the `a11y` CI job (per a11y-plan §11 CI · §11 Strategy).

## Provenance
- Spec anchors: architecture.md §Standard Contracts (Run report envelope · Per-check record) · §Established Decisions [Validation Library] · [Accepted Capability Set] · [Read-Back Dependency Posture] · security-plan §Input Validation · §Security Anti-Patterns (Logging · Universal) · §Dependency Security · test-plan §4 · §6 · §7 · §10 · §11 Quality · §1 Critical Path 6 · obs-plan §3 · §4 · §6 · §11 · layout-templates §Surface: cli — Component — Primary content block 2 · a11y-plan §6 · §11 CI.
- Research files inspected: `conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/research.md` (16 files, 5 graph queries) · `scope.md`.
- Capability: `verification-matrix.json#v3-04`.

## Constraints & rejected approaches
- **Correcting member 4's case instead of retiring it — REJECTED** (operator-ratified at P4). The token is one character from matching, but this chunk drives nothing, so a corrected declaration would be reachable-but-unproven and a non-firing recurrence path would hard-FAIL the next live run. The finding is preserved in the TOML header and routes forward.
- **Downgrading member 4 to `class = "CalibrationRegion"` — REJECTED** (same fork). It would split member 4 from the other three, and `v3-04`'s own note forbids decomposing the class per scenario.
- **Touching `slo_tier` anywhere — FORBIDDEN.** Every tier question, including `constellation-severity-live-wiring`'s `<20s`-vs-30 000 ms defect in the SAME file, belongs to `v3-05` (`Scenario tier honesty`, the next markerless route entry).
- **Editing `coverage.rs` / `drift.rs` — FORBIDDEN.** Measured: all seven P-IDs behind the five already-retired declare-only scenarios remain `Auto`, and `check_scenario_backing` keys on whether a scenario NAMES an id, never on `expected` emptiness (`drift.rs:140-175`). Declare-only and `Auto` coexist by shipped precedent.
- **Adding a garde rule or `Scenario::check_*` arm for the empty-`expected` shape — FORBIDDEN.** `expected_defaults_to_empty_when_omitted` (`scenario.rs:980`) proves `#[serde(default)]` already admits it; the route's own security validation rejected asserting this invariant for exactly that reason.
- **Shipping a mechanical audit gate — OUT OF SCOPE.** `v3-06` (`Scenario-assertion audit gate`) owns it; this chunk's count probe is a gate for THIS change, not the re-runnable audit.
- **Re-homing any retired claim to the harvest tier — NOT AVAILABLE.** Measured: no `conductor-run/tests/*_harvest.rs` names any of the four, and for all four the graded value has no producer, so there is nothing to grade at the harvest tier. Retirement, not re-homing, is the remedy.

## Implementation notes
- **Five pinning tests, not one.** The originating residual and the matrix note each named ONE (`scenario.rs:1188`); the corrected companion sweep (`grep -rln "<name>" crates/`, the WIDE form) measured five. The prescribed narrow form (`crates/**/tests`) returns nothing for all four scenario names — verified this phase — which is exactly the blind spot `v3-04`'s note predicts.
- **`pulse-run-contract` (P-073) has no pinning test.** No test in `crates/` asserts its declaration. The name's hits in `conductor-core/src/run_contract.rs` and `conductor-tauri/ui/test/a11y/operator-hold.e2e.ts` reference `contracts/pulse-run-contract.toml` — a different artifact; read and excluded.
- **Retirement changes run-time behaviour, by design.** A declare-only scenario takes the `ReadBack::AutoResolved` arm on an empty read-back where a checks-bearing one lands `Blocked`, raises an operator-checklist hold (`execute.rs:123`, `allow_no_go: true`), and lands `verdict: null` / `state: KnownResidual` with zero `CheckRecord` lines and zero `run_check` rows. None of the four declares `[[checklist]]`, so the hold presents empty — which is the shipped shape: all five precedent declare-only scenarios likewise carry zero `[[checklist]]` blocks (measured this phase). No run-seam change is owed.
- **`scenario.rs` holds both the tests and the validated structs.** Keep every `#[derive(Validate)]`/garde annotation byte-unchanged; the edit is confined to the inline `#[cfg(test)]` module (per security-plan §Input Validation).
- **A stale comment is NOT in scope:** `conductor-run/src/execute.rs:519` says "7 of the 9 empty-`expected` catalog entries" where the corpus now holds 25 declare-only scenarios (re-derived this phase: `36 total − 11 checks-bearing`). It is a comment inside a test, asserts nothing, and sits outside this chunk's modify-set; recorded here so the next reader does not treat the number as current.
- **Expected amendments (wrap):** `architecture.md` §Standard Contracts (Run report envelope) — the declare-only registry gains the four members, and the parenthetical "`findings-counter-refresh` is NOT a member: it carries one `[[expected]]`" is struck (verified verbatim at `architecture.md:135`, offset 652).
- **Expected amendments (wrap):** `obs-plan.md` §4 (Known-residual classification path → Delegated-timing family) — the sentence "`findings-counter-refresh` is not declare-only (one `[[expected]]`) … its unmet `CountAtLeast` floor grading `CalibrationRegion`" is restated to current truth (verified verbatim at `obs-plan.md:349`, offset 1351).
- **No `test-plan.md` amendment is owed:** none of the four scenario names appears anywhere in `test-plan.md` (swept this phase), so its §1/§6 declare-only sites need no re-base. A fifth member surfaced by the enumeration that IS one of the six named families would owe the two-site restatement.
- **No mutation audit is owed:** `scripts/mutation-roster.toml`'s `[[unit]]` members are `conductor-cli`, `conductor-verify`, `conductor-emit`; `conductor-core` is not registered (per test-plan §9).
