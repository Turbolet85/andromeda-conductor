# Report — 2026-09-15-structurally-dead-assertion-class-retired

**Chunk:** Structurally-dead assertion class retired — four declared checks that cannot pass in any world retired to declare-only as a class in one pass, every pinning test updated in the same change
**Date:** 2026-09-15
**Commits:** none since `last_wrap` — the prior commit is `267157f feat(2026-09-14-emit-scrubber-and-percentile-math-under-test)`; this chunk's work is uncommitted at report time and rides this wrap's commit

## Changes (structured — detectors read this)

- **Files:** `scenarios/constellation-severity-live-wiring.toml` · `scenarios/findings-counter-refresh.toml` · `scenarios/pulse-run-contract.toml` · `scenarios/cross-incident-recurrence.toml` · `crates/conductor-core/src/scenario.rs` · NEW `conductor-0.3.0/chunks/2026-09-15-structurally-dead-assertion-class-retired/evidence/assertion-class-enumeration.md` · the chunk folder (`scope.md` · `research.md` · `plan.md` · this report) · `conductor-0.3.0/verification-matrix.json` · `conductor-0.3.0/working-route.md` · `.andromeda/master-route.md`. Source delta `git diff --stat crates/ scenarios/` → **5 files, +136/−110**.
- **Symbols / APIs:** no public API change — **zero** new/changed/removed `pub` items (`git diff -U0 crates/ | grep -cE '^[+-]\s*pub '` → 0). Six `#[test]` fns inside `conductor-core/src/scenario.rs`'s inline `#[cfg(test)]` module, five renamed-and-inverted plus one new: `cross_incident_recurrence_is_declare_only_after_the_token_case_finding` (`:1188`, was `…_asserts_previously_seen_via_hard_contains`) · `constellation_context_grounding_suite_is_uniformly_declare_only` (`:1208`, was `…_has_operator_checklist_members`) · `findings_counter_is_declare_only_and_threshold_reload_keeps_its_hard_absent` (`:1298`, was `findings_counter_and_threshold_reload_carry_their_hard_checks`) · `constellation_severity_live_wiring_is_declare_only_after_the_empty_span_refs_finding` (`:1404`, was `…_asserts_incident_visibility_via_hard_count_floor`) · **NEW** `pulse_run_contract_is_declare_only_after_the_empty_span_refs_finding` (`:1423`) · `in_lane_sut_suite_is_uniformly_declare_only` (`:1440`, was `…_mixes_operator_checklist_and_auto`). No ports, sockets or env vars. **Remaining-caller fact:** `Scenario.expected` keeps all 55 references across 8 files (code-graph `refs` query, rust plane, `db_state: fresh`) — this chunk changes DATA the field carries, never the field or any reader of it; `ComparisonKind::CountAtLeast` keeps all 16 references across 6 files and no variant is removed.
- **Crates / modules:** none added, removed or changed. All edits sit inside `conductor-core` (test module) and `scenarios/` (declarative config).
- **Dependencies:** none added, none bumped. `Cargo.lock` **byte-unchanged** (`git diff --stat Cargo.lock` → empty), package count holds at **562** (`grep -c '^name = ' Cargo.lock`).
- **Schema / config:** four committed scenario fixtures moved from one `[[expected]]` block each to **zero**, plus a `DECLARE-ONLY ({marker})` header comment recording each retirement's measured ground. No config KEY added or removed — `[[expected]]` remains an `#[serde(default)]` optional table, and the shape is the already-governed declare-only one (25 scenarios shipped it before this chunk). No garde rule, no `Scenario::check_*` arm, no `from_toml_str` change: `git diff crates/ | grep -cE '^[+-].*garde|^[+-].*fn check_'` → 0.
- **Spec-master edits:** none by this chunk's own steps. **Two owed** — see *Expected amendments* below; both are wrap's to apply through the amendment flow.
- **Counts / qualifiers moved:**
  - live `[[expected]]` blocks in the committed corpus **12 → 8** (`cat scenarios/*.toml | grep -c '^\[\[expected\]\]'`, before and after)
  - scenarios declaring ≥1 check **11 → 7** (`grep -l '^\[\[expected\]\]' scenarios/*.toml | wc -l`)
  - declare-only scenarios **25 → 29** (36 total − 7 declaring; `ls scenarios/*.toml | wc -l` → 36)
  - workspace test count **982 → 983** (the one added pin; `cargo nextest run --workspace --profile ci` summary line)
  - **Doc-stated sites for these values:** exactly one — `architecture.md:135`, which states "EIGHT families now ship declare-only" and enumerates them. Swept for the others: `grep -rnE '\b12\b.*expected|expected.*\b12\b'` over the five value-bearing masters → 3 hits (`test-plan.md:307` tauri-driver/WebView2 · `layout-templates.md:153` dialog primitive · `design-system.md:265` operator-checklist render), **all read, all unrelated, 0 changed**; `grep -rnE '\b25\b.*declare|declare.*\b25\b'` → 2 hits in `test-plan.md` (`:611`, `:616`), both the mutation roster's survivor count, **read, unrelated, 0 changed**.
- **Dev-tool versions:** none — no host tool installed, upgraded or read changed this chunk.
- **Harness / gate surface:** none changed. No `agent-run` script, xtask verb, CI step or status/verdict shape touched. Two NEW `[[gate]]` entries exist in this chunk's own `plan.md` (a corpus-count probe and an evidence host-path hygiene grep) — plan-local gates, not project harness surface.
- **Cross-project / external claims:** several, all against **`andromeda-pulse` at HEAD `83d4060`** (unmoved since the prior wrap), each read from the SUT repo on disk this session: `pulse-app/src/inference_runtime.rs:871` sets `EvidenceRefs.span_ids: Vec::new()` and is the sole non-test writer (every other writer sits inside a `#[cfg(test)]` module — `interpretation/src/markdown.rs:507` > cfg@376 · `triage/src/digest/assembler.rs:802` > cfg@165 · `digest/retrieval.rs:170` > cfg@150 · `incident/persistence.rs:359` > cfg@335 · `incident/registry.rs:400` > cfg@376); `pulse-app/src/incidents_router.rs:67` derives `evidence_count` from `.span_ids.len()`; `crates/mcp-server/src/tools.rs:430-435` maps it 1:1 into `span_refs`; `crates/interpretation/src/markdown.rs:221` pushes the literal `"## Previously Seen"`; `crates/triage/src/contract.rs` declares `#[serde(rename_all = "snake_case")]` on `CueKind` with its own test pinning `"service_went_silent"`; `interpretation/src/markdown.rs:361-364` renders severity lowercase; `pulse-app/src/deterministic_inference.rs` pins `"severity": "autonomous"` and `"title": "Deterministic verification incident"`; `mcp-server/src/tools.rs:378` computes `degraded_mode = parsed_l4.is_none()`.
- **Reverted / negative API facts:** the correction of `cross-incident-recurrence`'s token to `"Previously Seen"` was considered, put to the operator as a marked fork at phase P4, and **declined** — it would make the check reachable but not proven, since the run-twice recurrence path has never been measured firing and this chunk drives nothing. Nothing of it shipped.
- **Insufficient fixes (written, kept, not the remedy):** the four retirements themselves. They are correct, ratified and shipped, and they do **not** resolve the defect `v3-04` names — its universal. The chunk's own enumeration measured six further structurally-dead blocks the four do not touch. **Owner of the remainder:** the new working-route entry this wrap mints under Epoch 2 (P5), which takes the remaining five scenarios as a class.
- **Spec claims disproved by measurement:**
  1. **`architecture.md:135`** (offset 652) states "`findings-counter-refresh` is NOT a member: it carries one `[[expected]]`" — false after this chunk: it carries zero. Measured by the retirement itself. → *Expected amendment, carried below.*
  2. **`obs-plan.md:349`** (offset 1351) states "`findings-counter-refresh` is not declare-only (one `[[expected]]`) … its unmet `CountAtLeast` floor grading `CalibrationRegion`" — false on both halves after this chunk. → *Expected amendment, carried below.*
  3. **Not a master claim, recorded for the ledger:** the originating residual's attribution for `cross-incident-recurrence` — "no producer anywhere emits that token" (`residuals.md`, the `2026-09-10-live-pulse-in-lane-scenario-round` item) — is **disproved**. The token IS emitted (`interpretation/src/markdown.rs:221`) and DOES reach the graded text (`conductor-verify/src/extract.rs` pushes `retrieve_report` markdown into `Observation.text`); the check failed on CASE against a case-sensitive `Contains` (`conductor-verify/src/slo.rs`). `residuals.md` is not one of the seven masters and the matrix note (P7.3) carries the correction; no master edit is owed for it.
- **Expected amendments (from plan):**
  - `architecture.md` §Standard Contracts (Run report envelope) — **carried**; its fact is *Spec claims disproved* #1 and the *Counts moved* bullet. Direction: the declare-only registry gains the four retired scenarios and the parenthetical excluding `findings-counter-refresh` is struck.
  - `obs-plan.md` §4 (Known-residual classification path → Delegated-timing family) — **carried**; its fact is *Spec claims disproved* #2. Direction: the sentence is restated to current truth.
  - **SWEEP CORRECTION (written at P2, after the fan-out disproved this report's first draft).** The draft recorded `grep -n 'findings-counter-refresh' .andromeda/architecture.md` → 2 hits, disposing `:70` as "fingerprint derivation, unrelated". That disposition was made from a clipped view: `:70` is a **10,432-character** line, and the token sits at **offset 5712** inside it, in §Established Decisions [Read-Back Dependency Posture], stating the SAME retired claim in the present tense ("`findings-counter-refresh` carries one `[[expected]]` … its unmet `CountAtLeast` floor graded `CalibrationRegion`"). A third arch site exists that no `findings-counter-refresh` grep could reach — `:72` §Established Decisions [Run-History Persistence], "the P-036 fingerprint-recurrence (\"Previously seen\") check" — keyed on a different token. Both were found by the arch doc-agent's own claim-wording sweep. **Corrected site set: `architecture.md` `:70`, `:72`, `:135` (3 sites, all amended) · `obs-plan.md` `:349` (1 line carrying the claim TWICE — offset 66 the three-member declare-only roster, offset 1351 the exclusion clause; both amended).** Resolved by offset reads, per the host rule that a line-granular grep cannot disposition a clause inside a multi-KB line.
  - No `test-plan.md` amendment is owed: `grep -nE 'constellation-severity-live-wiring|findings-counter-refresh|pulse-run-contract|cross-incident-recurrence' .andromeda/test-plan.md` → **0 hits**, so its §1/§6 declare-only sites name none of the four.
- **Coverage of new surfaces:** none — this chunk adds no external surface, hot-path operation or UI element. It removes assertions from four declarative fixtures and updates their pins.
  - `scenarios/*.toml` (declare-only shape, 4 members) → validation `garde✓` (unchanged; `#[serde(default)]` admits the empty shape, pinned by `expected_defaults_to_empty_when_omitted` at `scenario.rs:980`) · instrumentation `n/a` · PII `n/a` · tests `unit✓` (six pins, both runners) · a11y `n/a` · tokens `n/a`
  - `evidence/assertion-class-enumeration.md` (committed evidence) → validation `n/a` · instrumentation `n/a` · PII `redacted✓` (repo-relative paths only; hygiene grep exit 1 / 0 hits) · tests `unit✓` (the gate's own probe) · a11y `n/a` · tokens `n/a`

## Deviations from intent

1. **A sixth pinning test was added**, where the plan enumerated five. `scenarios/pulse-run-contract.toml` (P-073) had **no** test pinning its declaration — research recorded the asymmetry but the plan did not list a consequence. Retiring it unpinned would leave the class's one member unguarded against a regression re-adding the block while the other three are pinned. *Justification:* `scenario.rs` is already on the modify-set and the plan's own Implementation notes name the asymmetry, so this is an in-scope completion rather than a scope breach.
2. **Header prose was corrected beyond "replace the P-ID comment immediately above the block".** Three files carried present-tense sentences asserting a check that no longer exists ("The asserted check is a count floor…"; "Calibration points: the count floor of 1 is…"). *Justification:* leaving them would keep a false statement in a committed artifact — the exact class this chunk exists to remove. Scoped to sentences describing the retired check; `name`, `p_ids`, `seed`, `slo_tier`, `jitter_ms` and every `[[phases]]` table are byte-identical.
3. **The enumeration surfaced six more structurally-dead blocks and this chunk did not retire them.** *Justification:* the class was ratified as the four (route entry · `v3-04` notes · the operator's P4 fork answer); widening to five more scenarios plus their pinning tests is a re-plan, not an in-flight expansion. Surfaced at implement P2, ruled by the operator at this wrap, and routed to a new Epoch 2 entry (P5).
4. **`v3-04` was claimed at phase P5 and is un-claimed at this wrap.** *Justification:* operator ruling — the requirement is right and the world is wrong. The universal is not re-worded; the cap returns to the pool and this chunk stands as an explicit PARTIAL advance under the contract's multi-chunk rule (it cites the cap in its plan Provenance).

## Decisions & corrections

- **Operator fork (phase P4):** member 4's disposition — retire all four (chosen), vs. fix the case and keep it `Hard`, vs. fix the case and soften to `CalibrationRegion`. Reason for the choice: this chunk drives nothing, so a corrected token ships reachable-but-unproven.
- **Operator ruling (this wrap):** do not refine `v3-04`'s acceptance; un-claim instead; write the premise correction as a `notes` entry; mint the follow-up entry inside Epoch 2; leave the two satisfiable-but-weak survivors alone.
- **Sweep hazard — a bare `[[expected]]` token over-counts.** `grep -l '\[\[expected\]\]' scenarios/*.toml` matches retirement COMMENTS ("DECLARE-ONLY — no `[[expected]]` blocks"), so it reported 11 non-declaring scenarios where the anchored `grep -l '^\[\[expected\]\]'` gives the true 25 (now 29). Caught and corrected during P3 research; the anchored form is the only sound one for this corpus.
- **Sweep hazard — a dictated coordinate for the uppercase `"ERROR"` finding did not reproduce.** The wrap directive placed "the four uppercase `ERROR` strings" inside `appender.rs`'s `#[cfg(test)]` module "opened at `:931`"; measured, that file's `#[cfg(test)]` markers are at `:2`, `:660`, `:683` (the four hits at `:1291`, `:1974`, `:2000`, `:2102` are indeed past `:683`, so inside a test module — the line number is wrong, the containment holds), and uppercase `"ERROR"` also occurs OUTSIDE `appender.rs` (`ingest/examples/inject_scrub_canaries.rs:40` · `security/src/scrubber.rs:208` · several in `viz/src/query.rs` · `xtask/src/main.rs:378` · `xtask/src/self_verify.rs:144`), so "the four in the tree" is not the tree-wide count. **The finding is unaffected:** the enumeration's stated basis for `Contains "ERROR"` being dead is that the composed text's severity field renders lowercase (`interpretation/src/markdown.rs:361-364`), never an absence of the literal.
- **The concretization trap, recorded for the pipeline:** a P5 acceptance concretization whose clauses are all downstream of an unmeasured assumption is weaker than the outcome it replaces, and the never-weaken review card cannot show that — the measurement that would falsify it is work the same chunk performs later.

## Outcome

**Acceptance criteria, re-asserted against the diff:**

| Criterion | Verdict |
|---|---|
| (tests) both runners green, five pinning tests updated in the SAME change | **MET** — 983 tests under `nextest --workspace --profile ci`, 330 under `cargo test -p conductor-core`; six pins updated in the same diff |
| (tests) every edited TOML exercised through the production validator | **MET** — all six pins call `Scenario::from_toml_str` on the committed file |
| (tests) no retired declaration re-expressed as a vacuous `Absent`/`Contains`; neither suite-shape test deleted | **MET** — both re-scoped to the family's new true shape, set-valued; `git diff` adds no `Absent`/`Contains` declaration |
| (unit) the four carry zero blocks; corpus measures 8 blocks across 7 scenarios | **MET as measured** — 8 and 7, matching the forecast exactly |
| (arch) `p_ids`/`slo_tier`/phases kept; no garde, `check_*`, `from_toml_str`, coverage-row or `UNBACKED_AUTO` edit | **MET** — diff greps return 0 for each |
| (arch) no report state, verdict value or envelope key minted | **MET** — no `ReportState`/`Verdict` edit in the diff |
| (obs) no new span name, span attribute or envelope field | **MET** — no obs surface in the diff |
| (layouts) no new bracket label, lamp glyph, `ReportState` or ANSI entry | **MET** — no render surface in the diff |
| (security) `cargo audit` + `cargo deny` exit 0 over an un-drifted committed `Cargo.lock` | **MET** — both exit 0; lockfile byte-unchanged at 562 packages |
| (security) committed enumeration carries zero absolute host paths / internal seam-crate struct names | **MET** — hygiene grep exit 1, 0 hits |
| (security) every retired scenario still declares its P-ID(s) | **MET** — P-079, P-045, P-073, P-036 all intact |
| (a11y) no file under `ui/test/a11y/` in the modify-set; no WCAG claim | **MET** — not in the diff; no WCAG claim made |
| `verification-matrix.json#v3-04` — the universal | **UNMET, and not re-worded.** Six blocks across five scenarios still declare checks that cannot pass. Routed: cap **un-claimed** at P7.3 (returns to the pool), premise correction written to `notes`, remainder owned by the new Epoch 2 route entry. |

**Gates** (the `plan.md` `[[gate]]` entries, in order — /implement's first-pass run, re-run at this wrap's light gate):

| `run` | Verdict |
|---|---|
| `cargo fmt --all -- --check` | exit 0 ✓ |
| `cargo clippy --workspace --all-targets -- -D warnings` | exit 0 ✓ |
| `cargo nextest run --workspace --profile ci` | exit 0 ✓ · 983 passed, 0 skipped |
| `cargo test -p conductor-core` | exit 0 ✓ · 316 + 6 + 8 + 0 |
| `cargo test --workspace --doc` | exit 0 ✓ |
| `cat scenarios/*.toml \| grep -c '^\[\[expected\]\]'` | exit 0 ✓ · `last line 8` held (P5 baseline: red — printed 12) |
| evidence host-path hygiene grep | exit 1 ✓ · `last line 0` held (P5 baseline: red — exit 2, dir absent) |
| `git -C "$CARGO_HOME/advisory-db" status --porcelain` | exit 0 ✓ · no output — the copy read is `$CARGO_HOME/advisory-db` (`D:\dev\rust\cargo`), not the `~/.cargo` sibling |
| `cargo audit` | exit 0 ✓ · 1246 advisories · 562 packages · 7 allowed warnings |
| `cargo deny check advisories bans licenses sources` | exit 0 ✓ · advisories/bans/licenses/sources ok |

No `defer` entry, none deferred under the source-delta rule (Rust delta present: `scenario.rs` + four fixtures Rust tests read). No `leg` entry. **Smoke:** skipped — no boot-path / UI-surface change; touchpoints are declarative config plus an inline `#[cfg(test)]` module, and the plan lists no `smoke`/`self-verify` entry.

**Outcome basis:** /implement's P4 report as given, in this session's conversation — not reconstructed from the evolve ledger. Plus the operator's wrap directive issued between implement and this report, which changed four things: `v3-04` is un-claimed rather than refined; a premise-correction `notes` entry is written; a follow-up route entry is minted inside Epoch 2; the two satisfiable-but-weak survivors are explicitly left alone. The directive also supplied independent verification of the surfaced claim, one coordinate of which did not reproduce (see *Decisions & corrections*).

**Process hygiene:** none started. /implement's P4 census recorded zero surviving processes, re-measured at this wrap against the host process list: no `cargo`/`conductor`/`rustc`/`pulse`/`msedgedriver`/`tauri`/`node` process alive, and no listener on 4317, 4444 or 4445.
