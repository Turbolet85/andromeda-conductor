# Report — 2026-09-22-interpretation-proven-live

**Chunk:** Interpretation proven live — known root cause injected through the emission path, top hypothesis asserted to identify it, graded against the stated rule
**Date:** 2026-09-23
**Commits:** none since `last_wrap` 2026-09-18T10:14:26Z (HEAD `74a0872`); this wrap's commit carries the chunk.

## Changes (structured — detectors read this)

- **Files:**
  - `contracts/pulse-run-contract.toml` — term `l4-real-model` added; `l4-deterministic` tagged `posture = "deterministic"`; header gains a PROVENANCE line (a Conductor posture term, 2026-09-22) and a POSTURE paragraph.
  - `contracts/scenario-audit-ledger.toml` — `[[over_tier]]` row `real-model-interpretation`; `root-span-error-scope`'s `weakness` string carries a dated in-place `[corrected 2026-09-22 …]` note ("every verifiable run" → every DETERMINISTIC-posture run).
  - `contracts/pulse-real-model-leg-posture.md` (reader-less) — 11 dated mechanism corrections (`grep -c '\[corrected 2026-09-22'` → 5, `'\[corrected 2026-09-23'` → 6) and 1 dated measurement record (`grep -c '\[measured 2026-09-23'` → 1). The grading rule, launch posture, window rule and census were NOT edited.
  - `scenarios/real-model-interpretation.toml` (new).
  - `crates/conductor-core/src/{run_contract,preconditions,scenario,lib,error,load_envelope}.rs`.
  - `crates/conductor-run/src/{canary,execute,preconditions,lib,testkit}.rs`; `tests/{composition_root,dispatch_wire,run_contract_pin}.rs`; new `tests/{real_model_live.rs,real_model_harvest.rs,real_model_common/mod.rs}`.
  - `crates/conductor-cli/src/{cli,main}.rs`, `src/commands/{preconditions,run}.rs`, `tests/cli_smoke.rs`.
  - `crates/conductor-timeline/src/convert.rs` (a `Scenario` literal only).
  - `scripts/agent-run.sh`, `scripts/agent-run.ps1`.
  - `conductor-0.3.0/chunks/2026-09-22-interpretation-proven-live/evidence/rm-capture.txt` (new — the drive's bounded, scrubbed capture; byte-identical copy of `runs/live-suite/rm-capture.txt`, `cmp` exit 0).
- **Symbols / APIs:**
  - `conductor-core` (pub, re-exported from `lib.rs`): `L4Posture` {`Deterministic` (Default), `RealModel`} — serde kebab-case `deterministic` / `real-model`, `id()`, `Display`. `CheckKind::ShellAbsence` (wire `shell-absence`), `CheckKind::is_shell()`, `CheckKind::id()`. `ContractTerm.posture: Option<L4Posture>` (serde default `None` = binds under every posture). `RunContract::evaluate_for(&declared, posture)` — `ShellDeclaration` unmet iff undeclared, `ShellAbsence` unmet iff declared; `evaluate(&declared)` delegates `Deterministic`, so its result and its callers are unchanged. `RunContract::observed_env()` yields both shell kinds' vars, each ONCE, first-occurrence order (still `[L4, MCP]` on the committed contract). `validate()` requires `env` on both shell kinds. `Preconditions::evaluate_for(&observation, posture)` — under `RealModel` the L4 handle must NOT be declared (statement `declared in this environment, but the real-model posture requires it absent or falsy: …`); `evaluate` delegates `Deterministic`, byte-identical statements. New pub fns `flag_declared_on_either_side(value)` (Conductor's `true`/`1` ∪ Pulse's `1`/`true`/`yes`, trimmed + ASCII-lowercased — transcribed from andromeda-pulse `deterministic_mode_enabled_for`, `pulse-app/src/deterministic_inference.rs:86-89` @ `83d4060`) and `handle_declared_for(name, value, posture)` (differs from `handle_declared` only for the L4 handle under `RealModel`). `Scenario.l4_posture: L4Posture` (`#[serde(default)]`, `#[garde(skip)]` on a closed unit enum).
  - `conductor-run` (pub): `preflight_for(manifest_path, posture)` — `preflight(manifest_path)` delegates `Deterministic`; `observe_preconditions_for(posture)` — `observe_preconditions()` delegates `Deterministic`. `Preflight.posture` (crate-private field). `execute_scenario` returns a scenario-level `Blocked` record BEFORE the ready check when `scenario.l4_posture != pf.posture`, logging the message-borne line `scenario blocked: declared L4 posture {a} is not the posture this run's gate evaluated ({b})` — never a sixth gate precondition. `readiness()` (the `boot` verb) stays deterministic. `observe_run_contract(contract, posture)` (crate-private) reads a `shell-absence` term's var under the union rule.
  - Callers that KEEP the deterministic entry points (graph basis: research §Graph impact, 32-row query): `conductor-cli commands/suite.rs:26` (`preflight`), `conductor-tauri commands.rs:323` `run_thread` (`preflight`), `conductor-cli/tests/cross_surface_parity.rs:99`, `conductor-run drive.rs:80` — so a real-model scenario reached from the suite or the GUI lands `Blocked` via the mismatch check.
  - `conductor-cli`: `conductor preconditions --for <SCENARIO>` — a scenario NAME; a `P-NNN`-shaped value is refused at argument parsing (clap usage error, exit 2); without the flag the probe is unchanged. `conductor run` calls `preflight_for(…, scenario.l4_posture)`.
  - No new env handle: `ANDROMEDA_PULSE_L4_DETERMINISTIC` is now ALSO read for absence under the real-model posture (read, never written).
- **Crates / modules:** none added or removed. New test targets: `conductor-run --test real_model_harvest` (default suite, 39 tests) and `--test real_model_live` (`live-pulse` feature; one printing test + the `#[ignore]`d `rule_record`); shared test module `crates/conductor-run/tests/real_model_common/` (`rule_section`, `mask_host_paths`).
- **Dependencies:** none — `git status --short Cargo.lock` → empty; the `live-pulse` feature pre-existed and declares no dependency.
- **Schema / config:** scenario key `l4_posture` (closed `deterministic` | `real-model`; absent → deterministic; unknown → load-time `CoreError::Config`). Run-contract term keys `posture` (optional) and `check = "shell-absence"`; new term `l4-real-model` (`shell-absence` on `ANDROMEDA_PULSE_L4_DETERMINISTIC`, `posture = "real-model"`). Audit ledger `[[over_tier]]` row. No envelope key, verdict word, report state, span name or span attribute added.
- **Spec-master edits:** none (the seven `.andromeda/` masters are untouched; the posture doc is a reader-less `contracts/` member).
- **Counts / qualifiers moved:**
  - committed scenarios 36 → 37 (`ls scenarios/*.toml | wc -l` → 37); the declare-only complement is "2 of 37" (`grep -l '^\[\[expected\]\]' scenarios/*.toml | wc -l` → 2). Master stating the old value: `grep -n '2 of 36' .andromeda/*.md` → 1 hit, `architecture.md:135`.
  - audit ledger `[[over_tier]]` rows 11 → 12 (`grep -c '^\[\[over_tier\]\]'` @HEAD 11, working tree 12). No master states the row count (`over_tier` sweep: 1 hit, `architecture.md:179`, names the array only).
  - run-contract `CheckKind` set 3 → 4 kinds (`shell-declaration` · `shell-absence` · `asserted` · `declared-not-observable`); terms 6 → 7 (`grep -c '^\[\[term\]\]'` → 7); shell terms 2 declaration + 1 absence, observed handles still 2. Master stating the old qualifier: `grep -n 'ONE OF TWO' .andromeda/*.md` → 1 hit, `architecture.md:198`.
  - scenarios naming `P-018`: 1 → 2 (`fingerprint-storm`, `real-model-interpretation`).
  - workspace nextest 1053 → 1059 (gate log summaries; no master states this count).
  - `UNBACKED_AUTO` (8) and `coverage-matrix.md` byte-unchanged — verified (coverage_gate green).
- **Dev-tool versions:** none — no host tool installed or upgraded (SUT binaries used as built 2026-09-01: `pulse-app.exe`, `andromeda-pulse-mcp.exe`; `llama-cli` b9305 CUDA; model Llama-3.2-3B-Instruct-Q4_K_M — SUT-side, not Conductor tools).
- **Harness / gate surface:** `scripts/agent-run.{sh,ps1} run --live [real-model]` — the THIRD token selects the real-model leg (`live_real_model_leg` / `Invoke-LiveRealModelLeg`): (a) `conductor preconditions --for real-model-interpretation`, refusing at exit 1; (b) pre-build of the capture binary; (c) non-recursive removal of `rm.jsonl`, `rm-capture.txt`, `rm-capture.err` under `runs/live-suite/`; (c′) the `rule_record` test into `rm-capture.txt` BEFORE the leg, a non-zero exit stopping the arm; (d) `live_leg rm real-model-interpretation` (budget = preflight budget + 136 + 60 s); (e) the capture `cargo test -q -p conductor-run --features live-pulse --test real_model_live -- --nocapture`, stdout appended, stderr to `rm-capture.err`, `RUST_LOG` removed for the test invocations; (f) printed reminders (bootstrap default, stop form, census). An unknown selector prints `usage: <script> run --live [real-model]` and exits 2 before any probe, in both shells. The `.ps1` binds `$args[2]` by count, saves/removes/restores `RUST_LOG` in a `finally`, and writes BOM-less UTF-8 through .NET. Five commands, the deterministic `--live` suite and the other stage flags are unchanged. Both shells' `real-model formation (~110s)` comment now states its deterministic-L4 provenance. New gitignored artifacts `runs/live-suite/{rm.jsonl,rm-capture.txt,rm-capture.err}`.
- **Cross-project / external claims:** andromeda-pulse @ HEAD `83d4060` (`git rev-list --count 83d4060..HEAD` → 0; tracked tree clean but for Pulse's own bookkeeping), read this chunk: `pulse-app/src/deterministic_inference.rs:86-89` (truthy set `1`/`true`/`yes`); `pulse-app/src/inference_runtime.rs` `grounded_fingerprint_hashes` (an incident's `fingerprint_refs` = the model's refs ∪ the triggering cue's full-hex fingerprint) and `create_incident_from_l4_output`'s creation predicate (`is_resolution_summary` / `Decision::Dismiss` / `L4Severity::None` return with NO log line); `crates/triage/src/pattern/storm.rs` `synthesize_cue` (cue fingerprint full-width lowercase hex); `crates/mcp-server/src/tools.rs` (`query_incident_list` items carry `incident_id`/`status`/`severity`/`title`/`opened_at_unix_nano`, no fingerprint; `retrieve_telemetry_slice` returns `fingerprint_refs`; `retrieve_report` loads by id with no active filter); `crates/mcp-server/src/bin/andromeda-pulse-mcp.rs` (a tool error's JSON-RPC message is `tool dispatch failed for \`{tool}\`: {reason}`); `pulse-app/src/main.rs` (`interpretation.model.load` `inference_mode`, `app.boot.workspace_key` `workspace_root_basename`); `pulse-app/src/heartbeat.rs` (15 s tick targets). The live drive ran 2026-09-23 against the release `pulse-app.exe`; no CI run was read by this chunk's gates.
- **Reverted / negative API facts:** none.
- **Insufficient fixes (written, kept, not the remedy):** none.
- **Spec claims disproved by measurement:**
  1. "a single-storm leg has no hypothesis observable at all" — `contracts/pulse-real-model-leg-posture.md` §The emission profile (DISPOSED here: dated correction) and `architecture.md:182` ("the emission profile a hypothesis observable requires") — false at `83d4060`: `inference_runtime.rs:884` attaches the parsed interpretation at creation (commit `b2e4cb3`); leg b1 (2026-09-06) read a single-storm canary incident non-degraded.
  2. "~110 s real-model formation" — posture doc provenance + §Disposition + §The quiet window (DISPOSED: dated corrections), both `agent-run` comments (DISPOSED: corrected), `architecture.md:182` (1 hit of the `~110 s` pattern), `.claude/rules/verification-harness.md:60` (c) — the figure is a deterministic-L4 measurement (`crates/conductor-run/tests/lifecycle_live.rs:20` under `ANDROMEDA_PULSE_L4_DETERMINISTIC=true`; `conductor-0.2.0/chunks/2026-08-31-p-075-assert-round/report.md:161`), re-attributed at `conductor-0.2.0/chunks/2026-09-06-operator-gated-live-suite/plan.md:102`. No real-model formation or pickup figure exists — this chunk's drive measured none (below).
  3. "The assertion is a substring comparison against the composed observation text" — posture doc §The grading rule (DISPOSED: dated correction) — false as a mechanism: no artifact persists the report text (`extract.rs:99` logs key names only) and the composed text unions every active incident (`extract.rs:90-103`).
  4. "a null verdict and a known-residual state" for the success row — posture doc §Disposition (DISPOSED: dated correction) — a non-degraded declare-only read-back lands `ManualCheck` (`state_for`, `crates/conductor-run/src/execute.rs`).
  5. `verification-matrix.json#v3-09` 2026-09-18 note claim (2), "`Contains` … expresses it" — false by (3) → ledger note, owner P7.3.
  6. Plan B3 cites `crates/conductor-verify/src/preflight.rs:202-205` as the attribute-by-fingerprint precedent — that block is a STALE COMMENT: `assert_canary` attributes by incident FRESHNESS. Not a master claim; the stale comment is in `conductor-verify` source, outside this chunk's scope → routed forward (code comment, not a spec).
  7. Plan step 6 premise "the probe loads [the run contract] relative to cwd (`conductor-run/src/preconditions.rs:21-25`)" — false: `observe_preconditions*` never loads it (only `canary_gate` does). DISPOSED: the cli_smoke arm omits the copy (Deviation 3).
- **Expected amendments (from plan):**
  - `architecture.md` §Standard Contracts — readiness gate posture-aware (scenario-declared posture selects the run-contract terms + the probe's L4 grading; mismatch → scenario-level Blocked) — CARRIED (Symbols / APIs). Run-report-envelope passage `architecture.md:135`: "2 of 36" → "2 of 37" (`grep -n '2 of 36'` → 1 hit, arch:135) — CARRIED (Counts); the new scenario is declare-only with a `ManualCheck` success row, not a tenth family — CARRIED (Symbols / APIs + Spec claims 4); the passage's count basis to the escaped `'^\[\[expected\]\]'` form — CARRIED (Counts basis).
  - `architecture.md` §Occupied Resources: the `pulse-run-contract.toml` row (`architecture.md:178`, `shell-declaration` sweep 3 hits there) — CARRIED (Schema / config); the L4 row `:198` "ONE OF TWO shell-declaration proxies" (`grep -n 'ONE OF TWO'` → 1 hit) — CARRIED (Counts); the posture row `:182` (`grep -n 'pulse-real-model-leg-posture' .andromeda/*.md` → 1 hit) — its "emission profile a hypothesis observable requires" (Spec claims 1) + the ~110 s provenance (Spec claims 2) + the measured pickup figure: NONE (Outcome) — CARRIED; the `runs/live-suite/` row `:171` (`grep -n 'runs/live-suite'` → arch:171, test-plan:155) — CARRIED (Harness).
  - `architecture.md` §Established Decisions [Read-Back Dependency Posture] — the creation-time attach + the ~110 s re-attribution — CARRIED (Spec claims 1, 2); locate by the section header (the `110` pattern hits arch:182 only).
  - `architecture.md` §Established Decisions [Probabilistic-Assertion Policy] (`grep -n 'Probabilistic-Assertion'` → arch:70, arch:71) — D3: a match against a rule stated before the drive may `Pass`, a miss is `CalibrationRegion`, never `Fail` — CARRIED: the harvest's `row()` maps `Identified → (Pass, Pass)`, `NotIdentified → (CalibrationRegion, ManualCheck)`; `conductor-verify verdict.rs classify()` is unchanged (it routes CalibrationRegion claims; the harvest's table is test-tier, not `classify`).
  - `security-plan.md` §Input Validation — scenario-config row (`l4_posture`), READ SET row (`security-plan.md:114`, `grep -n 'READ SET'` → 1 hit; the L4 handle read for absence), CLI-arguments row (`preconditions --for`, scenario name only), committed-manifests row (`security-plan.md:113`; the new kind + tag), a model-output-ingest row for the harvest literal — CARRIED (Symbols / Schema / Coverage).
  - `security-plan.md` §Security Anti-Patterns → Data Protection (`grep -n 'Data Protection'` → security-plan:133, :330) — the ratified disposition for committing corpus-rendered model text (operator decision P4, "raised at wrap for ratification") — CARRIED as a REQUIRED ESCALATION (ratification); note this drive's committed capture holds NO corpus-rendered model text (no report was read — Outcome).
  - `test-plan.md` §3 — `run --live real-model` and `preconditions --for` (`--live` sweep: test-plan:155; `preconditions` sweep: test-plan:53, :155) — CARRIED (Harness).
  - `test-plan.md` §6 — the real-model harvest leg — CARRIED (Crates / modules).
  - `test-plan.md` §9 — the real-model live leg (`--live` sweep: test-plan:465): its pickup never budgeted; real-model formation AND pickup still unmeasured (the drive measured none) — CARRIED (Outcome).
  - `obs-plan.md` §4 — the family recorded in the existing path's detail block (`grep -nE 'declare-only' .andromeda/obs-plan.md` → §4 detail blocks at :325, :334, :349) — CARRIED (Symbols: no new span/attribute; the leg reuses Critical Path 1's chain).
  - `obs-plan.md` §5/§10 — the pickup figure as a measurement, named pickup never formation (`formation` sweep: 0 hits in obs-plan) — CARRIED as "no figure measured" (Outcome); verify at wrap.
  - `layout-templates.md` §cli Primary screens — the `agent-run` bullet (`layout-templates.md:190`) and the `preconditions` bullet (`:187`) — CARRIED (Harness / Symbols).
  - `matrix#v3-09 notes` — the drive's graded outcome, the (absent) pickup figure, the attempt ledger, and the 2026-09-18 note's (2) correction — `ledger-note — owner P7.3`, together with the D1 un-claim + deferral.
- **Coverage of new surfaces:**
  - `Scenario.l4_posture` → validation garde-skip on a closed unit enum (the parse is the validation; unknown → `CoreError::Config`)✓ · instrumentation n/a · PII n/a · tests unit✓ · a11y n/a · tokens n/a
  - `conductor preconditions --for` → validation clap `value_parser` (P-ID refused, exit 2) + the validated scenario selection✓ · instrumentation the existing probe log line✓ · PII n/a · tests e2e (assert_cmd ×3)✓ · a11y n/a · tokens n/a (existing `[PRECONDITION]` label)
  - `execute_scenario` posture-mismatch Blocked → validation✓ · instrumentation message-borne info line✓ · PII n/a · tests unit✓ · a11y n/a · tokens n/a
  - `agent-run run --live real-model` → validation selector allowlist (exit 2)✓ · instrumentation the leg's self-obs frozen to `rm.jsonl`✓ · PII capture scrubbed (`redact_value` + `mask_host_paths`, envelope `fingerprints` elided)✓ · tests both shells' bogus-selector probes + the live drive✓ · a11y n/a · tokens n/a
  - `real_model_live` capture (MCP re-read of model text + Pulse's log) → validation bounded (600 s poll, 64-id sweep, fields-only Pulse witnesses)✓ · instrumentation n/a (test) · PII model text scrubbed before print, basename never printed✓ · tests no-Pulse dry run + the live capture✓ · a11y n/a · tokens n/a

## Deviations from intent
1. **B1's union truthiness rule lives in core** (`flag_declared_on_either_side`, `handle_declared_for`). Step 1's "never a second truthiness rule" was overridden by the binding fold's B1 (counts Pulse's `yes`); the union delegates to `flag_declared` and transcribes Pulse's rule, so the two cannot drift and nothing a `shell-declaration` term or a probe flag reads is widened.
2. **B3 attribution needed a fingerprint source and an id source the plan did not name.** The capture transcribes the dispatcher's private `base_exception()` (`dispatch.rs` out of scope) and a default-suite guard (`the_capture_computes_the_dispatcher_s_own_exception`) holds both copies' literals equal. Resolved incidents are found by a bounded id sweep read by id (no MCP list tool returns them). Attribution = carries the scenario's cue fingerprint AND opened at/after the emission instant.
3. **The cli_smoke `--for` arm omits the run-contract copy** — step 6's premise (the probe loads it) is false.
4. **The capture buffers its output and writes it once from a drop guard.** A pre-drive no-Pulse dry run showed libtest's `-q` mark for the ignored `rule_record` (`i`) prefixing the capture's first line under B5's fixed command; it could have split a grammar line mid-run.
5. **`scenario.rs` path-qualifies `crate::L4Posture`** so the posture doc's `scenario.rs:52-72` citation stays line-true.
6. **Step 13's launch:** the operator directed the agent to start `pulse-app` ("Start it on your own"); done via a scripted `Start-Process` with the posture's env, confirmed from Pulse's own log before firing.
7. **The status smoke (`agent-run.sh status <id>`) was run by hand**: `gate.py` refuses a `leg = 'operator'` entry even with `--id`, contrary to the implement skill's text (pipeline drift, recorded in evolve).
8. **Step 14 on this route:** the pinned literal is the capture block (one literal, asserted EQUAL to its section of the committed capture); the "Evidence carries no `det-`" check is vacuous here (no report was read), so the binding B1 witnesses are `inference_mode: real` and the envelope's zero `det-` refs. B4's ref test was NOT written (the outcome is not Identified).

## Decisions & corrections
- Operator: "Start it on your own" — the agent may launch `pulse-app` for a live leg when directed (saved as a memory, per-leg approval still required).
- B2 classification of the drive's Blocked: MODEL-SIDE. Pulse's `digest.runtime.cadence_tick {cue_present: true, mode: tier1}` + `digest.assemble.request {cue_kind: retry_storm, cue_priority_tier: autonomous}` at 07:40:25Z, then `interpretation.inference.request {result: success, duration_ms: 4379, model_tier: primary}` and parse `ok`, and NO `interpretation.incident.created` — the creation predicate's silent exits are all model output fields. Every other digest in the window was a cue-less tier-3 baseline; from 07:42:41Z the generation damper engaged (`unchanged_digest`).
- D1 applied: recorded, never re-driven; `v3-09` un-claimed and deferred at P7 with the measurement in `notes`.
- Sweep/tooling hazards met this chunk: (a) the Bash transport collapsed a doubled backslash-n inside a quoted-heredoc Python string, planting real newlines in two Rust string literals (fixed with the Edit tool); (b) `grep '\\'` inside the Bash tool dies with "Trailing backslash"; (c) a `cd` inside a compound command moved the tool's working directory for the NEXT call (the host rule's persistence clause, recurred); (d) Pulse logs NO line for a Dismiss / severity-None decision — a parse `ok` with no `incident.created` is the only witness, and `digest.runtime.cadence_tick`'s `cue_present` is what separates a cue-bearing digest from a baseline one.
- Observation (out of scope, master-owned): `test-plan.md:44` and `:309` still call `corpus.db` "encrypted (P-049)" while `.claude/rules/security.md` records it plaintext since 2026-06-27.
- Observation: `conductor run P-018` / `SCENARIO=P-018` now resolve in unsorted `read_dir` order between two scenarios; `--for` refuses P-IDs but `run` does not.
- Observation: `crates/conductor-run/tests/storm_harvest.rs`'s header still says `fingerprint_refs` is pinned to `[]`; Pulse's grounded union now carries the cue's real fingerprint there.

## Outcome
**Acceptance criteria, re-asserted against the diff:**
- (arch) closed sets; a miss `CalibrationRegion → ManualCheck`, never `Fail`/`Err`; no new `Verdict`/`ReportState` variant, envelope key or `slo_tier` — MET (the diff adds none; harvest `row()`).
- (arch) `check_scenario_backing` / `check_sut_drift` / `check_load_envelope` (`[[exempt]]` empty) / `check_scenario_audit` (new row) / coverage byte-compare green; `UNBACKED_AUTO` + `coverage-matrix.md` byte-unchanged — MET.
- (arch) posture is scenario data; every pre-existing scenario loads `Deterministic`; `preflight()` / `observe_preconditions()` / `RunContract::evaluate` keep exact results; a real-model scenario under a deterministic gate lands `Blocked` — MET (unit tests).
- (arch) pickup figure from Conductor's own `std::time` emission instant; no outbound target or listener added — mechanism MET, figure NOT MEASURED (the scenario never emitted); no listener/target added — MET.
- (security) real-model L4 check declaration-only over Conductor's own env, naming the handle only; nothing writes/unsets an `ANDROMEDA_PULSE_*` handle; a deterministic run with L4 undeclared is still refused — MET (with Deviation 1's union).
- (security) `l4_posture` closed enum, unknown → load fault; no `#[garde(skip)]` on a nested struct field — MET.
- (security) failed read-back / degraded / empty body / unattributable → `Blocked`, each with a synthetic arm — MET (arms 3, 4, 10, 11, 12).
- (security) committed capture + harvest carry zero host paths and no data-dir value; audit + deny green over an unchanged `Cargo.lock` — MET (three hygiene entries → 0).
- (tests) nextest, doctests, both clippy entries, fmt, `cargo test -p` ×3 → 0 — MET.
- (tests) the harvest maps captures onto the four rows with every negative, extraction, token-surface and mask arm — MET (39/39).
- (tests) the drive: operator-gated, envelope-neutral, fired once through `bash scripts/agent-run.sh run --live real-model`, exit 0, null verdict, no `CheckRecord`, no CI step — MET (row `Blocked`).
- (tests) never re-driven; only a Conductor-side environment fault may be re-fired — MET (one drive, zero re-fires).
- (obs) harvest-pinned envelope: exactly the eleven keys + the closed sets — MET. The pinned `trace:` names the must-trace chain + the `retrieve_report` witness + ≥1 wire-shape witness — UNMET ON THIS ROUTE BY THE OUTCOME (not by the diff): the leg was `PreflightBlocked`, so no `timeline.execute` or `retrieve_report` line exists; the per-route witness set (step 11's `route`, built so no outcome forces a test edit) holds (`wire_shape_lines=15` from the canary). Owner: the D1 deferral of `v3-09`.
- (obs) the pickup figure recorded as pickup, never formation or a budget — MET vacuously: "no pickup figure" recorded (posture provenance; this report).
- (design) only existing lamps: the leg row `[BLOCKED]`; the graded outcome `NoAttributableIncident → [BLOCKED]`; no `[FAIL]`, no new label/ANSI/token — MET.
- (layouts) five commands; `real-model` a selector of `--live`; unknown selector exit 2 in both shells; `preconditions --for` takes a scenario name and refuses at exit 1 naming each unmet subject — MET.
- (a11y) the driven-a11y leg untouched; the real-model arm never runs `a11y:driven`; `[BLOCKED]` is an existing lamp label — MET.
- (matrix) `v3-09` met iff the pinned outcome is `Identified` AND the real-model witnesses hold — NOT MET: `NoAttributableIncident` (model-side Blocked) → D1: un-claim + defer at P7.3 with the measurement in `notes`; no ref was written (B4).

**The drive (one graded attempt, 0 environment re-fires):** `pulse-app` launched 07:38:40Z (PID 53540) under the posture — Pulse's own log: `inference_mode: real`, primary tier loaded, workspace basename clear, 0 bootstrap-override lines, gRPC receiver bound `127.0.0.1:4317`. Probe green. Leg 07:39:38Z → 07:50:32Z, exit 0, `[BLOCKED] real-model-interpretation`, 1268 self-obs lines frozen, `run_id 2026-09-23T07-39-39-845`. The canary storm reached the Autonomous band at 07:40:24Z (`fingerprint_hex 8cb9c5d5` = the canary's computed fingerprint); the real model answered its one cue-bearing digest and did not surface it; the preflight polled 600 s and blocked with the workspace-key/no-incident precondition; the scenario never emitted. The rule graded the pinned capture `NoAttributableIncident → (None, Blocked)`, route `PreflightBlocked`. Pickup figure: none. Real-model formation: still unmeasured.

**Gates (final implement run, by `run` text):**
- `git -C "$CARGO_HOME/advisory-db" status --porcelain` — green (exit 0, no output)
- `cargo audit` — green (exit 0; 1264 advisories · 562 crates · 7 allowed warnings)
- `cargo deny check advisories bans licenses sources` — green (advisories/bans/licenses/sources ok)
- `cargo fmt --check` — green
- `cargo clippy --workspace --all-targets -- -D warnings` — green
- `cargo clippy -p conductor-run --features live-pulse --all-targets -- -D warnings` — green (red once on `double_ended_iterator_last`, fixed)
- `cargo nextest run --workspace --profile ci` — green (1059/1059)
- `cargo test --workspace --doc` — green
- `cargo test -p conductor-core` / `-p conductor-run` / `-p conductor-cli` — green
- `cargo nextest run -p conductor-core --test scenario_audit_gate --profile ci` — green (7)
- `cargo nextest run -p conductor-report --test coverage_gate --profile ci` — green (3)
- `cargo nextest run -p conductor-run --test real_model_harvest --profile ci` — green (39)
- posture-doc host-path `grep -cE` — green (exit 1, last line 0)
- harvest host-path `grep -cE` — green (exit 1, last line 0)
- `bash scripts/agent-run.sh run --live bogus` — green (exit 2, `usage: scripts/agent-run.sh run --live [real-model]`)
- `powershell.exe … scripts/agent-run.ps1 run --live bogus` — green (exit 2, `usage: .\agent-run.ps1 run --live [real-model]`)
- `… preconditions --for real-model-interpretation` — green under the live env (exit 0, `[PRECONDITION] every live-Pulse precondition is satisfied`); `not run — env … unset` in later no-env runs
- census `tasklist … grep` (before) — recorded (`pulse-app.exe` 53540 only)
- the live leg `… bash scripts/agent-run.sh run --live real-model` — `leg = 'live'`, fired by the agent once: exit 0; both atoms held (`[live] leg rm: real-model-interpretation`, `[live] leg rm: froze`); artifact `runs/live-suite/rm-capture.txt` fresh (mtime 07:50:31Z > leg start 07:39:38Z)
- census `tasklist … grep` (after) — recorded (`pulse-app.exe` 53540 only)
- evidence `grep -cE … evidence/rm-capture.txt` — green (exit 1, last line 0)
- `bash scripts/agent-run.sh status <id>` — `leg = 'operator'`, run by hand with `2026-09-23T07-39-39-845`: exit 0, `"scenario": "real-model-interpretation"` present
- smoke: recorded from P2 — the live leg booted the changed CLI entry points end to end; the status smoke read its envelope.

**Outcome basis:** implement's P4 report in this session's conversation + the post-implement artifacts `runs/live-suite/{rm.jsonl,rm-capture.txt}` and the committed `evidence/rm-capture.txt`.

**Process hygiene (re-measured at implement's end with `Win32_Process`, pattern `conductor|andromeda-pulse-mcp|pulse-app|real_model_live-*|llama-cli`):**

| process | started by | final state |
|---|---|---|
| `pulse-app.exe` (PID 53540, created 07:38:40Z) | the agent, at the operator's direction | left running — the operator stops it |
| `conductor.exe` (the leg) · `andromeda-pulse-mcp.exe` (the leg's and the capture's sidecars) · `real_model_live-*.exe` (rule record + capture) | the live leg / the harness arm | terminated — absent from the census after (census after = census before) |
| `llama-cli.exe` (per inference) | `pulse-app` | not running at the census after |
