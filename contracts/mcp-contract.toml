# Pinned MCP read-back contract (arch §Standard Contracts).
#
# The preflight readiness gate asserts the negotiated protocol version + required-tool presence
# against these pinned values; any mismatch is a distinct `Blocked` state with a named precondition,
# never a silent downgrade. `2024-11-05` is Pulse's hand-rolled server version (the rmcp client
# negotiates DOWN to it). Do not bump without a contract review.

expected_protocol_version = "2024-11-05"

required_tools = [
    "query_incident_list",
    "retrieve_report",
    "retrieve_telemetry_slice",
    "mark_incident_resolved",
]
